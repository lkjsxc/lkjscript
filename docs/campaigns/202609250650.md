# Deliver native development and explicit resident quotas

## Original user mandate (unchanged)

> @Remote Desktop Commander  https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscript  について進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。限界まで深く考えてほしい。超長期的な視点からお願いしたい。ChatGPTの開発環境として、cw-bd235945fff44468a0f41292を用意しました。自由に活用ください。remote desktop commanderについて、月ごとにtool callingが1万回までという制限を考慮しつつお願いします。

## Reconciliation and selected milestone

Started 2026-09-25 06:50 Asia/Tokyo on authorized device
`cw-bd235945fff44468a0f41292`, in /home/coder/workspace/lkjscript.
At entry, local main 8242f3a7 had four unintegrated commits and its inherited
fresh full check was still active. Preserve its inputs; do not launch a duplicate.
The existing process subsequently exited zero with 26 fresh passes, zero reuse
and 740.134247095 seconds. Normal fast-forward push integrated that exact source;
an independent GitHub ref read confirmed 8242f3a71e71eab49c70857f76716c8cc30a72dd.
The earlier failed attempts and corrupt-debug-output originals remain retained.

No other worktree or stash was observed. Preserve unrelated untracked
202609242022.md. Root AGENTS.md remains appropriate. One combined diagnostic
read was blocked before execution; it was not retried. The inherited job's
ordinary terminal-output observation supplied its completed acceptance result.

Select v0.1.45 for the already integrated native guide/policy tooling, bytes-get,
ordinary standard text-join, contributor process repairs, and optional resident
cumulative quotas. Native application development and native tool adoption are
real capabilities; neither implies compiler self-hosting or hostile-code isolation.
This is a delivery milestone, not a wholesale kernel rewrite or a new web framework.
Public v0.1.44 and its immutable assets remain unchanged until a successor passes.
The observed tags/releases have no v0.1.45; no release job was active.

## Execution contract

Update only the current product version, its exact lockfile package and generated
metadata through their owners. Explain backward-compatible old descriptors, explicit
null policy, the Rust RunPolicy source-API change, retained exact suppliers, and
predecessor rejection of bytes-get/new descriptor fields. Preserve data/artifact
formats and old public inputs. Prepare useful release notes, not a hash inventory.

Verify changed preparation inputs locally; integrate normally and select exactly one
mainline candidate through the maintained Release workflow. Fresh hosted source and
finalized-target acceptance belong to that versioned source, not the predecessor
full receipt. After candidate acceptance, use the authorized ordinary annotated-tag,
scoped release selection and promotion path under docs/release.md. Change no access
control, credential, protection or immutable publication history. Confirm exact and
latest public verification, and report actual stage outcomes rather than an uploaded
artifact or green subjob as publication.

Use batched RDC operations and inspect jobs at meaningful work boundaries. Do useful
independent review while builds run; do not poll unchanged logs or start duplicate
broad suites. Any unresolved stage must retain its exact source/run/attempt and
next action. Subsequent reporting cannot relabel tested source.

## Version preparation

The package and exact lockfile entry now select 0.1.45, without dependency
changes. The public product build is running on the pinned toolchain. A bulk
release-note/readme update was blocked before execution; the script was not
rerun. The dedicated text writer subsequently created the shorter release notes.
The semantic CLI summary now distinguishes old bounded descriptors, fresh HTTP
explicit-null policies and foreground profile reporting. Frozen public-v0.1.44
walkthrough observations remain historical rather than being relabeled.

The copied 0.1.45 release-mode product regenerates and verifies all eight public
guides, discovers text-join and bytes-get, and completes fresh command-project
creation, check, build and run. The first run omitted its required target and
correctly exited 2; the corrected invocation names the observed main target.
The original failure is retained, not relabeled. Locked metadata, rustfmt and diff
checks pass. Generated changes are version/capabilities headers, not graph or
dependency bytes. Originals and the copied local product are under
.artifacts/202609250650-release/. This focused version-preparation evidence
does not replace finalized-target acceptance or establish public publication.

## Independent onboarding and information architecture

Candidate 36064706830/1 started at exact mainline source
4d64abc70f88dcfd6d31b0e800f23ca8bfb1459d on 2026-09-24T21:58:06Z.
It is still a non-publishing candidate, not accepted or publicly available. The
following documentation work is a descendant, not a mutation of its source.

README, status and roadmap had 1,307 lines / 80,597 bytes of mixed current facts
and repeated chronology. They now have 404 lines / 25,170 bytes. Preserve real
failed history and measurements at the existing linked campaign/release owners;
do not invent a second history archive. Keep the original README section anchors.
Correct the old blanket no-container claim: release userland admission uses Docker.

The former README's literal square program now has an explicit example owner and
a first-command guide. The guide index also discovers the existing text and HTML
HTTP guides. The same 16-command workflow passes on anonymously acquired public
v0.1.44 and the retained local v0.1.45 product: create, test, hello via project and
deployment, native plan/apply, square=144, canonical unchanged draft, then detached
execution with authoring paths moved away. Runtime directories contain exactly
executable, bundle and descriptor. Public checks report 46 recipe / 47 authored
tests; development checks report 64 / 65. Child environments have no credentials
and an empty PATH. Source inputs are ordinary native proposals, not graph edits.

The public archive matches the already independently observed immutable release
asset and passes native installation in an owned prefix without selecting a user
runtime. Installation's neutral/unverified-publication label is not relabeled as
an attestation. Original downloads, per-command status/output and retained roots
are in .artifacts/202609250650-release/onboarding/. Relative link/anchor checks
pass for 88 current overview/guide links. This is documentation-workflow
proof, not finalized-musl acceptance or a new application benchmark.

The first product-surface audit used the retained predecessor verifier and
rejected 27 executable identity observations: it expects its embedded 0.1.44
product metadata, whereas the supplied executable is 0.1.45. This is failed audit
evidence, not a successful documentation check. no-python was not run because the
compound command stopped at that failure. Preserve product-surface.json and build
the verifier from the versioned source before a fresh labeled audit.

The surface owner confirms its expected product/capability prefixes are compiled
from lkjscript::PRODUCT_VERSION. The version-matched verifier passes both required
product-surface and no-python audits; the mismatched original remains failed.
Every previous README H2/H3 heading remains present (11), and source, tooling,
package, application, workflow and Cargo inputs remain identical to candidate
4d64abc7. This is a documentation/reporting descendant, not a candidate rebuild.
