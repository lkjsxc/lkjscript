# Cancellation-safe resident lifecycle and withheld release

## Original user mandate

https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscript  について進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。最も筋の良い方向に進むために、限界まで深く考えてほしい。超長期的な視点からお願いしたい。どれだけ時間がかかっても構いません。開発環境として、cw-bd235945fff44468a0f41292という名前で、lxcのコンテナを用意しました、自由に活用ください。安全性チェックも気にかけつつ、なんとか進んでほしい。

## Reconciliation and decision

Initial local and independently read remote main are
`64c0334ddee01c01aaf5c6e7e646df0113f7b40d`. Work is confined to the supplied
cw-bd235945fff44468a0f41292, Home Coder `lkjsxc/tomato-ocelot-73`, as non-root coder,
at `/home/coder/workspace/lkjscript`. Preserve the unrelated untracked
`docs/campaigns/202609242022.md`, the detached privacy worktree, historical evidence
and unrelated services. No active compiler or resident application was observed.
Pinned Rust 1.98.0 and independent compiler/checker concurrency of two fit the
observed 8 GiB memory limit.

Continue the ordinary create/edit/build/run/recover workflow, not a speculative
runtime rewrite or another closed demonstration. Initially reconcile delivery of
the previously accepted native starter. While inspecting the shared shutdown
boundary, a reproducible correctness defect changes the selected priority:
repair cancelled queue ownership before adding restart convenience.

## Accepted predecessor and interrupted promotion

Original producer [36227761390/1](https://github.com/lkjsxc/lkjscript/actions/runs/36227761390)
completed successfully at exact product/controller source
`6536fea15635933d8d91eef5ca5e2d930ae5c3f6`. Its maintained candidate and terminal jobs
passed; publication jobs were intentionally skipped. The downloaded essential
receipt reports `candidate_accepted`, twenty source gates, six final target owners,
two pinned userlands, installed recovery, completed cleanup and no failure.
The original acceptance and CI terminal are retained under
`.artifacts/202609262000/acceptance` and `.artifacts/202609262000/terminal`.
Essential service artifacts were unexpired; earliest essential expiry was
2026-10-10T09:19:12Z. This historical acceptance is not overwritten by a later finding.

Fresh reads showed immutable v0.1.48 release 397062403 complete, no v0.1.49 tag or
release, main unprotected, no enabled rulesets, and immutable releases enabled.
The ordinary annotation was created from the exact source's Markdown notes with
`--cleanup=verbatim`; its complete annotation was compared with those notes.
Remote annotated v0.1.49 object `87434ee244bff305104bf12f2822cb7861b50210` selects
6536fea1. The prior scoped selection `da0ac737ddd546653eac676ed1220674eeeee908`
was read and compared immediately before selecting the new object, then read back.
No credential, access-control, protection or immutability setting was changed.

One `operation=promote` dispatch selected producer 36227761390/1:
[36237509998/1](https://github.com/lkjsxc/lkjscript/actions/runs/36237509998),
controller 64c0334d, created 2026-09-26T11:00:58Z. After the regression below failed,
the still-running selection stage was cancelled. Final service observation is
`completed/cancelled`; selection/publication/public-verification jobs are cancelled
and the terminal correctly fails rather than asserting completion. A subsequent
complete release-list read found no v0.1.49 release or draft. Latest remains
immutable v0.1.48. The annotation and existing scoped selection remain unchanged.
Do not resume this defective candidate or rewrite its tag. Select additive v0.1.50,
whose tag and release were observed unoccupied, for the correction.

## Counterexample and implementation

`ResidentKernel::invoke_timed` incremented queued accounting before awaiting a worker,
but decremented it only when that await returned. Dropping the pending future restored
its semaphore permit while leaving a phantom queued count. The literal regression
manually polls a real invocation future to its pending state, drops it, and observes
shutdown without permitting the operation to run. On unchanged predecessor logic,
`.artifacts/202609262000/queue-before.log` records exit 101: queued=1, active=0,
remaining_tasks=1, no operation executed, no cancellation requested, and
102476699 ns elapsed with two configured 50 ms grace periods.
This is an observed configured-grace delay, not a general latency benchmark.

Pending work now owns the operation and a queue guard in declaration order.
Cancellation destroys captured resources before releasing queue accounting and waking
idle waiters. Admission capacity remains separately guarded. Successful admission
establishes active ownership before removing queued ownership, avoiding the previous
false-idle handoff. Failure to allocate an execution identity also releases pending
ownership. No application-level retry, counter reset, shutdown timeout reduction or
hidden queue is introduced.

The first corrected runtime suite passes all ten tests in
`.artifacts/202609262000/queue-after.log`. A deliberate early-handoff mutation then
causes the independent notification witness to observe active=0 instead of 1;
`.artifacts/202609262000/handoff-mutant.log` records the genuine exit 101. The worker
is released and joined before that assertion fails. The mutation was removed, not
weakened into an accepted expectation. The final suite adds an actual blocking
worker beside a Tokio task aborted while queued.

The original runtime file is byte-identical between the v0.1.48 and v0.1.49 tags.
This is not a defect introduced specifically by the native starter. The exact
kernel witnesses do not establish all HTTP disconnect paths, data corruption,
hostile-code isolation, or rollback of already started effects. Normalized
resident targets and ports share the corrected kernel. Existing task/grant/stream
limits and application data formats remain unchanged.

## Verification and delivery boundary

Focused corrected tests, generated discovery, source-wide acceptance and public
distribution have distinct identities. `.artifacts/202609262000/quality.log` completes
at exit 0: all eleven shared-runtime tests pass (eight new regressions), the product
is built in the unoptimized profile, all eight native discovery guides regenerate,
and workspace/all-targets Clippy passes with warnings denied. The real-worker abort
case keeps the active invocation valid and observes no queued operation execution,
zero queued/active/permit counts, and drained shutdown. No compile/lint failure was
relabelled as an expected test failure.

The corrected source is ready for one dependency-complete `check full --fresh` with
stable committed inputs. Its exact source, immutable checker and original receipt
must identify that result. No source-wide acceptance, mainline integration or
v0.1.50 finalized candidate is claimed by this initial implementation entry.
