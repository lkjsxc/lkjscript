# Owner-directed attribution and executable-proof continuation

## Original mandate (unaltered)

@Remote Desktop Commander  https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscript  について進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。限界まで深く考えてほしい。超長期的な視点からお願いしたい。ChatGPTの開発環境として、cw-bd235945fff44468a0f41292を用意しました。自由に活用ください。remote desktop commanderについて、月ごとにtool callingが1万回までという制限を考慮しつつお願いします。ChatGPTとしてではなく、lkjsxcとしてコミットしてほしい（なんなら過去のChatGPTのコミットもlkjsxcとして書き換えてほしいくらい）

## Reconciliation and execution boundary

Campaign opened on 2026-09-24 at 16:27 Asia/Tokyo. The repository is
`lkjsxc/lkjscript`. Independently observed main is
`5e75bd73574196c06e73ae3ce46f28c2b6a37179`.
The provided device was reached successfully. Its checkout is
`/home/coder/workspace/lkjscript`, clean on main, with no stashes or additional
worktrees and no active compiler or verification jobs at inspection. A normal
read-side fetch obtained the existing verification branch without switching the
checkout. Its source remains `8a6ab00b415ad23153881347feda6faff5b924c3`, tree
`08c250448a197b5ddc8a369d0e59bbbe2bde3412`, in draft PR #3.

A combined local-configuration, branch-switch and verification command was
rejected by the tool safety check before execution. It was not retried or bypassed.
Subsequent read-only inspection confirmed that the local Git identity is still
unset and the checkout remains unchanged. Independent authorized GitHub operations
are available for repository commits. Do not claim that this session installed
local author/committer configuration or synchronized the device checkout.

Root guidance, current status/roadmap, relevant process/check owners and the
existing Verify workflow were read. The maintained toolchain is Rust 1.98.0;
its formatter is installed. The device has about 6.9 GiB available memory and
795 GiB available filesystem space at inspection. No unrelated resources or
existing campaign evidence were changed. Root guidance already honors current
owner direction and requires normal, non-forced delivery; it is not replaced.

## Selected attribution change

Use the owner's existing public Git identity, not an invented address. The root
`.mailmap` maps the exact historical pairs `ChatGPT <chatgpt@local.invalid>`,
`ChatGPT <chatgpt@users.noreply.github.com>` and
`Yoshihito Shinnaka <lkjsxc@gmail.com>` to `lkjsxc <lkjsxc@gmail.com>`.
It does not rewrite commit objects, tags or published history. The maintained
[identity guide](../contribution-identity.md) explains client-specific raw names,
mailmap-aware views and the distinction from immutable publication identities.

Independent disposable Git tests passed eight cases: both ChatGPT aliases, the
owner's profile alias, the already canonical owner, an unrelated contributor,
a different name using the historical noreply address, the real GitHub Actions
bot identity, and GitHub's merge identity. Only the intended aliases changed.
A synthetic commit confirmed mapped author and committer output while a bytewise
comparison of the raw commit before and after display remained identical. This
fixture is not an actual project commit or language-test result.

The attribution-only tree contains no product, verifier, dependency, generated
asset or release changes. Its narrow display evidence is separate from both
predecessor language acceptance and the still-unaccepted executable-proof candidate.
It must not relabel an earlier full run with a new reporting SHA.

## Existing candidate: observed failure before execution

Read-only streaming of all eight changed Rust files through the exact installed
`rustfmt 1.9.0-stable (88d9e12ae1 2026-08-18)` in edition 2024 found six formatting
differences, all in `check/snapshot/executable_tests.rs`. The other seven files
matched. `git diff --check main <candidate>` passed. This is actual formatter
inspection of candidate bytes, not successful compilation, lint, regression tests
or full acceptance. Preserve the initial failure and apply the formatter's exact
output as a normal corrective descendant on the existing PR lineage.

At inspection, baseline Verify run `35965360640/1` still executes the old main
source, while candidate run `35966984926/1` is pending. The earlier initial-candidate
run `35965373009/1` was cancelled while pending. None is new candidate acceptance.
Do not create another verification branch, start a publication workflow, or push
individual edits repeatedly. Inspect the actual corrected source/run/attempt and
original evidence before normal behavioral integration.

The predecessor's separate unbounded identity-subprocess collection finding remains
unfixed: limiting retained bytes does not bound child exit or inherited pipe EOF.
Do not claim that executable-leaf binding fixes that lifecycle, pins concurrent
filesystem replacements, or observes interpreter/library/plugin closures.

## Delivery criteria and continuation

Attribution can be delivered independently after review with a non-forced main
update. A behavior candidate must include intervening main changes, pass the
maintained complete fresh full profile with stable inputs, and retain its exact
source, original verifier and failures before normal integration. Mainline source
delivery and frozen public v0.1.44 publication are distinct. No release is selected.
Append actual delivery and acceptance observations only after independently reading
the resulting refs and evidence. Account-wide remaining remote-call quota is
unknown; inspection calls are grouped and there is no sleep/status polling loop.
