# Immutable 0.1.16 Function Definition Projection Release

Status: incomplete

Campaign ID: `202609021420-function-definition-release`

## 1. Identity, audit boundary, and lifecycle

Audit time: `2026-09-02 14:20:41 +09:00` (`Asia/Tokyo`).

Orientation source: remote GitHub repository `lkjsxc/lkjscript`, default branch `main`, commit
`9b7ee9f9937a781e93344c11e27a595c21ba5346`, tree
`d2dad1877cc4c6137ce2bad64fd16d10dbb2b5a8`. The source was the remote branch and GitHub API state,
not an active checkout. The checkout and legitimate later commits outrank this baseline.

The remote identity was refreshed immediately before this mandate. The path
`docs/campaigns/202609021420.md` did not exist. `docs/campaigns/` remains the one observed
ChatGPT-to-Codex archive; no mutable current-work ledger was observed. Do not create a competing
prompt, plan, work, or campaign tree.

Read-only inspection covered repository metadata and recent history; root `AGENTS.md` and the path
tree; the latest function-definition and immutable-release campaigns; current status, architecture,
roadmap, release and relevant specification owners; release workflow and first-party target/service/
distributed verifiers; maintained standard and `lkjournal` consumers; and current public tags,
releases, workflows, issues, and pull requests.

Observed external state:

- source product is unreleased `0.1.16`;
- immutable public latest is `v0.1.15`, release ID `380401840`, published
  `2026-09-01T10:25:09Z`;
- annotated tag object `d9a6e7812325810c5becae2e217e1b9a4a600c8f` targets
  `2b428645471e032ba3cfb415bf81565d7ea0159e`;
- public archive asset ID `539383016` is 7,459,326 bytes with SHA-256
  `5a16f67272784e93948cc779d13fab7b4ae8c5f42bf9b8860517bb72eae51f01`;
- latest relevant successful release run is `33495319420`, attempt 1;
- no `v0.1.16` tag/release, open issue/pull request, or queued/in-progress workflow was observed.

Unavailable and unclaimed: local branch, `HEAD`, tree, index/worktree, untracked/ignored files,
remotes/upstream/divergence, stash, local credentials and repository variables, installed binary,
toolchain execution, builds/tests, private workflow artifacts, deployments, application data,
queue/object state, and production behavior. The deployment collection was unavailable through the
connector. A direct artifact-workspace clone could not resolve GitHub and produced no checkout.
Historical records name stash `87c2265f6faf0e815d80450ec2480d46c5e774fb` and root-owned ignored
artifacts; preserve anything actually present without assuming it still exists.

Archive: `docs/campaigns/202609021420.md`. Preserve this initial body. Codex may update only the
minimal lifecycle field and append one concise terminal record. Current truth, semantics, boundaries,
rationale, proof, generated projections, and raw output remain in their established owners.

Predecessor disposition:

- `202609021013-function-definition-projection` is complete at remote closure
  `9b7ee9f9937a781e93344c11e27a595c21ba5346`, after implementation
  `3f581f793c7d2e26b6c35ab22a780186436c4894` and
  `c8e11170b721cffa9e080d1d8cd577380fcd5c4d`.
- It deliberately created source-only `0.1.16` and requires publication closure before another
  independent product delta.
- `202609011758` is complete and owns immutable `v0.1.15`; it is precedent, not active work.
- No active remote source, release, deployment, or recovery campaign was observed. Reconcile any
  coherent local in-flight work before editing.

## 2. Implementation mandate and stopping rule

This is implementation, publication, and proof, not another planning pass.

**Required final state:** the exact completed `0.1.16` source snapshot—including revision-pinned
complete local-function definition projection and its copied-binary inspect → plan → apply →
reinspect proof—is available as one immutable latest `v0.1.16`
`x86_64-unknown-linux-musl` release whose exact candidate, deterministic package, transferred
no-checkout workflow, and anonymous exact/latest downloads freshly satisfy the current source,
target, projection, maintained application/service, distributed/stateful/outbound HTTP, integrity,
containment, and cleanup contracts, while `v0.1.15` and every predecessor remain unchanged and no
deployment or hosted application state is touched.

This campaign adds no graph meaning. Its semantic leverage is distribution closure: accepted graph
meaning already proved in source becomes inspectable through the supported immutable public binary.
The graph remains sole editable program authority; release artifacts remain derived distribution and
evidence.

Repair one verified blocker before publication. The Rust distributed HTTP owner emits identity
`lkjscript-distributed-http-acceptance` version `3` with `definition_projection`. The current release
workflow still requires version `2` in both transferred pre-publication and anonymous exact/latest
acceptance and omits the new result. Reconcile this in the checkout, then cut every live release
consumer over once. Do not accept both versions, add an alias, edit historical evidence, or weaken
checks.

Outcome rules:

- `complete`: REL16-1 through REL16-8 freshly pass at their real boundaries; release-source and
  evidence commits are safely pushed; immutable latest is exactly `v0.1.16`; and the tree is clean
  or preserved work is named.
- `blocked`: the objective remains valid but a required reconciliation, local, hosted, publication,
  or anonymous gate cannot close safely.
- `terminated`: `0.1.16` is not a coherent release snapshot, requires an independent product delta,
  cannot preserve immutable predecessors, or cannot meet current contracts in this campaign.
- `superseded`: legitimate later work already provides an equivalent or stronger immutable release
  and closes current evidence.

Continue through benign warnings, optional cache failures, unavailable convenience tools, and
non-material path/command drift. Stop before overwriting unfamiliar work, changing maintained meaning
to rescue publication, moving published identity, performing unauthorized mutation, or claiming
completion from source tests, a draft, an artifact, or an unavailable gate.

## 3. Reconciliation gate

Before editing:

1. Read applicable instructions, this mandate, terminal records for `202609021013` and
   `202609011758`, and current status, architecture, roadmap, release, verification, and
   function-definition owners.
2. Capture branch, `HEAD`/tree, index/worktree/untracked/ignored state, remotes, upstream/divergence,
   recent commits, stashes, supplied files, toolchain, credentials boundary, and unfamiliar work.
   Preserve all unrelated work; do not reset, clean, restore, rebase, or mutate a stash.
3. Fetch safely and refresh `main`, tags, releases, open work, queued/running workflows, immutable
   release setting, the existing non-secret tag-object confirmation variable, and any partial
   `v0.1.16` state.
4. Confirm root version, expected annotated tag, supported target, public latest, release owners, and
   current contract identities. Verify orientation values rather than rewinding legitimate drift.
5. Inspect every live consumer of distributed receipt versions `2` and `3`; separate current release
   logic from historical evidence.
6. Confirm the predecessor is terminal and standard/`lkjournal` semantic and artifact identities are
   unchanged. Reconcile any active source, release, or recovery mandate.
7. Establish whether a `v0.1.16` tag object, draft/immutable release, assets, attestations, or workflow
   already exists. Never duplicate or mutate legitimate partial public state.

Correct non-material drift locally. Stop only when evidence invalidates the objective, irreversible
preconditions cannot be met, or closure requires unrelated scope. If a legitimate immutable
`v0.1.16` already exists, verify exact/latest and classify this campaign `superseded` or close only
missing current evidence. If only a tag exists, continue solely when it targets the intended source
and current recovery policy permits additive completion without moving it.

Narrow empirical questions left to Codex are: the exact checkout/preserved-work boundary; final
contract counts and receipt shape; whether the two stale predicates are the only live v2 consumers;
the smallest current test owner for their cutover; final candidate/package/receipt digests; release
setting, variable, authentication and concurrency state; and exact workflow/release/asset/
attestation identities. These are observations, not permission to redesign product semantics.

## 4. Current bottleneck and campaign decision

The completed source campaign supplies bounded `inspect owner KIND ID --detail definition` pages for one
accepted local pure or task function; exact revision/function/digest/range identity; deterministic
continuations with record and byte budgets; complete contract/body/reference/validation closure;
stale, malformed and projection-input rejection; unchanged authority before apply; an affine
`lkjournal` worker consumer; an implementation-disjoint oracle; direct-file-plan agreement; and
copied-binary inspect → plan → apply → reinspect behavior.

Its terminal record reports, as orientation, product `0.1.16`, CLI `18`, registry `6`, projection
contract `1`, distributed receipt `3`, service receipt `6`, Graph `6`, validator `6`, authored
change `7`, compact request `9`, package interface `4`, artifact `11`, and unchanged maintained
semantic/assets. Verify each owner. Its candidate and receipts are historical for older inputs:
this mandate and release-workflow correction create a new release-source commit `R`, so final binary,
package, and evidence must be fresh.

The bottleneck is the source/public split. Agents downloading latest receive `v0.1.15`, while the
release workflow consumes the predecessor distributed contract. Publication must therefore include
one complete v2→v3 release-consumer cutover.

Scores are 1–5; higher is better except cost and risk, where lower is better.

| Candidate | Value | Semantics | Authority | Closure | Completion | Proof | Unlock | Cost | Risk |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Publish `0.1.16` and cut receipt consumer to v3 | 5 | 3 | 5 | 5 | 4 | 5 | 5 | 3 | 3 |
| Task-function affine capability transfer | 4 | 5 | 4 | 2 | 2 | 3 | 4 | 5 | 4 |
| Million-owner compiler admission | 3 | 2 | 2 | 3 | 3 | 4 | 3 | 4 | 2 |
| Nostr WebSocket/event/session semantics | 3 | 5 | 4 | 1 | 1 | 2 | 4 | 5 | 5 |

Publication wins because it closes explicitly unfinished distribution work and exposes the completed
semantic slice through the supported binary. Task transfer lacks a maintained cross-task ownership
workload and would add scheduling/cancellation/lifetime proof. Nostr lacks a maintained event-flow
consumer and closed key/replay/resource security. Million-owner admission does not close the product
boundary.

## 5. Target design and scope

Mandates:

- Publish the coherent root snapshot as `v0.1.16`; do not bump version or add independent behavior.
- Preserve one semantic authority and all maintained accepted meaning. Release machinery may not
  redefine graph, package, application, deployment, or operational-data semantics.
- Require the current distributed receipt and complete projection result at pre-publication and
  exact/latest boundaries. Current v2 inputs must reject; historical v2 evidence remains unchanged.
- Preserve immutable `v0.1.15` and every predecessor tag/release/asset/attestation byte-for-byte.
- Preserve standard and `lkjournal` accepted/generated identities. Any required regeneration must be
  owner-produced and byte-identical; otherwise stop.
- Produce one admitted static musl binary, one deterministic archive, one checksum asset, and current
  required release/asset attestations.
- Exercise both pinned target userlands and every current maintained target oracle.
- Prove transferred no-checkout acceptance before publication and unauthenticated exact/latest after.
- Touch no deployment, live service, relay, database, queue, object store, secret, or user data.

Implementation latitude: Codex may choose narrow Rust tests, workflow factoring, and a first-party
helper needed to keep v3 assertions coherent. Do not add a public product command or second receipt
schema. Prefer the smallest change that leaves Rust as contract owner and workflow as explicit
consumer. Exact commands, test placement, evidence JSON shape, and release-note wording are local
choices when outcomes hold.

Non-goals: graph semantics, task transfer, Nostr/WebSocket flow, live relay access, authored
operations, deployment/storage adapters, JIT/AOT/performance work, new targets, installers,
registry publication, compatibility aliases, or documentation-authority migration.

Deferred after publication: maintained-workload affine task transfer; million-owner compiler
admission distinct from data/history scale; authored operations justified by maintained workflows;
and Nostr event/session semantics only with closed resources, keys, replay, cancellation, raw
protocol proof, and a maintained consumer.

## 6. Success conditions

### REL16-1 — Exact release source

Create one release-source commit `R` containing this mandate, the complete release-consumer cutover,
its proving tests, and only same-objective changes. At `R`, root version is `0.1.16`; expected tag is
`v0.1.16`; maintained semantics/assets match the predecessor; every live distributed predicate
requires the active owner version (expected `3`) and projection proof; no live release path accepts
v2; generated owners, lockfile, policy and truthful unreleased docs agree; `R` is safely pushed and
equals refreshed `origin/main`; and the tree is clean except named preserved work. Do not claim a
public release before publication.

### REL16-2 — Fresh source proof

After final `R` content stabilizes, run one authoritative fresh full source profile. It must prove
contract/generated coherence, graph authority, definition paging and independent-oracle agreement,
malformed/stale/projection-input rejection, public CLI, standard package, maintained `lkjournal`,
clean/incremental agreement, required service/application boundaries, and lint/security policy.
Reused, skipped, unavailable, or failed required evidence blocks publication.

### REL16-3 — Exact target admission

Build the final `R` candidate for `x86_64-unknown-linux-musl`, prove actual staticness, validate
hostile candidate/archive boundaries, and admit that exact binary in both pinned userlands through
all maintained oracles. Retain maintained service acceptance with its worker definition projection;
distributed v3 acceptance using a copied binary and fresh HTTP project; current stateful HTTP/data
acceptance; and current outbound HTTP/TLS/DNS/NIP-11 acceptance. The distributed result must page a
function, construct the intended compact body change, agree with direct-file planning, reject
projection bytes and bad/stale continuations, preserve authority before apply, apply once, reinspect,
and observe only intended body/digest change. Bind exact identities/digests, containment, resources,
and cleanup without implying deployment, portability, sandboxing, reproducibility, or live relay use.

### REL16-4 — Deterministic no-checkout package

Prepare the exact candidate twice with the first-party release owner. Archives and manifests must be
byte-identical and pass strict verification. The archive has only canonical inventory, binds `R`,
target and admitted candidate digest, transfers the required verifier/handoff, requires no checkout,
and rejects extra, missing, duplicate, symlink, non-regular, traversal, corrupt, mismatched, or
trailing input. Record bytes and SHA-256 for candidate, archive, manifest, verifier and target
receipt.

### REL16-5 — Hosted non-publication proof

After `R` is pushed and equals refreshed `origin/main`, dispatch one successful canonical `Release`
dry run with `publish=false` and tag input `v0.1.16`. It must bind exactly `R`, rebuild/admit/package
the target, run transferred no-checkout acceptance with distributed v3 projection assertions, and
skip every publication/post-publication mutation. It creates no tag, release, asset, attestation,
deployment, or evidence commit. A changed `R` invalidates the run; retry unchanged inputs only for a
classified transient hosted failure.

### REL16-6 — Immutable publication

Immediately before mutation, refresh source, tag/release/workflow state, immutable setting and
credentials. Require clean reconciliation, `R == origin/main`, successful final-`R` dry run, no
conflicting run, unused `v0.1.16`, immutable releases enabled, and the existing non-secret
`LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA` variable. Create one annotated `v0.1.16` tag targeting
`R`, resolve its tag-object SHA, set only that existing variable to the exact SHA, and push the tag
only. The canonical workflow must create one non-draft, non-prerelease, immutable latest release
with the canonical archive and `SHA256SUMS`, required attestations, isolated publish authority, and
no predecessor mutation. Do not upload manually.

### REL16-7 — Anonymous exact/latest proof

In fresh temporary roots, anonymously download both exact `v0.1.16` and latest. Do not reuse runner
or authenticated API bytes. They must agree on tag/source, archive/checksum/candidate/manifest
digests, target, staticness, product/commit/capabilities, inventory and attestation subjects. Both
must run the copied binary and transferred verifier through every maintained target boundary. The
distributed v3 result must independently prove stable projection contract, changed revision/
definition digest/literal hash, recomputed digest, alternate page-budget agreement, direct-file plan
agreement, malformed/stale/projection-input rejection, unchanged pre-apply authority, intended
body-only post-apply change, and cleanup.

### REL16-8 — Evidence and closure

After public proof, create evidence/current-truth commit `E` without changing release-bound source,
workflow, verifier, target policy, generated semantic assets, maintained meaning, or package
identity. Track bounded evidence at
`docs/evidence/202609021420-function-definition-release.json` or the exact current convention,
recording all exact identities/digests, classifications, projection outcomes, resources, external
actions, cleanup, limitations and ignored-artifact pointers. Update current status, architecture,
release, README and roadmap owners so source/public latest are truthfully `0.1.16`, projection is
public, v3 acceptance is current, and the release gate is closed. Append the terminal record, set
status `complete`, safely push `E`, and end clean or name preserved work.

## 7. Dependency order and proof discipline

Implement in this order:

1. Reconcile checkout and volatile external state; classify clean continuation, legitimate partial
   publication, supersession, or unsafe conflict.
2. Freeze predecessor semantic/package/application identities.
3. Cut both live release consumers from distributed v2 to v3 with projection-specific proof and
   narrow regression coverage.
4. Assemble and inspect `R`; keep current prose truthful about unreleased state.
5. Run narrow iteration checks, then one fresh full source profile.
6. Build/admit the exact static candidate in both userlands and all current oracles.
7. Prepare twice and strictly verify the deterministic no-checkout package.
8. Push `R`; run one successful final-`R` publish-false workflow.
9. Recheck irreversible preconditions; create/tag-confirm/push `v0.1.16`; observe canonical publish.
10. Run fresh anonymous exact/latest acceptance.
11. Write/push `E`, append terminal history, and report exact remaining boundaries.

A change to source, workflow, verifier, target, policy, generated or release inputs invalidates all
downstream receipts. If a defect appears before tagging, create a new `R` and rerun invalidated
proof. If immutable `v0.1.16` is already published and public acceptance finds a content defect, do
not mutate it or create `v0.1.17` here. Preserve state, retain bounded failure evidence, mark blocked,
and leave additive patch recovery to a separately authorized campaign.

Classify every gate exactly as `fresh passed`, `reused passed`, `skipped`, `unavailable`, `failed`,
or `not run`. REL16 completion requires fresh passes for source, target, package, dry run,
publication, and anonymous gates. Never relabel stale evidence.

Independent proof includes source reconstruction of definition records/facts, direct-file versus
compact-plan agreement, two target userlands, copied-binary maintained observations, transferred
no-checkout execution, anonymous downloads, checksum/manifest verification, static inspection, and
attestation-subject verification. Negative proof includes hostile package input, identity/digest
mismatch, bad/stale continuation, projection-input rejection, pre-apply authority preservation,
dry-run nonpublication, v2 rejection, failed-startup containment, and cleanup.

Raw logs, binaries, archives, receipts, downloaded assets, projects, and temporary roots remain
ignored. Tracked evidence stores bounded summaries, exact identities/digests/classifications and
pointers. Record material bytes, records/pages/output, commands/runners/responses, elapsed
observations, workflow attempts, downloads, attestations and cleanup with units. Make no unsupported
latency, throughput, safety, isolation, portability, reproducibility, provenance, or service-level
claim.

## 8. Read map

Begin with `AGENTS.md`; campaigns `202609021013` and `202609011758`; `docs/status.md`,
`docs/architecture.md`, `docs/roadmap.md`, `docs/release.md`, `docs/spec/verification.md`,
`docs/spec/semantic-cli.md`, the function-definition decision/evidence; `Cargo.toml`, `Cargo.lock`,
`README.md`; `.github/workflows/release.yml`; `tools/lkjscript-dev/src/distributed_http.rs`,
`service.rs`, release and target/check owners; `tests/public_cli.rs`; and maintained standard and
`applications/lkjournal/` identity owners. Expand by unresolved symbol or release decision, not by
directory. Search current receipt identity/version, `definition_projection`, no-checkout handoff,
exact/latest acceptance and immutable tag confirmation; do not ingest all historical campaigns or
raw logs.

## 9. Git, external actions, recovery, and final report

Use explicit staging, inspect every staged diff, and never use `git add .` or `git add -A`.

`R` is the final release-source commit and must equal pushed `origin/main` before dry run/tagging.
`E` is post-publication evidence/current truth/lifecycle only and must not change an input bound by
the release. Additional commits are allowed only when coherent and the `R`/`E` boundary remains
exact. Fetch before normal fast-forward pushes. End clean except named preserved work.

| Action class | Authorization |
|---|---|
| Scoped local edits and owned generation | Authorized for this campaign |
| Local builds/tests, copied binaries, pinned containers/userlands, loopback services, temporary data and cleanup | Authorized |
| Coherent commits | Authorized |
| Normal fast-forward push of `R` and `E` | Conditional on reconciliation, fresh gates, fetch and safe upstream relation |
| Workflow dispatch | One final-`R` `publish=false` dry run; one unchanged-input retry only for classified transient failure |
| Tag/release/assets/attestations | Conditional for exact `v0.1.16` only after REL16-1–5; canonical workflow owns publication |
| Existing non-secret repository variable | Only set `LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA` to the exact new tag-object SHA immediately before tag push |
| Deployment/service reconfiguration | Forbidden |
| Schema/data/queue/object/database/hosted-state mutation or live relay effects | Forbidden |
| Issue/PR/permission/general-setting/secret/credential mutation | Forbidden except the named existing non-secret variable; no interactive login |
| Package/installer/registry publication | Forbidden |
| Reset/clean/restore/rebase/force push/retag/history rewrite or published-object replacement/deletion | Forbidden |

Before the tag, repair by a new `R` and rerun invalidated proof. After tag creation but before release,
never move the tag; retry unchanged input only for a proven transient/idempotent workflow failure,
otherwise stop blocked. After immutable release creation, never edit, delete, replace or unpublish it.
Unavailable credentials allow local work/commits only, not claims about push or publication.
Unfamiliar work must be preserved exactly.

The final report must state:

- start/final branch, commit/tree, worktree, upstream/divergence, stash and preserved work;
- exact `R`, `E`, tag object/target, workflow run/attempt, release, asset and attestation identities;
- final product, contract, target, package, artifact, release and deployment identities;
- unchanged maintained semantic/assets and public projection workflow;
- every required gate and truthful classification;
- independent, negative, containment, cleanup, deterministic-package and clean/incremental results;
- all candidate/archive/manifest/receipt/evidence/verifier bytes and digests;
- deviations, failures, limitations and security nonclaims;
- every commit, push, workflow, variable update, tag, release, asset, attestation, deployment,
  migration and other external action performed or not performed;
- any remaining source/release/deployment split; and
- the smallest credible next candidates.

Append one concise terminal record with status `complete`, `blocked`, `terminated`, or `superseded`;
completion time; start/final identities; REL16-1–8 dispositions; evidence classifications/digest;
external-action result; limitations; preserved work; and next candidates. Do not rewrite this initial
body or paste raw logs.
