# Complete full-lifecycle acceptance and deliver the numerical-library milestone

- Repository: https://github.com/lkjsxc/lkjscript
- Observation/name timestamp: **2026-09-16T16:30:49+09:00 — Asia/Tokyo**. The environment clock was read and converted with `ZoneInfo("Asia/Tokyo")`.
- Intended initial archive: **`docs/campaigns/202609161630.md`**.
- Companion: the complete root **`AGENTS.md`** replacement candidate supplied with this mandate.
- Kind: **corrective verification and delivery continuation**. This is not another language-feature campaign.
- Selected result: make the implemented binary64, requirement-parameter, structural-authoring and transaction-completion capabilities available through one fully accepted immutable public release, with their launch and original-evidence repairs intact.

Execute this mandate as the responsible Codex integrator. Ordinary implementation, normal repository integration and the selected release are authorized, subject to actual permissions and maintained gates. This does not authorize production deployment, destruction of operational data, purchases, billing changes, credential or permission changes, protection bypasses, force-pushes or replacement of published identities. Ordinary use of existing configured CI/release services is authorized. Preserve unrelated work.

## 1. Starting point: the preceding correction worked, but its release rehearsal failed later

### Source and delivery observations

These are separate source contexts, not interchangeable versions of “current main.” Related implementation and tooling reads used the frozen corrective candidate below. The default ref and latest release were reconfirmed before the timestamp above; API observations are not an atomic snapshot of all remote state.

| Role | Observed identity and disposition |
| --- | --- |
| Default `main` | `0b6a9c08017fa7565408acfda10d3ce3b3c40c04`. It contains accepted transaction completion, but not the unintegrated F64 candidate. |
| Frozen corrective candidate | `a5bad65c14e118e47f774ecaac0f5adce4259f7d`, tree `3687cdf6ed9b2f049507330927ef12aa1f4409eb`, branch `codex/f64-cutover-202609161407`. |
| Its reporting child | `b5224018b282ea642e7c6d45c483640ff50782de`, tree `528d317a7e2b6bacb6491ca953e2cc8dd7ddcaa7`, branch `codex/f64-cutover-report-202609161407`. Its 05:29 UTC pending observation is superseded by the terminal run below. |
| Latest public release | Immutable **v0.1.36**, release **388927116**, published **2026-09-15T07:00:57Z**. Its publication does not close its failed original-public-evidence admission. |
| Next selected version | The exact `refs/tags/v0.1.38` lookup returned 404. **v0.1.38 remains the intended combined successor**, subject to a fresh occupancy/competing-work check; a past 404 is not a reservation. |
| Corrective rehearsal | **35059467604/1**, `workflow_dispatch`, exact candidate `a5bad65c…`, `publish=false`, selected version `v0.1.38`: **completed, failure**, updated **2026-09-16T07:17:57Z**. |

The preceding [cutover campaign ending](https://github.com/lkjsxc/lkjscript/blob/b5224018b282ea642e7c6d45c483640ff50782de/docs/campaigns/202609161407.md) records the actual implementation and remaining acceptance. Its parent is the [binary64 campaign](https://github.com/lkjsxc/lkjscript/blob/b5224018b282ea642e7c6d45c483640ff50782de/docs/campaigns/202609152217.md). Preserve both endings, but do not repeat their completed implementation plans.

The old `tests/general_service.rs` expectation of `LKJART20` was already corrected to the deliberately produced `LKJART21`. In the new rehearsal, job **104676523094**, “Build, verify, and package,” **succeeded**: the hosted workflow reports successful fresh full verification, exact static target admission, strict packaging and verifier handoff. Job **104693766893**, “Verify transferred packaged application candidate,” failed later. Publication and anonymous acceptance were skipped. Neither “everything is still unrun” nor “the release passed” describes this state.

### Authenticated failure diagnosis

The architect downloaded the original prepublication and target-admission ZIPs through authenticated GitHub access, independently checked their exact sizes and SHA-256 against API metadata, and inspected their retained JSON/log contents. **No product execution, build, original lkjscript evidence-reader admission, integration or publication was performed by the architect.** JSON inspection is not original-reader acceptance. The other handoffs below were identified from authenticated metadata, not downloaded in this investigation.

Run and artifact owners:

- [Rehearsal and jobs](https://github.com/lkjsxc/lkjscript/actions/runs/35059467604).
- [Artifact metadata inventory](https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/35059467604/artifacts).

| Original handoff | Artifact ID | ZIP bytes | SHA-256 |
| --- | ---: | ---: | --- |
| Prepublication application evidence | 10434419447 | 156062081 | `4dee61ce90cef4d989386c9d235e4fc051622f8ec20067116939c45cc813c86b` |
| Target-admission evidence | 10433977401 | 51114811 | `09e7cd8a47d4446bb8a96ca60ce41786f198292226a78fd58c36612f3ca8ab7f` |
| Verifier handoff | 10433612545 | 32892876 | `df29c288a4098599b4d501914fc725a33499db2c9b83fdfd97160a1a6a154c6e` |
| Release handoff | 10433547734 | 11080571 | `15469cba6ce9e0a1bd359544aa4a1186f93a03c230433c3bdb545ca6ad161119` |
| Full-verification evidence | 10433070564 | 50738774 | `403ad0eba5e9ae15a0ecd4b69f9000a9e0307e631f546cb0d45a68f4df28b0e0` |

**Acquire available originals promptly.** Release/verifier handoffs expire at approximately **2026-09-17 06:45 UTC**; the other three expire on **2026-09-23** at their respective creation times. Preserve authenticated originals in ignored owned storage, including metadata and the original verifier/context. Do not depend on this chat's filesystem. Expiration means unavailable historical material, not passing acceptance; new complete acceptance remains possible.

The decisive prepublication ZIP members are:

```text
prepublication/transferred/receipt.json
prepublication/transferred/full-suite/receipt.json
prepublication/transferred/full-suite/offline-packages/receipt.json
prepublication/transferred/full-suite/offline-packages.stdout.log
prepublication/transferred/full-suite/offline-packages.stderr.log
```

Their observations establish:

1. Both simulated exact/latest installation routes record successful small lifecycles. The aggregate passed distributed HTTP and outbound HTTP, then **timed out its offline-packages child**. Pure-tail and stateful-HTTP were **not run in this pair**, although their separate target observations passed.
2. The offline child `ProcessObservation` records `status=timeout`, `reason=timeout`, elapsed **1,800.016522592 seconds**. Its standard-output and standard-error files are empty. The pair records failure and completed cleanup.
3. The child's own retained receipt records `status="fresh passed"`, **776 command observations**, cleanup complete and elapsed **1,797.512640198 seconds**. This is **not terminal success**. Source sets that elapsed value before temporary-root cleanup, evidence enumeration/hashing, receipt publication, strict rereading and final summary/exit. The exact instruction at which the deadline fired is unobserved; it is unnecessary to guess it.
4. The same source's earlier target execution records the same 776-command owner with inner elapsed **1,235.970816014 seconds**, and a successful whole child process lasting **1,238.492505124 seconds**. The pair's command-duration sum is about **1,776.45 seconds**, versus **1,217.30 seconds** at target admission. These observations point to accumulated workload cost, not a measured multi-minute final-reader bottleneck. They come from different jobs/contexts and do **not** establish a controlled product-performance regression or its environmental cause.

The prepublication pair receipt is 199530 bytes, SHA-256 `c4f001460845896c2f647cf1701528c5d03f0ddc93730663ea6ebb59db8753f1`, digest `verification_ba0c9b640d0763a6606fdd3cdfe3981a20440f109127ef549a11ad1b4007af1e`. It remains failed. Its reported candidate is 24295968 bytes, SHA-256 `98a39bd192c1e98187a1a954f916f5ffc89ef2586317c25e1efa06c61b888c32`; the hosted verifier is 32892104 bytes, SHA-256 `52b839684f9024c3ce881e1c46b61328b4b1a5ba3bfee63868bae58720d3b14b`. Admit actual handoff bytes before using them; these identities do not certify a future source.

### Inherited obligations

| Item | Disposition |
| --- | --- |
| v0.1.35 | Reported last fully closed public milestone. Preserve it; do not republish it. |
| v0.1.36 | Published, but its original public reader rejected eight missing authoring requests/plans. Preserve immutable assets and failed admission. The successor must retain and admit genuine new originals. |
| v0.1.37 | Tag object `51ecaabf3a6dfbaef0f842e95c4fc480dced0b6d` selects `8baafff7f67b55481cda8d67f8945b78af80be4d`. Publisher `34965341260/1` failed full verification before publication. The binary64 mandate already superseded separate recovery with the combined successor. Preserve the old identities/history; do not restart that old lane without intervening contrary evidence. |
| Transaction completion | Accepted source `93b73f87ac480343b62fd05f298f5c144d7adeb5`, normally integrated through `3fdddef3e98f120c7f58bc3719b3d22595847a20`, with reported source/target proof. Its final companion has already been selected. **No further feature deferral remains.** |
| Binary64 and Artifact 21 cutover | Implemented; the previous stale-consumer failure is fixed. Newly observed target/full/package successes remain exact-source evidence. Installed aggregate, final original-reader/fault obligations, integration and public delivery remain unfinished. |
| Launch/retention repairs | Preserve once-only execution after successful spawn and complete original input/plan retention. Neither has been disproved by this different deadline failure. |

### Safe startup and bounded reconciliation

Before editing, inspect actual HEAD/branch, index/worktree, relevant untracked work, stashes, remotes/divergence, applicable guidance, toolchain/resources and active CI/release jobs. Read actual predecessor endings. The revisions above are observations, never reset targets.

At the observed state, use a **mutable descendant containing `b5224018…` and the complete F64 lineage**, reconciling intervening main changes normally. Preserve frozen `a5bad65c…`, earlier candidates, reporting history, unrelated worktrees/stashes and operational state. Archive this initial mandate unchanged; append clearly separated execution/reconciliation/resumption records. On an actual collision preserve both originals with observed seconds or a documented suffix. Reconcile the supplied root candidate with legitimate intervening instructions and install it in the mutable checkout before substantive work. Do not change frozen release inputs to add guidance or reports; changing the file does not override instructions already governing the session.

If an intervening correction or qualifying job exists, resume its missing obligations rather than dispatching duplicate work. If the promised capability, required proof and selected public delivery already exist, report this mandate satisfied/superseded with exact evidence and complete only real archival work. Do not silently select another feature. If access is unavailable, preserve the specific missing read/action and continue independent authorized work; never fabricate integration or acceptance.

## 2. Decision: one complete invocation has one coherent operational allowance

**Architectural thesis:** verification deadlines are contributor operational policy, not language semantics. A maintained acceptance owner needs a consistent allowance for its complete invocation—including evidence finalization—across source, target and installed execution. Independent acceptance must remain strict; completing useful work inside a child does not excuse failure to terminate and admit its evidence.

The present check registry gives offline-packages the default **3,600-second** gate allowance. Target and transferred execution both call `Oracle::timeout()`, which gives it only **1,800 seconds**. Meanwhile the prepublication and public jobs each have **60-minute** outer limits. The workload has grown across substantive language campaigns; the same deadline failure class previously appeared in the foreground release. The mechanism to repair is this inconsistent, undersized invocation policy, not F64 arithmetic, a current-artifact marker, or the latest sample size.

**Before:** substantial numerical libraries exist on a candidate branch, but an installed acceptance boundary kills their complete accumulated workflow and reports only “pair behavioral aggregate failed.”

**After:** unchanged meaningful workload coverage terminates and passes its original readers through the installed product; finite limits still reject genuinely stuck work, diagnostics expose the failing owner, and the combined capabilities reach anonymous users in the selected immutable release.

The strongest alternative is to optimize repeated public checking/authoring or split the growing offline suite into independently schedulable owners. That could reduce recurring elapsed time and deserve a later campaign. Here, existing successful target execution and the distributed per-command cost argue against inventing a new validation architecture to solve a demonstrated capacity mismatch. A single shared policy and bounded diagnostic correction are executable now without changing semantic proof. Do not claim this repair makes successful tests faster, reduces model tokens or proves billing savings.

After delivery, measured ordinary numerical/data-processing authoring should determine whether repeated preparation/check cost, data representation or further transaction composition is the next limiting dependency. Reconsider that direction when representative measurements disagree. This is not an authorized successor queue.

## 3. Resolved contract and owners

### Mandatory correction

**Use one explicit offline-packages complete-process allowance of 3,600 seconds**, owned by the offline acceptance implementation and consumed by the check registry and the target/transferred oracle path. A small shared constant/accessor is sufficient; do not create a policy framework. This deliberately aligns the stricter paths with the source gate's existing allowance and gives approximately twice the observed slow invocation's elapsed time. It is an engineering allowance, not a guaranteed runtime on every host.

Set both **pre-publication application** and **anonymous public application** job limits to **120 minutes**, retaining their finite outer bound. Leave the existing 180-minute build job and 30-minute publication job unchanged unless an independently established nesting constraint requires a justified correction. The outer job remains a separate global limit, not a promise that every child can consume its individual maximum in one run. Do not blindly raise unrelated limits or add artificial waits. Audit direct callers and maintained workflow assertions so the repaired child is not still wrapped by an obsolete shorter allowance elsewhere.

The allowance includes process startup through terminal exit, including child cleanup, evidence publication and child self-admission. Preserve existing per-command deadlines, output limits, cancellation, owned process-tree termination, input immutability guards and temporary-resource cleanup. Parent-side original-reader admission remains separately required. Do not restart the clock when a receipt appears, extend deadlines from child heartbeats, accept timeout as success, retry an executed application automatically, or require ordinary lkjscript programs to adopt this contributor budget.

Improve failure propagation within the existing transferred/pair owners. The top-level failure summary must identify the first failing oracle and its actual process classification/reason, with the retained evidence location and elapsed/limit information available in bounded diagnostics. A reader rejection after a zero exit must remain distinguishable from timeout, cancellation, output exhaustion and cleanup failure. Reuse existing receipt structures/failure fields where possible; do not dump large receipts into CI logs or change public product formats for diagnostic text. Do not hide the source of a failure behind a successful inner receipt.

### Preserved capability and compatibility

There is **no new language operation, application, scalar, graph generation, storage scheme, daemon or execution engine selected here**. Keep Graph 17/compiler 13/bytecode 9/Artifact 21 and their genuine supported predecessors under the current specification. Do not regenerate authentic historical fixtures, regress current production to Artifact 20, or alter operational data to repair a verifier.

Preserve the implemented F64 rules, including ordinary generic/nominal composition, normalized NaN representation versus numerical equality, signed zeros, finite external JSON, full-domain typed binary values, explicit conversions and unchanged integer behavior. Preserve `transaction-outcome`: `Committed(T)` is observable only after that transaction completes; aborted completion does not expose an uncommitted candidate. Independent callback effects may survive; no automatic retry or blanket rollback promise is introduced.

The existing literal statistics library remains the discriminating public workload. A separately authored consumer-owned nominal sample type is processed through a bound projection into the imported generic library; the producer is removed, calibration is edited through reviewed public authoring, and typed checkpoint bytes survive a fresh process. The granted data witness must still observe the committed value after later nonfinite JSON output fails. Do not replace these with a scalar smoke test, host-computed application logic or fewer acceptance cases. The 4,096-sample exact oracle and independent expected numerical results remain required at their existing owners. These are designed feasibility witnesses, not evidence of adoption or high numerical throughput.

### Source map (all implementation links are the inspected candidate)

| Concern | Maintained owner and inspected evidence |
| --- | --- |
| Offline lifecycle, literals, evidence | [tools/lkjscript-dev/src/offline_packages.rs](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/tools/lkjscript-dev/src/offline_packages.rs), and its `offline_packages/` children, especially `f64.rs`, `f64.producer.lkjc`, `f64.consumer.lkjc`, `f64.data.lkjc`, `f64.edit.lkjc`. |
| Shared target/transferred inventory and deadline | [release/transferred.rs](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/tools/lkjscript-dev/src/release/transferred.rs), `Oracle::timeout()` and supervised child execution. |
| Target caller and independent readers | [release/admission.rs](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/tools/lkjscript-dev/src/release/admission.rs); it already calls the shared oracle timeout. |
| Source-gate deadline and binding | [check/registry.rs](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/tools/lkjscript-dev/src/check/registry.rs), [check/model.rs](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/tools/lkjscript-dev/src/check/model.rs); the profile digest already includes gate timeout. |
| Pair completion and failure reporting | [release/transferred/pair.rs](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/tools/lkjscript-dev/src/release/transferred/pair.rs), its installation/lifecycle/recovery children and [existing test hooks](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/tools/lkjscript-dev/src/release/transferred/pair/tests.rs). Follow `crate::process` to its supervisor tests before changing supervision. |
| Hosted effects, retention and enclosing bounds | [.github/workflows/release.yml](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/.github/workflows/release.yml). Dry-run dispatch is read-only publication-wise; a matching `v*` tag push initiates publication. |
| Normative acceptance and publication | [docs/spec/verification.md](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/docs/spec/verification.md), [docs/release.md](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/docs/release.md). Numerical semantics remain at `docs/spec/language.md`. |
| Root guidance and current status | [AGENTS.md](https://github.com/lkjsxc/lkjscript/blob/a5bad65c14e118e47f774ecaac0f5adce4259f7d/AGENTS.md); [reporting status](https://github.com/lkjsxc/lkjscript/blob/b5224018b282ea642e7c6d45c483640ff50782de/docs/status.md). |

Mandatory outcomes outrank the recommended small implementation. Codex may choose an equivalent simpler ownership placement or better diagnostic representation, recording why. A different allowance needs concrete lifecycle/environment evidence and enough remaining enclosing time; it may not be chosen just above one favorable measurement. A new semantic defect must be diagnosed at its owner, not obscured by another timeout increase. If the complete suite cannot finish comfortably within the selected allowance, retain that result and identify the dominant measured phase before further changes. Do not weaken the workload to obtain a green result.

## 4. Execution and proof, ordered by dependency

### A. Preserve and understand, without another long reproduction

Complete startup reconciliation, archive/root installation and original acquisition. Read the two authenticated parent/child receipt chains and original source ordering. Record the disagreement between provisional child outcome and terminal supervision. **Do not spend another 30 minutes reproducing the already retained timeout merely for reassurance.** Use a bounded source-level regression to exercise that failure contract.

### B. Make the coherent correction and prove its failure semantics

Implement the shared allowance, its actual consumers, outer job limits and diagnostic propagation. Update only affected narrow documentation and workflow-contract tests. Keep detailed incident identities in this campaign, not root guidance. No maintained language assets or generated operation guides need regeneration merely for a contributor deadline; regenerate only if their actual inputs change.

Use existing process/pair test hooks with scaled short limits, deterministic synchronization where feasible and owned disposable processes. Cover a child that records a successful-looking intermediate result but remains running past the bound; cancellation or a stuck descendant must still fail and be joined. Cover normal finite completion and a zero-exit child whose evidence fails admission. Check the actual call-site policy selection and independent expected value, not only that two aliases of one constant are equal. Do not run hour-long timeout tests or introduce production-only fault controls.

Verify that the failure summary identifies `offline-packages` and `timeout` for the scaled case while preserving nonzero exit and the failed parent receipt. Other failure classes must not be relabeled timeout. A failed attempt stays failed even when a later fresh attempt succeeds.

### C. Freeze inputs and use the maintained acceptance route once

After affected tests and documentation stabilize, review the staged diff, commit the intended paths, and freeze a clean corrective source on a new ordinary branch. Keep reporting descendants separate. Inspect matching remote work before dispatch. Use one exact-source read-only release rehearsal for dependency-complete full, static target, package and installed-pair proof rather than running an overlapping local full/target/pair pipeline solely for reassurance. A newly changed verifier/source needs new binding; old successful subjobs cannot certify that new source.

Source-verified contributor entry points are:

```sh
cargo build --release --locked -p lkjscript-dev
cargo run --release --locked -p lkjscript-dev -- check focused --machine
cargo run --release --locked -p lkjscript-dev -- check full --fresh --machine
target/release/lkjscript-dev release target
```

These are alternatives for their appropriate stage, not a required sequence. Check parsing is owned by `tools/lkjscript-dev/src/check/mod.rs`. Use actual dispatchers and `docs/release.md` for target build/admit, package and transferred pair arguments. Do not invent CLI flags or treat a clean-tree `check changed` as proof of a committed campaign.

The installed acceptance must execute the unchanged full owner against the exact packaged/installed runtime in the cleared environment, outside the language checkout. Retain literal requests, plans, relevant outputs and original child receipts, including numerical and transaction witnesses. Require terminal child exit, strict owner rereading, completed cleanup, both independent route lifecycles, and the one full suite legitimately bound within that pair. Do not run a second identical full suite for latest merely because its bytes are independently acquired; preserve the maintained within-pair binding rule.

### Acceptance map

| Material claim | Decisive evidence and failure discrimination |
| --- | --- |
| The mismatch is corrected | Actual source/full, target and transferred call sites select the one 3,600-second offline allowance; workflow assertions cover both 120-minute application jobs. Other limits and coverage remain deliberate. |
| A provisional result cannot manufacture success | Short supervised tests retain an intermediate success-looking record but time out or cancel before terminal exit; parent and pair fail, preserve reason and join owned descendants. A zero exit with corrupt evidence also fails. A fresh healthy control succeeds under the same supervision. |
| The complete useful workload passes at its real boundary | Fresh exact-source target and packaged/installed observations with all existing behavioral requirements. Record both inner workload and whole supervised process durations; confirm the final original reader, summary and exit occurred. No claim that the two timing fields cover the same interval. |
| No compatibility or semantic coverage is lost | Maintained full/target inventories, Artifact 21 consumer and authentic predecessor cases; retained numerical composition/edit/restart and transaction visibility witnesses. Genuine old bytes stay unchanged. |
| Retained evidence can support its claims | Authenticated exact handoffs and matching original readers, plus the inherited source-bound omission/interruption/receipt-fault obligations below. Missing original requests/plans must reject even if enclosing hashes are recomputed. |
| Users can obtain and use the milestone | Normal integration; one immutable release; actual anonymous exact/latest acquisition, integrity/attestations, installed behavior and final original-public-pair admission. A dispatch, green build or upload alone fails this criterion. |

Complete the predecessor's still-unrun source-bound target omission and installed/transferred fault obligations using **genuine final-source evidence**. The preceding ending identifies these maintained test entry points and environment contracts:

- `release::admission::nominal_tests::live_nominal_receipt_omissions_reject_at_target_admission`, with `LKJSCRIPT_NOMINAL_ADMISSION_FIXTURE` selecting owned restored target originals.
- `live_pair_interruptions_preserve_failures_before_fresh_recovery`, with `LKJSCRIPT_PAIR_REHEARSAL_ROOT`; then one genuine fresh healthy `pair-run` recovery.
- `live_pair_receipt_fault_matrix`, with `LKJSCRIPT_PAIR_FIXTURE_ROOT`, and transferred `live_receipt_fault_matrix`, with `LKJSCRIPT_TRANSFERRED_FIXTURE_ROOT` selecting that genuine full-suite evidence.

Verify these names/contracts in current source before invoking. Reuse a qualifying final-source healthy recovery for the reader matrices instead of repeatedly executing applications. If a matrix requires the documented local layout, construct it from authenticated package/verifier originals: `verifier/`, independent `exact-assets/` and `latest-assets/`, genuine preparation `release-receipt.json`, and `pair/`. Preserve hosted path/environment bindings when rereading hosted receipts; do not edit JSON paths to imitate another execution. Distinguish archived-context rereading, fresh local simulated execution and fault-test results. No new exhaustive fault framework or arbitrary case-count target is selected.

Retain unfavorable elapsed/resource observations and exact source/verifier/candidate/workload bindings. Reuse prior evidence only for claims its unchanged bindings still establish. Existing full requires fresh evidence, and the current publication protocol runs its own fresh proof; do not quietly bypass those requirements. Avoid repeating successful expensive work after reporting-only commits or to bind a report to its own hash.

## 5. Integration, publication and finite return

The completion condition is the corrected acceptance path **and the already-due combined public milestone**. There is no new release deferral.

After acceptance, refresh actual permissions/protections, divergence, immutability, version occupancy and competing publication attempts. Integrate the accepted corrective source normally so the frozen release SHA is reachable from main; preserve unrelated descendants and old frozen refs. The default-branch SHA, tested/frozen source and later reporting commit need not be identical, but their roles must be explicit.

Follow `docs/release.md` and the actual workflow. The existing administrator check of enabled immutable releases and the non-secret `LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA` selector are part of the maintained publication protocol. Reconcile its prior owner and active attempts; perform the authorized scoped selection/readback only with actual permission, without changing the immutable-release setting or permissions. Create the exact annotated tag for the accepted source and publish once. Do not push a tag before its automatic effects and prerequisites are understood. If v0.1.38 has been consumed by legitimate intervening work, reconcile that work first; choose the next unused additive version only where a real new corrective release remains necessary, keeping package/version/generated metadata consistent.

The publisher must complete its own required full/target/package/installed proof, immutable publication, and anonymous exact/latest installed acceptance. Retain originals promptly and close the matching original-public-pair reader, including the formerly omitted requests/plans and numerical child evidence. Do not relabel simulated acquisition as anonymous download or resurrect v0.1.36's missing originals. An immutable but failed successor is recovered additively, never overwritten.

Only a **concrete material F64-specific correctness/compatibility blocker**, not this deadline mismatch or pending CI, may trigger the inherited fallback: retain F64 work separately and release the already accepted requirement/structural/transaction capabilities plus necessary launch/retention repairs from a coherent pre-F64 successor with full applicable proof. Name that changed outcome and exclusion honestly. Do not withdraw F64 merely to fit the old deadline or silently leave its accepted companion unpublished.

While a remote job runs, complete useful independent work such as original-reader preparation, bounded fault tests, release notes and concise reconciliation. Do not watch indefinitely, poll unchanged status/logs, hide polling in scripts, cancel healthy work or rerun unchanged failures. Inspect at meaningful work boundaries. If only external completion remains, end with exact source, run/attempt, observed state, missing gate, retained originals and the next concrete resumption action. A short bounded wait is permissible only when observed progress plausibly makes completion imminent; it is not a loop. No unattended monitoring is promised.

The supplied root candidate preserves the incumbent mission, graph/authority distinctions, safe startup, source-verified commands, no-Python contributor policy, proof boundaries and non-polling delivery practices, with wording consolidated. **It deliberately adds no incident-specific deadline, run identity, acceptance matrix or new governance layer.** Reconcile any legitimate intervening guidance rather than replacing it mechanically.

Keep `docs/status.md` concise: replace stale pending claims with the actual failed rehearsal and current accepted/pending boundary, and link this campaign for evidence. Reconcile the roadmap's consumed delivery trigger without copying the growing chronology into it. Preserve the initial mandate text unchanged and append concise records with actual actions and evidence.

The final Codex report must separately state engineering, verification, integration, publication and public acceptance. Include actual revisions/tree relationships, changed owners, what users can now do, compatibility impact, fresh versus valid-reused/failed/skipped/unavailable proof, original artifact bindings, cleanup and preserved unrelated work. For incompletion, identify the exact blocking gate and next resumption action, not generic “CI pending.” The most consequential current finding is that accumulated useful-program verification is inconsistent across execution paths; a successful inner receipt is not a completed invocation, and the observed operational mismatch does not justify reimplementing the language capability.

---
End of initial mandate. Append subsequent records below; do not rewrite the initial text.

## Startup reconciliation — 2026-09-16 UTC

Actual startup is `b5224018b282ea642e7c6d45c483640ff50782de`, tree
`528d317a7e2b6bacb6491ca953e2cc8dd7ddcaa7`, on the F64 cutover reporting branch.
The only worktree inputs were the supplied root replacement and initial mandate. The initial
archive's SHA-256 is `91c48c4cf4cf5ac2f696cc22ad00ec7402c7f4e7fc02c48abe3c736c10ba2092`;
the installed root candidate's is `54b276b7dff34caa522e66485ed32b381ac5c1f91513b3b32ebfd72fd3a4d710`.
No archive collision or narrower guidance was found. The root changes consolidate the incumbent
instructions and preserve their guarantees; no intervening guidance required reconciliation.
Mutable work continues on `codex/full-lifecycle-202609161630`, preserving both predecessor
endings and all frozen branches.

Fresh fetch finds main still `0b6a9c08017fa7565408acfda10d3ce3b3c40c04`, an ancestor six
commits behind startup, with no divergent work. The August 31 stash and the separate frozen
recovery worktree remain untouched. Pinned Rust/Cargo 1.98.0 is available in
`/home/coder/.cargo/bin`; the default shell PATH initially omitted it. Startup has 663 GiB free
disk and approximately 8 GiB available memory, with no active Cargo or release process.

Authenticated reads confirm `35059467604/1` is the latest release run and has failed;
there is no active competing release attempt. v0.1.38 remains unoccupied. Latest public release
is immutable v0.1.36 (`388927116`); v0.1.35 remains the last fully closed public milestone.
Repository permission is admin/push, main is unprotected, no rulesets are active, and release
immutability is enabled. The existing publication selector still names failed v0.1.37's tag
object `51ecaabf3a6dfbaef0f842e95c4fc480dced0b6d`; no setting or selector was changed.
These observations require refresh before integration or publication.

All five original ZIPs listed in the mandate were acquired through authenticated artifact-ID
endpoints into `.artifacts/full-lifecycle-202609161630/originals/`. Each exact length and SHA-256
matches both the mandate and newly retained API metadata. Run/jobs metadata, unmodified ZIPs,
extracted originals and the original verifier are retained. The verifier's 32,892,104 bytes match
`52b839684f9024c3ce881e1c46b61328b4b1a5ba3bfee63868bae58720d3b14b` before execution.
Authenticated inspection is distinct from the original-reader admission recorded below.

The retained aggregate confirms distributed/outbound passes, offline timeout, and pure-tail/
stateful children not run in this pair. The pair failed with completed pair cleanup; the aggregate's
offline cleanup field is false, while the child's provisional receipt records cleanup complete.
The observed 1,797.512640198-second child interval ends before evidence enumeration, publication,
self-admission and exit; the complete child was killed at 1,800.016522592 seconds. No terminal
success or exact interrupted instruction is inferred. The earlier target's complete invocation
passed. This is an operational allowance mismatch, not evidence of an F64 semantic defect or a
controlled performance regression. No long reproduction is selected.

Inherited capability/launch/retention work is complete and preserved. The stale Artifact 20
consumer correction is complete; its source-bound hosted full/target/package success remains
valid for that old source. Corrected final-source proof, omission/interruption/fault obligations,
normal integration and selected combined publication remain actionable. Separate v0.1.37 recovery
is superseded by this successor, while its immutable tag and failure history remain preserved.
Independent bounded policy, diagnostics/tests and original-reader preparation tasks are delegated;
the primary integrator owns review, builds, commits and delivery.
