# Complete persistent ordered-map execution and deliver it to main

Issued: 2026-09-19 19:22:58 JST (Asia/Tokyo).
Repository: https://github.com/lkjsxc/lkjscript
Architect baseline: `d77fb8bafab8a5a92b4d5251e62a9c44a41aea82`.
Initial remote observation: 2026-09-19 10:07 UTC. After the human's pull, local HEAD, origin/main and a fresh remote-main read all agreed at 2026-09-19 10:19 UTC.

## Execution request and completion boundary

Execute this mandate as the responsible implementer, verifier and integrator. Finish the existing ordered-map experiment as a useful runtime improvement: ordinary authored programs and transported libraries must build and update substantial immutable maps without copying or charging for the whole existing map on every insertion or removal. Retained versions must remain correct.

The concrete completion witness includes the existing public operations, a transported generic aggregation library using caller-owned records, and the 4,096-key program in the appendix succeeding through bounded project `run` under unchanged default policies. Preserve ordered content, exact type/provenance admission, evaluation order, cancellation, cleanup and canonical bytes. A storage-only prototype, lowered accounting without reduced copying, or a private host implementation is not this outcome.

Use the outstanding `verify/202609191648-sustained` lineage after refreshing ownership and remote state. Complete fresh acceptance and observe normal integration into remote main. There is no deadline or minimum session duration, and the predecessor's suggested 120-minute horizon is not a requirement of this mandate. Reserve capacity for integration and recovery.

Normal development, owned disposable tests, bounded independent delegation, commits, configured verification CI, non-forced main integration and safe cleanup of integrated campaign references are authorized within actual permissions and protections. Make routine engineering decisions without renewed human approval. Production deployment, destruction or migration of operational data, billing, credentials, access controls, protection bypasses and rewriting published history are outside scope. An explicit authorization/security denial must not be bypassed through another client.

Publication disposition is **mainline-only**, as detailed below. Communicate progress and the final handback in Japanese; maintain code and technical records in English.

AGENTS.md: unchanged

## Starting evidence and inherited obligations

These identities are observations, never reset targets. Refresh material facts before editing and again before integration.

| Work | Observed disposition |
| --- | --- |
| Callable input evolution | Accepted and integrated before this campaign; `set.parameter-type` is already implemented. Do not reimplement it. |
| Opt-in pre-main verification | Accepted at `bde2b283eb891e9e57db6db38ad85a7341592f27`, run `35413869613/1`, and contained by inspected main. |
| HTTP response correction | Accepted at `ffd155350d7f294ab5d2618a5a7784eb0c493213`, run `35422659608/1`, and contained by inspected main. The earlier formatter failure remains genuine history. |
| Ordered-map experiment | Actionable, unintegrated. Branch tip `552b00b085677e4b03cd4632e51730f171cf4385`; experiment source `ee442c07e6e21a368b85c5b13f77e96d5c2a3b18`. Four selected storage tests passed in `35434916472/1`, job `105875832455`; the workflow failed at formatting. No full or copied-product acceptance occurred. |
| Transient development transfer | `35430345691/1` succeeded, but transferred development inputs are not semantic acceptance. |
| Public release | Immutable `v0.1.38`, source `7083f9a6d56ed702017942e100c3696fc6f35308`, remains the latest observed public release. Its hosted publication/public checks completed. Historical later manual-reader gaps do not reopen that release. |

Read the [map predecessor ending](https://github.com/lkjsxc/lkjscript/blob/552b00b085677e4b03cd4632e51730f171cf4385/docs/campaigns/202609191827.md#L149). Its earlier local transport failures and tool safety-verification rejection describe that execution, not a demonstrated GitHub protection failure. The present architect session could use local shell, Git and gh read operations; it made no repository write attempt. Re-establish the actual permitted implementation path, without treating either observation as blanket authorization.

The predecessor mentions unreferenced partial-cutover tree `8b633f8b08ea2ee6c88f84461cf27594d6879336`. It was not compiled, committed or accepted and has no assured retention. The branch's committed prototype is the reliable starting asset. Inspect any recovered partial work before reuse.

The local checkout was initially clean on `d4e8ae113c86346d7c67d629ecdb8ec5d7e5458b`, five commits behind. The human pulled during investigation; at 10:19 UTC it was clean on main at the baseline above, with one worktree. An unrelated stash, `On main: preserve incomplete campaign 202608310137 before 202608311331 cutover`, remains and must be preserved. No narrower AGENTS.md was found. Rust 1.98.0 is pinned and installed under the user's Cargo/rustup location, although cargo/rustup were absent from this shell's PATH. At startup roughly 10 GiB memory and 833 GiB disk were available; recheck before expensive work.

Remote reads found no open PRs, queued jobs or running Actions jobs. Main reported unprotected and the ruleset list was empty; these are observations, not permission to bypass future rules. The two earlier verification refs remain even though their changes are integrated. Retain or safely retire them only after checking tips, evidence and dependencies; the sustained branch contains unique work.

## Why this objective wins

Three plausible choices were compared:

- More precise parameter/port inspection could improve signature-edit workflows; the preceding input-evolution campaign records discovery friction. It does not establish a stronger current blocker than the executed map witness.
- Another delivery campaign is unnecessary now: the maintained pre-main route has completed real full acceptance and the selected release-cutover/read-only-consumer obligations are already closed.
- Completing persistent maps addresses a confirmed whole-map-copy path and a fresh product-visible limitation while reusing worthwhile unfinished engineering. It enables ordinary indexing, grouped totals and evolving session dictionaries without adding application-specific compiler behavior.

At the inspected main, [NormalizedValue stores Arc<BTreeMap>](https://github.com/lkjsxc/lkjscript/blob/d77fb8bafab8a5a92b4d5251e62a9c44a41aea82/src/platform/execution/normalized/value.rs#L107). Both the [checked VM editor](https://github.com/lkjsxc/lkjscript/blob/d77fb8bafab8a5a92b4d5251e62a9c44a41aea82/src/platform/execution/normalized/vm_checked.rs#L427) and [checked reference editor](https://github.com/lkjsxc/lkjscript/blob/d77fb8bafab8a5a92b4d5251e62a9c44a41aea82/src/platform/execution/normalized/reference_checked.rs#L478) clone the complete map. Their intrinsic dispatchers charge the complete resulting collection. Growing a dictionary therefore entails quadratic cumulative copying and charging. The branch's new carrier is only registered under `#[cfg(test)]`; it currently improves no product execution.

### Fresh architect probe, with its limits

A copied existing executable outside the checkout authored, planned, applied, checked, built and ran the appendix program in a fresh command project. Its SHA-256 was `e82f8ed604fe65681b971f047c69283e033a3ea6f3b8c685bc77d4f840bc41f9`, matching the candidate identity recorded for input-evolution source `527341022d53c5abce69b9591e61ea5a4896a529`. This was **not** a new build of inspected main. Git inspection found no intervening normalized-runtime change in the five commits pulled during this investigation.

- Project `run count-keys --arguments '[1024]'` returned 1024, reference/production equal. Each evaluator reported 524,800 cumulative collection items; production reported 63,988,466 allocated bytes.
- The same project command at 2,048 returned exit 4, `normalized_collection_items`: cumulative collection-item budget exhausted.
- Artifact foreground execution at 4,096, with an empty environment except LANG/PATH and no cumulative quotas, returned 4096 with joined cleanup. It reported 8,390,656 collection items and 1,009,734,386 cumulative allocation-accounting bytes. One observation separated 36,109,016 ns preparation from 132,070,530 ns invocation.

These counters are accounting units, not measured peak heap usage. The timing is one observation, not a speedup claim. It establishes the distinction between bounded project execution and trusted foreground execution. No candidate runtime or candidate speedup was tested in this architect turn.

Original executable, literal request, disposable project/artifact and stdout logs were retained outside the repository at `/home/coder/workspace/architect-deliverables/lkjscript/evidence/map-probe-20260919T101322Z/`. This mandate includes the reproducing request body; those local files are supplementary and must not become a required missing attachment. Establish an exact baseline/candidate binding for implementation measurements.

## Required contract and selected design

### Meaning and authority

Keep existing map types and standard operations: length, lookup, contains, get-or, insert, remove and ordered entries. Preserve the current primitive key domain and its exact order, including existing StaticText treatment; do not add F64, arbitrary nominal or user-comparator keys. Preserve replacement, absent-key, duplicate-input and trap behavior separately at each existing boundary. Map literal evaluation order and strict argument evaluation must not change merely because storage is sorted.

Maps remain immutable values. Retaining a predecessor, updating either branch, replacing a value, and removing existing or absent keys must leave every other retained version unchanged. Nested values and retained callable values must preserve their current type, origin, effects and capture guarantees. Sharing never grants a deployment capability or makes a task callable pure.

Keep independent construction-controlled value admission in the VM and reference evaluator. The neutral carrier must contain no authoritative type, affinity or provenance certificate. Raw ingress, adapter results, captures and retained values still undergo the appropriate complete admission, including late invalid children and unencodable results before effects. Do not replace independent checks with the other evaluator's validator.

### Runtime mechanism

Complete the existing **persistent ordered AVL carrier with shared immutable entry handles**. Its bounded path copying is a reasonable selected mechanism given the independent BTreeMap experiment and existing persistent-list architecture. Updates must share untouched entries and subtrees; `Arc::make_mut` on a whole BTreeMap does not satisfy the retained-version requirement.

Use one normal runtime carrier, with bounded bulk construction at raw ingress where needed. Move owned keys/payloads during construction rather than repeatedly cloning them. A temporary flat builder may be a local construction detail; a permanent lazy flat/persistent dual runtime with repeated whole-map conversion is not selected. Preserve semantic iteration over ordered entries, independent of AVL shape and sharing.

Review and complete every actual producer and consumer, including:

- `value.rs`, the carrier, checked constructors/projections/edits, and iterative raw-value disposal;
- `vm.rs`, `vm_intrinsics.rs`, `reference.rs`, `reference_intrinsics.rs`, and both independent checked-value modules;
- JSON and typed-data ingress/egress, including `data_codec_reference.rs`;
- HTTP query maps, retained session state, captures, equality and value-work/admission traversals.

Trace callers of the old raw intrinsic paths around main's `vm.rs:3020` and `reference.rs:3802`. Update necessary paths and remove genuinely obsolete alternatives; do not leave a hidden whole-map copying path. Rust representation changes are permitted with all actual callers updated. Product syntax or format changes are not required by this representation work.

### Accounting, failure and lifetime

Replace whole-result update charging with a documented schedule for newly reserved nodes, entries, owned key buffers, reference-count storage and necessary transient work. Check accounting and cancellation before allocation/growth and before exposing the successful result. Charge copying at its actual owner; do not silently allocate a key or clone a payload before its reservation, and do not charge a shared existing payload as a fresh deep copy.

Each evaluator owns its ledger. Preserve cumulative no-refund accounting for work that actually occurred, while a refused reservation cannot install partial output. Observation saturation cannot authorize work or become quota exhaustion. Storage observations are not global allocator measurements.

Preserve the existing finite value-admission boundary, currently depth 256 and 1,000,000 aggregate collection items, and applicable single-allocation limits. The prototype's one-million-entry constant must be reconciled with those maintained owners, not adopted as an independent product rule. Finite input/container admission is distinct from cumulative fuel. Keep existing project defaults and trusted foreground's absence of cumulative quotas/deadline; do not make the witness pass by raising budgets or imposing a new trusted-execution budget.

A failed allocation reservation, cancellation or malformed raw value must preserve preexisting versions and leave owned resources joined/released. Cover destruction of deeply nested rejected raw values, shared subtrees, partly built trees and failed rotations/bulk construction. A balanced tree alone does not prove stack-safe disposal of its payloads.

### Compatibility and discretion

Keep graph, type, package, compiler, bytecode, artifact and application-data encodings/identities unchanged for unchanged meaning. JSON map decoding continues to accept unordered unique pairs and reject duplicate keys; encoding emits the current sorted pair representation. Typed-data decoding retains its stricter rejection of duplicate or non-increasing keys. No migration or version bump is selected. Different insertion histories must serialize the same logical map identically. Runtime allocation observations may change, and some programs may now complete within existing optional quotas; document those observable consequences honestly.

Codex may revise the AVL mechanism if measurements or correctness evidence contradict it. Record the failed premise, replacement, preserved guarantees and revised proof concisely at the campaign owner. Do not require another human decision for that engineering choice; do not quietly reduce the promised public outcome. Safe Rust, existing production lints and product-surface/no-Python gates remain.

Non-goals: new map syntax or operations, generic key constraints, new collection families, application-specific intrinsics, transaction redesign, graph-store maps/catalogs, operational-data compaction, AOT/JIT, broad verification/release redesign, deployments and additional platforms.

## Public program and consumer closure

Implement one ordinary, transported graph library with the following contract (descriptive notation, not new syntax):

`totals-by<Item>(List<Item>, Function(Item)->Text, Function(Item)->I64) -> Map<Text,I64>`

Use existing standard list folding, map get-or/insert and checked integer addition. Fixing the key type to Text deliberately avoids inventing a generic key-constraint campaign. The importing consumer supplies its own nominal event type and its own key/amount functions.

An independent small oracle is events `("b",3), ("a",2), ("b",4)`, yielding ordered content `[["a",2],["b",7]]`. Retain that map, branch by replacing a with 10, then remove b from the branch. The original stays `[["a",2],["b",7]]`, the intermediate is `[["a",10],["b",7]]`, and the final is `[["a",10]]`. Exercise missing lookup, fallback, repeated replacement and absent removal according to existing contracts. Also exercise values with nontrivial nested payloads in the focused runtime proof; scalar totals alone cannot establish absence of old-payload copying.

Author the producer and consumer through literal ordinary plan/apply requests with a copied candidate outside the checkout. Export, stage and bind exact package transport; build and run the consumer after the disposable producer is unavailable. Exercise a reviewed library body change with an independently specified changed result, exact dependency replacement and retained old bundle behavior. This proves usable composition/evolution, not independent adoption.

Use the appendix's scalar-driven 4,096-key workload to avoid oversized OS argument strings. For a larger aggregation witness, keep input within current adapter bounds or generate deterministic records in graph code from a scalar seed. Never substitute a host aggregation implementation or raise adapter limits to accommodate the demonstration.

Extend existing maintained public/offline-package and runtime proof owners rather than introducing another top-level verification pipeline. Preserve literals and exact expected outputs. Run the maintained standard and lkjournal checks/lifecycles, including their map-using HTTP/session paths under existing owners. Their semantic assets should remain byte-identical if their meaning is unchanged; regenerate only through maintained public owners when a justified authored change actually requires it. Correct the stale status statement that map authoring is unavailable; executable discovery and structural authoring already expose it. Update only relevant status, performance and semantic/verification documentation.

## Checkpoints and acceptance

1. **Recover the lineage and establish the baseline.** Read current guidance and predecessor ending; inspect status/index/untracked files, remotes, divergence, stashes, worktrees, jobs and resources. Preserve the human's pull and newer work. Reuse the sustained branch, incorporate consequential newer main work normally, and restore the maintained Verify workflow before any integration candidate. Fix the known prototype formatting. Capture a source/executable-bound predecessor workload.
2. **Complete and challenge the runtime cutover.** Reuse the independent storage oracle; extend it to real payloads, ingress, failures and disposal. Wire the complete production/reference/adapter path and account it correctly. Advance only after structural sharing and the public count witness work together. If the mechanism fails, revise it now and delete abandoned experiment machinery; do not integrate a disconnected carrier.
3. **Close public composition and compatibility.** Complete the transported library, retained-version/evolution witness, canonical bytes, affected maintained consumers and matched measurements. Finalize docs and discovery only where outputs actually change.
4. **Accept and integrate the coherent candidate.** Freeze relevant inputs, complete the maintained fresh full profile, review the complete intended diff, integrate normally and independently observe remote main. Do not split representation and its required admission/accounting fixes into unsafe mainline checkpoints.

The following claims are required for integration; no publication acceptance is selected:

| Claim | Observable result and independent proof |
| --- | --- |
| Ordered immutable semantics | BTreeMap oracle and explicit expected contents across insert/replace/remove, all supported key kinds, empty/singleton cases, randomized branching histories and retained versions; assert AVL invariants independently. |
| Sharing actually removes copying | Inspect newly reachable node/entry sharing and allocation bounds independently of the carrier's self-reported counters. Retained-alias updates must scale with search/rotation paths plus new key/payload storage, not existing map cardinality. Include a negative control that makes whole-map copying detectable. VM/reference equality alone cannot prove this shared carrier. |
| Correct accounting and failure | Independently derived storage charges; successful exact-fit and refusing next reservation; failures throughout bulk construction and edits; cancellation; unchanged retained aliases, no partial result, healthy subsequent use and joined cleanup. Current finite admission boundaries remain. |
| Authority and safe reuse | Each evaluator independently rejects wrong key/value types, an invalid final child, foreign layout/callable origins and hidden affine values before subsequent callbacks/effects. Cover captures/retained maps and nested shared payloads. Do not replay live effects; use controlled adapters for ordering/failure checks. |
| Stack-safe ownership | Deep rejected raw maps nested through existing aggregates, shared/unique subtrees and late failures dispose safely under the maintained bounded-thread test owner. Existing aliases remain usable. |
| Byte compatibility | Fixed independently specified JSON/typed-data bytes and genuine predecessor fixtures agree across tree shapes/history. JSON accepts unsorted unique input pairs, rejects duplicates and emits sorted pairs; typed-data rejects duplicate or non-increasing input keys. Unchanged project/artifact/standard/application identities stay unchanged. Retained bundles execute after source removal. |
| Real product improvement | Fresh copied-product authoring plus transported caller-owned aggregation succeeds. Appendix count-keys at 4,096 succeeds differentially under unchanged project defaults; all meaningful entries/snapshots are also checked elsewhere, rather than relying only on length. Foreground execution remains unmetered unless explicitly configured. |
| Performance scope | Equivalent source-bound predecessor/candidate workloads, matching outputs and policies, small maps plus increasing distinct-key/update/retained-snapshot workloads. Separate preparation, invocation, encoding and process end-to-end costs; retain repeated timings, memory observations, physical work and unfavorable results. |
| Complete acceptance and delivery | Fresh dependency-complete full receipts at the final candidate, affected maintained consumers, restored maintained workflow, and separately observed remote-main ancestry/content. A selected subset or successful upload is insufficient. |

For performance, the prototype's 1,000/4,000/16,000 sizes are useful starting scales, not mandatory quotas or a universal capacity claim. Include replacement/removal and retained versions, not just monotonically growing unaliased maps. Compare equal optional policy settings; a predecessor quota failure is a usability result, not a timed successful baseline. Use trusted foreground or equal explicit policies for comparable completed execution. Preserve small-map overhead and any regression; no fixed speedup percentage is required, but evidence must establish eliminated whole-map update work and a useful public consequence. If end-to-end timing is dominated by preparation/encoding, state that instead of advertising storage timing as application throughput. No token/billing claim is authorized.

After the primary objective is accepted, the only conditional extension is linear checked `map-entries` projection if measurements show the current key-iteration-plus-lookup path materially limits the same workload. Preserve child provenance and ordered output. Do not let this extension delay otherwise complete main delivery or open a new collection campaign.

## Verification, integration and recovery

Use [docs/development-verification.md](https://github.com/lkjsxc/lkjscript/blob/d77fb8bafab8a5a92b4d5251e62a9c44a41aea82/docs/development-verification.md), [docs/spec/verification.md](https://github.com/lkjsxc/lkjscript/blob/d77fb8bafab8a5a92b4d5251e62a9c44a41aea82/docs/spec/verification.md), and the current Rust check registry. The following maintained forms were inspected, not run as acceptance by the architect:

```sh
cargo build --release --locked -p lkjscript-dev --bin lkjscript-dev
cargo run --release --locked -p lkjscript-dev -- check full --fresh --machine
```

Discover focused profiles/gates from `tools/lkjscript-dev/src/check/` for iteration. `check changed` selects current Git-status paths, not a commit range; a clean committed tree does not select this implementation's proof. Keep immutable executable copies and respect Cargo feature/output dependencies.

The outstanding branch replaces `.github/workflows/verify.yml` with an experiment-only test/fmt runner. Restore current main's maintained workflow, reconciling any intervening edits. At the inspected baseline its blob is `776c52f0b58cf678b896a438f21ee11003ebf147`; that is a comparison identity, not authority to overwrite newer guidance. No experimental workflow substitution may reach accepted main.

A normal push of the stable candidate to the existing `verify/**` branch selects exact event SHA for hosted `check full --fresh --jobs 2 --machine`. The workflow binds checkout/workflow source, uses a copied verifier, validates stable source inputs and original full receipts, and retains evidence for a requested 14 days. Require all currently selected gates fresh/passed, no reused/unrun/failed required gates, and a successful matching terminal. Do not hardcode today's gate count or treat the experimental run as full acceptance. A suitable complete local full run is also a supported route; do not run equivalent broad profiles twice merely for reassurance.

Before integration, refresh remote main, candidate identity, current rules and active jobs. Review and explicitly stage only intended paths. Incorporate intervening changes without discarding them and revalidate consequential interactions. Use normal fast-forward or merge-based delivery; if protections require a PR, finish the permitted merge. Do not first put unverified implementation on main to obtain its first meaningful test.

After an ambiguous write, read remote state before retrying. Independently confirm remote main contains the accepted source and record the actual SHA/relation. A changed merge/squash/cherry-pick tree is not the exact previously tested source. Treat later reporting-only changes separately, with proportionate checks, without relabeling earlier evidence.

Keep failed receipts/logs, exact inputs, candidate/verifier digests and source/run/attempt bindings. Hosted service expiry governs availability. Download needed authenticated originals before expiry; the map experiment produced only job logs, not an artifact. Missing evidence is unavailable, not a reconstructed historical pass.

Remove the predecessor runtime whole-map clone/update implementation after all callers are cut over. Keep BTreeMap as an independent test oracle or bounded construction detail where justified, not a parallel normal evaluator. There is no persisted-data migration. Recovery is an additive reviewed source correction or selection of a retained compatible executable/artifact, not rewriting history, touching live data or retagging an immutable release. Preserve the prior executable and fixed compatibility inputs needed to prove that recovery.

Safely retire campaign-owned refs only after confirming unchanged tips, integration or justified supersession, preserved unique work and ignored evidence, terminal jobs and no reference dependency. Never delete the unrelated stash. If blocked, retain this lineage and exact inputs; identify the gate, permission, failure source/run and next executable action. Integrate only an independently useful complete subset, never the unused storage experiment.

## Publication and durable handback

This campaign selects mainline delivery, with no version bump, tag, release-control write, release dispatch or deployment. No concrete installed consumer requiring this map improvement was identified. Publication becomes due when an actual installed consumer needs the accepted improvement or the human explicitly requests a release. No additional language feature is a prerequisite once that trigger occurs. Preserve any independently authorized publication already underway at startup.

The accepted HTTP correction and callable-input authoring increment remain source-only relative to v0.1.38. State that accurately. A main push does not update installed binaries. A later selected publication uses the current `docs/release.md` owner: finalized candidate acceptance, exact producer run/attempt, unchanged-asset promotion and separate anonymous public checks. New-format live publication is still unexercised in the inspected history; do not turn this runtime campaign into another speculative release redesign or legacy reconstruction.

Archive this file unchanged at `docs/campaigns/202609191922.md` before substantive implementation. Preserve both originals on a genuine filename collision using the repository's collision convention. Append clearly separated concise execution/resumption additions there; make it the sole current execution record. Add only a short continuation pointer at the existing `202609191827.md` ending rather than duplicating its history. A new mandate filename does not require a new branch.

At meaningful boundaries record objective/checkpoint state, actual source/branch, consequential design changes, original evidence and failures, retained work, exact blockers and next executable action. Keep binaries and logs outside tracked narrative. Update the concise maintained status/performance owners and link the campaign record.

While authoritative jobs run, preserve stable inputs and do useful independent work. Inspect at meaningful boundaries or for diagnosis; no polling loops, overlapping broad suites or promises of unattended continuation. If only an external gate remains, return its exact observed state, source/run/attempt, retained evidence, missing acceptance and resumption command.

The final Japanese report must explain what became possible, separate inherited closure from newly delivered behavior, give accepted source and observed remote-main SHA/relation, summarize independent proof and performance limits, state compatibility and publication disposition, explain retained/removed work, and identify any exact blocker. Report incomplete delivery as incomplete. The most consequential remaining uncertainty is end-to-end benefit with real retained composite values, not whether a test-only AVL tree can insert integers.

## Appendix: executed public baseline request

These command forms were exercised with concrete paths and values. `P`, the current revision and the plan token below are replacement values, not literal product syntax:

```sh
./lkjscript new P --template command --name map-probe
./lkjscript --project P change plan --input-file grow.lkjc
./lkjscript --project P change apply --input-file grow.lkjc --plan RETURNED_PLAN_TOKEN
./lkjscript --project P check
./lkjscript --project P run count-keys --arguments '[1024]'
./lkjscript --project P run count-keys --arguments '[2048]'
./lkjscript --project P build --output P/generated/application.lkja
```

Prepend `request base=CURRENT_REVISION` to the following body using the new project's returned revision. The body is the literal executed request; only its revision-specific header is omitted here:

```text
reference.package as=$std source=builtin
reference.owner as=$add package=$std class=declaration name=add
reference.owner as=$less package=$std class=declaration name=less
reference.owner as=$insert package=$std class=declaration name=map-insert
reference.owner as=$length package=$std class=declaration name=map-length
create.module as=$module name=map-probe
type.map as=@Map key=i64 value=i64
add.parameter as=$index function=$grow name=index type=i64
add.parameter as=$limit function=$grow name=limit type=i64
add.parameter as=$state function=$grow name=state type=@Map
expression.block as=$grow-body
  (if (call $less (local $index) (local $limit))
    (call $grow
      (call $add (local $index) (i64 1))
      (local $limit)
      (call $insert (types i64 i64) (local $state) (local $index) (local $index)))
    (local $state))
expression.end
create.function as=$grow module=$module name=grow visibility=private result=@Map effect=pure body=$grow-body
add.parameter as=$count function=$count-keys name=count type=i64
expression.block as=$count-body
  (call $length (types i64 i64)
    (call $grow (i64 0) (local $count) (map i64 i64)))
expression.end
create.function as=$count-keys module=$module name=count-keys visibility=public result=i64 effect=pure body=$count-body
type.function as=@Entry result=i64
type.argument parent=@Entry index=0 type=i64
create.component as=$component module=$module name=MapProbe visibility=package
add.port as=$port component=$component name=count-keys type=@Entry function=$count-keys
create.target as=$target name=count-keys component=$component port=$port runner=command
```

For the foreground probe, a separate copy of the generated command descriptor changed only its target from `main` to `count-keys`; the built artifact path and empty grants were retained. The executed command form was:

```sh
env -i LANG=C.UTF-8 PATH= ./lkjscript run --deployment P/map.deployment.json --arguments '[4096]'
```

Expected successful scalar result is exactly N for nonnegative N; the candidate's required project run uses N=4096. This appendix is a discriminating baseline, not a substitute for the complete map-content, library, alias, authority and compatibility proof above.

---

## Execution additions — 2026-09-19

Checkpoint 1: local main, origin/main and a fresh remote read agree at
`d77fb8bafab8a5a92b4d5251e62a9c44a41aea82`. The worktree and index were clean;
one worktree, no local build/runtime jobs, no open PRs or active Actions were
observed. Remote main reports unprotected and the ruleset list is empty.
The sustained tip remains `552b00b085677e4b03cd4632e51730f171cf4385` and already
contains current main; it is now the local tracking branch. The unrelated stash
and existing ignored application/package state remain untouched. Some historical
ignored Docker evidence directories are unreadable and remain preserved.
The unreferenced partial tree is unavailable locally; the committed AVL prototype
is the starting asset. Rust 1.98.0 is installed at the user's Cargo/rustup location;
9.8 GiB memory and 832 GiB disk were available before builds.

Inherited callable-input evolution, pre-main verification and HTTP correction are
integrated; their original acceptance remains historical evidence. Ordered-map
production cutover and full acceptance are actionable. No release is selected;
existing release jobs are terminal. The original failed storage experiment remains
a failure, with only its four selected tests passing. Root guidance is unchanged.

### Runtime cutover and intermediate proof

Restored Verify directly from freshly fetched main; its blob remains
`776c52f0b58cf678b896a438f21ee11003ebf147`. Independent bounded work owns the
neutral carrier/storage oracle, codec compatibility, and ordinary offline-package
witness. The integrator owns both checked evaluators, raw-host callers, accounting,
cross-owner review and integration. No whole-map runtime alternative is retained.

The source-bound clean predecessor is `d77fb8bafab8a5a92b4d5251e62a9c44a41aea82`;
its newly built copied executable has SHA-256
`a30ace674685bbef5d2b15dc5b6566640b77b041f5c540a66e6a896c62a54b74`.
Originals, literal inputs, artifacts and logs are under
`/home/coder/workspace/lkjscript-map-evidence-20260919/`. Frozen source worktrees
exist solely to bind predecessor behavior and measurements; they contain no
operational data. Encoding was outside the existing invocation timer, so a separate
predecessor observation source `36e541f6` adds only the same encoding timer used by
the candidate. The clean original remains retained; this is not a new predecessor
semantic acceptance or a changed artifact format.

An exploratory candidate (`development-lkjscript-1`, SHA-256
`0a5e58ba859b633c592eabf1242ff06de76655158bcf88169de90be711489bf3`)
completed the 4,096-key default project run with exact scalar/differential results
and 53,236 collection items per evaluator. It also executed a genuine predecessor
aggregation artifact with unchanged retained snapshots. It is **not** the final
source-bound candidate: source edits overlapped its build. Final authoring,
measurements and full acceptance remain required.

The initial focused map selection passed 55 tests (including independent pointer
sharing, exact storage charges, all-reservation failures, codecs and deep raw
disposal). A subsequent filter matched zero newly added evaluator tests and supplies
no evidence. The corrected module selection found an in-progress test compile error
(`ReferenceSignature::read` does not exist), being corrected through the existing
controlled reference-host interface. The first compile found two obsolete bulk
constructor calls in tests, since corrected. Independent review identified raw
cancellation/unused-fallback disposal and map-entry projection metadata accounting
gaps, repaired before freezing inputs.

The expanded map selection subsequently passed 62 of 65 tests; its three failures
were new test defects: an ambiguous concrete/generic external selector and a VM
result-placement reservation omitted from the expected cumulative prefix. The
corrected seven evaluator tests all passed in `vm-map-tests-3.log`. They require
evaluator-specific admission errors, exact-fit and refused reservations at distinct
owners, callback ordering, retained captures, finite boundaries and bounded-thread
deep cleanup. The failed originals remain retained. All carrier and codec tests
passed in the expanded selection. Initial workspace Clippy rejected a test byte
array spelling; it was corrected without changing its inputs.

### Frozen implementation and copied-product closure

Implementation source `a8d8877cb554497d8cd2c92c6db2f26b528ffdcc`, tree
`32046f643b1f586dde34ea0c6538e21867721978`, passed focused evaluator tests and
workspace/all-target/all-feature Clippy before the clean source freeze. Two
necessary release configurations are retained separately: the workspace product
(`5d306dc957490b1a9f733b83356f14ed775ec0de4b824823ab8bef7ab1ff826d`)
and verifier (`dd73f8428ea28918c60dc5d88efc141046ef9ac6abb18612417df730faea600f`)
serve maintained public verification; the product-only candidate
(`97ca340b19c8780847887b910798113bcec2e6e7ddc601f8b12417fdef963969`)
matches the predecessor's `cargo build --release --locked -p lkjscript --bin lkjscript`
configuration for performance. Builds took 227 and 194 seconds respectively;
these are build observations, not runtime performance claims.

`focused-public-a8d8877c/receipt.json` passed 52 ordinary public commands, four
exact transport inventories and its original-file reader, with joined cleanup.
Its SHA-256 is `be2b9df8dad2eb33d869bdc1619c7d7946aebc10d50bd0fde2bae8c2ff50b69f`.
The imported caller-owned Event aggregation, explicit small contents, 512-event
oracle, snapshots, missing/fallback/trap behavior, reviewed body evolution, exact
dependency replacement and both old/new bundles after source removal all passed.
The default project count at 4096 returned 4096 differentially: each evaluator
allocated 53,236 map nodes and 4,096 entry handles, copied 49,140 entry handles and
zero key-buffer bytes. Trusted foreground retained absent cumulative quotas and
deadline. This focused receipt is not full integration acceptance.

A copy of the architect's original scalar project independently reproduced the
predecessor's `normalized_collection_items` refusal at 4096 (exit 4), while the
workspace candidate returned 4096 under the same default project policy. Its
literal request SHA-256 is `1df3b5dd18367aab1557f22ffd872d2d6b93689824c100b3f4104d10d05c9381`;
the original artifact remains `789cf258cbe4ed3a37ffd2dcb99d01493b8f7d12794f3fbbc823ffb42dcf2fe6`.
These concurrent-build observations establish behavior and counters, not timings.
The original architect files were untouched. Fresh authoring is separately covered
by the maintained offline case.

Authenticated predecessor run metadata and job log for `35434916472/1`, job
`105875832455`, are retained outside the repository. They confirm four selected
passes followed by formatter failure; no artifact/full acceptance was invented.
Generated guides come directly from the copied product; only the new observation
fields and their common capabilities digest change.

### Matched composite measurements and acceptance freeze

All 88 foreground invocations (22 warmups, 66 measured samples) passed independent
complete output, artifact/policy and joined-cleanup checks. The retained-composite
4096-key workload executes the same 2,695,291 instructions; median invocation is
1424.203 / 416.690 ms and process elapsed is 1466.079 / 458.021 ms (predecessor /
candidate). Median process peak RSS is 53,140 / 49,676 KiB. These are observed
host/workload results, not a general speed or proportional heap-saving claim.
Small-map regressions and preparation-dominated encoding cells are retained in
[the performance owner](../performance.md#persistent-ordered-maps-2026-09-19).
No conditional linear `map-entries` extension is selected by these measurements.

`performance/summary.json`, its source/tool manifest, literal requests and all
samples retain the exact proof under the external evidence root. Both authentic
predecessor artifacts rebuild byte-identically with the candidate, including the
fixed JSON/typed-data fixture. No graph/package/compiler/artifact/data format,
default execution policy or maintained standard/application meaning was changed.

The maintained check owner rotates older managed runs. All existing check evidence
was copied with metadata to `check-evidence-before-full/` outside the repository
before invoking it, preserving earlier originals and failures. Next: freeze this
documentation/generated-guide descendant and run one fresh full profile with the
immutable verifier, then refresh main/protections/jobs and integrate normally.
