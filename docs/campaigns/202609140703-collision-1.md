# Codex campaign — Stable release candidates and completion of the callable-library milestone

- Repository: https://github.com/lkjsxc/lkjscript
- Timestamp: **2026-09-14T07:03:18+09:00 — Asia/Tokyo (JST)**, converted from the observed clock **2026-09-13 22:03:18 UTC**.
- Inspected default branch: **main**, revision **6b0c19beafafc40d859274dc057f6212372dc57d**.
- Intended initial archive: **docs/campaigns/202609140703.md**. Preserve this mandate unchanged and append concise reconciliation, execution and resumption records. If that path has appeared, retain both histories with an observed-seconds or documented collision suffix.
- Companion: the supplied complete replacement candidate for root **AGENTS.md**.
- Outcome: a fixed release candidate remains exactly the same source while main advances, passes the appropriate ancestry policy without losing its evidence identity, and delivers the already implemented named-reference and finite-callable/validator-repair milestone through the existing immutable and installed public-download path.
- Publication is **selected in this campaign**. The observed unused version is **v0.1.35**; verify availability before reserving it.

## 1. Starting point and reconciliation

Implement and prove this campaign. Do not return another architectural proposal. The architect performed read-only repository inspection and disposable investigation; your task includes implementation, ordinary integration and the selected repository release, subject to actual permissions and protections.

Inspect actual branch, HEAD, index/worktree, relevant untracked work, stashes, remotes, divergence, applicable instructions, toolchain/resources and active local/hosted jobs before editing. The inspected SHA is an observation, not an instruction to reset or rewind. Preserve unrelated work, including any retained predecessor artifacts and stash. Read existing applicable guidance, reconcile the supplied complete AGENTS.md with legitimate intervening changes, and install it before substantive implementation. Replacement does not reload the running session's instructions. Keep existing frozen release checkouts intact.

Read the ending/resumption sections of **docs/campaigns/202609140057-collision-1.md**, **docs/campaigns/202609131008.md**, current **docs/status.md**, **docs/roadmap.md**, **docs/release.md**, and the existing evidence owners named below. Resolve current facts rather than trusting their last pending labels.

### Observed state

| Boundary | Observation and required disposition |
| --- | --- |
| Current main | API and disposable clone both identify 6b0c19beafafc40d859274dc057f6212372dc57d. It directly follows corrected implementation 0735cc78e41fc77166ca4fff61d0b1b5392c851c and changes reporting/status/roadmap/release records. The existing narrative that normal main integration is still pending is stale: the corrected code is reachable from remote main. This does not establish verification or publication. |
| Named references | Source 5f49c68d55bdf1f620f1a3ae2fdd462cb02c2847 is integrated. Its record reports fresh full and copied-product acceptance. Its explicitly limited deferral was consumed by the finite-callable campaign. Carry the capability into this release. |
| Finite callable and repair | Implementation dff13968b978b9db9a2f1d7db469c9d18cfb0fcc and subsequent corrections are integrated. Focused copied proof at 0735cc78 is reported passing, including original accepted receipt/record IDs on idempotent retry. Required corrected-source full/target/installed/public proof remains open. |
| Earlier rehearsal | **34777722470/1**, source **9cf26ecde03be6b77b3dc9e7e58628f17cd23c66**, completed **success** at **2026-09-13T21:09:55Z**. Build/full/target/package and transferred packaged application jobs passed; publication/public jobs were skipped. Preserve this real success, but it predates the accepted-retry response correction and cannot certify the final source. |
| Corrected rehearsal | **34779065098/1**, source **0735cc78e41fc77166ca4fff61d0b1b5392c851c**, completed **failure** at **2026-09-13T21:10:08Z**. Job **103793246882** passed checkout, failed **Validate workflow and release identity**, and skipped toolchain installation, builds and all semantic/target/package checks. Later jobs were skipped. This was not a failing language test or a compiler timeout. Preserve it as failed, not pending. |
| Earlier delivered milestones | Immutable **v0.1.33** and **v0.1.34** remain closed predecessors. The latter is release **387929218**, publisher **34760117554/1**, source **037a13483321497737a30ade6df88950bb19e0de**. Its installed public pair is reported admitted with its original reader. Do not reopen or retarget FG-7/INS-7. |
| New publication | Latest observed public release is v0.1.34; no v0.1.35 publication was observed. Neither of the two finite-callable runs above was a publisher. Refresh tags, releases, matching jobs and the publication control before acting. |

The GitHub jobs API establishes the failing step, not its precise failing shell line. Direct job-log/annotation retrieval was unavailable to this architect. Nevertheless the current source and graph establish an actionable defect independently: the workflow requires `origin/main` to be an ancestor of the dry-run checkout, while main now descends from the frozen corrected candidate. The same restriction exists in the Rust release owner. The checkout also selects the mutable dispatch ref rather than the event's exact source commit. Confirm the original log once at startup if available; do not claim it was already read.

An isolated experiment executed the actual extracted workflow identity Bash in disposable Git repositories. Equality and a candidate descending from main passed; a frozen candidate preceding main and a divergent candidate failed with exit 1. Missing main returned Git exit 128. Advancing a fixture dispatch branch changed its resolved commit while the captured SHA remained stable. This establishes the source-level defect and distinguishes Git failure from a false ancestry relation; it does not identify the unavailable hosted log's exact failing command.

The architect did not build Cargo, execute lkjscript, download release assets or validate hosted receipts. Cargo and gh were unavailable in the inspection environment. Repository contents, Git history and authenticated GitHub metadata were available; public web-page retrieval failed. The isolated shell/Git experiment and source/API observations are not fresh product acceptance.

### Preserve useful predecessor evidence

The successful earlier rehearsal has real source-specific evidence. Save missing original handoffs before expiration when accessible; do not repeat its behavioral run merely to produce another copy.

| Original artifact | ID | ZIP digest | Expiration UTC |
| --- | --- | --- | --- |
| release-handoff-34777722470-1 | 10324623970 | sha256:6159355f2c603cd3bb0b962ab01ab5bfebbd3aff8d8aea9c1669e8595037e661 | 2026-09-14T20:40:27Z |
| verifier-handoff-34777722470-1 | 10325562168 | sha256:a6e43ddc009616cf855bf6f9ba7c329c20251c2ad62d1582aad29bc20a6e6e43 | 2026-09-14T20:40:29Z |
| pre-publication-application-evidence-34777722470-1 | 10325499525 | sha256:0b3a012b3d36a77bf2d7bf1ae11e1bbbdb75e7de5f1c12bb99e24594309eb1f6 | 2026-09-20T21:09:42Z |

Full and target artifacts are **10324706211** and **10324748300**, respectively. Refresh metadata before acquisition. Authenticate retained inventories, source/candidate/verifier identities and original context; use original readers for historical proof. Original-reader verification is rereading, not fresh execution. Unavailable old artifacts limit old claims but do not prevent obtaining complete current-source acceptance.

## 2. Decision, rationale and finite scope

**Before:** a corrected candidate can spend more than an hour queued behind an earlier rehearsal, then be rejected because main acquired a reporting descendant. A dispatch can also checkout a later commit when its branch moves, obscuring the relationship between GitHub's event SHA and the bytes tested. Contributor packaging duplicates the ancestry restriction. Merely rerunning the old job does not change its workflow logic.

**After:** a dry run tests its immutable selected commit. Both workflow preflight and the Rust release owner admit an integrated ancestor or an unintegrated fast-forward descendant of the observed main, with exact failure handling for divergence and missing history. All downstream proof remains bound to that candidate, not to a moving branch tip. The existing publication boundary remains stricter: release source must already be reachable from main and the exact annotated tag/control must bind it. The selected combined language milestone is then actually released and exercised through installed exact/latest public downloads.

This is a concrete development and delivery restriction, not an excuse to grow a release framework. The existing policy already allows frozen release ancestors while main advances, but its rehearsal admission contradicts that policy. Repair both owners and close the inherited obligation in one coherent campaign. Do not substitute a new publisher, cross-run artifact promotion system, caching certificate or generalized CI scheduler.

The strongest alternative is **operation-bearing requirement parameters for reusable libraries**. Current effect parameters forward allowances, but `CapabilityCall` and `Transaction` still carry concrete `RequirementReference` values. A transported library cannot yet own a fully reusable transaction/configuration algorithm parameterized by distinct compatible concrete requirements. That is valuable language work, but it cannot discharge the promised release and does not fix this repeatable delivery defect.

A second materially different direction is structured request blocks over existing typed intent; the current witness still requires flat expression/type assembly. Record count is not token-cost evidence. Neither direction is authorized by this mandate.

Short revisable outlook: finish this release; next reassess operation-constrained requirement parameters against a useful transported transactional library; reconsider authoring structure if an observed authoring workflow shows that assembly dominates. Large old-validator repository revalidation cost is not yet characterized and may change that priority if measured. Do not turn this outlook into a feature queue.

If startup shows the complete admission repair and public milestone already finished, verify the relevant evidence and close this campaign without manufacturing another change. If the failed log names another identity check, repair that check only when justified while still correcting the demonstrated moving-main and moving-ref defects. If a real semantic or reader failure appears in final proof, repair its exact owner and refresh affected proof; do not weaken the finite-callable, recovery or accepted-retry contract to obtain a release.

## 3. Resolved source and publication contract

### 3.1 Immutable selection

For a **manual dry run**, select the exact commit recorded by the workflow dispatch event. Checkout must use that full SHA rather than a branch name; assert that actual HEAD equals the selected SHA before installing the toolchain or launching expensive work. Branch names are discovery/dispatch handles, not source identity.

For a **tag-push publication**, retain the exact event/tag checks and verify that the annotated tag peels to the candidate commit. A moved or mismatching tag must not silently substitute source.

For **manual publication of an existing exact annotated tag**, preserve the supported recovery protocol: the selected product source is the validated tag's peeled commit, which may differ from the commit supplying the workflow definition. Do not incorrectly demand equality with the manual dispatch's event SHA in this mode. Record these identities separately where relevant; preserve the remote tag-object/control checks performed by the publication job.

A minimal workflow implementation can retain its existing mode-specific tag selection and use the event SHA in the non-manual-publish checkout branch. Codex may choose a clearer implementation, but all three modes and their exact-source assertions must remain explicit.

Every candidate-changing edit, including this workflow/tooling repair, yields a new candidate source. Do not label the new source 0735cc78 or relabel earlier evidence. A reporting descendant may describe a frozen candidate without becoming that candidate or forcing a circular report/retest loop.

### 3.2 Ancestry is admission, not source selection

Resolve candidate commit **S** and the available fetched main commit **M** to immutable commit IDs before evaluating the relation. Use the same rule in workflow identity preflight and `release::source_facts`:

| Relation between S and M | Dry-run admission | Publication admission |
| --- | --- | --- |
| S = M | Admit | Admit, subject to exact annotated tag and remaining gates |
| M is a proper ancestor of S | Admit read-only rehearsal | Reject until ordinary integration makes S reachable from main |
| S is a proper ancestor of M | Admit the frozen candidate | Admit, subject to exact annotated tag and remaining gates |
| Both share ancestry but have diverged | Reject; reconcile normally and select a new candidate | Reject |
| Disconnected history, missing objects/main ref, invalid source, or failed Git invocation | Reject with an accurate source/infrastructure diagnostic | Reject |

An ancestry result of exit status 1 means false. Missing objects, command failure or other errors must not be converted into a successful alternate branch. Fetch sufficient history using the existing full checkout; never weaken checks because a shallow graph cannot answer. Read the main reference once for a check and use its resolved ID. Later forward movement must not retarget S; divergence or rewritten history requires ordinary reconciliation, not force-push or silent trust.

Admitting a historical ancestor for rehearsal is **not** reusing another source's test results, claiming compatibility, permission to publish obsolete code, or evidence that it is the best release. Version/tag choice and campaign suitability remain explicit. Do not introduce a list of permitted reporting filenames to decide whether S is valid; exact proof is about S, and main may legitimately contain subsequent implementation too.

### 3.3 Preserve existing guarantees and data contracts

Keep release source cleanliness, repository identity, strict package/version/tag agreement, action/toolchain pins, exact target policy, candidate hashes, deterministic archive/manifest, authenticated artifact identities, original-verifier binding, immutable publication, and public pair acceptance.

The read-only rehearsal must not create a tag, draft, public release or publication-control mutation. The single publication job retains its scoped token, no checkout and no execution of repository binaries/scripts. Do not broaden permissions or change immutability settings.

This change concerns contributor release admission and workflow source selection. No new language expression, runtime daemon, installer behavior, Graph/witness/Artifact encoding, public authoring grammar or application-data migration is required. Keep existing manifests and receipts unchanged if their source fields already express the contract. If a genuine missing field is demonstrated, explain the smallest needed revision and preserve original-reader access to historical evidence; do not bump contracts as ceremonial cleanup.

Retain the completed language semantics: finite parameter-flow admission includes direct calls and named function values, all relevant type occurrences and complete supplier bodies at executable boundaries; semantic expansion rejection is distinct from preparation exhaustion. Historical accepted identity is distinct from current validation. Current-binary repair must produce a fully valid candidate without rewriting history, and accepted retries return the original persisted acceptance IDs.

## 4. Source owners and ordered execution

All links here bind **6b0c19beafafc40d859274dc057f6212372dc57d**. Follow moved owners after startup reconciliation.

- [Workflow](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/.github/workflows/release.yml): checkout, identity step, downstream commit outputs and isolated publication.
- [Release owner](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/tools/lkjscript-dev/src/release/mod.rs): `source_facts`, `ensure_clean_checkout`, `run_git_success`, packaging evidence binding and existing workflow tests.
- [Target admission](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/tools/lkjscript-dev/src/release/admission.rs), [transfer owner](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/tools/lkjscript-dev/src/release/transferred.rs), [installed pair](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/tools/lkjscript-dev/src/release/transferred/pair.rs), [release protocol](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/docs/release.md).
- [Callable analysis](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/src/platform/kernel/callable_flow.rs), [independent oracle](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/src/platform/kernel/callable_flow_tests.rs), [current validation](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/src/platform/publication/validation.rs), [upgrade tests](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/src/platform/publication/upgrade_tests.rs).
- [Raw reference admission](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/src/platform/execution/normalized/reference_schema.rs), [accepted-retry CLI response](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/src/platform/cli.rs).
- [Public finite witness](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/tools/lkjscript-dev/src/offline_packages/finite.rs), [literal producer](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/tools/lkjscript-dev/src/offline_packages/finite.producer.lkjc), [literal consumer](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/tools/lkjscript-dev/src/offline_packages/finite.consumer.lkjc), [predecessor repair witness](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/tools/lkjscript-dev/src/offline_packages/finite/predecessor.rs).
- [Predecessor lifecycle](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/docs/campaigns/202609140057-collision-1.md), [finite evidence](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/docs/evidence/202609140057-finite-callable.json), [named-reference evidence](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/docs/evidence/202609131008-typed-named-references.json).
- [Verification contract](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/docs/spec/verification.md), [gate registry](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/tools/lkjscript-dev/src/check/registry.rs), [changed-profile selection](https://github.com/lkjsxc/lkjscript/blob/6b0c19beafafc40d859274dc057f6212372dc57d/tools/lkjscript-dev/src/check/snapshot.rs).
- Live metadata: [failed corrected rehearsal](https://github.com/lkjsxc/lkjscript/actions/runs/34779065098), [successful earlier rehearsal](https://github.com/lkjsxc/lkjscript/actions/runs/34777722470), [latest observed release](https://github.com/lkjsxc/lkjscript/releases/tag/v0.1.34).

Execute in dependency order:

1. **Reconcile and reproduce cheaply.** Refresh the two terminal runs and remote main. Read the failed identity-step log if accessible. Reproduce the old predicate with an owned temporary Git graph containing candidate S and reporting descendant M. Also demonstrate mutable-ref selection by advancing a fixture branch after recording an event SHA. Do not dispatch a full workflow to discover these simple failures.
2. **Repair both source-admission owners.** Implement the settled truth table, exact dry-run checkout/assertion and useful bounded diagnostics. Keep Git error handling explicit. Reuse a narrow helper or module inside the current release owner if that improves clarity; do not create another release service or user-facing command merely for this patch.
3. **Prove mode behavior and authority.** Extend the existing release tests with real temporary Git fixtures and the actual workflow identity shell, or an equally direct executable test of that shell. Existing string-presence tests alone are insufficient for ancestry behavior. Check exact source selection separately from ancestry. Use independent expected relations from the fixture's known history; the Rust and shell implementations cannot certify each other merely by agreeing.
4. **Update maintained guidance and freeze.** Align docs/release.md with the resolved contract and startup/resumption sequence. Correct stale main/pending claims in status/roadmap/evidence with appended observations, preserving earlier failures and their timestamps. Keep campaign details out of root AGENTS.md. Complete any affected generated assets through their existing owner; no regeneration is required solely to change unrelated metadata. Commit the final implementation, workflow, normative/procedure and necessary consumer inputs before expensive proof.
5. **Run one corrected candidate through existing acceptance.** Use an isolated full clone if required by the release owner's real .git-directory requirement. Check matching jobs and inputs before dispatch. A new dispatch must select the corrected workflow source; rerunning 34779065098 uses its old inputs and is not the repair. Keep the selected candidate commit and its inputs fixed, with all receipts bound to that commit. Branch movement must not retarget it. Independent reporting/main progress may continue under the corrected contract.
6. **Admit evidence, integrate and publish.** Re-read final full, target, package and transferred/installed-pair evidence with the correct original verifier and authenticated context. Complete inherited relevant receipt-reader omission checks using retained evidence, then perform ordinary integration and the selected annotated-tag/scoped-control publication protocol. Finish anonymous exact/latest installed acceptance and original-reader admission. Do not stop at a dry-run success when the publisher is ready.

Independent source-policy tests, guidance review and preservation of old evidence may run in parallel with explicit file ownership and one integrator. Do not add speculative feature work while waiting. Preserve useful completed work if a real external dependency blocks the last step.

## 5. Acceptance and verification cost

The useful program being delivered already exists: a separately authored transported generic library alternates typed task callbacks with simultaneous type/effect substitution; a consumer supplies concrete configuration/data requirements and owns transactions. It also exercises finite reset, dependency replacement after producer removal, and historical-validator repair using the current executable. This is a designed composition witness, not a claim of external adoption. It still cannot parameterize concrete capability operations or own a generic transaction requirement, and it does not establish large-repository upgrade performance.

Use the existing public owner and literal requests. The witness must remain authorable through the candidate executable outside the checkout, then stage/export/import/check/build/run through the installed candidate. Host drivers may observe and orchestrate; raw graph edits, hidden member-ID generators or contributor-built accepted state cannot substitute for product authoring. Retained hostile artifacts and authentic predecessor fixtures have their explicitly labeled negative-test role.

| Material claim | Decisive workload/oracle | Required failure/boundary evidence |
| --- | --- | --- |
| Fixed source survives branch movement | Record S, advance a temporary dispatch ref to another commit, then exercise the selected checkout/identity path; actual HEAD and emitted source remain S | Wrong/missing selected SHA fails before expensive work; branch movement never silently changes the candidate. Preserve manual tag-source distinction. |
| Fixed source survives main advancement | Owned Git DAG with equality, main-before-candidate, candidate-before-main, divergence and disconnected cases; run both real source-admission owners against the independent truth table | Reject divergence, missing history/ref/object, wrong origin and command errors. Include the exact former reporting-descendant failure and a descendant with a real code edit, so acceptance does not depend on a filename whitelist. |
| Publication authority is unchanged | Existing workflow authority tests plus tag/source fixtures | Unintegrated descendant is not publishable; lightweight/wrong-version/wrong-target tag rejects; manual existing-tag publication still binds the tag source; read-only dry-run has no publication side effects. |
| Correction reaches every release boundary | One final source, its candidate hashes, full receipt, six target owners, deterministic package and transferred installed pair, admitted by their original readers | Earlier 9cf26ecd proof cannot satisfy 0735cc78 or the new corrected source. Wrong candidate/source/verifier/attempt and required-reader omissions reject. Do not rewrite receipt metadata to make it match. |
| Delivered language/repair milestone remains intact | Existing offline named/finite owner, independent callable oracle, raw source/artifact admission and authentic old-validator installed repair | Finite swap/reset succeeds; direct/named expanding closure rejects semantically; current-invalid history stays inspectable/repairable; retry after a later edit preserves original accepted IDs; failure/cancellation does not partially publish. |
| Users can obtain and use the milestone | Existing immutable release job, authenticated three assets/attestations, anonymous exact/latest installed pair and five-owner aggregate | Two route acquisitions/admissions and installed lifecycles remain distinct; latest aggregate reuse is only the established within-pair binding. Joined cleanup and current-binary repair are required. Publication without public acceptance remains open. |

Use focused Cargo release-owner tests while iterating. The following are existing source-verified entry points, not proposed new product commands:

```sh
cargo test --locked -p lkjscript-dev release::
cargo test --locked --lib callable
cargo test --locked --lib publication::upgrade_tests
cargo run --release --locked -p lkjscript-dev -- check focused --machine
cargo run --release --locked -p lkjscript-dev -- check full --fresh --machine
target/release/lkjscript-dev release target
gh run view 34779065098 --repo lkjsxc/lkjscript --json headSha,status,conclusion,jobs
gh api repos/lkjsxc/lkjscript/actions/runs/34779065098 --jq '{id,run_attempt,head_sha,status,conclusion}'
```

Do not execute every profile sequentially. Choose the relevant release-owner unit/integration filters and lint checks first; run semantic filters only if their owners changed or a specific uncertainty warrants them. The selected release already requires full and exact target proof, so obtain them once through the final existing hosted rehearsal rather than duplicating unrelated local full/target runs. The existing publication workflow has its own required fresh proof; preserve that protocol rather than attempting cross-run promotion in this campaign.

`check changed` currently selects from Git status, not a committed campaign range. On a clean committed checkout it is not evidence for the implementation commits. Do not use its narrow result to replace final acceptance; no changed-profile redesign is selected here.

List actual release tests before invoking ignored evidence-dependent cases. Retain and complete the finite-callable campaign's target-reader omission coverage through the current admission owner. Existing broader pair/transfer fault matrices may be reused only where their source/verifier and affected contract bindings remain valid; rerun affected cases if the repair changes their inputs. A reader exercise consumes retained evidence and must not replay application effects. No new permanent aggregate, fault ledger or test-count target is authorized.

Freeze copied binaries against concurrent builds. The old successful run's source/target times are observations of that run, not performance targets. This campaign claims removal of a false preflight rejection and exact source selection, not a measured CI or token-cost speedup. Keep missing semantic coverage, timeouts, failed source checks and external acquisition failures separate.

Final engineering acceptance is finite: both owners implement the truth table and exact-source selection; the discriminating tests pass; necessary docs are current; the inherited milestone remains intact. Selected delivery closes only when the final exact-source proof, normal integration, immutable publication and installed public acceptance are all admitted. Report partial states independently.

## 6. Integration, release and return

Archive this initial mandate unchanged using the established convention; append execution/reconciliation/resumption records to it and append closure observations to the relevant predecessor record. Update existing concise evidence owners rather than creating a new parallel release ledger.

Normal integration is authorized. Current remote main already contains 0735cc78 through 6b0c19be; reconcile later divergence instead of repeating an imaginary pending merge. Commit and push accepted owned changes normally, observing actual protections and automatic effects. Never force-push or bypass a required gate.

Publish the combined named-reference, finite-callable and validator-repair milestone after corrected proof. Retain v0.1.35 if still unused and consistent with product metadata. If another actor has reserved or published it, inspect that exact state first; reuse a matching healthy owner or choose the smallest genuinely unused additive patch for changed content. Never overwrite a tag or immutable asset, and never republish 9cf26ecd as if it contained the accepted-retry correction.

Use docs/release.md's supported protocol, including the existing **LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA** scoped control and its prior-value checks. That control selects the exact authorized release attempt; it is not permission to change repository security settings, credentials or immutability. Preserve the publication job's no-checkout/no-repository-execution boundary. Repository release does not authorize production deployment or operational-data changes.

Check matching runs and exact inputs before expensive dispatches. Normally inspect status initially and at a meaningful work boundary; further reads require changed state or a concrete diagnostic purpose. No indefinite watch, sleep/status loop or scripted polling. Preserve a healthy frozen ancestor and original-reader evidence while independent main work advances.

If only external work remains, end the session with the exact candidate commit, workflow run/attempt, observed state/time, outstanding gate, retained artifact identities/expiry and precise next read/resumption action. Do not call publication or public acceptance complete, promise monitoring, launch a duplicate, or invent a new capability to disguise waiting. If a failure is actionable, diagnose and fix it before settling for a pending handoff.

### Root-guidance changes

The supplied root candidate preserves canonical graph authority, typed review/publication, effects/grants/lifetime, current validation versus historical acceptance, safe editing, contributor ownership, independent proof and immutable publication. It routes detailed named-reference and finite-callable rules to their normative owners and removes repeated campaign-local narrative. It adds the concrete limitation of the current changed-profile selector and makes frozen-candidate/main separation explicit. These are operational corrections, not a hidden semantic redesign.

### Required final report

Respond to the user in Japanese. State:

- what limitation was removed and what users can now obtain;
- actual starting, implementation, final proof, integrated and release-source revisions, with any reporting descendant identified separately;
- the original failed run's actual cause if its log was obtained, distinguishing that from source-established defects and unavailable evidence;
- focused, full, exact target, package, installed/transferred, reader and public results, labeled fresh, valid reused, failed, skipped, unavailable or unrun;
- preserved language/data compatibility and remaining limits, especially requirement-parameter composition and unmeasured large historical-repository revalidation;
- normal integration, immutable publication, public acceptance and production deployment states separately;
- exact blocking owner and resumption action if any step remains open, plus the most consequential finding for the next architect.

Do not report a feature milestone complete from a tag, upload, green subjob or unadmitted receipt. Finish the selected work when its dependencies are available; leave an honest precise handoff only when the remaining dependency is genuinely external.

## Startup reconciliation — 2026-09-13T22:22Z

Actual local and fetched remote main are `6b0c19beafafc40d859274dc057f6212372dc57d`,
with no divergence or staged changes. The supplied root replacement is already installed,
SHA-256 `cac21ec5ee65972f0122e8fd8add263e087c7a2ee35760d71cffd68edfc900cf`;
comparison with HEAD found no intervening root changes requiring reconciliation. It is retained
as supplied before implementation. The existing untracked `202609140703.md` contains this exact
mandate (SHA-256 `a35052284aab8445c116ba943f10cd802ecf11bd026991255c51c7e313a0b5ef`).
This documented collision copy preserves its unchanged initial text and owns subsequent records;
the original file remains intact. The August 31 stash and all predecessor frozen clones/readers remain.

Authenticated terminal reads confirm rehearsal `34777722470/1` succeeded at
2026-09-13T21:09:55Z at `9cf26ecd`, while `34779065098/1` failed at
2026-09-13T21:10:08Z at `0735cc78`. The latter job `103793246882` passed checkout,
failed identity preflight and skipped toolchain/build/full/target/package work. Its raw job log
is now accessible: it prints the identity shell and exit 1 without command tracing, so it does
not itself identify the failing line. Main already includes the accepted-retry correction;
the inherited pending-integration narrative is superseded. The demonstrated ancestry/ref-selection
defects require a new source and new workflow dispatch, not a rerun of that failed source.

No hosted release job is active. Main is unprotected, repository rulesets are empty and push
permission is present. v0.1.35 has no remote tag or release; v0.1.34 remains the latest public
predecessor. Scoped publication control still selects annotated v0.1.34 object
`d96d1623495d8c13968d99f567ea77e79b158237`; it has not been changed.
Rust/Cargo 1.98.0 and required components are installed under `/home/coder/.cargo/bin`
(absent from the initial PATH). No local Cargo or product process is active; about 9 GiB
available memory and 690 GiB disk are sufficient for focused work. The task-authorized
parallel work has disjoint ownership: source-policy fixture tests, three maintained guidance
files, and ignored historical evidence retention; one root integrator owns source/workflow,
builds, commits and publication. No semantic feature or evidence-schema change is selected.

## Engineering and focused verification — 2026-09-13T22:26Z

The actual old workflow identity Bash was extracted from initial HEAD and run in an owned
temporary Git repository: candidate `ae1a86b1c81dac07702de262d1460801ef506ae1` before reporting
main `4ab3fbe46bf70a44b226b25a8efcede87ddfd7ba` rejected with exit 1. Advancing a dispatch
branch changed its checkout to the latter commit while checkout of the captured event SHA
remained the former. The fixture was removed; the literal driver and output are retained locally.

Workflow checkout now uses the event SHA except in supported manual existing-tag publication.
Identity preflight asserts the selected source before expensive work. Both it and Rust
`source_facts` resolve candidate/main commits once, admit either ancestor direction for dry runs,
retain candidate-reachable-from-main publication, and distinguish Git false from invocation errors.
Wrong origins and incomplete history reject. Manual publication still selects the annotated tag's
product source independently of its workflow-definition event commit. Publication token scope,
no-checkout/no-repository-execution isolation and remote tag/control checks are unchanged.

Fresh `cargo test --locked -p lkjscript-dev release::` passed 47 tests, with six existing
evidence-dependent cases explicitly ignored. The 12 new real-Git/actual-Bash tests cover the
independent truth table, reporting and code descendants, exact source/branch movement,
publication modes/tags, missing history/source/origin, Git invocation failure and immutable main
resolution, and read-only/clean-source behavior. The first run retained one fixture-only failure:
Git update-ref supports `-d`, not `--delete`; after that correction all tests passed.
Strict workspace/all-target/all-feature locked clippy passed; formatting and diff checks passed.
No language source changed, so separate callable/upgrade filters or local full/target runs were
not duplicated. Final hosted full/target/package/installed proof is still required.

The five successful predecessor artifacts are authenticated and retained under the existing local
evidence tree. Original verifier `2a655facbea3e417760c5a3e5c3dd2846ec7752437fa71cac015e6a605ce664d`
passed package, six target readers and the installed simulated pair with all five children after
archived path/mode/kernel context reconstruction. Full receipt SHA-256
`3e531e6bb4f4ef3c12086aa328a95f56ec52ca6b4b6cfd9828ed40d076eab179` authenticates 26 fresh gates
at `9cf26ecd`, bound by original preparation. Its raw full reader is private to `release prepare`,
which also executes lifecycle work; no new prepare was run merely to reread it. These are
historical authentication and original-reader observations, not final-source proof or effect replay.
The inherited target omission matrix is being exercised through its existing evidence-dependent
test; final-source admission will require the final source's fixture and reader.
