# Identity-preserving native literal edits

## Original user mandate

https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscript  について進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。最も筋の良い方向に進むために、限界まで深く考えてほしい。超長期的な視点からお願いしたい。どれだけ時間がかかっても構いません。開発環境として、cw-bd235945fff44468a0f41292という名前で、lxcのコンテナを用意しました、自由に活用ください。安全性チェックも気にかけつつ、なんとか進んでほしい。

## Reconciliation and observed workload

Work only as unprivileged coder in the supplied cw-bd235945fff44468a0f41292,
Home Coder workspace tomato-ocelot-73, repository
`/home/coder/workspace/lkjscript`. Initial local and independently read remote main
both equal `0e9160930217ae24e4034ddd9afa7ed408ef467a`. Preserve unrelated untracked
`docs/campaigns/202609242022.md`, the retained detached corrective worktree and
all previous evidence. No stashes or local compiler/test jobs were observed.
Pinned Rust 1.98.0 is available after adding `/home/coder/.cargo/bin` to PATH.
The container has 8 GiB memory; keep compiler and checker concurrency bounded.

Public v0.1.47 is complete. Original v0.1.48 producer 36210073260/1 remains
separate, frozen at e2c0d1f58ed0b347c7956d2af248b944ccf422b9; initial observation
of candidate job 108314571815 finds source acceptance still in progress.
Do not restart it or relabel its proof with this new source.

A fresh web starter demonstrates recurring edit friction. Drafting `web::controls`
and changing only "Apply dark theme" to "Use the dark theme" produces a complete
80,402-byte, 264-record logical plan: 16 created expressions, 16 deleted expressions,
16 retirements, one updated function and 212 witness edits. The accepted base is
unchanged; plan is not apply. Original draft and complete plan remain in ignored
`.artifacts/202609261118-controls.lkjc` and
`.artifacts/202609261118-controls-before.lkjplan`, with the disposable project at
`.artifacts/202609261118-edit-baseline`. The baseline used the existing development
binary, not a newly finalized release archive. An attempted `query definition`
command was unsupported and made no change.

## Selected boundary and proof obligations

Prefer a reusable native editing improvement over another closed application
recipe. Recognize same-structure function edits changing only scalar literals.
Retain existing expression/binding identities and update only changed literal
values. Structural edits retain explicit whole-body replacement and retirement.
No runtime primitive, graph format, deployment grant, live reload or new implicit
effect is introduced. Full review evidence remains available rather than hidden.

Native normalization must prove the entire proposed body equals the accepted body
with exactly the selected literal substitutions, using the existing canonical
authored-intent encoder. Mere matching traversal counts, masked shapes or displayed
names are insufficient. An uncertain correspondence falls back to replacement.
The independent typed lowerer must enforce live function ownership, unique exact
literal selections, same literal kinds and current candidate reachability before
mutation. Ordinary complete validation, impact, budgets, review binding and atomic
publication remain required. Preserve unchanged behavior and all negative evidence.

Prove the useful edit through copied-executable public creation, draft, plan,
apply, checks and detached HTTP use. Add adversarial ownership, kind, binding,
ordering, structural-fallback, stale-review and resource cases. Measure the same
baseline without claiming optional proof-file bytes are compulsory model tokens
or that reduced graph edits imply runtime speedup. Stabilize inputs before fresh
dependency-complete acceptance, then perform normal mainline delivery within
actual permissions. This initial record is a plan, not an acceptance claim.

## Implementation and focused evidence

The native declaration normalizer now compares complete canonical authored intent after a
candidate scalar substitution. It does not replace references with placeholders or compare only
AST discriminants. Five same-kind literal families are admitted: Bool, I64, F64, Text and StaticText.
Accepted Reader-owned expression symbols identify update positions; proposed names never supply
those identities. An uncertain correspondence falls back to the existing whole-body path.

The separate typed `SetFunctionLiterals` lowerer traverses the actual current candidate ownership
tree, including bindings and pattern payload owners, with ordinary canonical-read and ownership
budgets. It requires a live accepted function, existing accepted expression identities, nonempty
unique selections and unchanged literal kinds. All membership/kind checks finish before selected
values change. Normal candidate validation, impact/review binding and atomic publication remain
in force. New intents use LKJACR19; prior intent forms retain their canonical encoding domains.
No runtime primitive, graph/artifact generation or compact-language selector was added.

The matched initial web project was not applied or recreated between measurements. The new
complete plan is `.artifacts/202609261118-controls-after.lkjplan`, 8,613 bytes versus the retained
80,402-byte original: zero creations/deletions/retirements and one updated expression instead of
sixteen recreated/retired expressions and an updated function. The complete proof is still present.
This is not a claim about runtime speed, model tokens, mandatory context size or monetary cost.

Ten focused unit tests pass. An independent fixture-specific kernel walker checks every original
expression/binding identity, unchanged neighbors and exactly the selected values after publication.
Coverage includes all five scalar families, Japanese text, signed zero, infinities/canonical NaN
commitments, equal-valued distinct occurrences, legacy codec identity, no-op rejection, duplicate
or missing identities, cross-function selection, wrong kind, empty selection, read/ownership/edit
budgets, overlong input, structural fallback, changed lexical reference, simultaneous invalid
function contract and ordered composition with an earlier/later body replacement.

Two deliberately weakened builds fail their targeted tests (exit 101). Removing complete-intent
comparison permits a same-shape lexical-reference change to masquerade as scalar-only; the new
scope/fallback test catches it. Removing ownership membership admits a type-correct update to a
neighboring function; the core selection test catches that acceptance rather than relying on a
type error. Both guards were restored. Retained logs:
`.artifacts/202609261118-mutation-intent.log` and
`.artifacts/202609261118-mutation-ownership.log`.

After restoration and the v0.1.49 version preparation, all ten unit tests, the copied-executable
web workflow and workspace/all-target Clippy succeed. The workflow creates the web app outside
the checkout with empty PATH, rejects wrong types and altered reviews, proves deterministic full
plans and stale-token rejection, preserves the function ID, runs all 107 graph tests with evaluator
agreement, builds old/new immutable snapshots and serves both after moving the authoring graph.
The old label remains old; the new label is new. Old artifact and operator-template bytes remain
unchanged; both owned servers join. Its complete plan is again 8,613 bytes. The maintained native
generator refreshes all eight discovery pages. These results are retained in
`.artifacts/202609261118-post-mutation-focused.log` (completed_exit=0).

A foreground Cargo call earlier lost its MCP connection while compilation continued. Process and
workspace reads established that it was still the same owned job; no competing build was started.
The resulting exact test runner passed the then-four focused tests. An initial background launcher
attempt failed before Cargo because it tried to execute `cd`; a proper `bash -c` argument vector
corrected the invocation. Neither transport/launcher observation is product acceptance. Subsequent
logged commands terminate explicitly. A guessed prior-artifact directory was absent, so the second
application proof is being freshly authored from maintained literal sources rather than guessed
old files or reconstructed graph storage.

The unoccupied v0.1.49 tag and release were independently read as absent before changing only the
root package version and its lock entry. Notes, normative editing semantics and the web guide now
separate this source from public v0.1.47 and the independently frozen v0.1.48 producer. Source-wide
fresh acceptance, integration and any later archive selection remain separate obligations below.

## Second maintained consumer and stabilized source

The fresh two-library paged-list workflow passes on its first execution. It authors the maintained
HTML and response declarations using command scaffolds, exports/stages their exact immutable
transports, authors `list-site.lkjc` and edits only the `page` function's count fallback from 10 to
25. The parser's independent decimal radix is never selected. A complete 9,085-byte plan updates
one existing expression with no allocation, deletion or retirement. All 111 graph tests pass with
production/reference agreement. Moving both library graphs, their transport files and the consumer
graph away leaves old/new bundles independently runnable: default counts 10/25 differ as intended,
explicit count 2 remains equal, and count 101 still rejects. Six HTTP observations pass, both servers
join with no remaining tasks, and the old artifact bytes remain unchanged. The scoped public-CLI
Clippy check also passes; `.artifacts/202609261118-list-focused.log` ends completed_exit=0.

This new fixture uses the current standard plus two command-library scaffolds, unlike the older
98-test historical observation. Therefore its 9,085 bytes are not presented as a matched direct
comparison with the old 155,246-byte plan. The separately retained web project provides the exact
same-base comparison. Documentation now distinguishes both measurements and preserves the earlier
campaign's original claims and source.

A renewed independent read of original v0.1.48 candidate job 108314571815 shows source acceptance
and finalized three-asset construction successful, with final candidate/original-evidence admission
still in progress. No candidate restart, changed source or public release is inferred. This healthy
original run remains separate while the v0.1.49 implementation is stabilized for source-wide proof.
