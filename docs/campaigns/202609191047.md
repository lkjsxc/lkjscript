# Opt-in pre-main source verification

## Selected execution mandate — 2026-09-19 10:47 Asia/Tokyo

Starting remote main: `d4e8ae113c86346d7c67d629ecdb8ec5d7e5458b`.
Base tree: `841dbdacf8dcbd98b13ed45a653fedee3f5fe401`.

Deliver an opt-in, non-publishing hosted execution path for coherent candidate
commits before normal mainline integration. Reuse the maintained fresh full
profile, keep verification bound to the actual candidate SHA and run/attempt,
retain failures, and confirm remote main independently after accepted delivery.
Do not add language features, release/tag/version changes, deployments, automatic
merges, mandatory broad PR CI, credentials, protections or new semantic verifiers.

The immediate consumer is a development session with authorized Git-object writes
but no native authenticated checkout, Rust compiler or working dependency network.
The existing release workflow only dispatches, and its candidate path requires
`refs/heads/main`; using it for first acceptance would expose untested code first.
A plain PR has no test trigger. The observed obstacle therefore justifies a small
verification bootstrap before additional numerical or transaction work.

Use a `verify/` branch push for execution, explicit read-only contents permission,
non-persisted checkout credentials, the repository toolchain/lockfile, an immutable
verifier copy, the existing full DAG, bounded concurrency and 14-day attempt-owned
evidence retention. The workflow must reject nonzero execution, empty selection,
reused/unrun/failed gates, unstable inputs and a receipt for another source.

Acceptance: inspect exact YAML/actions/permissions and shell syntax, exercise the
wrapper against independent synthetic failure cases, then observe one actual
hosted fresh full run at the created candidate. Do not integrate until that run's
required payload and evidence upload succeed. Refresh rules/main and update only
normally; any missing write/execution permission is a blocker, not a bypass.

## Startup reconciliation

Read root guidance, current README/status/roadmap, verification/check owners,
release workflow and actual GitHub branch/rules state. Main was unprotected and
repository rulesets were empty. No open PR or in-progress workflow was observed
at startup; this does not claim ownership of unseen user workspaces.

There was no existing checkout in this disposable environment. Native acquisition
failed on GitHub DNS resolution; Rust/cargo/rustup were absent, and the official
Rust distribution host also failed DNS resolution. The local runtime was Debian
13 x86-64, with a 4 GiB cgroup memory limit and about 30 GiB free storage. The
GitHub connector exposed Git objects/ref writes and workflow observation/rerun,
but no new-run dispatch; effective writes/execution remain to be established.
Additional connected-tool discovery found no separate execution service.

The preceding callable-input increment is already accepted on main; its public
release is explicitly conditional on a consumer, request or chosen milestone.
The release cutover/consume correction's formerly pending observation is recorded
as complete in current status. No inherited publication is selected by this
infrastructure increment, and genuine historical failures remain unchanged.

`AGENTS.md`: unchanged. Current guarantees already cover this execution route.
The maintained workflow contract is [development verification](../development-verification.md).

## Pre-publication checks

The initial workflow passed local YAML structure/permission/trigger checks and
`bash -n` for every run block. Direct execution of the actual acceptance block
passed a healthy synthetic receipt and rejected 12 distinct malformed or
incomplete cases (empty selection, summary reuse/failure, unsafe receipt path,
wrong source, unstable inputs, unrun/missing/failed/reused gates, retained failure,
and missing fresh requirement). Two pipeline probes preserved verifier exits
zero and seven even when its output claimed success. These are wrapper checks,
not Rust product acceptance or an independent agent trial.

Hosted execution and remote-main integration are pending at this record's creation.
Append actual source/run identities and terminal results without rewriting this
initial mandate or relabeling earlier evidence.
