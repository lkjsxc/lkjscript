# Opt-in pre-main source verification

## Selected execution mandate — 2026-09-19 10:47 Asia/Tokyo

Starting remote main: `d4e8ae113c86346d7c67d629ecdb8ec5d7e5458b`.
Base tree: `841dbdacf8dcbd98b13ed45a653fedee3f5fe401`.

Deliver an opt-in, non-publishing hosted execution path for coherent candidate
commits before normal mainline integration. Reuse the maintained fresh full
profile, keep verification bound to the actual candidate SHA and run/attempt,
retain failures, and confirm remote main independently after accepted delivery.
Do not add language features, release/tag/version changes, deployments, automatic
merges, mandatory broad PR CI, credentials, protections or new semantic verifiers.

The immediate consumer is a development session with authorized Git-object writes
but no native authenticated checkout, Rust compiler or working dependency network.
The existing release workflow only dispatches, and its candidate path requires
`refs/heads/main`; using it for first acceptance would expose untested code first.
A plain PR has no test trigger. The observed obstacle therefore justifies a small
verification bootstrap before additional numerical or transaction work.

Use a `verify/` branch push for execution, explicit read-only contents permission,
non-persisted checkout credentials, the repository toolchain/lockfile, an immutable
verifier copy, the existing full DAG, bounded concurrency and 14-day attempt-owned
evidence retention. The workflow must reject nonzero execution, empty selection,
reused/unrun/failed gates, unstable inputs and a receipt for another source.

Acceptance: inspect exact YAML/actions/permissions and shell syntax, exercise the
wrapper against independent synthetic failure cases, then observe one actual
hosted fresh full run at the created candidate. Do not integrate until that run's
required payload and evidence upload succeed. Refresh rules/main and update only
normally; any missing write/execution permission is a blocker, not a bypass.

## Startup reconciliation

Read root guidance, current README/status/roadmap, verification/check owners,
release workflow and actual GitHub branch/rules state. Main was unprotected and
repository rulesets were empty. No open PR or in-progress workflow was observed
at startup; this does not claim ownership of unseen user workspaces.

There was no existing checkout in this disposable environment. Native acquisition
failed on GitHub DNS resolution; Rust/cargo/rustup were absent, and the official
Rust distribution host also failed DNS resolution. The local runtime was Debian
13 x86-64, with a 4 GiB cgroup memory limit and about 30 GiB free storage. The
GitHub connector exposed Git objects/ref writes and workflow observation/rerun,
but no new-run dispatch; effective writes/execution remain to be established.
Additional connected-tool discovery found no separate execution service.

The preceding callable-input increment is already accepted on main; its public
release is explicitly conditional on a consumer, request or chosen milestone.
The release cutover/consume correction's formerly pending observation is recorded
as complete in current status. No inherited publication is selected by this
infrastructure increment, and genuine historical failures remain unchanged.

`AGENTS.md`: unchanged. Current guarantees already cover this execution route.
The maintained workflow contract is [development verification](../development-verification.md).

## Pre-publication checks

The initial workflow passed local YAML structure/permission/trigger checks and
`bash -n` for every run block. Direct execution of the actual acceptance block
passed a healthy synthetic receipt and rejected 12 distinct malformed or
incomplete cases (empty selection, summary reuse/failure, unsafe receipt path,
wrong source, unstable inputs, unrun/missing/failed/reused gates, retained failure,
and missing fresh requirement). Two pipeline probes preserved verifier exits
zero and seven even when its output claimed success. These are wrapper checks,
not Rust product acceptance or an independent agent trial.

Hosted execution and remote-main integration are pending at this record's creation.
Append actual source/run identities and terminal results without rewriting this
initial mandate or relabeling earlier evidence.

## Completed acceptance and mainline delivery — 2026-09-19 Asia/Tokyo

This continuation closes the earlier pending observation without rerunning the
accepted payload. Candidate `bde2b283eb891e9e57db6db38ad85a7341592f27`, tree
`f2d9d42855e2fd11c6e1fb28946fc8bc7944b4e1`, is the direct child of starting main.
Its entire delta is the Verify workflow, its development guide and this campaign;
product code, verifier code, dependencies, generated assets and root guidance are
unchanged. Git-object creation and a non-forced update of the owned verification
branch established effective write access and triggered real hosted execution.

Authenticated [run 35413869613, attempt 1](https://github.com/lkjsxc/lkjscript/actions/runs/35413869613)
executed that exact source via a push. Its only
[job 105818568097](https://github.com/lkjsxc/lkjscript/actions/runs/35413869613/job/105818568097)
completed successfully at 2026-09-19T02:29:03Z; the run completed at 02:29:04Z
(11:29:04 Asia/Tokyo). Exact-source checkout, clean-input preflight, pinned
provisioning/build, fresh full execution, original-receipt admission, evidence
upload and post-job cleanup all completed successfully, with no skipped step.
Legacy commit statuses were empty; the matching Actions check run separately
reported success. Empty statuses were not used as acceptance evidence.

The authenticated job log retains the literal command:

```sh
"$RUNNER_TEMP/verify-tools/lkjscript-dev" check full --fresh --jobs 2 --machine
```

Its actual summary reports **26/26 passed, 26 fresh, zero reused**, no failure,
and 1,918,351,146,527 elapsed nanoseconds. The subsequent receipt predicate
completed successfully against the event SHA, stable inputs, fresh-required
execution and no unrun/failed/reused gates. Original receipt path:
`.artifacts/lkjscript-dev/check/1789783018358501531-6074-0/receipt.json`.
It is 64,021 bytes; summary digest:
`verification_3dac80eafc33437ca72eaf239bb796c764d1b3795bfc8c88b28d35e22d3c0c1d`.
The receipt and verifier identities remain bound to this tested candidate, not
to the later reporting-only commit containing this ending.

[Artifact 10575832276](https://github.com/lkjsxc/lkjscript/actions/runs/35413869613/artifacts/10575832276)
is `verify-bde2b283eb891e9e57db6db38ad85a7341592f27-35413869613-1`, 49,514,043 bytes,
with API/log-matching SHA-256
`be9833dbfed46af3c26f9491b38b36a1c86403b21c91cca5cdb7101ae69c1753`.
The upload reports 541 files and successful finalization. API metadata confirms
that it is unexpired and belongs to this repository, source and run. Observed
expiry is **2026-10-03T02:28:57Z** (11:28:57 Asia/Tokyo), not a perpetual archive.
The uploaded paths preserve the verifier identity, context, summary, admitted
receipt and the check owner's manifests, logs and retained outputs. This
continuation reviewed the authenticated job log, workflow, attempt-specific job
and artifact metadata; it did not download/re-extract the artifact or rerun the
Rust verifier locally. The original receipt was admitted on the hosted runner.

The runner reported Ubuntu 24.04.5 x86-64, image `20260907.300.1`, Rust 1.98.0,
and contents/metadata read-only token permissions. The log confirms checkout
credentials were removed before candidate commands. No publishing credential,
release dispatch, version/tag change or deployment was selected. No additional
static-target or anonymous-install acceptance is claimed. The approximately
32-minute full profile is a measured cost, not a claimed performance improvement.

Immediately before integration, main still selected the starting SHA, was
unprotected and had no rulesets. The complete three-file delta and successful
source-bound check were reviewed. A normal `force=false` reference update advanced
remote main to exact accepted candidate `bde2b283eb891e9e57db6db38ad85a7341592f27`.
A separate GET of `git/ref/heads/main` confirmed that SHA. No squash, reconstructed
implementation commit, protection bypass or history rewrite was used. This
ending is an append-only reporting delta; it does not change the accepted workflow
or rebind the earlier full receipt. Its own publication identity belongs in the
external final report, avoiding a self-referential hash/retest cycle.

The owned `verify/202609191047-pre-main` tip was reread and remains the accepted,
integrated candidate; its job is terminal. It is retained because the connected
writer exposes no branch-deletion operation and no authenticated native checkout
is available. No unique branch work was discarded and no destructive substitute
was used. Original hosted evidence remains retained; no unrelated local work,
process, stash or operational data was removed. `AGENTS.md`: unchanged.

The selected increment is complete on remote main. Manual dispatch, failure-path
artifact upload and cancellation were not separately exercised on GitHub; local
wrapper fault cases are not relabeled as those hosted trials. The current runner
also warned that implicit installation through `rustup show` is deprecated; the
pinned installation succeeded here, but explicit toolchain provisioning is a
future maintenance concern. Subsequent work should use the now-proven pre-main
path only when its required proof warrants it, not start another full run merely
to reconfirm this completed campaign.
