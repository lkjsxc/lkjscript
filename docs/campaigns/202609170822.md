# Close the published numerical-library milestone on main

**Invocation clock:** 2026-09-17T08:22:21+09:00 — Asia/Tokyo (JST, UTC+09:00).  
**Mandate:** `202609170822.md`  
**Intended initial archive:** `docs/campaigns/202609170822.md`  
**Repository:** `lkjsxc/lkjscript`  
**Companion:** complete replacement candidate for root `AGENTS.md`.

## 1. Decision and completion contract

Complete the already-selected combined numerical-library delivery by admitting the authentic
originals of the **successful v0.1.38 publisher**, reconciling the public product documentation,
and normally integrating the accepted closure into **remote main**. This is a continuation of
`docs/campaigns/202609162154.md`, not another numerical-language implementation, another rehearsal,
or a request to publish v0.1.38 again.

Before this campaign, the implementation is on main and immutable public assets exist, but the
repository still records unfinished publisher admission and an older supported release. After it,
the published source and bytes have a complete, source/run-bound original-evidence admission;
current public guidance describes that accepted milestone; genuine failures and recovery assets
remain preserved; and the closure is on remote main without an abandoned reporting branch.

The architectural thesis is **finish delivery of one fixed product source while keeping mutable
main and its reporting independent**. A terminal successful workflow is evidence of hosted
execution, not by itself admission of all retained originals. Conversely, rereading originals is
not a reason to replay the whole product or freeze development indefinitely.

The strongest alternative is improving transaction-library composition, particularly a helper
joining an already-active transaction on the same canonical requirement. Preparation/checking cost
is another plausible constraint. Neither has a new discriminating workload or measured bottleneck
in the inspected current record, while the combined delivery trigger has already been consumed.
Finishing the actionable original-admission boundary therefore wins now. The decision is overturned
only by evidence that this delivery is already completely closed, or a concrete defect that prevents
its admission. The former means satisfaction/reconciliation, not permission to select unrelated
feature work; the latter follows the bounded correction rules below. [1][2][3]

The finite normal scope is acquisition, authentication, original-reader admission, accurate current
claims, root-guidance reconciliation, and mainline completion. It introduces no new language
meaning, format generation, application-data migration, runtime architecture or performance claim.
Do not build new receipt infrastructure, reorganize the whole archive, or repeat completed matrices
merely to enlarge the campaign. Stop when this contract is met.

After closure, the next architect can measure ordinary numerical/data-processing authoring and
operation, then choose preparation cost, representation or transaction composition from a realistic
consumer. A result showing another dominant cost should overturn that outlook. This is a hypothesis,
not an authorized queue.

## 2. Coherent starting state and evidence limits

### Source identities

| Role | Observed identity and meaning |
| --- | --- |
| Mutable default branch M | `108ca2777fc202b543f48816a99b7db519542f97`, tree `25deb45dd5503682cc39f9c1f146243e1ed7a01c` |
| Frozen product/release source T | `7083f9a6d56ed702017942e100c3696fc6f35308`, tree `e12b89965176dcb166f69c9e32441f71588b7b9f` |
| Immutable annotated tag | `v0.1.38`, tag object `d711b4cb7385243283e4cdc53999c8b65fc2bbf6`, peels to T |
| Owning publisher | `35117655769`, attempt `1`, tag-push event, `head_sha` T, completed/success |
| Published release | ID `390160031`, v0.1.38, immutable, not draft or prerelease; published `2026-09-16T18:11:58Z` |

Git comparison establishes T as M's exact ancestor, not merely a patch-equivalent source. The five
intervening commits change only `AGENTS.md`, `docs/campaigns/202609161809.md`,
`docs/campaigns/202609162154.md`, `docs/release.md`, `docs/roadmap.md` and `docs/status.md`.
No product, verifier or workflow implementation differs across that comparison. M is nevertheless
not the hash tested by T's product proof. [4]

The branch collection contained only main, reported unprotected; open PRs and the ruleset collection
were empty. In-progress and queued Actions collections were empty. These are observations, not
permanent permissions or evidence about local branches/stashes. The inspected workflow directory
contains `release.yml`; its triggers are version-tag pushes and explicit dispatch, not ordinary
main documentation pushes. Inspect real intervening changes and other actual configured side effects,
without inventing hypothetical deployment approvals. [5][6][16][17][18][19]

Publisher `35117655769/1` began `2026-09-16T15:47:06Z`; its terminal successful state was updated
`2026-09-16T18:49:29Z`. All four jobs report success:

| Job | ID |
| --- | --- |
| Build, verify, and package | `104867076698` |
| Verify transferred packaged application candidate | `104901413283` |
| Publish immutable release | `104917882267` |
| Verify anonymous public release | `104917987045` |

The public job reports successful anonymous exact/latest acquisition, asset-digest and attestation
checks, both route admissions with one fresh aggregate suite, and final evidence upload. The live
latest release is v0.1.38. This supersedes the predecessor's pending service observation, but does
not establish that this new session has admitted its downloaded originals. The tag is annotated
but unsigned; do not describe it as a verified signature. The release API's `target_commitish: main`
is not a substitute for the observed tag peel to T. [7][8][9]

### Inherited obligations

The predecessor's execution additions report complete T-source rehearsal admission, normal main
integration, the final target fault matrix, interruption/recovery, pair and transferred reader
matrices, original-reader recovery and joined cleanup. Its earlier failed debug-stack invocation
and historical delivery failures remain failed observations. Its completed local simulated pair
is not the publisher's anonymous public pair. Verify the retained bindings, but do not rerun these
completed obligations on unchanged T merely because this mandate has a new filename. [1]

The earlier temporary implementation/reporting branches and worktrees were reported retired. Live
remote branch inventory corroborates that none remains. Do not recreate them. The predecessor
retained an unrelated stash and raw evidence; local existence and ownership must be inspected,
not inferred from that report. The existing ignored evidence owner is
`.artifacts/numerical-delivery-202609161809/`, with a separate publisher context
`publisher-35117655769-1/`. Reuse it when present, without mixing rehearsal or failed attempts.

The actionable remaining obligation is admission of this publisher's actual full/preparation,
target, package, pre-publication installed and anonymous public originals, followed by truthful
current claims on main. Preserve v0.1.36's failed original-admission history and v0.1.37's failed,
unproduced publication; the combined successor already supersedes their separate delivery.

**Architect's evidence boundary:** repository files, ancestry, API state and the producing build-job
log were inspected read-only. No artifact ZIP was downloaded or hashed locally, no executable or
reader was run, and no checkout or remote resource was modified. Container Git access failed at
DNS resolution; connected GitHub reads succeeded. The archive inventories, original-reader results,
local environment and retained local evidence remain Codex startup work. Do not promote this
investigation into execution proof.

## 3. Startup, authority and archival handling

You own implementation through integration and the selected delivery outcome. Normal non-forced
main pushes and ordinary existing CI/release operations are authorized within actual permissions
and protections. No routine approval is needed for those actions. Do not force-push published
history, bypass checks, change credentials/access controls/immutability, buy services, change billing,
deploy production or destroy operational data. Preserve unrelated work and never expose secrets.

Before editing, inspect actual HEAD/branch, index/worktree, relevant untracked files, stashes,
worktrees, remotes, divergence, applicable instructions, active jobs and available resources. Read
the existing root guidance, this mandate, the predecessor ending, current status and the relevant
release owner. Refresh main, tag/release and owning run. The inspected M and T are observations,
never reset targets. Prefer the ordinary mutable main checkout; use isolation only where its
ownership or evidence reconstruction requires it.

Reconcile the supplied complete `AGENTS.md` with legitimate intervening guidance and install it in
the mutable checkout before substantive work. It does not reload active-session instructions or
override higher-priority rules. Do not modify frozen T to install it. Preserve this initial mandate
unchanged at its intended archive path; append separated execution/resumption records after the
boundary at the end. On a real filename collision retain both originals and use observed seconds
or a documented suffix, never an invented later clock.

Useful read-only remote entry points, whose argument forms are present in the maintained release
procedure, are:

```sh
gh api repos/lkjsxc/lkjscript/actions/runs/35117655769
gh api repos/lkjsxc/lkjscript/actions/runs/35117655769/artifacts --paginate
gh run view 35117655769 --repo lkjsxc/lkjscript --json headSha,status,conclusion,jobs
gh release list --repo lkjsxc/lkjscript
```

If startup proves this exact delivery and its required admission already complete on remote main,
report satisfaction or justified supersession and finish only real remaining reconciliation. Do
not start another feature, pipeline or healthy application run to create activity.

## 4. Preserve and authenticate the publisher originals first

Retain the two short-lived handoffs before expensive builds or optional investigation. Their observed
expirations are **2026-09-18 02:23:40 and 02:23:41 Asia/Tokyo**, respectively. The complete publisher
inventory has **six** ZIPs; the prior rehearsal's five-artifact inventory is not this inventory. [10]

| Artifact name | ID | ZIP bytes | Expiration UTC |
| --- | --- | ---: | --- |
| `release-handoff-35117655769-1` | `10459961469` | 11080592 | `2026-09-17T17:23:40Z` |
| `verifier-handoff-35117655769-1` | `10460061486` | 32902540 | `2026-09-17T17:23:41Z` |
| `full-verification-evidence-35117655769-1` | `10457477097` | 50748739 | `2026-09-23T16:27:59Z` |
| `target-admission-evidence-35117655769-1` | `10460345790` | 51077577 | `2026-09-23T17:21:08Z` |
| `pre-publication-application-evidence-35117655769-1` | `10462175034` | 162946641 | `2026-09-23T18:11:26Z` |
| `public-application-verification-evidence-35117655769-1` | `10464415201` | 162961138 | `2026-09-30T18:49:18Z` |

Observed raw ZIP SHA-256 by artifact ID:

```text
10459961469  0c62234e0dc4383d55188376f16b16c7d5957e733c8952a36e7a994167ffc376
10460061486  04d2921eecdbd4084d6d9ef29187800ca5f3f4dbf8c284f9ac7a6bf73bb2ea5c
10457477097  0c82c2614e51725e58e24a8b3205a2c0d7b90cbe3190424d818e6edd75d4c7e2
10460345790  1bf53e481f14d53e6f0a7cd60d6dc9ed1ad2608a18b9e17d9f12111943e9da37
10462175034  2a7cfd2439a891df8f148cdf067a3c9529a76500538a7ab1ed6a265941ef33d4
10464415201  4ab03cb5ce0728fa1467b0c45df8a53d99a3debb96fb50a8c585bafb7eca7e05
```

Acquire through the authenticated artifact-ID download endpoints or an existing equivalent
repository acquisition path that retains the actual ZIP. Record fresh API metadata, repository,
source, run/attempt, artifact ID, actual length, digest and expiration. Match both this observation
and fresh metadata; investigate a mismatch rather than silently accepting replacement bytes. Keep
unmodified ZIPs and relevant producing/consuming job logs in the existing ignored publisher owner.
Use separate create-new extraction/reconstruction directories. Never overwrite the sole original,
substitute rehearsal archives, or commit credentials, binaries or raw evidence dumps.

The producing build log independently records the verifier handoff before its upload. Authenticate
this executable against that external log binding **before executing it**; its self-description or
its own identity JSON is insufficient. The following are publisher-log observations, not local
hash results. The coincidentally matching rehearsal verifier hash does not transfer its receipts. [11]

| Object | Bytes | Raw SHA-256 |
| --- | ---: | --- |
| Publisher `lkjscript-dev` verifier | 32901768 | `5cf309a209ef4409f21a79c1049b17f140e2d6418f2a6213512774370388239e` |
| Publisher static product executable | 24295968 | `98a39bd192c1e98187a1a954f916f5ffc89ef2586317c25e1efa06c61b888c32` |
| Published archive | 11071841 | `fa10e7afb09c06e4a243f421050a0497ca9acd331cdf34c046650c85aef377aa` |

The verifier handoff is schema `lkjscript-application-verifier-handoff`, version 4, containing
`lkjscript-dev` and `verifier-identity.json`. Follow `tools/lkjscript-dev/src/release/verifier.rs`
and the frozen workflow's verifier-validation step for its exact admission arguments; do not
invent a new verifier command or alter identity JSON. The package handoff contains the archive,
`SHA256SUMS`, `install.sh` and private `release-receipt.json`. Only the first three are public assets.

The live release API records these public asset identities. Reconcile them with the publisher's
authenticated anonymous acquisition and attestation records, not just checksum equality. [8]

```text
568515780  lkjscript-x86_64-unknown-linux-musl.tar.gz  11071841
sha256:fa10e7afb09c06e4a243f421050a0497ca9acd331cdf34c046650c85aef377aa
568515815  SHA256SUMS  109
sha256:a043b6be11f0de394bbefb3a2e5cc33bb4c2597d10b6e17b701252553aa1f895
568515849  install.sh  3566
sha256:affe462b30ba78b50c8db2f1198533a23ff2acad9544338b202f374eafd9ac43
```

## 5. Admit the existing execution; do not execute it again by accident

Use the unchanged T readers and publisher originals. Reuse the predecessor's legitimate isolated
reconstruction approach where available, adapting actual paths and metadata to this run. Inspect
ZIP entries before extraction; reject traversal, aliases and unsupported entries. Restore recorded
paths, modes and required selection links only in owned disposable namespaces, never over real host
paths or installations. Preserve private review-plan permissions. The original ZIP bytes and
receipts remain unchanged.

The build log explicitly reports different upload roots: the full ZIP is rooted at
`/home/runner/work/lkjscript/lkjscript/.artifacts`, whereas the target ZIP is rooted at
`/home/runner/work`. Use actual inventories and recorded paths rather than flattening both archives.
Recorded hosted environment may be supplied solely to reread historical evidence in an isolated
context; report that reconstruction and its limits. Do not change host/kernel settings or call that
context a fresh execution on the original runner. Preserve every unsuccessful setup/reader attempt.

### Source, target and package binding

The publisher full summary reports contract 5, 26 fresh passing gates, zero reuse, stable inputs and
no failure. Its receipt is 61295 bytes at
`.artifacts/lkjscript-dev/check/1789574080424403235-6484-0/receipt.json`, under the original workspace.
The verification digest is
`verification_ed9be7d5bd409d7f23984e2af86bb6b87603ba3383efbff5ade0d067b90d45f3`;
this is not to be mislabeled as a raw SHA-256.

Authenticate that original full receipt, its gate outputs and exact T binding. Its admission through
the private full reader occurred inside the genuine successful `release prepare` with
`--require-full-verification`. Reconcile the preparation's exact full/target references and original
receipt hashes with the producing log and original package receipt. Do not invent a public
`full-verify` subcommand, re-run preparation as if it were a passive reader, or claim a new direct
full-reader invocation when only the original preparation has established it. [11][12]

The original target receipt is 73210 bytes, raw SHA-256
`ae88b1f082eaec6f6d4a5c47bf1708b850e5c5b650b6c7a3540ffc013b8d0525`.
It records all six target owners, both pinned userlands and complete cleanup. The genuine package
receipt's raw SHA-256 is
`0e03a8785f26c4d4c7e8940596a6e433549dc4745175ba6cd9e6d37ca23d77d4`.
The release manifest hash is
`96385f614abc5cd331bbd83c671e6b915b084953a88c5383d5c07b51f55da486`.
These observations bind specific originals, not a newly rebuilt candidate.

The following exact argument forms ran successfully in the publisher. They are historical-context
reader entry points, to use only after the corresponding isolated namespace and authenticated
executables/files exist; they are not commands to create paths on the user's host:

```sh
/home/runner/work/lkjscript/lkjscript/target/release/lkjscript-dev release admission-verify \
  --candidate /home/runner/work/_temp/target-admission/lkjscript \
  --receipt /home/runner/work/_temp/target-admission/acceptance/receipt.json \
  --commit 7083f9a6d56ed702017942e100c3696fc6f35308

/home/runner/work/lkjscript/lkjscript/target/release/lkjscript-dev release verify \
  --archive /home/runner/work/_temp/release-output/lkjscript-x86_64-unknown-linux-musl.tar.gz \
  --checksums /home/runner/work/_temp/release-output/SHA256SUMS \
  --receipt /home/runner/work/_temp/release-output/release-receipt.json \
  --expected-tag v0.1.38 --expected-publication release
```

Admit target evidence through every existing child owner, not a summary-only check. The six are
`distributed_http`, `outbound_http`, `offline_packages`, `pure_tail`, `stateful_http` and
`service_acceptance`. The maintained strict package reader owns archive/manifest/bootstrap validation;
do not replace it with host tar extraction plus a checksum.

### Installed and anonymous public boundaries

For each of the pre-publication and anonymous-public evidence archives, recover its literal pair
invocation, verifier context and input paths from its own producing job and receipt. The maintained
`release transferred pair-verify` accepts the same arguments as `pair-run`, but rereads the completed
pair, both admissions, route lifecycles and every actual child without executing applications.
Use `docs/release.md` and `tools/lkjscript-dev/src/release/transferred.rs` with its `pair` module
as the command/reader owners. Use `release transferred verify` for the aggregate only as needed by
the maintained admission path, not as another behavioral run. [12][13]

Preserve the distinction between `--acquisition simulated` with the pre-publication boundary and
`--acquisition anonymous` with the actual public exact/latest boundary. Both use release-mode
package bytes here; that does not make simulated acquisition public. The public pair must bind
independently acquired/admitted exact and latest assets, both small installed lifecycles, and one
fresh five-owner aggregate on exact. Latest's aggregate is a valid within-pair binding, not a
second fresh execution. Do not manufacture five new latest child receipts.

Require all **eight actual authoring request/review-plan originals** at the pair's required paths,
with bytes, modes and bindings preserved. Require every child terminal result and joined cleanup.
A hosted green job, a self-reported status field, a checksum-only comparison or a reconstructed
missing authoring request is insufficient. Existing negative/recovery matrices already reported
complete at unchanged T are not an instruction to mutate this publisher's sole evidence or to run
another healthy pair. Validate their retained identity; refresh affected proof only if inputs or
machinery truly changed.

The product claim being closed remains the designed composition witness: an independently authored
statistics library folds a consumer-owned nominal sample through a bound projection after producer
removal, accepts a reviewed calibration change, and resumes from typed persistence after restart.
Algorithm and failure policy remain ordinary language code. The retained independent numerical
oracle, imported composition, transaction completion and failed nonfinite-output observation remain
discriminating acceptance; do not weaken them to a built-in calculation or a pure checksum test.
This is not evidence of real adoption, numerical throughput or a new application implemented in this
campaign. New semantics and new fresh-authoring acceptance are inapplicable because the selected
source already implements and exercises the capability. [1][8]

### Decisive acceptance

| Claim | Required observation | Reject or retain as incomplete when |
| --- | --- | --- |
| Original acquisition is authentic | Publisher-ID ZIPs match metadata, lengths/digests and producing context; verifier matches independent build log before execution | Missing, substituted, mismatching or unauthenticated originals |
| Publisher product proof is bound | Original full and successful preparation references agree; strict target and package readers pass on T and its bytes | Summary-only admission, cross-run substitution, omitted owner or changed source |
| Installed delivery completed | Matching simulated pair reader admits all required inputs, children and cleanup | Missing plans, failed/unfinished children, wrong context or unjoined work |
| Public delivery completed | Immutable release/tag/assets plus authenticated anonymous acquisition/attestations and the matching public pair reader pass | Upload/green-job alone, simulated evidence presented as anonymous, or route mismatch |
| Current product guidance is accurate | README/current status/release summary identify the admitted release and preserved recovery limits | Premature supported-release claim, false migration/sandbox/self-contained claim |
| Repository delivery is complete | Relevant descendant checks pass and remote main contains the accepted closure with recorded ancestry/source relation | Local commit, topic push, open PR, or unaccounted integration changes |

Retain literal reader commands, actual inputs, output/exit status, context reconstruction and
source/run bindings under the existing evidence owner. Classify these as original-evidence rereads,
not fresh application execution. Record unavailable and unrun checks explicitly.

## 6. Bounded failure and adaptation rules

Classify the first meaningful rejection before changing source or starting another pipeline.

**Reconstruction problem:** compare the failing path, bytes, mode, link and recorded environment
with authenticated originals and the unchanged reader contract. Correct only the disposable
reconstruction, retain the failed observation, and rerun the affected reader. Rehash originals and
join the namespace afterward. Neither receipt editing nor broad permission relaxation is a fix.

**Missing/corrupt original or unavailable acquisition:** check the actual same-run uploaded inventory,
original local retention and producing logs. Recover only authentic same-run bytes with valid
bindings. Expired remote ZIPs are not fatal when authenticated local originals already exist; logs
or regenerated files are not equivalent substitutes for required originals. A network/permission
block is not evidence of a product defect and does not justify a replacement release. Preserve the
published state and report the exact missing identity, evidence location and concrete resumption
operation if authentic recovery is genuinely unavailable.

**Actual producer, verifier or product defect:** isolate a reproducible failure using preserved
originals and a disposable copy, identify its owning implementation, and perform the smallest
complete correction within this delivery thesis. Add the relevant regression, refresh invalidated
owner/source/target/installed/public proof according to `docs/release.md`, and integrate accepted
work normally. Never make T's reader accept forged evidence or relabel old failure as success.
Because v0.1.38 is already immutable and published, a content/input change requiring replacement
publication uses the smallest actually unused additive patch identity after live reconciliation,
not a moved v0.1.38 tag or overwritten asset. The published predecessor remains externally committed
with its honest admission state. Do not dispatch a corrective release merely because a local
historical context was inconvenient to reconstruct.

Codex may choose a better reconstruction or implementation technique with equivalent safeguards and
record why. It may not substitute weaker examples, documentation alone, a probe or an unfinished
prerequisite for the admission and delivery promised here. A broader redesign or unrelated capability
is outside this mandate. If genuine external dependency alone remains, preserve the useful work and
return precise pending status; no unattended monitoring or status/sleep loop.

## 7. Documentation cutover, validation and remote-main delivery

Only after original public admission succeeds, update the current supported-release paragraphs,
source identity and relevant installation/recovery pointers in `README.md` to the admitted milestone.
Do not blindly replace historical version strings. Keep the ordinary workflow explicit: public
binary installation, language/library authoring and use without the language checkout or contributor
tools; an application bundle requiring that runtime is not a self-contained application executable.
Installation and selection do not migrate projects or operational data.

Preserve the existing compatibility contract: Graph 17/compiler-unit 13/bytecode 9/Artifact 21,
authentic supported Graph 14/15/16 and Artifact 18/19/20 readers under their original encodings and
current validation, and clear rejection by unsupported predecessors. Preserve matching predecessor
executables, immutable slots, untouched projects/exports, bundles/descriptors and compatible data
backups for recovery. Do not promise reverse conversion or universal application-schema compatibility.
F64 numeric equality and canonical representation equality remain distinct; nonfinite JSON failure
cannot undo an already completed data commit. No new data migration, execution grant or retry
semantics is part of this closure. [8][14]

Keep `docs/status.md` concise with source, admission and current public disposition. Reconcile the
current pending obligation in `docs/roadmap.md` and `docs/release.md`, linking a compact closure record
to the existing campaign lineage rather than appending another chronology everywhere. Preserve
initial historical mandates and genuine failed records. Do not create a second evidence ledger or
regenerate unchanged product assets merely because their dates are old.

For this expected documentation/guidance-only descendant, inspect the final intended diff and run
the maintained changed-path check while those changes are visible in Git status:

```sh
cargo run --release --locked -p lkjscript-dev -- check changed --machine
```

This argument form is verified in the inspected dispatcher/guidance. Check the receipt and selected
gates against the actual changed paths; include the new mandate in the visible intended file set.
A clean committed checkout's narrow result is not campaign coverage. Use the pinned toolchain;
a contributor build, when actually needed, is `cargo build --release --locked -p lkjscript-dev`.
Review links, source facts and compatibility prose directly as well. No full product replay is
required solely for this reporting descendant. Actual corrective implementation changes require
affected dependency-complete acceptance and any wider maintained gates/protections; do not exploit
the expected documentation scope to skip them. [15]

Stage only intended paths, inspect the staged diff and preserve unrelated work. Refresh remote
main, actual protections and active jobs before integration. Incorporate intervening work normally
and revalidate consequential interactions. Prefer a normal main commit and non-forced push when
permitted; otherwise use the required PR/merge path through actual completion. A topic push or
open PR is not the finish line.

After integration, fetch/read remote state and prove remote main contains the accepted closure and
T. Record the actual revision and its relation to the original verified source; do not stamp T's
proof with the descendant's hash. A squash or cherry-pick requires explicit patch/source-binding
accounting, not invented ancestry. A later concise final report may describe the integrated result
without requiring another identical full run forever.

Reconcile inherited temporary references, but do not recreate the already-retired branches. Remove
only clearly campaign-owned new temporary branches/worktrees after checking their tips, integration
or justified supersession, unique work, ignored evidence and active-job dependencies. Preserve
immutable tags, failure originals and unrelated stashes. Record an exact retention reason when a
reference or evidence context is still needed. No cleanup by blanket deletion.

**Release disposition:** normally close the existing v0.1.38 delivery. Create no new version, tag,
release, selector mutation, rehearsal, publisher or public application run. The existing run is
terminally successful and no longer needs watching. The defect rule above is the sole corrective
exception; applicable release protections and proof still govern it. Main reporting and frozen
publication source remain separate. If integration itself is blocked, retain the one outstanding
lineage and report its exact gate/permission/conflict, source/run and next action; do not call it
complete.

## 8. Guidance changes and final return

The companion root candidate retains the mission, current graph authority, safe startup, verified
navigation and commands, Rust/toolchain/no-Python/product-surface requirements, essential type/effect/
publication guarantees, valid evidence bindings, remote-main completion and safe cleanup. It
consolidates prose and leaves incident details, exact release identities, reconstruction procedures
and matrices at the campaign/release owners. It does not loosen a semantic or publication gate,
mandate a new branch, add a permanent artifact-expiration checklist or freeze current architecture.

Return a concise report in Japanese stating the actual user-visible milestone, what was already
implemented, what this session actually admitted or corrected, relevant source/verifier/byte/run
identities, original versus fresh/reused/failed/unrun proof, compatibility impact, exact remote-main
revision, public release/admission state, actions not taken, cleanup/retention and any exact blocker.
The most consequential next finding should describe an observed constraint, not automatically
promise the next feature. Make clear whether the combined due delivery is genuinely closed.

### Revision-bound sources and live observation endpoints

Repository source links below pin the inspected contexts. Live endpoints establish the observation
above and must be refreshed when their state matters; they are not immutable evidence archives.

[1]: https://github.com/lkjsxc/lkjscript/blob/108ca2777fc202b543f48816a99b7db519542f97/docs/campaigns/202609162154.md
[2]: https://github.com/lkjsxc/lkjscript/blob/108ca2777fc202b543f48816a99b7db519542f97/docs/roadmap.md
[3]: https://github.com/lkjsxc/lkjscript/blob/108ca2777fc202b543f48816a99b7db519542f97/docs/status.md
[4]: https://github.com/lkjsxc/lkjscript/compare/7083f9a6d56ed702017942e100c3696fc6f35308...108ca2777fc202b543f48816a99b7db519542f97
[5]: https://api.github.com/repos/lkjsxc/lkjscript/branches?per_page=100
[6]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/.github/workflows/release.yml
[7]: https://github.com/lkjsxc/lkjscript/actions/runs/35117655769
[8]: https://github.com/lkjsxc/lkjscript/releases/tag/v0.1.38
[9]: https://api.github.com/repos/lkjsxc/lkjscript/git/tags/d711b4cb7385243283e4cdc53999c8b65fc2bbf6
[10]: https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/35117655769/artifacts?per_page=100
[11]: https://github.com/lkjsxc/lkjscript/actions/runs/35117655769/job/104867076698
[12]: https://github.com/lkjsxc/lkjscript/blob/108ca2777fc202b543f48816a99b7db519542f97/docs/release.md
[13]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/tools/lkjscript-dev/src/release/transferred.rs
[14]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/docs/spec/semantic-storage.md
[15]: https://github.com/lkjsxc/lkjscript/blob/108ca2777fc202b543f48816a99b7db519542f97/tools/lkjscript-dev/src/check/mod.rs
[16]: https://api.github.com/repos/lkjsxc/lkjscript/rulesets?per_page=100
[17]: https://api.github.com/repos/lkjsxc/lkjscript/pulls?state=open&per_page=100
[18]: https://api.github.com/repos/lkjsxc/lkjscript/actions/runs?status=in_progress&per_page=100
[19]: https://api.github.com/repos/lkjsxc/lkjscript/actions/runs?status=queued&per_page=100

---

**End of the initial mandate.** Preserve all preceding text unchanged when archiving. Append only
clearly labeled, concise reconciliation, execution or resumption additions after this boundary,
containing actual observations and actions rather than prospective success claims.
