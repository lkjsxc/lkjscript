# Immutable 0.1.14 Public Topology Authoring Release

Status: complete

Campaign ID: `202609010950-public-topology-authoring-release`

## 1. Identity, audit boundary, and lifecycle

Audit time: `2026-09-01 09:50:04 +09:00` (`Asia/Tokyo`).

Orientation source: remote GitHub repository `lkjsxc/lkjscript`, default branch `main`, exact commit
`f690bfc11d5be491d24f01a066d801d22b403bee`, tree
`e477973e44bd0911227ffd6f785d03cafc2f5f65`. This is a remote-branch observation, not an active
checkout. The mandate author did not inspect a local branch or `HEAD`, index, worktree, untracked or
ignored files, remotes, upstream relation, divergence, stash, installed binary, toolchain, local
build, test, benchmark, deployment, running service, private evidence, or credentials.

External orientation during this turn:

- root source declares product version `0.1.14`;
- immutable latest public release `v0.1.13`, release `379981013`, selects
  `dadc142c8230c58af6fc7884a3a4e3a5b7267d5e` through annotated tag object
  `893dbcb107decd72d9bffbd6ca2d66389be7257a`;
- no `v0.1.14` remote tag or release was observed;
- latest observed Release run `33425711515` succeeded for `v0.1.13`;
- no queued/in-progress workflow and no open issue or pull request was observed; and
- immutable-release administration, the confirmation variable, hosted artifacts, installed copies,
  deployments, and non-public state were unavailable.

Archive: `docs/campaigns/202609010950.md`. `docs/campaigns/` remains the sole mandate archive and no
mutable current-work ledger exists. Do not create another campaign, prompt, plan, or work tree.
Preserve this body; Codex may change only the lifecycle field and append the bounded terminal record.
Current facts, specifications, rationale, proof, and raw output remain in their existing owners.

Predecessor disposition:

- `202609010657` is complete. `ff913d13deb29f2de8a570bb0e29aea4268d6187` implemented public
  exact dependency/topology authoring, unified recipe lowering, migrated stateful HTTP to
  `minimal`, and advanced source to `0.1.14`; `f690bfc11d5be491d24f01a066d801d22b403bee`
  recorded closure evidence.
- `202609010158` is complete and published immutable `v0.1.13`.
- Immutable `v0.1.8` is an unmodified historical recovery point superseded by additive releases.
- Historical reports name stash object `87c2265f6faf0e815d80450ec2480d46c5e774fb` and ignored
  artifacts. Preserve them if present.
- No active remote mandate or unresolved publication was observed. Active-checkout and refreshed
  external evidence win.

## 2. Mandate and stopping rule

This is a release, publication, and proof campaign, not another design pass or feature campaign.

**Required final state:** the exact complete `0.1.14` source snapshot, including public
dependency/component/requirement/function-backed-port/target authoring and unified recipe lowering,
is available as one immutable latest `v0.1.14` self-contained
`x86_64-unknown-linux-musl` release whose exact candidate and anonymous exact/latest downloads
freshly pass static inspection, command lifecycle, distributed HTTP, stateful-from-`minimal`,
outbound HTTPS/NIP-11, and maintained service acceptance, while predecessor publications remain
unchanged and no deployment or live relay is contacted.

`R` is the exact release-source commit. It must contain this mandate, the reconciled root
instructions, all release inputs, and only necessary release-blocking or release-truth corrections.
If supplied `AGENTS.md` is byte-identical, retain it without a synthetic diff. `R` must be clean,
normally pushed, and equal to `origin/main` before tag creation. Root version remains `0.1.14`.

`E` is the post-publication evidence/current-truth commit. It may update the campaign lifecycle and
terminal record, bounded evidence, status, architecture, release documentation, README, and
performance summary. It must not change source, workflow, target, verifier, generated contracts,
normative behavior, maintained artifacts, or another receipt-bound release input.

Complete only when `REL14-1` through `REL14-8` pass, publication is immutable and latest, anonymous
exact/latest acceptance is fresh, commits and pushes close, documentation is truthful, and the tree
is clean or preserved work is exact. Before tag push, a required failure leaves public state
unchanged. After tag push, use only Section 9 recovery; never mutate a published identity.

Outcome:

- `complete`: every required source, distribution, external, and evidence boundary is closed;
- `blocked`: the objective remains valid but a required gate/action is unavailable; leave top status
  `incomplete` and append the exact blocked state;
- `terminated`: a material defect or safety conflict invalidates this campaign; or
- `superseded`: a legitimate later authority or release already owns the final state.

## 3. Reconciliation gate

Before editing or mutation:

1. Read applicable instructions, this mandate, terminal records `202609010657` and `202609010158`,
   current status/architecture/roadmap/release procedure, verification and product specifications,
   the release-version decision, and the public-topology decision.
2. Capture branch, `HEAD`/tree, index/worktree/untracked/ignored state, remotes/upstream/divergence,
   recent commits, stashes, supplied files, toolchain, root version, and newer active work. Preserve
   unfamiliar and unrelated work; do not reset, clean, restore, apply/drop a stash, or rebase it.
3. Fetch without history rewrite. Audit the delta from public source
   `dadc142c8230c58af6fc7884a3a4e3a5b7267d5e` through active `HEAD`; confirm the topology campaign
   is terminal and no later source delta adds a release dependency.
4. Locate current first-party owners of full verification, target build/admission, deterministic
   package verification, verifier handoff, hosted dry run, isolated publication, anonymous
   exact/latest acceptance, generated docs, and structured evidence.
5. Confirm that full, target, no-checkout pre-publication, and post-publication boundaries all use
   the current stateful workflow that starts at `minimal` and publicly authors dependency and
   topology. A stale HTTP-starter verifier blocks release.
6. Immediately before each external mutation refresh `origin/main`, tag/release availability,
   immutable-release administration, confirmation-variable authority, workflows, permissions,
   concurrent work, and public latest identity. Use only existing noninteractive credentials.

The checkout outranks this baseline. Correct non-material symbol, command, count, receipt, or file
drift and continue. Stop only for a different authority, conflicting active objective, used identity,
unrecoverable required gate, unsafe unfamiliar work, or impossible dependency closure. Ambient
warnings, optional cache failures, or missing convenience tools are not blockers unless a named gate
or safety property changes.

## 4. Current state and bottleneck

At the orientation revision:

- the accepted typed graph remains sole editable program authority;
- source `0.1.14` owns CLI `16`, compact-change records `8`, and stateful acceptance `4`;
- 25 public compact operations include exact staged built-in dependency addition, component and
  requirement creation, function-backed port addition, and command/HTTP target creation;
- all four built-in recipes use the same authored normalization, allocation, plan, validation, and
  publication path; `new` still publishes one validated initial revision atomically;
- copied stateful acceptance starts with empty `minimal`, stages immutable built-in transport without
  changing semantic `HEAD`, and publicly authors its complete topology and BBS; and
- deployment, operational data, artifacts, release objects, and receipts remain separate authority.

Predecessor proof reported fresh source, recipe, stateful, service, and static-musl admission. Its
16,279,776-byte candidate had SHA-256
`d0b8e994420a11558e3fb4d211f338becb7028ebf28f4664a027de7166bfe774`. This is orientation only:
adding this mandate or changing any release input creates a new `R` requiring fresh bound proof.

The bottleneck is distribution authority. Supported `v0.1.13` lacks the source-only topology records
and current stateful-from-`minimal` path. Another semantic delta would deepen the source/public split
that root policy requires closing first.

## 5. Campaign selection

Scores are comparative; value/proof use 1 weak to 5 strong, while risk uses 1 low to 5 high.

| Candidate | Value | Semantic leverage | Authority contraction | Dependency closure | Completion | Proof | Future unlock | Context/retry cost | External risk | Decision |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| Immutable `v0.1.14` release | 5 | 4 | 5 | 5 | 5 | 5 | 5 | 3 | 4 | **Select** |
| Nostr WebSocket/NIP-01 with session typestate | 5 | 5 | 4 | 3 | 2 | 5 | 5 | 5 | 5 | Defer |
| Lightweight million-owner capacity closure | 3 | 2 | 2 | 3 | 3 | 4 | 3 | 4 | 1 | Defer |
| Data/history retention and compaction evidence | 4 | 3 | 4 | 2 | 2 | 5 | 4 | 5 | 4 | Defer |

The release closes coherent unfinished public work and exposes the already accepted authority
contraction through the actual distributed binary. It introduces no new graph meaning; the final
report must say so.

The strongest alternative is persistent Nostr WebSocket/NIP-01 using affine connection ownership,
session typestate, capability-scoped signing/key authority, and bounded reconnect semantics. It
could reject invalid protocol transitions mechanically, but it would stack another unreleased delta
and requires a new raw WebSocket oracle and live-resource model. Defer, do not reject it.

Million-owner work is still an environment-bounded capacity question. History retention, packing,
compaction, a new database, or non-SQL query model needs measured pressure and independent
reachability/recovery proof. Owner-detail projection lacks a maintained authoring failure.

## 6. Fixed target state and scope

### 6.1 Authority and identity

- Root version remains `0.1.14`; the intended unused annotated tag is `v0.1.14`.
- Publication alone advances no CLI, graph, compact-change, codec, plan, creation, package, artifact,
  deployment, runtime, data, queue, compiler, adapter, registry, verifier, or evidence contract.
- `R` is the sole tagged source. Changing source, workflow, target policy, verifier, generated or
  normative input, lockfile, maintained asset, or release procedure forms a new `R` and invalidates
  affected receipts.
- Candidate, target policy, archive, manifest, checksum, handoffs, digests, attestations, and
  receipts are derived distribution identities, not graph or operational-data authority.
- Public claims advance only after anonymous exact/latest behavioral acceptance.
- Predecessor tags, releases, assets, attestations, and evidence remain immutable; recover additively.

### 6.2 Product and maintained consumers

Copied discovery must expose these exact current records, bounded fields, references, limits,
diagnostics, and next actions:

```text
add.dependency package=PKG semantic-revision=REV package-revision=PACKAGE_REVISION
create.component as=$COMPONENT module=MODULE name=NAME visibility=private|package|public
add.port as=$PORT component=COMPONENT name=NAME type=TYPE function=DECLARATION
create.target as=$TARGET name=NAME component=DECLARATION port=PORT runner=command|http
```

Direct and input-file forms must normalize equally. Stale, foreign, duplicate, incomplete,
conflicting, unavailable, and predecessor forms fail without advancing authority. Exact dependency
addition uses only explicitly staged built-in transport: no registry, network, ambient directory, or
unchecked file.

The supported boundary includes:

- editable dependency-free `minimal`;
- checked/built/runnable pure `command`;
- checked/built/served `http`;
- closed `nostr-relay-info` against loopback raw HTTP/TLS/DNS, never a live relay;
- stateful HTTP authored entirely from `minimal`, with clean/incremental equality, first-party data,
  rollback, restart, backup/restore, authority preservation, and cleanup; and
- maintained `lkjournal` service/worker with exact package closure.

Prove recipe parity and absence of recipe-specific owner/snapshot builders, direct semantic-ID
allocation, hidden authoring fixtures, stale HTTP-starter dependence, aliases, or fallback paths.

### 6.3 Distribution and security boundary

The sole target remains `x86_64-unknown-linux-musl`. Directly inspect the exact ELF candidate, run
the complete lifecycle in both pinned userlands, and run every maintained copied-binary oracle owned
by current target policy. Do not infer staticness from the target name.

Claim only the exact target, candidate, userlands, archive identity, integrity, and observed
behavior. Do not claim generic Linux portability, minimum kernel, other platforms, reproducibility,
signing, general provenance, sandboxing, multi-tenant isolation, encrypted storage, inbound TLS,
DNSSEC, privacy, or production service levels. Outbound proof remains deployment-bound, exact
endpoint, loopback-only, no redirects, and no live relay.

### 6.4 Latitude and non-goals

Codex may choose commit count, focused iteration checks, private evidence roots, release notes, and
internal factoring for a proved release blocker. Reuse current owners and discover commands from
`docs/release.md` and executable help. Do not refactor a passing release path for style.

A workflow, verifier, target, release-tool, generated-metadata, or documentation correction is
allowed only when necessary for this final state; commit it into a new `R` and rerun invalidated
gates.

Do not add WebSocket/NIP-01 events, session semantics, language/effect operations, owner-detail
projection, package management, user recipes, source text, another graph form, worker recipe, target,
installer, registry publication, database/query redesign, packing, compaction, garbage collection,
deployment, migration, or live-relay behavior. Affine/linear resources, typestate/session protocols,
richer effects, and refinement evidence remain preferred evidence-gated future options.

## 7. Success conditions

- **REL14-1 — exact source:** active work is reconciled; topology closure is terminal; root version
  is `0.1.14`; `R` contains this mandate and coherent inputs only; `R == origin/main`; the tree is
  clean except exact preserved work; and tag/release `v0.1.14` are unused before tag push.
- **REL14-2 — fresh source semantics:** one fresh final full profile for `R` passes with no required
  reuse, skip, unavailability, failure, or unrun result. Discovery, generated docs, standard and
  `lkjournal`, recipes, contracts, topology authoring, parity, stateful-from-`minimal`, predecessor
  rejection, and production/reference agreement all hold.
- **REL14-3 — exact target:** first-party tooling builds one exact static-musl candidate from `R`.
  Direct ELF inspection, both pinned userlands, distributed HTTP, stateful HTTP, outbound
  HTTPS/NIP-11, and standalone service admission fresh-pass with exact binding and cleanup.
- **REL14-4 — deterministic package:** preparation binds source/tag, target, candidate, capabilities,
  toolchain/native inputs, locked closure, notices, and fresh receipts. Repeated packaging is
  byte-equal; strict verification accepts the canonical archive/checksum inventory and rejects
  malformed, linked, traversing, duplicate, extra, reordered, mismatched, or corrupt inputs.
- **REL14-5 — hosted dry run:** one Release dispatch for exact `R`, `publish=false`, tag `v0.1.14`
  freshly passes build/full, target, package, no-checkout handoff, and transferred distributed,
  current stateful-from-`minimal`, and outbound oracles. Publication/post-publication jobs skip and
  no public object is created.
- **REL14-6 — immutable publication:** after refreshed preconditions, one annotated `v0.1.14` tag
  selects `R`; immutable releases are enabled; the confirmation variable binds that tag-object SHA;
  only the tag ref is pushed; and the canonical workflow alone creates one immutable latest,
  non-draft, non-prerelease release with canonical assets and matching digests.
- **REL14-7 — public acceptance:** separate anonymous exact and latest downloads each fresh-pass
  checksum/owned attestation, strict extraction, identity/static inspection, distributed HTTP,
  stateful-from-`minimal`, and outbound HTTPS/NIP-11 on fresh roots. Compare exact/latest bytes only
  after both behavioral paths pass; retain failure, cancellation, rollback, authority, and cleanup.
- **REL14-8 — closure:** bounded evidence at
  `docs/evidence/202609010950-public-topology-authoring-release.json` records exact identities,
  classifications, resources, retries, recovery, cleanup, pushes, and mutations. Current public
  facts are truthful; `E` changes no receipt-bound input; predecessor publications are unchanged;
  normal push closes; and the campaign is terminal.

## 8. Dependency-ordered work

1. Reconcile Section 3, preserve work, and confirm no later campaign or external state owns this
   release.
2. Add this mandate, retain byte-identical root policy, audit from `v0.1.13`, make only necessary
   release corrections, regenerate only through owners, commit `R`, normally push, and prove
   `R == origin/main`.
3. After inputs stabilize, run one fresh full profile, then build and admit the exact candidate
   through static inspection, pinned userlands, and all target oracles.
4. Prepare notices/archive/checksum/manifest/receipt through first-party tooling; require deterministic
   repeat and strict extraction/verification into absent private paths.
5. Dispatch the hosted dry run for `R`; require read-only jobs to pass and mutation jobs to skip.
   Inspect exact hosted handoffs. Any input correction creates a new `R`.
6. Refresh external state, create and verify the annotated tag, bind the confirmation variable, push
   only that tag, and observe canonical publication. Never manually create or upload a release.
7. Run hosted and independent anonymous exact/latest acceptance; verify immutable/latest inventory;
   write bounded evidence/current truth, append terminal state, commit `E`, run narrow closure checks,
   normally push, and report source/release/deployment separation.

## 9. Proof, evidence, failure, and recovery

Verification ladder: minimal baseline; focused correction checks; one fresh full profile; exact
target admission; deterministic local package; hosted dry run; immutable publication; hosted and
independent exact/latest acceptance; evidence-only closure. Do not repeat broad gates unless an
input changes or failure isolation requires it.

Classify every gate exactly `fresh passed`, `reused passed`, `skipped`, `unavailable`, `failed`, or
`not run`. Final full, target, local package, dry run, publication preconditions, publication, and
both public boundaries require fresh exact-input proof. Historical receipts are orientation. A
required unavailable gate blocks completion.

Independent proof must retain production/reference agreement, independent graph/projection checks,
raw local HTTP/TLS/DNS clients, direct ELF parsing, strict no-checkout package/handoff verification,
separate anonymous exact/latest behavior, and exact GitHub tag/workflow/release/asset observations.

Negative/containment proof covers stale or foreign receipts; version/tag mismatch; used identity;
disabled immutability; bad confirmation binding; concurrent release; mutated handoff; dynamic or
malformed candidate; policy mismatch; archive/link/traversal/checksum/inventory failure; asset
clobber; exact/latest disagreement; predecessor/invalid topology; unavailable built-in binding;
invalid port/target closure; stateful rollback; outbound address/TLS/media/limit/timeout/cancellation
failure; startup without readiness; unchanged semantic authority; and cleanup.

Record only owned resource observations. Do not infer latency, throughput, memory safety,
portability, reproducibility, provenance, isolation, or service levels. Keep logs, binaries,
archives, tools, projects, certificates, and temporary receipts in
`.artifacts/campaign/202609010950/` or current ignored verifier roots. Tracked evidence contains
bounded identities, digests, classifications, summaries, and pointers; never secrets or raw dumps.

Before tag push, correct a defect in a new `R` and rerun invalidated gates. Remove an unpushed local
tag only after proving it never reached the remote. After tag push:

- never move or delete the tag;
- one unchanged exact-tag recovery dispatch is allowed only for a proven transient idempotent
  orchestration failure;
- read-only propagation/attestation/download verification may retry;
- a content defect requires the smallest unused additive patch, new source, and affected fresh
  gates; and
- at most one content-changing additive recovery release is authorized. A second defect terminates
  with exact externally committed state.

## 10. Narrow empirical questions

1. What active branch, `HEAD`/tree, worktree/index/untracked/ignored state, upstream/divergence,
   stashes, supplied files, and later work exist?
2. What final `R`, gate inventory, toolchain/native inputs, target-policy digest, candidate,
   capabilities, verifier, receipt schemas, and generated owners bind release?
3. Does current workflow carry public topology and stateful-from-`minimal` through full, target,
   handoff, no-checkout pre-publication, publication dependency, and exact/latest acceptance?
4. Are immutable releases enabled, tag/release unused, confirmation authority available, and current
   permissions/concurrency/isolation consistent with `docs/release.md`?
5. Which tag object, peeled source, workflow run/attempt/jobs, handoff IDs/digests,
   release/assets/attestations, anonymous receipts, retries, and recovery prove closure?

These local/volatile questions do not reopen product semantics, target scope, compatibility, or
proof policy.

## 11. Read map

Begin with:

- `AGENTS.md`, this mandate, campaigns `202609010657` and `202609010158`;
- `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`, `.github/workflows/release.yml`,
  `docs/release.md`;
- current status/architecture/roadmap, public-topology and release-version decisions;
- verification, product-surface, packages/components, semantic-CLI, and HTTP specifications;
- first-party check, target, release, verifier-handoff, and evidence owners;
- public CLI, distributed/stateful/outbound HTTP, service, generated-doc, and predecessor tests; and
- standard and `lkjournal` generated assets and owners.

Expand only for exact dependencies. Do not reread every campaign or ingest raw artifact trees.
Remote names and commands require checkout verification.

## 12. Git and external actions

Stage explicit paths, inspect each staged diff, avoid broad staging, formatting/dependency churn,
history rewrite, or destructive cleanup, and end clean or name preserved work.

| Action | Authorization |
|---|---|
| Local edits/generated output | Authorized only for mandate, release blockers, evidence, and truthful current docs after reconciliation |
| Local builds/tests/copied binaries/pinned containers/loopback services/private data | Authorized; no live relay or ambient/user/hosted application data |
| Coherent commits | Authorized; keep `R`, recovery, and `E` distinct |
| Normal fast-forward push | Conditional for `R`, `E`, and one additive recovery after fetch, proof, review, and collision checks |
| Workflow dispatch | One dry run per distinct `R`; one unchanged exact-tag recovery only under Section 9 |
| Annotated tag | `v0.1.14` conditional once while unused and all preconditions pass |
| Release/assets/attestations | Only canonical tag-triggered isolated workflow; manual creation/upload/edit/delete forbidden |
| Confirmation variable | Read authorized; set only to exact new tag-object SHA immediately before tag push |
| Package/registry publication | Forbidden |
| Deployment/service reconfiguration | Forbidden |
| Schema/data migration or hosted/user/database mutation | Forbidden |
| Issue/PR/settings/permissions/secrets/credentials | Forbidden except exact non-secret confirmation binding |
| Force push/rebase/shared-history rewrite/reset/clean/restore/retag/published-object mutation | Forbidden |
| Read-only release/workflow/artifact/attestation/download inspection | Authorized and bounded |

Missing credentials do not justify interactive login or weaker proof. Finish unaffected local work,
classify the exact action unavailable, preserve state, and report whether blockage is before or after
an irreversible boundary.

## 13. Final report and terminal record

Return exact:

- start/final Git, worktree, upstream, stashes, supplied and preserved work;
- `R`, `E`, recovery commits, purpose, pushes, and all external actions;
- product and independently owned contracts changed or intentionally unchanged;
- public records, maintained-consumer results, and semantic-leverage result: no new graph meaning,
  publication of preceding authority contraction, private recipe authority still deleted;
- every gate/classification, receipt path/digest, independent/negative/rollback/interruption/
  predecessor/clean-incremental/generated/static/cleanup result;
- target policy, toolchain, candidate, capabilities, verifier, archive, checksum, manifest, handoffs;
- tag object/peeled source, run/attempt/jobs, artifacts, immutable release/assets/digests/
  attestations, and exact/latest observations;
- deviations, resources, limitations, security nonclaims, retries/recovery, and final
  source/release/deployment split; and
- next candidates: Nostr WebSocket/NIP-01 session semantics, separate history/retention work,
  lightweight million-owner capacity, and owner detail only when a workflow requires it.

Append one concise terminal record below. On completion set top status `complete`; use `terminated`
or `superseded` when applicable; blocked leaves `incomplete`. Include terminal time, identities,
commits, `REL14-1` through `REL14-8`, evidence path/SHA-256, required classifications,
product/contracts, candidate/tag/workflow/release/assets, external actions/pushes, preserved work,
limitations, and next candidates. Do not rewrite this body or paste logs, manifests, inventories,
generated documents, or long measurements.

## Terminal record

Completed at `2026-09-01 03:38:47 UTC`. Entry `main` was
`f690bfc11d5be491d24f01a066d801d22b403bee` (tree
`e477973e44bd0911227ffd6f785d03cafc2f5f65`); release-source commit `R` is
`b9f37cb86353596d260521924b5986300595450e` (tree
`0f0a4d2c5c49fcfd07823f312a18e603e63ff0fc`). `R` adds this mandate and the
release-blocking stateful-schema-v4 workflow predicates and tests; it changes no product semantics.
The containing evidence/current-truth commit is `E`, whose exact SHA and normal push result are
reported outside its own content. Structured evidence is
`docs/evidence/202609010950-public-topology-authoring-release.json`, SHA-256
`826d9a9b12d5c039be548d735f3b8535440e7ecfbbc02f237eb44dfce92d6285`.

`REL14-1` through `REL14-8` are `fresh passed`. Final source verification passed 24/24 gates
freshly with zero reuse; exact local and hosted static-musl target admission passed 7/7
classifications across both pinned userlands and all four maintained oracles; deterministic local
packaging, hosted dry run, publication preconditions, canonical publication, and hosted plus
independently repeated anonymous exact/latest boundaries all fresh-passed. The dry run's publication
and post-publication jobs were `skipped` as required. No required final gate was reused, skipped,
unavailable, failed, or unrun.

The exact public 16,283,872-byte candidate is SHA-256
`7215a4f1d00588b36410da62e781cc8b861f6ea051219639049372677732e18f`.
Annotated tag object `07590034938283fabe48721980290dd24c1f72e3` peels to `R`; canonical run
`33464502157` attempt 1 published immutable latest release `380190031`, archive asset `538870315`
at SHA-256 `7cffbbb45f114048dcf7f5df9e3a4ae9fe6661edbb96a9015c202b648e1ff173`,
and checksum asset `538870344` at SHA-256
`31909dc2f1a73de8c74bfecfe4f123f8b8da0f825d156b54c200f04f55dac25a`.
Both public paths separately passed owned attestations, strict extraction and static inspection,
distributed HTTP, outbound HTTPS/NIP-11, and stateful HTTP from empty `minimal`; exact/latest bytes
were compared only afterward.

Product 0.1.14, CLI 16, compact-change records 8, and stateful acceptance 4 are public. The release
adds no graph meaning: it publishes the preceding authority contraction for exact dependency,
component, requirement, function-backed-port, and target authoring plus unified recipe lowering;
private recipe owner/snapshot/semantic-ID authority remains deleted. Project creation 3 and graph,
codec, plan, deployment, artifact, compiler, runtime, data, registry, and adapter identities remain
independently unchanged. Maintained standard and `lkjournal` assets matched exactly.

`R`, `main`, and only annotated `v0.1.14` were normally pushed; one `publish=false` dry run was
dispatched, the authorized confirmation variable was bound exactly to the tag object, and the
canonical tag-triggered workflow performed the sole release/asset publication. No workflow rerun,
exact-tag recovery, additive patch, or content-changing retry ran. One independent verifier check
failed before any application execution because local artifact extraction applied mode `0750`;
after exact byte verification its transient copy was normalized to declared mode `0755`, and all
six application oracles passed without retry. The optional `file` utility was unavailable; both the
first-party ELF parser and independent `readelf` inspection fresh-passed.

The final pre-`E` reconciliation found `R == origin/main`, zero divergence, no active release run,
exact immutable/latest state, no open issue or pull request, and unchanged immutable `v0.1.13`.
Stash object `87c2265f6faf0e815d80450ec2480d46c5e774fb`, pre-existing ignored artifacts,
and maintained derived roots remain preserved. No predecessor publication, package registry,
deployment, hosted/user data, issue/PR, setting, permission, secret, credential, history, or other
published object was mutated; no deployment or live relay was contacted. Next candidates are Nostr
WebSocket/NIP-01 session semantics, separate history/retention and recovery work, lightweight
million-owner capacity closure, and owner detail only when a maintained workflow requires it.
