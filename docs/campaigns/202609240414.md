# Native repository-policy adoption and reusable pure tools

## Original mandate (unchanged)

https://github.com/lkjsxc/lkjscript@Remote @Remote Desktop Commander lkjsxc/lkjscriptについて進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。限界まで深く考えてほしい。超長期的な視点からお願いしたい。ChatGPTの開発環境として、cw-bd235945fff44468a0f41292を用意しました。自由に活用ください。remote desktop commanderについて、月ごとにtool callingが1万回までという制限を考慮しつつお願いします。

## Reconciliation and decision

On 2026-09-24 Asia/Tokyo, local and remote main both contain `41861c85`.
The preceding native-guide implementation `ee2c6079` and its full source proof
are integrated; the old authentication blocker is no longer current. Normal
push dry-run succeeds. The checkout is clean with one main worktree, no stashes
and no active repository build. Rust 1.98.0 is installed in the existing Cargo
home; 8 GiB memory and adequate disk are available. Root guidance remains valid.
Remote operations are grouped to avoid fine-grained calls and repeated polling.

The guide program already shares its prepared immutable program. Do not invent
a redundant preparation optimization. Select the existing required no-Python
policy as the next native development-tool consumer: its filename-extension and
bounded raw shebang decisions can be ordinary native functions. Rust retains
Git path observation, filesystem admission/read limits, reporting and exit codes.
Keep the existing gate, exact classification, order, errors and report bounds.
Do not move policy into a special intrinsic or decode raw bytes as lossy text.

Extract the guide's grant-free pure embedding boundary into a reusable small
host API and use it from both maintained native tools. Preserve strict artifact
admission, exact target/type admission, task rejection, cancellation, fresh
invocation state and output bounds. It is not a sandbox or compiler self-hosting.

Author the new program through the copied public product, retain literal input,
reproduce/edit/draft/run it detached, and replace the old production decision
code rather than keeping competing implementations. Retain independent test
expectations and byte-level comparison against the predecessor semantics.
Measure matched complete policy costs; retain unfavorable outcomes. Run focused
feedback, then stable dependency-complete acceptance and normal main integration.
No frozen release bytes are changed. Publication is separate from mainline delivery.

## Implementation and focused evidence

`tools/native-policy/project` was created through a copied executable using the
literal native proposal. Its accepted revision is
`rev_789555a65fd64a1d0673778d125c34973ec26a5ec045a5b157b9030664daa19e`.
The ordinary public build produces the 750,750-byte complete bundle. Eleven native
policy tests plus 45 tests of the unchanged exact standard supplier pass in both
execution tiers. No storage or bytecode was authored directly.

The existing required gate now observes extension strings and at most 512 raw
bytes per regular file, invoking native decisions in batches of 64. Extension
precedence, path order, complete violation counts, the report cap and error exits
remain. Its old Rust classification functions are deleted; the independent byte
oracle exists only in tests. `platform::native_tool::PureTool` owns shared strict
artifact preparation and grant-free execution for this tool and all eight guides.
It exposes neither writable meaning nor a capability grant and is not a sandbox.

Focused checks pass: workspace compilation, all-target/all-feature warnings-denied
Clippy, nine policy tests, and three copied-public-CLI tests. The byte oracle covers
64 case patterns and 1,536 single-byte substitutions, plus separate raw/first-line/
read-boundary cases. Filesystem tests cover final links, missing/directory extension
precedence and 137 observations crossing three batches. The public tests verify
exact clean reconstruction of the maintained bundle, fresh authorship, canonical
no-op/edit, native differential checks, and old/new execution after removing
source. A task with an empty effect row and a pure port with an unused component
requirement both reject before deliberately trapping bodies. Strict malformed
bundle/input rejection, output-byte admission and cancellation/recovery also pass.

The same three public tests were repeated with the retained official v0.1.44
executable from the preceding campaign; all pass. That is reuse of its documented
acquisition plus fresh behavior, not a new anonymous download. Public CLI commands
execute copied binaries with an empty PATH. The current debug product also verifies
all eight generated pages unchanged. The first public-test compile rejected a
shadowed local variable in the new test; it was corrected before any behavioral
acceptance. No failing result has been relabelled as passed.

Focused logs, original authoring inputs/plan, predecessor executables and matched
cost observations are retained under `.artifacts/202609240414-native-policy/`.
Full-profile acceptance, measured costs and normal mainline delivery are still
separate obligations at this implementation checkpoint.
