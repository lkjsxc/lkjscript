# Native durable editor — 2026-09-25 12:11 Asia/Tokyo

## Original user mandate

> @Home Coder MCP https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscript  について進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。限界まで深く考えてほしい。超長期的な視点からお願いしたい。ChatGPTの開発環境として、cw-bd235945fff44468a0f41292を用意しました。自由に活用ください。

## Reconciliation and selected contract

Home Coder identifies the authorized machine as workspace `tomato-ocelot-73`,
ID `bd235945-fff4-4468-a0f4-1292e6333578`. Work remains in
`/home/coder/workspace/lkjscript`. Initial local and freshly fetched remote main
are `d7f57cb8cbbdb2694d5a1946649b5447e0074633`. There are no stashes or other
worktrees and no relevant active verification. The pre-existing untracked
`docs/campaigns/202609242022.md` is unrelated and is preserved.
Root guidance remains suitable and is not replaced. The pinned compiler is Rust
1.98.0; the workspace has 8 GiB RAM. Explicit Cargo/checker concurrency will be
selected independently. The form increment's accepted source is already on main;
its originals and the immutable v0.1.45 release are not changed.

Implement a useful durable browser editor as ordinary native composition. Reuse
the UI/form libraries, HTTP runner, configuration, secret verification and
transactional data store. No application-specific kernel intrinsic, JavaScript
runtime, browser script, external database or direct graph writer is selected.
App authors must not write HTML/CSS/JavaScript. The UI library owns fixed rendering.

The workload must demonstrate typed POST/multiline/hidden controls, bounded
strict forms, explicit text validation and line-ending policy, trusted configured
origin/Host admission, separate authentication before body/data access,
conditional transactional saves, visible conflicts retaining the draft,
GET without mutation, POST/redirect/GET, restart persistence and source-free use.
There is no automatic overwrite or live-effect replay.

The shared credential is a small access model, not accounts, an identity provider,
session management, public Internet hardening or TLS. Keep credentials and
operational state out of source/artifacts. Use only owned disposable local tests.

## Implementation and acceptance plan

Extend the ordinary UI library additively without replacing its text joining
strategy. Add a literal editor and native/public-CLI regression coverage.
Observe independent HTTP clients and concurrent writers. Include malformed
input, duplicate/missing headers, stale base, storage failure, Unicode/newlines
and renderer injection. Prove public plan/apply/check/build and detached use
through a copied executable. Obtain browser evidence where possible and report
its actual boundary.

Review the final diff and dependency-complete acceptance before normal mainline
integration. Re-read remote main/protections; never force push. Select a successor
release only after the durable-editor trigger is actually met and through the
maintained release owner. Do not relabel the frozen v0.1.45 assets.

## Execution

### Implementation and focused observations

Implemented ordinary native `editor.lkjc`, 38 pure policy/domain tests, an
operator-facing loopback deployment descriptor, a complete guide and a maintained
public-CLI integration workload. The UI supplier adds fixed POST forms, hidden
inputs, labeled multiline fields and line-preserving text blocks. The application
uses the existing form codec, configuration, secret verifier and DataStore rather
than a new runtime feature. The old private text-joining strategy remains intact.

Public authoring uses copied executables, literal inputs and exact package exports;
only returned metadata is substituted. The retained development-v0.1.45 proof ran
95 UI, 106 forms and 165 combined editor tests with production/reference agreement.
The editor's 38 tests are new; shared supplier/standard/starter tests are not.
The maintained integration test removes library producers and the application
project before serving, and additionally checks the unchanged GET UI consumer
against the extended supplier. The source contribution is not an installed
first-party app or a self-hosted compiler.

The first independent HTTP attempt closed the client's write side too early and
received no response; the driver was corrected to write the request and let the
HTTP response close the connection. The initial native editor proposal had two
parenthesis/arity errors, and a separate data-fixture proposal missed a closing
parenthesis. These were real rejected proposals, not partially published meaning.
An attempted extra UI injection-test edit was denied by tool safety review and was
not applied or retried through another path. Existing escaping tests remain;
that unavailable proposed test set is not counted as passed.

The initial browser trial lacked system libraries. Only test prerequisites were
installed on the authorized development workspace (34 new Ubuntu packages,
no package upgrades/removals; about 223 MB installed), with Playwright 1.63.0 and
Chromium headless-shell 153.0.8010.12 in the ignored campaign directory. The first
package acquisition encountered a stale mirror URL; refreshing the package index
resolved it. These are verifier prerequisites, not application dependencies.

Actual browser operation then exposed a real application defect: `no-referrer`
makes a non-CORS form POST send `Origin: null`. The editor correctly rejected it
with 403, so a raw HTTP pass was insufficient. The response policy is now
`same-origin`, consistent with Fetch's Origin algorithm; null/missing/foreign and
duplicate origins still reject. Corrected Chromium operation, with page JavaScript
disabled, passed real authentication, four form POSTs, redirects, two-tab conflict,
explicit review and refresh. It preserved initial LF, Japanese text and emoji.
All ten observed requests were same-origin documents, and the 360px viewport had
360px document width. The external browser measurement is not application script.
Original failed browser root: `/tmp/lkjscript-editor-browser-I5r7wx`.
Corrected development-v0.1.45 root: `/tmp/lkjscript-editor-browser-E3ARYi`.

Independent raw HTTP completed 36 requests across three joined lifecycles,
including eight writers competing across two processes: exactly one committed and
seven received conflicts retaining their individual drafts. Status counts were
200:4, 303:3, 400:8, 401:4, 403:4, 404:2, 409:8, 413:1, 415:2.
The earlier development root is `/tmp/lkjscript-editor-runtime-UlBr6b`.
The maintained integration workload additionally covers incompatible/corrupt
stored values and revision exhaustion through a separate ordinary native command
against only disposable data. Its earlier release-profile run completed one test
in 20.22 seconds (`public-editor-release-4.log`); subsequent fixture-strengthening
changes require their own acceptance and are not attributed to that earlier run.

### Successor selection and continuation

At successor selection, remote main was still `d7f57cb8`, v0.1.46 had no matching
tag or release, and the preceding v0.1.45 producer and promotion had completed
successfully. The durable-editor publication trigger is met; select v0.1.46 without
rewriting any old tag, archive, selection history or application data. The version
change affects Cargo.toml/Cargo.lock; all eight generated guides are regenerated
through the actual public product. Meaningful notes are in `docs/releases/v0.1.46.md`.

The resumed session found the v0.1.46 build completed and no remaining build job.
Its fresh copied-product authorship again passed 95/106/165 graph tests. The
current roots are `/tmp/lkjscript-native-editor-NbGtat` (literal inputs, plans,
exports and bundle), `/tmp/lkjscript-editor-browser-XSzphg` (real browser,
operator-facing descriptor and screenshots), and
`/tmp/lkjscript-editor-runtime-vOx48k` (the independent 36-request observation).
Those observations used the newly built 0.1.46 executable; they are not evidence
for a finalized musl archive or public distribution. Browser and raw HTTP processes
were joined. Original logs/drivers remain in `.artifacts/202609251211-native-editor/`.

Home Coder long command transports sometimes returned `Connection closed` while
the owned command continued. Commands are not restarted merely because the tool
connection ended; retained logs and actual process state are checked first. The
maintained verifier will own source acceptance; diagnostic script logs do not
replace its original receipt. Current main integration, candidate acceptance and
publication remain pending until their actual results are appended below.

The strengthened focused release-profile workload also passed, in 20.69 seconds
(`public-editor-release-5.log`). A DataStore grant with a one-byte value limit
produced HTTP 500 on the attempted save and left the store HEAD unchanged; the
owned server joined. This operational failure was not classified as a malformed
form, successful commit or safe automatic retry. A final fresh source-tier run
is still required after all test/document inputs stabilize. Source acceptance
uses the maintained 20-gate `release-source` profile; the six archive-bound
behavioral owners and installation/userland proof belong to candidate acceptance,
not an invented duplicate local full suite.

### Final development-binary observations

A further v0.1.46 public pass at `/tmp/lkjscript-native-editor-ItFbpF` retains fresh
literal authoring inputs, complete plans, supplier exports and independent bundles.
UI/forms/editor pass 95/106/165 tests; the unchanged GET site explicitly rebuilt
against the updated UI supplier passes 113 tests (response library 83), all with
production/reference agreement. These overlapping counts are not summed as new
independent tests. The eight generated guides independently verify as current for
v0.1.46 through `capabilities --verify-generated docs/generated`.

The operator-facing descriptor, with only owned ephemeral address/origin changes,
passes actual Chromium 153.0.8010.12 operation at
`/tmp/lkjscript-editor-browser-PKYGtf`: ten document requests, four POSTs, no page
JavaScript, same-origin traffic, explicit conflict review and 360px content width.
`/tmp/lkjscript-editor-runtime-fYW27d` separately passes the same 36 raw HTTP cases
and one-of-eight two-process conditional-save race, with three joined lifecycles.
These are development-host executable observations, not final-musl-archive proof.

The local v0.1.46 product/verifier build completed in 6m26s. Its focused final
public CLI test uses the literal descriptor and additionally requires an operational
put failure not to publish data. The original transcript is
`.artifacts/202609251211-native-editor/public-editor-release-5.log`: it completed
successfully, one test passed with 86 filtered out in 20.69s, after 4m31s compilation.
That result includes the failed-put/no-publication assertion and the operator-facing
descriptor. It is focused feedback, not the dependency-complete source acceptance.

For this selected release, dependency-complete acceptance follows the existing
release-source mapping: twenty fresh source gates plus six behavioral owners
against the finalized distributable at candidate admission. Do not label the
source profile alone as standalone full 26/26. Select one Cargo job and one checker
worker on this 8 GiB host, and execute through an immutable verifier copy after the
source is committed. Preserve exact source identity and stable inputs throughout.
The prior unrelated campaign remains outside the intended index.

### Source-tier interruption and test-fixture repair

The next continuation independently reread local source `e2620cc532d0c3f4a1bbcccf43836d39c0467930`
and fetched remote main `d7f57cb8cbbdb2694d5a1946649b5447e0074633`.
While the source-tier run `1790309674698605738-613644-0` was still active,
review found that the newly added unchanged-GET-consumer check exports the same
UI library twice to one `library.lkjp` path. A copied-product reproduction against
an already owned UI export correctly returned `output_conflict`. The product's
no-overwrite rule is correct; this is a test-fixture defect added after earlier
focused acceptance, not a reason to relax product admission.

At 13:23 JST the exact owning verifier process was interrupted with SIGINT rather
than finishing a known-defective broad run. The script transcript records exit 130;
subsequent process inspection found neither that verifier/Cargo process nor a
remaining compiler/test child. No terminal success or original-reader acceptance
is claimed for this interrupted run. Its logs and original inputs are retained.

The fixture now writes each supplier export to the independent consumer's owned
path and removes that transfer file after public staging. Both the editor and the
unchanged GET consumer must still check/build against the same exact UI supplier;
no assertions or source/runtime gates are removed. A successor commit and fresh
source-tier acceptance are required. This is not a change to editor behavior,
credentials, stored data, or any already published release.

The repaired focused workload passed in 31.04 seconds after 1m10s compilation
(`public-editor-release-6.log`), including the unchanged GET consumer and removal
of transfer files after staging. Its Cargo-reported test executable is
`target/release/deps/public_cli-5a145ebbffd14b4e`; one selected test passed and 86
were filtered out. No source-tier or final-archive acceptance is inferred from
that focused result. The verifier implementation is unchanged by this test-only
repair, so its already retained immutable executable can run a fresh source tier
against the successor commit without a redundant verifier rebuild.

### Product-surface inventory correction

The successor source `7d08319fad54914922e8abc2c3a10eff6546bc00` completed the fresh
source-tier run `1790310395103027809-621183-0` with stable inputs, zero result reuse,
19 passed gates and one failed gate in 859.658728038 seconds. The complete workspace
suite passed. `product_surface_audit` rejected the new `editor.deployment.json`
because the examples directory previously admitted only `.lkjc` entries. This is
a real incomplete source acceptance; retain its failed receipt and logs unchanged.

The audit now explicitly owns that exact descriptor path in CURRENT_FILES. During
directory enumeration only explicitly owned paths may differ from the directory's
normal suffix. Every such path still goes through the same bounded regular-file
read and content audit, and duplicate inventory entries are deduplicated. This is
not a blanket JSON allowance or a content-audit exemption. Unknown `.json` and
`.deployment.json` names, missing required descriptors, directories and symlinks
remain rejected. Two new tests cover explicit-file scanning exactly once and
regular-file admission; the existing unowned-entry tests now include both JSON
suffixes. All five focused surface tests pass. The actual public-surface audit
passes with 69 scanned files, 26 executable commands, zero exceptions and violations.

This correction changes the verifier implementation, so the predecessor verifier
must not certify it. Build a new pinned release verifier, retain an immutable copy,
commit the reviewed correction and run a fresh 20-gate source tier on that exact
source with one checker worker and one Cargo build job. Do not relabel the failed
19-of-20 receipt or waive the failed gate. Product application behavior, grants,
secrets, deployment data and the prior immutable release remain unchanged.

### Accepted source and mainline delivery

Corrected source `b25bde2d92c1882470fc4444c94319a68fe9a10c` (tree
`89ddde3853d4f53850bba61adfa1afd53e493eeb`) passes all **20 release-source gates
freshly**, with zero result reuse, no unrun gates and stable initial/final inputs.
The original run is `1790311774672592153-637605-0`, with its receipt under
`.artifacts/lkjscript-dev/check/`; elapsed time is 867.885697679 seconds.
The script transcript `source-b25bde2d.log` records exit 0 at
2026-09-25 14:04:02 Asia/Tokyo. The owning process exited; no replacement run or
relabeling of the earlier interrupted/19-of-20 attempts was used.

This is source-tier acceptance, not standalone full-26 or finalized-archive proof.
The new immutable verifier was built in 3m04s and retained as
`.artifacts/202609251211-native-editor/source-verifier-b25bde2d/lkjscript-dev`.
The actual surface gate scans the descriptor together with the other public files,
with zero exceptions and violations. Workspace, lint, generated assets, current
standard/application owners and the remaining required source gates all passed.

After refreshing remote main and confirming no required branch rules/protection,
a normal fast-forward push integrated `e2620cc5`, `7d08319f` and `b25bde2d` from
`d7f57cb8`. An independent GitHub branch read confirmed remote main at the exact
accepted b25bde2d source. No force update, history rewrite, protection change,
credential change or production deployment occurred. The unrelated untracked
`docs/campaigns/202609242022.md` remains untouched.

### Candidate delivery boundary

After independently confirming the mainline source, absent v0.1.46 tag/release and
no matching candidate, one `operation=candidate` dispatch was accepted. Original
producer [36097087012/1](https://github.com/lkjsxc/lkjscript/actions/runs/36097087012)
was created at 2026-09-25 14:05:20 Asia/Tokyo with product/controller event source
`b25bde2d92c1882470fc4444c94319a68fe9a10c`. Its observed state is **in progress**.
No candidate terminal, final-archive acceptance or publication is claimed yet.
The selected v0.1.46 annotation and release-scoped selection have not been created
or changed by this campaign; immutable v0.1.45 remains the public release.

Continue at that exact producer/attempt. First require its source, finalized
candidate, installation/userland and original-reader acceptance and successful
terminal. Only then create the ordinary matching annotated tag from the accepted
source and reviewed `docs/releases/v0.1.46.md`, read/check/write/read back the
explicit release-scoped selection, and dispatch the configured promotion once.
Do not rebuild a healthy accepted candidate or treat a green substep as publication.
Later reporting-only commits must not relabel the b25bde2d source receipt or this
producer's event source.

### Independent edit, update and recovery observations

Separate copied-product observations used retained disposable application roots,
not the compiler checkout's tracked source. A page-only draft was 7,134 bytes
versus a 44,691-byte module draft for the same intended button-label edit at the
same initial revision. Both exported plans were 300,765 bytes/976 records, with
68 recreated internal owners and one updated declaration. They are not identical
plans, token-cost measurements or compulsory model-input sizes. Only the page
plan was applied; the page declaration identity survived, all 165 graph tests
still agreed, and the old module proposal was rejected as stale.

At `/tmp/lkjscript-editor-upgrade-yLKKOH`, the original app saved note revision 1,
the separately built label-edited app read it and saved revision 2, and the original
app then restarted and read revision 2 unchanged. Seven HTTP requests, two commits
and three joined lifecycles passed with Japanese/leading-LF data. This is an
application-bundle update/rollback on the same runtime, not universal schema
migration or runtime-downgrade safety.

An independent scratch refactor moved the label into a private ordinary constant.
Its one-time refactor plan cost 306,932 bytes; a subsequent constant draft was
544 bytes and its complete plan 17,274 bytes/48 records. This requires the prior
refactor and is not a same-revision comparison with the original function edit.
It is not adopted into the selected source. It supports investigating ordinary
application structure before inventing a new compiler edit mechanism.

Retained recovery observations at `/tmp/lkjscript-editor-backup-VaIJRV` report a
397-byte logical backup, seven public data operations and three HTTP requests.
The separately restored store preserved revision 2 and accepted a new revision 3;
the source store stayed unchanged. Existing output/destination and truncated backup
cases rejected without overwrite or partial destination publication. These are
application-data observations, not meaning-graph backup, encryption or replication.
The drivers, literal drafts, plans and originals remain in the ignored campaign
area and their recorded disposable roots.

The retained public v0.1.45 executable also rejected the new editor bundle with
exit 5, `artifact_nominal_meaning` / `intrinsic_unknown` for `core.bytes.from-list`,
before readiness. That is expected compatibility rejection, not an execution pass
on the old release. Candidate-executable observations must select the authenticated
finalized binary separately; the optional explicit path in `author.mjs` supports
that observation without changing any application source or passing credentials
to product child processes.
