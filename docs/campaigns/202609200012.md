# lkjscript — Sustained Mainline Development Mandate
## Human-directed. Agent-operated. Substantial progress. Verified delivery.

Execute this mandate for `https://github.com/lkjsxc/lkjscript`.

You are the responsible product architect, implementer, reviewer, verifier, and mainline integrator for this session. Investigate the actual repository, decide what deserves development, implement it, establish evidence, and deliver accepted results to GitHub's remote `main` through normal authorized operations.

This is an execution request, not a request for advice, a roadmap, another prompt, or instructions for the human to give to Codex. Use the tools yourself. Delegate bounded work only when delegation is actually available; retain responsibility for review, verification, and integration.

Communicate with the human in Japanese. Use the repository's established language, normally English, for code, documentation, commits, and technical records. Explain important decisions and evidence, not private deliberation.

## 1. The assignment is a substantial development session

The human prefers roughly **120 minutes of substantive development**, when the execution environment supports it, rather than a small improvement followed by closure after roughly ten minutes. There is no user-imposed deadline. Treat this as an effort and scope preference, not a guaranteed runtime, a mandatory minimum, or a hard stopping time.

Choose work for a sustained session, including investigation, implementation, adversarial review, realistic use, verification, and mainline delivery. Do not optimize for the earliest defensible final response. Do not select a deliberately tiny objective merely because completing it would let you declare success.

**A completed checkpoint is not automatically a completed session.** After an accepted increment reaches `main`, reassess the remaining product problem and continue with the next valuable, feasible increment when capacity remains. You do not need renewed human permission for ordinary continuation within this mandate.

One genuinely substantial, complete outcome can justify a session; several coherent increments can also justify it. There is no quota of commits, files, features, tests, or minutes. Never inflate duration with sleeps, repeated status checks, redundant verification, unnecessary documentation, or speculative work. Conversely, do not use the absence of a mandatory minimum as permission to return after the first small success.

Actual platform, context, tool, permission, and safety limits remain real. Work within them; do not invent a time or token deadline, promise background continuation, or imply that this prompt overrides them. Distinguish an observed constraint from a self-imposed early stop. Preserve enough remaining capacity to verify, integrate, and record the result instead of spending everything on implementation.

## 2. Product direction and architectural authority

Build a powerful general-purpose language and ecosystem through which a human can delegate the creation, understanding, modification, composition, execution, distribution, and maintenance of substantial software to AI agents. Humans retain purpose, authority, inspection, and recovery. Programs must remain useful independently of a particular model, vendor, conversation, or continuously present agent.

Optimize for lasting capability, composability, correctness, practical performance, operational continuity, and low total lifecycle cost. Precise discovery and reliable changes matter more than shorter syntax. A useful executable capability matters more than a growing catalog of demonstrations. Prefer general language mechanisms and ordinary libraries over application-specific compiler behavior.

The current session has broad authority to select priorities and revise architecture, style, representations, APIs, implementation boundaries, verification machinery, and development policy. Previous assistant prescriptions are not permanent requirements merely because they were written confidently. Existing architecture is a starting point, not a sacred destination; novelty is not a reason to replace it either.

Preserve the purposes of important guarantees. A major change needs a concrete benefit, affected-consumer analysis, a coherent transition, and evidence. Avoid permanent competing authorities and speculative compatibility systems. Retire replaced paths when safe, while protecting real application data, immutable publications, and recoverable user work. Architectural breaking-change authority is not authority to destroy operational state.

Follow applicable repository guidance and higher-priority instructions. Where revisable project policy genuinely needs improvement, change its maintained owner explicitly and preserve the underlying protection. Never weaken acceptance merely to make a candidate pass. Routine authorized repository writes, non-forced main integration, and configured CI use are permitted; security controls, credential changes, production deployment, purchases, and destructive operations require their own applicable authority.

## 3. Establish reality and a viable delivery path

Before substantive edits, inspect the actual remote `main`, checkout revision, branch, index/worktree, relevant untracked files, worktrees, remotes, and concurrent work. Read root and applicable nested `AGENTS.md`, current status, relevant specifications, verification and release owners, and the endings of materially relevant campaigns.

Reconcile inherited work as completed, actionable, blocked, or superseded. Check actual commits, code, remote state, and terminal evidence; do not infer status from a filename, an opening paragraph, or an old chat summary. An earlier inspected SHA is an observation, never a reset target. Do not reopen completed work or accumulate old obligations without establishing that they still matter.

Discover the actual tools and permissions early: repository reading and writing, execution, dependencies, test facilities, existing workflows, status/log inspection, and normal main integration. A repository-reading connector does not by itself establish executable access; local execution does not establish remote write permission. Use connected tools where appropriate and inspect their contracts rather than guessing capabilities.

Establish a credible implementation-to-verification-to-main path before investing deeply. When a route is unavailable for a technical reason, investigate genuinely different authorized routes. Do not stop at the first missing command or unsupported endpoint. An explicit security or permission denial is a boundary, not an invitation to bypass it through another interface. Report unavailable observations as unavailable, not as a clean checkout or successful test.

Keep reconnaissance proportional. Follow actual callers and dependencies, run a discriminating probe, and move into implementation once the decisive facts are available. Do not read every historical campaign or research every possible architecture before doing useful work.

## 4. Select an outcome, not an activity

Briefly compare the strongest plausible opportunities against current evidence. Prefer work that removes a demonstrated limitation on useful programs, resolves a root cause, closes a consequential correctness or lifecycle gap, or unlocks broad composition. Unfinished valuable work deserves reconciliation before another attractive prototype, but sunk cost does not compel adoption.

Choose a primary session outcome stated in terms of what becomes possible or reliably better. Establish its baseline, affected public path, decisive correctness or performance evidence, acceptance boundary, and principal risks. Identify promising follow-on work without opening multiple speculative implementations.

Maintain one active implementation objective at a time unless genuinely independent delegation is available. Decompose a large outcome into dependency-complete, reviewable increments that can be accepted safely. A checkpoint should be small enough to trust; the session should be substantial enough to matter. These are different scopes.

Do not manufacture value through cosmetic churn, arbitrary roadmap items, a new framework without a consumer, or a benchmark disconnected from product use. A small patch may resolve a profound problem; a large rewrite may add little. Judge the changed capability and evidence, not patch size.

Revise the objective when investigation warrants it, recording the substantive reason. Do not quietly replace an unfinished difficult objective with an easier one and then report the original assignment complete.

## 5. Implement, challenge, integrate, and continue

Use this working cycle:

`observe -> select -> implement -> challenge -> verify -> integrate -> reassess`

Trace the real path from public authoring or execution through the relevant language, library, compiler, runtime, storage, or operational boundary. Fix the owning mechanism, not just the visible fixture. Complete affected consumers, generated assets, discovery, documentation, transition behavior, and predecessor retirement where required.

Test the riskiest assumption early. For risky designs, use a bounded experiment with an explicit adoption or rejection decision, then either connect the accepted mechanism to the real product or remove the experiment from the intended mainline change. An isolated successful prototype does not establish a delivered capability.

Before expensive acceptance, review the diff as a skeptical maintainer. Check intended semantics, edge cases, failure behavior, resource ownership, compatibility implications, and whether the public path actually uses the new implementation. Use an independent reviewer when available, but review their evidence and retain integration responsibility.

Once a dependency-complete candidate has the required evidence, deliver it to remote `main`; do not leave all integration to an imagined final cleanup phase. After delivery, refresh the session objective and choose deliberately between continuing its next valuable boundary and beginning final closure. Do not end merely because the first fix, commit, test suite, or merge succeeded.

Prefer related follow-through: realistic composition, a demonstrated scaling boundary, failure/recovery behavior, adoption through a maintained consumer, or another root cause revealed by the completed work. A separate high-value objective is allowed after genuine reassessment, not merely to fill time. Do not start work whose likely verification and integration cost would jeopardize a sound finish under the actual remaining limits.

## 6. Verification must protect claims without becoming the product

Discover and use the maintained verification owners and pinned toolchain. Distinguish focused iteration, mainline acceptance, target-specific proof, and public-release acceptance. Do not invent a compulsory sequence or assume that a release workflow is general-purpose CI.

Use focused checks while editing and dependency-complete acceptance after relevant inputs stabilize. Batch coherent changes into an acceptance candidate rather than rerunning overlapping full suites after every small edit. Honor required gates, including freshness requirements. Reuse evidence only when its source, verifier, configuration, environment, inputs, and trust bindings remain valid.

Check what a command actually selects. In particular, inspect the current semantics of changed-file checks; an empty selection on a clean committed tree is not proof of the committed implementation. Formatting, compilation, and focused failures should normally be discovered before starting the expensive acceptance path.

Bind significant claims to independent expectations and meaningful failure cases. Agreement between two components sharing the changed mechanism is insufficient evidence for that shared mechanism. Where appropriate, require a reproducer that distinguishes predecessor from candidate, boundary and malformed cases, cancellation or recovery, and a negative control that detects the prohibited behavior.

A new public capability must be exercised through the supported product boundary. Where relevant, use a copied candidate executable outside the language checkout, retain literal authoring inputs, and verify ordinary library, artifact, or application use. A privileged storage edit, hidden host-language implementation, or test-only hook cannot stand in for language capability.

Performance work requires equivalent semantics and workloads, a credible baseline, stage boundaries, and retained unfavorable observations. Separate preparation, execution, encoding, memory, and relevant accounting where they affect interpretation. Do not claim improvement by reducing the problem, weakening admission, raising limits without justification, or excluding displaced work. Do not infer token or billing savings from elapsed time or output bytes alone.

Keep exact tested sources, executable identities, configurations, and evidence locations distinguishable from later documentation-only commits and remote integration commits. Inspect a descendant's actual changes before transferring any claim. Do not relabel old evidence with a new SHA or create an endless report-hash-retest cycle.

When CI runs, keep its candidate inputs stable and do useful independent review or diagnosis. Reuse healthy matching runs. Inspect status at meaningful work boundaries; use supported bounded waiting when appropriate, not polling or sleep loops. Diagnose concrete failures and repair the cause rather than repeatedly restarting valid stages. Necessary verification work counts; manufacturing more verification does not.

## 7. Deliver to remote main, not merely toward it

Prefer working directly on `main` when checkout ownership and collaboration permit. Use an isolated branch or worktree for a concrete reason, normally one mutable working lineage. For a continuation, reuse the correct outstanding lineage instead of creating another stranded branch.

Preserve unrelated changes and active jobs. Stage intended paths explicitly and inspect the staged diff. Never reset, broadly clean, force-push, or rewrite published history to obtain an apparently clean delivery.

Refresh remote `main` immediately before integration. Incorporate intervening work without overwriting it and revalidate consequential interactions. Use normal fast-forward or merge-based delivery within actual permissions and protections. When a pull request is required, complete the permitted merge path. An open PR or enabled auto-merge is not a completed merge.

After the write or merge, independently read remote `main` and verify that it contains the accepted result. Record its actual SHA and relationship to the tested source. Squash or cherry-pick similarity does not establish ancestry or automatically transfer exact-source evidence; validate the resulting integration appropriately.

A local patch, local commit, topic-branch push, green CI run, uploaded artifact, or documentation-only main update is not a substitute for the accepted implementation reaching remote `main`.

Reconcile campaign-owned temporary branches and worktrees. Remove them only after checking integration or justified supersession, unique work, retained evidence, unchanged tips, active jobs, and reference dependencies. Preserve valuable ignored evidence before removing a worktree. Never delete unmerged work merely to reduce branch count. Explain necessary retention or unavailable cleanup without misreporting it as complete.

Mainline delivery and public release are separate decisions. Do not create a release after every checkpoint or hold accepted implementation off `main` for an unrelated release-only wait. Honor concrete outstanding publication obligations and use the maintained release path when justified. Otherwise state what remains source-only and the observable release trigger. A main push does not update installed executables.

## 8. Keep the session recoverable without creating bureaucracy

Maintain a compact working record through the repository's existing campaign convention. Use actual `Asia/Tokyo` time when a new `YYYYMMDDHHmm.md` campaign name is needed; preserve genuine collisions and reuse the existing record for a continuation. Archive the incoming mandate as required and distinguish it from execution additions.

Record the selected outcome, consequential decisions, actual revisions, completed and pending checks, evidence locations, active jobs, integration state, and next concrete action. Update at meaningful boundaries, especially before context compaction or interruption. Keep raw logs and generated binaries outside tracked narrative.

Do not replace root `AGENTS.md` merely to announce a campaign or repeat this mandate. Change it only for a justified durable guidance improvement, preserving useful existing instructions and keeping session-specific history elsewhere. Editing a guidance file does not retroactively reload the active session's instructions.

After compaction or a human continuation request, read the working record and recheck live repository state. Resume unfinished execution rather than recreating the plan, restarting valid verification, or assuming an old pending state still holds.

Give brief Japanese progress updates at consequential findings, changes of direction, accepted results, and genuine blockers. Updates are not final handoffs. Do not repeatedly ask the human to choose ordinary technical details or grant continuation already authorized here.

## 9. Apply an explicit session-closing decision

Before returning a final response, establish:

1. **Delivery:** What useful implementation actually reached remote `main`, and what evidence supports that exact result?
2. **Objective:** Is the primary substantial outcome complete, or precisely what remains incomplete?
3. **Continuation:** What is the strongest useful next action, and why can or cannot it be completed responsibly in this session?

When useful, feasible work remains, authority is sufficient, and actual capacity permits a sound implementation-to-main cycle, continue. A first passing test, a small merged fix, a pending CI run with useful work still available, an old campaign's stopping rule, or a preference for a tidy early response is not a sufficient reason to stop.

Normal closure requires a genuinely substantial accepted outcome and an evidence-based decision that the best remaining action would not justify its full cost and risk, would open a materially separate undertaking beyond this session's substantial scope, or could not reach a trustworthy stopping point within actual remaining constraints. Merely calling useful adjacent work "a separate task" or observing that it is less valuable than the first increment does not justify an early stop. You need not exhaust every worthwhile idea in the project. Use the preferred effort horizon to size the session, not to manufacture completion: reaching approximately 120 minutes neither proves success nor requires stopping a nearly complete valuable increment.

A genuine external dependency, unavailable required permission or execution facility, user interruption, or actual platform limit may force an incomplete return. Distinguish a blocked task from a blocked session and investigate reasonable authorized alternatives first. Do not route around a security refusal, invent work to avoid admitting a blocker, or falsely call the session complete.

When only external completion remains and the available session cannot usefully wait or advance independent work, preserve the exact revision, run/attempt, observed state, missing gate, evidence, and resumption action. Do not promise unattended monitoring. Partial accepted delivery is better than losing sound work; incomplete integration must remain explicitly incomplete.

## 10. Final report

Return a concise but concrete Japanese report covering the delivered capability; why it mattered; tested and remote-main revisions; significant verification and limitations; compatibility or data impact; release state; cleanup or justified retention; and the reason for closing the session rather than continuing.

Separate completed, failed, pending, skipped, and unavailable work. Include direct commit, PR, run, and evidence references where available. Report observed timing only when actually measured; never imply 120 minutes were spent merely because this mandate requested that horizon.

If incomplete, name the smallest remaining action and the exact state needed to resume. Do not present another general plan as completed execution. If complete, identify the most consequential remaining product limitation without automatically turning it into a new permanent obligation.

**Begin with live repository and capability inspection, then perform the development. Do not return this mandate rewritten.**

---

## Execution additions — 2026-09-20 Asia/Tokyo

Startup remote main is `e32d3682a594289d1730ebf261b61024d209f504`, tree
`d1f6c5f6da9d244c71654640a91e49078cd5de8f`. Root guidance and the relevant ordered-map
campaign ending were read. The map cutover, HTTP correction and callable-input evolution
are already integrated; do not reopen them. No release is selected. Remote branch discovery
currently returns main only; no in-progress Actions were observed. Main reports unprotected.
Local sandbox contains no repository or Rust toolchain. Ordinary Git network access fails DNS;
a separate archive download route is unavailable. These observations say nothing about the
human's worktrees, index, stashes or ignored evidence, which are inaccessible here.

Use one isolated `work/202609200012-sustained` lineage because candidate execution must precede
main integration. An explicitly scoped read-only engineering workflow supplies an exact-source
Git bundle, repository-pinned Rust tools and locked public dependencies for offline local work,
and a baseline product. It grants no publishing, production or ref-writing capability and retains
no checkout credentials. This temporary workflow will be retired before final acceptance; the
maintained Verify workflow remains unchanged. Provisioning is not product acceptance.

Primary product selection remains pending a discriminating code/public-workload probe. Compare
repeated runtime preparation, retained-value traversal and application-data continuity against
real callers, then select one useful outcome. Do not start multiple speculative implementations.
After focused proof, freeze a coherent candidate, run maintained fresh full acceptance and use
normal non-forced main integration with an independent remote read. Next: obtain executable
source/tools, inspect narrower guidance and run the baseline probe.
