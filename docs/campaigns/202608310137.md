# Current-Authority Scale and Retention Admission

- **Status:** terminated
- **Campaign:** `202608310137`
- **Audit time:** `2026-08-31 01:37:04` Asia/Tokyo
- **Repository/source:** `lkjsxc/lkjscript`, remote GitHub default branch `main`
- **Orientation baseline:** commit `649bf88a126c70ef6a88c3fb8c7d228978eae85c`, tree `f04e346ac6d170458c96af7ea957ebea609508b6`
- **Checkout boundary:** no active local checkout, local `HEAD`, index, untracked state, remotes, upstream relation, toolchain execution, build, test, benchmark, or copied binary was available to the upstream author
- **External boundary:** immutable latest release `v0.1.12`, release `379320112`, annotated tag object `d6d81e2398ced78023a8ea62d69e03f6b9f5d4da`, source `8a0141a151a87fe59ccc1ebc738a7e5dd51c6882`; no open issue, open pull request, queued workflow run, or in-progress workflow run was observed
- **Unavailable external facts:** local ignored evidence and receipts, installed binaries, deployments, hosted or user data, repository administration values, credentials, and non-public runtime state
- **Predecessor disposition:** campaign `202608302224` is complete and publicly closed; campaign `202608300840` is complete; no active or externally committed incomplete mandate was found
- **Archive:** `docs/campaigns/202608310137.md`; no distinct mutable active-work ledger exists
- **Planned evidence:** `docs/evidence/202608310137-current-authority-scale-retention.json`; raw output under `.artifacts/campaign/202608310137/` or the current ignored verifier-owned root

Preserve this initial mandate body. Completion may change only `Status` to `complete` and append the
bounded terminal record required in section 8. Termination may set `terminated` and append the exact
reason. A blocked handoff leaves `incomplete` and appends one concise blocked record. Current facts,
large measurements, receipts, inventories, and logs belong in their repository owners rather than in
this historical initial mandate.

## 1. Implementation mandate and stopping rule

This is a bounded implementation-and-evidence campaign for Codex CLI. It is not another planning
pass, a public feature campaign, a benchmark-only diary, a compactor implementation, or permission
to weaken current correctness gates.

**Final state:** the repository owns one deterministic Rust contributor boundary that replaces the
predecessor-bound `lkjscript-dev scale` path, exercises the current public typed-graph workflow and
the current first-party ordered-data authority at maintained, 100,000, and 1,000,000 scale plus a
bounded long-history workload, proves logical equality and failure containment through
implementation-disjoint oracles, and records an exact retain-or-escalate decision without changing
public product semantics or adding destructive retention behavior.

Complete when all of the following are true:

- the scale tool and its receipt use only current semantic, query, compiler, artifact, and data
  owners and reject the predecessor receipt/grammar;
- the finite semantic and operational-data matrices reach terminal measured results with exact
  input identities and required correctness proof;
- current-layout admission is classified separately for semantic storage, compiler/artifact work,
  first-party data, and long-history retention;
- a durable decision selects either continued retention of the present layout or a specifically
  bounded follow-on packing and/or compaction comparison;
- no public CLI operation, language behavior, accepted storage format, product release, deployment,
  or maintained application behavior changes;
- final verification and evidence classification are truthful, commits are coherent, and the tree
  is clean or preserved work is named exactly.

A measured **not admitted** result is a valid completion when the required experiment itself is
sound, reproducible, and terminal. Do not hide or “fix” a product correctness, durability, recovery,
or intrinsic-bound failure inside this campaign. Record it, preserve authority, select the smallest
repair or replacement boundary, and close the evidence campaign. A missing host prerequisite,
insufficient disk or memory before a required corpus can be formed, an invalid harness, or an
unresolved contradiction in exact inputs is `unavailable` or `failed` and blocks completion.

Terminate without implementation only if active-checkout reconciliation proves that equivalent
current-authority scale and retention admission already exists, or that a coherent conflicting
campaign has become authoritative. Symbol, path, contract-number, or command spelling drift is not
grounds for termination.

## 2. Reconciliation gate and read map

Before editing or running mutation-capable commands:

1. Read every applicable agent instruction, this complete mandate, the terminal records for
   `202608302224`, `202608300840`, and the bounded-context predecessor, plus current status,
   architecture, roadmap, performance, verification, semantic-storage, data-capability, product,
   and release owners.
2. Record actual branch, `HEAD` and tree, worktree/index/untracked state, remotes/upstream,
   divergence, recent commits, toolchain, product and contract identities, filesystem, available
   CPU/memory/disk, and unfamiliar work. Fetch without rewriting history. The active checkout
   outranks this orientation baseline.
3. Reconcile open work, later commits, releases, workflows, issues, pull requests, and any supplied
   files. Preserve unrelated work. Close, recover, or terminate coherent in-flight work before
   stacking this objective.
4. Establish the smallest predecessor baseline for the current `lkjscript-dev scale` command. Do
   not treat source inspection as runtime proof: record its actual help, exit classification, and
   receipt behavior against the exact candidate binary before replacing it.
5. Locate the canonical typed change-request owner, executable capability registry, current
   `GraphRepository` publication/read-view owner, normalized query and context owners, compiler
   cache and artifact verifier, `platform::data::DataStore`, logical backup owner, contributor
   measurement/evidence primitives, and current full-gate owner before changing them.
6. Use isolated absent temporary roots. Do not inspect, import, compact, repair, overwrite, or
   benchmark ambient, hosted, production, or user repositories or data stores.

Small read map after reconciliation:

- `AGENTS.md`, `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`;
- `docs/campaigns/202608302224.md`, `docs/campaigns/202608300840.md`, and the latest bounded-context
  terminal record;
- `docs/status.md`, `docs/architecture.md`, `docs/roadmap.md`, `docs/performance.md`;
- `docs/spec/semantic-authority.md`, `docs/spec/data-capabilities.md`,
  `docs/spec/product-surface.md`, and `docs/spec/verification.md`;
- `docs/decisions/20260822-object-packing-retention.md`,
  `docs/decisions/20260830-first-party-ordered-data.md`, and relevant canonical-store,
  incremental-validation, and compiler-invalidation decisions;
- `tools/lkjscript-dev/src/lib.rs`, `scale.rs`, `data_oracle.rs`, `measure.rs`, `evidence.rs`,
  `process.rs`, and the current check-gate owners;
- the `GraphRepository` publication/read-view implementation, the `DataStore` implementation and
  reference model, compiler/artifact owners, `tests/public_cli.rs`, generated change grammar and
  operations guide, and maintained standard/`lkjournal` authorities.

Names above are orientation until verified in the active checkout. Discover narrow current commands
through executable help and first-party owners rather than preserving this mandate's spelling.

## 3. Load-bearing current state and campaign selection

At the audit boundary, remote `main` ended at
`649bf88a126c70ef6a88c3fb8c7d228978eae85c`. Root product version `0.1.12` is also the immutable
public latest. The release closes bounded semantic context traversal and the complete first-party
ordered-data/durable-queue cutover in one static `x86_64-unknown-linux-musl` binary. The latest
campaign is terminal, and no active issue, pull request, or workflow run was observed.

Current maintained scale is small: the standard package has 409 live semantic owners and
`lkjournal` has 1,559. Current documentation explicitly does not claim a million-owner application
lifecycle, million-key data admission, long-history policy, live packing, compaction, or garbage
collection. Both semantic and operational stores retain immutable history.

The contributor tool still advertises `lkjscript-dev scale`, but baseline source binds scale receipt
contract 1 to public change contract 3 and invokes predecessor `inspect project`, `query find`,
`doctor --deep`, and project `backup` paths. Current product documentation says those predecessor
project workflows are deleted and current compact change is independently versioned at contract 7.
This is a source-level authority mismatch, not yet a runtime observation. It leaves the project with
historical Graph-4 scale rows but no current distribution, million-owner, million-key, or
long-history receipt.

| Candidate | User/workload value | Contraction | Dependency closure | Proof | One-campaign probability | Decision |
|---|---:|---:|---:|---:|---:|---|
| Replace stale scale authority and admit current graph/data retention | 5 | 5 | 4 | 5 | 4 | **Selected** |
| Bounded owner-body projection or more authored operations | 3 | 3 | 3 | 4 | 4 | Defer: no maintained workflow proves current context insufficient |
| Outbound HTTP or remaining topology authoring | 4 | 3 | 2 | 3 | 3 | Defer: no maintained consumer or complete security contract |
| Another public platform target | 3 | 2 | 4 | 5 | 3 | Defer: no named target consumer; adds hosted and publication risk |

Scale and retention admission wins because it repairs an existing contributor contract instead of
adding a parallel feature, directly tests the custom semantic and database authorities now shipped
to users, and produces the evidence required before any packing or compaction decision. The
strongest feature alternative lacks a maintained consumer; choosing it would grow the public
surface while the current storage generation still has no large-scale admission.

## 4. Target state, decisions, and success conditions

### 4.1 Authority, contract, and non-goals

- `GraphRepository` remains the sole editable program-meaning authority. `DataStore` remains the
  separate deployment-selected application-data authority. Artifacts, compiler caches, indexes,
  scale fixtures, receipts, and measurements remain derived or evidentiary.
- Replace scale contract 1 with one current scale/retention evidence contract. Advance only that
  contributor contract. Do not move the root product version, CLI, compact-change, query,
  repository, data-store, backup, package, artifact, deployment, or maintained application
  identities.
- Remove hard-coded copies of public contract versions and predecessor command schemas from the
  scale owner. Construct requests through the canonical typed owner or executable-discovered
  contract, and use current public `status`, `inspect`, bounded `query`, reviewed change,
  `check`, and `build` behavior. Do not recreate `doctor`, project backup/restore, old query names,
  or another authoring format.
- The retained harness may generate only deterministic isolated fixtures and bounded receipts.
  Million-scale repositories, stores, artifacts, backups, and logs remain ignored temporary
  evidence. Do not check them into Git.
- Add no pack, segment, compactor, garbage collector, destructive cleanup command, reader lease,
  production retention registry, data migration, or format rewrite in this campaign.
- If read-only instrumentation is required, keep it contributor-only and incapable of selecting or
  advancing accepted authority. Do not make storage coordinates, filenames, or measurement fields
  part of semantic or data identity.

### 4.2 Finite semantic matrix

The current-authority semantic suite must deterministically produce and bind these exact classes:

1. **Maintained baseline:** current standard and `lkjournal` authority, clean and exact-current
   check/build, maintained artifact byte equality, and current owner/compiler/object inventory.
2. **Shape admission:** current equivalents of independent/distributed owners, wide local
   ownership, bounded deep dependency, and wide fanout at 10,000 live local owners or relations.
   For depth-limited forms, test the exact admitted executable limit and one deterministic
   over-limit rejection rather than fabricating an invalid 10,000-deep success.
3. **History and locality:** at least 100,000 live local owners, 1,000 accepted exact-base
   revisions over a deterministic hot set, and a local edit whose selected validation/compiler
   work is compared with the same edit in the maintained-size baseline.
4. **Million-owner vertical slice:** exactly 1,000,000 accepted live local owners in one
   deterministic sparse executable topology, followed by reopen, bounded status/inspect/name and
   context queries, one reviewed local change, clean check/build, exact-current rebuild, and
   artifact equality.

Count accepted owners and relations from the current authority, not requested rows. Every generated
graph must have an expected logical digest independent of allocation order. A deliberately
non-executable count fixture cannot stand in for compiler/artifact admission.

### 4.3 Finite operational-data and retention matrix

Use the current first-party engine, not PostgreSQL or a new provider:

1. **Maintained-shaped baseline:** deterministic BBS-like ordered primary/index facts,
   `lkjournal`-like actor/resource/object metadata, and durable-queue lifecycle facts.
2. **Hundred-thousand history:** 100,000 live logical records, 1,000 exact-base commits, and at
   least 1,000,000 conditional mutations over a deterministic hot set, including delete/recreate
   ABA and queue lease/attempt churn.
3. **Million-live vertical slice:** exactly 1,000,000 live records with mixed ordered key parts,
   forward/reverse prefix scans and continuations, reopen, public verify, logical backup,
   absent-root restore, and exact logical equality.
4. **Fresh-versus-retained comparison:** construct a fresh store with the exact final live logical
   facts from the long-history case. Compare physical bytes/files, complete verify, cold reopen,
   full logical scan, backup bytes/time, restore time, and peak RSS against the retained-history
   store.

Use the existing implementation-disjoint ordered-map/data-codec model where possible. Public
`data initialize|verify|backup|restore` must be exercised through an exact copied candidate; direct
engine load generation must be classified separately.

### 4.4 Independent proof, failures, and measurements

- A contributor-owned semantic reachability/inventory oracle must reconstruct sorted live
  owner/relation facts from canonical authority without using production catalogs, query indexes,
  compiler caches, or the scale generator's expected-count implementation. Its digest must equal
  the deterministic generator model and the accepted revision identity.
- Production/reference semantic execution and clean/exact-current artifact bytes must agree.
  Derived cache/catalog absence or corruption may rebuild without changing authority; canonical
  corruption must reject.
- The data BTreeMap/codec oracle, logical backup, restored store, and production scan digest must
  agree exactly. Stale expectations, ABA, interruption, malformed continuation, corrupt canonical
  data, path/symlink input, and resource exhaustion must leave the accepted old or new state exact.
- Run at least one retained interruption point for semantic publication and data commit at the
  100,000 tier. Do not repeat every historical fault matrix merely because it exists.
- Record exact corpus seed and digest, binary and tool identities, host/kernel/filesystem, available
  resources, wall time, sampled CPU, peak RSS, logical work, files/inodes, bytes by authority area,
  opens/reads/writes/fsync publications when first-party instrumentation can establish them, and
  cleanup. Keep unavailable dimensions explicitly unavailable.
- After one warm-up, take three fresh samples for cold reopen, bounded query/scan, verify,
  check/build where applicable, backup, and restore on unchanged exact corpora. Retain samples and
  medians; do not call them service-level objectives.

### 4.5 Decision rule

The terminal evidence must classify each domain as `admitted`, `not_admitted`, or `unavailable` and
select one retention disposition:

- `retain_current_layout` when every correctness boundary passes, every required 100,000 and
  1,000,000 workflow completes inside its current explicit product limits, and neither escalation
  trigger below fires;
- `semantic_pack_comparison_required` when an intrinsic current semantic-layout failure prevents a
  required workflow, or the 1,000,000-to-100,000 normalized per-owner wall/RSS cost worsens by more
  than 2× for at least two independent required semantic operations;
- `data_compaction_design_required` when a correctness/recovery failure is intrinsic to retained
  history, or the 1,000-revision store exceeds the fresh equal-live-state store by more than 8×
  physical authoritative bytes or more than 4× median backup/restore wall time;
- both escalation dispositions when both independent triggers fire.

These ratios are campaign selection gates, not public performance promises. A trigger selects only
the next bounded comparison/design campaign. It does not authorize a physical-layout change,
deletion, migration, or destructive command here. Preserve the current format and public behavior
even when it is classified `not_admitted`.

### 4.6 Success conditions

**SC-1 — Current scale contract.** One typed contributor scale/retention owner replaces the
Graph-4-era receipt and calls. Help, self-test, invalid input, limits, deterministic receipt bytes,
bounded logs, cleanup, and predecessor receipt/grammar rejection pass. No duplicate public
contract number or request grammar remains.

**SC-2 — Semantic admission.** Every required semantic corpus reaches its exact accepted counts and
digest; current public reads and reviewed write behavior pass; the million-owner clean and
exact-current vertical slice agrees; locality and retained-history observations are complete.

**SC-3 — Data admission.** Maintained-shaped, 100,000-history, and 1,000,000-live data workloads
complete; scans, continuations, ABA, queue churn, verify, backup, restore, reopen, and exact logical
digest equality pass or yield a reproducible terminal `not_admitted` result.

**SC-4 — Independent and negative proof.** Semantic reachability and data reference oracles agree;
derived recovery, canonical rejection, stale/exhausted/interrupted behavior, and unchanged accepted
authority are proved at the specified tiers.

**SC-5 — Measured decision.** Exact samples and classifications produce one unambiguous retention
disposition under section 4.5. A new durable decision records rationale, consequences, reversal
conditions, and the smallest follow-on boundary without presenting observations as universal
claims.

**SC-6 — Product non-drift.** Root product and independently owned public contract identities,
capabilities digest, generated product documentation, maintained standard/`lkjournal` artifacts,
public release, and deployment descriptors remain unchanged. Any unavoidable public-product input
change invalidates this campaign boundary and requires a new product campaign.

**SC-7 — Repository closure.** Focused checks, contributor self-test, one final fresh full profile,
documentation/evidence validation, and deletion audit pass; tracked evidence is bounded; raw
artifacts are ignored; commits are coherent; push state and preserved work are exact; the campaign
is terminal.

## 5. Dependency-ordered work

1. **Reconcile and capture the predecessor.** Establish exact active state, run only the minimum
   current `scale` baseline needed to prove the stale boundary, and retain its source/help/exit
   classification. Do not spend the campaign making Graph-4 calls work.
2. **Replace the evidence owner.** Define the new receipt and suite model, route typed requests
   through current owners, use an exact copied candidate, delete predecessor fields/calls/version
   duplication, and add fast deterministic self-tests and negative cases.
3. **Close semantic scale.** Implement deterministic current-authority corpora, independent
   reachability/inventory proof, current public read/write/check/build vertical slices, locality,
   history, and resource capture. Run focused tiers before the million-owner final corpus.
4. **Close operational-data scale.** Reuse current codec/model/lifecycle owners, add maintained,
   history, million-live, fresh-equal-state, interruption, backup/restore, and resource evidence
   without introducing another provider or data format.
5. **Decide, do not compact.** Apply the fixed rule in section 4.5, add a current decision record,
   update performance, status, architecture, and roadmap only with proved facts, and remove stale
   claims that the predecessor scale receipt is current. Do not hand-edit executable-generated
   product docs.
6. **Verify and close.** Run narrow checks while editing; after all implementation inputs settle,
   run the exact matrix and one fresh full profile once. Write structured evidence and a concise
   terminal record in an evidence-only closure commit that changes no measured input. Fetch and
   normally push only if still authorized and safe.

## 6. Verification and evidence contract

Use this ladder:

1. predecessor source/help/exit baseline;
2. scale owner unit/self-tests and small current-authority smoke;
3. 10,000 and maintained semantic/data tiers;
4. 100,000 history/locality, independent oracles, and specified interruption cases;
5. exact 1,000,000 semantic and data vertical slices;
6. fixed decision evaluation and documentation;
7. one final fresh full repository profile after implementation inputs settle;
8. evidence-only closure checks.

Every gate is exactly `fresh passed`, `reused passed`, `skipped`, `unavailable`, `failed`, or
`not run`. The scale contract, all required matrix tiers, independent oracles, decision evaluation,
and final full profile require fresh evidence for exact inputs. A layout-domain `not_admitted`
result is not a failed harness gate; record the proving operation and exact trigger separately.
Unavailable required infrastructure blocks completion.

Structured evidence must bind implementation commit, tree, tool and candidate digests, scale
contract, product/public contract identities, corpus parameters and digests, host/resource
inventory, per-operation samples, medians, correctness outcomes, decision inputs, final
classification, raw paths/digests, cleanup, and every limitation. Raw repositories, stores,
artifacts, backups, logs, and process inventories stay ignored.

Do not claim a general million-owner or million-key public support level, latency distribution,
minimum hardware, filesystem portability, universal Linux behavior, online compaction, bounded
retention, reclaimable-byte safety, hostile-code sandboxing, encryption, replication, consensus,
tenant isolation, or production service-level objective. This campaign proves only its exact
candidate, corpora, host, and contracts.

## 7. Git and external-action policy

Stage explicit paths, inspect every staged diff, pair behavior with proof, and avoid unrelated
formatting, renames, dependency updates, or cleanup. Do not use destructive reset/clean/restore,
rebase, broad staging, force push, history rewrite, or unfamiliar-work deletion. End clean or name
preserved work exactly.

| Action | Authorization |
|---|---|
| Edit contributor tooling, tests, read-only instrumentation, specifications, decisions, current facts, performance, roadmap, and this campaign within the objective | authorized after reconciliation |
| Run builds/tests, exact candidate binaries, isolated million-scale repositories/data roots, fault injection, and delete only campaign-owned temporary roots | authorized |
| Make coherent local implementation and evidence commits | authorized |
| One normal safe fast-forward push of the completed source/evidence sequence | authorized after fetch, final proof, and collision check |
| Change public product semantics, accepted graph/data formats, maintained application meaning, deployment descriptors, or public contract identities | forbidden in this campaign |
| Add or run destructive packing, compaction, GC, repair, pruning, migration, or deletion over accepted data | forbidden |
| Dispatch workflows; create/move/delete tags; create/edit/delete releases or assets; publish packages | forbidden |
| Deploy, switch a running service, or inspect/mutate hosted, ambient, production, or user data | forbidden |
| Mutate issues, pull requests, settings, variables, secrets, credentials, permissions, or repository administration | forbidden |
| Force push, rewrite history, destructively clean, or mutate any published object | forbidden |

If upstream changes make a normal push unsafe, stop after local commits and report the exact
relation. No permission here authorizes destruction of unfamiliar local work.

## 8. Final report and terminal record

Return exact starting and final branch, `HEAD`/tree, worktree/index/untracked state,
remotes/upstream/divergence, preserved work, deviations, commits, and push result. Report:

- predecessor scale runtime classification and deleted predecessor assumptions;
- new scale contract, exact help/limits, suite profiles, request authority, and candidate digest;
- product and independently owned contract identities proving non-drift;
- every semantic/data corpus count, seed/digest, accepted identity, artifact/backup identity, and
  retained/fresh inventory;
- every required gate and evidence classification;
- independent-oracle, failure, interruption, cleanup, and unchanged-authority results;
- exact host/toolchain/filesystem/resource observations and unavailable dimensions;
- section 4.5 calculations and final admission/retention dispositions;
- structured-evidence path/digest and bounded raw-artifact pointers;
- tested, observed, and not-tested boundaries, limitations, nonclaims, external actions, and
  smallest deferred candidates.

Append a concise terminal record here with final status/time, starting/final identities,
implementation and evidence commits, evidence path/digest, required classifications, disposition,
push state, preserved work, deviations, external actions, and deferred limitations. Do not paste
raw logs, corpora, inventories, generated documents, or complete measurements into this file.

## Terminal record

- **Final status/time:** terminated at `2026-08-31 13:51:52` Asia/Tokyo
- **Identity:** remained on `main` at commit
  `649bf88a126c70ef6a88c3fb8c7d228978eae85c`, tree
  `f04e346ac6d170458c96af7ea957ebea609508b6`
- **Reason:** the later user-selected campaign `202608311331` became authoritative and requires a
  narrower semantic-only topology harness whose grammar and scope conflict with this campaign's
  in-flight combined semantic/data retention profiles; this campaign's own termination condition
  therefore applies
- **Commits/evidence/push:** no campaign commit, terminal structured evidence, or push
- **Preserved work:** the complete uncommitted implementation is retained in local stash object
  `87c2265f6faf0e815d80450ec2480d46c5e774fb`; ignored raw outputs remain under
  `.artifacts/campaign/202608310137/`
- **External actions:** none
- **Deferred boundary:** operational-data and long-history scale remain candidates for the
  separately designed campaign named by `202608311331`
