# Deliver the accepted native-authoring milestone as v0.1.40

Timestamp: 2026-09-22T18:13:58+09:00 (Asia/Tokyo).
Campaign path: `docs/campaigns/202609221813.md`.
Repository: `https://github.com/lkjsxc/lkjscript`.
Inspected remote main: `dd160c0e93fa15f895cbb9c3f65538b238a1f742`.
Accepted product source: `1cdaf72888a1f46359a6d38956050747335f8e32`.
Selected producer: `35685667968`, original attempt `1`.
Predecessor: `docs/campaigns/202609220955.md`.
Publication disposition: resume the already selected native-authoring release, promoting the accepted assets unchanged.
AGENTS.md: unchanged. Current guidance already covers native self-sufficiency, supported host boundaries, proportionate verification and safe publication.

## 1. Startup and authority

Execute this delivery continuation, not another planning exercise. Complete the existing native-authoring milestone through actual public acquisition and use. New language functionality is not required merely to make this campaign appear substantial.

First reconcile the actual checkout, branch/HEAD, index/worktree, relevant untracked files, stashes/worktrees, remotes, divergence, applicable instructions, permissions and relevant active jobs. The inspected SHA is an observation, never a reset target. Preserve unrelated work and predecessor evidence. Reuse the appropriate outstanding lineage; do not create a competing implementation or discard unique work. Read the current root guidance and any narrower instructions; indexed investigation found only the root, not necessarily every local instruction.

Archive this initial mandate unchanged at its campaign path. The path was absent at the inspected revision. On a real collision use the established `-collision-N` convention and preserve both originals. Append clearly separated reconciliation and execution findings. Link this continuation from the predecessor ending rather than copying or rewriting its history.

Implementation, diagnosis, repair, owned disposable testing, maintained asset generation when necessary, documentation, commits, ordinary remote-main integration and configured release operations are delegated within actual permissions. Specifically authorized for this selected release are creation/push of the matching annotated tag and updating only the existing non-secret selection variable `LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA`, after the checks in section 4. This is not permission to change credentials, access controls, immutability or protections. Force-pushes, historical retagging, production deployment, purchases and operational-data destruction are excluded.

## 2. Reconciled baseline and evidence boundary

The architect inspected connected GitHub source and service metadata. No product build, workload execution, candidate ZIP authentication or anonymous executable acquisition was performed in this architect session. A disposable clone attempt failed at DNS resolution; connected reads supplied the relevant source. Runtime baselines below are either predecessor reports or work explicitly required from you, not invented local observations.

| Item | Observation and disposition |
| --- | --- |
| Remote main | `dd160c0e93fa15f895cbb9c3f65538b238a1f742` is a reporting commit directly following the accepted product source. Product implementation and mainline integration are complete. Do not tag the reporting descendant as the accepted product. |
| Native-authoring producer | Release run `35685667968/1` at `1cdaf72888a1f46359a6d38956050747335f8e32` completed successfully on 2026-09-22T05:43:45Z. Candidate job `106611827079` passed source-specific acceptance, final construction, candidate/original-evidence admission and handoff uploads. Terminal job `106630279105` passed. Authenticate the actual handoff through the maintained selector before promotion. |
| Remaining jobs | Producer-selection job `106611827786`, publication job `106611828367` and public-acquisition job `106611830597` were skipped in that candidate-only invocation. Publication and public verification are actionable and due, not established by the green producer. |
| Existing public release | Latest metadata identifies immutable v0.1.39, release `393365830`, published 2026-09-21T23:49:32Z. Its product is `b05e9e12133ef625b33462123468b9b896885dd4`, producer `35508727722/1`; its completed delivery is recorded in the maintained owners. Do not rebuild or reconstruct that closed milestone. |
| v0.1.40 occupancy | The inspected tag-prefix lookup returned no matching ref. Current scoped-variable contents and authenticated draft occupancy were not read. Absence of a public tag or release does not prove absence of an owned or foreign draft. Refresh before writing. |
| Earlier failures | The predecessor records repaired alias capture, runner-discovery expectations and corrupted executable-copy failures, followed by successful source acceptance. Preserve those genuine failures; they are not newly established current blockers. Historical external shutdown observations are also not a reason to replay an application experiment here. |
| Current summaries | `docs/status.md`, `docs/release.md`, `docs/roadmap.md` and the native evidence summary still describe hosted candidate acceptance as pending. Correct current statements from direct observations; preserve dated earlier observations. |

The selected producer's following artifacts were listed as not expired. These are service metadata observations, not proof that this session downloaded or admitted their contents.

| Handoff | Artifact ID | Observed expiry, UTC |
| --- | --- | --- |
| `candidate-assets-35685667968-1` | `10679200648` | 2026-10-06T05:43:13Z |
| `candidate-verifier-35685667968-1` | `10679310544` | 2026-10-06T05:43:15Z |
| `candidate-acceptance-35685667968-1` | `10678004282` | 2026-10-06T05:43:17Z |
| `candidate-diagnostics-35685667968-1` | `10678720996` | 2026-10-06T05:43:18Z |
| `release-terminal-35685667968-1` | `10678499678` | 2026-10-06T05:43:42Z |

The earliest listed expiry is 2026-10-06T14:43:13+09:00. Refresh availability and expiry; a configured retention request is not permanence. Use existing authenticated inventory and digest checks, not another handwritten hash ledger.

### Inspected implementation and evidence owners

Paths refer to the inspected main unless a different identity is stated:

- `AGENTS.md`, `docs/status.md`, `docs/roadmap.md`, `docs/release.md` and `docs/campaigns/202609220955.md`: guidance, current obligations, compatibility and lineage.
- `.github/workflows/release.yml`: explicit candidate/promote/resumption selection, serialized publication/latest state, separated credentials, retained handoffs and terminal decisions.
- `tools/lkjscript-dev/src/release/controller/publication.rs`: exact annotated-tag/source authorization, bounded draft occupancy, matching-asset admission, missing-upload resumption and independent exact/latest identities. Its existing-tag create request already omits `target_commitish`; do not reopen the repaired v0.1.39 HTTP 403 incident without new evidence.
- `tools/lkjscript-dev/src/release/transferred/pair/lifecycle.rs`: the existing bounded installed structural/numerical lifecycle. It uses literal compact/structural authoring; its inspected commands do not exercise complete declaration units or `change draft`.
- `docs/evidence/202609220955-native/README.md`, `independent-create.lkjc` and `independent-edit.lkjc`: retained small native-authoring consumer and its development-only observations. They do not certify final release bytes.

For targeted repairs, follow these callers into the existing `release/controller/`, `release/candidate.rs`, `release/transferred/`, `release/verifier.rs` and their tests. `docs/spec/verification.md` and `tools/lkjscript-dev/src/check/` remain the maintained gate owners; inspect their actual selectors before invoking changed-code acceptance.

## 3. Outcome, tradeoff and boundaries

An ordinary Linux x86-64 consumer must be able to acquire the official native-authoring release without a compiler checkout or Python/Rust toolchain, create declarations directly, reconstruct an editable proposal from accepted meaning, review an edit and execute the resulting program. The distributed executable, not an internal helper or private candidate link, is the entry point.

The nearest attractive alternative is moving a real contributor tool's implementation into lkjscript. That remains valuable, but native user authoring, native tool implementation and compiler self-hosting are different achievements. Beginning another migration now would withhold a completed and explicitly selected authoring improvement while its trusted handoffs expire. Transaction composition and measured storage/preparation work are likewise substantive future candidates, not companions to this delivery.

This is a continuation through the remaining boundaries of one result, not a new release-system rewrite. The current blocker is unfinished delivery, not an observed failed controller. Repair the owning mechanism only if actual execution exposes a defect. Do not add process machinery or unrelated features to extend the session.

Do not claim that earlier releases could not express these programs: the predecessor's official flat-input baseline succeeded. The improvement is direct complete native declarations and reliable canonical re-entry without reconstructing owner/type/child scaffolding in an external semantic generator.

No compiler self-hosting, general process runner, new platform matrix, runtime optimization, graph-authority redesign, new execution quota or operational migration is selected. Orbloam is an experimental consumer, not a product objective. Do not modify its repository, pin, gameplay or saves, and do not rerun its full experiment as a release prerequisite.

## 4. Exact-candidate delivery contract

### Preserve accepted content and separate authority

The normal path promotes producer `35685667968/1` unchanged as v0.1.40. Authenticate repository/head repository, event, workflow, exact source/run/attempt, required jobs/steps, terminal contract, artifact identities, inventories and bytes through the maintained controller. Names, matching hashes and a successful subjob are not independent publication authorization.

Preserve the three finalized public assets exactly: `lkjscript-x86_64-unknown-linux-musl.tar.gz`, `SHA256SUMS` and `install.sh`. Do not rebuild the executable, rerender the bootstrap, alter the neutral content manifest or replace the original verifier merely because a consumer job starts from newer main. Product source, controller source, producer, consumer and subsequent reporting revisions have distinct identities.

Prepare meaningful annotated release notes covering complete declaration units, canonical drafting, reviewed identity-preserving edits, scope-safe generated aliases and actual compatibility. Refresh remote main, exact tag/release occupancy, relevant jobs and configured immutability. Authenticate the accepted product's ancestry rather than substituting a descendant.

Create/push an ordinary annotated `v0.1.40` tag at the exact accepted product source only when unoccupied. Reuse a matching existing annotation; never overwrite a foreign or conflicting one. The predecessor reported selection object `7f2d0187c7f3d5f6ce02086fae861020965457e2` for completed v0.1.39; this is a lead, not the current variable value. Read the current scoped variable, reconcile its owner's terminal state, compare again immediately before the authorized update and read it back afterward. This sequence is not atomic compare-and-swap. If another release owns the control, reconcile that work instead of racing it.

Use the current integrated controller and the configured serialized publication group. Candidate or transferred-verifier execution must not receive publication credentials. The publisher only admits and transfers selected content; preserve its separation from candidate execution.

### Compatibility

The predecessor reports native authored codec 18 for the new intents, while existing intents retain their supported 14–17 encodings and retry commitments. Compact input contract 24, CLI contract 33 and registry 18 advertise the additions. Reconcile these against the selected content before writing release claims. Do not describe every encoding as unchanged.

Graph, compiled-program artifact, transport, installation and application-data formats were not changed by the native-authoring increment. No new migration is selected here. Preserve old runtime slots, pinned paths, accepted history and application data. Older managers that reject neutral release containers use the exact new bootstrap recovery route, not rewritten receipts. Installation/selection neither grants application authority nor migrates data or reverses accepted edits. Program bundles remain runtime-dependent, not self-contained executables. State only the actually admitted Linux x86-64 portability boundary.

## 5. Connected execution route

### A. Admit and promote

Refresh producer jobs and trusted handoff availability. Use the maintained selection boundary; do not manually reconstruct every diagnostic original or require an optional `consume` rehearsal before ordinary promotion. Complete the tag/control prerequisites above, then dispatch once unless an appropriate active or completed invocation already owns the work:

```sh
gh workflow run release.yml --repo lkjsxc/lkjscript --ref main \
  -f operation=promote \
  -f producer_run=35685667968 -f producer_attempt=1 \
  -f latest=required
```

Record the resulting consumer run/attempt and controller event SHA. Confirm actual selection/transfer, publication, public-acquisition and terminal outcomes. Do useful independent notes and fixture preparation while work runs; inspect at meaningful boundaries, not in repeated unchanged polling loops.

For a real publication failure, diagnose and use `operation=resume-publication` with the same producer selectors. For a failed public boundary after successful publication, use `operation=resume-public`. These are state-dependent alternatives, not three mandatory dispatches. Integrate an actual controller repair normally and dispatch from that new mainline controller; rerunning an old event does not select newer workflow source.

### B. Exercise the newly distributed authoring path once

The maintained exact/latest installed pair remains required. Supplement it with one small native-authoring lifecycle using the anonymously acquired exact release executable, without modifying the frozen verifier. This distinguishes the newly advertised capability from the existing structural smoke without repeating broad semantic acceptance.

Use an owned directory/container outside the compiler checkout, with the installed executable and literal inputs but no usable Python, Node or Rust/Cargo toolchain. A separate launch/assertion harness may manage files and independently check outputs, but must not synthesize declarations, access compiler internals or write accepted storage.

Discover actual commands, draft selectors and input encoding through `capabilities` and the maintained generated guides. Use the literal units body from `docs/evidence/202609220955-native/independent-create.lkjc`: module `packing`, record `Parcel`, function `recommend`, four graph tests, component/port and `packing-advice` command target. Obtain a fresh base through public creation/status; do not reuse its historical request base. Do not replay historical exact IDs from `independent-edit.lkjc` into a fresh project.

Plan/apply the fresh native request and check/build/run it. Retain the original input outside the consumer environment, remove access to it inside that environment, and generate a fresh canonical draft. Untouched planning must show no semantic change or recreation. Independently inspect declaration/parameter identities and before/after objects; affected dependents with identical objects are not changed owners.

Edit the new draft's function and corresponding graph test together: a parcel that is both fragile and chilled now requires padding as well as insulation. Preserve the other cases and existing declaration/parameter identities. The independent expected table is:

| Fragile | Chilled | Initial result | Edited result |
| --- | --- | --- | --- |
| false | false | `Use a standard carton.` | unchanged |
| true | false | `Use a padded carton.` | unchanged |
| false | true | `Use an insulated carton.` | unchanged |
| true | true | `Use an insulated carton.` | `Use a padded insulated carton.` |

Before accepting the reviewed edit, alter a copy's meaning while presenting the original review token. It must reject without changing accepted HEAD; the original behavior must still execute. Then submit the unchanged reviewed request or freshly replan the intended edit and complete it. Do not confuse this negative with retrying an already accepted idempotent request, which must retain its original result.

Check all four independently specified cases and the graph tests. Retain old/new bundles and execute their distinguishing case through public deployment after the authoring project is unavailable. Keep paths/descriptors within their supported contract. This small witness uses existing language facilities, not a hidden host implementation. Retain literal requests, the fresh draft/edit, commands, observed results and cleanup state at the existing native evidence owner. Label it a bounded final-public-binary observation, not a new independent-agent experiment or broad maintained adoption.

### C. Close maintained records and integrate

Update current summaries in `docs/status.md`, `docs/release.md`, `docs/roadmap.md` and the existing native evidence README as warranted. Append hosted/public closure to the predecessor ending and this continuation, with cross-references rather than duplicate chronology. Preserve original development evidence and failures unchanged; do not stamp them with the final product SHA.

Review and stage intended paths only. Integrate records and any necessary repairs into remote main normally, incorporating intervening work and validating consequential interactions. Verify remote ancestry and the accepted result's presence. A local commit, topic push, open PR or enabled auto-merge is not delivery. Reporting commits do not retag the accepted product or trigger its broad acceptance again.

## 6. Acceptance and proportionate verification

| Claim | Required proof and failure discriminator |
| --- | --- |
| Accepted candidate, not reconstructed content | Maintained authenticated selection of the exact producer and original attempt, complete required outcomes and admitted handoffs. Missing, expired, contradictory or foreign material cannot pass as reused acceptance. |
| Authorized immutable publication | Fresh exact tag/source/control/occupancy admission; matching three public assets and immutable release identity. Conflicting tags, foreign drafts or mismatched assets reject without destructive repair. Existing controller fixture tests own these negative cases; never manufacture conflicts in live releases. |
| Public acquisition and installation | Configured anonymous exact/latest acquisition, strict installation, small lifecycle and terminal success. Initial selected publication requires latest to identify this candidate; a later superseding release must be recorded separately, not passed off as these bytes. |
| Native creation and re-entry on public bytes | The Parcel workflow above, copied/installed public runtime, independent four-case table, actual fresh draft and disjoint owner observations. Original authoring input is unavailable to the consumer during reconstruction. |
| Reviewed change and recoverability | Tampered pre-acceptance request rejects with unchanged accepted HEAD; healthy reviewed edit succeeds, identities remain, detached old/new bundles retain their distinct results and owned resources are joined. |
| Mainline closure and honest boundaries | Remote main contains accepted product plus intended final changes; product/controller/reporting identities remain distinct. Current documentation agrees with actual jobs/public state without relabeling historical evidence. |

Reuse the producer's original source/finalized-target/installation proof only after maintained admission verifies its bindings. Do not run standalone 26-gate full plus the 20-gate release-source tier plus all six target owners again for an unchanged promotion. Do not repeat Batch/library evolution, Orbloam, every old fault fixture or a fresh-agent experiment to prove file transfer.

A documentation-only update receives relevant documentation checks. A controller/tooling repair receives focused tests at its existing owner, then the applicable dependency-complete changed-source gates. Discover selector semantics: `check changed` observes current Git-status paths, not an arbitrary committed range. A clean-tree changed check cannot validate a committed implementation. Required repository gates and protections remain binding.

For a product/installer defect, preserve the failing source and inputs, repair the responsible owner with a durable regression, and renew the affected acceptance through the maintained release-source/finalized-candidate union. Do not weaken acceptance or relabel a new build as the old producer. A source-built controller configuration is not another distributable product build.

## 7. Adaptation, completion and stopping

If the selected release has already completed at startup, verify its exact identities and remaining native-public observation/record obligations, close those only, and stop. Do not create another version or feature to replace completed work.

If trusted artifacts are unavailable, report the actual unavailable boundary and use the maintained new-candidate route only when justified by unavailable trusted material or an invalidating defect. Preserve original producer history. An unoccupied version may select a newly accepted corrected candidate; an existing annotation is not movable. A defective already published release requires additive corrected publication with an unused successor version and an explicit supersession record. Foreign/conflicting ownership is a blocker, not permission to erase state.

Codex may revise local implementation details when evidence contradicts the route, recording the reason and preserving the outcome, authority and compatibility guarantees. No speculative rewrite is authorized. Fix in-process repairable failures rather than stopping at the first failed command. Do not manufacture fixtures, waive unavailable acceptance or redefine completion as a tag or upload.

Full completion requires admitted candidate provenance, matching immutable publication, selected anonymous public verification, the bounded native-public lifecycle, and normal remote-main closure of intended records/repairs. Mainline-complete/publication-blocked or published/public-verification-failed are accurate partial states, not success.

When only external completion or unavailable authority remains, return a blocked/partial handoff with exact source, lineage, run/attempt/job or gate, observed state, retained resources/expiry and next concrete action. Do not promise unattended monitoring, use indefinite polling, bypass protections or ask for routine approval where the mandate already grants authority.

Retain the configured handoffs for realistic resumption and preserve needed local originals outside rotating checker output. Predecessor local archives and `/home/coder/workspace/lkjscript-native-20260922` resources are reported leads whose availability must be checked before relying on them. Commit small reproducible inputs and concise observations, not binaries, secrets or giant logs. Do not add a new evidence service or ceremonial receipt schema.

## 8. Execution return

Return a concise Japanese account and append clearly separated execution findings. State what a consumer can now do; actual changes and deviations; accepted/tested product source, controller and remote-main revisions; fresh, reused, failed, skipped, cancelled, unavailable and unrun evidence; compatibility and recovery impact; exact publication/public-verification status; retained artifacts/resources with observed expiry; exact remaining blockers; and the most consequential new finding for the next architect.

Explicitly distinguish native user authoring from lkjscript-implemented tooling and compiler self-hosting. Name any new failure rather than hiding it in a green aggregate. Do not create another parallel status ledger or claim remote delivery based only on locally generated files.

---

## Execution findings — startup reconciliation, 2026-09-22 UTC

The actual checkout started clean on `main` at `dd160c0e93fa15f895cbb9c3f65538b238a1f742`,
with zero divergence after fetching origin. Its parent is the accepted product
`1cdaf72888a1f46359a6d38956050747335f8e32`; the descendant changes only six reporting/evidence
files. Only root `AGENTS.md` applies and remains unchanged. This mandate path was absent and
was archived without collision. The existing unrelated stash and two map-evidence detached
worktrees remain untouched. No relevant local jobs were running. Rust is pinned to 1.98.0;
available disk was 825 GiB and available memory about 11 GiB before any expensive work.

Direct authenticated GitHub reads confirm producer `35685667968/1`, exact product/head repository,
workflow-dispatch event, release workflow, successful candidate steps and terminal. Selection,
publication and public jobs were skipped in that candidate-only invocation. All five handoffs in
the mandate remain unexpired with the same IDs and actual expiry times, earliest
2026-10-06T05:43:13Z. These initial metadata reads do not replace the maintained selector's
subsequent byte/inventory admission. The predecessor local accepted/failed original archives and
`/home/coder/workspace/lkjscript-native-20260922` exist; no date expiry is configured locally.

Authenticated paginated release occupancy contained 24 releases and no draft or v0.1.40 release;
both remote matching refs and local tags were empty for v0.1.40. Immutability is enabled.
Main has no branch protection (API reports `Branch not protected`), applicable rules and rulesets
are empty. The repository selection variable still equals
`7f2d0187c7f3d5f6ce02086fae861020965457e2`, whose annotation targets the completed v0.1.39 product.
Its release is immutable and resumption `35668854407/1` passed selection, publication, public and
terminal jobs. No active release invocation competes for this control.

Product implementation/mainline and producer acceptance are complete; promotion, public verification,
the bounded public-native witness and record integration are actionable. Earlier genuine failures
remain historical, not current blockers. No additional functionality, broad semantic replay or
Orbloam experiment is selected. The existing `promote` workflow owns authenticated handoff selection;
no optional `consume` rehearsal is required.

### Annotated selection and single promotion dispatch

After refreshing main, occupancy, enabled immutability and the absence of active release jobs,
ordinary annotated `v0.1.40` object `a11c344317d1ec9dff38f00d9d97d963a4d1407e` was pushed at the
exact accepted product commit. Its annotation describes complete declarations, canonical drafting,
reviewed identity continuity, scope-safe aliases, compatibility and recovery. The repository
selection variable was reread immediately before the authorized update, compared with the completed
v0.1.39 object's value, changed only to this annotation object, and read back successfully.
This sequence is not atomic compare-and-swap; no competing owner was observed.

One `operation=promote`, `latest=required` dispatch was created at 2026-09-22T09:41:46Z:
[35711837606/1](https://github.com/lkjsxc/lkjscript/actions/runs/35711837606), controller/event
`dd160c0e93fa15f895cbb9c3f65538b238a1f742`, producer selectors `35685667968` / `1`.
The serialized workflow builds its trusted controller, authenticates original producer handoffs,
and isolates transferred execution from publication credentials. It intentionally skips the
candidate build job. No product source, finalized asset, original verifier or bootstrap is changed.
Raw operator metadata and annotation text are retained under `.artifacts/native-delivery-20260922`.

### Immutable publication

Consumer selection job `106694043955` authenticated the exact original producer and its three
essential handoffs, admitted current annotation/control/occupancy authority, and passed the small
transferred installed lifecycle. Publisher job `106696164661` then completed successfully:
immutable [release 393611808](https://github.com/lkjsxc/lkjscript/releases/tag/v0.1.40) was published
at 2026-09-22T09:49:23Z. Direct exact/latest metadata and the maintained publication state agree
that latest selects this release and product `1cdaf72888a1f46359a6d38956050747335f8e32`.
The publisher reports `immutable_assets_published`, zero product builds and zero heavy application
invocations. Its archive, `SHA256SUMS` and `install.sh` are the original admitted candidate bytes.
No release-system or product repair was required at this boundary.

The maintained authenticated selection and publication originals are retained locally under
`.artifacts/native-delivery-20260922/selection` and `publication`. Their existing inventories
and identities are authoritative; this narrative does not introduce another hash ledger.

### Hosted anonymous acquisition and terminal closure

Public job `106696386907` and terminal job `106696900567` completed successfully in the same
consumer attempt. The retained terminal explicitly reports
`immutable_published_and_public_verified`, `promotion_authority=authorized`,
`public_verification=success`, `latest=selected`, tag v0.1.40 and the accepted product source.
This includes the configured anonymous exact/latest acquisition, release/asset attestations,
strict admission and installed small lifecycles. Candidate job `106694045439` was intentionally
skipped. No `consume`, `resume-publication`, `resume-public` or duplicate dispatch was necessary.

The admitted original candidate terminal binds 20 source gates, all six final-target owners,
two pinned userlands and installed recovery at producer `35685667968/1`, with joined cleanup and
no failure. Those proofs are reused after maintained provenance/content admission; they were not
replayed during promotion. The separately anonymously bootstrapped native witness executable
matches the original terminal's executable identity. This is an identity check, not its behavioral
oracle; the witness below supplies the new public native-authoring observation.

Consumer artifact availability was refreshed after completion. All are unexpired: controller
`10686189729` expires 2026-10-06T09:46:47Z; selection `10686514612` at 09:48:28Z;
consumer diagnostics `10686469603` at 09:48:31Z; publication `10686734603` at 09:49:25Z;
public `10686874698` at 09:50:59Z; terminal `10688065182` at 09:51:13Z, all on 2026-10-06 UTC.
Original producer handoffs still have their earlier expiry, earliest 2026-10-06T05:43:13Z.
Local downloads retain the selected originals and public/terminal evidence outside checker rotation;
they do not extend GitHub service availability. No cancellation or unavailable boundary occurred.

### Bounded final-public-binary native-authoring observation

The [literal requests, command harness and small output copies](../evidence/202609220955-native/public-v0.1.40/README.md)
record the supplementary Parcel lifecycle. Anonymous exact v0.1.40 bootstrap installation used
the official public URL with credentials cleared. Consumer commands ran outside the compiler
checkout in the pinned Debian Linux x86-64 container, without Python, Node or Rust/Cargo and with
only the installed runtime and the relevant owned input/data directory mounted. The executable
matches the original admitted candidate terminal; neither a rebuilt binary nor a private handoff
was the consumer entry point.

The predecessor's literal units body was copied unchanged with only a fresh public-status base.
Creation, build, all four independent expected initial outputs and all four graph tests passed.
The original request and review output were moved outside later consumer mounts before canonical
drafting. The fresh draft planned with `semantic-change=false`, `owner-recreation=false` and
`publication=none`; accepted HEAD remained unchanged. Explicit fresh-draft edits changed the
recommendation function and its matching fragile/chilled graph test together. Of seven affected
named review rows, exactly two before/after objects changed and five dependent objects stayed equal.
All 13 named identities and the parameter contract remained equal after acceptance.

Before that acceptance, a meaning-altered copy submitted with the original review token rejected
with `change_request_commitment_mismatch`. Accepted HEAD and the old fragile/chilled result remained
unchanged. The original reviewed request then accepted; all four edited cases and all four graph
tests passed. With the authoring project moved outside the execution mount, retained old/new bundles
returned `Use an insulated carton.` and `Use a padded insulated carton.` through public deployment.
Both runs reported zero remaining tasks and no cleanup failures; all owned containers were joined
and removed. These bundles require a compatible runtime.

New unfavorable observations were witness setup/usage errors, preserved rather than hidden:
declaration lookup omitted its required `--parent` (`query_parent_required`); an unchanged plan
was incorrectly given `--output` (`cli_usage`); and a resume script's transcript-overwrite guard
stopped before product execution. Correct commands resumed the existing project without repeating
creation or editing accepted storage. No product or controller defect was observed. The intended
tampered-review rejection is a successful negative test, separate from these setup errors and from
accepted idempotent retries.

This is a bounded final-public-binary observation of the designed small consumer. It is neither
a new independent-agent experiment nor broad maintained adoption. The full local originals,
public installation, retired authoring project, requests and old/new bundles remain under
`/home/coder/workspace/lkjscript-native-public-20260922` with no observed date expiry. Earlier
development originals and Orbloam resources are unchanged. No application data migration or
production deployment occurred.

Tracked presentation copies remove trailing whitespace emitted by the host harness; the evidence
README identifies that formatting transformation. Literal requests and external original transcripts
remain unchanged. No acceptance gate or original failure was rewritten to accommodate presentation.

### Compatibility, evidence disposition and next finding

Selected-source inspection confirms native authored codec 18 for the new intents and supported
14–17 encodings/retry commitments for existing intents; compact input 24, CLI 33 and registry 18
advertise the additions. This increment changes no graph, compiled-program artifact, transport,
installation or application-data format. Existing runtime slots, pinned paths and accepted history
remain intact. An older manager rejecting a neutral container can use the exact v0.1.40 bootstrap;
installation/selection grants no application authority and reverses no accepted change.

Fresh proof covers authenticated selection/authority, unchanged immutable publication, anonymous
exact/latest admission and installed lifecycles, and the bounded native witness. Original accepted
source/final-target/installation proof was reused after maintained admission. The consumer candidate
job was skipped intentionally; no proof was cancelled or unavailable. Standalone full, broad source
and target acceptance, old fault suites, Batch/library evolution, Orbloam and a new fresh-agent
experiment were unrun because this delivery changed no product. Witness usage failures above and
predecessor failures remain genuine records.

The consequential finding is that official public bytes now complete the whole native declaration,
canonical re-entry, review and detached execution workflow without a semantic generator or host
language toolchain. The existing maintained release smoke exercises structural/numerical authoring;
the bounded supplement proves the newly advertised complete-unit/draft path. Native user authoring
is now delivered; migration of tool implementations and compiler self-hosting remain distinct,
unselected work. No speculative feature or release-system rewrite was needed.

### Documentation verification and integration preparation

Only the mandate/predecessor ending, current summaries and the bounded witness evidence are changed
in the checkout. Intended paths were staged explicitly and the staged diff reviewed. With those
documentation inputs staged, `check changed --fresh --machine` selected and passed exactly
`diff_check` and `rust_only_tooling`: two fresh gates, zero reuse, receipt
`.artifacts/lkjscript-dev/check/1790071373165743693-2401808-0/receipt.json`.
The focused product-surface policy passed against the installed public version-slot executable:
33 scanned files, 26 executable discovery commands, zero violations and zero exceptions. Neither
command rebuilt or reaccepted the product. These check originals and machine summaries are retained
under `.artifacts/native-delivery-20260922` and the existing checker owner.

Two initial documentation-check invocations failed before their checks ran. The credential-cleared
environment omitted the pinned Rust bin directory, so the first changed check could not locate
`rustc` (zero gates selected); the first product-surface invocation supplied the installation's
selection symlink, while that policy requires a regular executable file. The first checker receipt
is `.artifacts/lkjscript-dev/check/1790071327204641947-2401634-0/receipt.json`; its failed summary is
retained. Supplying the actual pinned 1.98.0 bin path and the resolved immutable version-slot path
fixed these invocation errors. The first surface diagnostic was observed in the tool transcript;
its redirected stdout file is empty, not a fabricated receipt. These are harness setup failures,
not release/product defects and not cancelled or skipped successful proof.

The appended check report is a documentation-only descendant of those staged inputs; final staged
whitespace/no-Python validation covers that reporting addition without replaying semantic acceptance.
Before integration, origin/main remained `dd160c0e93fa15f895cbb9c3f65538b238a1f742` with zero
divergence, no required branch protection and empty applicable rules/rulesets. The normal mainline
push and its observed remote revision are recorded separately below; the tag remains fixed at the
accepted product throughout.

### Observed remote-main closure

Normal fast-forward integration delivered reporting/evidence commit
`5408028438b138a72ff2fb8724e590d60d9eab2c` to remote main. Both Git remote lookup and the
GitHub branch API independently returned that exact revision. It descends directly from controller
`dd160c0e93fa15f895cbb9c3f65538b238a1f742`, which directly follows accepted/tested product
`1cdaf72888a1f46359a6d38956050747335f8e32`. The release annotation still targets the product,
not either reporting descendant. This final closure appendix is a separate reporting-only
descendant and receives the same narrow documentation checks; it changes no tested behavior.

All selected boundaries are complete: accepted provenance, unchanged immutable publication,
required anonymous exact/latest installed verification, the bounded native-public lifecycle and
normal mainline record integration. There is no remaining blocker, active owned process or
container. Original handoffs, local evidence/installations/bundles, predecessor archives and the
unrelated stash/map worktrees remain preserved at the owners and actual expiry recorded above.
No operational state, release protections, credentials, immutable history or frozen source changed.
