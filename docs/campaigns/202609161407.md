# Complete the binary64 cutover and combined public delivery

- Repository: https://github.com/lkjsxc/lkjscript
- Clock/name stamp: **2026-09-16T14:07:54+09:00 — Asia/Tokyo**; read from the environment clock and converted with `ZoneInfo("Asia/Tokyo")`.
- Intended immutable initial archive: **`docs/campaigns/202609161407.md`**.
- Companion: the complete replacement candidate **`AGENTS.md`** supplied alongside this mandate.
- Campaign kind: **corrective integration and delivery continuation**, not another language-feature campaign.
- Selected outcome: make the implemented F64 scalar and accepted requirement/structural-authoring/transaction-completion capabilities available in one fully accepted immutable public release, retaining the launch and original-evidence repairs.

Execute this mandate as the responsible Codex integrator. Complete ordinary authorized repository integration and the selected release when ready. This authorization does not permit production deployment, destruction of operational data, new spending, credential/permission changes, bypassing protections, or rewriting published history. Preserve unrelated work. Do not ask the user to select another campaign.

## 1. Starting point and reconciliation

### Exact observations

The architect inspected the following immutable source contexts during this session. Do not mix their current-state claims:

| Role | Revision and observation |
|---|---|
| Observed default `main` | `0b6a9c08017fa7565408acfda10d3ce3b3c40c04`, “Record accepted transaction completion and independent recovery publisher.” This is not the F64 candidate. |
| Frozen F64 candidate | `256edf0018ce1fd9935e9f9af9cbe428a6b93514`, tree `25882650463f9de178755f4381f5ba71feb96fad`, branch `codex/f64-202609152217`. GitHub comparison reports three commits ahead of the observed main, with no divergence. Implementation, test, specification and workflow reads below use this revision unless explicitly marked otherwise. |
| Subsequent F64 report | `cccd62fca0ec88adb04ceef06d5db3b284db01bd`, branch `codex/f64-report-202609152217`. Comparison reports one additional commit changing only `docs/campaigns/202609152217.md` and `docs/status.md`. Preserve its execution ending; it is not new candidate verification. |
| Current public `latest` | GitHub's live latest-release endpoint returned immutable **v0.1.36**, release **388927116**, published **2026-09-15T07:00:57Z**. The v0.1.38 tag-ref lookup returned 404 in this session. Recheck occupancy and competing work before reserving a version; a past 404 is not a lock. |
| Latest F64 rehearsal | Release run **34988075601/1**, event source `256edf0018ce1fd9935e9f9af9cbe428a6b93514`, `publish=false`, selected tag string `v0.1.38`: **completed, failure**, updated **2026-09-15T15:52:33Z**. The old campaign's pending observation is now superseded. |

The architect used read-only connected GitHub access, including source reads, comparisons, live release/run metadata and the failed job log. Direct local Git network access was unavailable. **No product build, test, artifact extraction, original-reader admission, integration or publication was performed by the architect.** Prior campaign results remain reported results; the hosted log is freshly inspected evidence of an earlier execution, not a fresh local test.

### The demonstrated failure

Job **104445250259**, “Build, verify, and package,” failed in **Run fresh full verification**. The full receipt summary in its log reports **25 passed/fresh, zero reused, 26 selected**, with `workspace_tests` failing with a nonzero exit. It is not a passing full profile.

The Cargo output reports **690 library tests passed, zero failed, eight ignored** before reaching `tests/general_service.rs`, where eight tests passed and this test failed:

```text
checked_in_service_artifact_uses_only_bundle_contract_20
assertion at tests/general_service.rs:177
actual first eight bytes:   LKJART21
expected first eight bytes: LKJART20
```

Source inspection independently confirms the mismatch: `src/platform/compiler/artifact.rs` declares current production Artifact 21, while that maintained-consumer test still explicitly requires Artifact 20. The loader separately recognizes authentic Artifact 18, 19 and 20. The new producer and backward reader policies must not be conflated.

The workspace command stopped at this integration-test executable. Do not infer that later integration executables passed. Exact-musl candidate build/admission, release packaging, transferred installed acceptance, publication and anonymous acceptance were skipped in this attempt. The observed failure does not establish an arithmetic defect, but neither does correcting it establish completion of those missing gates.

Retain the existing failed evidence, without representing its acquisition as successful acceptance:

- Artifact **10405324042**, `full-verification-evidence-34988075601-1`.
- Authenticated metadata: **50,780,840 bytes**, SHA-256 **`04831ceed96ee2f28ba92fa85ad06727777cbb2826140ac6230658e73a383b6e`**.
- Observed not expired; expires **2026-09-22T15:52:26Z**.
- Log-reported receipt: `.artifacts/lkjscript-dev/check/1789486162891744866-6319-0/receipt.json`.
- Log-reported receipt identity: **`verification_dd87717ce3b411f98fa20dd7eac4b743662d0d366349a287e8d2e139250f22f5`**.

The architect did not download or authenticate the ZIP contents through their original reader. Acquire and authenticate available originals once in owned storage. If they have expired, preserve the accessible log/metadata and label the missing materials; that does not turn the failure into a pass or prohibit new, independently complete acceptance.

### Inherited obligations: dispositions, not a fresh queue

| Item | Disposition for this campaign |
|---|---|
| v0.1.35 | The predecessor records it as the last fully closed public acceptance. Preserve its immutable source, assets and evidence; do not repeat its release. |
| v0.1.36 | Actually published. Its original public reader reportedly rejected eight missing authoring inputs/plans despite a green publisher. Do not invent retrospective originals or rewrite its assets. The already implemented retention repair must survive in the successor and succeed with new originals. |
| v0.1.37 | Annotated tag object `51ecaabf3a6dfbaef0f842e95c4fc480dced0b6d` selects `8baafff7f67b55481cda8d67f8945b78af80be4d`. Its earlier rehearsal was accepted, but publisher `34965341260/1` failed full verification on pre-launch `ExecutableFileBusy`; no release was produced in the inspected history. The previous mandate explicitly superseded separate v0.1.37 delivery with the combined successor. Preserve the old tag, source and failures. Reconcile any intervening qualifying publisher before acting. |
| Transaction completion | Accepted source `93b73f87ac480343b62fd05f298f5c144d7adeb5`, reported full/static acceptance, normally integrated through `3fdddef3e98f120c7f58bc3719b3d22595847a20`. Keep the capability in the combined delivery. No additional feature deferral remains. |
| F64 | Implemented with substantial reported focused numerical/public proof; the new hosted full attempt exposes an unfinished maintained-consumer cutover. Finish the cutover and missing proof, not the original implementation plan again. |
| Launch/evidence repairs | Present in the candidate lineage. Preserve once-only execution and original input/plan retention. The current failure is not evidence that the earlier OS launch cause was rediscovered or resolved by changing arithmetic. |

### Safe startup and bounded branches

Read existing applicable guidance before any edit. Inspect actual HEAD/branch, index/worktree, relevant untracked work, stashes, remotes, divergence, narrower instructions, available resources/toolchain and active CI/release work. Inspect predecessor endings at their actual revisions. An observation above is never a reset target.

Use a mutable descendant containing the F64 implementation and its report ending when these remain the relevant tip; at the observed state the report branch is a natural starting point. Reconcile intervening main changes normally. Preserve every frozen source and unrelated worktree/stash. Do not edit the old frozen candidate in place or force-move its branch to make it appear tested.

Archive this initial mandate unchanged. Append clearly separated reconciliation/execution/resumption records. On a real filename collision retain both originals using observed seconds or a documented collision suffix; never invent a later clock time or overwrite history. Reconcile the supplied `AGENTS.md` against legitimate intervening guidance and install the reconciled file in the mutable development checkout before substantive work. Replacing it does not override instructions already governing the session.

Take only these outcome-preserving branches:

1. **Still incomplete:** repair the demonstrated mismatch and real remaining blockers, then complete this campaign.
2. **Intervening correction or qualifying run exists:** admit its exact inputs/results; resume only missing obligations. Do not repeat an already fixed migration or relaunch matching healthy work.
3. **Capability, required proof and selected public delivery already complete:** record exact evidence and satisfy/supersede this mandate. Complete only real archival/reconciliation work; do not select another feature.
4. **A material numeric-specific blocker is established:** use the inherited fallback in section 6. Pending CI alone is not that blocker.

## 2. Decision and finite scope

### Architectural thesis

**A scalar capability is complete only when ordinary authoring, maintained consumers, strict compatibility and the distributed executable agree on its contract. Finish that existing cross-boundary capability before adding another abstraction.**

Before this campaign, the private candidate can reportedly express generic numerical libraries, but it is not integrated or available in a publicly accepted combined release. A consumer test still asserts the old production wire generation, and important acceptance boundaries have not run.

After this campaign, an anonymous user can install the selected release and author, transport, modify and run a graph-owned numerical library through the product, with the already accepted transaction-completion API and retained compatibility. The release is supported by its actual source, target, installed and public evidence, not by the existence of a branch or a release asset.

The immediate cause is a missed consumer assertion during an intentional encoding transition. Repair that owned contract and inspect the adjacent transition consumers. Do not reinterpret all historical version constants as stale. The prior evidence-retention and pre-launch failures have different causes; accumulating root rules or rebuilding the publication system is not a demonstrated common solution.

The strongest materially different alternative is to expand useful transaction composition: a helper owning a transaction still cannot join an already active transaction on the same canonical requirement, as the roadmap explains. That may unlock more ordinary libraries. It does not remove this delivery blocker, and another semantic change would expand an already unaccepted release candidate. Numerical representation/performance work likewise needs a measured workload rather than assumptions about scalar throughput. Neither belongs in this campaign.

Reconsider this choice only if startup proves delivery already complete, an independent publisher now owns the relevant state, or a concrete defect prevents safe F64 acceptance. Do not use elapsed waiting, a new attractive feature or a report filename as reversal evidence.

### Mandatory scope and exclusions

Complete the current-artifact test/consumer cutover, affected compatibility and public regressions, missing F64 acceptance, normal integration, and one combined immutable/public release. Carry fixes through affected maintained generated assets and documentation only where their inputs actually change. Update concise status to replace stale pending claims, linking history rather than copying it.

There is **no new public operation, intrinsic, numeric type, graph authority, storage architecture, daemon, package system or format generation selected here**. Those implementation-design fields are inapplicable unless a demonstrated defect requires a bounded corrective change. A test-only correction does not require an Artifact 22 or Graph 18. Do not regress the product to Artifact 20 to appease the failing assertion.

Exclude packed numeric arrays, tensors, BLAS/GPU/JIT work, new transcendental functions, new applications, same-store transaction joining, compaction, authoring-authority replacement and a new proof framework. Preserve useful experimental work separately if encountered. The finish line is the selected usable release, not a gate count or a stream of repair campaigns.

The contingent direction after delivery is ordinary numerical/data-processing libraries with explicit failure and persistence policy. A discriminating workload measurement should decide whether their next limiting dependency is representation/execution cost, authoring, or transaction composition. This is a revisable hypothesis, not permission to implement a successor while waiting.

## 3. Contract and ownership

### Current production versus historical admission

Correct `tests/general_service.rs` to require the deliberately selected **current produced** `LKJART21` artifact. Prefer a version-neutral test name while retaining an explicit, independently reviewed expected wire generation. Preserve its size/strictness purpose and the neighboring resident-command rejection tests.

Do not replace that independent expectation with a comparison against the very producer constant under test, remove the assertion, skip the test, widen it to any supported version, or blanket-replace old generation markers. The test should fail if a newly generated maintained artifact silently regresses to a predecessor or drifts to an unreviewed successor. The loader may still correctly accept genuine older artifacts.

Perform a bounded transition audit of the changed compiler/kernel/codec owners, maintained standard and `lkjournal` artifacts, public/structural CLI tests, discovery-generated guides and the relevant release readers. Classify matches as current producer expectations, current discovery inventories, authentic compatibility fixtures or deliberate rejection cases before changing them. Stop expanding the audit when the affected boundary is accounted for; do not reread the repository archive.

Retain genuine historical fixtures and their original identities. In particular, the previous execution ending explains that a file named `graph16-standard` actually has a Graph 15 root and that a separate transaction fixture provides genuine Graph 16 evidence. Filename spelling is not format evidence. Do not fabricate an old input by editing a current marker, regenerate genuine historical bytes with the current producer, or silently relabel their provenance.

The candidate reports Graph 17/compiler 13/bytecode 9/Artifact 21. Cross-check those declarations at their actual owners during reconciliation. Existing historical admission, pre-effect rejection of unsupported/forged inputs and viable predecessor-runtime recovery remain required. No installation operation migrates accepted meaning or operational data. Rebuilding a disposable derived catalog is not the same as changing accepted HEAD, authoritative packs, artifacts or application data; assert the correct no-mutation boundary.

### Preserve the implemented numerical semantics

Use the current normative `docs/spec/language.md` and owning codecs; this mandate does not redesign them. Material release guarantees include:

- F64 is an ordinary immutable typed scalar, composing through nominal/structural values, lists, explicit generics, pure binding, permitted retained state, package transport and typed persistence. No implicit I64 coercion, F64 map/data keys, extra capture permission or execution authority is introduced.
- Preserve subnormals, signed zeros and infinities; normalize NaN to `0x7ff8000000000000`. Preserve authored order, nearest-even operation rounding and correctly rounded square root. No reassociation, implicit FMA or flush-to-zero substitution.
- Program equality remains numerical at F64 leaves, recursively even through shared aggregates: NaN is unequal to itself; the zeros compare equal. Canonical identity and evaluator/test observation instead compare normalized representation bits. Do not make one policy stand in for the other.
- Preserve explicit conversion boundaries, including the strict I64 upper bound, complete-original-token decimal parsing, flat/structural literal agreement and shortest round-trip text formatting. Do not route precision-sensitive literals through an intermediate decimal conversion that loses information.
- Application JSON accepts the implemented finite-number path; control/operational JSON keeps its own existing contract. Nonfinite typed values may persist internally but do not become valid JSON numbers. Rejection/output failure does not retroactively undo an earlier committed effect. Intrinsically invalid types/grants still reject before activation.
- Statistical accumulation, merging, calibration, finiteness and failure policy remain ordinary lkjscript library code. Do not fix a witness by calculating its application result in the host driver or adding a statistics intrinsic.

Ordinary trusted execution still does not require an invented instruction budget. Preserve optional metering and distinct live/input/cumulative limits. Static linkage and safe Rust are not hostile-code isolation. No benchmark, adoption or scientific-package performance claim is selected.

### Public workflow and discriminating workload

Reuse the maintained literal programs:

```text
tools/lkjscript-dev/src/offline_packages/f64.producer.lkjc
tools/lkjscript-dev/src/offline_packages/f64.consumer.lkjc
tools/lkjscript-dev/src/offline_packages/f64.data.lkjc
tools/lkjscript-dev/src/offline_packages/f64.edit.lkjc
```

The ordinary workflow is discovery and fresh project creation, literal reviewed authoring, checking/building, exact offline dependency export/staging, producer removal, consumer execution, a reviewed calibration edit, and typed checkpoint/restart execution through the copied or installed runtime. Discover exact command syntax from the candidate and existing driver; do not invent new CLI flags.

Its important composition case is a transported generic fold/library consuming a **different, consumer-owned nominal sample type through a bound projection**, then changing calibration and using a restarted typed checkpoint. A hard-coded scalar demo or a producer checkout secretly supplying library behavior cannot satisfy this case.

`tools/lkjscript-dev/src/offline_packages/f64.rs` already retains literal requests and plans. It may fill discovered identity/revision preludes, stimulate the product, transport opaque files and perform independent observations. Host-side data-store inspection is an oracle, not the application implementation. State those boundaries honestly. These are designed feasibility workloads, not external adoption.

### Existing owners to use

| Concern | Owners |
|---|---|
| Demonstrated cutover failure and resident behavior | `tests/general_service.rs`; `applications/lkjournal/generated/lkjournal.lkja`; maintained deployment descriptors |
| Producer and strict compatibility | `src/platform/compiler/artifact.rs`; `src/platform/compiler/unit.rs`; `src/platform/kernel/contract.rs`; `src/platform/kernel/codec.rs`; existing compiler/kernel/package-transport F64 tests |
| Numeric semantics and encodings | `src/platform/binary64.rs`; `src/platform/json.rs`; `src/platform/execution/normalized/`; `docs/spec/language.md`, `semantic-cli.md`, `semantic-storage.md`, `data-capabilities.md`, `packages-components.md` |
| Consumer assets/discovery | `packages/standard/`; `applications/lkjournal/`; `src/platform/builtin_standard.rs`; `src/platform/contract/`; `docs/generated/` |
| Launch regression boundary | `tests/support/mod.rs`, `tests/public_cli.rs`, `tests/structural_cli.rs` |
| Numerical public proof | `tools/lkjscript-dev/src/offline_packages.rs`; `offline_packages/f64.rs` and the four literal inputs; existing normalized F64 codec/effect tests |
| Target and installed evidence | `tools/lkjscript-dev/src/release/admission/nominal_tests.rs`; `release/transferred/pair.rs`, `pair/lifecycle.rs`, `pair/tests.rs`; existing transferred readers/fault tests |
| Source and publication orchestration | `tools/lkjscript-dev/src/check/`; `docs/spec/verification.md`; `docs/release.md`; `.github/workflows/release.yml` |

Preserve the shared launch helper's bounded retry only for failure to create a child before execution starts. Once a child exists, collect/join it once. Never rerun an effectful application because its output, reader or cleanup failed. Do not broaden spawn retry into a generic “retry until CI passes” wrapper.

## 4. Execution order

### A. Preserve evidence and establish the smallest real correction

Complete startup reconciliation and root/archive installation. Preserve the prior reporting commit normally rather than dropping its ending. Acquire the failed artifact once if available, authenticate metadata/digest, and retain the specific failing output. Use compatible original readers only with their real source/environment/path bindings; do not rewrite receipts to make archival contexts match.

Reproduce the maintained-consumer failure in the mutable candidate before correcting it when practical. This Cargo target exists in the inspected manifest/source:

```sh
cargo test --locked -p lkjscript --test general_service
```

Record a prior hosted failure rather than inventing a fresh red run if startup finds the correction already present. Resolve the expectation from the normative/current producer policy and genuine checked-in bytes, then make the bounded correction and transition audit. A stale test needs a test fix; an actually wrong artifact needs regeneration through its public/maintained writer. A genuine loader/runtime defect needs a source correction and affected proof, not a weaker oracle.

### B. Finish affected public/compatibility feedback before expensive freezing

The earlier workspace command stopped before later public integration executables. Run the affected executable set once after correction, using the actual pinned toolchain and preserving Cargo output ownership:

```sh
cargo test --locked -p lkjscript --test general_service --test public_cli --test structural_cli
```

List/discover narrower F64 and compatibility tests in their maintained owners and run those affected by the correction. Preserve the reported scalar/JSON/effect/transport tests; do not rerun every old semantic campaign just to restate history. If this inspection finds another current-generation assertion or inventory omission, finish that same cutover before freezing. If a new semantic issue appears, isolate its counterexample and repair its owner without changing the promised semantics.

Keep generated artifacts and discovery consistent through maintained generators. Do not manually patch accepted graph packs or generated grammar. Keep release notes and current status accurate about what is still unaccepted. A stale current-format comment in a touched owner may be corrected; historical descriptions do not need a sweeping rewrite.

### C. Freeze one corrective source and use the maintained acceptance path

Commit all intended implementation/test/generated/specification/workflow/verifier inputs before expensive acceptance. Preserve the old failed `256edf…` source unchanged and select a **new corrective descendant**. Record its full SHA, tree, candidate and verifier identities; do not relabel old receipts. A reporting descendant may later describe it without retargeting it.

Prefer one matching hosted rehearsal to provide the required fresh source and exact-static-target proof, as `docs/release.md` already permits. Do not first run every overlapping full local profile for reassurance. If local full proof is actually necessary, its maintained entry point is:

```sh
cargo run --release --locked -p lkjscript-dev -- check full --fresh --machine
```

The host verifier and target policy entry points are:

```sh
cargo build --release --locked -p lkjscript-dev
target/release/lkjscript-dev release target
```

Read actual `release build`, `admit`, `prepare`, `verify` and transferred-pair argument contracts from `docs/release.md` and their dispatchers. Copy command forms with real owned paths/identities; do not treat path placeholders in documentation as literal inputs. The selected target policy, not a duplicated list here, owns pinned native prerequisites and userlands.

Respect `release_command_lifecycle` dependencies and immutable executable copies: concurrent Cargo feature unification can replace release outputs. Existing 25 passing gates from the failed source are useful diagnostics, not a whole-source certificate for a new commit. `check changed` sees current Git status, not the implementation range; clean-tree success cannot replace acceptance.

### D. Admit original target/installed evidence, integrate, publish

On successful rehearsal completion, authenticate and retain its actual handoffs. Use matching original package, target-admission and installed-pair readers, restoring only owned archived contexts where needed. Missing literal requests/plans or numerical child evidence must fail admission even when enclosing hashes are recomputed.

The previous ending leaves the existing **130-case source-bound target omission/fault matrix** and installed-pair fault obligations unrun against genuine new target originals. Run those existing owners with the final candidate's genuine evidence; do not create another matrix. Discover the exact ignored target test in `release/admission/nominal_tests.rs`. Follow `docs/release.md` for `live_pair_receipt_fault_matrix`, transferred `live_receipt_fault_matrix`, and the applicable interruption/recovery proof. Where an unchanged source-bound obligation already has genuinely valid matching evidence, record and reuse it; do not count fixtures or a reader unit test as distributed execution.

Complete normal integration and the release protocol below when the source is accepted. All target, package and installed boundaries remain mandatory; a clean local integration test does not replace them.

## 5. Acceptance and honest completion

Use existing owners and retain only evidence needed to support these claims. This table is a claim map, not a new framework or an instruction to run every row as a separate duplicate pipeline.

| Material claim | Discriminating observation / independent expectation | Evidence boundary |
|---|---|---|
| Maintained producer cutover is complete | Newly generated/current standard and `lkjournal` artifacts use the reviewed current format; `general_service` and affected public/structural executables pass. Current-output assertion still fails on old/unreviewed production output. | Fresh affected tests; generated comparison gates in final full proof. |
| Historical admission remains honest | Genuine supported predecessor transports/artifacts retain their identities; malformed/unsupported or forged inputs reject before readiness/live effects. Correct authoritative no-mutation boundary; no marker-edited “historical” control. | Existing authentic fixtures, loader/public rejection tests and applicable original readers. |
| F64 is a composable scalar, not a host demo | Fresh outside-checkout producer/consumer; generic library with consumer-owned nominal projection, producer removal, exact transport, reviewed calibration edit, and restarted typed checkpoint. | Literal inputs/plans, outputs, artifact/dependency/runtime bindings; current offline-package owner on actual copied/installed bytes. |
| Results and numeric boundary behavior are correct | Existing independent bit/rational oracles, full-token rounding cases, NaN/signed-zero recursive equality versus identity, conversion bounds, nonfinite policy and I64 regressions. Preserve separate VM/reference dispatch; a shared primitive is not its own independent oracle. | Current scalar/compiler/codec/effect tests and original numerical evidence. |
| Stateful failure is not silently replayed or rolled back | Checkpoint survives fresh-process restart; independently committed prior state survives a later nonfinite JSON-result failure; cleanup is joined and later work stops. | Controlled public/data observations. No differential replay of live effects. |
| Final source and exact static product are accepted | Required fresh full profile, complete workspace tests including formerly unreachable executables; exact musl build/static inspection, pinned userlands and all maintained target owners. | Final source/verifier/candidate-bound receipts, original strict admission. |
| Release evidence survives transfer | Strict package and installed pair; complete original structural and numerical requests/plans; existing target/pair fault tests reject missing or tampered substantive evidence. | Authenticated handoffs and matching original readers; no fabricated retrospective files. |
| Users actually receive the accepted capability | Immutable release; anonymous independent exact/latest acquisition, integrity/attestation admission, installed small lifecycles and prescribed aggregate; matching original public-pair reader succeeds after archival retention. | Actual published identities/bytes, run/attempt and authenticated public originals. |

Use the predecessor's fixed numerical expectations as independent checks, not as observations made by this architect. The existing workload specifies:

- Small values `[0.5,1.5,2.5,3.5]`; calibration edit changes the corresponding nominal-consumer mean **2.25 to 3.25**, retaining variance **1.25**.
- Large-offset values `[1099511627776.25,1099511627776.5,1099511627776.75,1099511627777.0]`; mean **1099511627776.625**, population variance **0.078125**, including its split/restarted computation.
- The existing 4,096-sample stream: count **4096**, mean **1000511.875**, `m2` **357913920**, population variance **87381.328125**.

Verify literal generation/formulas against their existing oracle before interpreting results. Do not use two different floating accumulation orders as a supposed exact equivalence law. Retain failures, empty/nonfinite/overflow policy cases and unfavorable observations. No throughput or token-cost improvement is claimed.

For each retained result distinguish fresh execution, valid reuse, original rereading, failed, cancelled, skipped, unavailable and unrun work. Source, verifier, environment, workload and trust bindings must fit the particular claim. Preserve failed history. Required missing proof blocks its associated completion claim, not useful unrelated engineering.

The main route is complete only when the cutover is corrected, required source/target/installed proof passes, intended work is integrated, and the combined immutable release plus anonymous/original-reader public acceptance closes. Engineering, verification, integration, publication and public acceptance are separate statuses. A pending external job can leave this campaign resumable; it cannot make it complete.

## 6. Delivery, fallback and return

### Selected publication, with no renewed feature deferral

Use **v0.1.38** for the corrective combined candidate if its version remains unoccupied and consistent with current work. The inspected failed rehearsal created no tag or release; failure alone does not consume a version. Confirm the manifest and live identities. If another candidate legitimately occupies that version, reconcile ownership and choose the smallest coherent unused successor only when a separate corrected source is necessary. Never move an existing tag, overwrite immutable assets or steal a concurrent reservation.

The current workflow triggers publication on **`v*` tag push** and supports explicit dispatch. Its concurrency group is tag/ref keyed with `cancel-in-progress: false`; this is not a global lock for the shared publication selector. Ordinary branch push is not this release trigger. Refresh other applicable checks/effects before integration.

Follow the maintained sequence, without inventing new authorization gates:

1. Inspect matching runs and exact inputs before dispatch. Freeze/push the corrective source normally, and run one matching `publish=false` rehearsal if no qualifying attempt exists. Confirm its event SHA equals the selected source. Dry run must not mutate tags, drafts, releases or the publication selector.
2. Complete fresh full, target, package and transferred installed proof and authenticate/admit originals. Publication's own fresh proof remains mandatory under the current protocol; rehearsal receipts do not substitute across runs.
3. Integrate accepted intended work normally with actual permissions/protections and required checks. Record tested source, integration revision/tree and later reporting separately. Publication source must be reachable from remote main; reporting descendants do not invalidate a frozen ancestor. Reconcile genuine divergence without force pushes.
4. Prepare accurate useful release notes, which the existing annotated-tag publisher uses. Include numerical composition, transaction completion and compatibility boundaries, not unsupported performance/adoption claims. Do not claim public acceptance before it occurs.
5. Recheck unused tag/release, immutability enabled, required authority, and all potentially competing publishers. Create the exact annotated tag. Follow `docs/release.md`'s prior-value/owner checks for **`LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA`**, the existing non-secret release coordination variable. Its read/check/write is not atomic. Updating that already configured selector is part of the authorized release protocol, not permission to change credentials, protections or immutability. If actual authority is absent, retain the exact blocked action rather than bypassing it.
6. Push the annotated tag once; that push owns publication. Do not manually create a parallel release. Preserve immutable v0.1.36/v0.1.37 identities and failed histories. Complete the publisher, anonymous pair and matching original-reader admission of actual downloaded bytes. If a published source needs correction, recover additively under a new identity.

### Preserve the inherited material-blockage fallback

The previous mandate explicitly requires delivery of already accepted requirement/structural/transaction capabilities plus retention/launch repairs if F64 is materially blocked or abandoned. Keep that obligation; there is no permission to defer those accepted capabilities to another abstraction.

Use it only on concrete evidence that the numeric contract cannot presently be safely admitted and a focused correction cannot finish this campaign, not because CI is running, a routine test failed, or artifact acquisition is slow. Preserve the numeric branch and exact counterexample/blocker. Freeze a separate clean source containing accepted nonnumeric capabilities and necessary delivery repairs, excluding incomplete F64 and incompatible generated assets. Establish its dependency-complete acceptance and publish it under an unused identity. Do not claim that release contains F64. Record this as the fallback delivery completed and the numeric outcome incomplete, with a precise resumption condition.

If an external permission/infrastructure blocker prevents either release, preserve accepted work and report that blocker. Do not manufacture a featureless release chain that has the same unsatisfied dependency. The demonstrated current-format test failure alone justifies the primary corrective route, not this fallback.

### No idle agent loops

The known failed `34988075601/1` must not be rerun unchanged. A corrected descendant requires its own selected attempt. Check healthy qualifying work before dispatching or rerunning anything expensive. Never blindly cancel foreign or healthy jobs.

While remote work runs, complete only independent authorized review, notes, evidence acquisition or concise reporting that does not change frozen inputs. Inspect status at meaningful work boundaries or to answer a concrete diagnostic question. No indefinite watches, repeated sleep/status scripts, hidden polling, unchanged-log downloads or waiting disguised as new feature work.

If only external completion remains, at most one short bounded wait is justified by observed progress and tool support. Otherwise end with exact source, run/attempt, observation time/state, missing gate, retained artifact identities and the next concrete resumption action. Do not promise unattended monitoring. On the next invocation, reconcile that work before replacing it.

### Root guidance and retained records

The supplied root candidate is deliberately **unchanged from the reviewed F64-branch `AGENTS.md`**. Its SHA-256 is **`993f8c3357ea7dd0af647db0814cb1034c74deab2e32619900d4d84156c5db8a`**; Git blob identity **`7426b16b3529591e2641a2fc4dff1fdaf54826d9`**. It already supplies a concise practical entry point, revisable graph authority, safe startup, tooling/validation and truthful release/waiting guidance. Fresh review does not require another rule. Keep numeric and format semantics at their owners and this repair's identities here. Reconcile legitimate intervening changes instead of enforcing this snapshot blindly.

Preserve the original `202609152217.md` initial mandate and all existing execution records; append a concise cross-link/current disposition rather than editing away its earlier pending observation. Keep current status concise and update stale release state with exact evidence. Do not copy a growing chronology into the roadmap or create a parallel ledger. Keep binaries, logs and authentic evidence under appropriate owned artifact storage, not tracked narrative.

The final Codex report must identify actual starting/tested/integrated/frozen/public revisions and actions; the useful capability delivered or still pending; the precise correction and whether any deeper defect appeared; verification classifications and independent results; compatibility/data impact; separate engineering, verification, integration, publication and public-acceptance states; preserved unrelated work and cleanup; any exact blocker and resumption action; and the most consequential finding for the next architect. Do not select another campaign merely to fill idle time.

## Revision-bound evidence and live observation references

These URLs are durable source pointers, not instructions to assume that the named branch/ref is still current. Live endpoints must be refreshed before consequential writes. The linked campaign results are reported verification unless explicitly identified above as freshly inspected hosted logs/metadata.

- [Observed main commit](https://github.com/lkjsxc/lkjscript/commit/0b6a9c08017fa7565408acfda10d3ce3b3c40c04)
- [Main to frozen F64 comparison](https://github.com/lkjsxc/lkjscript/compare/0b6a9c08017fa7565408acfda10d3ce3b3c40c04...256edf0018ce1fd9935e9f9af9cbe428a6b93514)
- [F64 to reporting-only comparison](https://github.com/lkjsxc/lkjscript/compare/256edf0018ce1fd9935e9f9af9cbe428a6b93514...cccd62fca0ec88adb04ceef06d5db3b284db01bd)
- [Predecessor mandate, complete ending and fallback](https://github.com/lkjsxc/lkjscript/blob/cccd62fca0ec88adb04ceef06d5db3b284db01bd/docs/campaigns/202609152217.md)
- [Reported status and inherited delivery dispositions](https://github.com/lkjsxc/lkjscript/blob/cccd62fca0ec88adb04ceef06d5db3b284db01bd/docs/status.md)
- [Focused roadmap and transaction-composition alternative](https://github.com/lkjsxc/lkjscript/blob/256edf0018ce1fd9935e9f9af9cbe428a6b93514/docs/roadmap.md)
- [Reviewed root guidance](https://github.com/lkjsxc/lkjscript/blob/256edf0018ce1fd9935e9f9af9cbe428a6b93514/AGENTS.md)
- [Demonstrated stale maintained-consumer assertion](https://github.com/lkjsxc/lkjscript/blob/256edf0018ce1fd9935e9f9af9cbe428a6b93514/tests/general_service.rs#L173-L194)
- [Current Artifact 21 and separate predecessor reader policy](https://github.com/lkjsxc/lkjscript/blob/256edf0018ce1fd9935e9f9af9cbe428a6b93514/src/platform/compiler/artifact.rs#L50-L126)
- [Normative F64 computation](https://github.com/lkjsxc/lkjscript/blob/256edf0018ce1fd9935e9f9af9cbe428a6b93514/docs/spec/language.md#L165-L215)
- [Literal numerical workflow and retained requests/plans](https://github.com/lkjsxc/lkjscript/blob/256edf0018ce1fd9935e9f9af9cbe428a6b93514/tools/lkjscript-dev/src/offline_packages/f64.rs)
- [Current package/version/tooling inputs](https://github.com/lkjsxc/lkjscript/blob/256edf0018ce1fd9935e9f9af9cbe428a6b93514/Cargo.toml)
- [Normative verification owner](https://github.com/lkjsxc/lkjscript/blob/256edf0018ce1fd9935e9f9af9cbe428a6b93514/docs/spec/verification.md)
- [Maintained release protocol and original-reader commands](https://github.com/lkjsxc/lkjscript/blob/256edf0018ce1fd9935e9f9af9cbe428a6b93514/docs/release.md)
- [Actual release triggers, concurrency and workflow inputs](https://github.com/lkjsxc/lkjscript/blob/256edf0018ce1fd9935e9f9af9cbe428a6b93514/.github/workflows/release.yml)
- [Failed exact-source rehearsal](https://github.com/lkjsxc/lkjscript/actions/runs/34988075601)
- [Inspected failed job and log](https://github.com/lkjsxc/lkjscript/actions/runs/34988075601/job/104445250259)
- [Authenticated failed-artifact metadata endpoint](https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/34988075601/artifacts)
- [Live latest-release endpoint](https://api.github.com/repos/lkjsxc/lkjscript/releases/latest)
- [Observed immutable v0.1.36 release](https://github.com/lkjsxc/lkjscript/releases/tag/v0.1.36)

---
End of initial mandate. Append subsequent records below; do not rewrite the initial text.

## Startup reconciliation — 2026-09-16 UTC

Actual startup is `cccd62fca0ec88adb04ceef06d5db3b284db01bd` on the F64 reporting branch.
Remote main remains `0b6a9c08017fa7565408acfda10d3ce3b3c40c04`; its history is an ancestor,
not a reset target. Mutable work continues on `codex/f64-cutover-202609161407`, preserving
both the frozen F64 source and its complete reporting ending. The existing untracked initial
mandate matches the supplied archive byte-for-byte (SHA-256
`77604d7d9b0e6e9cd4c3d037d34c1799a2406aec1f7c99b3ed6047dceab7c090`). No collision exists.
The installed root guidance already matches the supplied candidate and its stated SHA-256/blob;
no guidance change is needed. No narrower guidance was found in the affected owners.

Fresh authenticated GitHub reads confirm `34988075601/1` failed at exact source `256edf00`;
there is no newer release run or competing active publisher. Latest is still immutable v0.1.36,
release `388927116`. v0.1.38 has no remote tag. The prior failed evidence remains available as
artifact `10405324042` with the mandate's exact length/digest. Acquisition/admission follows below.
v0.1.35's closed acceptance, v0.1.36's missing public originals, and v0.1.37's superseded separate
delivery retain their prior dispositions. Requirement/structural/transaction implementation and
launch/evidence repairs are retained; the F64 cutover and combined proof/delivery are actionable.
No material numerical blocker or fallback trigger has been established.

Direct Git/CLI network access works in this session. Repository permission is admin/push,
main is unprotected, no rulesets are active, and release immutability is enabled; these observations
must be refreshed before consequential writes. Pinned Rust/Cargo 1.98.0 is installed under
`/home/coder/.cargo/bin`. Startup has 663 GiB disk available and about 8 GiB available memory;
no Cargo or release process is active. The unrelated August 31 stash and frozen recovery worktree
remain preserved. Independent read-only transition/release reviews are delegated; the primary
integrator owns all edits, execution, integration and publication.

## Correction and affected feedback — 2026-09-16 UTC

The failed artifact was acquired once into `.artifacts/f64-cutover-202609161407/startup/`.
Its 50,780,840 ZIP bytes match authenticated SHA-256
`04831ceed96ee2f28ba92fa85ad06727777cbb2826140ac6230658e73a383b6e`.
Original inventory, receipt, stdout/stderr and API job log are retained without receipt edits.
The original receipt reports exact `256edf00`, stable inputs, 25 fresh passes/zero reuse out of
26 selected, and failed `workspace_tests` exit 101. The job log binds receipt identity
`verification_dd87717ce3b411f98fa20dd7eac4b743662d0d366349a287e8d2e139250f22f5`.
This is authenticated failed-evidence acquisition and original inspection, not successful
acceptance. The maintained full reader is private to package preparation and requires a passing
receipt; there is no standalone `check verify` command or successful package/target handoff for
this attempt. The initial `gh run view --log-failed` returned an empty file; the specific job API
returned the retained actual log. No failed workload was replayed to reconstruct originals.

Fresh local reproduction of `cargo test --locked -p lkjscript --test general_service` produced
eight passes and the same single Artifact 21-versus-20 failure. The correction renames that test
to `checked_in_service_artifact_uses_current_bundle_contract`, explicitly expects `LKJART21`, and
retains the old-generation exclusions, bounded size and adjacent resident-preflight rejection
tests. The expected generation remains independent of the producer constant.

The bounded audit confirms Graph 17/compiler-unit 13/bytecode 9/Artifact 21 at their owners.
Both maintained artifacts already have the correct producer bytes: standard SHA-256
`24be2f4fe7d5479e5a960089b4dee8d1a6fe321000087ab4e1f6155b29a3cfff` and lkjournal
`5b5452b01d67e5d3b09308f5ba9ecd53945b7f2926f20cda92eca9d036e7be8d`.
Current public/structural inventories, generated discovery and installed numerical readers agree.
Authentic older artifact/transport fixtures retain their identities; the `graph16-standard`
fixture's accepted root is Graph 15, while the transaction fixture supplies genuine Graph 16.
Historical digest domains, supported readers and deliberate unsupported-marker tests are retained.
No further current-output mismatch or deeper numerical defect was established; no asset, accepted
graph, runtime implementation, format generation or application-data change was needed.

Fresh affected executable results: general_service 9 passed; public_cli 46 passed/1 existing
scale test ignored; structural_cli 11 passed/1 authenticated-predecessor test ignored. The latter
two executables now complete, unlike the failed hosted workspace attempt. Existing spawn tests
pass, including no replay after collection/exit failure. Seven focused library tests also pass:
the five `f64_successor_` compatibility cases, unreachable rehashed predecessor instructions, and
F64 hidden in unused predecessor type closure under old/new outer artifacts. The synthetic-owner
case is labeled synthetic; authentic fixture execution supplies historical proof. Logs and test
inventory are under `feedback/`. Existing prior scalar/oracle/workload results retain their
reported-development classification; these focused checks do not constitute final full proof.

The next boundary is one new committed corrective source and matching hosted `publish=false`
rehearsal. No local overlapping full profile, tag, release or publication-selector write was made.
The selector still belongs to terminal v0.1.37 object `51ecaabf3a6dfbaef0f842e95c4fc480dced0b6d`.
Original target/package/installed admission and source-bound matrices remain mandatory after
successful rehearsal, followed by normal integration and the selected combined publisher.
