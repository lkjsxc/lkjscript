# Native form decoding and byte conversion

## Original user mandate, unchanged

@Remote Desktop Commander  https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscript  について進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。限界まで深く考えてほしい。超長期的な視点からお願いしたい。ChatGPTの開発環境として、cw-bd235945fff44468a0f41292を用意しました。自由に活用ください。

## Reconciliation and selected work

Designated RDC host and independently read GitHub main agree at fa42c1ab5a2549b96994ddb909e651fb50c574ca. Only the unrelated untracked campaign 202609242022.md is present; preserve it. No stashes, other worktrees or active project jobs were observed. Root AGENTS.md applies without replacement. The earlier host-process work is already superseded by integrated successors.

Public immutable v0.1.45 (release 396174945) is latest. Use its anonymously downloaded, metadata-admitted Linux executable in a separate authoring workspace; do not rebuild the compiler. Keep the existing UI/HTTP library split. Prove native POST form composition, bounded validation, durable exact-base updates, errors and restart through the public product. No browser-language application, framework intrinsic or Wasm backend is selected.

The selected trust boundary is a local shared editor, not a public account service. Explicit deployment host/origin policy must reject foreign browser requests before reading body or data. State-changing GET is forbidden. Origin checks are not authentication; local host access is trusted. Existing services, real data, credentials and access controls remain unchanged.

One combined supplemental read was denied by tool safety policy. It was not retried or rerouted. Proceed only with separately permitted development operations and preserve failures.

## Execution

Implementation and evidence are pending. Record observed outcomes, not anticipated acceptance.

### Revised scope after public discovery

The public standard can inspect but cannot directly construct arbitrary byte values, and its UTF-8 conversion traps on malformed input. JSON Bytes use a tagged base64 representation, not integer arrays. Two exploratory plans also exposed incorrect assumed signatures; both rejected before acceptance and their literal proposals remain in the separate workspace. Avoid implementing a codec through a JSON/base64 detour merely to preserve a premature no-kernel-change plan.

Add general byte construction and a nontrapping UTF-8 result at their representation boundary, then consume them in ordinary native form parsing. This revises the initial public-v0.1.45-only goal: new work will need a successor development executable; the official release stays untouched. Full source verification is required. State-changing UI remains the motivating application, not a preclaimed completed result.

The initial compile succeeds. Its fixed validator-identity test fails exactly because the closed signature feature changed from 2 to 3; the observed successor is validator_contract_d0c6f2031df60bd2e83d72db6173433f71fb87a064e565e405ddb768fbe8efd6. The original failing log and initial source patch are retained. Update that fixed expectation, keeping the closed identity assertion. Both checked execution and explicitly supplied core-host paths now implement the new names. Checked byte construction reserves separate scratch and retained-byte allocations before either allocation and checks cancellation through traversal.

### Focused native boundary observations

The first host regression compile failed on three fixture-reference coercions; correcting those to byte slices preserves their vectors. A later public CLI test compile found that its helper returns parsed compact records rather than strings; diagnostics are now asserted by their exact code fields. The original logs remain, and neither failure is reported as a successful test run. A separate native consumer first used the unsupported form `struct`; that plan rejected without publication. Its corrected `record structural` proposal was newly reviewed.

Fourteen byte-filtered library tests pass, including five new conversion tests for all unsigned octets, persistent-list boundaries, invalid ranges and UTF-8, old strict conversion, cancellation/allocation failure, and explicit core-host paths in both evaluators. The exact signature test also passes. Three new public CLI tests pass using isolated copied products and empty PATH: exact JSON byte/results, detached use, invalid values without output, and eight foreign signatures without publication.

Standard publication adds exactly 97 owners, two types and twelve graph tests, with no updates/deletions or dependency changes. All 75 standard tests pass with both evaluators equal. Its exact public exports alone update embedded derived assets; existing application/tool supplier selections remain unchanged.

The pure native form library adds 522 owners and thirty fixed tests. It passes 106 total tests (75 standard, one template, thirty form), and its independent exact transported consumer passes 107. Body and field limits are explicit; duplicate order, raw and percent-encoded Unicode, plus/delimiter distinctions, malformed encoding and no partial result are native policy. No form-specific host intrinsic or browser scripting is added. The imported consumer has 45 new owners and one new dependency with no mutation to its existing standard selection.

Current publication status was separately reread: the original v0.1.45 promotion is completed/success, and release 396174945 is immutable/latest. Current README/status no longer describe that completed publication as pending. The new conversion and form code remains unreleased, distinct from those official bytes. Full source acceptance and integration are still pending here.

### Supplemental investigation boundary

A later combined supplemental read of data/UI examples and the first form-integration test log was denied by tool safety. It was not retried through another read or process route. That particular debug integration result is unavailable, not accepted. The task therefore closes the independently implemented byte/form boundary rather than claiming the motivating durable editor. Its newly maintained public integration test must be observed under the separate fresh full-profile acceptance; the blocked log is not used as evidence. No data/UI implementation from the denied request is inferred.


## Resumption — 2026-09-25 10:54 Asia/Tokyo

User continuation, unchanged: 環境を復活させました

The designated device responds again. Local main retains implementation
24a301acb3b06f9cd835f9a02eb4441404a4ffeb; independently read remote main remains
fa42c1ab5a2549b96994ddb909e651fb50c574ca. The sole unrelated untracked historical
campaign is preserved. No compiler/checker processes, stashes or other worktrees
remain. Root guidance is unchanged.

The interrupted full run is
`.artifacts/lkjscript-dev/check/1790298346391345014-538054-0`. It binds exactly
24a301ac and selected 26 gates with four workers. It retains successful fmt,
Clippy, release-build, checker-self-test and policy log endings, but no final
receipt. Workspace and release-lifecycle compilations overlapped when their logs
stopped. The current cgroup records eight OOM kills cumulatively; without a dated
victim record this does not establish which process was killed or the exact cause
of the remote outage. Keep the interrupted evidence unaccepted and unmodified.

Use one checker worker and one Cargo job for the new fresh acceptance. This is
existing scheduling policy, not reduced coverage or changed release settings.
Review also found that the old tiny-allocation test could fail before reaching the
new primitive. Add boundary-observed tests that require actual intrinsic entry,
independently fixed scratch/output sizes, refusal without ledger change, UTF-8
output allocation errors and cancellation after scratch reservation. Complete the
normative language specification for both general operations. No runtime behavior
or exact package selection is changed by this resumption patch.


### Focused resumption outcomes

The first new resource-test run reports six passes and two fixture failures:
pre-state cancellation has no observer yet, and successful invocation charges
final return bookkeeping after the primitive. Do not infer a reservation location
by subtracting only the output buffers from the complete success total. The corrected
fixture uses an invalid first octet with the same input shape to locate the boundary
after scratch allocation, requires intrinsic entry, and checks both refusal ledgers
against independent 2048-byte buffer sizes. Early cancellation remains unobserved;
only a later cancellation after scratch reservation satisfies the traversal test.
All eight conversion tests now pass, including the three new boundary-specific tests.
The original failed run remains in the resumption evidence directory.

The original release jobs were reread independently: publication 107888383589,
anonymous installed verification 107888499461 and terminal 107888830800 all succeeded
under original promotion 36075872648/1. Current release/roadmap guidance now reflects
that completion. New byte/form publication is deferred until a verified native POST
application consumes the library, followed by a separate versioned distribution
candidate. No release, credential or deployment operation is performed here.


## Formal first resumption result — failed, 25 of 26 gates

The fresh one-worker full attempt at e22a65c2f06f90b1925a55ac708a15aa45cb2007
completed in 1769.601543292 seconds with stable inputs and zero reused results.
Its formal receipt is
`.artifacts/lkjscript-dev/check/1790301569107549628-545421-0/receipt.json`.
Twenty-five gates pass, including every live service/HTTP/package boundary. The
workspace gate fails with exit 101: public CLI tests report 81 passed, four failed
and one unchanged ignore. This is failed source acceptance; it is not pushed.
The complete originals are preserved in `full-attempt-one-failed.tar`.

The failures are current-versus-pinned test-count assertions, not permission to
upgrade a consumer implicitly. Fresh native text authorship contains 75 standard,
one command starter and two join tests: expect 78, not 66. Fresh native policy
contains 75 standard and 11 policy tests: expect 86, not 74. The maintained native
guide graph keeps its exact earlier supplier: expect its original 79, not 91.
Its separate fresh-authorship test correctly expects 91 and remains unchanged.
Keep exact generated-asset equality, owner/revision preservation, all independent
value cases and negative cases. Fix only these expectations and clarify ownership;
do not weaken checks, change packages or relabel failed evidence as passing.

Cgroup OOM-kill observations remain eight before and after this complete run.
This establishes no additional observed OOM kill during the run, not the exact
cause of the earlier remote outage. Repository guidance now explicitly separates
checker concurrency from per-Cargo compiler concurrency.
## Verification and ordinary POST composition

This supplement describes new evidence, not a replacement for the original failed
or interrupted runs. The repository-wide acceptance result must be read from its
formal receipt before this section is completed or integrated.

The independently specified detached batch covers 688 encoded/raw octet, Unicode,
invalid sequence, duplicate, field-limit and body-limit cases. It runs through the
exact product retained by the fresh profile's release-build gate with an empty PATH.
Every result equals its independent expectation. Its original cleanup reports no
remaining tasks or live handles/frames/transactions. The 117,274,746 allocated-byte
counter is cumulative work, not RSS, and there is no matched speedup comparison.

A separate ordinary native HTTP receiver imports the exact form consumer. Its
three new media-type tests bring the shared closure to 111 passing graph tests,
with production/reference agreement. After moving its authoring root away, the
copied executable, bundle and descriptor complete 35 real loopback HTTP requests
across two joined SIGINT lifecycles, including eight simultaneous clients and an
exact-bundle restart. Outcomes are 20 HTTP 200, six 400, four 404, one 413 and four
415 responses. Both shutdown records contain zero active/queued work, admission
and worker permits, remaining tasks and cleanup failures.

The receiver handles POST /decode only, with an inclusive 8192-byte transport/body
limit and 64 fields. Its header policy admits exactly one Content-Type with one of
two literal spellings: application/x-www-form-urlencoded, or that value followed
by '; charset=UTF-8'. It deliberately does not claim general MIME normalization.
Decoded reports return JSON with no-store and nosniff; they do not save data.
The response codec and HTTP launch remain lkjscript; the external Node driver only
sends/asserts test requests and joins the owned process.

This is a stateless protocol-composition witness, not the selected durable editor,
authentication, CSRF protection, a browser-navigation test or a public deployment.
Publication should follow a useful durable native UI consumer and a separate
versioned exact-distribution candidate, rather than treating a diagnostic echo
endpoint as completion of that product milestone.

One supplementary canonical-draft/intermediate-log read was denied by tool safety.
It was not retried or rerouted; its unobserved result is not counted. The formal
full-profile receipt is an independently permitted output and remains authoritative.


### Exact-supplier repairs verified

All four previously failing tests pass after the scoped expectation repairs:
two native text tests, the pinned-guide reproduction test, and the fresh native
policy author/edit/detached test. Their original independent outputs, negative
cases, canonical preservation and exact-asset comparisons remain required.
Native text's 2197 independent value cases execute rather than being hidden behind
the stale precondition. Focused originals are retained under
`.artifacts/202609251054-resumption/focused-*.log`.

The supplementary stateless POST literal and reproduction instructions are added
without changing kernel, standard, maintained tool graphs or runtime artifacts.
The publication criterion is explicitly clarified to the useful durable UI editor:
a diagnostic POST decoder does not by itself satisfy that product milestone.
A new one-worker full profile must accept this corrected source; no failed receipt
or output from the unavailable supplementary read is reused as passing evidence.


## Accepted corrected source and mainline delivery

Corrected source **`0ce651cad480e4d62668da45fb273d2539441233`** passes **26/26**
full-profile gates freshly, with zero result reuse, no unrun gates, stable input
bindings and 1397.481 seconds elapsed. The formal original is
`.artifacts/lkjscript-dev/check/1790303650316933431-572079-0/receipt.json`;
`accepted-receipt.json` preserves its original bytes in the resumption directory.
The earlier interrupted and failed originals remain separate and unchanged.

Normal, non-forced mainline integration was followed by an independent GitHub
branch read confirming this exact source. It contains implementation 24a301ac,
resource-proof/specification descendant e22a65c2 and the scoped current/pinned
supplier assertion repair. Reporting follows as a separate documentation commit;
it changes neither the accepted product nor the tested native literals.

Every live service, stateful HTTP, distributed/outbound HTTP, offline package,
pure-tail, workspace, checker, lint, standard/application and generated-asset gate
passes. The four previously failing public tests retain their independent value,
negative-case, accepted-identity and exact-artifact checks. No test is newly ignored
or removed. This is local designated-host source acceptance, not a newly dispatched
GitHub Actions run or a release-candidate/musl portability acceptance.

Direct byte comparisons confirm that the corrected full profile's retained
release-build product is identical to the exact copies used for the 688-case
command oracle and 35-request HTTP observation. These are unchanged-program
observations, not a renamed source receipt or a repeated behavioral suite merely
because a filename changed. Both recorded HTTP lifecycles use the same bundle and
join cleanly. No experimental receiver remains running.

A later optional incomplete-body/header probe had its result-read request denied
by tool safety. That result was not retried, rerouted or counted; the observed
HTTP acceptance remains the original 35 requests. Its separate operational liveness
check found no remaining owned receiver. The unavailable canonical-draft supplement
also remains uncounted. Neither supplement is a gate bypass or a replacement for
the completed full profile.

The public v0.1.45 assets, credentials, production services, real operational data,
and unrelated untracked campaign are unchanged. No tag, release candidate or
publication was created. The next product milestone is the useful durable native
UI editor: typed POST actions, explicit access/origin policy, visible validation
errors, exact-base saves, conflict handling and restart persistence. This stateless
receiver proves composition but does not claim that milestone is complete.

Final cgroup observation (cumulative, not per-process attribution):

```text
low 0
high 0
max 3159193
oom 142
oom_kill 8
oom_group_kill 0
sock_throttled 277
```


The reporting descendant passes the existing no-Python and product-surface gates
using the accepted immutable verifier/product, plus git diff --check and 127 local
Markdown link targets. It contains only README, current status, roadmap and this
campaign narrative; the accepted source and all executable literals are unchanged.
