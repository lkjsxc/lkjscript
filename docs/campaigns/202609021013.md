# Revision-Pinned Function Definition Projection

Status: complete

Campaign ID: `202609021013-function-definition-projection`

## 1. Identity, audit boundary, and lifecycle
Audit time: `2026-09-02 10:13:14 +09:00` (`Asia/Tokyo`).

Orientation source: remote GitHub repository `lkjsxc/lkjscript`, default branch `main`, commit
`eca5d6862846f60e5792913757ebb940da717ca6`, tree
`bde5655dc3aed7c7f3015abd13af6e68622e9072`. This is not an active checkout. Reconciled checkout
state and legitimate later commits outrank it.

Inspected read-only: repository identity and recent commits; root `AGENTS.md`; the latest affine
source and release campaigns; current status, architecture, roadmap, semantic CLI, language,
effects, verification, and distribution owners; maintained standard and `lkjournal` boundaries;
public inspection/query/change implementation; generated owners; immutable latest release/tag;
recent workflow runs; and public issue/PR state.

Observed external state:

- source and immutable latest release are product `0.1.15`;
- release `380401840` selects source `2b428645471e032ba3cfb415bf81565d7ea0159e`
  through tag object `d9a6e7812325810c5becae2e217e1b9a4a600c8f`;
- release run `33495319420`, attempt 1, succeeded;
- archive asset `539383016` is 7,459,326 bytes with published SHA-256
  `5a16f67272784e93948cc779d13fab7b4ae8c5f42bf9b8860517bb72eae51f01`;
- no open issue/PR or queued/in-progress workflow was observed; and
- `docs/campaigns/202609021013.md` was absent.

Unavailable: local branch/`HEAD`/tree, index/worktree/untracked/ignored files, remotes/upstream,
divergence, stash, supplied files, installed binary, toolchain and execution, private workflow
artifacts or credentials, repository variables, deployments, hosted data, and production behavior.
A direct artifact-workspace clone could not resolve GitHub and produced no checkout. Historical
records name stash `87c2265f6faf0e815d80450ec2480d46c5e774fb` and ignored/root-owned artifacts;
preserve anything actually found without assuming it still exists.

Archive: `docs/campaigns/202609021013.md`. `docs/campaigns/` remains the sole mandate archive. No
mutable current-work ledger was observed; do not create another campaign, prompt, plan, or work tree.
Preserve this body, change only the minimal lifecycle field, and append one bounded terminal record.
Keep current truth, semantics, rationale, generated output, proof, and raw artifacts in their current
owners.

Predecessor disposition:

- `202609011758` is complete: immutable latest `v0.1.15` and anonymous acceptance closed the
  source/public split.
- `202609011246` is complete: Graph 6 affine resources, borrow/consume use, compiler/runtime
  enforcement, durable-queue cutover, standard/`lkjournal` migration, deletion, and proof are current.
- Older complete, terminated, superseded, and immutable historical campaigns are not active work.
- No active remote source, release, deployment, or recovery campaign was observed. Reconcile coherent
  local in-flight work before editing.

The bounded-context campaign deferred owner-body projection until maintained public authoring proved
need. That evidence now exists: the distributed surface writes rich function/effect/resource graphs,
while local inspection returns only one owner plus summary and context returns summaries plus
relations. This write/read asymmetry is now a machine-first application bottleneck.

## 2. Implementation mandate and stopping rule
This is implementation and proof, not another planning pass.

**Required final state:** unreleased `0.1.16` lets a copied distributed executable project the complete
accepted definition of one local function—contract, typed expression closure, lexical/pattern
structure, exact references, and revision-bound validation facts—through deterministic bounded
stateless pages under `inspect owner KIND ID --detail definition`; the result independently agrees for
the maintained affine `lkjournal` worker and closes a copied-binary inspect → plan → apply →
reinspect HTTP workflow, without exposing storage, accepting a second authoring format, changing
maintained meaning, or touching immutable `v0.1.15` or operational state.

This campaign adds no graph semantics. Its leverage is making existing accepted semantics completely
observable at the function boundary used by agents. The graph remains sole editable authority;
definition output is revision-bound derived transport. Do not add source files, a body cache/index,
mutable inspection sessions, application-specific dumpers, or a projection reader in `change`.

Advance the source snapshot from released `0.1.15` to unreleased `0.1.16`. This is source-only. A
later campaign must reconcile publication before another independent product delta. Do not tag,
release, dispatch, deploy, or migrate here.

Outcome rules:

- `complete`: FDP-1 through FDP-8 pass freshly; coherent commits and authorized normal push close;
  generated/current documents are truthful; and the tree is clean or preserved work is exact.
- `blocked`: the objective remains valid but a required gate, safe reconciliation, or push cannot
  close. Leave status `incomplete` and append exact reversible state.
- `terminated`: the projection cannot avoid storage exposure or parallel editable authority, or
  cannot be bounded and independently proved within this campaign.
- `superseded`: later legitimate work already provides an equivalent or stronger supported result.

Continue through benign warnings, optional cache failures, unavailable convenience tools,
non-material drift, and absent interactive authentication. Stop before overwriting unfamiliar work,
weakening a bound, accepting projection bytes as authority, performing an unauthorized mutation, or
claiming completion without copied-binary and independent proof.

## 3. Reconciliation gate
Before editing:

1. Read applicable agent files, this mandate, terminal records `202609011758`, `202609011246`, and
   `202608300506`, current status/architecture/roadmap, relevant specs/decisions, generated owners,
   and verification policy.
2. Capture branch, `HEAD`/tree, index/worktree/untracked/ignored state, remotes/upstream/divergence,
   recent commits, stashes, supplied files, toolchain, and unfamiliar work. Preserve unrelated work;
   do not reset, clean, restore, rebase, or mutate a stash.
3. Fetch safely. Refresh remote `main`, active campaigns, issues/PRs, queued/running workflows,
   latest release/tag, and root version. Later legitimate identities win; never rewind one.
4. Locate exact owners of local inspection, bound summaries/facts, canonical owner decoding, function
   contracts, expression/pattern records, type/effect/resource facts, compact output/continuations,
   capability generation, and copied-binary verification.
5. Capture the smallest predecessor baseline: summary-only function inspection, context output,
   guessed body/raw/JSON rejection, and no-write behavior.
6. Identify the exact `lkjournal` task function performing queue claim, live match, metadata borrow,
   heartbeat consume, renewed-state match, and complete/fail consume; record its identity and size.
7. Finish compatible partial work. Stop for a conflicting active semantic or inspection authority;
   do not create dual surfaces.

Correct non-material names, paths, commands, and counts locally. Routine factoring and test placement
are Codex latitude. Public semantics, authority, maintained consumer, bounds, and authorization are
fixed.

## 4. Current bottleneck and campaign decision
At the orientation revision:

- one accepted typed graph revision is sole editable program authority;
- `inspect owner KIND ID [--package PACKAGE]` reads an exact owner and bound summary, not a complete
  function body;
- `query context` reads revision-pinned owner summaries and relations, not expression fields;
- public `change` can replace bodies and author current expression, effect, topology, higher-order,
  transaction, and affine-resource forms;
- storage/Rust access can recover missing fields, but neither is an ordinary distributed workflow;
- `lkjournal` is a maintained 1,579-owner application whose worker uses the complete affine queue
  protocol in graph meaning.

The bottleneck is incomplete distributed observability: agents can write sophisticated meaning and
locate its neighborhood but cannot reconstruct one existing nontrivial function without checkout-
specific implementation knowledge.

Scores are comparative; `5` is strongest, while cost/risk `5` is highest.

| Candidate | Value | Leverage | Authority | Closure | Completion | Proof | Cost | Risk | Decision |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| Function definition projection | 5 | 4 | 5 | 5 | 5 | 5 | 3 | 2 | **Select** |
| Task-function resource transfer | 4 | 5 | 4 | 2 | 2 | 5 | 5 | 3 | Defer: no maintained call-boundary need |
| Nostr WebSocket/session slice | 3 | 5 | 3 | 1 | 1 | 3 | 5 | 5 | Defer: transport, resource, key/auth/replay, and consumer scope unclosed |
| Million-owner/history admission | 3 | 2 | 2 | 3 | 3 | 4 | 4 | 2 | Defer: separate scale decision |

Projection wins because current public writing and one maintained application prove need, the exact
read view already exists, no semantic/data migration is required, and a disjoint oracle is possible.
Resource transfer remains the strongest semantic alternative, but choosing it now would manufacture a
workload and violate evidence gating.

## 5. Fixed target semantics and scope
### 5.1 Public operation and disclosed meaning
Extend local project inspection with exactly:

```text
inspect owner KIND ID --detail definition \
  [--limit N] [--bytes N] [--continuation TOKEN]
```

Current exact `KIND`/`ID` parsing remains. Definition detail succeeds only for a live local function
with a body. Omission retains current summary inspection. Do not add `inspect body`, `query body`,
`--full`, `--raw`, `--source`, `--recursive`, JSON, or aliases. Built-in/dependency package inspection
remains implementation-free; dependency-body requests reject.

Every page identifies repository, package, accepted revision, function owner, projection contract,
complete definition digest/counts, page range, and continuation. The complete projection contains:

- function visibility, type parameters, ordered parameters/use modes, result, pure/task effect,
  requirements, and body root;
- every structurally owned body record, including expression, lexical binding, construction,
  projection, list, match/case/pattern, call, transaction, and resource forms present;
- canonical parent/slot/index structure and exact local/package/declaration/interface/operation/
  requirement/field/type/parameter references, without recursively expanding referenced declarations;
- resolved type/effect facts and resource provenance/borrow/consume classification where current bound
  facts own them; and
- the owner-summary/validation identity binding the definition to the accepted revision.

Expose only accepted meaning and identified derived facts. Never expose storage bytes/paths, cache,
compiler/artifact operands, runtime handles, deployment/grant/secret/environment state, operational
data, queue transition tokens, object bytes, host layout, or evidence paths. Program literals remain
visible under current escaping/bounds; operational secrets are never consulted.

This is read-only transport. `change` rejects it as input. Shared names/encodings may match authored
change where meanings agree, but output retains exact accepted identities rather than editable labels.
Do not create whole-project source export or claim projection round-trip authority.

### 5.2 Closure, order, bounds, and continuation
Materialize and validate the complete admitted definition before rendering success. Follow structural
body ownership only; named declarations and package/interface/operation/type owners are reference
boundaries. Missing/foreign/shared/cyclic children, duplicate slots, noncanonical order, invalid
bindings, and summary/fact mismatch are corruption, never repaired partial output.

Canonical sections are: definition/revision header; function contract in semantic field order; body
records in structural preorder using normative slot order and explicit index, with typed identity as
tie-breaker; external references ordered by `(role, typed target)`; then validation facts in body
record order.

The complete logical result admits at most 4,096 body records, 16,384 structural/reference edges,
32,768 fact reads, depth 256, and 8 MiB canonical logical encoding. Derive and advertise separate
map/object/decode/output admissions from these maxima and current physical contracts. Prove exact fit
and one-over failure. If current global contracts make a fixed maximum impossible or unsafe, terminate
with the contradiction rather than silently shrinking the maintained boundary.

Existing compact page limits remain authoritative. Item limit counts emitted records, not visits.
Exhaustion is atomic; paging never hides incomplete closure. Each page repeats complete digest/counts.

Introduce one stateless `icont_` continuation binding repository, package, revision, function,
projection contract/digest, order, section, and exclusive resume key. It carries no body, frontier,
cursor, cache, or process state. Resume recomputes the complete definition from the immutable view.
Only page item/byte budgets may change. Malformed, padded, truncated, oversized, foreign, stale,
selector-mismatched, impossible, and predecessor tokens reject without writes.

### 5.3 Maintained and public workflow proof
Through a copied candidate and isolated authority copy, force multipage projection of the maintained
`lkjournal` worker. Prove generic records/references show acquisition through `jobs`, absent/live
match, `lease-info` borrow, heartbeat consume, renewed live state, and complete/fail consume. Add no
application-specific mode or decoder.

In a fresh copied-binary HTTP project:

1. discover inspection and change grammar offline;
2. locate/project `response-text`;
3. construct one ordinary compact `replace.body` request changing its bounded static response;
4. plan, review, apply, and reinspect at the accepted child;
5. prove exact intended body change and unchanged function contract;
6. reject stale continuation/base/plan and malformed projection input without advancing authority;
7. check, deterministically build, serve on loopback, and observe the new response; and
8. clean up completely.

Projection records are not submitted to `change`; the verifier constructs a current compact request
from discovered meaning. This proves sufficient observation without adding a second writer.

### 5.4 Identities, deletion, and non-goals
With the orientation baseline unchanged:

- advance product `0.1.15` to unreleased `0.1.16`, CLI contract 17 to 18, registry 5 to 6, and exactly
  one inspection-definition/continuation owner;
- regenerate capabilities and executable-owned docs;
- keep Graph 6, witness 3, owner-summary 2, validator 6, authored-change 7, compact change 9,
  package-interface 4, compiler/bytecode 2, artifact 11, resident runtime 2, queue adapter 2,
  data/store/backup/queue-data, deployment, HTTP-client, package transport, project creation,
  maintained semantic/artifact, and release identities unchanged unless checkout proof shows their
  normative bytes include this projection; and
- never synchronize unrelated versions.

Delete obsolete application-specific body formatters, direct storage-decoding inspection paths,
duplicate continuation codecs, compatibility spellings, and hard-coded body descriptions made
unnecessary by the generic projector. Retain genuinely disjoint oracle and generic inventory code.
There is no predecessor public body surface to preserve.

Do not add resource transfer, closures, affine containers, general linear/session semantics, new
write operations, whole-project export/import, dependency implementation disclosure, history/fuzzy/
impact query, mutable sessions, source text, IDE/LSP, WebSocket/NIP-01, signing/key custody,
large-scale admission, another target, package manager, deployment, migration, release, or live
network contact. Do not change standard or `lkjournal` meaning for presentation convenience.

## 6. Success conditions
### FDP-1 — Public contract
Discovery exposes exactly the new grammar, local-function boundary, records, `icont_` continuation,
limits, diagnostics, authority effects, and nonclaims. Summary inspection remains coherent. Aliases,
dependency bodies, projection-as-change, JSON/raw/storage/source forms, and unknown fields reject.

### FDP-2 — Complete definition
Production emits the complete function contract, structurally owned body, exact references, and
required bound facts in canonical order. Missing/shared/cyclic/foreign/noncanonical/corrupt ownership
rejects without partial output; referenced declarations are not recursively expanded.

### FDP-3 — Bounded stateless paging
Logical maxima, derived admissions, exact-fit/one-over behavior, complete digest/counts, and stateless
continuation are proved. Multipage resume with changed page budgets has no omission, duplication, or
reordering. Every stale/mutated/foreign/mismatched/impossible token rejects with zero writes.

### FDP-4 — Authority and containment
Success/failure read one immutable view and leave semantic `HEAD`, graph inventory, cache, generated
application output, descriptors, operational data, queue/object state, and filesystem authority
unchanged. Output contains no forbidden implementation/operational detail; cancellation/exhaustion
release temporary resources.

### FDP-5 — Independent maintained oracle
A disjoint oracle reconstructs definitions from complete typed authority without production
traversal/order/paging/render/token logic. It agrees on the maintained affine worker, representative
pure/task/generic/data functions, and adversarial cases. Worker conclusions follow generic records
and exact references, not only hard-coded expectations.

### FDP-6 — Copied-binary read/change workflow
A copied release-mode candidate performs the fresh HTTP discover/project/page/plan/apply/reinspect/
check/build/serve workflow. Only intended meaning changes; stale/malformed cases do not. No Cargo,
checkout source, private builder, ambient package path, or contributor writer performs product work.

### FDP-7 — Identity and documentation closure
Only owned product/inspection/CLI/registry identities advance. Generated docs reproduce. Standard and
`lkjournal` revisions, transports, compiler units, artifacts, tests, and descriptors reproduce
byte-for-byte. Current docs distinguish unreleased source `0.1.16` from immutable latest `v0.1.15`.

### FDP-8 — Proof and repository closure
Focused, public CLI, generated, product, maintained-application, clean/incremental, full, unchanged-
asset, predecessor-rejection, and static-musl target gates pass freshly after inputs stabilize. One
bounded evidence owner records identities, classifications, definition digests/sizes, negatives,
resources, and cleanup. Coherent commits push safely; the tree is clean or preserved work is exact;
and the campaign becomes terminal without rewriting this body.

## 7. Dependency-ordered implementation
1. Reconcile state and retain only the summary/context/rejection baseline needed for proof.
2. Define the typed definition model, closure, disclosed fields, reference cutoff, facts, order,
   limits, diagnostics, contract identity, and `icont_` binding.
3. Implement one read-only projector over an exact repository view; validate the whole closure before
   rendering; keep storage/application knowledge below it.
4. Add stateless paging and separate logical/physical/output accounting; create no cache or cursor.
5. Implement the disjoint oracle and finite corruption/mutation corpus. Production must not call its
   traversal, ordering, expected-result, or encoder helpers.
6. Extend copied-binary verification for multipage `lkjournal` inspection and the HTTP read/change/
   service workflow while keeping maintained inputs unchanged.
7. Delete obsolete equivalents, regenerate docs, and update semantic CLI/authority specs,
   architecture/status/roadmap, README examples, durable rationale when needed, and bounded evidence.
8. After inputs stabilize, run broad profiles and target admission once, review staged diffs and
   unchanged identities, commit coherently, perform only the authorized push, refresh external state,
   and append the terminal record.

Latitude: internal names/types, collections, helper layout, compact record labels consistent with
current conventions, test decomposition, and bound-fact access path. Fixed: public grammar,
local-function-only scope, disclosed meaning, reference cutoff, limits/order, stateless token, no
projection reader, maintained proof, no maintained meaning change, identities, and authorization.

## 8. Verification and evidence
Use current first-party owners and discover exact invocations.

1. Baseline summary-only inspection, context, guessed-form rejection, and no-write behavior.
2. Iterate with typed model, closure validation, ordering, renderer, limits, tokens, hostile input, and
   zero-write tests.
3. Run the oracle over all currently public-authored forms, including pure/task effects, generics,
   transactions, nominal matches, capability calls, and affine borrow/consume.
4. Prove exact fit/one-over for records, edges, facts, depth, logical bytes, map/object/decode,
   page records/bytes, plus cancellation during materialization/rendering.
5. With a copied candidate, page the maintained worker using small and changed resumed limits; compare
   complete records/order/references/facts/digest/inventory with the oracle.
6. Run the copied-binary HTTP workflow, including direct/file request equality, stale token/base/plan,
   malformed projection/change input, build equality, loopback response, and cleanup.
7. Regenerate/compare docs and prove standard/`lkjournal` meaning, transports, clean/current/
   incremental artifacts, tests, and descriptors unchanged.
8. After final inputs stabilize, run focused, public/product, maintained, generated, and full profiles,
   then exact `x86_64-unknown-linux-musl` target admission. Do not run release/workflow gates.

Negative cases include missing/retired/non-function/foreign/package owners; malformed identity;
dependency body; missing/shared/cyclic/foreign child; duplicate/noncanonical slot; summary/fact
mismatch; every bound and token mutation; stale revision after change; projection-version mismatch;
projection bytes supplied to change; predecessor/raw/JSON/source aliases; cancellation; and unchanged
authority after rejection.

Record only owned dimensions: records/edges/facts/depth/logical bytes, map/object reads, rendered
records/bytes, token length, process wall/CPU/RSS where already owned, copied-binary operations,
gates, and cleanup. Make no latency, throughput, memory-safety, portability, isolation,
confidentiality, reproducibility, provenance, or service-level claim.

Classify every gate `fresh passed`, `reused passed`, `skipped`, `unavailable`, `failed`, or `not run`.
Final oracle, copied-binary `lkjournal`, HTTP workflow, focused, product, maintained, generated, full,
unchanged-assets, predecessor, and target gates must be fresh. Required unavailable/failed proof
blocks completion.

Keep raw logs, pages, requests, copied binaries, temporary projects, mutated roots, artifacts, and
service data under `.artifacts/campaign/202609021013/` or current ignored owners. Track bounded proof
at `docs/evidence/202609021013-function-definition-projection.json` unless current convention owns an
equivalent path.

Narrow checkout questions:

1. What later Git/worktree/stash/supplied/active state exists?
2. Which function record, structural relations, bound facts, and inspection contracts own this without
   storage coupling?
3. What is the largest maintained function, and do fixed maxima admit it with exact-fit proof?
4. Which contract identities actually advance while Graph/query/change/maintained assets stay exact?
5. Which verifier entry points own copied-binary use, disjoint reconstruction, inventory comparison,
   and target admission?

These questions do not reopen grammar, disclosure, package boundary, limits, consumer, lifecycle, or
authorization.

## 9. Read map
Start with root `AGENTS.md`; this mandate; campaigns `202609011758`, `202609011246`, and
`202608300506`; status/architecture/roadmap; semantic CLI/authority/language/effects/product/
verification specs; capability/generated owners; `src/platform/cli.rs`; normalized query and token
models; publication read views and bound summary/fact readers; kernel function/expression/pattern/
type/effect/resource records and codecs; public CLI tests; `lkjscript-dev` check/product/target/
evidence owners; and maintained standard/`lkjournal` authority.

Expand only for exact dependencies, changed identities, or failures. Do not reread all campaigns,
ingest raw artifacts, inspect operational data, or freeze remote symbol names before checkout proof.

## 10. Git and external actions
Stage explicit paths, inspect staged diffs, and avoid broad staging, destructive cleanup, unrelated
format/dependency churn, or history rewrite. Prefer one implementation commit; allow at most one later
evidence/lifecycle commit that changes no receipt-bound implementation, verifier, target, generated,
maintained, or normative input.

| Action | Authorization |
|---|---|
| Scoped local edits and owned generation | Authorized after reconciliation |
| Builds/tests, copied binaries, pinned userlands, loopback services, isolated roots/data | Authorized; no non-loopback exposure, live relay, hosted/user data, or production authority |
| Temporary public changes in fresh/isolated project copies | Authorized only for Section 5.3 proof |
| Checked-in standard/`lkjournal` meaning, transport, artifact, descriptor, or operational mutation | Forbidden; an unexpected need invalidates this campaign |
| Coherent local commits | Authorized after proof and staged review |
| Normal fast-forward source push | Conditional after fetch, no conflicting divergence, proof, review, and existing noninteractive credentials; required for `complete` |
| Bounded read-only GitHub/release/workflow inspection | Authorized |
| Workflow dispatch/rerun | Forbidden |
| Tag/release/asset/checksum/attestation/installer/package publication | Forbidden |
| Deployment/service reconfiguration | Forbidden |
| Schema/data/database/queue/object/hosted/user mutation | Forbidden |
| Issue/PR/settings/permission/secret/variable/credential mutation | Forbidden |
| Interactive login | Forbidden; absence blocks only push |
| Force push/rebase/reset/clean/restore/stash mutation/retag/published-object mutation | Forbidden |

Missing authentication does not stop implementation, proof, or commits; it blocks push and leaves the
campaign `blocked`. Before final reporting refresh branch/upstream, issues/PRs, workflows, latest
release/tag, and immutable `v0.1.15`. No external mutation is authorized except a safe normal push.

## 11. Final report and terminal record
Return a bounded report with start/final Git, worktree, upstream, stash, supplied/preserved state;
commits and push; changed/unchanged product, contract, capability, semantic, package, artifact,
target, release, and deployment identities; public grammar/bounds/token/security boundary;
`lkjournal` function identity, definition digest/counts, pages, affine observations, and oracle result;
HTTP read/change/service result; every gate/classification; negative/stale/cancel/exhaustion/no-write/
cleanup/generated/clean-incremental results; unchanged maintained digests; evidence path/digest;
resource observations; deviations, limitations, nonclaims; every external action performed or not;
remaining source/public/deployment split; and smallest next candidates.

Next candidates include immutable `0.1.16` publication; task-function resource transfer only from a
maintained need; separate million-owner compiler and data/history decisions; remaining authored
operations from observed workflows; and Nostr WebSocket/session only after general connection,
resource, key/auth/replay, consumer, and raw-protocol proof close.

Append one concise terminal record below. Completion changes status to `complete`; termination or
supersession uses that status; blocked remains `incomplete`. Include time, start/final identities,
commits, FDP-1 through FDP-8, required classifications, evidence digest, definition/target and
unchanged maintained identities, push/external actions, preserved work, limitations, source/public
split, and next candidates. Do not rewrite this body or paste raw pages, logs, inventories, generated
documents, manifests, or long measurements.

## Terminal record

Completed at `2026-09-02T04:29:20Z`. Entry `main` was
`eca5d6862846f60e5792913757ebb940da717ca6` (tree
`bde5655dc3aed7c7f3015abd13af6e68622e9072`); final receipt-bound source is
`c8e11170b721cffa9e080d1d8cd577380fcd5c4d` (tree
`69b0cf8d6e22a58ff6d9b1de674e757ab3011870`). Implementation commits
`3f581f793c7d2e26b6c35ab22a780186436c4894` and
`c8e11170b721cffa9e080d1d8cd577380fcd5c4d` contain the generic projector, aggregate admitted
read view, stateless continuation, disjoint oracle, copied-binary HTTP and maintained-service
proof, generated/current/normative documentation, and the stateful guide-count correction exposed
by the first full run. The containing evidence/lifecycle commit and normal push result are
necessarily reported outside their self-referential content.

FDP-1 through FDP-8 are `fresh passed`. Final focused, product, and full profiles passed 6/6,
19/19, and 24/24 gates freshly with zero reuse. Generated guides, public/predecessor behavior,
standard and `lkjournal` checks, clean/exact-current artifacts, built-in transport/artifact export,
unchanged assets, product-surface audit, distributed/outbound/stateful HTTP, and service acceptance
all passed at stable worktree digest
`verification_28f2620792ae21a74997d8e47dd3c32c4ff318ff8087e81a2a8ff71bd7d09da2`.
The first focused, product, and full attempts correctly failed on stale seven-guide/64-KiB public
test expectations, internal-catalog prose, and the stateful verifier's predecessor guide count;
each was corrected before the final fresh receipts.

Source advances to unreleased product 0.1.16, CLI 18, registry 6, capabilities digest
`7bbb0d3a2410911d815ee4159a3a9b0244c12eef0045db241565d449b9158d09`, and sole projection
`lkjscript-function-definition-projection-1`. Exact grammar is `inspect owner KIND ID --detail definition [--package PACKAGE] [--limit N] [--bytes N] [--continuation TOKEN]`. Logical maxima are
4,096 body records, 16,384 combined edges, 32,768 fact
reads, depth 256, and 8 MiB canonical encoding; pages retain 10,000 records/4 MiB maxima and a
320-byte stateless `icont_` binding. Complete closure validates before rendering; dependency bodies,
source/raw/JSON/recursive aliases, projection-as-change, malformed/stale/mismatched tokens,
corruption, one-over exhaustion, and materialization/render cancellation reject without writes.

The maintained worker `decl_a914bb78de075ff44a857ac028d704f3` at revision
`rev_33c934d060f13ede19acbcbd6ff60d37a3532db215eef90b6cdd49e420e9a704` projects digest
`definition_eaaf72ff8b3d39f940af04405dc256bb081f27070918ec73f1624fa63f65e2be`:
210 records over six changed-budget pages (9 contract, 48 body, 99 reference, 51 fact), 51
structural edges, depth 8, and 88,035 logical bytes. The disjoint oracle agrees on owner/fact/
relation digests and generically observes `jobs` claim, absent/live matches, `lease-info` borrow,
heartbeat consume, renewed live state, and complete/fail consume. Largest maintained function
`decl_0693166bd7c29bee83d2ead289148f65` has 192 body records and is admitted at digest
`definition_7494a67ac0a3538d57e108eddd6e6b9379949b6717b0267f5563415ab1ebd0a2`.

The copied static candidate completed offline discover/project/page/plan/apply/reinspect/check/
clean-build/serve for `response-text`; raw-record digest recomputation agreed, the contract remained
unchanged, exactly the bounded response literal changed, direct/file plans agreed, and malformed or
stale continuation/base/plan plus projection input left authority unchanged. The exact
16,713,952-byte `x86_64-unknown-linux-musl` candidate has SHA-256
`c6cc5b3d1b2cf10243e6dfc7a8902fc9f8c1b137cbe5d573f80468f360c8bde6`. First-party static ELF
inspection, both pinned networkless userlands, and all four application/service oracles are
`fresh passed`; target receipt SHA-256 is
`d443c4521958e751ea882213a7a9d053ac700013d2af6e6c5fe3862f425fc2ac`, with complete cleanup.

Graph 6, witness 3, owner-summary 2, validator 6, authored 7, compact 9, package-interface 4,
compiler/bytecode 2, artifact 11, resident runtime 2, queue adapter/data, store/backup, deployment,
HTTP client, package transport, project creation, release, and maintained semantic identities did
not advance. `packages/standard` and `applications/lkjournal` Git trees equal entry exactly;
maintained artifact SHA-256 values remain
`fe9e633c71d07bd434d2055ca5197250fcb0a15e36bfd734c48d735880a3e79b` and
`12b39dce25366bd6f6ee2d78dc4d73f03b55d020df7332e1ef914497ad46e728`.
Structured evidence is `docs/evidence/202609021013-function-definition-projection.json`, SHA-256
`0d7c0be0ab8892a720d2bd8ee07968b98da7ae67e912f95b4e1dc24ade10e4ad`.

A final fetch found `origin/main` unchanged at entry, this campaign 0 behind and 2 ahead, no open
issue or pull request, no queued/in-progress workflow, and latest successful run `33495319420`.
Immutable/latest `v0.1.15` remains release `380401840`, tag object
`d9a6e7812325810c5becae2e217e1b9a4a600c8f`, source
`2b428645471e032ba3cfb415bf81565d7ea0159e`, and archive SHA-256
`5a16f67272784e93948cc779d13fab7b4ae8c5f42bf9b8860517bb72eae51f01`.
The authorized normal push dry-run was a safe fast-forward; at record assembly no external mutation
had occurred. Workflow dispatch, tag/release/publication, deployment, migration, hosted/user data,
issue/PR/settings/credential mutation, and live network application contact were not run. Stash
`87c2265f6faf0e815d80450ec2480d46c5e774fb`, ignored derived/artifact material, and four named
unreadable root-owned historical container directories remain preserved.

Claims remain limited to one local function, the exact candidate, named bounds, pinned userlands,
and tested behavior; no source round trip, dependency-body disclosure, whole-project export,
latency/throughput, confidentiality, sandboxing, reproducibility, provenance, signing, or generic
portability is claimed. Source 0.1.16 remains unpublished and must be reconciled before another
independent product delta. Smallest next candidates are immutable 0.1.16 publication; workload-
driven task-function resource transfer; separate million-owner compiler and data/history decisions;
remaining authored operations from observed workflows; and Nostr WebSocket/session only after its
general authority, consumer, and raw-protocol proof close.
