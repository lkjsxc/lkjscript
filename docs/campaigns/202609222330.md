# Autonomous improvement continuation

## Original user mandate

限界が来れば自動的にあなた自身が停止されるようになっているので、限界まで、自律的に次にやることを探して、より良くするループを続けてください。

## Startup reconciliation — 2026-09-22 UTC

The mutable checkout is clean on main at
`4f34fe421b131e0c03653142aa67899ca2226597`, matching freshly fetched origin/main.
The supplied repository guidance is identical to the existing `AGENTS.md`; no
guidance edit is needed. The unrelated preservation stash and two detached map
evidence worktrees remain intact. No active local build or product process was
observed. Rust 1.98.0 is pinned; approximately 11 GiB available memory and 824 GiB
free disk were observed. Main is unprotected and effective branch rules/rulesets
are empty; refresh these before integration.

The [composable-cell mandate](202609221952.md) remains the responsible lineage.
Its implementation, focused proof and local source acceptance are complete;
selected immutable delivery remains actionable. Hosted candidate `35725872480/1`
at `b52f51bd334dc71462e46c298dea12454f59ed6f` actually failed on
2026-09-22T13:51:55Z, superseding the predecessor's last running observation.
The final-candidate admission step and terminal failed; essential accepted
handoffs were not uploaded. Diagnostics artifact `10697836925` is available
until 2026-10-06T13:51:37Z. Diagnose the original failure before choosing repair
or resumption. Do not promote an unaccepted candidate or duplicate a healthy job.

v0.1.40 publication and public verification are complete and remain unchanged.
Native tooling/self-hosting and contingent roadmap features are not selected
merely because they exist in the roadmap. Continue with demonstrated defects or
concrete public workloads after the selected release boundary is reconciled.
Append execution findings at the existing campaign and evidence owners rather
than maintaining a second detailed ledger here.

## Documentation follow-up

After the verified repair reached main and replacement candidate `35798767360/1`
started, an independent review found stale current-product claims: the root README
still recommended v0.1.38 and its source, omitted complete native declarations and
canonical drafting, and described supported predecessor readers as absent. Current
status and roadmap mixed delivered work with old pending/unreleased observations.

The README now names published v0.1.40 and gives a literal native square-function,
test and command example with canonical re-entry. A fresh copied official v0.1.40
executable outside the compiler checkout accepted that exact declaration body,
passed 39 graph tests with evaluator agreement, returned the independent expected
144, and planned its generated module draft unchanged. All product invocations
used a cleared environment with no PATH or compiler/toolchain access. Literal
request, plan, draft, bundle and command outputs are retained under
`/home/coder/workspace/lkjscript-native-docs-20260922`. This is tutorial verification,
not renewed release acceptance or native contributor-tool implementation. The
first copy command selected the extracted directory instead of its executable and
failed before product execution; the empty initial output remains preserved.

Status is reduced from 1,153 to 171 lines and roadmap from 218 to 107 lines, with
current capability boundaries, actual pending delivery and links to maintained
history. The original summaries remain unchanged in Git at tested source
`c78840a74ce19ad5145e1e23ca5ff2b2a7198c0f`; campaign originals and genuine failures
are untouched. No old failed observation becomes a pass. A local documentation
check resolves all 65 selected relative links/fragments and confirms the README
native body is identical to the executed literal except its user-supplied base.
These documentation descendants do not change or reaccept the candidate source.

With intended documentation paths staged, `check changed --fresh --machine` selected
and passed only `diff_check` and `rust_only_tooling`, zero reuse; receipt
`.artifacts/lkjscript-dev/check/1790120803841196854-2604963-0/receipt.json`.
The maintained product-surface audit also passes all 33 files and 26 executable
discovery commands with zero violations/exceptions against immutable Product 02.
The first surface invocation used official v0.1.40 with the v0.1.41 verifier; its
27 protocol-version mismatches are preserved in `documentation-surface.stdout.json`.
The owner intentionally binds the current product version. Correcting the selected
audit binary, without changing its assertions, produced
`documentation-surface-current.stdout.json`. Tutorial execution remains separately
bound to actual public v0.1.40. No product/source/target suite was repeated for these
documentation changes. Final reporting additions receive whitespace/no-Python checks.

## Diagnostic retention follow-up

A bounded audit of maintained artifact pins found no additional hard-coded product
artifact pin outside the corrected service owner. The analogous opt-in `Verify`
workflow did omit that owner's separate `.artifacts/lkjscript-dev/service`
directory. Its `full` profile launches service acceptance, but the gate declares no
retained outputs: only the summary logs live under the uploaded check directory.
The workflow now uploads the service originals on success or failure, matching the
release repair. The development-verification guide states that distinction. No
verifier, gate selection, publication authority or candidate source changes.
Local validation confirms the workflow differs only by that one literal upload
path and covers both retained genuine service success/failure originals.
Whitespace and the maintained no-Python policy pass. This does not claim a hosted
upload observation; no new full run or duplicate release candidate was dispatched
for this additive diagnostics-only change.

Separately, the copied Product 02 executable generated and replanned unchanged
native drafts for four exact maintained modules: standard core/data and lkjournal
worker/service. Literal drafts and observations remain outside the checkout at
`/home/coder/workspace/lkjscript-maintained-drafts-20260922`. This bounded selection
does not claim an exhaustive inventory, fresh authoring, new maintained adoption,
performance, or candidate acceptance. Both copied accepted HEAD files stayed equal
to their maintained originals; no apply or live application execution occurred.

## Native library guide and discovered deployment defect — 2026-09-23 UTC

The new [native-library guide](../guides/native-library.md) exercises a complete
literal generic `sum-by<Item>` library and a separately authored shipping command
whose nominal `Parcel` belongs to the consumer. A copied official v0.1.40 executable
outside the compiler checkout passes 40 library and 42 consumer graph tests with
evaluator equality. Fixed inputs produce 850 and zero; multiplication and summation
overflow reject. An unchanged canonical draft replans without publication. Export,
staging, reviewed exact binding, build after moving the producer, and standalone
execution after moving the consumer all pass. Original projects are retained under
new names, not deleted. Native literals and the complete deployment descriptor in
the guide match the executed inputs after substituting only observed identities.

Evidence remains under `/home/coder/workspace/lkjscript-library-guide-20260923`,
using the same official v0.1.40 bytes as the earlier README observation. Product
commands use only `LANG=C.UTF-8` in their environment. A host assertion checks text
equality and independently fixed results; it does not generate semantic input or
implement the library. This is tutorial verification, not maintained application
adoption, contributor-tool migration, or new release acceptance.

Initial exploratory commands used an invalid owner-kind filter and capability
selector; their genuine diagnostics remain. Initial descriptor attempts used an
object for grants or omitted required fields; each rejected before invocation.
The corrected complete descriptor runs successfully. Those authoring mistakes
are not relabelled as product failures. The last omission, however, exposed a
real public-discovery contradiction: `listen`, `http`, `session` and `worker` were
advertised as `required=false`, while the specification and strict decoder require
their presence, using null for foreground commands. This reproduces in official
v0.1.40 and remains in the selected v0.1.41 candidate source.

The bounded repair changes those four executable-owned schema flags to true and
regenerates their public guides. It preserves decoder behavior, descriptor bytes,
runtime policy, artifacts and application data. A regression removes each top-level
field from a valid foreground descriptor and independently compares strict decoding
with discovery. It fails against the original flags and passes after correction;
all seven deployment unit tests pass. Original failed/passed test logs and immutable
Cargo-reported test executables are retained under `.artifacts/composable-20260922`.

Candidate `35798767360/1` at `c78840a7` was still running source acceptance when this
invalidating product defect was established. Cancellation was requested through the
configured workflow on 2026-09-23 UTC, preserving its original run identity and
pre-cancellation API records in `superseded-candidate-35798767360-1`. It must not be
promoted or described as accepted. This is explicit supersession for a reproduced
product-discovery defect, not a restart for reporting or a failed hosted observation.
Fresh corrected source and final-candidate acceptance remain required before the
selected v0.1.41 tag or publication authority can advance.

The workflow subsequently completed as **cancelled** at 2026-09-23T00:03:58Z,
not failed or accepted. Its diagnostic artifact `10725497846` expires
2026-10-07T00:03:40Z and terminal artifact `10725348104` expires
2026-10-07T00:03:55Z. Pre-cancellation and terminal API originals are retained;
both original downloadable artifacts are preserved under the same local
supersession directory. No accepted assets/verifier/acceptance handoff exists.
The source summary is empty; its terminal reports `status=incomplete` and
`candidate=cancelled`, with the terminal job correctly failing incomplete delivery.

The corrected immutable product is retained at
`.artifacts/composable-20260922/deployment-presence-product/lkjscript` (SHA-256
`4531dc10c97cb93e914cf1561731fe3f0998c7975d02bdba8a68c7610cfe6502`).
Through a copy outside the checkout, seven public commands pass: discovery marks
all four fields required and the two foreground policy objects optional, omission
of each topology field still rejects, and the previously authored official-v0.1.40
bundle still returns 850 and zero. Literal descriptors and original outputs remain
in the tutorial's `fixed-runtime` directory. No live adapter is used. Generated
documents come from this executable; only the four flags and generated capability
headers change. The updated capability digest requires refreshing cached discovery;
no accepted graph, package/artifact encoding or operational-data migration occurs.

### Corrected source acceptance and current candidate

Commit `346c0366bde29151952a19332cb540681ad7bc7c` passes all 20 fresh
release-source gates with stable inputs, zero reuse and no unrun gates. The source
receipt is `.artifacts/lkjscript-dev/check/1790122013811056220-2612728-0/receipt.json`,
digest `verification_e430d9330b3f1387eeaaa034b1d3f458976681f8d559bdb3c674e592f8ecf897`.
The complete original run is retained outside checker rotation at
`.artifacts/composable-20260922/deployment-presence-release-source-originals`.
Workspace proof includes 759 root tests, 62 public CLI tests and 149 developer
tests; pre-existing explicit ignored workloads stay ignored. Standard/application
checks and byte-for-byte generated artifact/transport comparisons pass. The 72
selected documentation links/fragments and exact tutorial literals also check.

After refreshing main and observing no incoming divergence, an unprotected branch
and empty effective rules/rulesets, normal fast-forward push delivered that exact
tested source. Git remote lookup and the independent branch API both agree. A new
occupancy check found no v0.1.41 tag/release or active release workflow. One candidate
was dispatched: [35801102943/1](https://github.com/lkjsxc/lkjscript/actions/runs/35801102943),
created 2026-09-23T00:13:32Z, exact product/controller source `346c0366`, official
repository/head repository, main, workflow_dispatch, `.github/workflows/release.yml`.
The immediate filtered API list was empty during dispatch propagation; a subsequent
read found the single matching in-progress run. No duplicate dispatch occurred.

Final-candidate acceptance, tag/scoped selection, promotion and anonymous public
verification remain due. After acceptance, the annotated tag must select exactly
`346c0366bde29151952a19332cb540681ad7bc7c`, not a later main/reporting descendant.
Retain actual handoff identities and observed expiry before selecting this exact
producer. The prepared installed participation smoke also checks corrected field
discovery; that extra read-only assertion has passed separately, while installed
execution remains unrun. Historical failed/cancelled producers retain their status.

### Maintained guide audit coverage

The new hand-authored guide lives in `docs/guides`, outside the product-surface
policy's previous generated/spec directory inventory. Add that directory to the
existing bounded scanner so future guides receive the same public-vocabulary
checks. This expands source verification coverage without changing its forbidden
terms, exceptions, executable discovery checks or product behavior. The exact
literal tutorial and descriptor already passed independent execution/text checks
before candidate selection; no known product defect invalidates that candidate.

The immutable rebuilt verifier passes `check changed --fresh --machine` against
the staged coverage/reporting changes: `checker_self_test`, `rust_only_tooling`
and `diff_check`, all three fresh with stable inputs and zero reuse. Receipt
`.artifacts/lkjscript-dev/check/1790122655622578968-2627573-0/receipt.json` is
retained with the complete original run at
`.artifacts/composable-20260922/native-guide-audit-originals`. The expanded public
surface audit passes 34 files and 26 discovery commands, no exceptions/violations.
Formatting and all 72 selected documentation links/fragments also pass. Final
report additions receive whitespace/no-Python checks. This descendant does not
reassign the 20-gate source acceptance or frozen candidate from `346c0366`.

The last candidate boundary observed is job `106991398177`, `Accept final candidate`,
building immutable host tools after checkout and pinned toolchain succeeded. Its
terminal is pending. After acceptance, use the release owner to admit the exact
handoffs and then complete the selected tag/promotion/public-verification boundary;
do not dispatch a duplicate healthy candidate or select a reporting descendant.

## Native command input files — next measured workload

Resumption confirms clean main/origin at `4d2c37b1`, preserved unrelated stash and
both map-evidence worktrees, pinned Rust 1.98.0 and no owned local jobs. Candidate
`35801102943/1` is still live at frozen v0.1.41 source `346c0366`; its existing
acceptance/publication work continues independently.

The native tutorial's exact shipping bundle supplies the next workload. With the
copied corrected executable outside the compiler checkout and an environment
containing only `LANG=C.UTF-8`, repeated parcel inputs of 2, 128, 1,024 and 4,096
items return independently expected `250*N`. One warm-up and three samples per
size retain whole-process, preparation, invocation and encoding observations.
At 8,192 items, the 196,611-byte inline JSON causes host `spawnSync` to report
`E2BIG` before product startup. This is an observed Linux argument-delivery
boundary, not an evaluator exhaustion or inferred throughput ceiling. The product's
existing JSON admission allows 1 MiB, but `run` currently has no file-input selector.
All inputs, outputs and samples remain at
`/home/coder/workspace/lkjscript-native-input-20260923`; no live effects are involved.

Select a general `--arguments-file PATH` alternative for project and standalone
command execution. It reads one bounded regular file relative to the invoking
directory, shares existing strict application JSON/type admission and limits,
and is mutually exclusive with inline `--arguments`. Omission remains `[]`.
Reuse the existing bounded no-follow input reader and preserve pre-effect input
admission, exact grants, joined cleanup, accepted HEAD and runtime semantics.
Prove large input through the fresh native library workload, both public routes,
malformed/oversized/nonregular input, option conflicts, exact-fit admission and
pre-effect failure. Extend the existing public CLI and candidate owners instead
of a new verifier. No JSON-limit increase or arbitrary file capability is selected.

This is a successor development change, not a defect invalidating the healthy
v0.1.41 candidate. Advance mutable main to unused development version 0.1.42 after
checking occupancy; retain the exact v0.1.41 source/verifier/assets for its selected
delivery. The controller binds publication to the accepted source and annotated
object, independently of the later main product version. Later publication of the
input-file milestone requires its own stabilized source and dependency-complete
candidate acceptance after the current shared publication identity is clear.

### Implementation and focused observations

Both command routes now select inline JSON, one bounded regular input file or the
existing empty array. The shared selector rejects duplicate/conflicting options
before I/O and reuses the maintained no-follow/nonblocking bounded reader. Runtime
JSON decoding, typed admission, grants, artifact encoding and application data do
not change. The 1 MiB input limit includes whitespace; unsupported stdin and final
symlinks do not acquire alternate meanings. Main's development version is 0.1.42;
the predecessor rejects the new selector through `cli_usage`.

Public CLI tests freshly author a native Text length command. Both routes return
200,000 for the large Text and 2 for an exactly 1 MiB whitespace-padded input.
Oversized, malformed/trailing, duplicate-key, invalid-UTF-8 and wrongly typed values
reject; invalid typed input wins over an absent deployment secret, while the valid
input reaches `secret_missing`. Standalone success joins cleanup after the authoring
project is moved. Two initial nonregular-test runs failed because the new test
used a source-error exit expectation for an absent file's infrastructure error;
the first proposed correction also confused the resource status. The test now uses
the existing literal source/infrastructure/resource statuses 2/6/4. Original failed
stdout/stderr remain retained; runtime classifications were not changed.

The release-configuration copied executable is SHA-256
`8da427c6c5c18ce5a55a20b1c2ce3ea4b1fea662608514d455b20080791c27a5`.
The external workspace's `fresh-42` run passes 19 public commands under only
`LANG=C.UTF-8`: it freshly authors the guide's literal generic library and Parcel
consumer, reviews both, checks graph tests, exports/stages the exact library and
computes `2048000` for 8,192 parcels through both routes. Input is 196,612 bytes
including a newline. Producer paths are moved before build and consumer paths
before standalone execution. Malformed JSON, wrong types and overflow reject;
HEAD is unchanged. The launcher substitutes only observed base/package identities
into literal declarations; it generates no semantic program.

The existing focused offline F64 owner passes 49 commands with its original reader
and complete cleanup. Project decimals, the artifact's unchanged 4,096-sample
workload and both invalid-before-effect cells use files. The scale JSON is padded
to 200,000 bytes without changing arithmetic inputs or expectations. Remaining
calls retain inline coverage. Numerical evidence schema 2 explicitly rejects old
coverage; frozen v0.1.41 continues with its own original verifier. This focused
host observation is not final-candidate acceptance. The eight generated guides are
regenerated by the copied product; no hand-written generated grammar is introduced.

The corrected nonregular and existing foreground-grammar tests both pass against
the copied release-configuration executable. The bounded original-reader fault
test also passes: a same-result file-to-inline substitution and rehashed removal
of the scale padding each reject for their intended reason; a restored private
copy and the untouched original pass again. The expanded product-surface policy
passes 34 files/26 executable commands with zero violations or exceptions, and
generated-guide verification passes. Detailed original logs and copied tools are
retained under `.artifacts/composable-20260922/argument-file-*`. Fresh dependency-
complete source acceptance follows after these inputs stabilize; no full/source
result or final-candidate acceptance is inferred from the focused observations.

### Input-file source acceptance and mainline

Exact source `b7f4a83dcd2f6b99403336125895aac183416c85` passes all 20 fresh
release-source gates, stable inputs, zero reuse and no unrun gates. The original
receipt is `.artifacts/lkjscript-dev/check/1790123953405884054-2636426-0/receipt.json`,
digest `verification_dfea1f623913dd4177016808bfa45722553318579cb7667afb466612356c02a8`;
the complete original run is also retained at
`.artifacts/composable-20260922/argument-file-release-source-originals`.
Elapsed time is 487.224 seconds. Root tests pass 759 with 8 explicitly ignored;
public CLI passes 64 with 1 ignored; structural expressions pass 12 with 1 ignored;
contributor tools pass 149 with 18 ignored. The newly ignored retained-original
fault test was separately executed and passed above. The data-admission, scan and
service suites also pass. No ignored test is relabelled as part of the ordinary suite.

After refreshing main and observing unchanged empty rules/rulesets and unprotected
main, normal fast-forward push delivered that exact source. Remote Git lookup and
the independent branch API both confirm `b7f4a83d`. Original v0.1.41 source
`346c0366` remains an ancestor and its live candidate remains unchanged. No tag,
scoped publication selection or new candidate was created for v0.1.42.

An additional pure boundary observation returns 4,096,000 for 16,384 parcels and
8,192,000 for 32,768 parcels (786,436 input bytes including newline). At 33,334
parcels, the unchanged JSON item admission rejects before execution. These are
correctness observations during concurrent source verification, not timing or
throughput comparisons. The authentic v0.1.40-authored shipping bundle also
returns 2,048,000 from the new file selector without rebuilding. These originals
remain beside the earlier workload; no application data or graph format migrates.

## Generated operation-table presentation

Review during the source run reproduced a separate guide presentation defect
through GitHub's Markdown renderer. Literal `|` in the generated Usage cells
becomes a table delimiter even within backticks: the `run` cell stops at
`run TARGET [--arguments JSON`, and other operation alternatives are likewise lost.
Original input/HTML are retained as `generated-operations-before-*` under the same
ignored evidence root. The source run and b7f4a83d integration finished without
changing their inputs before this follow-up begins.

Escape usage pipes in the existing product document generator and regenerate
through the copied executable. This changes generated Markdown presentation only;
operation discovery, CLI grammar, accepted meaning and runtime behavior retain
their existing contracts. Keep its focused document/render proof separate from
b7f4a83d's source-tier proof and the frozen v0.1.41 candidate. No release candidate
is restarted to reproduce this presentation observation.

The corrected generated table passes GitHub rendering for all 14 operations,
compared with the complete pre-change public capability projection. The original
has 7 truncated Usage cells; the corrected HTML has zero differences. Both
capability projections are byte-identical, including their digest. The existing
two generated-document tests, public generate/verify operations, default library/
binary production Clippy, formatting and expanded product-surface audit all pass.
Only `operations.md` changes among the eight generated outputs. Renderer request,
HTML, comparisons and check logs remain at `generated-operations-*`; the copied
executable is outside the checkout in the native input workspace's `guide-render`.
No additional broad suite is inferred or repeated for the presentation change.

## Repeated native editing — measurement selection

The next resumption confirms clean main and refreshed origin/main at
`3f94643cf9b1071a729ca3ab3b3d7f6160c77349`, no owned local jobs, the unchanged
unrelated stash/worktrees, pinned Rust 1.98.0, 11 GiB available memory and 822 GiB
free disk. The previous turn made mainline progress. The v0.1.41 producer remains
live at `35801102943/1`, job `106991398177`, source acceptance started at
2026-09-23T00:21:07Z. Its completion is external; no restart or new publication
selection follows from a running observation. Input-file implementation/source
acceptance and the separately checked guide rendering are complete on main.

Select the roadmap's unmeasured repeated-edit workflow for a bounded observation.
Use the retained optimized b7f4a83d executable; 3f94643c changes only Markdown
rendering, which is outside these timed operations. Fresh native declarations
define one command and graph test. Alternating literal value/expected-result edits
return the program to value zero at each measurement boundary, preserving its
declaration identities and active graph shape. Measure the same public status,
check, run, draft and reviewed edit operations as accepted history grows. Keep
literal requests, plans, outputs, failed attempts and environment facts. The host
driver may substitute observed identities and scalar edit values, but must not
generate semantic graph structures or write accepted storage. No compiler, cache,
history retention or collection redesign is selected before these observations.

Both bounded arms completed 635 public commands and 256 accepted edits. Literal
requests, plans, observations, environment and all outputs are retained outside
the checkout at `/home/coder/workspace/lkjscript-history-evidence-20260923`; the
rename control is its `rename/` child. Three launcher setup failures remain in
separate `attempt-1`, `attempt-2` and `attempt-3` directories: incorrect apply-result
field, parser metadata colliding with the owner's `kind`, and an existing draft
output path. These are not product failures or completed timing evidence.
[Performance](../performance.md#repeated-native-editing-2026-09-23) records the
results and limits. Check/run rises to roughly 54 ms after body edits and 24–26 ms
after renames, with nine active owners and equal result zero. Status reports no
semantic revalidation. The body arm has 768 retirements; renames have none and
retain the original semantic-state identity. Accepted storage and history remain
intact. The evidence does not select history removal or weaker admission.

Following the read path found an independently visible copying cost:
`PackDirectoryStore::validated_pack_entry` clones all `PackMetadata.entries` on
every cache lookup, then clones the whole index again when returning an entry.
Both callers discard that returned entry. Select immutable shared metadata in the
existing request-local cache, move newly sealed metadata into it, and return only
the shared metadata after the same descriptor/entry validation. Catalog lookup,
admission, per-read file length, payload checksum and typed digest checks retain
their order and meaning. No serialized format, graph identity, retention, quota or
public grammar changes. This is a bounded storage improvement, not a claim that
the history cost is solved.

All 23 focused storage tests pass, including strengthened corruption proof after
warming both freshly sealed and reopened metadata caches. Corrupted payloads and
truncated files still fail with their independent expected diagnostics. Existing
exact admission, hostile pack/catalog, catalog recovery and all publication
interruption checkpoints also pass. Matched public-workload measurement and fresh
dependency-complete source acceptance remain due for this implementation.

The frozen v0.1.41 producer `35801102943/1` has completed source
acceptance (00:54:39Z) and final distributable construction (01:01:52Z). Its exact
candidate admission is running. This progress neither changes its source
`346c0366` nor licenses publication before the remaining gates pass.

The immutable storage implementation is source
`caea6309139a64ec004ad5b9f3fd77df5fb6c8ea`. Its product/dev release build uses the
same pinned toolchain, locked dependencies, two-package configuration, LTO and one
codegen unit as the retained b7f4a83d baseline. Product `67699f6a…` and verifier
`a281c766…` are retained in `history-storage-release-tools`; the timed product is
also copied outside the checkout. No owned build/test overlaps the 112 paired
commands (28 excluded warmups, 84 measured samples, 14 cells). Every accepted HEAD,
fixed result, artifact identity and native draft agrees between binaries. The
standard package's 37 graph tests agree as well. All observed current-validation
rows still report zero semantic work. The [paired table](../performance.md#shared-pack-index-comparison)
includes unfavorable cases. Removing copies is established by the implementation;
a general speedup, lower live memory and a solution to history preparation cost
are not established by these small samples.

Fresh source acceptance at exact caea6309 passes all 20 gates in 282.582 seconds,
with stable input identity, no reuse and no unrun gate. The source receipt is
`.artifacts/lkjscript-dev/check/1790125930646643001-2665293-0/receipt.json`, digest
`verification_7df82900e194f3aa1bda5287e5317e6b3f539771fd797f44d319c72de2c55ed8`.
Its complete originals are also retained under
`.artifacts/composable-20260922/history-storage-release-source-originals`. This
includes root 759 passed / 8 ignored, public CLI 64 / 1, structural public 12 / 1,
dev 149 / 18 and data/scan/service 10/7/9 passes, plus maintained assets, consumers,
production lint, no-Python, public-surface and lifecycle owners. Ignored tests do
not become performed evidence merely because their source suite passes.

Fresh remote/protection inspection finds main at 3f94643c, no effective rules or
rulesets, and exactly one local intended commit ahead. Normal push and fetch then
confirm remote main exactly caea6309, retaining b7f4a83d and the frozen 346c0366
product source as ancestors. Subsequent narrative updates keep this tested source
and receipt identity; no report-hash/retest cycle is introduced.

At 2026-09-23T01:19:21Z v0.1.41 candidate `35801102943/1` is still healthy in final
candidate admission, started 01:01:52Z. The run/job API originals are retained as
`candidate-deployment-presence-*-after-storage.json`. No tag/control update or
promotion has occurred. An independent bounded file-I/O observation is prepared
with locally extracted Ubuntu strace 6.8, checked against the configured package
metadata. It traces only owned pure projects, retains literal native creation and
every command, and makes no elapsed-time comparison with the earlier measurements.

That observation completed all 12 commands, including 9 traced commands, with
successful joined exits and unchanged accepted HEAD. Warm exact-current `run`
opens catalog manifests 13 times and HEAD files 22 times in each of the three
nine-owner cases. Total read bytes and pack-file opens grow from the fresh case
through the two retained histories; the [performance owner](../performance.md#shared-pack-index-comparison)
records exact counts and limits. The first fresh check builds derived cache and
is not treated as a matched warm call. All originals and the extracted diagnostic
tool remain in the external evidence root's `tracer/` child. The next consequential
finding is repeated repository opening during preparation; sharing a view would
need exact revision, derived-cache and execution-authority analysis before any
implementation change.

## One preparation authority view — selected continuation

After normal integration of the reporting descendant `d1297499`, select the
smallest independently justified repeated-open correction. Lifecycle preparation
currently reads `repository.current()` and then opens another revision-pinned
view to export the exact source closure. Retain the existing accepted binding and
package identity from that export view instead, then drop the view before later
compilation. This removes one redundant opening and binds authority and closure
to the same observation. Keep all closure admission, derived-cache validation,
final preparation recheck, and check/run authority rechecks. The other measured
openings are not automatically removable, especially when derived cache pointers
can change independently of accepted source.

Add a real publication regression: two accepted renames restore the original
name and semantic state but change revision. A previously prepared check and run must
both reject that stale authority; a fresh preparation must still check and return
the independently expected command value. Measure syscall counts with the same
retained projects after implementation. A reduced call count alone will not be
reported as a general elapsed-time improvement.

The first focused run passed the two existing lifecycle tests and failed the new
test's oracle: one module rename also changes semantic-state identity. This is a
fixture expectation error, not a weakened publication or execution check. Retain
the original source in `preparation-view-first-test.rs` and the first build/run
originals under the ignored campaign evidence root. Correct the witness to perform
two ordinary accepted renames back to the original name, matching the earlier
history experiment's even-boundary identity comparison. Keep the final same-state,
different-revision and stale check/run requirements intact.

The corrected regression passes: both stale operations reject with
`lifecycle_authority_changed`, accepted HEAD stays intact, and fresh preparation
checks and returns `"hello"` with equal differential results. The two existing
lifecycle recovery/maintained-artifact tests already passed on the unchanged
production implementation. Formatting and diff checks pass after the fixture
formatting correction. Exact release build, retained public syscall comparison
and fresh source acceptance remain due for this successor to d1297499.

Source `a1c462b521bdeab7c00797a0ebf329f1b3af57d2` builds successfully with the pinned
two-package release configuration. Product `dd8e130f…` and verifier `289c6721…`
are retained in `preparation-view-release-tools`; the product is also copied into
the external history evidence workspace. Twelve traced public commands compare
that executable with retained caea6309 on the same three accepted projects. All
complete stdout, fixed results, artifact identities and HEAD bytes match. Every
check/run pair reduces catalog openings from 13 to 12 and HEAD openings from 22 to
20. The [performance owner](../performance.md#one-source-view-during-preparation)
records actual read counts/bytes and pack opens, without an elapsed-time claim.

Fresh source acceptance passes all 20 gates in 283.023 seconds, with stable inputs
and zero reuse at exact a1c462b5. Its receipt is
`.artifacts/lkjscript-dev/check/1790127565379596159-2693932-0/receipt.json`, digest
`verification_d6270e1f94d1a392dd4d79214a7a1ef1e1171252060a161afdc028879cc601dc`.
Complete originals are copied to
`.artifacts/composable-20260922/preparation-view-release-source-originals`. Root
tests now pass 760 with 8 ignored; the public CLI, structural, dev and data/scan/
service counts retain their previous passing totals. The whole fresh source owner,
including lint, generated/maintained assets, public surface and lifecycle, passes.

Fresh remote/protection inspection finds d1297499 on main, no effective rules or
rulesets and only this intended local commit ahead. Normal push/fetch verifies
remote main exactly a1c462b5, retaining the earlier measured/accepted sources and
the frozen 346c0366 producer as ancestors. Later reporting does not rebind its proof.

The retained traces expose a further bounded investigation: in the candidate's
256-body-edit run, 1,453 reads repeat an exact byte range on the same open catalog
segment handle, accounting for 9,458,665 returned bytes. The parser resolves every
read handle; its range inventory remains at `preparation-view/segment-ranges.json`.
`SegmentMetadata::read_block` currently rechecks and decodes the complete bounded
block for each lookup. Consider bounded request-local reuse only after auditing
segment/context binding, cache retention during catalog replacement, hostile
block admission, exact resource checks and observed work semantics. Current
objects must still pass their own payload checks. No block-cache implementation,
history removal or format transition has been made in this result.

At 2026-09-23T01:51:08Z producer `35801102943/1`, job `106991398177`, still runs
the final-candidate/original-evidence admission step that started 01:01:52Z. Its
source remains 346c0366 and its artifact list is still empty. Preserve this owned
run; no duplicate candidate, v0.1.41 tag/control update or promotion is dispatched.
The API originals are `candidate-deployment-presence-*-after-view-integration.json`.
The current public release remains v0.1.40, and successor v0.1.42 candidate/delivery
remains deferred by the recorded shared-publication trigger.

## Bounded catalog block reuse — selected continuation

Startup on 2026-09-23 finds clean local/remote main at `677aa750`, no divergence
or owned local jobs, the same unrelated stash and two retained map worktrees,
Rust 1.98.0, about 10 GiB available memory and 822 GiB free disk. Existing root
guidance remains applicable; no replacement or narrower guidance is required.
The earlier implementation/source acceptance is complete. Repeated block reads
are actionable. v0.1.41 delivery remains a separate selected obligation; its
original producer has now completed successfully and publication is being resumed.
The failed and cancelled predecessors remain superseded, with originals preserved.

Select a bounded in-memory cache of at most 64 fully admitted catalog blocks per
open store. Bind entries to that store's immutable segment identity and block
index, and clear them when installing a replacement catalog and handle set.
The existing decoder still validates every entry, checksum, range, filter, pack
membership and ordering before retaining a block. Exhausted optional cache space
falls back to ordinary reads; it is not a semantic or object-admission limit.
Every lookup still searches all candidate segments for duplicates and charges
its request allowance. Every object read still checks actual pack length, payload
checksum and typed content identity. Work counters must describe actual block
reads/decoding, including uncached fallback, without inventing work on cache hits.

Prove cold corruption rejection, failed-block nonretention, correct reads beyond
the cache bound, replacement/reopen behavior, and unchanged warmed object-read
admission and corruption checks. Compare the same retained pure public projects
through copied executables, retaining complete results and unfavorable observations.
No persistent encoding, history deletion or general speedup claim is selected.

The bounded implementation passes all 26 storage tests, including the three new
independent boundary witnesses and existing request-budget, warmed-payload/length
corruption, duplicate-object and all seal/recovery interruption cases. The 65-block
fixture successfully reads beyond retention, charges all 67 object/lookups and
records 66 physical block reads; its first retained block still avoids a reread.
A corrupted unrequested entry rejects before serving the requested entry, and
restoring the owned fixture proves the failed block was not retained. Replacement
and fresh-open views admit the new block, while an older pinned view retains only
its original objects. Originals are `catalog-block-tests-build.*`, the copied
Cargo-reported test executable, and `catalog-block-focused.*` under campaign storage.

The [release evidence owner](../evidence/202609221952-cells/README.md#accepted-candidate-and-publication-selection)
records accepted producer `35801102943/1`, authenticated handoffs/expiry, the
read-only launcher buffer failure, exact annotated tag/scoped selection and
promotion `35809096928/1` from 677aa750. Source and target acceptance were not
replayed for this transfer. Public delivery and installed participation remain due.

Focused production Clippy initially rejected a newly collapsible nested condition.
The equivalent let-chain preserves the duplicate-location check and passes Clippy
with warnings denied; both originals remain in `catalog-block-clippy*`. Formatting,
diff checks and 56 selected local documentation links/anchors pass. Fresh complete
source acceptance and the copied-executable comparison remain due for this source.
