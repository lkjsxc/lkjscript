# Autonomous improvement continuation

## Original user mandate

限界が来れば自動的にあなた自身が停止されるようになっているので、限界まで、自律的に次にやることを探して、より良くするループを続けてください。

## Startup reconciliation — 2026-09-22 UTC

The mutable checkout is clean on main at
`4f34fe421b131e0c03653142aa67899ca2226597`, matching freshly fetched origin/main.
The supplied repository guidance is identical to the existing `AGENTS.md`; no
guidance edit is needed. The unrelated preservation stash and two detached map
evidence worktrees remain intact. No active local build or product process was
observed. Rust 1.98.0 is pinned; approximately 11 GiB available memory and 824 GiB
free disk were observed. Main is unprotected and effective branch rules/rulesets
are empty; refresh these before integration.

The [composable-cell mandate](202609221952.md) remains the responsible lineage.
Its implementation, focused proof and local source acceptance are complete;
selected immutable delivery remains actionable. Hosted candidate `35725872480/1`
at `b52f51bd334dc71462e46c298dea12454f59ed6f` actually failed on
2026-09-22T13:51:55Z, superseding the predecessor's last running observation.
The final-candidate admission step and terminal failed; essential accepted
handoffs were not uploaded. Diagnostics artifact `10697836925` is available
until 2026-10-06T13:51:37Z. Diagnose the original failure before choosing repair
or resumption. Do not promote an unaccepted candidate or duplicate a healthy job.

v0.1.40 publication and public verification are complete and remain unchanged.
Native tooling/self-hosting and contingent roadmap features are not selected
merely because they exist in the roadmap. Continue with demonstrated defects or
concrete public workloads after the selected release boundary is reconciled.
Append execution findings at the existing campaign and evidence owners rather
than maintaining a second detailed ledger here.

## Documentation follow-up

After the verified repair reached main and replacement candidate `35798767360/1`
started, an independent review found stale current-product claims: the root README
still recommended v0.1.38 and its source, omitted complete native declarations and
canonical drafting, and described supported predecessor readers as absent. Current
status and roadmap mixed delivered work with old pending/unreleased observations.

The README now names published v0.1.40 and gives a literal native square-function,
test and command example with canonical re-entry. A fresh copied official v0.1.40
executable outside the compiler checkout accepted that exact declaration body,
passed 39 graph tests with evaluator agreement, returned the independent expected
144, and planned its generated module draft unchanged. All product invocations
used a cleared environment with no PATH or compiler/toolchain access. Literal
request, plan, draft, bundle and command outputs are retained under
`/home/coder/workspace/lkjscript-native-docs-20260922`. This is tutorial verification,
not renewed release acceptance or native contributor-tool implementation. The
first copy command selected the extracted directory instead of its executable and
failed before product execution; the empty initial output remains preserved.

Status is reduced from 1,153 to 171 lines and roadmap from 218 to 107 lines, with
current capability boundaries, actual pending delivery and links to maintained
history. The original summaries remain unchanged in Git at tested source
`c78840a74ce19ad5145e1e23ca5ff2b2a7198c0f`; campaign originals and genuine failures
are untouched. No old failed observation becomes a pass. A local documentation
check resolves all 65 selected relative links/fragments and confirms the README
native body is identical to the executed literal except its user-supplied base.
These documentation descendants do not change or reaccept the candidate source.

With intended documentation paths staged, `check changed --fresh --machine` selected
and passed only `diff_check` and `rust_only_tooling`, zero reuse; receipt
`.artifacts/lkjscript-dev/check/1790120803841196854-2604963-0/receipt.json`.
The maintained product-surface audit also passes all 33 files and 26 executable
discovery commands with zero violations/exceptions against immutable Product 02.
The first surface invocation used official v0.1.40 with the v0.1.41 verifier; its
27 protocol-version mismatches are preserved in `documentation-surface.stdout.json`.
The owner intentionally binds the current product version. Correcting the selected
audit binary, without changing its assertions, produced
`documentation-surface-current.stdout.json`. Tutorial execution remains separately
bound to actual public v0.1.40. No product/source/target suite was repeated for these
documentation changes. Final reporting additions receive whitespace/no-Python checks.

## Diagnostic retention follow-up

A bounded audit of maintained artifact pins found no additional hard-coded product
artifact pin outside the corrected service owner. The analogous opt-in `Verify`
workflow did omit that owner's separate `.artifacts/lkjscript-dev/service`
directory. Its `full` profile launches service acceptance, but the gate declares no
retained outputs: only the summary logs live under the uploaded check directory.
The workflow now uploads the service originals on success or failure, matching the
release repair. The development-verification guide states that distinction. No
verifier, gate selection, publication authority or candidate source changes.
Local validation confirms the workflow differs only by that one literal upload
path and covers both retained genuine service success/failure originals.
Whitespace and the maintained no-Python policy pass. This does not claim a hosted
upload observation; no new full run or duplicate release candidate was dispatched
for this additive diagnostics-only change.

Separately, the copied Product 02 executable generated and replanned unchanged
native drafts for four exact maintained modules: standard core/data and lkjournal
worker/service. Literal drafts and observations remain outside the checkout at
`/home/coder/workspace/lkjscript-maintained-drafts-20260922`. This bounded selection
does not claim an exhaustive inventory, fresh authoring, new maintained adoption,
performance, or candidate acceptance. Both copied accepted HEAD files stayed equal
to their maintained originals; no apply or live application execution occurred.

## Native library guide and discovered deployment defect — 2026-09-23 UTC

The new [native-library guide](../guides/native-library.md) exercises a complete
literal generic `sum-by<Item>` library and a separately authored shipping command
whose nominal `Parcel` belongs to the consumer. A copied official v0.1.40 executable
outside the compiler checkout passes 40 library and 42 consumer graph tests with
evaluator equality. Fixed inputs produce 850 and zero; multiplication and summation
overflow reject. An unchanged canonical draft replans without publication. Export,
staging, reviewed exact binding, build after moving the producer, and standalone
execution after moving the consumer all pass. Original projects are retained under
new names, not deleted. Native literals and the complete deployment descriptor in
the guide match the executed inputs after substituting only observed identities.

Evidence remains under `/home/coder/workspace/lkjscript-library-guide-20260923`,
using the same official v0.1.40 bytes as the earlier README observation. Product
commands use only `LANG=C.UTF-8` in their environment. A host assertion checks text
equality and independently fixed results; it does not generate semantic input or
implement the library. This is tutorial verification, not maintained application
adoption, contributor-tool migration, or new release acceptance.

Initial exploratory commands used an invalid owner-kind filter and capability
selector; their genuine diagnostics remain. Initial descriptor attempts used an
object for grants or omitted required fields; each rejected before invocation.
The corrected complete descriptor runs successfully. Those authoring mistakes
are not relabelled as product failures. The last omission, however, exposed a
real public-discovery contradiction: `listen`, `http`, `session` and `worker` were
advertised as `required=false`, while the specification and strict decoder require
their presence, using null for foreground commands. This reproduces in official
v0.1.40 and remains in the selected v0.1.41 candidate source.

The bounded repair changes those four executable-owned schema flags to true and
regenerates their public guides. It preserves decoder behavior, descriptor bytes,
runtime policy, artifacts and application data. A regression removes each top-level
field from a valid foreground descriptor and independently compares strict decoding
with discovery. It fails against the original flags and passes after correction;
all seven deployment unit tests pass. Original failed/passed test logs and immutable
Cargo-reported test executables are retained under `.artifacts/composable-20260922`.

Candidate `35798767360/1` at `c78840a7` was still running source acceptance when this
invalidating product defect was established. Cancellation was requested through the
configured workflow on 2026-09-23 UTC, preserving its original run identity and
pre-cancellation API records in `superseded-candidate-35798767360-1`. It must not be
promoted or described as accepted. This is explicit supersession for a reproduced
product-discovery defect, not a restart for reporting or a failed hosted observation.
Fresh corrected source and final-candidate acceptance remain required before the
selected v0.1.41 tag or publication authority can advance.

The workflow subsequently completed as **cancelled** at 2026-09-23T00:03:58Z,
not failed or accepted. Its diagnostic artifact `10725497846` expires
2026-10-07T00:03:40Z and terminal artifact `10725348104` expires
2026-10-07T00:03:55Z. Pre-cancellation and terminal API originals are retained;
both original downloadable artifacts are preserved under the same local
supersession directory. No accepted assets/verifier/acceptance handoff exists.
The source summary is empty; its terminal reports `status=incomplete` and
`candidate=cancelled`, with the terminal job correctly failing incomplete delivery.

The corrected immutable product is retained at
`.artifacts/composable-20260922/deployment-presence-product/lkjscript` (SHA-256
`4531dc10c97cb93e914cf1561731fe3f0998c7975d02bdba8a68c7610cfe6502`).
Through a copy outside the checkout, seven public commands pass: discovery marks
all four fields required and the two foreground policy objects optional, omission
of each topology field still rejects, and the previously authored official-v0.1.40
bundle still returns 850 and zero. Literal descriptors and original outputs remain
in the tutorial's `fixed-runtime` directory. No live adapter is used. Generated
documents come from this executable; only the four flags and generated capability
headers change. The updated capability digest requires refreshing cached discovery;
no accepted graph, package/artifact encoding or operational-data migration occurs.

### Corrected source acceptance and current candidate

Commit `346c0366bde29151952a19332cb540681ad7bc7c` passes all 20 fresh
release-source gates with stable inputs, zero reuse and no unrun gates. The source
receipt is `.artifacts/lkjscript-dev/check/1790122013811056220-2612728-0/receipt.json`,
digest `verification_e430d9330b3f1387eeaaa034b1d3f458976681f8d559bdb3c674e592f8ecf897`.
The complete original run is retained outside checker rotation at
`.artifacts/composable-20260922/deployment-presence-release-source-originals`.
Workspace proof includes 759 root tests, 62 public CLI tests and 149 developer
tests; pre-existing explicit ignored workloads stay ignored. Standard/application
checks and byte-for-byte generated artifact/transport comparisons pass. The 72
selected documentation links/fragments and exact tutorial literals also check.

After refreshing main and observing no incoming divergence, an unprotected branch
and empty effective rules/rulesets, normal fast-forward push delivered that exact
tested source. Git remote lookup and the independent branch API both agree. A new
occupancy check found no v0.1.41 tag/release or active release workflow. One candidate
was dispatched: [35801102943/1](https://github.com/lkjsxc/lkjscript/actions/runs/35801102943),
created 2026-09-23T00:13:32Z, exact product/controller source `346c0366`, official
repository/head repository, main, workflow_dispatch, `.github/workflows/release.yml`.
The immediate filtered API list was empty during dispatch propagation; a subsequent
read found the single matching in-progress run. No duplicate dispatch occurred.

Final-candidate acceptance, tag/scoped selection, promotion and anonymous public
verification remain due. After acceptance, the annotated tag must select exactly
`346c0366bde29151952a19332cb540681ad7bc7c`, not a later main/reporting descendant.
Retain actual handoff identities and observed expiry before selecting this exact
producer. The prepared installed participation smoke also checks corrected field
discovery; that extra read-only assertion has passed separately, while installed
execution remains unrun. Historical failed/cancelled producers retain their status.

### Maintained guide audit coverage

The new hand-authored guide lives in `docs/guides`, outside the product-surface
policy's previous generated/spec directory inventory. Add that directory to the
existing bounded scanner so future guides receive the same public-vocabulary
checks. This expands source verification coverage without changing its forbidden
terms, exceptions, executable discovery checks or product behavior. The exact
literal tutorial and descriptor already passed independent execution/text checks
before candidate selection; no known product defect invalidates that candidate.

The immutable rebuilt verifier passes `check changed --fresh --machine` against
the staged coverage/reporting changes: `checker_self_test`, `rust_only_tooling`
and `diff_check`, all three fresh with stable inputs and zero reuse. Receipt
`.artifacts/lkjscript-dev/check/1790122655622578968-2627573-0/receipt.json` is
retained with the complete original run at
`.artifacts/composable-20260922/native-guide-audit-originals`. The expanded public
surface audit passes 34 files and 26 discovery commands, no exceptions/violations.
Formatting and all 72 selected documentation links/fragments also pass. Final
report additions receive whitespace/no-Python checks. This descendant does not
reassign the 20-gate source acceptance or frozen candidate from `346c0366`.

The last candidate boundary observed is job `106991398177`, `Accept final candidate`,
building immutable host tools after checkout and pinned toolchain succeeded. Its
terminal is pending. After acceptance, use the release owner to admit the exact
handoffs and then complete the selected tag/promotion/public-verification boundary;
do not dispatch a duplicate healthy candidate or select a reporting descendant.

## Native command input files — next measured workload

Resumption confirms clean main/origin at `4d2c37b1`, preserved unrelated stash and
both map-evidence worktrees, pinned Rust 1.98.0 and no owned local jobs. Candidate
`35801102943/1` is still live at frozen v0.1.41 source `346c0366`; its existing
acceptance/publication work continues independently.

The native tutorial's exact shipping bundle supplies the next workload. With the
copied corrected executable outside the compiler checkout and an environment
containing only `LANG=C.UTF-8`, repeated parcel inputs of 2, 128, 1,024 and 4,096
items return independently expected `250*N`. One warm-up and three samples per
size retain whole-process, preparation, invocation and encoding observations.
At 8,192 items, the 196,611-byte inline JSON causes host `spawnSync` to report
`E2BIG` before product startup. This is an observed Linux argument-delivery
boundary, not an evaluator exhaustion or inferred throughput ceiling. The product's
existing JSON admission allows 1 MiB, but `run` currently has no file-input selector.
All inputs, outputs and samples remain at
`/home/coder/workspace/lkjscript-native-input-20260923`; no live effects are involved.

Select a general `--arguments-file PATH` alternative for project and standalone
command execution. It reads one bounded regular file relative to the invoking
directory, shares existing strict application JSON/type admission and limits,
and is mutually exclusive with inline `--arguments`. Omission remains `[]`.
Reuse the existing bounded no-follow input reader and preserve pre-effect input
admission, exact grants, joined cleanup, accepted HEAD and runtime semantics.
Prove large input through the fresh native library workload, both public routes,
malformed/oversized/nonregular input, option conflicts, exact-fit admission and
pre-effect failure. Extend the existing public CLI and candidate owners instead
of a new verifier. No JSON-limit increase or arbitrary file capability is selected.

This is a successor development change, not a defect invalidating the healthy
v0.1.41 candidate. Advance mutable main to unused development version 0.1.42 after
checking occupancy; retain the exact v0.1.41 source/verifier/assets for its selected
delivery. The controller binds publication to the accepted source and annotated
object, independently of the later main product version. Later publication of the
input-file milestone requires its own stabilized source and dependency-complete
candidate acceptance after the current shared publication identity is clear.

### Implementation and focused observations

Both command routes now select inline JSON, one bounded regular input file or the
existing empty array. The shared selector rejects duplicate/conflicting options
before I/O and reuses the maintained no-follow/nonblocking bounded reader. Runtime
JSON decoding, typed admission, grants, artifact encoding and application data do
not change. The 1 MiB input limit includes whitespace; unsupported stdin and final
symlinks do not acquire alternate meanings. Main's development version is 0.1.42;
the predecessor rejects the new selector through `cli_usage`.

Public CLI tests freshly author a native Text length command. Both routes return
200,000 for the large Text and 2 for an exactly 1 MiB whitespace-padded input.
Oversized, malformed/trailing, duplicate-key, invalid-UTF-8 and wrongly typed values
reject; invalid typed input wins over an absent deployment secret, while the valid
input reaches `secret_missing`. Standalone success joins cleanup after the authoring
project is moved. Two initial nonregular-test runs failed because the new test
used a source-error exit expectation for an absent file's infrastructure error;
the first proposed correction also confused the resource status. The test now uses
the existing literal source/infrastructure/resource statuses 2/6/4. Original failed
stdout/stderr remain retained; runtime classifications were not changed.

The release-configuration copied executable is SHA-256
`8da427c6c5c18ce5a55a20b1c2ce3ea4b1fea662608514d455b20080791c27a5`.
The external workspace's `fresh-42` run passes 19 public commands under only
`LANG=C.UTF-8`: it freshly authors the guide's literal generic library and Parcel
consumer, reviews both, checks graph tests, exports/stages the exact library and
computes `2048000` for 8,192 parcels through both routes. Input is 196,612 bytes
including a newline. Producer paths are moved before build and consumer paths
before standalone execution. Malformed JSON, wrong types and overflow reject;
HEAD is unchanged. The launcher substitutes only observed base/package identities
into literal declarations; it generates no semantic program.

The existing focused offline F64 owner passes 49 commands with its original reader
and complete cleanup. Project decimals, the artifact's unchanged 4,096-sample
workload and both invalid-before-effect cells use files. The scale JSON is padded
to 200,000 bytes without changing arithmetic inputs or expectations. Remaining
calls retain inline coverage. Numerical evidence schema 2 explicitly rejects old
coverage; frozen v0.1.41 continues with its own original verifier. This focused
host observation is not final-candidate acceptance. The eight generated guides are
regenerated by the copied product; no hand-written generated grammar is introduced.

The corrected nonregular and existing foreground-grammar tests both pass against
the copied release-configuration executable. The bounded original-reader fault
test also passes: a same-result file-to-inline substitution and rehashed removal
of the scale padding each reject for their intended reason; a restored private
copy and the untouched original pass again. The expanded product-surface policy
passes 34 files/26 executable commands with zero violations or exceptions, and
generated-guide verification passes. Detailed original logs and copied tools are
retained under `.artifacts/composable-20260922/argument-file-*`. Fresh dependency-
complete source acceptance follows after these inputs stabilize; no full/source
result or final-candidate acceptance is inferred from the focused observations.

### Input-file source acceptance and mainline

Exact source `b7f4a83dcd2f6b99403336125895aac183416c85` passes all 20 fresh
release-source gates, stable inputs, zero reuse and no unrun gates. The original
receipt is `.artifacts/lkjscript-dev/check/1790123953405884054-2636426-0/receipt.json`,
digest `verification_dfea1f623913dd4177016808bfa45722553318579cb7667afb466612356c02a8`;
the complete original run is also retained at
`.artifacts/composable-20260922/argument-file-release-source-originals`.
Elapsed time is 487.224 seconds. Root tests pass 759 with 8 explicitly ignored;
public CLI passes 64 with 1 ignored; structural expressions pass 12 with 1 ignored;
contributor tools pass 149 with 18 ignored. The newly ignored retained-original
fault test was separately executed and passed above. The data-admission, scan and
service suites also pass. No ignored test is relabelled as part of the ordinary suite.

After refreshing main and observing unchanged empty rules/rulesets and unprotected
main, normal fast-forward push delivered that exact source. Remote Git lookup and
the independent branch API both confirm `b7f4a83d`. Original v0.1.41 source
`346c0366` remains an ancestor and its live candidate remains unchanged. No tag,
scoped publication selection or new candidate was created for v0.1.42.

An additional pure boundary observation returns 4,096,000 for 16,384 parcels and
8,192,000 for 32,768 parcels (786,436 input bytes including newline). At 33,334
parcels, the unchanged JSON item admission rejects before execution. These are
correctness observations during concurrent source verification, not timing or
throughput comparisons. The authentic v0.1.40-authored shipping bundle also
returns 2,048,000 from the new file selector without rebuilding. These originals
remain beside the earlier workload; no application data or graph format migrates.

## Generated operation-table presentation

Review during the source run reproduced a separate guide presentation defect
through GitHub's Markdown renderer. Literal `|` in the generated Usage cells
becomes a table delimiter even within backticks: the `run` cell stops at
`run TARGET [--arguments JSON`, and other operation alternatives are likewise lost.
Original input/HTML are retained as `generated-operations-before-*` under the same
ignored evidence root. The source run and b7f4a83d integration finished without
changing their inputs before this follow-up begins.

Escape usage pipes in the existing product document generator and regenerate
through the copied executable. This changes generated Markdown presentation only;
operation discovery, CLI grammar, accepted meaning and runtime behavior retain
their existing contracts. Keep its focused document/render proof separate from
b7f4a83d's source-tier proof and the frozen v0.1.41 candidate. No release candidate
is restarted to reproduce this presentation observation.

The corrected generated table passes GitHub rendering for all 14 operations,
compared with the complete pre-change public capability projection. The original
has 7 truncated Usage cells; the corrected HTML has zero differences. Both
capability projections are byte-identical, including their digest. The existing
two generated-document tests, public generate/verify operations, default library/
binary production Clippy, formatting and expanded product-surface audit all pass.
Only `operations.md` changes among the eight generated outputs. Renderer request,
HTML, comparisons and check logs remain at `generated-operations-*`; the copied
executable is outside the checkout in the native input workspace's `guide-render`.
No additional broad suite is inferred or repeated for the presentation change.

## Repeated native editing — measurement selection

The next resumption confirms clean main and refreshed origin/main at
`3f94643cf9b1071a729ca3ab3b3d7f6160c77349`, no owned local jobs, the unchanged
unrelated stash/worktrees, pinned Rust 1.98.0, 11 GiB available memory and 822 GiB
free disk. The previous turn made mainline progress. The v0.1.41 producer remains
live at `35801102943/1`, job `106991398177`, source acceptance started at
2026-09-23T00:21:07Z. Its completion is external; no restart or new publication
selection follows from a running observation. Input-file implementation/source
acceptance and the separately checked guide rendering are complete on main.

Select the roadmap's unmeasured repeated-edit workflow for a bounded observation.
Use the retained optimized b7f4a83d executable; 3f94643c changes only Markdown
rendering, which is outside these timed operations. Fresh native declarations
define one command and graph test. Alternating literal value/expected-result edits
return the program to value zero at each measurement boundary, preserving its
declaration identities and active graph shape. Measure the same public status,
check, run, draft and reviewed edit operations as accepted history grows. Keep
literal requests, plans, outputs, failed attempts and environment facts. The host
driver may substitute observed identities and scalar edit values, but must not
generate semantic graph structures or write accepted storage. No compiler, cache,
history retention or collection redesign is selected before these observations.

Both bounded arms completed 635 public commands and 256 accepted edits. Literal
requests, plans, observations, environment and all outputs are retained outside
the checkout at `/home/coder/workspace/lkjscript-history-evidence-20260923`; the
rename control is its `rename/` child. Three launcher setup failures remain in
separate `attempt-1`, `attempt-2` and `attempt-3` directories: incorrect apply-result
field, parser metadata colliding with the owner's `kind`, and an existing draft
output path. These are not product failures or completed timing evidence.
[Performance](../performance.md#repeated-native-editing-2026-09-23) records the
results and limits. Check/run rises to roughly 54 ms after body edits and 24–26 ms
after renames, with nine active owners and equal result zero. Status reports no
semantic revalidation. The body arm has 768 retirements; renames have none and
retain the original semantic-state identity. Accepted storage and history remain
intact. The evidence does not select history removal or weaker admission.

Following the read path found an independently visible copying cost:
`PackDirectoryStore::validated_pack_entry` clones all `PackMetadata.entries` on
every cache lookup, then clones the whole index again when returning an entry.
Both callers discard that returned entry. Select immutable shared metadata in the
existing request-local cache, move newly sealed metadata into it, and return only
the shared metadata after the same descriptor/entry validation. Catalog lookup,
admission, per-read file length, payload checksum and typed digest checks retain
their order and meaning. No serialized format, graph identity, retention, quota or
public grammar changes. This is a bounded storage improvement, not a claim that
the history cost is solved.

All 23 focused storage tests pass, including strengthened corruption proof after
warming both freshly sealed and reopened metadata caches. Corrupted payloads and
truncated files still fail with their independent expected diagnostics. Existing
exact admission, hostile pack/catalog, catalog recovery and all publication
interruption checkpoints also pass. Matched public-workload measurement and fresh
dependency-complete source acceptance remain due for this implementation.

The frozen v0.1.41 producer `35801102943/1` has completed source
acceptance (00:54:39Z) and final distributable construction (01:01:52Z). Its exact
candidate admission is running. This progress neither changes its source
`346c0366` nor licenses publication before the remaining gates pass.

The immutable storage implementation is source
`caea6309139a64ec004ad5b9f3fd77df5fb6c8ea`. Its product/dev release build uses the
same pinned toolchain, locked dependencies, two-package configuration, LTO and one
codegen unit as the retained b7f4a83d baseline. Product `67699f6a…` and verifier
`a281c766…` are retained in `history-storage-release-tools`; the timed product is
also copied outside the checkout. No owned build/test overlaps the 112 paired
commands (28 excluded warmups, 84 measured samples, 14 cells). Every accepted HEAD,
fixed result, artifact identity and native draft agrees between binaries. The
standard package's 37 graph tests agree as well. All observed current-validation
rows still report zero semantic work. The [paired table](../performance.md#shared-pack-index-comparison)
includes unfavorable cases. Removing copies is established by the implementation;
a general speedup, lower live memory and a solution to history preparation cost
are not established by these small samples.

Fresh source acceptance at exact caea6309 passes all 20 gates in 282.582 seconds,
with stable input identity, no reuse and no unrun gate. The source receipt is
`.artifacts/lkjscript-dev/check/1790125930646643001-2665293-0/receipt.json`, digest
`verification_7df82900e194f3aa1bda5287e5317e6b3f539771fd797f44d319c72de2c55ed8`.
Its complete originals are also retained under
`.artifacts/composable-20260922/history-storage-release-source-originals`. This
includes root 759 passed / 8 ignored, public CLI 64 / 1, structural public 12 / 1,
dev 149 / 18 and data/scan/service 10/7/9 passes, plus maintained assets, consumers,
production lint, no-Python, public-surface and lifecycle owners. Ignored tests do
not become performed evidence merely because their source suite passes.

Fresh remote/protection inspection finds main at 3f94643c, no effective rules or
rulesets, and exactly one local intended commit ahead. Normal push and fetch then
confirm remote main exactly caea6309, retaining b7f4a83d and the frozen 346c0366
product source as ancestors. Subsequent narrative updates keep this tested source
and receipt identity; no report-hash/retest cycle is introduced.

At 2026-09-23T01:19:21Z v0.1.41 candidate `35801102943/1` is still healthy in final
candidate admission, started 01:01:52Z. The run/job API originals are retained as
`candidate-deployment-presence-*-after-storage.json`. No tag/control update or
promotion has occurred. An independent bounded file-I/O observation is prepared
with locally extracted Ubuntu strace 6.8, checked against the configured package
metadata. It traces only owned pure projects, retains literal native creation and
every command, and makes no elapsed-time comparison with the earlier measurements.

That observation completed all 12 commands, including 9 traced commands, with
successful joined exits and unchanged accepted HEAD. Warm exact-current `run`
opens catalog manifests 13 times and HEAD files 22 times in each of the three
nine-owner cases. Total read bytes and pack-file opens grow from the fresh case
through the two retained histories; the [performance owner](../performance.md#shared-pack-index-comparison)
records exact counts and limits. The first fresh check builds derived cache and
is not treated as a matched warm call. All originals and the extracted diagnostic
tool remain in the external evidence root's `tracer/` child. The next consequential
finding is repeated repository opening during preparation; sharing a view would
need exact revision, derived-cache and execution-authority analysis before any
implementation change.

## One preparation authority view — selected continuation

After normal integration of the reporting descendant `d1297499`, select the
smallest independently justified repeated-open correction. Lifecycle preparation
currently reads `repository.current()` and then opens another revision-pinned
view to export the exact source closure. Retain the existing accepted binding and
package identity from that export view instead, then drop the view before later
compilation. This removes one redundant opening and binds authority and closure
to the same observation. Keep all closure admission, derived-cache validation,
final preparation recheck, and check/run authority rechecks. The other measured
openings are not automatically removable, especially when derived cache pointers
can change independently of accepted source.

Add a real publication regression: two accepted renames restore the original
name and semantic state but change revision. A previously prepared check and run must
both reject that stale authority; a fresh preparation must still check and return
the independently expected command value. Measure syscall counts with the same
retained projects after implementation. A reduced call count alone will not be
reported as a general elapsed-time improvement.

The first focused run passed the two existing lifecycle tests and failed the new
test's oracle: one module rename also changes semantic-state identity. This is a
fixture expectation error, not a weakened publication or execution check. Retain
the original source in `preparation-view-first-test.rs` and the first build/run
originals under the ignored campaign evidence root. Correct the witness to perform
two ordinary accepted renames back to the original name, matching the earlier
history experiment's even-boundary identity comparison. Keep the final same-state,
different-revision and stale check/run requirements intact.

The corrected regression passes: both stale operations reject with
`lifecycle_authority_changed`, accepted HEAD stays intact, and fresh preparation
checks and returns `"hello"` with equal differential results. The two existing
lifecycle recovery/maintained-artifact tests already passed on the unchanged
production implementation. Formatting and diff checks pass after the fixture
formatting correction. Exact release build, retained public syscall comparison
and fresh source acceptance remain due for this successor to d1297499.

Source `a1c462b521bdeab7c00797a0ebf329f1b3af57d2` builds successfully with the pinned
two-package release configuration. Product `dd8e130f…` and verifier `289c6721…`
are retained in `preparation-view-release-tools`; the product is also copied into
the external history evidence workspace. Twelve traced public commands compare
that executable with retained caea6309 on the same three accepted projects. All
complete stdout, fixed results, artifact identities and HEAD bytes match. Every
check/run pair reduces catalog openings from 13 to 12 and HEAD openings from 22 to
20. The [performance owner](../performance.md#one-source-view-during-preparation)
records actual read counts/bytes and pack opens, without an elapsed-time claim.

Fresh source acceptance passes all 20 gates in 283.023 seconds, with stable inputs
and zero reuse at exact a1c462b5. Its receipt is
`.artifacts/lkjscript-dev/check/1790127565379596159-2693932-0/receipt.json`, digest
`verification_d6270e1f94d1a392dd4d79214a7a1ef1e1171252060a161afdc028879cc601dc`.
Complete originals are copied to
`.artifacts/composable-20260922/preparation-view-release-source-originals`. Root
tests now pass 760 with 8 ignored; the public CLI, structural, dev and data/scan/
service counts retain their previous passing totals. The whole fresh source owner,
including lint, generated/maintained assets, public surface and lifecycle, passes.

Fresh remote/protection inspection finds d1297499 on main, no effective rules or
rulesets and only this intended local commit ahead. Normal push/fetch verifies
remote main exactly a1c462b5, retaining the earlier measured/accepted sources and
the frozen 346c0366 producer as ancestors. Later reporting does not rebind its proof.

The retained traces expose a further bounded investigation: in the candidate's
256-body-edit run, 1,453 reads repeat an exact byte range on the same open catalog
segment handle, accounting for 9,458,665 returned bytes. The parser resolves every
read handle; its range inventory remains at `preparation-view/segment-ranges.json`.
`SegmentMetadata::read_block` currently rechecks and decodes the complete bounded
block for each lookup. Consider bounded request-local reuse only after auditing
segment/context binding, cache retention during catalog replacement, hostile
block admission, exact resource checks and observed work semantics. Current
objects must still pass their own payload checks. No block-cache implementation,
history removal or format transition has been made in this result.

At 2026-09-23T01:51:08Z producer `35801102943/1`, job `106991398177`, still runs
the final-candidate/original-evidence admission step that started 01:01:52Z. Its
source remains 346c0366 and its artifact list is still empty. Preserve this owned
run; no duplicate candidate, v0.1.41 tag/control update or promotion is dispatched.
The API originals are `candidate-deployment-presence-*-after-view-integration.json`.
The current public release remains v0.1.40, and successor v0.1.42 candidate/delivery
remains deferred by the recorded shared-publication trigger.

## Bounded catalog block reuse — selected continuation

Startup on 2026-09-23 finds clean local/remote main at `677aa750`, no divergence
or owned local jobs, the same unrelated stash and two retained map worktrees,
Rust 1.98.0, about 10 GiB available memory and 822 GiB free disk. Existing root
guidance remains applicable; no replacement or narrower guidance is required.
The earlier implementation/source acceptance is complete. Repeated block reads
are actionable. v0.1.41 delivery remains a separate selected obligation; its
original producer has now completed successfully and publication is being resumed.
The failed and cancelled predecessors remain superseded, with originals preserved.

Select a bounded in-memory cache of at most 64 fully admitted catalog blocks per
open store. Bind entries to that store's immutable segment identity and block
index, and clear them when installing a replacement catalog and handle set.
The existing decoder still validates every entry, checksum, range, filter, pack
membership and ordering before retaining a block. Exhausted optional cache space
falls back to ordinary reads; it is not a semantic or object-admission limit.
Every lookup still searches all candidate segments for duplicates and charges
its request allowance. Every object read still checks actual pack length, payload
checksum and typed content identity. Work counters must describe actual block
reads/decoding, including uncached fallback, without inventing work on cache hits.

Prove cold corruption rejection, failed-block nonretention, correct reads beyond
the cache bound, replacement/reopen behavior, and unchanged warmed object-read
admission and corruption checks. Compare the same retained pure public projects
through copied executables, retaining complete results and unfavorable observations.
No persistent encoding, history deletion or general speedup claim is selected.

The bounded implementation passes all 26 storage tests, including the three new
independent boundary witnesses and existing request-budget, warmed-payload/length
corruption, duplicate-object and all seal/recovery interruption cases. The 65-block
fixture successfully reads beyond retention, charges all 67 object/lookups and
records 66 physical block reads; its first retained block still avoids a reread.
A corrupted unrequested entry rejects before serving the requested entry, and
restoring the owned fixture proves the failed block was not retained. Replacement
and fresh-open views admit the new block, while an older pinned view retains only
its original objects. Originals are `catalog-block-tests-build.*`, the copied
Cargo-reported test executable, and `catalog-block-focused.*` under campaign storage.

The [release evidence owner](../evidence/202609221952-cells/README.md#accepted-candidate-and-publication-selection)
records accepted producer `35801102943/1`, authenticated handoffs/expiry, the
read-only launcher buffer failure, exact annotated tag/scoped selection and
promotion `35809096928/1` from 677aa750. Source and target acceptance were not
replayed for this transfer. Public delivery and installed participation remain due.

Focused production Clippy initially rejected a newly collapsible nested condition.
The equivalent let-chain preserves the duplicate-location check and passes Clippy
with warnings denied; both originals remain in `catalog-block-clippy*`. Formatting,
diff checks and 56 selected local documentation links/anchors pass. Fresh complete
source acceptance and the copied-executable comparison remain due for this source.

Source `8e9628ad3c8e71f886cd64aa750e032b20104bfa` builds with the same pinned
two-package release configuration. Product `bfc17d83…` and verifier `223730a1…`
are preserved in `catalog-block-release-tools`. Fresh source acceptance passes
all 20 gates in 258.590 seconds, with stable inputs, zero reuse and no skipped
gate. The receipt is
`.artifacts/lkjscript-dev/check/1790129990031276902-2729403-0/receipt.json`, digest
`verification_2b690eb1c330ca1067e1ccd3665dd18a2c18ef4932864ce87a96b8008879a4a1`.
Complete originals are copied to `catalog-block-release-source-originals`. Root
tests pass 763 with 8 ignored; public CLI 64/1, structural CLI 12/1, developer
149/18, and data/scan/service 10/7/9 retain their passing totals. Required lint,
no-Python, product-surface, generated/maintained assets and lifecycle gates pass.

Six fresh public authoring/execution commands, 112 timed comparison commands and
twelve separately traced check/run commands all pass outside the checkout. The
[performance owner](../performance.md#bounded-catalog-block-reuse) records every
cell, including slower body-draft generation. Standard checking changes from
1,750.575 to 1,498.009 ms at the median; the 256-body-edit run reads 7,997,703 bytes
versus 14,556,344, while all 1,935 pack opens remain. The cache removes bounded
repeated catalog work, not object integrity checks. Originals remain in the history
evidence root's `catalog-block/` child, including both immutable executables and
literal native input. No build, test, download or tracer overlaps timed calls.

Fresh fetch/protection reads find remote 677aa750, no effective rules/rulesets and
only the intended implementation ahead. Normal push/fetch and the independent
branch API verify remote main exactly 8e9628ad. The tested source is an exact
ancestor of later reporting; no proof is relabelled with a descendant SHA.

## v0.1.41 public completion and successor candidate

The [existing evidence owner](../evidence/202609221952-cells/README.md#immutable-publication-and-installed-participation)
records immutable publication and anonymous verification of original producer
35801102943/1 by consumer 35809096928/1. Terminal completion is
2026-09-23T02:16:09Z; release 394244926 publishes unchanged v0.1.41 at source
346c0366. Its separate thirteen-command installed participant witness also passes
with `Committed(60)`, reopened value 60, early missing-owner rejection and unchanged
data HEAD. The owned installation/evidence root is
`/home/coder/workspace/lkjscript-installed-cells-20260923`. All work is joined;
the prefix, native inputs, bundle, originals and application state are retained.
The failed/cancelled predecessors retain their genuine status. v0.1.41 has no
remaining implementation or selected-delivery obligation.

This fires the recorded v0.1.42 candidate deferral. After observing unused tag and
release occupancy, no active workflow and remote main exactly 8e9628ad, dispatch
one non-publishing candidate: [35810382909/1](https://github.com/lkjsxc/lkjscript/actions/runs/35810382909),
created 2026-09-23T02:26:50Z, product/controller 8e9628ad, official repository/head
repository, main, workflow_dispatch and `.github/workflows/release.yml`. Preflight,
unchanged scoped selection, dispatch and run originals are retained in
`candidate-v0.1.42` under campaign artifact storage. No v0.1.42 tag or scoped
selection change has been made; that control still selects completed v0.1.41.
Meaningful next-version notes are prepared, including argument files, Markdown
rendering and bounded storage improvements. Final-candidate acceptance remains due;
later publication uses only explicitly authorized release-scoped operations and
fresh identity/occupancy admission. Do not restart the healthy producer for later
reporting or unrelated development.

The reporting descendant refreshes public README installation pins, the existing
release/evidence owners, status/roadmap and the measured comparison. Its 105 local
links/anchors and staged diff check pass. These reporting changes receive their
own review; they do not relabel or repeat the predecessor's broad source acceptance.

## Native keyed counting — next public workload

Reporting descendant `c59d92ad` reached remote main unchanged. The v0.1.42
candidate remains at frozen 8e9628ad; at 2026-09-23T02:34:24Z its candidate job
`107020419020` has entered source acceptance, started 02:34:21Z. The earlier
publication control still selects completed v0.1.41. No owned local job remains.

Select an ordinary text-key frequency counter composed from the public standard
list fold, map lookup/insertion and integer addition. Public discovery exposes
those operations already, so begin with native authoring rather than a new
compiler feature. Check fixed empty, repeated, reordered and Unicode-key inputs,
then compare independently expected counts as the input grows through the public
JSON-file path. Keep the literal native program, exact inputs, command outputs
and accepted bindings outside the checkout at
`/home/coder/workspace/lkjscript-grouping-evidence-20260923`. Host code may launch
commands and create operational test data; it must not synthesize the program or
edit accepted storage. A successful witness is not itself maintained adoption,
a language extension or a throughput guarantee. Select any implementation change
only after a concrete observed obstacle, preserving the healthy frozen candidate.

The native counter is authored successfully and all 42 graph tests pass (the
four new fixed cases, starter test and exact standard closure). Its detached
bundle returns the independently expected small result through official installed
v0.1.41. Through 8e9628ad's JSON-file input, every completed case is correct,
including 65,536 items over 16 keys and an uneven 4,101-item case. The former
invocation reports 514.863 ms and 6,671 type-derivation steps; these are single
observations, not throughput/RSS or cumulative-budget claims.

At 4,096 distinct text keys, execution reaches result rendering and rejects with
`control_render_record_bytes`: the compact output record exceeds its documented
65,536-byte bound. The launcher stops and preserves the genuine failed exit and
all originals; 16,384/32,768-distinct-key cases are unrun. This is an existing
output-capacity boundary; it does not demonstrate incorrect counting or a newly
introduced candidate defect. No result value was returned for the failed case.
Investigate a bounded explicit result-file path for project/foreground
commands, with exact output ownership, pre-effect path admission, atomic exposure,
unchanged typed encoding and honest post-effect failures. Keep any new facility
separate from the healthy frozen v0.1.42 candidate and its input-file scope.

The [performance owner](../performance.md#native-text-frequencies-2026-09-23)
records the successful single observations and the output failure. The existing
`src/platform/owned_output.rs` owns create-new atomic publication and truthful
durability/cleanup observations. `src/platform/cli.rs` owns both command paths;
the current typed encoder already bounds complete result JSON at 1 MiB before
compact rendering. A new file path should reuse those owners and preserve the
default compact result, exact JSON bytes and post-effect failure reporting. Its
preparation and concurrent output-conflict behavior still require implementation
and tests; no result-file flag or product change has been implemented for it.

At 2026-09-23T02:48:16Z candidate `35810382909/1`, job `107020419020`, remains
healthy in source acceptance, started 02:34:21Z. Its exact source is still 8e9628ad.
The original API snapshots are `candidate-v0.1.42/*-after-counting-boundary.json`.
No duplicate/replacement run or release-scoped selection change is made. The
grouping launcher and all children have joined; its native project, bundle,
inputs and complete successful/failed observations remain owned evidence. The
definitions of the unrun larger cases remain in the launcher for continuation.

## Result-file implementation selection

Resumption at clean `99672641` reconciles main/origin, both detached evidence
worktrees and the unrelated preservation stash. No owned local job remains;
10 GiB memory and 821 GiB disk are available under pinned Rust 1.98.0. Candidate
35810382909/1 remains healthy in its source acceptance at 2026-09-23T02:56:38Z;
its exact frozen source is 8e9628ad. Public v0.1.41 is complete; candidate v0.1.42
acceptance remains external work. The observed compact-result boundary is the
selected actionable improvement, not a reason to restart that producer.

Implement optional `--result-file PATH` for both project and foreground Commands
on development v0.1.43. Reuse the owned create-new publisher and exact existing
bounded typed JSON. Inspect an absent destination and ordinary parent before
context/secret/adapter reads, without creating or reserving a stage; publication
rechecks after execution, typed encoding and joined cleanup. This cannot promise
future absence, write permission or disk capacity. A competing file is preserved,
and post-effect failure never implies rollback or safe retry. Default compact
output is retained; explicit file output substitutes an `execution.result-file`
path for `value` and appends the existing output publication fields. No graph,
artifact, application-data or typed-codec format transition is selected. Required
verification includes exact byte preservation, the 1 MiB boundary, preflight and
late conflict, failures without partial publication, copied detached use of the
actual counting workload and meaningful current acceptance. Implementation and
verification are in progress; this entry is not a claim of completed capability.

### Focused result-file implementation and use

The CLI and owned output publisher implement the selected contract. No extra
semantic/evaluation quota or JSON limit is introduced. The output owner now
uses an independent bounded stage name so a valid 255-byte destination component
is not extended beyond its filesystem bound; failure removes only a stage it
created and retains cleanup diagnostics. Foreground rendering/publication errors
include the already completed cleanup observation and possible-effect visibility.
A closed stdout after publication explicitly preserves the result file.

All five focused output-owner tests and three focused CLI/API tests pass. These
include exact 1 MiB JSON, overflow and nonfinite rejection, Unicode, negative zero,
adjacent F64, both routes, absent authoring context and pre-secret rejection. At the
public parse/execute API split, a deterministic competing file appears after
inspection; the task commits 42, publication rejects, the competitor is preserved,
and a copied executable reads 42 afterward. This is source/API boundary proof,
not an installed-candidate invocation. The later added closed-stdout assertion
remains due in final source acceptance. The initial Clippy compile failed on
cleanup-note error-type inference; the explicit Diagnostic annotation corrects it,
and the subsequent workspace/all-target Clippy check passes. An initial test-build
command repeated `--lib` and was rejected before compilation; the corrected
command succeeds. All failed originals remain under campaign artifact storage.

Locked two-package release build completes in 3m46s. Immutable tools under
`result-file-release-tools` are product 24,822,952 bytes, SHA-256
`bd876cdf2ff3e2f2a542ac606729d8812374290a1dc79d9ef35a39c30df7970c`, and verifier
35,934,568 bytes, SHA-256
`b12fa384c3819e9d0443a6f6f2ecbb0653e436ff8ed919ea44aa23f94367a9d4`.
Their original build identity records base 99672641 with the implementation
worktree, not a later acceptance SHA. The product regenerates all eight owned
public documents through `capabilities --generate-docs`.

The fresh copied native observation at
`/home/coder/workspace/lkjscript-result-file-evidence-20260923` completes all
13 commands, including 42 passing graph tests. The original native counter
request remains literal, with only its observed revision substituted. The
4,096-key project result agrees between evaluators and writes exactly 65,537
bytes. With its authoring project moved away, 4,096/16,384/32,768 distinct-key
artifact runs write 65,537/262,145/524,289 exact independently expected bytes.
Default compact output still fails at its existing bound; a competing existing
file is unchanged. The prior 8e9628ad-authored bundle also returns the same map
through the new runtime. Literal requests, inputs, complete outputs, artifact,
execution identities and cleanup observations remain outside the checkout.
Single elapsed observations are at the existing performance owner, without a
speedup or throughput claim.

The numerical owner retains all 49 original commands and arithmetic expectations,
but selects result files for project decimals, artifact scalar observations and a
completed typed-data write. It requests an output for the post-commit nonfinite
failure and observes its absence plus the still-committed value afterward. Its
fresh focused receipt is `result-file-f64/receipt.json`, digest
`verification_ee600ee59e55792bc4206c6153cd514d0cd306ba217497f2330f3c6a42f4a8b6`,
with cleanup complete and child schema 3. This expanded obligation rejects schema
2; frozen candidate v0.1.42 retains its original source and verifier. No artifact,
graph or data format changes, release-scoped control updates or publication occur.

The explicitly invoked original-reader fault test also passes: rehashed input
padding/selector substitution, removal of the result-file selector, and a same-size
rehash of an incorrect arithmetic result are all rejected. Healthy relocated and
original receipts still pass. The test uses its exact immutable verifier and
retained focused receipt; it does not rerun the live data applications. Its complete
stdout/stderr are `result-file-f64-original-faults.*`. Local documentation link and
anchor checks and the intended diff check pass before source stabilization.

### Result-file source acceptance and mainline delivery

Implementation source `78b32e78f7ee14d17d47823d101b4ab109a7988e` passes all
20 fresh release-source gates in 269.144600467 seconds with zero reuse and stable
inputs. Receipt `.artifacts/lkjscript-dev/check/1790133923674334034-2786026-0/receipt.json`
has digest `verification_0936ad22e1de374333c3b3b991baecbb26124be5ce9718e09643220dc8ed5e3e`;
initial/final input digest is
`verification_91456ed63e921fd0f47af5cbe273987f7aade6c75cda2362c6898110244f78df`.
Root tests pass 765 with eight ignored; public CLI passes 67 with one ignored,
including the newly added closed-stdout assertion. Structural 12/1, dev 149/18,
data 10, scan 7 and service 9 also pass; required production lint, no-Python,
product-surface, generated assets, maintained libraries/application and default/
all-feature distinctions remain intact. The separately invoked ignored F64
original-reader mutation test is genuinely run, not inferred from this suite.

Fresh remote-main/protection admission observes no divergence, no protection and
empty effective rules/rulesets. Normal push/fetch and independent branch API
observation confirm remote main exactly 78b32e78, with origin divergence 0/0.
All owned local tests, builds, launchers and downloads join. Reporting descendants
must not restamp these proofs. The supplemental link checker initially omitted
Unicode punctuation from its anchor normalization; after correcting that harness,
77 local links/anchors pass. No repository link was changed to conceal that error.

### v0.1.42 accepted predecessor and retained publication inputs

At 2026-09-23T03:26:16Z, producer `35810382909/1` and terminal are complete/success.
Candidate job `107020419020` completes source acceptance at 02:58:23Z, final build
at 03:05:33Z and final-candidate/original-reader acceptance at 03:18:46Z; terminal
job `107031032677` reports `candidate_accepted`. Product and controller remain
8e9628ad. The accepted contract binds 20 source gates, six final-target owners,
two pinned userlands, installation/recovery, cleanup complete and no failure.

All five original artifact ZIPs are authenticated against independent repository,
workflow, run/attempt, source, mandatory jobs, service IDs, byte lengths and ZIP
SHA-256 metadata. They remain under `candidate-v0.1.42/accepted` with raw APIs,
terminal and original acceptance JSON. Asset ZIP `10729969040` expires
2026-10-07T03:18:46Z; verifier `10729998999` at :47; acceptance `10730163479` at
:48; diagnostics `10730673469` at :50; terminal `10729919574` at 03:19:15Z.
The three extracted distributables are byte-checked against the accepted receipt:
archive 11,376,314 bytes SHA-256
`81be257ac11b397c21ec79eb5d026667691c9ad977aceec7fc0252d60ef69ec2`,
SHA256SUMS 109 bytes `4a80b3de40e3a83a633f0f2c8477c85bcf74f55b9e78a2f6e20bbe342db7652b`,
and installer 3,566 bytes `0a03bfd48f7e8acf3a814da06e66664a0b179f8a421545c80365b7b59516392c`.
The accepted static executable is 24,943,136 bytes,
`c4cf17eb66b5415e4ccfe3186fdd3d4dbb6d9c5b367c889922c9ba528f469c69`;
original verifier 35,909,000 bytes,
`1a45591078f214715d0c7825608f982b737090028d11487cc8fd3ba748fe1c60`.
No transferred executable runs with publishing credentials.

Publication preflight finds immutability enabled, no v0.1.42 tag/release, no active
release workflow and the completed v0.1.41 release/control unchanged. Meaningful
notes are prepared. The maintained release procedure explicitly requires separate
scoped tag/control authorization, so that permission was requested asynchronously.
The subsequent finding below withdraws that publication request before any tag,
control mutation or promotion dispatch; do not treat its pending UI question as a
current instruction to publish the now withheld predecessor.

## Typed JSON framing — newly observed output boundary

After mainline delivery, continue the exact native counter at adjacent item bounds
using the same immutable bd876cdf executable. This is a new boundary probe, not a
reinterpretation of earlier 32,768-key observations. Originals are in
`/home/coder/workspace/lkjscript-result-file-evidence-20260923/json-boundary` and
`json-boundary.cjs/.stdout/.stderr`; all four child commands join.

At 33,333 distinct keys, the emitted 533,329-byte JSON contains 99,999 collection
children and passes the strict application JSON reader (then fails at the deliberately
absent descriptor with `deployment_read`). At 33,334, output still succeeds and
publishes 533,345 correct-content bytes, but those bytes contain 100,002 JSON children
and the same public reader rejects them with `json_decode`, item count exceeded.
Independent arithmetic is three JSON children per pair: one outer entry-array member
and its two members. The encoder charges only two per map entry. Source inspection
also finds map key/value depth advanced by one instead of the two levels represented
by entry arrays. Byte-object and nominal-variant synthetic JSON fields require the
same representation-bound review; no claim of those further cases being tested yet.

This is an existing shared encoder defect exposed by larger result files. Withhold
v0.1.42 publication and the pending scoped permission request; retain its successful
producer/evidence without changing its terminal or inventing a failure. The current
v0.1.43 source proof establishes its recorded checks, not absence of this newly found
edge. Select a bounded codec correction: count actual JSON array members/object
fields and nesting, preserve successful canonical bytes, use independent small
shape oracles plus the exact public adjacent-limit workload, and renew dependency-
complete source and final-target proof for corrected delivery. No tag, release,
selection variable, credentials or protections have changed. Mainline remains
78b32e78 until this reporting descendant; JSON repair has not been implemented yet.

### JSON framing correction and focused evidence

Continue on clean main 775b0d07967ca7a13e74d819028b246274aeeaea, preserving
all prior sources, immutable executables, stashes, detached evidence worktrees and
v0.1.42 accepted artifacts. No owned job was active at resumption; Rust remains
1.98.0, with about 10 GiB available memory and 819 GiB disk before compilation.
No new publication authority is inferred from the withdrawn v0.1.42 UI question.

The shared encoder now charges three actual JSON items per Map entry with checked
arithmetic, advances key/value depth by two and checks the entry-array container.
Bytes objects account for the generated string's depth and `$bytes` key length.
Structural/nominal keys and variant `case`/`value` keys and labels obey the existing
string-byte bound. Value/type admission, complete directional closure traversal,
JSON input grammar/numeric interpretation and binary-data codecs are unchanged.
The pinned serde_json 1.0.151 reader independently admits 127 array/object
containers and rejects 128 despite JsonLimits' depth 128 default. Output now
respects that existing guard; the reader's recursion check stays enabled.
Discovery adds the actual container/string bounds and item-counting definition to
its existing foreground-limits record. This is a current representation boundary,
not a permanent language rule or cumulative execution quota.

New fixed literal/one-less oracles check primitive/nested Maps, byte keys/values,
Unicode text, structural/nominal keys and variants with and without payloads
through both exact schemas. The unchanged strict byte reader independently
accepts exact minima and rejects smaller bounds. Original-code regressions
reproduce Map item undercounting and successful output at the rejected 128-container
shape. The nominal fixture initially had an invalid Unicode identifier and then
unreachable fixture types; these are retained fixture failures, not encoder
regressions. A valid consuming declaration repairs the fixture without bypassing
accepted-graph admission. The first compile also identified a nonexistent module
accessor. All originals remain under `json-framing-red-*`.

Focused immutable tests selected from Cargo's reported executables pass: 10 Map
codec tests, five binary64 codec tests, two recursive tests and the nominal/structural
representation test. The joined 2 MiB worker retains typed-data depth 128, shared
values and independent bytes while JSON succeeds through 63 nested Maps and
rejects 64. The public native counter test passes in 19.42 seconds, including
both routes at 33,333 keys (533,329 exact bytes, every count one) and 33,334 keys
(rejection, absent result, no stage debris and unchanged accepted HEAD).
Artifacts are `json-framing-focused-build.*` and `json-framing-focused-tools/`.
The focused product is 5375b038cf454558cc152c4f4dea77565c59c398f8ecdfc31581cfc4f6ddb7cc;
it precedes the per-Map depth-check hoist and added discovery fields. Subsequent
source acceptance must cover those final changes instead of relabelling this binary.

The first release build after that hoist fails on the internal Map's absent
`is_empty` method. It is corrected to the existing length API; the failed build
original remains `json-framing-release-build.*` and the renewed build has a
separate `-corrected` identity. No failed output is executed or promoted. Fresh
outside-checkout native authoring and dependency-complete acceptance remain in
progress at this entry; no v0.1.43 candidate has been dispatched.

### Fresh corrected native authoring outside the checkout

The corrected locked release build completes in 3m 50s. Immutable executables are
retained under `json-framing-release-tools/`: product 24,829,608 bytes SHA-256
`33fdd6165a4e87a051bc4b70bb9d94a089793de66250d979eb2fc365ee632f10`, dev
35,938,728 bytes `bc8b72d49fd4d0803da48ebd4ba1ed1c55b4ff722fb58a23b096bec6ca414b10`.
Build identity honestly records base HEAD 775b0d07 and the implementation worktree.
All eight maintained guides regenerate through the copied product; discovery digest
is ec0859c15fc00073e1a79596b197ceae68ab0586c2f970fe0f2a786d398ccbdb.
The complete producer JSON/stderr and generated records are retained separately.

`/home/coder/workspace/lkjscript-json-framing-evidence-20260923` contains a fresh
copied product, unchanged literal `counts-template.lkjc`, reviewed concrete request,
plan, project/bundle, argument files, results and all argv/stdout/stderr/exits.
The launch harness substitutes only the observed base revision and supplies ordinary
input data. Native create/plan/apply/check pass all 42 graph tests differentially;
no external semantic generator authors the program. All 14 public commands join.

Both project and detached runs return every independently expected key/count at
33,333 entries, exactly 533,329 bytes, then the public strict reader admits those
bytes before its deliberately absent deployment fails with `deployment_read`.
At 33,334 entries both routes reject `normalized_json_type` before creating a file;
detached cleanup reports zero owned tasks/failures. The new artifact is
artifact_bundle_4408515d16c75a3644f54ee5f517efa72353802495a1f0bfefcbc9239b610ccb,
revision rev_d5f5cda2b1b8a23fae6ace3576588724b13cd57aacadd8b278089ceb87ddea76.
After the authoring project moves away, the original 8e9628ad-authored bundle also
returns the same boundary map unchanged. Original 33,334-entry output from bd876cdf
remains byte-identical and is still rejected by the unchanged strict reader.
Accepted HEADs do not move during execution; no stage remains. Raw timings are
retained as single correctness observations, without a performance comparison.

The harness's newline literals were inspected before execution: they already had
one correct escape, so a proposed double-escape edit rejected its own assumption
and wrote nothing. That operational check is not a product failure. Source and
published bytes still need their own dependency-complete acceptance. There are no
owned local jobs at this stabilization boundary; v0.1.41 remains the public product.

### Source-gate failure, correction and expanded publication authority

Committed implementation 8fbff0af55c02d9f1eeca9122043f4ac8442e6bd receives a
fresh release-source failure: 18 passed, Clippy failed and its dependent workspace
suite skipped, zero reuse, stable inputs, 222.448939395 seconds. Original receipt
`.artifacts/lkjscript-dev/check/1790136480657014070-2826309-0/receipt.json` has
digest verification_c3b83fe09ca93cba11b45881477284d2a8de2abe6c89a70cb9fdbdc595b0b246.
This is not source acceptance or a passing workspace suite. The internal Map's
`is_empty` predicate existed only under `cfg(test)`: production needed `len != 0`,
while all-target Clippy rejected that spelling because its test build saw the
predicate. Make the existing predicate available to production and call it from
the encoder. No check is suppressed and no semantics, limit or representation
changes. The exact required Clippy command now passes in 19.78 seconds; its separate
originals are `json-framing-clippy-corrected.*`. Renew all 20 source gates for the
corrected descendant rather than relabelling the failed source.

The user then responds to the earlier publication prompt: “許可します。これに限らず、
なんでも許可するものとします”. This explicitly expands the authorization to continuing
release operations. The assistant records that v0.1.42 remains withheld for its
known defect and will deliver corrected v0.1.43 after acceptance. Under the existing
release procedure, the grant covers its annotated tag creation/push, exact scoped
selection update, unchanged accepted-asset promotion and anonymous acquisition
verification. No further confirmation is needed while that instruction stands.
The grant does not turn failed or incomplete proof into acceptance; no tag, scoped
control or publication has changed at this entry.

While source gates run, fresh native counting is composed with ordinary map-entries
and list-fold-left to return only a four-field Summary. Literal source, 12 public
operations and all originals remain in
`/home/coder/workspace/lkjscript-summary-evidence-20260923`, using immutable 33fdd616.
All 42 native graph tests pass, including empty, repeated, tie and exact Unicode
keys. Project execution at 8,192 keys agrees differentially. Detached trusted runs
produce independently expected complete summaries for 33,334, 65,536 and 99,999
unique keys; 99,999 rows over 16 keys yield frequency 6,250 for k00000. At 100,000
input rows, the outer argument member makes 100,001 JSON items and the unchanged
strict reader rejects before invocation. The internal Map need not itself be
encoded as JSON. Every child joins, success cleanup is empty and accepted HEAD
remains unchanged. Timings overlap source work and support no performance claim.
This designed native workflow is separate from maintained library adoption.

### Corrected JSON source acceptance and mainline delivery

Corrected source adbe4f63f178d4a418276461ea1345131565fbe4 passes all 20 fresh
release-source gates, zero reuse, stable inputs, 494.078278137 seconds. Original
receipt `.artifacts/lkjscript-dev/check/1790136934693653897-2833374-0/receipt.json`
has digest verification_b694dbe9354476f5d53f7de794349fd4e387ce9ba770b8b907c5ba0f2d4826f0;
initial/final input digest is
verification_493c460d223841ecb67eafeb573f91204191e5dc355b0ebca3d76548637ef985.
Root tests pass 768 with eight ignored; CLI 68/1, structural 12/1, dev 149/18,
data 10, scan 7 and service 9 pass. Required lint, no-Python, product-surface,
generated guides/assets and default/all-feature distinctions remain intact.
The source owner's retained release executable is
`.artifacts/lkjscript-dev/check/1790136934693653897-2833374-0/retained/release_build/0`,
SHA-256 59580ca92467abe3b46397e85b23be90dcfa7f1db0695f25999cd9ad220aeaa5.
Earlier copied 33fdd616 observations retain their original build binding and are
not relabelled as this later executable.

Fresh fetch/protection admission finds remote main 775b0d07, no divergence except
the two intended commits, no branch protection and empty effective rules/rulesets.
Normal push/fetch and independent branch API observation confirm exact remote main
adbe4f63 with divergence 0/0. Raw admission is under `json-framing-mainline/`.
All owned processes join, including both source checks and the independent native
summary. This mainline delivery is complete; it does not imply candidate/public
acceptance. The reporting descendant must retain the tested source identity.

### Next measured improvement: reject text growth before materialization

A new literal native list-map program in
`/home/coder/workspace/lkjscript-json-buffer-evidence-20260923` freshly creates,
reviews, applies, checks and builds through copied 33fdd616, then executes after
its authoring project moves away. It repeats one shared 65,536-byte Text in an
ordinary List. At 15 copies the exact 983,086-byte result file succeeds. At 16,
128 and 1,024 copies the correct resource/output-byte diagnostic rejects logical
JSON sizes 1,048,625, 8,388,993 and 67,111,937, with no file and joined cleanup.
The encoder nevertheless materializes the full text/serialization first. Sampled
per-process VmHWM rises from 17,292 KiB for 16 copies to 31,960 and 147,108 KiB.
`observe-buffers.cjs`, all samples, argv/stdout/stderr/exits and literal inputs are
retained. The absent `/usr/bin/time` launch failed before any measured product
invocation; only pending invocations resumed through the Linux process sampler.
No result is fabricated. Sampling is every 1 ms and may miss a final peak; concurrent
source work prevents an elapsed-time comparison. No global memory quota is claimed.

Select aggregate JSON-text reservation against the existing output-byte bound
before copying strings, keys, tags or base64 text. Valid bytes and the final exact
serialized-byte check must remain. This reduces unnecessary preparation for a
result already too large without imposing an internal language execution quota.
v0.1.43 is still mutable: its candidate dispatch/publication trigger is the bounded
reservation improvement plus renewed source/final-target acceptance. The already
granted publication authorization persists, and v0.1.42's successful but withheld
lineage remains preserved. No v0.1.43 producer, tag, selection or publication exists.

### Aggregate JSON text reservation

The selected change reserves the sum of UTF-8 text lengths before cloning strings,
object keys and variant tags, and reserves computed base64 length before encoding
Bytes. Every byte counted is a lower bound on encoded output. Checked addition and
the existing output-byte limit reject with resource/normalized_json_output_bytes;
the final exact serialized-byte check remains for escaping, punctuation and other
scalars. Whole-type admission, runtime values, successful canonical bytes, internal
execution policy and graph/artifact/data formats are unchanged. This is not a
whole-process memory cap. Oversized results can now report an earlier minimum-text
diagnostic instead of first materializing the complete serialized size.

Focused tests use fixed literal byte counts and the unchanged byte reader. List
repetition, text Map keys, Unicode, base64/key text, structural/nominal fields and
variants reject during JSON tree construction under insufficient byte limits.
Retained values then reproduce their original bytes under sufficient bounds.
All 11 Map codec tests, the nominal/structural fixture and five F64 codec tests
pass. The copied executable and original logs remain under
`json-text-reservation-tools/`, selected from Cargo's JSON artifact output. The
first launch filter omitted the `tests` module and selected zero tests; the
harness explicitly rejected that empty result. The original zero-test log remains,
and only the corrected filters count as proof. No product failure is inferred.

The locked product-only release build and matched public buffer observation are
next. Fresh dependency-complete source and final-candidate acceptance remain due;
the existing publication authorization persists. No candidate is frozen here.

The locked product-only release build passes in 3m 23s. Its Cargo-selected
24,824,328-byte executable has SHA-256
f6c44a8c8ed2889cfa64e6c0ee820a39b345db2c65b52970c1a64f3aa60d25d1;
it is copied immutably both into the tool directory and outside the checkout.
The comparison also copies the previous source owner's exact accepted 59580ca9
executable. All eight invocations use the same already authored native artifact
and identical input files, with no publishing credentials and joined cleanup.
At 15 copies the 983,086-byte files match exactly. At 16, both reject the same
1,048,625-byte output; at 128/1,024 the corrected encoder rejects at text 17,
before cloning it, with a 1,114,112-byte minimum-text diagnostic. Failed files stay
absent. Sampled peak RSS changes 17,560→17,056, 17,552→18,080, 31,984→15,600 and
146,924→16,036 KiB. The larger small-case observation is retained. These are single
alternating-order Linux VmHWM observations, not a universal memory bound or elapsed
performance claim. The predecessor uses the workspace release gate while the
successor uses product-only Cargo selection; pinned versions and release profile
match, but the configuration distinction remains explicit. Originals and identities
are `reservation-products.json`, `reservation-comparison.json` and each child's
argv/streams/exit/samples in the existing buffer evidence directory.

The original comparison harness expected the failure-note cleanup spelling on its
first successful product run. That assertion failed after the child had correctly
published its exact result and exited zero. `compare-reservation.cjs` and all first
originals remain unchanged. `compare-reservation-resumed.cjs` independently admits
the original success fields, exit, argv, exact output and samples, then runs only
the seven pending children. No application invocation is repeated or fabricated.
All required focused behavior now passes; stabilize this implementation for fresh
source acceptance. No v0.1.43 producer/tag/selection/publication exists at this point.

### Text-reservation source acceptance and v0.1.43 candidate

Source 9394c0ea863823f0ce068e1e956253d92ae80471 passes all 20 fresh source
gates, zero reuse, stable inputs, 501.424601032 seconds. Receipt
`.artifacts/lkjscript-dev/check/1790138904626460374-2864361-0/receipt.json`
has digest verification_8f0faaac696411da6adf93c687944e70c57190a7d8c4bc9183fcff6fb736749e.
Initial/final input digest is
verification_ffd106e1e95b9bcdad16b6365b0dc891321d03f7e2e379d17eb759691cefb20b.
Root tests pass 769 with eight ignored; CLI 68/1, structural 12/1, dev 149/18,
data 10, scan 7 and service 9 pass. Default/all-feature distinctions, lint, safe
Rust, no-Python, product-surface and maintained generated assets remain admitted.
The source owner's retained 24,830,632-byte release executable is
`retained/release_build/0` under that receipt directory, SHA-256
18f158281de1c0a0a2da7d07012cd7e671ba582735a4c5c8d2dbfd0b41f538c9.
Earlier focused/native observations retain f6c44a8c's actual product-only build.

Once that immutable source-gate output exists, the same buffer workload receives
one additional comparison to resolve the earlier Cargo-selection difference.
Both 59580ca9 and 18f15828 use the maintained workspace release configuration,
with unchanged toolchain, lockfile and profile. The same eight byte/diagnostic/
absent-output/cleanup assertions pass. Sampled peak RSS changes 17,192→17,472,
17,728→17,732, 31,756→17,096 and 147,180→15,660 KiB; both small increases remain.
`compare-workspace-configurations.cjs` and `reservation-comparison-workspace.json`
retain separate originals beside the original bundle and inputs. They do not
replace earlier observations or establish an elapsed-time/general memory guarantee.

Fresh mainline admission finds remote 8e91d143, the one intended source commit,
no protection, no effective rules and no rulesets. Ordinary push/fetch and the
independent branch API confirm remote main exactly 9394c0ea with divergence 0/0.
The original admission lives under `json-text-reservation-mainline/`. Relevant
Release occupancy covers all 93 preceding runs and finds none active. Both v0.1.43
tag and release return explicit HTTP 404; immutability is enabled and the scoped
selection still equals v0.1.41's a4c7416c6213f870ae449f900ecb6359fc327aa6.

Dispatch one candidate at 2026-09-23T04:57:30Z:
[35820435256/1](https://github.com/lkjsxc/lkjscript/actions/runs/35820435256),
product and controller source both 9394c0ea863823f0ce068e1e956253d92ae80471.
The independent API binds repository/head repository, workflow path, main branch,
event/source/attempt and observes `in_progress`. Originals are under
`candidate-v0.1.43/`. No tag, scoped-selection change or publication is performed
before candidate acceptance. The complete later promotion remains authorized;
keep this healthy lineage and add no unrelated prerequisite. v0.1.42 remains an
authentically accepted but withheld predecessor; v0.1.41 remains the public product.

### Native collection-summary guide

While source inputs stay frozen, prepare the public collection-summary guide in
an ignored draft and exercise its literal native request outside the checkout.
`/home/coder/workspace/lkjscript-summary-guide-evidence-20260923` retains the guide,
extracted request/descriptor, public plan/apply/check/build inputs, moved authoring
project and all 13 child argv/streams/exits using immutable f6c44a8c. All 42 native
tests pass with evaluator agreement. Project repeated/empty cases match independent
results. Detached runs preserve the expected repeated result, distinguish an actual
empty-string mode by its positive frequency, and summarize 99,999 unique keys in
four fields. A 100,000-key input remains under one MiB but exceeds JSON item count
and rejects before execution. Every child joins and accepted HEAD stays unchanged.
The authored bundle is artifact_bundle_a86a16404dc4f61a39aa7a7b54caf9a0493facd01781aca155b62d04c987ae04;
accepted revision is rev_5f78f5534450b15326b2637d9d67f97b6c241e35eb038ac4a59bd935778c2283.

Publish that already exercised prose at `docs/guides/native-summary.md` and link it
from the native-library guide and README. This reporting/guidance descendant is
separate from the frozen candidate source. Its evidence proves a designed native
workflow, not new compiler behavior, maintained library adoption, a self-hosted
tool implementation or unbounded streaming. No broad source gate is replayed just
to stamp a later reporting SHA onto the accepted source.

### Native complete-table pages and transported generic windows

Guidance descendant c4ecb2edb27f25f6c16ed72d0715e3f7dd3198d2 reaches remote
main by normal push/fetch, divergence 0/0, after fresh protection/rule admission.
The v0.1.43 product source stays 9394c0ea. Its candidate job 107050915081 proceeds
from host tools into source acceptance; seven preceding steps succeed, no failure
or cancellation is observed. `run-after-native-pages.json` and
`jobs-after-native-pages.json` retain that actual boundary, separate from acceptance.

The summary exposes a useful adjacent workflow: retrieving an entire larger table
in bounded pages. Standard discovery returns no `list-slice`; an attempted owner
kind `function` rejects explicitly as unknown. The complete 471-owner query shows
`pure_function` and `external`, including ordinary list-get, list-append and
list-length. Those original query outputs remain in the campaign artifact root.
Select a native generic window function using those public operations, with no
compiler or builtin change and no additional release prerequisite.

`/home/coder/workspace/lkjscript-paged-counts-evidence-20260923` retains literal
`pages-template.lkjc`, the substituted request and reviewed plan, copied source-
accepted 18f15828 executable, bundle/descriptor, unchanged moved authoring project,
input/output files and all 15 original child argv/streams/exits. Its ordinary
generic tail-recursive helper selects the requested list range. The application
clamps negative start to zero, negative count to empty and start beyond the end
to empty. Count is clamped to remaining length before addition; exact signed I64
extremes are admitted without index overflow. Native tests cover I64 and Text
instances and the counting command uses structural key/value records.

All 46 native tests pass with evaluator agreement. Small project execution agrees
with the independent expected page. Detached commands return 99,997 unique counts
in three exact files: 33,333/33,333/33,331 records and 899,992/899,992/899,938 bytes.
Their inputs are 899,984/899,988/899,988 bytes; the three argument members plus
99,997 strings fit exactly 100,000 JSON items. The harness checks every returned
key/value against independently constructed expectations and all assembled
entries, without using output size as an oracle. A 33,334-record page exceeds
the representation's item count, rejects with normalized_json_type and publishes
no file. Every child joins and accepted HEAD stays unchanged. The pure command
recomputes counts per page, so callers must keep input content unchanged; there is
no stored snapshot, streaming claim or elapsed-time comparison.

The same ordinary package is exported and staged into a separately authored
consumer that supplies its own nominal Shipment with label/mass fields. Public
interface query exposes the generic window; reviewed exact dependency selection
instantiates it for that consumer's type. Ten additional public operations and
all 48 tests pass. Project/reference execution and detached execution after the
consumer moves return the complete expected pear/猫 records. Staging preserves
the prior consumer HEAD; both accepted project HEADs remain unchanged afterward.
Literal `consumer-template.lkjc`, `consumer.lkjc`, exported `windows.lkjp`, both
bundles, plans, descriptors and `transport-result.json` remain alongside originals.
The supplier package is pkg_2a4752c450efb8b6971803deae0738c8,
revision rev_b035abecc395c4ebcaa90924c9de06c76d2d50f31fd2ab897670c670a6ed6062,
package_revision_1b169362bcdf678616ba2621aa10b75375335614a6bc33f14430e43fb8430551,
transport package_transport_f097705f086d573326df9240ccc45fd02ea8a1862a7d5f9d6021212838ea9689.

The public paging guide links both unchanged native template originals and shows
their normal plan/apply/check/build/export/stage flow. This is designed native
application/library composition with a real separate consumer; it is not maintained
standard-library adoption or a new kernel capability. The running v0.1.43 producer
continues independently at its frozen source, with promotion still authorized.

### Selected successor: an ordinary standard list window

At continuation startup, local/remote main are clean at
9f9c67cb7ff9984427480242e1679eccb3ed5f01. The unrelated stash and two detached
map-evidence worktrees are unchanged. Rust 1.98.0 remains pinned; approximately
10.5 GiB memory and 819 GiB disk are available. The preceding turn made concrete
progress through source integration, independent native proofs and guidance.
Candidate 35820435256/1 remains live at exact 9394c0ea in source acceptance;
its publication is still authorized and is not coupled to new work. v0.1.41 is
complete; v0.1.42 is authentically accepted but withheld and superseded for delivery.

The native window now has two concrete uses: complete-table paging and a transported
library called with a separate consumer's nominal Shipment. Select an ordinary
generic `list-window<Item>(items, start, count)` in the maintained standard graph,
using the already exercised clamping policy and private tail-recursive helper.
Author through public plan/apply, retain literal input, and derive embedded assets
through the public build/export owners. No window-specific intrinsic or compiler
behavior is needed. Adopt the function in the existing transported map-aggregation
acceptance owner; retain independent full-content, boundary and old/new-bundle proof.
This is a development v0.1.44 addition. Preserve v0.1.43's frozen source and normal
promotion path. Audit actual dependent consumers and exact package selections;
do not silently upgrade retained user programs or rewrite predecessor evidence.

### Native list-window implementation and maintained adoption

Public plan/apply accepts the unchanged native input
`packages/standard/requests/20260923-list-window.lkjc`. Its plan creates 146 owners
and four types, with no updated/deleted owner, dependency or retirement. Existing
validation summaries are reused during planning; execution is proved separately
by 45 passing differential graph tests (eight new). The public build/export owners
produce the exact new standard artifact and transport recorded in its README.
The only Rust product edit selects those derived built-in identities. Item remains
unconstrained; the pure helper clamps count before addition and uses ordinary
list reads/appends and tail calls. No new intrinsic, data encoding or user migration
is introduced. Product-generated discovery includes the complete public signature.

The copied development product c5e216cd2ace9fccea5b4dfe5310a12c0b3df13499d8956cd66be846247919ce
(24,916,648 bytes) is retained outside the checkout at
`/home/coder/workspace/lkjscript-list-window-evidence-20260923/lkjscript`.
The existing transported map aggregation owner now calls standard `list-window`
for sorted structural entry pages and consumer-owned nominal Event lists. Twenty
additional commands cover independent first/tail/middle/clamped results before
and after the reviewed aggregation supplier change, and both bundles after source
removal. The 512-event middle-page oracle independently selects fixed BTreeMap key
bounds; small and I64 extreme results are fixed expectations.

Focused persistent-map receipt 2 passes all 72 commands and its original reader
with joined cleanup in 22.064767950 seconds. Its four inventories and 212 retained
files remain at the external root's `offline/`; receipt identity is
verification_7deaedc2879d3a506b234fee9c23f51b78e53fe30ea767c80f9fd995241897fb.
Original verifier a489d54ea6d0f4473ad5f689ddc74369407d7a762265d4f252307108594011b4
is retained in `.artifacts/composable-20260922/list-window/tools/` with Cargo-reported
producer paths and build logs. The explicitly executed reader fixture passes:
omitting the new page, changing invocation arguments, consistently rehashing a
changed input or authored body, and relabelling it with the predecessor schema
all reject; restored and untouched originals pass. That fixture is normally
ignored because it requires an authentic retained run and its original verifier.
The aggregate workload advances from receipt 18 to 19. Frozen v0.1.43 retains its
original receipt 18 verifier; the new workload is not retroactive release proof.

Consumer analysis finds no need to change lkjournal's exact earlier supplier.
Public check passes its 44 tests; a full public rebuild reproduces the existing
artifact byte for byte and accepted HEAD stays unchanged. The predecessor paging
example remains valid for v0.1.43; its guide also discovers and shows the direct
standard call for v0.1.44. Literal authored programs, all command inputs/outputs,
failed-output assertions, generated assets and tool identities stay with their
existing owners. Complete source acceptance and mainline integration are next.

### v0.1.43 candidate acceptance and authorized publication start

Candidate 35820435256/1 completes successfully at exact product/controller source
9394c0ea863823f0ce068e1e956253d92ae80471. Its terminal is `candidate_accepted`:
20 source gates, six final-target owners, two pinned userlands, installation and
original-reader admission pass with joined cleanup. Final executable
85fc3b808a29b14c3789aab1b881742c6ac105df4e0188fb844730077024495f is 24,967,712 bytes.
The five authenticated GitHub ZIPs and compact terminals remain at
`.artifacts/composable-20260922/candidate-v0.1.43/accepted/`. Assets/verifier/acceptance/
diagnostics/terminal artifact IDs are respectively 10733453790, 10734445162,
10733434010, 10733443816 and 10734655225. Earliest observed service expiry is
2026-10-07T05:39:18Z. Receipt records bind exact distributable/verifier bytes and
original proof owners; local ZIP hashes and sizes agree with GitHub service metadata.

Fresh remote main is 9f9c67cb7ff9984427480242e1679eccb3ed5f01, containing the tested
source. Main remains unprotected with no effective rules/rulesets; immutability
is enabled. No competing release workflow is active. v0.1.43 tag/release occupancy
is absent; v0.1.41's immutable release and successful terminal own prior selector
a4c7416c6213f870ae449f900ecb6359fc327aa6. The installed gh lacks `variable get`;
that read-only command fails, then the exact variable REST endpoint supplies the
current value. No selection is changed by the failed getter.

The authorized ordinary annotation v0.1.43 is created and pushed at the accepted
source, object ce2fbbabef3bf6e00ee95c92b266d3473d439604. Git's default annotation
cleanup removes Markdown heading lines beginning with `#`; an initial byte-for-byte
notes assertion therefore fails before the scoped write. The original notes and
actual annotation remain preserved. Independent local-object/remote-object/source
checks and `git stripspace --strip-comments` of the original notes establish the
exact difference, with all explanatory body text retained. The tag is not rewritten.

The execution wrapper erroneously continued to dispatch promotion 35823850909/1
at 05:47:06Z after that local assertion failure. Its controller is unchanged main
9f9c67cb. The already authorized scoped selection is then completed: compare the
prior v0.1.41 value immediately before the write, set the exact v0.1.43 annotation,
and read it back at 05:47:53Z. This is not an atomic compare-and-swap. The promotion
job was already building its credential-free controller, so its eventual authority
result must establish whether it received the updated variable. If it retained the
old value, preserve its rejected history and resume only the permitted boundary
using the original accepted producer. No candidate is rebuilt or acceptance weakened.
Publication and anonymous verification remain due; no successful publication is
claimed from the tag, scoped update or queued workflow alone.

### Expanded-source verification and exact-pin test correction

The first complete source attempt at 2d7e1ac7188213c2750b529264ba11cbad55ec03
fails honestly: six fresh gates pass, workspace tests and the copied release
lifecycle fail, and twelve downstream gates skip. Inputs remain stable. Original
receipt `.artifacts/lkjscript-dev/check/1790142607739471654-2904519-0/receipt.json`
has identity verification_76ea117d78a2221e3a264b85ab5deacf060329f3c838be3fe4b8fc621fe65a04;
elapsed time is 405.973683502 seconds. Root tests report 766 passes, three failures
and eight ignored. The copied lifecycle's exact selection also fails its old count.

Three expectations still name the old standard inventory/test totals: update the
independently observed 1,158 owners, 45 standard tests and 46 recipe-plus-standard
tests. Another clean-build fixture incorrectly supplies the current built-in
artifact for lkjournal's retained older exact dependency, which the linker correctly
rejects as `artifact_link_dependency_binding`. Repair the fixture to independently
compile the admitted dependency source selected by the application, then link that
fresh exact artifact into the clean root compilation. It still compares complete
bytes against the maintained artifact. Do not replace the application's correct pin
or loosen exact linking to satisfy the obsolete test assumption.

### Publication resumption after a captured prior selection

Promotion 35823850909/1 confirms the suspected ordering problem: its authority step
receives prior selector a4c7416c6213f870ae449f900ecb6359fc327aa6 and rejects at
05:52:18Z. Transfer, publication and anonymous verification do not run. All three
authenticated controller/diagnostics/terminal ZIPs remain in the candidate root's
`promotion-first/`, including original failure logs; the failure is not relabelled.
The valid candidate, its tag and updated selection remain intact.

Fresh preflight observes the same annotation/source, enabled immutability, absent
v0.1.43 release and no active competing run. Promotion 35824461469/1 starts at
05:55:49Z from unchanged controller 9f9c67cb, selecting original producer
35820435256/1. `promote` is required because transfer admission never ran;
`resume-publication` would skip that still-unproved dependency. Candidate acceptance
is reused at its exact frozen bindings and no product rebuild is requested.

The four focused corrected tests now pass, including the copied full command
lifecycle against the unchanged copied development product. Cargo-reported test
executables, literal selectors and outputs remain under
`.artifacts/composable-20260922/list-window/compatibility-*`. Source changes are
limited to the fixture's exact dependency preparation and maintained expectations;
the public linker and application data remain unchanged. The release guide now
uses explicit verbatim annotation cleanup and requires successful scoped read-back
before dispatch, with the correct resumption choice for a pre-transfer rejection.

### v0.1.43 publicly complete

Promotion 35824461469/1 succeeds at controller 9f9c67cb and final terminal
`immutable_published_and_public_verified`. Exact/latest anonymous acquisition,
strict admission and the small installed lifecycle pass; latest selects v0.1.43
at exact source 9394c0ea863823f0ce068e1e956253d92ae80471. The immutable public release
is 394333274, published 2026-09-23T06:02:11Z:
<https://github.com/lkjsxc/lkjscript/releases/tag/v0.1.43>.
All three asset digests and lengths equal the accepted candidate's originals.

The archive is 11,381,824 bytes, SHA-256
a8cec20f4bd97a77a34169d0dac32eb40368e6a5006a002f08c9cbe8fed853f0;
SHA256SUMS is 109 bytes, cb0c2dd5aa575d1cf8358ba01f119b3567ce820bf621293eadaa8c1a0295adae;
install.sh is 3,566 bytes, c8d2051b0c5972be6801734423eef37e0c463e0b96023a6135021a2722f841ce.
The executable remains the accepted 24,967,712-byte 85fc3b80… value recorded above.
All six authenticated publication-run ZIPs, actual release metadata and terminal
are retained in `.artifacts/composable-20260922/public-v0.1.43/`; earliest service
expiry is 2026-10-07T06:00:49Z. The genuinely rejected first promotion, accepted
but withheld v0.1.42, and completed v0.1.41 remain preserved. There is no remaining
v0.1.43 publication obligation or unattended monitoring promise.

### Native generic ranking and an older exact-pin consumer

While source verification ran without input changes, an ordinary native stable
merge-sort library and frequency-ranking command were authored outside the checkout.
Literal `ranked-template.lkjc` uses standard list-window for recursive halves and
final result selection. A private tail-recursive merge takes a pure comparator;
it retains the left item when the right does not precede it. Native tests cover
empty/singleton comparator non-invocation, numeric order, stable frequency ties,
empty and bounded results. No compiler or sort intrinsic implements the algorithm.

The copied c5e216cd… development product in
`/home/coder/workspace/lkjscript-ranked-counts-evidence-20260923` accepts 248 new
owners and 11 types, with no updated/deleted owner or dependency. Separate checking
passes all 52 graph tests. The first 17 public commands return independently exact
results for 512, 16,384 and 99,998 input rows, including 99,998 distinct keys.
The latter input is 899,988 bytes and each requested top-17 result is 460 bytes.
Counts and the complete table are computed/sorted before selection; these values
are correctness bounds, not timing, memory or optimized top-k claims. The project
moves away before detached runs; cleanup joins and retained HEAD is unchanged.

Ten more commands export the generic sort library and author a separate consumer
with its own private Shipment and named heavier comparator. All 54 graph tests
pass. Project and detached execution place the 11-unit 猫 first and retain the
original pear, apple, plum order among the three equal 9-unit items. Two additional
commands independently check a nonuniform 65,516-row/4,096-key frequency table and
all 4,096 numeric permutation results. Native templates, actual requests, plans,
bundles, descriptors, inputs, original output/exit records and the three result
files remain together; the guide copies the literal templates byte for byte.

The library is pkg_c3dac3e50491625a5cdcb8db84880ee2,
rev_13935dbb799f7c2896ff8d09bf9f7576563d510d5efb6f4483b630bb17ef29d1,
package_revision_72cb982383f3b37020b7be49c7ff39f23443a990999cb7480d6cfb903503de7c,
package_transport_3d4caa6a0665b263c2f97f56c992157190614dc17f6b511b05f6ac2e464b74ea.
It is a designed ordinary library/workflow, distinct from maintained standard adoption.

A copied v0.1.43 source executable f6c44a8c… creates an older client in
`/home/coder/workspace/lkjscript-ranking-predecessor-evidence-20260923`.
Staging the new library preserves HEAD; adding it alongside the old direct standard
pin rejects with `package_revision_closure_package_conflict`, exposing no plan or
accepted change. Publicly stage the exact newer standard, propose its dependency
replacement together with the new consumer, and bind native `std` to that exact
package instead of the executable's earlier builtin. The resulting plan changes
two dependencies and creates 48 owners; application of the reviewed token succeeds.
All 54 tests, project use, build and detached use pass under that same v0.1.43
executable. The ten-command observation contains nine successes and the one expected
unchanged-HEAD rejection. It proves this explicit package repair, not automatic
upgrades, arbitrary predecessor compatibility or a locally anonymous acquisition.
The public ranking guide now includes that exact-selection recovery path.

### Register native guide examples without weakening the product audit

The corrected source attempt at b9e5b2ace4fa570a4f067e60e91566ea1fd7412e passes
19 fresh gates and fails only `product_surface_audit`, with stable inputs and zero
reuse in 494.862726424 seconds. Receipt identity is
verification_dbd28de8f8caafe4f8eb78da7a0f43e7baf63eac6a0da6562f9eb128b578dec4
at `.artifacts/lkjscript-dev/check/1790143409686104817-2913572-0/receipt.json`.
The guide's new `examples` directory, introduced by reporting descendant 9f9c67cb,
is unregistered in the previously flat Markdown-only owner. This does not invalidate
v0.1.43's earlier frozen product source 9394c0ea or its independent public proof.

Register `docs/guides/examples` explicitly for native `.lkjc` inputs. Every child
still receives file-size, UTF-8, regular-file and forbidden-text inspection; unknown
extensions, unowned directories, and file/directory symlinks still reject. Three
focused policy tests pass, including injected forbidden text inside a native input.
The real product-surface audit passes 41 files and 26 executable observations with
zero exceptions and violations, including the new ranking guide and both native
inputs. The updated immutable verifier is 1ce56517329e7dd3afc95790bd61ec243c4c124b6354bb9dca64e3b7c6cff3a4
(36,051,400 bytes), retained as `list-window/tools/lkjscript-dev-surface` alongside
Cargo-reported build originals. Complete acceptance will use this updated verifier;
the predecessor checker cannot prove the expanded directory owner.

### v0.1.44 source accepted and integrated; candidate dispatched

Source d104edc749dad4489f52d73b96c4bab2d320475b passes all 20 fresh release-source
gates with zero reuse in 221.508059173 seconds. Original receipt is
`.artifacts/lkjscript-dev/check/1790144640868671138-2933980-0/receipt.json`,
verification_f13786802069a2fd0f030aea10829c3e0d2c274e74e71cea4c03f9879d204831.
Initial/final input identity is
verification_8e6eced7785ce01d98ee164b467940a4ced2b5ff1eac9f6120a41b5d63ff0d5a,
with stable inputs. Root tests pass 769 with eight ignored, CLI 68/one ignored,
structural CLI 12/one ignored, and dev 151/19 ignored; data 10, scan seven and
service nine also pass. The selected original-reader fixture was executed separately,
as recorded above. Every required source gate, generated/embedded asset comparison,
product surface and no-Python protection remains present. Earlier failed attempts
keep their actual classifications and original inputs.

The retained workspace product is
`.artifacts/lkjscript-dev/check/1790144640868671138-2933980-0/retained/release_build/0`,
24,916,648 bytes, SHA-256 f21062c098f059d47b57accdc30e751b588d51a7b27dd7fb4598f431c966529d.
It is distinct from the earlier native-workflow copy c5e216cd…, whose exact byte
identity and bounded observations remain recorded separately. The source checker
also retains its later default-feature release-lifecycle executable independently.
Final distributable bytes will receive their own complete target proof in CI.

Fresh remote main remains 9f9c67cb with no intervening work, no effective rules or
rulesets and no branch protection. Normal fast-forward integration delivers all
three continuation commits (2d7e1ac7, b9e5b2ac, d104edc7). Fetch/read-back confirms
local and remote main exactly d104edc7, zero divergence and a clean worktree.
The immutable v0.1.43 accepted source remains an ancestor, not relabelled with the
new source SHA. No application data or lkjournal dependency migration is performed.

The ordinary standard-window milestone selects v0.1.44 for publication. Preflight
finds no matching/active release run, absent local/remote tag and release occupancy,
enabled immutability and completed v0.1.43 public verification. Candidate
35827065894/1 starts at 2026-09-23T06:30:27Z, product and controller both exact
d104edc749dad4489f52d73b96c4bab2d320475b:
<https://github.com/lkjsxc/lkjscript/actions/runs/35827065894>.
Dispatch/preflight originals remain under
`.artifacts/composable-20260922/candidate-v0.1.44/`; draft notes are
`.artifacts/composable-20260922/release-notes-v0.1.44.md`. Candidate acceptance,
annotation/scoped selection, same-asset publication and anonymous verification
remain due and authorized. Reuse the healthy producer; do not add a new workload
as a publication prerequisite or mutate its frozen source.

A further read-only check reconstructs the ranking module from accepted meaning:
public `change draft` writes a 12,265-byte canonical native request, whose unchanged
re-entry reports `semantic-change=false`, `owner-recreation=false`, `publication=none`.
The original native input is not an editing authority. Query/draft/unchanged-plan
outputs remain with the ranking witness. Two initially attempted unsupported
capabilities selectors (`change draft` and `change.draft`) reject as usage errors;
`capabilities change` and the generated grammar provide the actual supported family.
This is a discovery correction, not a product semantic failure. The guide now shows
canonical re-entry without introducing another synchronization authority.

### Native bounded selection and a matched ranking comparison

The next continuation observes healthy v0.1.44 candidate 35827065894/1 and clean
local/remote main 98f466c28dab23e5bfc1443ec33808dc15d169a9. The unrelated stash,
two detached evidence worktrees and completed v0.1.43 publication remain intact.
No local owned job is outstanding. Pinned Rust is 1.98.0; about 10 GiB available
memory and 816 GiB disk suffice for the selected native workload. The candidate's
source remains frozen; this independent increment is not a publication prerequisite.

The prior ranking witness sorts the complete frequency table before returning
17 entries. A new ordinary native `select-by<Item>(items, count, precedes)` shares
the input through index ranges and retains at most the requested prefix at each
merge. Count is clamped before recursion, including both signed I64 extremes.
The common merge helper stops when its remaining count reaches zero and retains
left-before-right ties. Full `sort-by` remains available. For a consistent,
non-failing strict weak ordering, selection returns that stable sort's prefix;
its comparator call sequence and reached failures are a separate contract.
Counting still constructs the whole Map and entries even for nonpositive count.
No kernel, intrinsic, standard package, artifact format or lkjournal input changes.

Fresh public native authoring in
`/home/coder/workspace/lkjscript-selection-evidence-20260923` uses the preserved
24,916,648-byte c5e216cd2ace9fccea5b4dfe5310a12c0b3df13499d8956cd66be846247919ce
development executable. The reviewed plan creates 414 owners and 18 types, with
no existing owner update/deletion, dependency change or retirement. All 58 graph
tests pass differentially, including comparator non-invocation at empty, singleton
and nonpositive boundaries. The accepted library is
pkg_6a47b3b2682d507da9b860575f8cb10a,
rev_8e57f2f96a756dcc3fc85a381c79bdafe38e28e6f9da32a942f8800414ea91fb,
package_revision_cfb40ac33b28eafafab8e8bc37a4296f1dfc9e9794009cadcb1b12e49bbd05e3,
package_transport_ca0e04b1eb418efa74b1c42feace0931031547dd94f472c4c69c9e49349c9841.

An independently authored consumer passes all 61 tests and transports its own
private Shipment and heavier comparator. Five counts over 511 varied records
match an independent mass/index oracle, including stable boundary ties. A native
batch wrapper also checks every list of length zero through five over {-1,0,1}
at seven counts: all 2,548 results match independently sorted/clamped expectations.
The minimal maintained consumer input is then authored separately, passes all
61 tests and both full/selected targets in project and detached execution. Guide
native files are byte-identical to those actual authored originals. Projects move
away before detached use; retained HEADs remain unchanged and cleanup joins.

One executable runs the preserved full-sort bundle and new selection bundle with
identical inputs and independently exact result bytes. Nine workloads each have
one excluded warmup pair and three measured pairs, with alternating order and no
owned overlapping workload or build. All 72 commands succeed. The 99,998-distinct
top-17 invocation median changes from 28,667.597 to 6,896.300 ms; the empty-input
whole-process median instead rises from 55.263 to 56.061 ms. Preparation, encoding,
charged cumulative allocation, all samples and unfavorable cases remain in
`paired.json`, environment and raw originals. Cumulative allocation is not peak
memory. `docs/performance.md` owns the full table and scope. Together with the
41 public authoring/functional commands, this root retains 113 successful commands.
This is a designed ordinary library observation, not new standard adoption,
release acceptance, a general speed guarantee or a model-token/cost claim.

The maintained surface audit passes 41 files and 26 executable observations with
zero exceptions/violations using immutable verifier 1ce56517… and retained f21062c0…
product. Original output remains in `selection/surface.stdout` under the campaign
artifact root. All seven local links in the ranking guide resolve. The earlier
39-command sorting/old-pin recovery observation remains historical, not relabelled
as proof of the new library. Fresh remote-main/protection reads at 06:57 UTC find
unchanged 98f466c2, unprotected main, no effective rules and no rulesets. Only the
seven intended native-guide, performance, status/roadmap and campaign paths are
selected for ordinary mainline integration; runtime source remains d104edc7's
already accepted implementation.

### Integrated range selection and a measured heap alternative

Ordinary fast-forward push and fetch/read-back confirm remote main exactly
997a88932543e194be03fbf17bf978d1999d63ec, with a clean worktree and zero divergence.
The d104edc7 product source is an exact ancestor. This delivers the preceding
native guide increment without relabelling its copied executable or prior source
acceptance. The next continuation starts from this state, preserving the stash,
detached worktrees and external candidate 35827065894/1.

An owned copy of the accepted selection project is edited through public
query/draft/plan/apply in
`/home/coder/workspace/lkjscript-heap-selection-evidence-20260923`. No accepted
storage is edited. Literal `heap-edit.lkjc` adds an ordinary Map-backed heap with
a private generic Indexed record and original-position tie breaking. Its plan
creates 339 owners and 28 types, updates the existing select-by declaration and
retires its 27 replaced expression/binding owners. Generic/positional parameter
identities and the public contract remain unchanged. All 58 graph tests pass.
The successor is rev_d149e172214e1f217c8e5cac5784bd901b0f97f91a9319d32ce1c3dce473278b;
the baseline accepted graph and bundle remain unchanged in their original root.

An existing nominal consumer stages this successor and changes only one exact
dependency, with zero created/updated/deleted local owners. All 61 tests pass;
2,548 small numeric cases, five counts over 511 nominal records, and the four
earlier counting inputs preserve exact expected results after both project paths
move away. There are 24 successful public edit/functional commands. Another 112
commands compare range merge and heap over 14 workloads, with one excluded
warmup pair and three measured pairs per workload. Each uses the same copied
c5e216cd… executable and independently expected result bytes.

The 99,998-distinct top-17 median improves from 6,729.238 to 2,118.791 ms in this
new matched observation. However, 4,096 descending numbers at count 17 worsen
from 121.449 to 245.014 ms, and complete/permuted selection also worsens. Thus the
experiment does not replace the general guide unconditionally and does not justify
a size-only threshold. `docs/performance.md` owns all 14 medians; original requests,
plans, graphs, bundles, stage counters, environment, warmups and slower cases remain
with this root. No new standard function, kernel behavior or receipt contract is
introduced by the experiment. All local experimental processes have been joined.

### v0.1.44 failed target admission: repair current fixture counts

Candidate 35827065894/1 at d104edc749dad4489f52d73b96c4bab2d320475b finishes with
failure. Its source acceptance passes all 20 fresh gates with zero reuse and stable
inputs in 1,533.390189893 seconds. Input identity is
verification_38cdd1270704511b17f4b26d93619c7f32bd974d8675683404631ee01c6fbb30.
The final product and three assets are constructed successfully. Target admission
then fails in distributed-http: the actual product check succeeds with 46 tests,
zero failures and equal differential execution, while the verifier still requires
the prior 38. The target stage ends failed after 19.865038801 seconds; its parent
reports zero accepted target owners. The terminal is genuinely incomplete, not
candidate_accepted, and no promotable handoff is uploaded.

Both authenticated original artifact ZIPs are retained under
`candidate-v0.1.44/failure/` in the campaign artifact root: diagnostics 10736977911
(59,259,302 bytes, d3707a8aaad84e7a9a3c1c6cf17e1310f7bee2412e3f1bb1f61c89c3b5f3ffe8)
and terminal 10737002659
(289 bytes, 677e0a9f21928d600d00166876a1121e24f1364006987fd9d8cefdd7b154ee8b).
Their repository, head repository, workflow, event, source/run/attempt, lengths and
service SHA-256 identities are checked independently. Earliest observed service
expiry is 2026-10-07T07:12:04Z. Run/jobs/artifacts metadata, failed step logs, source
receipt, failed release receipt and original HTTP result remain together.
HTTP cleanup reports the isolated root removed and complete runner cleanup.

The final extracted candidate is 25,057,824 bytes, SHA-256
1ae34fb76cd5aed8dc9ef6d085f84b72c2245287436b39c239bf491367e6a560; the original CI
verifier is 36,034,952 bytes,
6e6fd786a984d83e72f888bddf43e2c9ec04a6c0d1211abe97c9c7a11d3f2ce1.
The retained candidate is only used without publishing credentials. No v0.1.44 tag,
scoped-selector update, publication or acceptance is claimed.

Audit all current test-count consumers instead of stopping at the first failure.
The HTTP expectation becomes 46 (45 standard plus one recipe assertion), the two
diamond checks become 49 (45 plus four package assertions), and the nominal check
becomes 45. Existing exact equality, zero-failure and differential-equality checks
remain; historical predecessor fixtures keep their original meaning. Wire receipt
schemas do not change. The previous standard-window increment's focused map
workflow did not cover these complete target-owner fixture expectations.

The repaired local immutable verifier is 36,051,400 bytes, SHA-256
2ae0416627f1bb06b1db7040f1eb1ffe45d9921d6659b38a5e9fa67c9764381a, copied from
Cargo's reported executable into `target-count-repair/lkjscript-dev`. Against the
same failed CI product bytes, a fresh distributed-http observation succeeds:
46 commands, two runners, 14 responses, six startup rejections, exact clean/
incremental artifact equality, unchanged authority and equal restart. Receipt is
verification_8f07737d30248b42eccad1837de8e3b8b361ed7b0fa1ed646ab434b1d439dffe
at `/home/coder/workspace/lkjscript-target-count-evidence-20260923/http/receipt.json`.
The complete offline owner also passes against those exact bytes: 1,029 commands,
47 inventories and complete cleanup in 312.457561462 seconds. Its generation-19
receipt is verification_7e66496b7435649ae37da32e6dd4cb3e88ea36a0e3bfde15f0be0629cede485f
at the same evidence parent's `offline/receipt.json`; its isolated root is removed.
Both focused owned processes are joined. These observations do not reclassify the
original candidate as accepted; the changed verifier requires renewed source and
candidate proof before publication. At 07:33 UTC, remote main remains 997a8893,
unprotected with no effective rules/rulesets and no intervening work.
