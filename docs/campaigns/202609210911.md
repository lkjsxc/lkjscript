# Deliver v0.1.39 from the already accepted candidate

Created: **2026-09-21T09:11:31+09:00 — Asia/Tokyo**  
Campaign: `docs/campaigns/202609210911.md`  
Repository: `https://github.com/lkjsxc/lkjscript`  
Disposition: **Resume due publication; finish public delivery, not another candidate campaign.**

You are Codex, the responsible implementer, verifier and integrator. Execute this campaign through the maintained release owner and normal remote-main delivery. Communicate with the human in Japanese; use English for repository work. Ordinary diagnosis, necessary repair, tests, documentation, commits and non-forced integration are delegated within actual permissions. Do not stop after preparing a plan or dispatching a job when useful in-process work remains.

## 1. Identity, evidence and startup

### Inspected baseline

| Identity | Observed value |
| --- | --- |
| Remote main | `0bf919f8718dcaaa265e84f73fc93f9d52f88f94` |
| Main tree | `addcf19bfa7d8ce67da0ffdde721c608ec128c06` |
| Selected product source | `b05e9e12133ef625b33462123468b9b896885dd4` |
| Selected product tree | `d15dab56bcc93ac4bb889e94bb84c10607b0a8ad` |
| Candidate producer | `35508727722`, original attempt `1` |
| Producing workflow | `343592338`, `.github/workflows/release.yml`, `workflow_dispatch`, branch `main` |
| Acceptance job | `106072815810`, `Accept final candidate`, successful |
| Terminal job | `106087422142`, successful; original log reports `candidate_accepted` |
| Candidate run completion | Successful; API updated at `2026-09-20T13:42:00Z` |
| Latest public release | Immutable `v0.1.38`, release `390160031` |
| Root guidance | `AGENTS.md`, blob `b3fde2334f145146ddaec04219cfd586654c9a49`; unchanged |

The comparison API establishes that inspected main is three commits ahead of the selected product source, with that source as merge base and no behind commits. The descendants record the corrected candidate and retire temporary diagnosis/generation workflows. Recheck their actual diff before relying on unchanged product/controller inputs; do not substitute the descendant SHA for accepted source.

The architect observed remote source, run/job metadata, the terminal log and artifact metadata. The producer's source-specific acceptance, final construction, candidate/original-evidence admission and required uploads succeeded. Controller, publication and public-acquisition jobs were **skipped** in this candidate-only invocation. The architect did not download/admit the essential ZIP contents or execute product tests locally. Hosted acceptance is observed; independent local byte admission is not claimed. The v0.1.39 tag lookup returned 404. Current draft occupancy, scoped-control value, immutability setting and your permissions remain startup checks.

### Predecessor disposition

`docs/campaigns/202609201910.md` owns the existing release lineage and both explicit human publication requests.

- **Complete:** the corrected candidate above. Its earlier recorded “in progress” state is superseded by the observed successful terminal. Version-bound guide generation is included in its source.
- **Actionable:** authenticate that producer, establish authorized tag/control prerequisites, promote unchanged assets and complete anonymous public verification. Publication is already due; another feature is not a prerequisite.
- **Historical/superseded producer:** `35504414463/1` at `2bb72e4864485b73dc6aa152fb16ac34d4552285` failed source acceptance. The predecessor records guide drift and inconsistent earlier diagnostics, followed by corrected generation. Preserve those failures; do not consume this failed producer or invent a defect from its disputed symbols.
- **Potentially externally blocked:** the former session lacked a supported scoped-variable write and administrative immutability read. That reports the former environment, not your present capabilities. Establish the actual boundary without weakening it.
- **Not revived:** old v0.1.38 manual-reader gaps, retired temporary bridges and unrelated historical obligations. They do not require another broad reconstruction.

Read current root and applicable narrower guidance, `docs/release.md`, this predecessor's relevant ending, and the owners below. Inspect HEAD, worktree/index, relevant untracked files, stashes/worktrees, remotes, divergence, active jobs and protections. Preserve unrelated work and operational data. The inspected SHA is an observation, never a reset target. Reuse the appropriate outstanding lineage; do not accumulate replacement release branches.

Archive this initial mandate unchanged at its campaign path. Append clearly separated reconciliation/execution additions; do not edit the initial diagnosis retrospectively. The path was absent at the inspected revision. On a real collision, preserve both originals using the existing `-collision-N` convention. Do not modify frozen candidate inputs to archive this mandate.

## 2. Outcome and decisive tradeoff

An ordinary Linux x86-64 consumer must be able to acquire the selected immutable release and the selected latest release, install without Cargo or a language checkout, author/change/build/run through the installed executable, and retain the previous runtime for recovery.

This delivers the already integrated persistent-map improvements, callable-input evolution, HTTP correction, exact data pagination, strict data admission and stack repairs described in `docs/releases/v0.1.39.md`. It also completes the first live publication of the neutral release-content cutover. Attribute behavioral claims to admitted candidate evidence, not merely the release notes.

Whole-snapshot application-data costs and further authoring/expressiveness improvements are credible next product work. They lose priority here because substantial improvements already have an accepted distributable and the human has explicitly requested distribution twice. Rebuilding a healthy candidate or adding another feature would delay that usable result without addressing the present boundary. The outcome is public delivery, not a status-document-only change; no artificial code patch is required when the maintained mechanism works.

Do not add language features, change application schemas, promise simulation throughput, redesign storage, expand supported platforms, replace the release framework, or reintroduce push-triggered helper workflows. No production deployment is selected.

## 3. Contracts and engineering route

### A. Preserve and authenticate the accepted candidate

Start from the exact producer and original attempt, not a newest-name search. The following are observed **GitHub service ZIP identities**, not inner release-asset or executable digests:

| Handoff | Artifact ID | Service ZIP SHA-256 | Observed expiry (UTC) |
| --- | --- | --- | --- |
| `candidate-assets-35508727722-1` | `10606560930` | `97bfd0d54b2bbaa85e56721cd9be823ab8767e1f472446e0571a567f7c544bb9` | `2026-10-04T13:41:29Z` |
| `candidate-verifier-35508727722-1` | `10606740704` | `ee4eb68f155b1e00900c83c7b33aaabcefe2c1708fd79103f781286deb445273` | `2026-10-04T13:41:30Z` |
| `candidate-acceptance-35508727722-1` | `10606675801` | `0cd1fc4bb4e615b487f892e37ed063d2bfc529dc7b934c1eeec76c7d60c516a0` | `2026-10-04T13:41:31Z` |

All three were listed as unexpired. The earliest essential expiry is **2026-10-04 22:41:29 Asia/Tokyo**; retention is not permanence. Terminal artifact `10606251772` and diagnostic artifact `10606466117` belong to the same producer. Use diagnostics when a concrete discrepancy requires them, not as an automatic second acceptance stage.

The authoritative selection belongs to the maintained controller inside `promote`: authenticate repository/head repository, workflow/event, source ancestry, original run/attempt, required successful job/steps, service artifact identities, complete inventories and receipt/content bindings. Preliminary operator inspection may use authenticated API reads/downloads and inspect the acceptance decision and inner asset identities as data. It neither replaces controller admission nor requires a second consumer invocation. Do not forge a hosted environment or build another admission parser. Preserve the original required handoffs and acquisition context in owned storage according to the maintained retention contract. A permission failure downloading an artifact does not establish that it expired.

The normal path reuses the producer's accepted source/target/installation/original-reader proof. It must not replay all broad owners, build a second product executable or rewrite the manifest merely because assets cross jobs. A trusted current controller build is not a distributable rebuild and is permitted. Later narrative changes are not grounds for retesting the frozen product or relabelling its receipts.

### B. Establish publication authority deliberately

This campaign **explicitly authorizes Codex as the delegated release operator**, within existing permissions, to create and normally push the selected annotated release tag and update the existing non-secret `LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA` release-selection control. This is a specific publication decision, not permission to change credentials, access controls, protections or release immutability.

Before those writes, reconcile current release jobs, complete tag/release occupancy including drafts, current main ancestry, enabled repository immutability and the prior control's owner/release state. Supported reads include:

```sh
gh api repos/lkjsxc/lkjscript/immutable-releases
gh api repos/lkjsxc/lkjscript/actions/variables/LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA
```

Discover available authenticated operations without printing credentials. Do not interpret a permissions error as proof that immutability is disabled. If the required setting cannot be read, or is disabled, stop before publication and preserve a precise blocker; do not enable it under this mandate.

For the normal path, create an ordinary annotated **v0.1.39** tag selecting **exactly `b05e9e12133ef625b33462123468b9b896885dd4`**. Review `docs/releases/v0.1.39.md` and use meaningful release notes in the annotation: the publisher reads the annotation message. Do not tag current main merely because it contains the candidate. Reuse an already matching authorized tag; never overwrite or force-move an occupied tag.

Read the remote annotation object and its commit target independently. Set the existing scoped control to the **annotation object's SHA**, not the product commit, tree, ZIP digest or executable digest. The predecessor reported `d711b4cb7385243283e4cdc53999c8b65fc2bbf6` as the old v0.1.38 annotation; that is historical information, not a current expected value. Read the actual prior value, reconcile it, compare immediately before the authorized update and read back afterward. This is not atomic compare-and-swap; do not race another operator.

Acceptance and matching hashes establish eligibility, not authority. The deliberate operator selection above supplies authorization, which the maintained independent control represents. Do not replace that control with a workflow literal, silently derive authorization from candidate content, broaden token privileges or create an alternate publication path. Lack of actual authority is a blocked boundary, not a reason to build again.

### C. Promote once through the maintained owner

When the preliminary operator checks and authority prerequisites are established, use the existing workflow:

```sh
gh workflow run release.yml --repo lkjsxc/lkjscript --ref main \
  -f operation=promote \
  -f producer_run=35508727722 \
  -f producer_attempt=1
```

First check that an equivalent healthy invocation is not already active or complete. Record the actual consuming run/attempt and immutable controller revision. `promote` already selects/authenticates the producer, checks authority, runs transfer admission and the small installed lifecycle, publishes and performs anonymous public checks. A separate `consume` invocation is not a compulsory rehearsal; use it only to resolve a specific read-only uncertainty, especially when publication authority is unavailable.

The published inventory must be exactly the accepted archive, `SHA256SUMS` and `install.sh`. Promotion must preserve their bytes. Candidate/transferred-verifier execution remains separate from publishing credentials. The publisher must retain its independent authority/occupancy checks and fail without destructive correction on conflicting assets, foreign drafts or mismatched immutable releases.

Use `operation=resume-publication` with the same producer selectors for a failed publication boundary, and `operation=resume-public` for an already published candidate whose public boundary failed. Preserve every producing and consuming attempt; a consumer does not become the producer. Do not repeat a successful publication to repair an acquisition failure. Do not use a rerun to pretend the workflow source changed: dispatch from corrected integrated main when a controller repair is necessary.

Selected delivery requires latest to identify this candidate. A later legitimate release must not be rolled back to manufacture that result. If another execution already completed this delivery, authenticate its evidence and record completion rather than republishing. If a newer legitimate release has superseded it, retain exact-release verification and report explicit supersession; do not use `allow-superseded` to claim that this candidate passed the initial required-latest obligation.

### D. Prove acquisition and real public use

Use the existing transferred/public small lifecycle, not a second application harness. The implementation owner is `tools/lkjscript-dev/src/release/transferred/pair/lifecycle.rs`, with installation/recovery at its sibling owners. Its public literal authoring and independent expected results give a concrete witness:

- Discover operations from the installed executable; create a command project; plan/review/apply ordinary structural and numerical definitions.
- Run the integer entry with fixed input 41: initial result **42**, reviewed replacement result **43**. Run the F64 input `[1.25e0]`: initial result **1.75**, replacement result **2.75**.
- Submit the modified request with the previous review binding: it must reject, leaving accepted meaning and subsequent results unchanged. Complete the maintained check/build/artifact execution and joined cleanup.

Both anonymous exact and latest routes must acquire the selected bytes, validate container/install identities and execute the maintained small lifecycle outside the language checkout. The ordinary consumer needs neither Cargo nor privileged graph edits. Harness bookkeeping must not implement the language behavior. Preserve literal requests and original observations. This is a designed distribution witness, not evidence of adoption by an independent production application.

The archive's product executable is the static Linux x86-64 runtime. A program `.lkja` bundle executed by that runtime is **not** thereby a self-contained application executable.

Preserve neutral-container compatibility as documented: an older manager may reject it; the new exact bootstrap supplies the upgrade route. Retain authentic predecessor slots and pinned executables. Runtime selection does not migrate application data, reverse graph edits or grant capabilities. Keep valid data encodings and operational roots unchanged; document newly strict malformed-data rejection and recovery without silent rewriting.

### E. Repair only an established blocker

Follow these actual owners rather than introducing parallel machinery:

| Concern | Maintained owner |
| --- | --- |
| Operation selection, job isolation, terminal result | `.github/workflows/release.yml` |
| Producer authentication, source/receipt/artifact admission | `tools/lkjscript-dev/src/release/controller/mod.rs` |
| Authority, occupancy, publication and public acquisition | `tools/lkjscript-dev/src/release/controller/publication.rs` |
| Controller transport and regression fixtures | `tools/lkjscript-dev/src/release/controller/operations.rs`, `tests.rs` |
| Candidate and transferred acceptance | `tools/lkjscript-dev/src/release/`, especially `transferred/pair/` |
| Required coverage and contributor selection | `docs/spec/verification.md`, `tools/lkjscript-dev/src/check/` |
| Procedure, notes and current state | `docs/release.md`, `docs/releases/v0.1.39.md`, `docs/status.md`, `docs/roadmap.md` |

A reproducible controller-only defect permits a focused mainline repair, an independent negative regression at the existing owner, applicable acceptance and a new consumer of still-valid original assets. No such defect was established by the architect; do not assert an unrun probe fails.

A real product, packaged-installer or acceptance-integrity defect invalidates the affected candidate claim. Preserve the failing evidence, repair the actual owner and finalize a corrected candidate through the maintained dependency-complete release acceptance. Do not quietly waive the failing contract. Reuse unoccupied v0.1.39 only when no immutable/tag identity would be rewritten; otherwise choose the next unoccupied patch version and record the explicit additive correction. Update version-bound generated guides through the product. This exceptional route must state why the original candidate cannot safely be delivered; it is not permission to bundle unrelated features.

Expired or genuinely unavailable trusted handoffs require the documented new-candidate or separately authorized trusted-recovery route, not fabricated originals. Missing permissions require a blocked handoff, not an alternate bridge. You may adjust algorithms, decomposition and repair order when evidence contradicts this route, but preserve the selected usable outcome, independent authority and compatibility guarantees.

## 4. Acceptance: claims and falsifying evidence

| Claim | Required proof / failure condition |
| --- | --- |
| The chosen bytes are the accepted product | Authenticate the original producer and all three handoffs; bind inner assets/executable to the candidate receipt and exact source. Wrong attempt/source, missing or ambiguous artifacts, failed required steps or mismatched bytes must reject. Metadata alone is insufficient. |
| Publication is separately authorized | Current annotated object selects accepted source; existing scoped control selects that object; immutability and occupancy prerequisites are satisfied. A commit SHA in the control, conflicting tag or absent authority must not produce publication. |
| Promotion preserves accepted content | Compare accepted inner asset identities with the published three-asset inventory; record release ID, annotation/source and immutable state. No rebuild, manifest rewrite, replacement or foreign-draft takeover. |
| Consumers can actually acquire and use it | Anonymous exact/latest identities and attestations, strict installation, the concrete public lifecycle above, meaningful rejected review, retained prior runtime and joined resources. An upload or checksum alone cannot pass. |
| All selected delivery stages completed | Require actual controller/authority, publication and public results. The normal terminal is `immutable_published_and_public_verified`; a candidate-only green result is insufficient. For split resumption, bind successful publication and subsequent successful public evidence to the same producer rather than rewriting the failed invocation. |
| Repository delivery is accurate | Review and integrate intended repairs/documentation normally; verify remote-main ancestry and record its relation to tested source/controller. Current summaries distinguish accepted, published, public-verified, blocked and superseded states. |

For unchanged source, reuse authenticated hosted candidate acceptance: the maintained release union is the 20-gate fresh source profile plus six final-executable owners, static/pinned-userland, installation/recovery and original-reader obligations. Do not add a second full profile for reassurance. Transfer/public checks protect different acquisition and installation claims.

For actual changes, discover maintained selector semantics, use focused feedback and run dependency-complete required gates after relevant inputs stabilize. Extend existing regression owners when behavior changes. Preserve gate requirements, independent oracles, deadlines and unfavorable results. `check changed` observes working-tree changes, not an arbitrary committed range; a clean-tree result cannot certify a repair. Keep tested product/controller identities separate from later reporting-only commits. No performance, token-cost or end-to-end application claim is selected.

## 5. Integration, retention and stopping

The selected product is already an ancestor of inspected main. Integrate necessary owner repairs, the archived campaign and concise current-state corrections through normal non-forced operations; incorporate intervening work and verify remote main contains the accepted result. A local commit, topic push, PR or enabled auto-merge is not final mainline delivery. Preserve unrelated branches, stashes, worktrees and evidence; stage intended paths only.

Update `docs/status.md` at its current summary and correct the active publication disposition in existing procedure/roadmap locations without rewriting historical failures. The previous “release on explicit request” trigger has fired. Link the original release campaign to this continuation and place new execution facts in clearly separated additions. Do not make the root guidance or another ledger into a release history.

Preserve source, controller, producer/consumer attempts, annotated object, release/asset IDs, actual hashes, successful and failed terminal decisions, essential handoffs and actual expiry. Use configured artifact owners and owned retained storage, not session-only download links. Do not commit binaries, secrets or large logs. Verify preservation before deleting any campaign-owned resource; do not promise indefinite artifact availability.

Completion means authenticated accepted bytes, authorized immutable public release, completed required public acquisition/lifecycle evidence, and accurate integrated repository records. Public upload without a passing public boundary remains partial. Permission/control/immutability blockers do not become product failures; product defects do not become mere publication waits.

While hosted jobs run, perform necessary review, compatibility/documentation reconciliation and evidence preparation. Inspect at meaningful work boundaries; no repeated unchanged polling, endless watches or duplicate candidate runs. If only external completion or unavailable authority remains, return a blocked/partial handoff with the exact preserved lineage, source, run/attempt, last observed job/state, missing gate, retained artifacts/expiry and precise next authorized action. Do not ask for routine approval, promise unattended monitoring or hide remaining in-process repairs behind a request to continue.

No force-push, protection bypass, credential/access-control change, immutability toggle, immutable-history rewrite, purchase, production deployment or destruction of operational data is authorized. Destructive fixtures belong only in owned disposable environments.

## 6. Execution return

Return a concise Japanese account of what became usable; actual changes and evidence-driven deviations; selected/tested product source, controller source and final remote-main revision; fresh, valid reused, failed, cancelled, skipped, unavailable and unrun evidence; compatibility/recovery impact; producer and consumer attempts; tag/release/public-verification state; retained resources and expiry; exact blockers; and the most consequential finding for the next architect.

If normal promotion succeeds, state explicitly that the previously accepted product was not rebuilt and identify the evidence supporting that statement. If it does not, retain an honest failed or blocked ending, not a completion declaration based on a green subjob. If delivery was already completed elsewhere, report the independently reconciled identities without manufacturing extra changes.

## Inspected primary references

Repository paths above refer to inspected main `0bf919f8718dcaaa265e84f73fc93f9d52f88f94` unless a different immutable source is stated. Reconcile live state before executing.

- Baseline: https://github.com/lkjsxc/lkjscript/commit/0bf919f8718dcaaa265e84f73fc93f9d52f88f94
- Candidate ancestry: https://api.github.com/repos/lkjsxc/lkjscript/compare/b05e9e12133ef625b33462123468b9b896885dd4...0bf919f8718dcaaa265e84f73fc93f9d52f88f94
- Release lineage: https://github.com/lkjsxc/lkjscript/blob/0bf919f8718dcaaa265e84f73fc93f9d52f88f94/docs/campaigns/202609201910.md
- Candidate run: https://github.com/lkjsxc/lkjscript/actions/runs/35508727722
- Terminal log owner: https://github.com/lkjsxc/lkjscript/actions/runs/35508727722/job/106087422142
- Artifact inventory: https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/35508727722/artifacts
- Release procedure: https://github.com/lkjsxc/lkjscript/blob/0bf919f8718dcaaa265e84f73fc93f9d52f88f94/docs/release.md
- Release notes: https://github.com/lkjsxc/lkjscript/blob/0bf919f8718dcaaa265e84f73fc93f9d52f88f94/docs/releases/v0.1.39.md
- Scoped-variable API: https://docs.github.com/en/rest/actions/variables
- Immutability-setting API: https://docs.github.com/en/rest/repos/repos#get-repository-immutability-settings

---

## Execution additions — 2026-09-21 UTC

### Reconciliation and preserved lineage

The initial mandate above is unchanged. On resumption, main and remote main both selected
`0bf919f8718dcaaa265e84f73fc93f9d52f88f94`; the index was empty. The existing documentation
edits and this already archived mandate belong to this delivery continuation and are retained.
The mandate matches the supplied original byte for byte. No replacement campaign branch is needed.
The unrelated August stash and the two detached map-baseline evidence worktrees are preserved.
No local build or release process was active at startup. The pinned toolchain is Rust 1.98.0;
the host had approximately 9.5 GiB available memory and 829 GiB free disk.

The selected product is an ancestor of main, three commits behind. The actual descendant diff
contains only the predecessor campaign ending and removal of the completed temporary diagnosis
and guide-generation workflows. Product, dependencies, toolchain, generated guides, maintained
release workflow/controller and verification obligations are identical. The accepted product
source is not relabelled as its reporting descendant.

Authenticated repository reads report enabled immutability, repository push/admin access,
no main branch protection and no applicable main rules. The complete release inventory contained
23 releases and no drafts; v0.1.39 was absent when publication was selected. The prior scoped
control selected annotation `d711b4cb7385243283e4cdc53999c8b65fc2bbf6`, the immutable v0.1.38
release. Retained operator before/after observations show its authorized change to annotation
`7f2d0187c7f3d5f6ce02086fae861020965457e2` at 2026-09-21T23:22:34Z. Independent remote
tag-ref and annotation reads bind that v0.1.39 object to exactly
`b05e9e12133ef625b33462123468b9b896885dd4`; its message carries the capability and compatibility
release notes. The matching existing tag and control are reused, without another write.

The same lineage already dispatched maintained `promote` consumer
[35667263704/1](https://github.com/lkjsxc/lkjscript/actions/runs/35667263704) at
2026-09-21T23:22:35Z from controller source `0bf919f8718dcaaa265e84f73fc93f9d52f88f94`.
Its producer selectors remain `35508727722/1`. No duplicate candidate, consume rehearsal,
promotion or rerun was dispatched during resumption. No credentials, access controls,
protections, immutability setting, operational data or frozen source were changed.

### Original handoffs and preliminary byte inspection

Authenticated fresh reads confirm producer repository/head repository `lkjsxc/lkjscript`, main
workflow-dispatch event, workflow `343592338`, exact source and original attempt. All mandatory
acceptance, construction, original-reader and upload steps succeeded. The original terminal
artifact and job log report `candidate_accepted`; its controller/publication/public jobs were
skipped because it was a candidate-only invocation.

The three original service ZIPs and terminal ZIP are retained under ignored owned storage
`.artifacts/release-202609210911/producer-35508727722-1/`. Their recomputed SHA-256 values match
the authenticated service metadata and the mandate's three essential identities. Exact ZIP
inventories are three public assets, the verifier plus its identity, and the acceptance receipt.
Preliminary inspection is data-only and does not replace maintained controller admission.
It executes neither the candidate nor the transferred verifier and fabricates no hosted context.

The original accepted receipt binds these inner identities:

| Content | Bytes | SHA-256 |
| --- | ---: | --- |
| `lkjscript-x86_64-unknown-linux-musl.tar.gz` | 11125783 | `2e9a9ad52182c609c0309e2718e1080a5534c4f204e764d35b0a8bd4e28d7fa2` |
| `SHA256SUMS` | 109 | `7bb4ee525a6427763daccb20aa1ebe4977ac63a5d23a17196f8bf6605f3e1627` |
| `install.sh` | 3566 | `0112a472a60fe22978caa15b720947b5f10b1c76f1f6b410156b79f68c9972a9` |
| Static product executable | 24418848 | `7b7e66b5c7d13c51bcf00e0d1dee76e4e84fb1cf7c027a3917edc8c83cec2e21` |
| Original candidate verifier | 33873320 | `a8602ffb64962db212bd1e87f31632ef80150b9a4670ed8751cb2a4a06422bfe` |

The receipt records 20 fresh source gates, six final-executable owners, two pinned userlands,
installation/recovery and original-reader admission, complete joined cleanup and no failure.
Those accepted hosted claims are valid reused evidence; no second full profile or candidate
build is selected. The archived earlier failed producer and disputed diagnostics remain failures
and are not consumed. No new product defect has been established.

Operator acquisition context is retained in the sibling `operator/` and
`resumption-20260921T2329Z/` directories, including fresh API responses, original terminal log,
tag/control/immutability observations and repository permissions. Essential producer service
expiry remains 2026-10-04T13:41:29Z, 13:41:30Z and 13:41:31Z respectively for assets, verifier
and acceptance. Local copies have no service expiry but are not an indefinite availability
promise or an alternate authorized recovery route. The 257564516-byte producer diagnostics
artifact remains available at its service owner until 2026-10-04T13:41:32Z; no discrepancy
requires downloading or replaying that broad evidence.

### First promotion and established publication failure

Consumer `35667263704/1` completed with a genuine failed terminal; its API was updated at
2026-09-21T23:29:48Z.
Job `106555691327` successfully authenticated and admitted the exact three original handoffs,
reported `promotion_authorized`, and ran both transferred small installed lifecycles with
joined cleanup. Transfer receipt version 5 reports `fresh_passed`, `boundary-smoke`, acquisition
`simulated`; this is not anonymous public proof. The candidate job was correctly skipped.

Publication job `106557256492` failed at operation 33, `POST /repos/lkjsxc/lkjscript/releases`,
with HTTP 403 `Resource not accessible by integration`. Its original request attempted draft
creation for the existing v0.1.39 tag with `target_commitish` set to the accepted old source.
The runner explicitly granted `Contents: write`, `Actions: read` and `Metadata: read`.
The public job was skipped and terminal job `106557335905` failed; no public completion is
claimed. A fresh complete occupancy read still found no v0.1.39 draft or published release.

The [current GitHub API contract](https://docs.github.com/en/rest/releases/releases#create-a-release)
states that the target parameter is unused when the Git tag exists. It also documents the
workflow-write requirement when an explicit target's workflows differ from the default branch;
the Actions token cannot receive that permission. That documented condition matches the retained
403 and the two retired workflow files in the source/main diff. The causal diagnosis remains
an inference until the corrected normal request succeeds live. The narrow correction belongs
in the existing controller: omit the irrelevant tag-creation parameter and admit draft ownership
through the already independently checked annotation/source, notes marker, bot author and exact
asset inventory. Neither candidate bytes nor publication permission changes. No different
credential, direct publication bridge, setting change or workflow restoration is selected.

All five consumer ZIPs were downloaded by their exact service IDs and their digests checked,
then retained under `.artifacts/release-202609210911/consumer-35667263704-1/`:

| Artifact | ID | Service ZIP SHA-256 | Expiry UTC |
| --- | ---: | --- | --- |
| Controller | 10669504442 | `41de7550ce070b9e0272f205b1bed4581d8b7ed3a0d572c71bd5c3056f1927a7` | 2026-10-05T23:27:41Z |
| Selection | 10669925526 | `7e1c623d38891b22300401b02ab79d0e2942565b874b3e823c18703540b2ba90` | 2026-10-05T23:29:02Z |
| Consumer diagnostics | 10670085382 | `4ef6c92c93310d5cb5f458b0c4a65b9c90af64da955b3e1a1ec27de677cf1698` | 2026-10-05T23:29:05Z |
| Failed publication | 10669594538 | `22c31c6b4c455927720fff2306af4e675b3f2e3c6878b951ebb91ad2d3b9507d` | 2026-10-05T23:29:36Z |
| Failed terminal | 10669234948 | `5f8cc8a491ee052316285c9379be553c615bede07067fb5ee679e0506e27de11` | 2026-10-05T23:29:45Z |

### Bounded controller correction

Only the existing publication owner and its regression fixture change executable behavior.
Release creation omits `target_commitish`; draft admission no longer mistakes that tag-creation
metadata for the authoritative source. The existing annotation/source/control checks, exact
producer ownership notes, bot author, asset identities, repeated authorization and immutable
publication checks remain. No workflow, token grant, handoff, verifier, manifest, installer,
product or data encoding changes.

The independent create-release fixture first reproduced the old request's permission failure:
`existing_annotated_tag_publication_needs_no_historical_workflow_target` failed on unchanged
production code. With the correction, all **16 controller tests passed**, none ignored, including
that regression and the existing genuine handoff, transfer/public workflow, terminal and resume
owners. New negatives cover missing/lightweight/wrong-source/different annotation at creation,
upload and publication boundaries, five foreign-draft ownership mutations and a product commit
incorrectly supplied as the scoped authorization value. Existing conflicting-asset and immutable
idempotent resume tests remain. The fixture's default-main release metadata never changes the
independently resolved product source.

As in the [previous bounded controller correction](202609180528.md#verification-scope),
`check changed` would select the full profile through its unknown-path fallback; that is not
used to replay the already accepted product for this controller-only repair. Validation uses
the actual controller owner, pinned formatting, workspace/all-target/all-feature production
Clippy, maintained no-Python policy and reviewed diff. The maintained release workflow is
unchanged, and the original product acceptance remains bound to b05e9e12. No full-profile,
new target acceptance or broad application rerun is claimed for the reporting/controller source.

Local original failure/pass logs, reviewed source patch, exact tested source hashes and the
test executable selected by Cargo are retained in
`.artifacts/release-202609210911/controller-repair/`. A source-built contributor/controller
tool is permitted tooling, not a second distributable product executable.

Pinned `cargo fmt --all -- --check`, workspace/all-target/all-feature Clippy with `-D warnings`
and the no-Python policy passed. Final staged whitespace admission reports only the initial
mandate's three intentional Markdown hard breaks at lines 3–5; preserving the explicitly required
original bytes takes precedence over rewriting them. All other paths and the execution additions
are whitespace-clean, with no checker-policy change. The policy used the immutable contributor tool
built before the repair; its policy implementation is unchanged. Controller source hashes remained
unchanged after the successful tests. Fresh remote main and protection reads still show the same
base with no intervening commits or required PR path. The repair and reconciled documentation
are ready for normal main integration; public completion remains a separate hosted gate.
