# lkjscript implementation campaign: accept once, promote unchanged, resume the boundary

Observed creation time: **2026-09-18T00:07:25+09:00** (`Asia/Tokyo`, UTC+09:00; live environment clock, cross-checked with a UTC+09:00 time service).
Mandate filename: **`202609180007.md`**.
Repository: `https://github.com/lkjsxc/lkjscript`.
Inspected main: **`108ca2777fc202b543f48816a99b7db519542f97`**.

You are Codex, the implementation and integration owner. Execute this campaign, not another architecture investigation. Make the release path a dependable delivery mechanism for a human-directed, agent-operated general-purpose language. Complete the selected implementation, its proportionate verification, documentation and normal delivery to remote main. This mandate is self-contained; an accompanying root `AGENTS.md` is a replacement candidate, not an additional source of transient tasks.

## 1. Decision, outcome and starting evidence

**Selected outcome:** an agent can nominate an accepted release candidate, promote its exact already-verified assets without rebuilding or repeating broad application acceptance, and resume a failed publication or public-acquisition boundary without restarting successful stages. CI owns admission and an interpretable terminal result. A later architect session is not a routine release-finalization dependency.

This is one release-contract cutover, including the native container/installer consumers that make byte-preserving promotion possible. It is not a new language-feature campaign, a general CI rewrite, an evidence warehouse, or a retrospective audit of every release.

### Live observations, not reset targets

The architect inspected main above through GitHub. It is five commits ahead of release source **T = `7083f9a6d56ed702017942e100c3696fc6f35308`**, with T as its ancestor. That difference contains only root guidance and five documentation/campaign paths. The branch listing contained only main, reported unprotected; the open-PR listing was empty. These observations establish neither your local worktree nor your current permissions or protections. Reconcile newer work; never reset to the inspected SHA. [E1]

**v0.1.38 is already published.** Immutable release **390160031** was published at **2026-09-16T18:11:58Z** (2026-09-17T03:11:58+09:00). Annotated tag object **`d711b4cb7385243283e4cdc53999c8b65fc2bbf6`** selects T. Publisher **35117655769/1** completed successfully at **2026-09-16T18:49:29Z**. All four jobs, including anonymous exact/latest acquisition, attestation checks and the public application pair, report success. The preceding read-only rehearsal **35083514716/1** also succeeded at T. Do not republish this milestone or recreate its already-integrated F64, requirement-library, structural-authoring or transaction-completion implementation. [E2]

The inspected README still describes v0.1.35 as supported, while status/roadmap/predecessor endings describe v0.1.38 publication or manual admission as pending. The publication-pending statements are stale. The architect did **not** run the old full/target/pair original readers, so their historical manual closure must not be invented. Distinguish actual release availability and hosted CI success from that narrower historical gap. Preserve genuine v0.1.36 and v0.1.37 failures. [E3]

### Demonstrated bottleneck and strongest alternative

The publisher's observed job intervals were:

| Existing job | Observed elapsed interval |
| --- | ---: |
| Build, verify, and package | 96 minutes 37 seconds |
| Transferred packaged application verification | 47 minutes 52 seconds |
| Immutable publication | 16 seconds |
| Anonymous public verification | 37 minutes 25 seconds |

These are one run's job wall intervals, not CPU usage, billing or a forecast of savings. The earlier successful rehearsal is additional work. Do not rerun the old pipeline just to manufacture a baseline. [E2]

The implementation explains the repeated work: `Release` rebuilds and verifies on both rehearsal and publication; `docs/release.md` explicitly forbids promotion of rehearsal proof. The source checker runs heavy copied-binary owners; target admission runs six behavioral owners; both transferred and public pairs run a five-owner aggregate. **Within-pair exact/latest deduplication already exists; reuse it rather than claiming it as new work.** The public manifest itself contains `publication_mode` and `annotated_tag_object_sha`, so merely skipping a workflow job cannot turn a dry-run archive into byte-identical published content. [E4–E6]

The strongest alternative is measuring ordinary numerical/data-processing authoring after the delivered F64 milestone, as the roadmap suggests. That remains valuable, but its limiting dependency has not been measured here. Repeated delivery overhead is observed in both code and completed jobs and affects every subsequent capability. Remove this specific recurring burden now, then return to useful language/program development without a compulsory second infrastructure campaign.

## 2. Startup, authority and finite historical reconciliation

Before edits, inspect actual HEAD/branch, index and worktree, relevant untracked/ignored evidence, stashes, worktrees, remotes, divergence, scoped guidance and active local/remote jobs. Check the pinned toolchain and resources before builds. Earlier local release-simplification work may exist despite no remote branch: preserve and reuse it when it serves this outcome. Do not create another implementation/reporting lineage merely for a new timestamp.

Read the relevant ending of `docs/campaigns/202609162154.md`, current release/status owners, and actual code at the paths below. Classify inherited work as implemented, integrated, published, historically unverified, blocked or superseded. Refresh the selected release/run once; expand only when facts could change this campaign.

The user authorizes ordinary non-forced integration into remote main and the configured **non-publishing** CI needed to verify this campaign. Prefer main when safe; otherwise use one justified isolated lineage and finish the permitted merge path. A local commit, branch push or open PR is not mainline completion. Preserve unrelated changes and operational data; stage only intended paths.

**This campaign does not authorize a new public release, remote tag creation, release-control-variable mutation, deployment, access/credential/protection changes, purchases, force pushes, rewriting immutable history or live-data migration/destruction.** Implement the future publication path completely, but exercise its writes only through owned local fixtures. Do not create a public version bump simply to test infrastructure.

Reconcile the supplied `AGENTS.md` with current/scoped instructions. Its durable change is to separate content, acceptance and publication authority, eliminate compulsory later manual closure, and resume valid stages. Preserve useful knowledge and concurrent guidance. An active session must explicitly account for changed guidance; editing a file does not reload instructions. If the candidate is not present, apply those same limited durable changes from this mandate rather than waiting for an attachment. Archive this mandate unchanged under `docs/campaigns/` using its filename; append short clearly separated execution notes. Preserve both originals on a real collision.

### Preservation is not a new prerequisite chain

Two expiring publisher handoffs were downloaded by the architect through the authenticated connector and their ZIP hashes matched GitHub metadata. No binary was executed. Their original identities are:

| Artifact ID | Original name | ZIP SHA-256 |
| --- | --- | --- |
| 10459961469 | `release-handoff-35117655769-1` | `0c62234e0dc4383d55188376f16b16c7d5957e733c8952a36e7a994167ffc376` |
| 10460061486 | `verifier-handoff-35117655769-1` | `04d2921eecdbd4084d6d9ef29187800ca5f3f4dbf8c284f9ac7a6bf73bb2ea5c` |

GitHub listed expiry at 2026-09-17T17:23:40Z and 17:23:41Z respectively. The archive member hash matched the published archive's `fa10e7afb09c06e4a243f421050a0497ca9acd331cdf34c046650c85aef377aa`; installer and checksum members also matched public asset metadata. This is integrity preservation, not old-reader or behavioral admission. [E2, E7]

Use available authenticated copies for a genuine diagnostic/compatibility need. The accompanying preservation bundle is optional and this implementation must not depend on it. Other original artifacts are listed by E7. Preserve endangered unique evidence when readily available, but do not make downloading all historical ZIPs, reconstructing old paths/environments, replaying applications or rerunning old fault matrices a gate to this cutover. Record unavailable historical material honestly. Supersede the *recurring manual obligation*, not the actual failed history or a real product defect.

## 3. Chosen product and release contract

### A. Neutral content, separate publication authority

Introduce an explicitly distinguished, canonical **publication-neutral release-manifest encoding** at the existing `src/release_container/` owner. The new writer emits one current encoding. Its content binds the product version/intended tag, actual source commit, target/build policy and relevant toolchain/lock inputs, executable identity, license/notices and packaging facts. Use an unambiguous format discriminator; select its exact unused identifier after inspecting current consumers. This is a container-reader identity, not an extra human-facing language/runtime version; reconcile the product-surface policy without changing that public version contract.

A candidate's content must not vary with publication mode, the presence of a remote tag, annotated tag-object identity, CI consumption attempt, promotion permission or a later reporting commit. Move publication-event and authorization facts out of the archive into the release operation's existing private evidence/decision boundary. An intended version/tag is a content constraint, not evidence that the tag exists or publication is authorized.

Finalize the archive, checksum asset and generated bootstrap **before authoritative distributable acceptance**. Once accepted, all three assets remain byte-identical through transfer and publication. Promote them without rebuilding the executable, regenerating notices, repacking the archive, changing the manifest or rerendering the bootstrap. Independent deterministic-package comparisons are still legitimate; they are not a reason to rebuild the product for promotion.

Changing source, version/tag, required build configuration, executable, archive, installer or relevant acceptance inputs selects new or appropriately revalidated work. Never patch an accepted receipt to disguise changed content. An old source-bound proof remains about its original source even when current main advances.

### B. One coherent coverage plan, not repeated full-suite labels

Keep the existing check registry and behavioral owners. Partition release acceptance into **source-specific proof** and **exact-final-candidate proof**. Preserve the union of meaningful existing guarantees; change their scheduling and bindings, not their expected semantics.

The source tier retains source/reference tests, default/all-feature distinctions where they matter, lint/safe-Rust and tooling policy, checker correctness, generated-asset and maintained-consumer guarantees. The candidate tier runs the six current target behavioral owners against the immutable executable extracted from the final archive, together with independent static ELF admission, both pinned userland boundaries and the relevant installer/recovery contract.

Move duplicate external `--binary` application invocations out of the release source tier when the same owner can protect its claim on the final candidate. Inspect any embedded source-only probes and retain those separately. Do not assume every host test equals target execution. A genuinely different required build configuration or environment retains its distinct proof with an explicit reason.

Implement the partition through the existing registry/policy/receipt machinery, not another scheduler. Preserve `check full` as an honest standalone complete profile; do not quietly shrink it while continuing to report the former full proof or 26 fresh gates. If a release-source profile is needed, give it its actual identity and require its dependency-complete union with final-candidate acceptance at the release owner. Document a concise old-claim-to-new-owner mapping. A required protection cannot be skipped because a new label sounds equivalent.

The broad candidate application owners run **once per accepted candidate/configuration**, not again because the bytes cross a job boundary or become public. Keep the installer/two-version recovery and pinned-userland behaviors once where they protect a distinct candidate property. Transfer/public stages retain their actual transport and installation claims. Source unit/reference work and necessary host verifier builds are not counted as forbidden second product builds.

### C. Candidate production and terminal acceptance

Factor the current ordering, in which `prepare` demands target acceptance before producing the final archive, into content construction followed by final-content admission. Incomplete packages may exist in owned temporary storage, but they must not acquire an accepted or promotable result before all required source/candidate/container/installer checks and joined cleanup pass.

Run the existing original readers in the owning CI context while their required originals and modes are available. An accepted outcome must result from actual maintained-reader admission, not only `jq` of self-declared `passed` fields. Tests must establish that a forged, incomplete or weakened producer result cannot get through.

Extend the existing release receipt/handoff only as necessary to communicate an authenticated terminal decision and its exact bindings: product source, controller/workflow source where different, acceptance contract/workload/environment, verifier identity, candidate/assets, producing repository, run/attempt and artifact identities. Separate the producer attempt from later consumer attempts. Record incomplete, failed and cancelled stages before returning; a successful child with failed cleanup is not accepted.

Validate handoff inventory and digests after transfer. The trusted producer's admitted terminal result may authorize reuse of unchanged behavioral evidence; every consumer need not reconstruct the producer's filesystem or run its applications. Do not claim diagnostic archives are completely relocatable/replayable unless actually proved. Retain what acceptance, provenance and diagnosis need without building another historical evidence system.

### D. Explicit promotion with a trusted controller

Provide an explicit candidate-selection path and an explicit promotion path that nominates an **exact producer run and attempt**, not the latest artifact bearing a convenient name. These are required capabilities to implement, not claims that corresponding CLI verbs already exist.

The production promotion controller must come from a trusted, normally integrated mainline workflow definition. Pin and record its actual revision separately from the product source. Remove the tag-triggered rebuild path for future releases; retain ordinary annotated tags and existing scoped publication authorization as publication prerequisites, not as build selectors. Do not replace it with automatic execution of artifacts from arbitrary PRs or `workflow_run` events.

Before granting publication eligibility, authenticate the repository and head repository, permitted event/workflow and controller provenance, exact producer run/attempt and successful required jobs, terminal acceptance contract, artifact IDs and service digests, and the expected asset/verifier hashes. Require the product source to be reachable from the freshly resolved remote main. The nominated tag must match the candidate version and peel to that exact source; independently validate the annotated object and the existing immutability authorization/control. A main descendant is not interchangeable product source. A caller-supplied hash, artifact name or self-declared success must not be able to select a different trusted producer.

The privileged publisher continues to run **no candidate or transferred verifier and no scripts taken from the candidate handoff**. Any executable admission happens in a read-only job after provenance validation, without publishing credentials. Keep write permissions scoped to the publisher and retain strict inventories, bounded parsing and pinned trusted operations there. Do not add a new credential or weaken the current immutability/access model. Attestations establish provenance, not semantic correctness or permission by themselves. [X1]

A promotion controller update may have a different SHA from the candidate builder. Record and test that relationship; never misdescribe the promotion workflow SHA as the product build source. Preserve GitHub's supported release-attestation checks on the actual subject bytes and tag. No network request or credential used only for publication belongs in ordinary program execution or native installation.

### E. Small transfer/public checks and truthful terminal states

For transfer and anonymous acquisition, independently verify exact inventories, trusted expected identities, strict native container admission, bootstrap/installation behavior and a small ordinary create/edit/build/run lifecycle. Keep exact-version and latest acquisition distinct. Do not run the five-owner aggregate again merely to decorate those boundaries with fresh receipts.

Reuse the existing safe bootstrap and native installer; do not invent a parallel shell parser or require Cargo, Python or a checkout to use the product. The once-per-candidate installation acceptance covers upgrade/recovery; later boundary smoke proves that received assets install and execute the expected small program.

Serialize operations that mutate or verify shared publication/latest state, while allowing independent read-only candidate work. Check the immutable exact release independently of the mutable latest alias. If latest moves to a different release, record that actual identity and a distinct superseded/not-applicable latest result where appropriate; never execute or certify different bytes as this candidate. During a selected latest-publication acceptance, an unexpected latest identity cannot silently count as its success.

Expose terminal distinctions for candidate accepted, promotion authorized or rejected, immutable assets published, and public verification passed/failed/unavailable. A job graph with skipped mandatory stages cannot produce the final successful state. Local fixture success is not GitHub publication success. An uploaded release alone is not public verification.

### F. Resume only the invalidated boundary

An explicitly selected resume consumes the original accepted candidate and original producing identities. A transient publication/API failure retries publication after fresh authority and occupancy checks. If immutable matching assets already exist, verify them and continue at the missing public check without changing them. A partial draft may be completed only when its ownership, source, inventory and existing bytes match; reject conflicting or foreign state without deletion/overwrite. A published mismatch always rejects.

A public acquisition/smoke failure repeats only the failed boundary while the previous acceptance remains valid. A reproducible product/installer defect invalidates the affected candidate acceptance and requires a real correction; it is not an exception allowing broken software through. Changed relevant source/build/verifier/workload/environment/trust inputs invalidate their affected evidence. A later documentation or controller-only change does not automatically invalidate unrelated product behavior, but must receive its own appropriate proof.

Preserve original producer run/attempt values when a later attempt consumes earlier artifacts. GitHub reruns retain the original event SHA/ref; they are not a way to load a fixed workflow from newer main. Support a new explicit consumer invocation when needed rather than requiring all jobs to rerun. [X2]

Set the essential candidate assets, verifier and terminal acceptance handoff to a documented **14-day resumption retention request**, using the existing artifact mechanism; record the actual service expiry and limitations. Do not create an unlimited archive service. Missing or expired trusted material produces a clear unavailable result and a concrete new-candidate or trusted-recovery action, not a silent rebuild labeled as reuse. For a published release, read-only public rechecking may acquire its immutable public bytes and a separately trusted compatible verifier; it does not manufacture missing historical acceptance.

## 4. Compatibility, maintained owners and implementation order

**No language meaning, Graph, compiler/bytecode, program-artifact or application-data generation bump is selected.** This changes distribution content/evidence and its native readers. The one-product-executable workflow, runtime-dependent artifact distinction, exact grants and data authority remain unchanged.

Retain strict read-only admission for authentic legacy release manifests and existing installation slots. Never reinterpret legacy `dry-run` as proof of publication, default away missing identities, or loosen `deny_unknown_fields`/canonical validation globally. New neutral content has an explicit format identity; old and new encodings cannot be ambiguous. Old writers remain only in their frozen source/history, not as a second normal production mode.

The updated executable/installer reads supported legacy archives and new content. An older manager may clearly reject the new format before changing an installation; do not promise forward compatibility it does not have. The exact bootstrap extracts the new manager and provides the supported upgrade path. Preserve existing pinned runtime paths, immutable slots, idempotent installs and recovery selection. Do not migrate installed legacy receipts, rewrite old archives or touch real application data. Record publication provenance as unverified/neutral where appropriate instead of inventing authenticity from package metadata.

Follow callers and scoped guidance. The inspected starting owners are:

| Boundary | Maintained owners to change or reconcile |
| --- | --- |
| Content and native admission | `src/release_container/model.rs`, `src/release_container/mod.rs`; strict archive/ELF tests and `src/platform/installation/` |
| Construction, proof and source selection | `tools/lkjscript-dev/src/release/{mod,model,archive,target,admission,verifier}.rs`, `source_tests.rs`, adjacent submodules |
| Installer and transferred/public behavior | `tools/lkjscript-dev/src/release/bootstrap/`, `bootstrap.rs`, `transferred.rs`, `transferred/pair.rs`, `transferred/pair/recovery.rs` |
| Coverage and dependencies | `tools/lkjscript-dev/src/check/{registry,policy,model,executor}.rs` and existing checker tests |
| Hosted state and authorization | `.github/workflows/release.yml`, existing release-control and publication procedure |
| Semantics and navigation | `docs/spec/verification.md`, relevant product-surface/install specifications, `docs/release.md`, README, status/roadmap, root guidance |

First establish the neutral encoding, compatibility and proof partition; then implement construction/admission/portable terminal consumption; then wire actual candidate/promotion/resume jobs and small smoke; then reconcile all consumers, docs and generated assets. Change this dependency order where local evidence warrants it, not the selected guarantees. Avoid unrelated parser/runtime refactors and workflow-version churn.

Explicitly replace the current requirements that the publisher rerun fresh full/target/installed proof after a successful rehearsal, that dry-run versus publication change archive content, that each transferred/public pair rerun the broad aggregate, and that routine public closure requires a later manual reconstruction campaign. Preserve their original historical records. Remove obsolete current producers, parameters and acceptance assumptions when their consumers have transitioned; do not leave two normal paths with no retirement condition.

Update current README/status claims to the actual v0.1.38 availability and hosted success, with the historical manual-admission limitation plainly separated. Retire stale “publication still due/no further feature deferral” obligations where actual publication or this explicit process revision supersedes them. Do not stamp new CI semantics onto old evidence or require a retrospective success fiction before development can resume.

## 5. Acceptance: evidence that can falsify the selected outcome

### Coverage and actual executable behavior

Use focused compilation/tests while iterating. On stabilized inputs, choose **one dependency-complete final acceptance route** through the revised owner. Prefer the final non-publishing hosted candidate for the distributable claims rather than requiring an equivalent complete local rehearsal first. Honor actual protections. A local source full plus hosted source full plus repeated target/public suites is not the default.

These are established discovery/feedback entry points, not a mandatory sequential script:

```sh
cargo build --release --locked -p lkjscript-dev
cargo run --release --locked -p lkjscript-dev -- check changed --machine
cargo run --release --locked -p lkjscript-dev -- check full --fresh --machine
target/release/lkjscript-dev release target
```

Discover release/check arguments and focused test names in their dispatchers. `check changed` examines current Git-status paths, so an empty committed tree's result cannot certify committed implementation. Confirm focused test selection actually ran the intended tests. Select any compiled test executable from Cargo output and keep candidate/verifier copies immutable across other builds.

Prove the source/candidate coverage union against an independently reviewed inventory of the affected current claims. The six target owners, static-linkage and both pinned userland guarantees, required independent reference behavior, installer recovery, maintained consumers and generated/policy gates must have real owners and required outcomes. Missing/unavailable/failed required proof cannot become a pass. Record a retained extra run only when it covers a distinct claim. Do not reduce workload sizes, weaken assertions or silently increase deadlines to produce an apparent speedup.

Outside the language checkout, extract the final candidate archive, install into an owned fresh prefix, and use only the installed product and documented discovery to author an ordinary standard-library-dependent program. Starting from a recipe is permitted, but add or change real logic through reviewed public operations. Choose a simple independently calculated result, then a requirement variation with a different independently calculated result; retain literal requests/plans and both outputs. Build and run the artifact from a new process, with authoring directories unavailable when claiming standalone execution. Verify the rejected edit/install leaves the previous accepted program/selection usable. Use no hidden host application or privileged graph edit. This is a distribution/composition witness, not a claim of independent-agent usability or adoption.

Exercise legacy archive installation, new neutral installation, old-reader rejection and new-bootstrap recovery on disposable prefixes. Retain native bounds, no links/traversal/extra members, exact modes, checksum/ELF/version checks and no replacement of conflicting immutable slots. A new manager's behavior is tested; do not assume old managers accept its new format.

### Promotion, rejection and resume tests

Extend existing release/source/reader tests with independent Git/API fixtures and the real controller path. Keep tests grouped by meaningful boundaries rather than building a cartesian matrix of every historical omission.

| Claim | Required falsification and positive recovery |
| --- | --- |
| Content is immutable across promotion | Compare archive, installer, checksum and extracted executable before/after consumption; changing approval, tag existence or consumer attempt cannot change them. A one-byte asset change rejects. |
| Acceptance is source/configuration-bound | Wrong source, target/policy, verifier or workload identity rejects even with plausible/rehashed metadata. Complete restored genuine input succeeds. A mere main descendant cannot substitute for the candidate. |
| Trust is not self-assertion | Foreign repository/fork, disallowed producer event/workflow, wrong run/attempt, missing/skipped/failed acceptance job and fake “passed” summary reject. Detect a tampered producer result through the maintained reader. |
| Publication has independent authority | Wrong version, lightweight/moved/wrong-source annotated tag, missing/mismatching scoped authorization and source not on main reject before writes. Exercise equality, ancestor and divergent Git cases with known expected relations. |
| Failure remains failure | Timeout, cancellation, failed child or incomplete cleanup never yields an accepted terminal result. Missing/expired handoffs remain unavailable, not silently rebuilt. Restored valid inputs recover. |
| Resume does not redo successful work | Fail a publication/API boundary and a public-smoke boundary separately; recover at each boundary with the same accepted producer. Observe zero product builds and zero heavy application-owner invocations during each resume. |
| Existing remote state is safe | Matching immutable release is idempotent; matching owned partial draft can resume; mismatching draft/assets or immutable content rejects without overwrite, deletion or retagging. |
| Public identity is truthful | Exact and latest wrong-source/wrong-bytes cases reject before candidate execution. A genuinely superseded latest alias is reported as such, not as this candidate's success. |

Use a test process/API adapter that independently records or refuses forbidden build/heavy-owner invocations during consumption and resume; do not rely solely on the new orchestrator's counters. Exercise the **actual workflow/controller logic**, not only a parallel model or YAML string snapshots. The privileged path must not execute a canary supplied as a candidate/verifier/handoff script, and no test should expose real credentials.

### Hosted non-publishing integration proof

Run the completed candidate path at the actual new source with all required acceptance, content finalization and terminal handoff. Then exercise a separate **read-only consumer invocation** that selects those real producer artifacts by exact identity and uses the actual promotion admission path, without builds or broad semantic replay. Pin producer/consumer sources separately. Confirm the three asset byte identities across both runs and report actual stage invocation counts and wall intervals.

Because the checkout may still carry already-occupied version 0.1.38, private test packaging of the new source is not the public v0.1.38 release and must never be promoted to that occupied identity. The real remote publication preflight must reject that mismatch. Demonstrate the positive authorized publication/write sequence through the same controller with an isolated fake GitHub state and disposable Git objects where the intended identity is unused and authorization is valid. Do not mutate the actual remote tag/control to make the test pass. A private content candidate can be behaviorally accepted while remaining ineligible for a particular public tag.

Also exercise post-public acquisition logic against locally served exact frozen assets and fake API metadata, including injected failures. Such fixtures prove implementation behavior, not new GitHub release availability or live attestation issuance. Existing v0.1.38 metadata may be used for read-only compatibility checks; do not replace its inputs or replay its full applications.

Report measured results without promising a percentage improvement. The historical job intervals are context with different source/environment, not a controlled speed comparison. Actual elimination of repeated invocations and reuse of exact assets are primary acceptance facts. Retain unfavorable timing and infrastructure limits.

## 6. Mainline completion and the explicit release decision

**Deliver the working cutover to remote main. Do not publish a new release in this campaign.** v0.1.38 already delivers the combined language milestone. The new neutral distribution format and pipeline receive a non-publishing proof now; their first actual public use is triggered by the next selected useful public capability, important product fix, or explicit user request. Select that future version/source before candidate creation, then use this same promotion path. Do not attach another arbitrary “one more campaign” deadline or create a release solely to exercise CI.

The implementation is not complete if only a disabled neutral core, test-only promotion, shell TODO or documentation proposal exists. The production candidate, promotion and stage-resume paths must be wired and the ordinary future operator procedure must be executable. Non-publishing and fixture proof are the deliberately selected verification boundaries; live new-format publication and subsequent public acceptance remain **unexercised**, not failed and not claimed passed.

Before integration, refresh remote main and actual protections, preserve intervening work and validate consequential interactions. Integrate by normal fast-forward/merge or the required completed PR path. Confirm remote main contains the accepted result, report its actual SHA and ancestry/tree relation to the verified source, and separately validate later guidance/reporting edits. A squash/cherry-pick is not exact-source proof transfer. Never force shared history or bypass gates.

Retire only campaign-owned branches/worktrees after confirming integration or justified supersession, preserving unique work and necessary originals, checking unchanged tips and active dependencies. Do not remove unrelated stashes, ignored operational files or retained evidence to manufacture cleanliness. No report-only lineage is needed for a frozen candidate whose immutable source remains available.

## 7. Adaptation, stopping and final report

If newer main or local work already implements part of this outcome, reuse it and complete only the missing behavior/proof/transition. If the entire selected outcome is already demonstrably complete, do not rebuild it or invent a new feature: reconcile that fact, finish any justified documentation/lineage work, and report it.

You may revise implementation tactics or a falsified mechanism with a concise evidence-based explanation while preserving the outcome, exact-byte/trust guarantees and authorization limits. A real implementation defect must be fixed rather than reclassified as release-only. Retire obsolete ceremony explicitly; do not silently skip protections or promise hypothetical rollback.

When a healthy external CI stage is the sole remaining dependency, check at useful work boundaries, preserve the exact run/attempt and return its observed state and concrete next action. No sleep/status loop, repeated unchanged logs or unattended monitoring. Integrate independently main-ready work where permitted, but label the campaign's missing required hosted proof as incomplete rather than claiming success. Permission/API/retention blockers need precise scope and resumption instructions, not a fresh unrelated campaign.

Return a concise Japanese report with: the newly possible release operation; actual implementation/verified/remote-main revisions; fresh and valid-reused tests with failures/unrun limits; old-to-new claim placement and measured invocation changes; distribution compatibility/recovery impact; v0.1.38's actual historical/public state; the explicit absence of a new release and unexercised live boundary; cleanup/retention reasons; and the most useful concrete finding for the next architect. Put transient identities and short execution notes in this campaign or existing status owners, not root guidance. Once this outcome passes, useful product development can resume without another routine release-reconciliation campaign.

## Evidence and primary-source pointers

Repository observations were read at the inspected revision unless an API endpoint is explicitly live. Paths below are navigation, not a command to restore older source. Retrieve only the portions needed for changed-state reconciliation.

- **E1 — main and integration:** `https://api.github.com/repos/lkjsxc/lkjscript/branches/main`; `https://api.github.com/repos/lkjsxc/lkjscript/branches?per_page=100`; `https://api.github.com/repos/lkjsxc/lkjscript/pulls?state=open&per_page=30`; `https://github.com/lkjsxc/lkjscript/compare/7083f9a6d56ed702017942e100c3696fc6f35308...108ca2777fc202b543f48816a99b7db519542f97`.
- **E2 — actual publication and timing:** `https://github.com/lkjsxc/lkjscript/releases/tag/v0.1.38`; `https://api.github.com/repos/lkjsxc/lkjscript/releases/390160031`; `https://github.com/lkjsxc/lkjscript/actions/runs/35117655769`; `https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/35117655769/jobs?per_page=10`; `https://github.com/lkjsxc/lkjscript/actions/runs/35083514716`.
- **E3 — inherited status and ending:** `README.md`, `docs/status.md`, `docs/roadmap.md`, and the final execution/resumption sections of `docs/campaigns/202609162154.md` at `108ca2777fc202b543f48816a99b7db519542f97`. Example: `https://github.com/lkjsxc/lkjscript/blob/108ca2777fc202b543f48816a99b7db519542f97/docs/campaigns/202609162154.md`.
- **E4 — current release contract:** `https://github.com/lkjsxc/lkjscript/blob/108ca2777fc202b543f48816a99b7db519542f97/.github/workflows/release.yml`; same revision `docs/release.md` (source proof, hosted dry run, public closure) and `tools/lkjscript-dev/src/release/mod.rs` (prepare/source facts).
- **E5 — content versus permission coupling:** `https://github.com/lkjsxc/lkjscript/blob/108ca2777fc202b543f48816a99b7db519542f97/src/release_container/model.rs`; same revision native container, installation, transferred pair and release source tests.
- **E6 — existing claim owners:** `https://github.com/lkjsxc/lkjscript/blob/108ca2777fc202b543f48816a99b7db519542f97/tools/lkjscript-dev/src/check/registry.rs`; same revision `docs/spec/verification.md`, `tools/lkjscript-dev/src/release/admission.rs` and `transferred/pair.rs`.
- **E7 — original artifact identities/expiry:** `https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/35117655769/artifacts?per_page=20`. Artifact preservation establishes byte identity against this authenticated observation, not independent semantic admission.
- **X1 — GitHub provenance semantics:** `https://docs.github.com/en/actions/concepts/security/artifact-attestations`. Verified attestations bind provenance and must be evaluated under an explicit trust policy; they are not a guarantee that the artifact is secure.
- **X2 — GitHub rerun semantics:** `https://docs.github.com/en/actions/how-tos/manage-workflow-runs/re-run-workflows-and-jobs`. Reruns retain the original event SHA/ref and initial actor privileges; validate the actual producer/consumer attempt relationship instead of assuming a rerun loads current main.

---

## Execution reconciliation — 2026-09-17 UTC

The original mandate above was already present at startup (SHA-256
`a79a0bcc02974ffda07e4ae5a607dba847d113847a9c3db0e29b10f88f0fd00c`).
Local main and freshly fetched remote main both selected `108ca2777fc202b543f48816a99b7db519542f97`;
the supplied root guidance was the only tracked modification. Its durable changes are accepted
for this execution. There is one working tree and an unrelated historical stash, both retained.
The untracked original `202609170822.md` is preserved unchanged; its compulsory later manual
closure is superseded by this mandate, not asserted as completed. No prior local implementation
of this cutover or active release/build job was found. Rust/Cargo 1.98.0 are available via
`/home/coder/.cargo/bin`; initial available memory was about 6.8 GiB and disk about 836 GiB.

Fresh authenticated service reads confirm immutable v0.1.38 release `390160031`, published
2026-09-16T18:11:58Z, and successful publisher `35117655769/1` at source
`7083f9a6d56ed702017942e100c3696fc6f35308`. All four jobs succeeded. This establishes public
availability and hosted success; this execution has not admitted that run's old original readers.
The language milestone is implemented, integrated and published. Genuine predecessor failures
and the narrower historical manual-admission gap remain history. No release/tag/control write
is authorized or planned in this campaign.

## Implementation and proof placement

The one mutable lineage remains main. The current writer uses the closed neutral container format
`lkjscript-release-content-1`; strict legacy canonical decoding preserves installed receipts.
`prepare` constructs the final three assets without claiming acceptance. The new candidate owner
admits source originals, extracts the final executable, runs target/installation owners and admits
their originals before publishing an accepted terminal. Interrupted target and pinned-bootstrap
containers have durable exact ownership and a parent cleanup path; failed cleanup cannot accept.

`full` retains its 26 gates. The release union is the independently checked 20-gate source profile
plus the same six target behavioral owners, static ELF, both pinned userlands, installation and
two-version recovery. Default release lifecycle and all-feature workspace proof remain distinct.
No workload size, assertion or individual behavioral deadline is reduced. The old five-owner
transferred producer is retired; its historical diagnostic reader remains. Transfer/public owners
perform strict content admission and small installed lifecycles. The original within-pair exact/latest
deduplication is retained, not claimed as a new optimization.

The integrated workflow has explicit candidate, exact-run/attempt read-only consumption, promotion,
publication-resume and public-resume operations. The separate trusted controller authenticates
provider job/artifact identities and retains the authenticated ZIPs so that every subsequent consumer
can compare its terminal and selected bytes with those original members. Its process adapter permits
only bounded GitHub CLI, curl and unzip operations; candidate/verifier/script execution is absent
from the privileged publisher. Scoped publication authority and actual annotated source are checked
independently of acceptance. Private 0.1.38 content remains ineligible for the occupied public tag.

Essential assets, verifier and terminal request 14 days of artifact retention; actual service expiry
is recorded by selection. Missing trusted inputs report unavailable, without silent rebuilding.
Current README/status/roadmap now distinguish actual v0.1.38 hosted/public success from the old
manual-reader gap. The next public use is triggered by a useful capability, important fix or explicit
request; this campaign does not select another release.

Focused local evidence is retained under `.artifacts/release-cutover-202609180007/`, outside tracked
narrative. Initial native container tests passed 6/6; installation tests passed 13, with one internal
subprocess fixture intentionally ignored as a standalone test. Checker tests passed 13/13 after its
fixture replaced a cross-filesystem hard-link assumption with an immutable copy. Early development
failures (incomplete concurrent edits, stale internal-version/count expectations and the fixture
link failure) remain in their original logs. Final focused results and hosted source/consumer
identities will be recorded separately below; these local results are not final distributable proof.

The unchanged predecessor mandate has four intentional Markdown hard-break lines (3–6) that
`git diff --cached --check` reports as trailing whitespace. Its initial SHA-256 remains
`c5038fbdf81c47176714781541c7a7f338687176e03d303845348d4a86b0e1dc`; preserving the original
mandate takes precedence over rewriting those bytes. Other staged paths pass whitespace admission.

### Stabilized local verification

The final contributor test executable was selected from Cargo's JSON build report and copied before
execution. It passed 139 tests, with zero failures and 15 explicitly ignored fixture/measurement
entry points (2.14 seconds). Workspace/all-target/all-feature clippy with `-D warnings` and formatting
passed. The actual workflow passed actionlint 1.7.12, whose archive was checked against authenticated
GitHub metadata. These results include the real controller's Git/API/ZIP admission, source/configuration
and tag/authority rejection, partial-draft/immutable-state recovery, public exact/latest faults,
forged original-reader inputs, cancellation/cleanup rejection, and restored genuine inputs. Tests
execute the actual workflow terminal and smoke scripts; the independent failing/recovering process
adapter refuses product builds and broad owners. The privileged canary is never executed.

A separate focused real lifecycle used the already authenticated public v0.1.38 archive, SHA-256
`fa10e7afb09c06e4a243f421050a0497ca9acd331cdf34c046650c85aef377aa`, outside the checkout.
Its unchanged bootstrap installed into an owned prefix, then 30 public operations demonstrated
standard-library composition, I64 42 → 43, rejected edit with 43 retained, F64 1.75 → 2.75, and
standalone artifact result 43 after the authored project was removed. Literal requests/plans,
outputs, omitted/substituted original-reader failures and healthy restoration are retained.
The lifecycle measured 13.355090553 seconds; the whole fixture measured 21.09 seconds. It invoked
zero product builds and zero broad behavioral owners. This tests the new small boundary machinery;
it does not substitute for the required neutral-candidate hosted proof or establish a performance
comparison with the historical publisher's different workload/environment.

The first lifecycle run failed its reader's command-name expectation, and the first complete
contributor run failed a synthetic userland fixture's inherited file mode. Both fixtures were
corrected to match independently inspected product/producer behavior; no product assertion was
weakened. Failed originals and their successful recovery remain retained. The two lifecycle roots
are additionally preserved in the ignored 65 MiB diagnostic archive; no diagnostic replayability
claim is made. Broad local full/target/application acceptance was deliberately not repeated.
