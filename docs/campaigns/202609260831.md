# Immutable deployment builds

## Original user mandate

https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscript  について進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。最も筋の良い方向に進むために、限界まで深く考えてほしい。超長期的な視点からお願いしたい。どれだけ時間がかかっても構いません。ChatGPTの開発環境として、cw-bd235945fff44468a0f41292を用意しました。自由に活用ください。

## Reconciliation

The specified host maps to Home Coder workspace `tomato-ocelot-73`.
The checkout is `/home/coder/workspace/lkjscript`, on main at
`3a34c305119ca7c1dc34ee7326ebe5b960d2a0b2`, with no remote divergence.
There are no stashes or additional worktrees. The unrelated untracked
`docs/campaigns/202609242022.md` is preserved. No product/check jobs were active.
The pinned toolchain is Rust 1.98.0; this environment has 8 GiB memory.
Root guidance is unchanged.

The predecessor's web starter, source acceptance and mainline integration are
complete. Original v0.1.47 candidate `36198402289/1` remains in progress at exact
product/controller `18e04bc0e6611ff0b79a49da1e5ce5da3632da35`, most recently building
its final distributable. Do not restart healthy work or relabel that source.
Public v0.1.46 remains unchanged. Promotion is actionable only after candidate
acceptance. No production deployment is authorized by this campaign.

## Selected direction

The predecessor's real edit loop required an unused bundle name and a manually
copied/repointed deployment descriptor after every change. Select the recurring
development loop rather than another large starter or speculative browser backend.

Add `build --deployment PATH` as an alternative to unchanged create-new
`build --output PATH`. Observe one strict operator descriptor, compile the current
accepted graph and statically admit the selected target and exact grants.
Publish a content-addressed complete artifact in the template's artifact parent,
then a content-addressed descriptor beside the template. Change only the artifact
value; preserve other values and omissions, original files and relative data roots.
No secrets/adapters/listeners are opened and no process is selected or restarted.

Identical outputs may be reused only after bounded exact-byte comparison. Hash
names are not authority. Existing conflicting bytes, nonregular files, symlinks
and invalid parents reject without overwrite. Inspect both outputs before
publication; a later failure may retain an inert complete artifact, not a partial
descriptor. Report actual visibility/durability/cleanup. The caller explicitly
runs the returned deployment, retaining earlier pairs for recovery. Re-selecting
an earlier program neither rolls back nor migrates application data.

## Acceptance and delivery

Require public copied-binary command and web edit/rebuild/reuse/restart workflows,
an unchanged already-running old server, original descriptor preservation,
static rejection before external effects, output conflict and concurrent reuse
cases, and persistent-data-root preservation. Keep ordinary --output rejection.
Maintain discovery, executable guide inputs and user-facing instructions.
Run focused checks while developing and dependency-complete fresh acceptance once
inputs stabilize. Integrate normally into main and verify the remote independently.
Source, candidate, publication and subsequent reporting retain separate identities.

## Implementation and focused observations

The new platform child module reuses strict deployment decoding, current normalized
preparation, static target/runner/grant admission and the existing create-new output
owner. It introduces no new language primitive, package resolver or application
renderer. Additional overlap admission prevents outputs inside a declared local
data/object/queue root. JSON values and omissions survive rebinding; directory
placement, not an absolute-path rewrite, preserves operational roots.

The first shell-background attempt failed because the tool's nohup wrapper treated
`export` as an executable. A subsequent long streaming connection ended without a
terminal result; do not interpret its compilation line as acceptance. The first
foreground compile exposed a test-only use of nonexistent rustix `mkfifo`; the
fixture now uses pinned `mkfifoat`. These originals are retained under
`.artifacts/202609260831/`. A later build finished but lost its stream before test
execution; observing no owned Cargo/test jobs preceded the explicit execution.

`unit-execution.log` records 8 passing new tests, zero failures, including eight
concurrent exact publishers, wrong bytes, symlinks, directories, FIFO, byte/path
bounds, malformed templates and local-root separation. `public-execution.log`
records all 4 new copied-binary workflows passing in 56.97 seconds. Four independent
command builds converge on one exact artifact/descriptor pair. Legacy output
conflicts and preflight descriptor conflicts preserve originals. Static mismatch
cases reject without publishing, while absent secrets and uninitialized data do
not block an otherwise valid static build.

The durable-data witness authors a normal native task, saves through a real data
adapter, makes an identity-preserving native function edit, builds again without
changing data HEAD, removes its graph and invokes both new and retained old bundles
against the same verified store identity. Selecting code is not data rollback.
The web witness edits page-title through draft/plan/apply, passes all 107 native
tests with differential equality, serves old/new processes independently, confirms
that the old page stays unchanged, then removes the graph and restarts both exact
snapshot files. Five real HTTP responses and four joined server shutdowns pass.
This is HTTP/lifecycle evidence; no new live-browser rendering claim is added.

These focused working-tree observations precede the version bump and generated
asset update. Development now selects 0.1.48 without changing the original 0.1.47
candidate or its product source. Broad source acceptance remains a separate step.

## Original v0.1.47 candidate and promotion

Independent service metadata and the downloaded essential original terminal now
confirm candidate `36198402289/1` as `candidate_accepted`, phase complete, with
cleanup complete and no failure. Product/controller source remains exactly
`18e04bc0e6611ff0b79a49da1e5ce5da3632da35`. Its 20 source gates, six target owners,
two pinned userlands, installation/recovery and original readers are complete.
The service retains original assets, verifier and acceptance handoff through
2026-10-09; this date is observed retention, not a permanent availability promise.

Fresh publication preflight found main at `3a34c305119ca7c1dc34ee7326ebe5b960d2a0b2`,
no v0.1.47 tag/release, no active release job, no branch protection/rulesets,
enabled immutable releases, and the completed immutable v0.1.46 release owning
selection `1551a2da98c120c98d69c2ccd88d4de940073457`. The first tag command rejected
because the new Coder shell lacked Git committer configuration; it created no tag.
A per-command identity matching the established repository commits/tags completed
the normal annotated tag without changing global configuration. Its notes exactly
match accepted blob `1214535ff56a22dc09556d6975765f58c9346278`, with Markdown retained.

The pushed annotated object is `79a87148d7d5ccb2b1b6960a0a26d0afd832c588` and peels
to the original accepted product source. An independent GitHub tag read confirms
both the annotation and source. After immediately rereading/comparing the prior
scoped non-secret selection, the authorized update selected that object and was
read back before dispatch. This is a checked read/write/readback, not atomic CAS.
No credentials, immutability or protection settings changed.

Exactly one `operation=promote` dispatch created `36203046819/1` at
2026-09-25T23:58:20Z, with controller source
`3a34c305119ca7c1dc34ee7326ebe5b960d2a0b2` and original producer selectors
`36198402289/1`. Promotion is pending; no public-v0.1.47 claim is made here.
The candidate executable and assets are neither rebuilt nor rewritten.

## Stabilized source preparation

The 0.1.48 product and contributor verifier compile successfully in the pinned
toolchain (`build-0148.log`, 1m23s). Product discovery regenerated all eight
maintained reference pages. The observed capability digest is
`e86fe2ee5768c262611d63a4459720d82f47ec5d99689db34d1b08f8c18af79a` and discovery
exposes both mutually exclusive build selectors. Release notes, the web editing
loop and the semantic CLI contract describe the exact operational boundaries.

## Complete v0.1.47 publication

The original promotion `36203046819/1` completed all selected jobs, including
anonymous exact/latest acquisition and installed smoke, and its terminal. The
candidate job was intentionally skipped. Independent exact/latest metadata select
immutable ordinary release `396991797`, published at 2026-09-26T00:04:57Z, with
three original asset digests matching the accepted producer. Product source remains
`18e04bc0e6611ff0b79a49da1e5ce5da3632da35`; promotion controller remains
`3a34c305119ca7c1dc34ee7326ebe5b960d2a0b2`. No candidate, public asset or earlier
release was rebuilt or rewritten. The preceding pending observations remain history.

## Privacy counterexample and correction

The prototype implementation was committed locally as
`7009061def5a3cf2d846c1882f5104c90bedc675`, not pushed. Its fresh source owner is
`.artifacts/lkjscript-dev/check/1790380928226667166-933052-0`, launched from an
immutable verifier with Cargo/checker concurrency each one. That source remains
frozen while a detached corrective worktree under the campaign's ignored artifacts
is used. The original check is a prototype observation, not permission to deliver a
known-defective product. Its eventual terminal must not be relabeled as corrected
source acceptance.

A literal copied-executable relative-path witness runs with an empty environment
and PATH, creates a command project in a path containing spaces, passes 76 native
tests with differential equality, builds twice and observes exact reuse, then
runs the returned relative deployment and gets `hello`. The first creation omitted
an explicit logical name and correctly rejected a name containing spaces; using
the discovered `--name` option fixes that fixture, not the product.

Review then reproduced a genuine problem: an operator descriptor with mode 0600
was copied into a new descriptor with mode 0640 under the host's umask. A controlled
child-only umask 000 oracle reproduced mode 0666 and unsafe exact reuse. The
unchanged driver is `.artifacts/202609260831/private-descriptor.mjs`; its corrected
baseline output `privacy-original.json` has four failed and four passed assertions.
It independently checks POSIX modes and actual public-command effects. Its first
attempt mistakenly supplied --project to standalone run and was rejected; that
harness failure is retained in `privacy-before.json` and is not the counterexample.
Fixtures contain no actual secrets and affect only newly owned disposable paths.

The correction adds a private mode to the existing create-new output owner rather
than copying its publication algorithm. It creates the private stage at 0600 before
writing, never a chmod-after-publication window. Only deployment descriptors use
that path; ordinary artifacts retain existing permissions. Exact descriptor reuse
also checks current effective ownership and absence of all group/other bits before
reading bytes. Broad or foreign-owned existing files reject before artifact
publication, with no permission, ownership or byte changes. Explicit operator
repair or sharing is outside build. The receipt reports `access=owner-only`.

A new unit regression and copied-executable workflow cover permissive umask,
group/other read/write/execute bits, original-file preservation, preflight ordering,
explicit repair and unchanged raw-output policy. The public test changes umask only
inside a child shell, not the parallel test process. Corrective compilation,
focused reruns, the unchanged independent oracle and fresh source acceptance are
still required; the existing functional passes alone do not prove this correction.

## Continued descriptor-admission review

Continuation observes the original prototype source tier complete: 20 passed,
20 fresh, zero reuse, 1652.653 seconds. This remains proof for the prototype,
not the later privacy correction. The privacy implementation and reporting
commits were fast-forwarded locally through
`a59158809aec13566aa4fb9d65b36b358916f568`; remote main was not changed.
A separate corrective driver starts focused public tests, an unchanged independent
privacy oracle and then its own fresh source tier. Long foreground tool streaming
closed during the unit compilation; a compiler-finished line alone is not a
completed unit-test result.

A new independent literal public-CLI oracle,
`.artifacts/202609260831/duplicate-descriptor.mjs`, then reproduces an existing
common-reader defect through a copied development executable. Of five cases,
two valid distinct-key controls pass and three duplicate-key cases are wrongly
accepted: identical values, conflicting values and an escaped spelling of the
same decoded key. Original inputs and JSON results remain under `duplicate-before`
and `duplicate-before.json`. The copied executable is retained as the observed
baseline; its version label alone is not a new exact-source attestation.

Typed BTreeMap deserialization silently selected the last configuration value,
while the raw schema observer used another permissive JSON map. The correction
reuses the existing strict JSON reader at that raw observation boundary instead
of adding a third parser or a build-only rule. It preserves the full u64 execution
range and signed configuration range. Byte-derived structural budgets do not
add a lower descriptor-size ceiling. New unit and copied-public-CLI regressions
cover equal/conflicting/escaped duplicates, integer extremes, quoted JSON-shaped
text, all three build/run/serve entry points, unchanged source HEAD and no output
publication before rejection. The configuration specification also no longer
advertises an unsupported Bytes value variant.

An attempted cancellation of the queued corrective driver was refused by the
tool before execution. No cancellation is claimed. The main checkout remains
frozen for that driver's original observations; the existing detached corrective
worktree is reused for this additional repair. Neither an earlier functional pass
nor a later pass for a source without this fix permits delivery of the known
ambiguous-reader behavior. Corrected-source proof and integration remain required.

Select the existing hosted `Verify` full-profile owner for this combined repair,
using one campaign-owned `verify/202609260831-deployment-snapshots` ref. That
isolates final acceptance from the already-running local predecessor and avoids
concurrent Cargo writers in one target directory. Its read-only workflow binds
the exact event source and requires all 26 gates freshly, stable inputs and zero
reuse. Hosted acceptance will not be called a local pass, and the older local
source tier is not substituted for it. Integrate the accepted exact source by
normal fast-forward after required proof, then retire this campaign ref without
disturbing older unrelated verification branches. No candidate or release is
published by the verification workflow.

## Corrected-source focused results and hosted handoff

Exact combined implementation `e5847f1e568ad823325dd3a1e56c7a6fd64fa3fa` is preserved
on remote `verify/202609260831-deployment-snapshots`. Its single push-triggered
[Verify 36206083541/1](https://github.com/lkjsxc/lkjscript/actions/runs/36206083541)
started at 2026-09-26T00:46:42Z. The last successful hosted observation, at
approximately 00:51:48Z, has job `108302807605` in pinned-toolchain/verifier
provisioning. No full-profile success, source acceptance, main integration or
v0.1.48 candidate/publication is claimed.

A separate local focused driver uses the pinned Rust 1.98.0 toolchain, one Cargo
job, offline locked dependencies and an independent target directory. Development
and test debug symbols are disabled to bound memory; these are unoptimized focused
observations, not proof for a final optimized distributable. The source and profile
are recorded by `.artifacts/202609260831/final-focused-result.json`: source e5847f1e,
step `complete`, exit 0. It proves:

- All 24 deployment unit tests pass, including the existing policy boundaries,
  snapshot file tests, duplicate-key rejection and exact integer/string controls.
- All six copied-executable public deployment-build workflows pass in 18.06 seconds.
  The new regression rejects six duplicate configurations through each of build,
  standalone run and standalone serve, while preserving HEAD and publishing no output.
- The unchanged independent permissions oracle passes all eight checks, including
  child-only umask 000, rejection before artifact publication, no automatic chmod,
  explicit owner repair and unchanged ordinary-output permissions. Its original
  four failed checks remain failed in `privacy-original.json`.
- The unchanged independent duplicate-key oracle now passes all five cases: its two
  valid controls still succeed and all three formerly accepted ambiguities reject.
  The three original failures remain in `duplicate-before.json`.

The exact copied development executable is retained under `final-focused-executable/`.
Original logs and results are `final-focused-unit.log`, `final-focused-public.log`,
`final-private.json` and `duplicate-corrected.json`. Node and shell code are external
launch/assertion drivers, not application dependencies or program generators.

A further independent public recovery oracle, `late-publication.mjs`, passes all
11 checks in `late-publication.json`. In a fresh non-root-owned fixture it makes
only the descriptor directory unwritable, leaving the artifact child writable.
The public build reports the late failure and the retained complete artifact;
no partial descriptor or stage is exposed, and template/accepted HEAD stay intact.
After an explicit fixture-owner permission repair, retry reuses the exact artifact,
creates one owner-only descriptor and runs the literal expected `hello` after the
authoring project is moved aside. This verifies the documented non-atomic pair
failure boundary rather than claiming rollback. All permission changes affect
only this disposable fixture, not a live deployment or service.

The predecessor a5915880 local driver separately passes its five optimized public
workflows in 6.50 seconds. Its later source tier is not substituted for e5847f1e.
The original foreground unit invocation emitted a finished-compilation line but
no test result; the 24-test focused result above is independently complete.

A later combined status read (hosted job, predecessor receipts, remote refresh)
was refused by the tool before execution. That refusal provides no new CI or
remote state and is not retried through another route. At the last successful
branch observation remote main remained `3a34c305119ca7c1dc34ee7326ebe5b960d2a0b2`,
local main was a5915880 (four commits ahead), and the corrected worktree was clean
at e5847f1e before this reporting-only append. The unrelated untracked
`docs/campaigns/202609242022.md`, original evidence, existing unrelated verification
refs and the running predecessor driver are preserved.

Resume from this exact lineage: obtain the original hosted run's terminal and
original full receipt; require source e5847f1e, all 26 fresh passes, stable inputs
and no reuse before normal mainline integration. Reconcile the predecessor driver
and actual remote main first. This reporting descendant does not relabel the tested
source and must not be pushed to the verify ref merely to trigger a duplicate full
run. After accepted integration, independently confirm remote ancestry and retire
only this campaign's unchanged verification ref/worktree when no jobs or unique
resources depend on them. Then perform the configured v0.1.48 candidate workflow
once; candidate and public acceptance remain separate. Public v0.1.47 and all of
its immutable original assets remain complete and unchanged.
