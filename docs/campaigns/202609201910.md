# v0.1.39 public release

## Incoming request (verbatim)

@GitHub  リリースもお願いしていいですか？

## Execution additions — 2026-09-20 19:10 Asia/Tokyo

The human explicitly requests publication of the accepted mainline work. Startup main is
`9d2e262afdced3d52b8f24a2e4e40c4c1ebf723b`, a reporting-only descendant of the fresh-full
accepted implementation `36b9846dfc0a7d94368e83dba5c2f785f55a808f`. Root guidance is unchanged
(blob `b3fde2334f145146ddaec04219cfd586654c9a49`); the current release owner and workflow
were read. Immutable release `390160031` is v0.1.38; v0.1.39 has no tag at the startup check.
Choose v0.1.39 for the next public milestone. Do not reuse occupied v0.1.38 or relabel its assets.

Scope: publish persistent ordered maps, reviewed callable-input evolution, the HTTP response
correction, exact data pagination, strict data admission and the stack repairs already on main.
The release-container cutover is also new relative to v0.1.38. Annotation text is retained in
`docs/releases/v0.1.39.md`. No new language feature, data migration or production deployment
is selected. Current main's source acceptance does not replace final distributable acceptance.

Capability findings: connected repository reads, content/tree/commit writes and non-forced
branch updates exist. The exposed tools do not dispatch workflows, create annotated tags or
update repository variables. The alternate local container and Python-backed execution probes
returned transport timeouts before commands could run; no local checkout or credential state
was observed. Plugin discovery found the already installed GitHub integration, not an alternate
usable execution provider. No permission denial is to be routed around.

Use a narrowly scoped, temporary main-push bridge to perform only the deterministic root
package/lock version change and dispatch the existing `Release` workflow with
`operation=candidate`, `ref=main`. GitHub supports authenticated workflow_dispatch from
GITHUB_TOKEN; the maintained release workflow and its workflow_dispatch trust boundary stay
unchanged. The bridge performs no candidate execution with write credentials and has no tag,
release, variable, secret, immutability or protection mutation. Its prepare job writes only the
two inspected version fields; its separate dispatch job has no contents-write permission.
Both reject moved sources and unexpected inputs. Retire the bridge after its selected candidate
has been identified; preserve the exact candidate source and run/attempt first.

Full publication still requires the accepted producer, an ordinary annotated v0.1.39 tag,
and the existing `LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA` control bound to that exact
object. This control is not weakened, substituted or automatically rewritten by the bridge.
The connected tools do not expose that variable operation. Resolve it through an authorized
operator if no supported route becomes available. Keep genuine pending/failed results distinct.

Next: integrate the reviewed bridge and notes non-forcibly, inspect its exact terminal and version
diff, identify the one candidate run, and let the maintained 20 source gates, six final-binary
owners, target/installation/original-reader acceptance protect the eventual publication. Reuse
matching healthy work; do not rerun broad full acceptance merely to launch a release.

## Candidate dispatched; publication still incomplete

Bridge source `32688dc2297c07b7fa84e6b0d53335b7a9a2dd12` reached main by an explicit
non-forced update. Its [run 35504401739/1](https://github.com/lkjsxc/lkjscript/actions/runs/35504401739)
completed both jobs successfully. Prepare job `106061649035` checked the exact predecessor,
manifest/lock/workflow blobs, absent tag and current main, and wrote only two version fields.
Independent commit inspection confirms that candidate source
`2bb72e4864485b73dc6aa152fb16ac34d4552285`, tree
`ad77c9cfd87f4634badd5e0866368353af1d8e38`, changes only the root package version and its
Cargo.lock entry from 0.1.38 to 0.1.39 relative to the bridge source. No dependency changed.
Dispatch job `106061670660` then invoked the unmodified release.yml candidate operation.

[Release 35504414463/1](https://github.com/lkjsxc/lkjscript/actions/runs/35504414463) is the one
observed candidate producer: workflow `343592338`, event `workflow_dispatch`, branch `main`,
exact source `2bb72e48`, created 2026-09-20T10:12:30Z. The last job inspection found
`Accept final candidate` (`106061683222`) running at host-tool compilation after successful
checkout and pinned-toolchain installation. Source acceptance, final static product, target
owners and candidate handoffs were still pending, not passed. Consumer, publisher and public
jobs are intentionally skipped for this candidate-only operation. Do not dispatch another
candidate or relabel the earlier mainline full result as this version's release acceptance.

The dispatch job exposed only the documented non-secret scoped authorization value:
`d711b4cb7385243283e4cdc53999c8b65fc2bbf6`. An independent Git-tag read confirms that this
is the v0.1.38 annotation selecting `7083f9a6d56ed702017942e100c3696fc6f35308`, not authority
for v0.1.39. It remains unchanged. After candidate acceptance, an authorized operator must
create the new annotated tag, recheck prior control/occupancy and update that exact scoped
variable using a supported settings operation. Never replace this independent authorization
with a workflow literal or derive it silently from the candidate.

This reporting-only cleanup removes the completed temporary bridge from current main; it
remains in the immutable candidate source. The release controller explicitly admits its
source as an ancestor of freshly resolved main, so this cleanup neither changes frozen inputs
nor invalidates candidate selection. The maintained Release workflow is unchanged. No new
mutable branch, tag, public release, production deployment or operational-data change occurred.
Original job logs remain on GitHub; local execution/evidence packaging is unavailable here.

Resume with the terminal and artifacts of `35504414463/1` at exact `2bb72e48`; if accepted,
retain all three essential handoffs and their actual expiry, then follow docs/release.md for
annotated-tag/scoped-control selection, consume/promote and anonymous public verification.
The candidate is not yet accepted, the release is not yet public, and this session promises
no unattended monitoring or later publication. Candidate completion is the current external
dependency; the scoped-variable write route is a separate unresolved publication prerequisite.

## Continued publication request and diagnostic reconciliation — 2026-09-20 20:16 Asia/Tokyo

### Additional incoming instruction (verbatim)

@GitHub  最新バージョンを配布するところまでよろしくお願いします。どれだけ時間がかかろうとも構いません

### Observed state, not inherited chat claims

Live main already contained the version change and bridge retirement (`419a44b5`). The earlier
chat claiming v0.1.38 remained the main package and no candidate was dispatched was incorrect.
The original candidate `35504414463/1` has now failed source acceptance: 19 of 20 gates passed,
`workspace_tests` failed, and final product construction and candidate acceptance were skipped.
The original result remains failed; no accepted v0.1.39 assets exist from it.

Original diagnostic artifact `10603825475`, `candidate-diagnostics-35504414463-1`, was
independently authenticated by repository, run/attempt, event, workflow, source and service ZIP
SHA-256 `5aed778e6c4704c652c0eadc1b589b4de39988f17eb991972451e16cf8a2b9c9`.
The observed expiry is 2026-10-04T10:47:11Z. Its source receipt is
`.artifacts/lkjscript-dev/check/1789899541488498295-6227-0/receipt.json`.
A connector download was retained as `lkjscript-release-039-failure-35504414463.zip`;
local execution remains unavailable due to transport timeouts, not a clean local checkout.

A temporary read-only workflow inspected that original ZIP without executing its contents.
The first inspection `35506762954/1` failed on ambiguous nested receipt-name selection;
`35506806986/1` corrected selection to the exact receipt and displayed the original failure.
No product acceptance was rerun or relaxed by those diagnostic operations.

There is a material discrepancy in the retrieved original evidence. Its workspace log names
`tests::pure_preparation_capacity_fails_before_reference_recursion`, asserts the string
`type_instantiation_storage`, and attributes a panic to `tests/public_cli.rs:1100`.
Its Cargo log also names `tests/map_predecessor.rs`. Independent connector reads and a fresh
checkout of exact `2bb72e48` in `35507147230/1` found neither reported symbol nor the latter
file. The reported source location is an unrelated authored fixture, and the captured input
manifest also lacks `tests/map_predecessor.rs`. The artifact's source claim and input-stable
flag do not resolve this discrepancy. Its failure is retained, but it cannot justify changing
a nonexistent test, accepting a candidate, or guessing a compiler defect. No cause or actor
is inferred from the disagreement.

Commit `96bddf5d34d44b40f18ad1ddf5790dab67773b65` changes only the temporary diagnosis
workflow to compile and enumerate the actual `public_cli` executable from exact `2bb72e48`
in new Cargo home/target directories, using the pinned default profile and no published
credentials. It selects the test executable from Cargo JSON, retains source and binary hashes,
and runs the nonempty test suite without changing assertions or raising runtime limits.
This is a focused independent reproduction, not final candidate or public-release acceptance.
Next: inspect that terminal result and its concrete actual failures; only then select a
necessary correction or a fresh trustworthy candidate. Preserve the original failed run.

The maintained Release workflow, acceptance requirements and independent tag-control variable
remain unchanged. No v0.1.39 tag, release, production deployment or data migration has occurred.
The variable-write operation remains a separate unresolved publication dependency; never
substitute a workflow literal, derive approval from candidate bytes, or alter access controls.

## Generated-guide repair and corrected candidate — 2026-09-20 20:47 Asia/Tokyo

The fresh isolated reproduction is [35507363939/1](https://github.com/lkjsxc/lkjscript/actions/runs/35507363939),
job `106069334268`. Its actual binary lists 53 public CLI tests, including
`generated_public_guides_match_executable`, and not the reported preparation-capacity symbol.
The guide test fails; the diagnostic invocation reaches its own 900-second outer timeout
before all tests complete. It is not a complete test pass. Its original artifact is
`10604067896`, SHA-256 `d1108a1120dea73d2ae75818db434b917620d06cd327f075a882025a1e48c7c7`.
The maintained source gate retains its own existing deadline and full selection, not that
ad-hoc diagnostic timeout. No test, runtime limit or acceptance requirement was relaxed.

The concrete defect is version-bound discovery: Cargo selects 0.1.39 but all eight generated
guides retain 0.1.38 headers. The product's exact-byte checker independently returns exit 2
and `capabilities_generated_drift`. The first generation helper `35508041506/1` mistakenly
looked for this diagnostic on stderr; it is on stdout. Read-only original inspection
`35508356394/1` confirms that error. Its original artifact `10604253456` has SHA-256
`1f64ce628458f6e935f2815435abe9fd9ad540c830afdbf2a5390348c2609841`. The failed helper changed
no guides and dispatched no candidate; it is not a product failure or accepted generation.

The corrected [generation 35508544776/1](https://github.com/lkjsxc/lkjscript/actions/runs/35508544776)
passed all three jobs. It used the exact product source `2bb72e48` without write credentials,
required the observed negative control, generated all guides through `capabilities --generate-docs`,
checked the exact eight-file inventory and unchanged bodies, and passed the actual one-test
`generated_public_guides_match_executable` regression. A separate text-only integration job
verified the service artifact digest and original context, then committed only eight generated
headers by a normal non-forced update. Independent commit inspection confirms version and
capability digest changes only. Generated bytes and original logs are in artifact `10604458419`,
SHA-256 `dcd016b36d6ba0673137949895570393cd4cdb4105a45ba5959a6272afd0d966`, observed available
until 2026-10-04T11:45:26Z. Generation is focused proof, not final distributable acceptance.

The corrected candidate is **`b05e9e12133ef625b33462123468b9b896885dd4`**, tree
`d15dab56bcc93ac4bb889e94bb84c10607b0a8ad`. Its one maintained
[Release 35508727722/1](https://github.com/lkjsxc/lkjscript/actions/runs/35508727722) was created
at 2026-09-20T11:45:46Z with event `workflow_dispatch`, workflow `343592338`, operation
`candidate`, and branch main. It is in progress, not accepted or public. The previous candidate's
failed or inconsistent diagnostics are not reused. Future resumption starts with this exact run,
not a duplicate build, the old failed candidate, or an assumed successful test report.

Both temporary diagnostic/generation workflows have completed their jobs and may now be retired
from current main, preserving their frozen source and authentic evidence. Such removal/reporting
must remain a descendant-only change; it must not rewrite the candidate source or relabel its
proof. The maintained Release workflow and all product/verification source are unchanged.

Publication is still incomplete. After this exact candidate is accepted, authenticate its three
essential handoffs, select its annotated tag, check current immutability/occupancy and set the
existing scoped authorization variable to that tag object's SHA through an authorized settings
operation. The present connector cannot perform the variable write or administration-only
immutability-setting read; local execution probes still time out. A source commit SHA is not
an annotation SHA, and replacing the independent control with a workflow literal is not a
valid continuation. No release, tag, installed executable, operational root or access control
has been changed in this continuation.

## Accepted candidate and delivery continuation — 2026-09-21 UTC

The corrected producer `35508727722/1` completed successfully with terminal `candidate_accepted`.
The [delivery continuation](202609210911.md) supersedes the preceding in-progress observation,
preserves the original failures, and selects unchanged accepted assets for authorized publication.
This environment can read enabled repository immutability and update the existing scoped control;
the former environment's capability limitation is not a current product failure. New execution
facts and the public terminal belong to that continuation.
