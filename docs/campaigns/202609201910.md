# v0.1.39 public release

## Incoming request (verbatim)

@GitHub  リリースもお願いしていいですか？

## Execution additions — 2026-09-20 19:10 Asia/Tokyo

The human explicitly requests publication of the accepted mainline work. Startup main is
`9d2e262afdced3d52b8f24a2e4e40c4c1ebf723b`, a reporting-only descendant of the fresh-full
accepted implementation `36b9846dfc0a7d94368e83dba5c2f785f55a808f`. Root guidance is unchanged
(blob `b3fde2334f145146ddaec04219cfd586654c9a49`); the current release owner and workflow
were read. Immutable release `390160031` is v0.1.38; v0.1.39 has no tag at the startup check.
Choose v0.1.39 for the next public milestone. Do not reuse occupied v0.1.38 or relabel its assets.

Scope: publish persistent ordered maps, reviewed callable-input evolution, the HTTP response
correction, exact data pagination, strict data admission and the stack repairs already on main.
The release-container cutover is also new relative to v0.1.38. Annotation text is retained in
`docs/releases/v0.1.39.md`. No new language feature, data migration or production deployment
is selected. Current main's source acceptance does not replace final distributable acceptance.

Capability findings: connected repository reads, content/tree/commit writes and non-forced
branch updates exist. The exposed tools do not dispatch workflows, create annotated tags or
update repository variables. The alternate local container and Python-backed execution probes
returned transport timeouts before commands could run; no local checkout or credential state
was observed. Plugin discovery found the already installed GitHub integration, not an alternate
usable execution provider. No permission denial is to be routed around.

Use a narrowly scoped, temporary main-push bridge to perform only the deterministic root
package/lock version change and dispatch the existing `Release` workflow with
`operation=candidate`, `ref=main`. GitHub supports authenticated workflow_dispatch from
GITHUB_TOKEN; the maintained release workflow and its workflow_dispatch trust boundary stay
unchanged. The bridge performs no candidate execution with write credentials and has no tag,
release, variable, secret, immutability or protection mutation. Its prepare job writes only the
two inspected version fields; its separate dispatch job has no contents-write permission.
Both reject moved sources and unexpected inputs. Retire the bridge after its selected candidate
has been identified; preserve the exact candidate source and run/attempt first.

Full publication still requires the accepted producer, an ordinary annotated v0.1.39 tag,
and the existing `LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA` control bound to that exact
object. This control is not weakened, substituted or automatically rewritten by the bridge.
The connected tools do not expose that variable operation. Resolve it through an authorized
operator if no supported route becomes available. Keep genuine pending/failed results distinct.

Next: integrate the reviewed bridge and notes non-forcibly, inspect its exact terminal and version
diff, identify the one candidate run, and let the maintained 20 source gates, six final-binary
owners, target/installation/original-reader acceptance protect the eventual publication. Reuse
matching healthy work; do not rerun broad full acceptance merely to launch a release.

## Candidate dispatched; publication still incomplete

Bridge source `32688dc2297c07b7fa84e6b0d53335b7a9a2dd12` reached main by an explicit
non-forced update. Its [run 35504401739/1](https://github.com/lkjsxc/lkjscript/actions/runs/35504401739)
completed both jobs successfully. Prepare job `106061649035` checked the exact predecessor,
manifest/lock/workflow blobs, absent tag and current main, and wrote only two version fields.
Independent commit inspection confirms that candidate source
`2bb72e4864485b73dc6aa152fb16ac34d4552285`, tree
`ad77c9cfd87f4634badd5e0866368353af1d8e38`, changes only the root package version and its
Cargo.lock entry from 0.1.38 to 0.1.39 relative to the bridge source. No dependency changed.
Dispatch job `106061670660` then invoked the unmodified release.yml candidate operation.

[Release 35504414463/1](https://github.com/lkjsxc/lkjscript/actions/runs/35504414463) is the one
observed candidate producer: workflow `343592338`, event `workflow_dispatch`, branch `main`,
exact source `2bb72e48`, created 2026-09-20T10:12:30Z. The last job inspection found
`Accept final candidate` (`106061683222`) running at host-tool compilation after successful
checkout and pinned-toolchain installation. Source acceptance, final static product, target
owners and candidate handoffs were still pending, not passed. Consumer, publisher and public
jobs are intentionally skipped for this candidate-only operation. Do not dispatch another
candidate or relabel the earlier mainline full result as this version's release acceptance.

The dispatch job exposed only the documented non-secret scoped authorization value:
`d711b4cb7385243283e4cdc53999c8b65fc2bbf6`. An independent Git-tag read confirms that this
is the v0.1.38 annotation selecting `7083f9a6d56ed702017942e100c3696fc6f35308`, not authority
for v0.1.39. It remains unchanged. After candidate acceptance, an authorized operator must
create the new annotated tag, recheck prior control/occupancy and update that exact scoped
variable using a supported settings operation. Never replace this independent authorization
with a workflow literal or derive it silently from the candidate.

This reporting-only cleanup removes the completed temporary bridge from current main; it
remains in the immutable candidate source. The release controller explicitly admits its
source as an ancestor of freshly resolved main, so this cleanup neither changes frozen inputs
nor invalidates candidate selection. The maintained Release workflow is unchanged. No new
mutable branch, tag, public release, production deployment or operational-data change occurred.
Original job logs remain on GitHub; local execution/evidence packaging is unavailable here.

Resume with the terminal and artifacts of `35504414463/1` at exact `2bb72e48`; if accepted,
retain all three essential handoffs and their actual expiry, then follow docs/release.md for
annotated-tag/scoped-control selection, consume/promote and anonymous public verification.
The candidate is not yet accepted, the release is not yet public, and this session promises
no unattended monitoring or later publication. Candidate completion is the current external
dependency; the scoped-variable write route is a separate unresolved publication prerequisite.
