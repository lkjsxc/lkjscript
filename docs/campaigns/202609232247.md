# Native development-tool adoption

## Original mandate (unchanged)

https://github.com/lkjsxc/lkjscript@Remote Desktop Commander  lkjsxc/lkjscriptについて進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。限界まで深く考えてほしい。超長期的な視点からお願いしたい。ChatGPTの開発環境として、cw-bd235945fff44468a0f41292を用意しました。自由に活用ください。

## Reconciliation and selected work

Observed clean main: `9db66daaf91767d3c1504a6ce03d20cfa12777af`.
The v0.1.44 publication obligation is complete. The prior stopping point is
superseded by this new implementation mandate, not rewritten as unfinished work.
Orbloam remains an independent experiment. Native user authoring is available;
compiler self-hosting and a wholesale Rust rewrite are not selected.

Implement a real first-party development tool in ordinary lkjscript: operation
and diagnostic guide rendering. Rust retains registry observation, typed input
admission, execution and derived-file publication. Native accepted meaning owns
layout, field rendering and escaping. Retire the replaced Rust renderers, retain
the seven/then-six remaining renderer boundaries honestly, and use no special
instruction or document-specific compiler rule. Public copied-executable use,
canonical re-entry and independent expected results must exercise the native tool.

The fresh authorized device has 12 visible CPUs and 8 GiB memory. Install the
repository-pinned Rust 1.98.0 without modifying shell configuration. Existing
GitHub connection owns delivery because the fresh device has no Git credentials.
Preserve full failed outputs in the campaign's ignored artifact directory.

## Implemented boundary

The accepted native project owns `markup` and `guides`, with 332 root owners,
eight native tests and the exact v0.1.44 standard supplier's 45 tests. It builds
the 794,397-byte code-complete `generated/guides.lkja` artifact. Native functions
own balanced Text concatenation, escaped HTML tables/code blocks, typed row
projection, header and complete operation/diagnostic layout. Both public guide
commands actually execute this embedded artifact; the two old Rust formatters
are deleted. The other six document outputs remain byte-identical.

The shared internal pure-artifact invocation has no ambient grants, no source
repository and no reference-equality claim. It uses normal target/type/JSON
admission, checks both task identity and component requirements before execution,
and preserves cancellation and output limits. The host uses ordinary trusted
foreground policy, not mandatory cumulative metering. Its registry adapter only
observes fields and calls the native tool; no compiler or external-document opcode
was introduced. Artifact/graph/data contracts and the standard supplier are unchanged.

`tests/public_cli/native_guides.rs` extends the existing workspace verification
owner: exact canonical artifact regeneration, 53 differential tests, copied-product
fresh authoring, explicit independent expected text, canonical no-op re-entry,
accepted native edits and old/new detached execution after deleting authoring
inputs/project/transport. It clears PATH and external process environment. Native
adapter tests independently cover escaping, columns/order, typed-record rejection,
cancellation and result bounds. The shared runner tests reject real capability
components and empty-effect tasks while accepting the corresponding pure callable.

Focused debug tests passed for the initial four adapter cases, the shared runner,
and both public native lifecycles. The first lifecycle-test attempt incorrectly
requested a logical-plan export for a no-op; the product correctly rejected that
request. The test now omits the output flag for the no-op and preserves the product
contract. Attempt2 passes both cases in 17.18 seconds. An additional operation-row
adapter test is part of the final suite. Clippy/all-target/all-feature checks pass.
The earlier failed log remains under the ignored campaign artifacts.

A concurrent Continue review was kept read-only and used a separately named
release-focused log. It did not own source mutation, integration or full acceptance.
Its release `native_` filter passed 26 tests with one intentional ignore on an
earlier build; a subsequent recompilation was terminated. That observation is
not accepted as final-source proof. Stable-source acceptance is recorded separately.

## Delivery and disclosure

This development change is not in the frozen public v0.1.44 executable. It is
native first-party tooling adoption, not compiler self-hosting or a wholesale
Rust replacement. Markdown output now uses HTML tables instead of pipe tables;
raw-text consumers see that intentional layout change. Runtime measurements and
source acceptance must identify their actual binary/source.

The fresh authorized device lacks normal GitHub push authentication. No remote
ref, branch, workflow permission, credential or release was changed during focused
implementation. A coherent local commit is retained before broad verification;
remote main integration and public distribution cannot be inferred from local
success. Reporting follows the exact source boundary rather than retroactively
calling a later documentation revision tested.
