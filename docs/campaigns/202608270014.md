# Normalized Command Lifecycle Cutover

Campaign `202608270014` is complete except for live service acceptance, which is environment-
unavailable because Docker is absent. The service gate is classified unavailable, not passed.

## Baseline and commits

- Work began on `main` at `09b83ad182a37b96ed323d30edc8930222b44b05`; no later commit followed
  the audited baseline. `origin/main` was equal at the starting observation.
- Pre-existing work was the replacement root `AGENTS.md` and untracked
  `prompts/202608270014.md`; both were preserved explicitly.
- `8de34fb0f102a9f40c288c6e2389116552f20cf0` implements the dependency-closed
  authority, maintained-consumer, compiler/runtime, public CLI, generated-document, test,
  performance, and predecessor-deletion cutover.
- This completion record is a separate evidence commit. The final exact-content product/full
  receipts and push status are reported in the handoff because recording them here would change
  the verified input.

## Current public system

The executable registry is `lkjscript-contract-registry-3`, digest
`c63d0c4653d6de50e6f375d6da14bfb9101bba5a438aba5c0ae10a9dd27dbc43`; CLI contract 10 exposes
exactly `capabilities`, `new`, `status`, `inspect`, `query`, `change`, `package`, `check`, `build`,
`run`, `serve`, and `worker`. Removed predecessor names use the ordinary `cli_usage` failure.

Current semantic and derived identities are meaning graph 5, revision 7, receipt 5, transaction 5,
semantic diff 3, compilation manifest 3, compiler unit 1, bytecode 1, and artifact manifest/bundle
10. `serve` and `worker` remain explicitly separate artifact-4 runtime operations; they do not open
editable project authority and are not normalized-service claims.

`new --template command` creates an absent destination through a private synchronized sibling and
one atomic rename. Its typed recipe has 10 owners, one application module, one private pure
function, one component/port, target `main`, one local test, and one exact built-in standard
dependency. It calls the standard `text-from-static` declaration and returns `"hello"`.

One shared normalized path opens `GraphRepository`, validates the exact dependency transport,
selects an exact-current compiler cache or a clean/recovery build, links artifact 10, strictly
loads it, and prepares dense execution. `check` runs all closure tests through VM/reference
equality; `build` publishes deterministic bytes only to an explicit absent regular path; `run`
accepts pure command targets and a bounded JSON array adapter and requires VM/reference equality.
Check/build/run do not advance accepted HEAD.

After accepted `change apply`, an exact base cache may be handed to `build_incremental` while the
prepared publication is still in process. Authority publication completes first. Cache absence or
failure is reported separately and cannot roll back or misreport accepted meaning; no durable
impact journal was added. Corrupt exact-current cache data takes the clean-recovery path.

## Maintained authority and assets

| Consumer | Repository | Package | Revision | Package revision | Artifact manifest |
|---|---|---|---|---|---|
| standard | `repo_c1358d64c351873b51c954b69d1ac988` | `pkg_10000000000000000000000000000001` | `rev_27c3a79c798fe402d114e0000fefa0d628916808062d63d1782a6d9ed5e5aa83` | `package_revision_4290e78132570943c17a9cd800af0742dfc8c16baa6f471354792dab1d0db981` | `artifact_manifest_1d2b53b867cbe1027d4b537f34ecf93007ea28ce54f28bd6674ebdba0b15fe6e` |
| `lkjournal` | `repo_95f988c5423fe3eb823c329ef0832d51` | `pkg_20000000000000000000000000000001` | `rev_0f660831701b710fc7cd6e5f2c87cd754a944adc4ce77e1aca4649711946b4db` | `package_revision_420eb96689c2d10215a26b0863dab7adb96a76d1fbfd07dae65c94a573581ed6` | `artifact_manifest_97447a36407a29bb2b979ac42191d774334e661d799f65399b6eba904d593834` |

The standard transport is
`package_transport_76566ff6df6024e573d3fc7f868cbc74760170dbd2111805c4c8c30a3a95b154`.
Its generated artifact and transport SHA-256 values are respectively
`7cc4637334751d36f284cd26a394ba885e570aa2e51a366f8ab91c1aea315436` and
`3031ebba737e219964687b04adf7fb0d320289771635209e2053ff322a739623`. The generated `lkjournal`
artifact SHA-256 is `80c69d69aec80e49cc0c023ec65eef3106f4a876eff1dc347defb461f3037ccb`.
Product verification rebuilds and compares all three byte-for-byte.

Migration preserved predecessor package and stable semantic identities where those concepts had
continuity. Modules, expressions, bindings, parameters, type objects, and other concepts without a
predecessor typed identity used deterministic one-time allocation. Sorted generation-neutral
migration manifests are retained under `.artifacts/campaign/202608270014/migration/`: standard
SHA-256 `a1ee27ae23afdc4760fbb1a79e5b32fd3d71634c9189d7d6c2c6455cbadcc9e1` and `lkjournal`
SHA-256 `f7a5f544fc10fb574bcf751c959a57365c348c88481b38a0973d20d868e2e866`.
All 7 standard and 12 `lkjournal` tests are deterministic and pass without a replay adapter.

The retained service artifact is
`applications/lkjournal/frozen-service/lkjournal-artifact-v4.lkja`, contract
`lkjscript-artifact-4`, SHA-256
`d0a57a74161903a302472cbd8997762434b64cc58bd8ae36577b9ba31d2f96a3`. Both descriptors and the
service harness bind this exact path/digest. The harness copies only the artifact/descriptors and
uses environment/host adapters; `PreparedDeployment::load` does not open a project repository.

## Deletion and corrections from checkout evidence

The maintained `.lkjscript` stores, old standard dependency artifact, ambiguous `lkjournal.lkja`,
predecessor bootstrap/workspace/repository writers, drafts/history/diff/merge/review/query/change
lifecycle modules, public `execute_cli`, Graph 4 check/build/run routing, test project factories,
removed operation descriptors, and stale current documentation were deleted. Artifact-4 encoding
is test-only; strict read-only decoding/runtime remains solely for the named frozen service
consumer and implementation-disjoint tests.

Checkout evidence required four objective-preserving corrections, not a design change:

- Maintained host implementation locators are hierarchical (`core.text.concat`, etc.), while
  Graph 5 `Name` is intentionally one segment. A bounded canonical `ImplementationName` preserves
  the semantic distinction; strict codec/kernel tests cover it.
- Sequential let initializers and task-signature requirement references exercised lexical and
  ownership relations absent from the private normalized checkpoint. Validation, compiler closure,
  and runtime preparation were corrected locally; complete maintained compilation/tests prove it.
- Maintained pure tests use more standard intrinsics than the private VM/reference subset covered.
  Matching, independently implemented intrinsic coverage was added to both execution tiers; all
  maintained tests and differential checks agree.
- The preliminary service wrapper reported its typed unavailable result as a generic failed child.
  Verification contract 5 now binds an explicit unavailable exit classification; the final gate
  remains unavailable rather than being treated as passed.

No general package registry, migration command, dual dispatch, compatibility reader, graph edition,
durable compiler-impact log, or normalized service adapter was introduced.

## Verification and performance

The pre-completion fresh full run selected 20 gates: 19 passed fresh, none reused or skipped, and
`service_acceptance` was unavailable. Its receipt is
`.artifacts/lkjscript-dev/check/1787770292667385426-2131633-0/receipt.json` (SHA-256
`3559429ddc7911119c45f77a918ab0226c79bf6fb29508cd4374c079525908e9`). It includes:

- strict formatting and clippy;
- workspace tests: 380 library passed/1 intentional release-scale ignored, 18 public CLI passed/1
  intentional scale ignored, 11 service integration passed, and 29 developer-tool tests passed;
- copied release command lifecycle;
- generated-registry verification;
- standard/`lkjournal` normalized checks and deterministic artifact comparisons;
- built-in artifact and transport export comparisons; and
- diff check.

The nested service receipt is
`.artifacts/lkjscript-dev/service/1787770542647603976-2139686-0/receipt.json` (SHA-256
`6e4d3af47b68f63c685bf21b29aca5146448bc141607484849b5f9b46cddef75`):
`required_command_unavailable`, `docker-image-inspect is unavailable (command_not_found)`. It
verified the frozen artifact path/digest before the unavailable probe and required no cleanup.

Structured migration/performance evidence is
`docs/evidence/202608270014-normalized-command-lifecycle.json` (SHA-256
`7ad39a36d4882ced98c16b3b847ce85c25f5c1a61a87f4844dc0186346667605`). Raw measurements are under
`.artifacts/campaign/202608270014/performance/work-KCVcbb/`; each observation digest is in the
tracked evidence. Release observations were: command creation 80.423 ms; first check 84.583 ms;
clean/cached builds 66.519/70.566 ms with 4/0 and 0/4 compiled/reused units; post-change incremental
handoff 36.533 ms with 0/4/0 compiled/reused/removed; run 60.980 ms returning `"hello"`; standard
check/build 1.100/1.106 s; and `lkjournal` check/build 597.420/555.658 ms. Exact-current work was
strictly smaller but one uncontrolled-cache timing was not faster, so no latency claim is made.

Aggregate repository/cache byte I/O and exact synchronization syscall counts remain unavailable;
successful writers report synchronized visibility and owned-stage cleanup. CPU has 10 ms sampling
resolution, filesystem cache state was uncontrolled, and provider telemetry is unavailable.

## Remaining work

The smallest next dependency-closed campaigns are: normalized service/worker adapters and
deployment over Graph 5/artifact 10; bounded context traversal; remaining maintained authored
operations; normalized package management and removed operational workflows by exact consumer; and
large-graph compiler/artifact/retention evidence.
