# Native durable web starter

## Original user mandate

[https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscript](https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscript)  について進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。最も筋の良い方向に進むために、限界まで深く考えてほしい。超長期的な視点からお願いしたい。どれだけ時間がかかっても構いません。開発環境として、cw-bd235945fff44468a0f41292という名前で、lxcのコンテナを用意しました、自由に活用ください。安全性チェックも気にかけつつ、なんとか進んでほしい。

## Reconciliation and selected boundary

Initial local and independently read remote main are
`3082805f7ec6a45ace479f3d9160b0db5e52e434`. Work only as coder in the specified
cw-bd235945fff44468a0f41292 (Home Coder tomato-ocelot-73), at
`/home/coder/workspace/lkjscript`. Preserve unrelated untracked
`docs/campaigns/202609242022.md`, the retained detached privacy worktree, prior
evidence and unrelated services. No active compiler/checker or stash was observed.
Pinned Rust 1.98.0 is available; limit compiler and verifier concurrency for 8 GiB.

Public v0.1.48 and the accepted development v0.1.49 scalar-edit source are distinct.
This increment must not relabel either earlier proof. Select a durable web starter
rather than adding another kernel primitive, rewriting the runtime, or making the
existing stateless starter require secrets and storage.

Add opt-in `new --template web-editor`, reusing the maintained ordinary UI, forms,
editor and pure tests. Keep the application declaration body under one source owner.
Only explicitly reviewed linkage metadata may differ between independent exact
packages and locally vendored modules. Reject drift in an expected import preamble;
do not search/replace application literals. The complete public native decoder,
typed validation and atomic private-stage creation remain responsible for meaning.

Keep shared Basic authentication, exact Host/Origin admission, bounded forms and
transactional conflict behavior. Creation/build must not initialize operational data,
read secrets, open adapters/listeners or start services. Use an explicit
`notes.lkjdata` directory for the new template only; preserve existing paths and
storage format. The descriptor carries a secret variable name, never a credential.

Prove copied-executable creation without an external generator, editable local
declarations, failed creation/publication isolation, native checks, immutable
deployment build and detached HTTP save/conflict/restart. Exercise missing data and
secret configuration without weakening rejection. Retain failed trials honestly.
Stabilize source before dependency-complete fresh verification, then integrate by
normal fast-forward after refreshing remote state and applicable protections.
This section records intent, not acceptance.

## Implementation and focused evidence

Added opt-in `web-editor` to the closed creation registry (creation contract 7).
The shared UI/tests, forms, editor and editor/tests become ordinary editable local
modules, with one exact built-in standard dependency. The editor's native body is
unchanged. An anchored, exact, executable-owned import preamble is the only linkage
adaptation; its independent body-equality and literal-preservation tests reject
unexpected preamble drift. The existing public decoder, typed validation, private
lowering stage and atomic final visibility remain the owners of meaning.

Strict decoding reuses the existing editor deployment. Only the artifact path and
data-root placement differ: `generated/application.lkja` and `notes.lkjdata`.
The root is a directory with unchanged storage semantics, not a new format or a
migration of old roots. Explicit next actions report private operator initialization
and `LKJSCRIPT_EDITOR_AUTHORIZATION` before foreground serve. No default credential,
secret read, data initialization or listener is introduced by creation/build.
The guide distinguishes initialization from restart and warns against deleting an
authoring directory that now also contains operational data.

An initial background command failed before compilation because the remote runner
applied `nohup` directly to `cd`; a normal explicit Bash command corrected invocation.
The initial release-profile creation suite compiled and ran, with 13 passes and two
failed test expectations. The implementation correctly reused the existing artifact
path `generated/application.lkja`; the first test had guessed `.generated/app.lkja`.
A duplicate grant was correctly rejected earlier by the shared strict reader with
`deployment_grant_duplicate`, not the template-specific policy diagnostic expected by
the second test. Both assertions were corrected against their existing independent
contracts. No product admission was weakened. The failed prototype output remains
at `.artifacts/202609261600-unit.log` (exit 101); it is not stable-source acceptance.

With corrected expectations, `.artifacts/202609261600-focused.log` completes at exit 0:

- `cargo test --locked --lib platform::project_creation:: -- --nocapture --test-threads=2`:
  15 passed, zero failed, including five publication failure points for the new
  template, exact layout/policy checks and private-stage cleanup (134.97 seconds).
- `cargo test --locked --test public_cli native_declarations::native_editor:: -- --nocapture --test-threads=2`:
  both the original independent-package editor and the copied-executable starter
  pass the same full HTTP/adversarial/durability workflow (92.45 seconds).
- `cargo test --locked --test public_cli native_declarations::web_starter:: -- --nocapture`:
  the unchanged stateless web starter passes (15.20 seconds).

The new workflow creates with an empty PATH and environment, checks 180 graph tests
(105 local plus 75 standard) in production/reference evaluation before and after a
reviewed native page-title edit, and rejects a wrong-type proposal without changing
HEAD. Old/new immutable builds retain their own identities and original operator
bytes. Missing secrets or stores reject without auto-initialization. After removing
the authoring graph and edit proposals, both artifacts serve their independently
expected titles. The shared suite observes UTF-8 and line-ending handling, explicit
POST, 401/400/403/415 admission, duplicate/malformed inputs, 409 stale drafts, eight
concurrent writers across two processes (one 303 and seven 409), restart persistence,
operational failure, malformed stored values and exhausted revisions. Every owned
server joins with zero remaining/active/queued tasks; only disposable fixture data
is intentionally corrupted through a separate public native command.

A subsequent strengthened public test also compares ordinary `build --output` bytes
with the immutable build artifact. `.artifacts/202609261600-quality.log` completes at
exit 0: all eight guides are regenerated and verified through the product's native
discovery path, `cargo clippy --workspace --all-targets --locked -- -D warnings` passes,
the strengthened starter workflow passes, and capability plus generated-guide public
tests pass. These focused tests use the unoptimized Cargo test profile. They do not
claim finalized static-archive or real-browser acceptance of this new template.

## Source-acceptance boundary

The focused implementation is ready for one dependency-complete `check full --fresh`
with stable committed inputs. Preserve the exact source commit and checker receipt
when recording the outcome; earlier v0.1.49 scalar-edit proof and public v0.1.48
publication do not prove this increment. At this entry there is no source-wide
acceptance, mainline delivery, new release producer or publication for the starter.
