# Parametric Nominal Data: Verified Public Delivery

Deliver the already implemented parametric nominal-data foundation as an immutable, independently
accepted public binary release. Do not add another language feature to justify or delay this
milestone. Execute this mandate through its release disposition; it is not a request for another
plan or an internal demonstration.

Status: ready for execution; architect inspection only
Campaign ID: 202609101130-parametric-nominal-public-milestone
Created: 2026-09-10T11:30:51+09:00 (Asia/Tokyo; tool-observed clock)
Created UTC: 2026-09-10T02:30:51Z
Repository: lkjsxc/lkjscript
Canonical archive: docs/campaigns/202609101130.md
Inspected default branch: main
Inspected commit: 881f6eacbd2bfd29fbff2b20be8b695e4333cb4b
Inspected tree: 58a929699f6a41e943d84b95e2a877fdba93d4e3
Inspected source version: 0.1.29
Observed public latest: immutable v0.1.28, release 385653688
Predecessor: docs/campaigns/202609100459.md — completed nominal-data implementation and local target admission
Release decision: publish this coherent milestone after the exact-source and distribution gates below
Initially intended release: v0.1.29, subject to actual manifest, ownership and unused-identity reconciliation
Architect activity: read-only connected investigation and creation of the two local handoff files; no implementation or execution proof

## 1. Decision and finish line

An ordinary user must be able to download the supported release and author reusable parameterized
records and variants, compose them with generic graph functions and retained pure callbacks, move
code-complete libraries between projects, and use admissible concrete instances with typed data
and structured sessions. The source already implements this outcome. This campaign completes its
distribution boundary rather than claiming that release engineering invented the language feature.

The predecessor explicitly selected source-only completion and a next-cycle release review when
NOM-1–NOM-7, maintained-consumer migration, normal integration and exact local musl admission passed.
Its terminal record reports precisely that state. Honor this achieved trigger instead of moving it
behind recursive generic types, a new application or an idealized final language. [S1–S3]

The strongest alternatives are broader recursive generic data, richer effect/resource composition,
and a coherent optional-metering execution policy. Each could have substantial long-term value,
but each introduces a different semantic, ownership or failure contract. None is needed to deliver
the completed nominal abstraction. A compiler/backend campaign also lacks a selected measured
bottleneck here. Publishing now has higher immediate leverage: it makes the completed reusable
foundation available to independent programs without the source checkout, and creates a reliable
external baseline for discovering the next composition limit. [S2,S4]

Do not inflate the scope into a new release platform. Existing transferred offline-package and
pure-tail owners already execute nominal workloads, and the current transferred fault matrix
already contains nominal omissions. The work is to run, verify and finish that path for the new
source, repairing only demonstrated defects necessary for truthful delivery. A documentation-only
campaign, a tag without download proof, and another local target pass without publication are not
the selected outcome. [S5–S8]

Completion requires one normally integrated release source, its required fresh acceptance, a
successful exact-source hosted dry run, immutable publication through the established workflow,
and independently passing exact-version and latest public downloads. Deployment is not included.
If a genuine external blocker prevents publication, complete safe local obligations and report a
blocked distribution boundary; do not redefine the campaign as successfully released.

## 2. Baseline, predecessor disposition and startup reconciliation

### Inspected facts, not newly executed proof

The connected default branch selected the commit and tree in the header; a later branch read still
selected that source and reported `protected: false`. The root manifest directly selects 0.1.29.
The branch observation does not waive actual execution-time review or protection policy. [S9]

The predecessor terminal record identifies integrated tested implementation
`0851b635651e3fa4990a2d8a9cfd3ece4bec7162`, tree
`5b2a8c56979e410315ca661bf9c89d962b3212cf`. It records 26 fresh full-profile gates, zero reuse,
nine exact local target classifications, and 40 independently mutated target-receipt failures.
Its 21,256,448-byte musl candidate has SHA-256
`2349591418c01f1f5ee3bf1d90b9f8688eb82f00a0f3c04a7d78e0c92adc06b4`;
its frozen verifier has SHA-256
`b151b94c1199d50d69dc4171c65a8ffc30966f44f1a6c668e5481882e546a6d0`.
These are historical evidence bindings, not mandated future candidate hashes. [S1]

The full receipt SHA-256 is
`7e6b1fa3f7b3b197a01f930e7d1335b8f61b41a35af07c1f00da514782d9d9d4`;
target admission is `d40c6049b30013cd788e1e1f1baee1a8510be7d9081e0400bda09b83b45437ad`;
nominal receipt faults are `f40d558a6a2f5f6d9e50f2c443c93c55bf4a742c460862fad14885944e226979`.
The bounded evidence owner is `docs/evidence/202609100459-parametric-nominal-data.json`.
The inspected HEAD is a later reporting commit. Its out-of-band containing-record verification was
not established by these reads; neither invent that pass nor reopen completed implementation merely
to recover an ignored log. Obtain new release-source evidence. The predecessor explicitly did not
run the release rehearsal or claim public-download acceptance. [S1,S3]

The earlier public milestone `docs/campaigns/202609092050.md` is closed, and its release remains
supported. Fresh latest-release metadata returned non-draft, non-prerelease, immutable v0.1.28,
release 385653688, published at 2026-09-09T15:49:29Z. Its recorded annotated tag object
`c317df24fe90edff1acbd7440dd0c4c6b3e5f488` selects source
`221beca0df7b31dbf10c9b22fe35c6db3af1b903`, not the present HEAD. Preserve all its assets. [S3,S10]

The current public source contains `TypeForm::Applied { declaration, arguments }`, with a distinct
positive-arity application envelope and retained base type encoding. Preparation derives the
instantiated closure; the offline oracle publicly authors generic library and consumer projects,
removes producers, and checks independent complete values. This is implemented code, not just a
roadmap statement. Current nominal source contracts include Graph 13 and Artifact 17; inspect their
actual owners rather than copying generation numbers into a new release scheme. [S4,S5,S11]

The selected campaign path and remote `refs/tags/v0.1.29` each returned 404. Separate Actions reads
for `in_progress` and `queued` returned zero runs. These are narrow observations, not a lock, proof
that no draft exists, or a complete inventory of waiting/requested/pending actions. Indexed code
search returned only the root AGENTS.md for that filename. It does not reveal local/global guidance
or every override. [S12]

### Required startup actions

Before substantive work, read this entire mandate, the supplied root candidate, applicable
instructions and the complete lifecycle of any genuinely newer active work. Establish the actual
branch, commit/tree, index/worktree, relevant untracked files, stashes, remotes/divergence, toolchain,
resource capacity, current tags/releases/drafts and nonterminal workflow states. Preserve unrelated
work, including any surviving predecessor stash; do not assume historical local paths still exist.
Check available credentials and administrative read/write permissions without printing secrets.

Archive this received initial mandate at the stated path. An identical existing archive may be
adopted. Never overwrite an occupied unrelated archive: obtain the actual current Tokyo time with
seconds, select an unused suffix/path, and record that adjustment without fabricating a later time.
Reconcile and install the full supplied AGENTS.md before substantive edits. An actual later higher-
priority instruction wins; document consequential reconciliation rather than silently forcing a
hash. Editing the file does not automatically reload the running session's instruction chain.

Append one startup reconciliation here. Preserve the initial body, timestamps and baseline. Only
Status and an explicitly recorded canonical-path/lifecycle metadata adjustment may change in place.
Append progress, deviations, resumption and terminal records using the existing campaign convention;
do not create a new active-work ledger or choose authority by lexicographic filename order.

Repository access establishes neither your worktree cleanliness nor your installed resources,
credentials, global instructions, GitHub immutability setting, release-control variable or deployed
state. The architect ran no build, product, test, benchmark, service or download acceptance. An
isolated source download was unavailable because name resolution failed; primary inspection used
connected, commit-pinned reads. No conclusion depends on a claimed local source audit.

## 3. Fixed product and distribution contract

### Deliver existing meaning without a gratuitous cutover

The release retains one accepted typed semantic graph as program authority, exact nominal
application identity, explicit parameter order and constraints, strict artifact/package admission,
independent reference behavior, checked raw-value provenance, evaluation order, cancellation and
transaction/resource cleanup. Distribution metadata never becomes program meaning.

Nominal identity includes the declaration and complete ordered type arguments, including phantom
arguments. Capture safety, equality, data encoding and session retention are different properties;
one must not grant another. JSON shape alone does not authenticate a phantom type or prepared-program
origin. Whole-type rejection must not depend on an empty collection or an inactive variant case.
Existing finite generic nominal definitions are sufficient for this release. Do not expand recursive
nominal data, generic tasks, affine containers or resource polymorphism incidentally. These are
current feature boundaries, not permanent root-policy prohibitions. [S1,S4]

Do not change identities, graph/type/data encodings, maintained semantic owners, operational schema,
execution defaults, or the admitted target merely to produce a release diff. Necessary correctness
repairs are allowed, but require affected-owner reasoning, consumer cutover and renewed proof. A
materially incoherent or unsafe source blocks publication, not permission to hide the defect in notes.

There is no new performance claim. Preserve the existing public workload and cost acceptance,
including preparation accounting and failure behavior. Do not enlarge runtime fuel or defensive
limits to make it pass, hide fallback work, or substitute verifier-side execution for candidate
behavior. The optional-metering direction in root guidance is not implemented by this campaign.

### Executable representative behavior

Use the existing public-authored offline and pure-tail workloads, freshly executed for each required
candidate boundary. Their requests are actual compact public authoring records; the following
notation describes meaning, not a proposed source syntax or new CLI:

- One library defines `batch<T>` and `edit<T>`; one consumer instantiates them for I64, Text,
  nested lists and a consumer-owned nominal type. Inspection exposes complete parameter/bound and
  member/application structure. No per-use monomorphic declaration clones substitute for the library.
- Starting from `{revision:7, items:[1,2,4]}`, keep, identity and snapshot preserve the full value.
  Replacing with `{revision:8, items:[-2,0,9]}` changes the current value while a retained snapshot
  still returns the original. Independently configured mappings yield `[8,11,17]` and `[2,0,-4]`;
  an empty input stays empty. Heterogeneous mapping produces the expected consumer-owned values.
- All 8,192 ordered mapped results are checked against `3*i+5` for `i=0..8191`, not merely their
  count or aggregate; their fixed sum is 100,691,968. A body change and an exact dependency replacement
  must change the specified results and invalidate affected derivations. Removed producers must
  remain unnecessary for build and execution.
- Existing typed batch/edit persistence checks compare independently constructed bytes and public
  HTTP readback. Trap/cancellation paths conceal staged transactional writes and permit healthy
  recovery. A fresh concrete pair session exchanges the prescribed raw WebSocket messages.
  Its separate state probes observe initial `0/""`, then `1/"a"`, then `2/"ab"` and remain explicitly
  source/verifier-bound, not proof that the downloaded product exposed its internal state.

Keep the exact current fixture expectations in their existing owners. The inspected changed-body
result is `{revision:9,items:[1,5,9]}`; the replacement-library result uses revision `"next"` with
`[1,5,9]`, while its keep branch preserves revision `"retained"` with `[1,2,4]`. These witness that
meaning, rather than a familiar fixture identity, determines behavior. Reconcile legitimately
changed fixtures with independent expected values, not relaxed assertions. [S5,S6]

Important counterexamples include wrong application arity/order/scope or bounds; phantom/nested
secret or callable session state; foreign prepared origin; stale or malformed package/artifact
inputs; missing nominal observations; and a passing receipt borrowed from a different source,
candidate, verifier, root or distribution boundary. Preserve their rejection and recovery owners.
Do not make a new public application, game or relay the release prerequisite.

### Compatibility and user communication

The existing maintained standard and lkjournal cutover is complete. Preserve it. Verify generated
assets through their canonical owners; do not regenerate into a second editable source. This
milestone does not add an automatic external Graph 12/Artifact 16 upgrade path. Older incompatible
inputs must reject through supported admission, without partial publication or mutation. Keep
predecessor rejection fixtures and unchanged monomorphic type/data golden evidence. [S1,S4,S6]

Release notes must state that upgrading the executable does not migrate existing repositories,
artifacts or operational data. Recommend retaining backups/exports and the matching predecessor
executable for unsupported older inputs; do not advertise an unimplemented converter. Existing
operational data is not a disposable graph asset. Do not open live user stores with destructive
fixtures, migrate deployed services, or confuse unchanged operational formats with universal
cross-version application compatibility.

## 4. Owners and implementation sequence

### Keep work at the actual owners

| Responsibility | Current owner and expected work |
| --- | --- |
| Version, target and release policy | Root Cargo.toml, docs/release.md, docs/decisions/20260829-release-contract-version-authority.md, tools/lkjscript-dev/src/release/. Derive the tag and target; no parallel catalog. |
| Source verification | tools/lkjscript-dev/src/check/ and docs/spec/verification.md. Use selected dependency-closed gates and exact input bindings. |
| Nominal public composition | tools/lkjscript-dev/src/offline_packages.rs and offline_packages/nominal.rs, nominal_session.rs. Run the current workflow and complete typed receipt reader. |
| Typed data, retained state and cost proof | tools/lkjscript-dev/src/pure_tail.rs, pure_tail/nominal_data.rs and service/nominal.rs. Distinguish public process observations from source-bound probes. |
| Target and transferred admission | release/admission/nominal_tests.rs, release/transferred.rs and release/transferred/tests.rs. Execute existing failure-sensitive readers and real-fixture tests. |
| Hosted delivery | .github/workflows/release.yml. Preserve the read-only verifier jobs, single isolated writer, exact tag binding and anonymous post-publication acceptance. |
| Product defects only when necessary | src/platform/ semantic, control, compiler, strict loading and normalized execution owners; tests/ public acceptance. Repair the cause, not the gate. |
| Guidance and evidence | Root AGENTS.md, this archive, current status/release docs and docs/evidence/. No competing release ledger or permanent campaign policy. |

Stage A — Reconcile and freeze scope. Install/archive the handoff, read actual release state and
instructions, inventory the inherited product/consumer inputs, and decide the exact release source
preparation route. Initially retain source version 0.1.29 if it remains correct and unused. Commit
only scoped preparatory changes after review. Historical evidence informs the choice but does not
satisfy the new candidate's freshness requirements.

Stage B — Complete local admission and transferred rehearsal. Obtain fresh full and target proof
for one committed source. Prepare and strictly extract its dry-run archive and verified two-file
verifier handoff through the existing release/verifier owners. In an owned execution workspace with
no source checkout, run the five transferred owners against that extracted candidate. Product
authoring must not depend on source assets or producer projects. Record the actual isolation;
working outside a checkout is not by itself a claim that the host has no other readable files.

Run the existing ignored nominal target test and transferred fault matrix against actual completed,
campaign-owned fixtures. Preserve candidate/verifier bytes and restore every mutated evidence file
before healthy recovery. Do not point fault tests at published assets, shared evidence or another
agent's admission root. Their missing-observation rejection must reach the semantic receipt reader
after enclosing hashes are recomputed, not merely fail a checksum. Both tests already exist.

If an actual failure exposes a defect in a required release/public path, fix it through the owner,
record the failed attempt, commit the correction, and refresh all affected inputs. Do not invent a
new framework, count-only admission, optional nominal test mode or changed schema merely to make
this a feature campaign. Extend an existing owner only for a demonstrated missing guarantee.

Stage C — Integrate and obtain the hosted dry run. Normally integrate the final release-input commit
through the actual branch/review policy, refresh remote state, and verify its exact identity. Run the
established hosted dry run; inspect actual run commit, attempt, jobs, handoff identities and typed
receipts, not only a green summary. It must include the checkout-free transferred job. If main moved,
a successful run for a different source does not admit the intended source.

Stage D — Publish and independently accept downloaded bytes. After the publication preconditions
and narrow authority in section 7 are satisfied, push the one annotated tag and follow the existing
workflow through immutable publication and both anonymous behavioral runs. Keep every real external
state transition accounted for. Publication is part of completion, not an optional final suggestion.

Stage E — Close the record. Update current-public documentation only after exact/latest acceptance.
Append terminal evidence, integrate scoped reporting, and report the release-source and containing
commit separately. Do not manufacture a new release merely because the final evidence report has a
later commit. Revalidate checks invalidated by actual reporting changes; avoid self-referential
hashes and an endless report-change/rebuild cycle.

## 5. Acceptance claims and evidence

All required claims must pass, or have an explicit blocked boundary. A historical successful test
and a newly executed pass are different evidence states. Use existing authoritative profiles and
readers rather than treating this matrix as a new independent test harness.

| Claim | Required executable observation and falsification |
| --- | --- |
| NPD-1: The intended semantic milestone is intact. | Fresh authoritative full profile at the chosen source, maintained asset regeneration/equality, public nominal positive/negative owners and VM/reference pure results. Consumer identities and unchanged formats are inventoried; any necessary change has a cutover. No lowered gates or new host-implemented application policy. |
| NPD-2: The exact admitted target is usable. | Current release build/admit owners bind the candidate to source/toolchain/target policy, strict static ELF inspection, both pinned userland command lifecycles and all six named behavioral oracles, including maintained service acceptance. Missing/stale/foreign/skipped evidence rejects. Run the existing 40-fault nominal target test on a fresh owned admission. |
| NPD-3: A release handoff actually carries this capability. | Strict deterministic package verification and a fresh five-owner transferred rehearsal in a no-checkout execution workspace. Require complete nominal offline and pure-tail observations, current child readers, candidate/verifier/source bindings and cleanup. Run the existing live transferred fault matrix against that real aggregate; missing roles, nominal results/projections/state/data evidence and rehashed child mutations must reject and recover. |
| NPD-4: Hosted pre-publication proof selects the exact source. | A fresh dry-run workflow for the final integrated release input passes full, target, packaging and no-checkout transferred jobs. Inspect exact head/source, attempts, candidate and verifier identities. Publish/post-release jobs are skipped; the dry run creates no tag or public release. |
| NPD-5: Publication is immutable and correctly authorized. | Reconcile the unused tag/release identity and existing control variable; read enabled immutability with actual administrative authority; bind only the intended annotated tag object; verify readback; use the tag-driven isolated publication job. Record tag object/peeled commit, release/asset IDs, immutable/non-draft/non-prerelease/latest state and actual settings action. Published predecessors remain unchanged. |
| NPD-6: Users receive and can use the admitted product. | Separately downloaded anonymous exact-tag and latest archive/checksum pairs pass integrity, release/asset attestation, strict extraction, manifest/source/target/static checks and all five fresh transferred behavioral owners each. Each uses its own state and evidence root. Compare exact/latest bytes only after both runs. Public nominal behavior must come from each downloaded candidate; internal state probes remain source-bound. |
| NPD-7: The outcome is closed and reproducible. | Accurate public/source documentation, bounded durable evidence, archived lifecycle, required scoped integration and complete owned-resource cleanup. Report starting/final identities, actual actions, failed attempts, source version, public latest, limits and preserved unrelated work. A public but unverified release fails this completion claim. |

The current target inventory has six behavioral owners, while transferred pre-publication and each
public-download boundary has five. The maintained lkjournal service is required at target admission;
nominal raw session behavior is already covered inside the transferred offline owner. Do not
silently omit the service target gate or add a redundant sixth transferred application framework.
Use the current owned inventory and independently specified expected inventory. [S6–S8]

Fault evidence is proportionate to this distribution milestone. The predecessor's target omission
proof does not replace the current transferred rehearsal. Conversely, there is no requirement to
rerun historical million-owner or old PostgreSQL campaigns unless an actual current gate or affected
input requires them. Observe the existing subprocess, file/output and timeout bounds. Resource
shortage is a failure to diagnose or a blocker, not permission to inflate semantic budgets, omit a
workload, change global host configuration or purchase capacity.

Retain a bounded summary at
`docs/evidence/202609101130-parametric-nominal-public-milestone.json` (adjust consistently only after
an actual archive collision). Include exact source/tree/subtree inputs, source version, candidate
and verifier hashes/bytes, target policy, toolchain/environment, gate dispositions, run/attempt IDs,
receipt digests, essential public request/result evidence, fault sensitivity, package and download
identities, compatibility facts, failed attempts, external actions and cleanup. Large raw logs stay
in the existing owned ignored evidence root. Tracked summaries must remain useful after ephemeral
CI artifacts or ignored local logs disappear.

Do not require a report to contain the hash of its own containing commit. Bind implementation and
verifier inputs using the existing snapshot scheme; record final containing identities out of band.
Receipts with changed inputs are not validly reused simply because an edit was called documentation.
No unexecuted final check may be reported as passed.

## 6. Verified command entry points and source binding

These are inspected command forms, not new CLI proposals. Bind variables below to private absolute
paths satisfying the current owner's create-new and clean-source rules. Reuse no occupied evidence
root and do not run a destructive test on an unfamiliar path. Read the actual argument owners and
`docs/release.md` before execution; preserve a legitimate changed command through explicit startup
reconciliation rather than using stale flags.

```sh
cargo build --release --locked -p lkjscript-dev
target/release/lkjscript-dev release target
cargo run --release --locked -p lkjscript-dev -- check focused --machine
cargo run --release --locked -p lkjscript-dev -- check changed --machine
cargo run --release --locked -p lkjscript-dev -- check full --fresh --machine

target/release/lkjscript-dev release build \
  --output "$candidate" --receipt "$build_receipt"
target/release/lkjscript-dev release admit \
  --candidate "$candidate" --build-receipt "$build_receipt" \
  --evidence-root "$target_root"
```

The first-party driver uses the release build profile; this does not alter the gates' own build
options. Use focused checks during iteration and one required fresh full after relevant inputs
stabilize. The release owner may require an ordinary clean clone rather than a linked worktree.
Use an owned disposable clone where needed; never relax clean-source admission or clean the user's
worktree. Freeze candidate/verifier copies before tests and do not rebuild outputs in parallel with
acceptance reading them. Source full and target admission are independently required.

```sh
LKJSCRIPT_NOMINAL_ADMISSION_FIXTURE="$target_root" \
  cargo test --locked -p lkjscript-dev --lib \
  live_nominal_receipt_omissions_reject_at_target_admission -- --ignored --nocapture

product_version=$(cargo metadata --locked --no-deps --format-version 1 |
  jq -er '.packages[] | select(.name == "lkjscript") | .version')
release_tag="v$product_version"
release_source=$(git rev-parse HEAD)

target/release/lkjscript-dev release prepare \
  --candidate "$candidate" \
  --cargo-about "$cargo_about" --cargo-about-archive "$cargo_about_archive" \
  --output "$release_output" --tag "$release_tag" --publication dry-run \
  --full-verification-receipt "$full_receipt" \
  --target-admission-receipt "$target_root/receipt.json" \
  --require-full-verification

target/release/lkjscript-dev release verify \
  --archive "$release_output/lkjscript-x86_64-unknown-linux-musl.tar.gz" \
  --checksums "$release_output/SHA256SUMS" \
  --receipt "$release_output/release-receipt.json" \
  --extract-to "$extracted" --expected-tag "$release_tag" \
  --expected-publication dry-run
```

Obtain the version/digest-admitted cargo-about executable/archive and verifier handoff using the
existing owners. Do not invent their metadata or duplicate the handoff generator. The archive has
the executable, root license, generated third-party notices and canonical release manifest under
its exact owned inventory; checksums are separate. Deterministic double construction and strict
rejection of malformed/traversing/extra/foreign inputs remain mandatory. [S7]

```sh
"$transferred_verifier" release transferred run \
  --candidate "$extracted/lkjscript" --manifest "$extracted/RELEASE-MANIFEST.json" \
  --tag "$release_tag" --commit "$release_source" --publication dry-run \
  --boundary pre-publication --evidence-root "$transfer_root" \
  --verifier-identity "$verifier_identity" \
  --expected-verifier-sha256 "$verifier_sha256" \
  --expected-verifier-bytes "$verifier_bytes"

LKJSCRIPT_TRANSFERRED_FIXTURE_ROOT="$transfer_root" \
  cargo test --locked -p lkjscript-dev --lib \
  live_receipt_fault_matrix -- --ignored --nocapture
```

The fixture tests are source/verifier-bound falsification of genuine executed receipts, not product
behavior. Build them from the matching admitted verifier source. Preserve complete baseline and
restored healthy evidence. The target test invokes its frozen verifier; the transferred test directly
exercises the matching source reader. Do not misdescribe those as the same execution boundary.
`release transferred verify` accepts the same context arguments for read-only revalidation; it does
not run behavior or certify a different source/root/boundary.

After safe normal integration and checking source identity:

```sh
gh workflow run Release --repo lkjsxc/lkjscript --ref main \
  -f publish=false -f tag="$release_tag"
gh run watch --repo lkjsxc/lkjscript "$run_id" --exit-status
```

Resolve `$run_id` from the actual newly dispatched run, and verify its head SHA equals
`$release_source`. Inspect the current workflow's named handoffs and evidence by run and attempt,
not an arbitrary newest run. If no exact run was established, do not claim a hosted pass. Publication
and independent download commands remain orchestrated by the established workflow in section 7;
do not create a parallel manual release or accept a transport-only curl check as behavioral proof.

## 7. Execution authority, publication protocol and recovery

### Authorized work and limits

Under the current user delegation, this mandate authorizes scoped source/documentation/tooling
changes necessary for this outcome, canonical regeneration when affected, required local checks,
disposable loopback/container fixtures, coherent commits, and ordinary non-force integration/pushes
to `lkjsxc/lkjscript` through its actual branch/review policy. The intended route is normally
integrated `main`; the inspected unprotected branch is not a lasting exemption from later policy.
Refresh remotes, inspect staged paths/divergence and automatic effects before pushing. The inspected
Release workflow triggers on `v*` tags and explicit dispatch, not ordinary main pushes. Recheck it.

Publication is authorized conditionally on NPD-1–NPD-4 and the preflight below: the existing hosted
dry run, annotated release tag creation/push, established tag-driven publication and supported
same-input recovery actions, and independent public acceptance. Do not ask again for these routine
decisions. Use only actual available execution permissions and credentials; never bypass a missing
permission or report an unperformed action.

There is exactly one narrowly authorized release-control write: update the existing non-secret
repository variable `LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA` to the exact new annotated tag-object
SHA for this selected release, following its established preflight/readback/ownership protocol.
This mandate supplies that authorization anew; it is not inherited from a prior campaign and does
not survive as an evergreen root grant. If the variable is absent or the protocol has changed,
reconcile the established owner without inventing a new settings mechanism.

No grant here authorizes changing the immutable-release setting, other administrator settings,
permissions, protections, secrets, credentials, deployment, live-data migration, additional spending,
force pushes, shared-history rewriting, replacement of published tags/assets, or changes to unrelated
projects/host configuration. Use already permitted local resources and established CI. Missing actual
administrative access to the required setting read or narrow variable write blocks publication.
A publicly immutable predecessor is not evidence that the current repository setting is enabled.

### Exact publication preconditions

Before irreversible action, the chosen release source must be committed, clean in its release
checkout, normally integrated and exactly reconciled with origin/main. The hosted dry run must be
fresh for that source and its workflow/verifier inputs. Confirm the intended tag and release are
unused, with actual successful absence checks; an authentication or transport error is not absence.
Inspect drafts and queued, in-progress, waiting, requested, pending and other nonterminal publishers.
Do not cancel another publisher or claim a global publication lock from two empty status queries.

Derive the tag from the canonical root package version. Use v0.1.29 when that remains the correct
unused source identity. Do not bump solely to archive this mandate. If legitimate intervening
progress or immutable attempted identities require a successor, use the current version owner and
smallest appropriate unused additive patch, record the reason, and refresh affected proof. Do not
publish a semantic downgrade or overwrite someone else's reserved/active identity.

Create the annotated local tag only for the admitted release-source commit. Verify both tag-object
SHA and peeled commit. With actual administrative read authority, freshly require:

```sh
gh api repos/lkjsxc/lkjscript/immutable-releases --jq '.enabled'
```

The result must be true. Do not enable the setting under this grant. Read and record the control
variable's exact prior value and establish that it belongs to completed, non-conflicting work.
Compare that prior value again immediately before writing; set only this attempt's exact tag-object
SHA; read back and require equality. This read/check/write sequence is not atomic compare-and-swap.
An unexplained change is a conflict, not permission to overwrite.

Before tag push, abort and restore the previous variable value only when it still equals this
attempt's value and no tag/run has acquired it. After tag push, retain the binding for its owning
run; do not restore it merely because the agent session is ending. Record any residual externally
active state and a precise resumption condition.

Push only the intended tag through normal Git transport. The established isolated publication job
owns release creation/resumption, missing exact asset uploads without clobber, digest verification
and immutable publication. Do not manually create a second release. Its write token must not be
combined with checkout or execution of repository code. Preserve pinned actions and the current
release/verifier authority separation. [S7,S8]

### Download acceptance and failures

Follow the actual tag run through completion. For the exact-version and latest boundaries, require
separate anonymous archive/checksum downloads, strict package/static verification, release and asset
attestation checks, and five complete fresh behavioral owners each. Keep authenticated metadata or
attestation reads distinct from token-free downloads. Compare archive, checksum, manifest and
executable identities only after both full independent runs. A checksum pass, a successful upload,
or one green child cannot substitute for this closure.

If another legitimate release changes latest before this campaign's latest acceptance, do not
relabel a foreign release as this one, mutate published predecessors, or use old exact evidence as
latest. Reconcile publisher ownership; complete safe exact acceptance and report the precise latest
boundary blocker unless the established workflow can complete it without violating immutable state.

Before tag push, repair source/workflow defects and rerun all invalidated proof. An unpushed local
tag may be removed only after confirming it never reached the remote. Once pushed, never move or
delete it. A transient, idempotent failure with unchanged exact source, workflow, verifier, candidate
and package may use the repository's bounded same-tag recovery dispatch after state reconciliation.
Any changed release input after tag push requires an unused additive patch and new proof. Do not
edit, unpublish, relabel, replace or delete a published release or asset. [S7]

A dispatched or waiting workflow is not completion. Continue safe work and read actual status within
the execution session; do not promise unattended future completion. When an external wait, denied
permission or unavailable mandatory resource really prevents closure, retain useful commits and
artifacts, append a blocked/resumption record, and state the smallest real condition for resumption.

## 8. Refreshed root guidance and bounded adaptation

The supplied root AGENTS.md is a complete replacement candidate: 1,357 whitespace-delimited words,
11,042 UTF-8 bytes, SHA-256
`ec351d13976e185d9f243a5d4bf83a14fddb9410b855beb4706d9bbd46e110da`.
The inspected root was 14,688 bytes. This identity describes the delivered candidate, not a demand
to discard legitimate intervening instructions to force byte equality.

The meaningful revisions are:

| Revision | Reason and preserved property |
| --- | --- |
| Replace the predecessor's campaign-local source-only disposition with conditional publication here. | Its stated trigger passed. Release authority is current and bounded; immutable delivery and no deployment remain required. This does not rewrite the completed predecessor's authorization. |
| Remove ambiguity that all existing identities/encodings must remain unchanged forever. | Logical continuity and representation are distinct. Purposeful breaking changes remain permitted with identified consumers, migration/rejection/recovery and retirement. This release itself has no gratuitous representation change. |
| Separate optional execution fuel direction from current safeguards. | Profiling, cancellation, quotas, defensive limits and fuel are not one concept. Existing policy remains effective until a separate coherent change is implemented and proved; no unmetered or sandbox claim is made here. |
| Compress repeated campaign history, adaptation detail and proof narration out of root. | Retain authority, safety, atomic graph publication, actual owners, verified commands, evidence honesty and completion. Task-specific details stay in this mandate and their normative owners. |

The root is intentionally not a copy of the parent architect prompt. It contains no nominal-cycle
subset, release version, active campaign ID, permanent release-control exception or new status ledger.
Official Codex guidance describes startup instruction discovery, per-directory override precedence
and a default combined project-file limit of 32 KiB. Check the actual installed configuration and
applicable chain; the candidate size is not proof of available headroom. Do not change global limits
to accommodate it or presume automatic session reload. [S13]

Fixed outcomes are the existing nominal milestone, intact semantic/safety/compatibility properties,
NPD-1–NPD-7, immutable release protocol and public acceptance. Routine factoring, exact filenames
inside owners, justified pre-publication version choice, private algorithms and commit grouping are
adjustable. Record consequential drift and its invalidated proof.

If legitimate newer source already delivers the required milestone with a richer implementation,
validate its actual contracts and boundaries instead of forcing an old subset or downgrade. If an
already published equivalent release has complete exact/latest evidence, verify that state and close
or explicitly supersede this mandate without manufacturing a duplicate release. A missing critical
proof can be completed when authorized; it must not be retroactively asserted.

A real conflicting owner, unsafe release source or unavailable mandatory permission stops the
dependent operation. It does not authorize dropping required acceptance, silently choosing a new
language objective or discarding unrelated work. Repair directly necessary defects, or preserve safe
progress and report the exact blocker. This campaign is not open-ended approval for infrastructure
redesign, new targets, additional applications or speculative optimization.

## 9. Terminal handoff and next decision boundary

The terminal record and user handoff must report:

- Actual starting/final branch, commit/tree, index/worktree, scoped commits and integration state;
  release-source commit, annotated tag object, peeled commit, later containing commit and actual
  source/public versions as distinct identities.
- What the public product now permits, each NPD claim and required gate disposition, fresh versus
  validly reused evidence, exact candidate/verifier/package identities, durable receipt references,
  fault results, hosted run/attempt identities, and both independent download results.
- Every external action actually performed, including the narrow variable read/write/readback and
  any safe restoration, tag push, dispatch, draft/publication or recovery; immutable predecessor
  preservation; no deployment/live-data work unless separately authorized elsewhere.
- Failed attempts, consequential adaptations, compatibility limitations, actual resource/isolation
  boundary, preserved unrelated work and stashes, cleanup and intentionally retained owned artifacts.
  If blocked, identify the exact incomplete boundary and smallest resumption condition.

Use complete, blocked/incomplete, terminated or explicitly superseded accurately. Implementation,
local admission, integration, publication, public acceptance and deployment are separate statuses.
Do not report a partial prototype, locally packaged archive or published-but-unverified release as
the selected completed milestone.

After successful delivery, the next architect can evaluate a new language advance against actual
public composition needs. Broader regular recursive data and richer effect/resource composition are
credible candidates; optional metering policy and performance work need their own discriminating
workloads and contracts. None is pre-authorized by this short horizon, and none can be used to move
this campaign's already justified release trigger further away.

## 10. Durable source references

Except for explicitly mutable API or external references, all paths below refer to inspected commit
`881f6eacbd2bfd29fbff2b20be8b695e4333cb4b`. Resolve them under:

https://github.com/lkjsxc/lkjscript/blob/881f6eacbd2bfd29fbff2b20be8b695e4333cb4b/

These references identify evidence and owners, not a claim that every line of every named downstream
file was audited. Follow actual dependencies when a change affects an owner.

- **S1 — Completed predecessor:** docs/campaigns/202609100459.md, especially the release trigger,
  stable-candidate corrections and complete terminal record; its linked
  docs/evidence/202609100459-parametric-nominal-data.json is the retained historical evidence owner.
- **S2 — Direction and public gap:** docs/roadmap.md and docs/status.md, nominal source completion,
  source/public distinction and contingent further abstractions.
- **S3 — Current proved-state account:** docs/status.md, maintained consumers and nominal/public
  milestone sections. These are repository reports, not architect-executed tests.
- **S4 — Semantic boundaries:** docs/spec/language.md; docs/architecture.md; predecessor's complete
  nominal contract and docs/decisions/20260910-parametric-nominal-data.md as its rationale owner.
- **S5 — Public authoring and independent nominal expectations:**
  tools/lkjscript-dev/src/offline_packages.rs and offline_packages/nominal.rs. Inspected receipt,
  fixed-result validation, public library/consumer creation, projection, export and producer-removal
  code; follow nominal_session.rs and service/nominal.rs for session implementation.
- **S6 — Verification contract:** docs/spec/verification.md, current nominal receipt obligations,
  copied behavior versus source-bound state probes, independent data/cost checks and failure cases.
- **S7 — Distribution and version procedure:** docs/release.md, fully inspected identity, build,
  preparation, hosted dry-run, tag/control-variable, anonymous acceptance and recovery sections;
  docs/decisions/20260829-release-contract-version-authority.md is its version rationale owner.
- **S8 — Executable release proof:** tools/lkjscript-dev/src/release/transferred.rs;
  release/transferred/tests.rs, especially live_receipt_fault_matrix and its existing nominal fields;
  release/admission/nominal_tests.rs, complete live_nominal_receipt_omissions_reject_at_target_admission;
  .github/workflows/release.yml, inspected triggers, permissions, identity and source selection.
- **S9 — Source setup:** Cargo.toml, AGENTS.md and mutable branch observation
  https://api.github.com/repos/lkjsxc/lkjscript/branches/main . Rust/Cargo pinning is also explicit in
  the inspected release guide/workflow; verify rust-toolchain.toml at startup.
- **S10 — Latest-release observation:**
  https://api.github.com/repos/lkjsxc/lkjscript/releases/latest ; observed v0.1.28/release 385653688.
  Its durable public release is https://github.com/lkjsxc/lkjscript/releases/tag/v0.1.28 .
- **S11 — Implemented nominal representation and preparation:**
  src/platform/kernel/type_object.rs, TypeObject::new, validate_local and TypeForm::Applied;
  src/platform/execution/normalized/prepared_types.rs, Budget and complete_controlled.
- **S12 — Narrow startup observations:** selected archive at the pinned commit and
  https://api.github.com/repos/lkjsxc/lkjscript/git/ref/tags/v0.1.29 returned 404;
  https://api.github.com/repos/lkjsxc/lkjscript/actions/runs?status=in_progress&per_page=10 and
  https://api.github.com/repos/lkjsxc/lkjscript/actions/runs?status=queued&per_page=10 each returned zero;
  indexed AGENTS.md filename search returned only the root. These do not establish execution-time
  absence, settings authority, complete instruction discovery or exclusive publisher ownership.
- **S13 — Official agent instruction discovery:**
  https://learn.chatgpt.com/docs/agent-configuration/agents-md , checked during this architect session.
  Actual installed configuration, external instructions and session permissions remain unverified.

## Startup reconciliation — 2026-09-10T02:43:19Z

Started on `main` at `881f6eacbd2bfd29fbff2b20be8b695e4333cb4b`, tree
`58a929699f6a41e943d84b95e2a877fdba93d4e3`, with unchanged index and exactly the supplied
modified root guidance and untracked campaign archive. The 46,878-byte archive (SHA-256
`b432ad82711967be3848ed3c730457afe193e4a8ad4b0357eb6b9ab72d2eae66`) is adopted in place;
its initial body is preserved. The root matches the supplied 11,042-byte SHA-256
`ec351d13976e185d9f243a5d4bf83a14fddb9410b855beb4706d9bbd46e110da` and applies explicitly
in this session. No global, ancestor, scoped or override AGENTS file or configured fallback/size
override was found. The root fits the documented default combined 32 KiB allowance; no automatic
reload is assumed and no global configuration is changed. Official discovery reference:
https://learn.chatgpt.com/docs/agent-configuration/agents-md .

Fresh fetch finds zero divergence. GitHub reports admin/push authority, main unprotected, no
rulesets or open pull requests, immutability enabled, no draft among all 14 returned releases,
and no nonterminal run among the latest 100. The remote v0.1.29 tag is absent; no returned release
uses it. The existing control variable remains `c317df24fe90edff1acbd7440dd0c4c6b3e5f488`,
owned by completed immutable v0.1.28/release 385653688. These observations are not a publisher lock.
The established workflow triggers only on v-prefixed tags and explicit dispatch; its annotated
message supplies release notes. Version 0.1.29 remains the selected unused source identity.

The complete predecessor lifecycle establishes completed source-only implementation and local
target admission, without release rehearsal. No product reimplementation is selected. Fresh full,
clean ordinary-clone target build/admission, both live fault tests and checkout-free transferred
rehearsal will precede normal integration and exact-source hosted dry run. The supplied mandate
conditionally authorizes publication and the one control-variable write; no deployment is selected.

Rust/Cargo 1.98.0 are installed under the existing Cargo bin directory, absent from shell PATH;
commands will extend PATH locally. Host observations are 12 GiB memory, 4 GiB swap and 173 GiB
available disk. No product process or running container was observed. The sole registered worktree,
stash `87c2265f6faf0e815d80450ec2480d46c5e774fb`, historical ignored evidence and unrelated data
remain preserved. Campaign-owned raw evidence uses `.artifacts/campaign/202609101130/`.
No external mutation has occurred and no new acceptance is yet claimed.
