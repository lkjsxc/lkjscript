# Immutable 0.1.13 Outbound HTTPS Product Publication

Status: complete

Campaign ID: `202609010158-public-outbound-https-release`

## 1. Identity, audit boundary, and predecessor disposition

Audit time: `2026-09-01 01:58:09 +09:00` (`Asia/Tokyo`).

Orientation source: remote GitHub repository `lkjsxc/lkjscript`, default branch `main`, exact commit
`1473532faa23bb8b958157ffe07bb73d422db48f`, tree
`1a258ab4f75a653f88d392b2d1caad2db73c65bd`. This is a remote branch observation, not an active
checkout. The mandate author did not inspect a local branch, `HEAD`, index, worktree, untracked
files, remotes, upstream relation, stash, installed binary, build, test, benchmark, ignored artifact,
deployment, or running service.

External orientation at the audit boundary:

- root product source is `0.1.13`;
- immutable latest public release is `v0.1.12`, GitHub release `379320112`, selecting source
  `8a0141a151a87fe59ccc1ebc738a7e5dd51c6882` through annotated tag object
  `d6d81e2398ced78023a8ea62d69e03f6b9f5d4da`;
- no `v0.1.13` tag or release exists;
- no open issue or pull request and no queued or in-progress workflow run was observed;
- the administration-only immutable-release setting, repository-variable value, credentials,
  hosted artifacts, installed binaries, deployments, and private evidence were unavailable to the
  mandate author and must be refreshed by Codex immediately before any external mutation.

Archive location: `docs/campaigns/202609010158.md`. The current repository has no distinct mutable
active-work ledger, so do not create one for this campaign. Preserve this initial mandate body as
historical intent. Completion may change only `Status` to `complete` and append the bounded terminal
record required below. Termination may set `terminated` and append the exact reason. A blocked
handoff leaves `incomplete` and appends one concise blocked record. Current facts, large receipts,
measurements, and logs belong in their existing owners.

Predecessor disposition:

- `docs/campaigns/202608311849.md` is complete. It implemented and freshly proved the
  deployment-bound `HttpClient.get` capability and the closed `nostr-relay-info` recipe, migrated
  maintained consumers, advanced the source snapshot to 0.1.13, admitted the exact static-musl
  target locally, and passed local release preparation and strict verification in dry-run mode.
  Publication, workflow dispatch, deployment, and live-relay contact were deliberately not run.
- `docs/campaigns/202608302224.md` is complete. It established the current immutable publication
  path and published `v0.1.12`; use it as historical release evidence, not as current 0.1.13 proof.
- `docs/campaigns/202608310137.md` was terminated and superseded during later scale closure. Do not
  revive its private retained project or stale protocol.
- The last campaign reported a modified local `AGENTS.md` and stash object in that historical
  checkout. Their current existence and ownership are unknown. Reconcile and preserve any such work
  rather than assuming it is absent, current, or disposable.
- Immutable predecessor tags, releases, assets, and historical evidence remain read-only. No
  externally committed 0.1.13 state was observed.

## 2. Implementation mandate and stopping rule

This is an implementation, publication, and proof campaign, not a new feature or target-matrix
campaign.

**Required final state:** the complete current 0.1.13 source snapshot is available as one immutable
latest `v0.1.13` self-contained `x86_64-unknown-linux-musl` release whose exact candidate passes
maintained service admission and whose exact-tag and `releases/latest` downloads independently pass
integrity, static-linkage, distributed HTTP, first-party stateful HTTP, and deployment-bound
outbound HTTPS/NIP-11 acceptance, while every predecessor publication remains unchanged and no
deployment or live relay is contacted.

`v0.1.13` is the intended first identity only while the reconciled root package still owns version
`0.1.13` and the exact remote tag and release remain unused. The final release-source commit `R`
must contain this mandate, the reconciled root instructions, every release input, and only necessary
release-blocking or release-truth corrections. `R` must be clean, normally pushed, and exactly equal
to `origin/main` before tag creation.

Complete only when all success conditions pass for exact final inputs, the release is immutable and
latest, both anonymous public boundaries pass all required behavior, current public facts and
structured evidence are committed without changing release inputs, normal pushes are closed, and no
externally committed release state remains unresolved. Do not begin an unrelated product campaign
inside this mandate.

Before tag push, any required unavailable or failed gate leaves public state unchanged and blocks or
terminates the campaign; do not weaken the gate. After tag push, preserve the immutable identity and
close through only the bounded unchanged-input retry or additive recovery below. If reconciliation
finds a later coherent release, a deliberately advanced product version, a conflicting active
mandate, or an authority change that invalidates this objective, stop safely and report whether this
mandate is superseded, narrowed, or terminated.

## 3. Reconciliation gate

Before editing or invoking mutation-capable commands:

1. Read every applicable `AGENTS.md`, this complete mandate, the terminal records for
   `202608311849` and `202608302224`, and current `docs/status.md`, `docs/architecture.md`,
   `docs/roadmap.md`, `docs/release.md`, release/version decisions, outbound-HTTP decisions, and
   verification specifications.
2. Record the exact active branch, `HEAD` and tree, worktree/index/untracked state, remotes,
   upstream relation and divergence, recent commits, stashes, supplied files, toolchain, root
   product version, current contract owners, tags, releases, open work, and queued or running
   workflows. Fetch without rewriting history.
3. Preserve all pre-existing, later, supplied, unfamiliar, or unrelated work. Determine whether the
   historically reported modified `AGENTS.md` and stash still exist and whether either belongs to
   the user or an already terminal campaign. Do not overwrite, drop, apply, or delete them merely to
   obtain a clean tree.
4. Audit the exact public delta from `v0.1.12` source through current `HEAD`. Confirm that the
   outbound HTTPS/NIP-11 source campaign is coherent and terminal and that no later source change
   creates another release dependency.
5. Locate the current first-party owners of full verification, target build/admission, deterministic
   packaging, verifier handoff, hosted dry run, isolated publication, exact/latest anonymous
   acceptance, structured release evidence, and current public documentation. Verify that the
   distributed, stateful, outbound, and standalone-service oracles are bound at the intended stages.
6. Refresh immediately before each external mutation: `origin/main`, exact tag and release
   availability, immutable-release administration, the exact confirmation-variable authority,
   workflow permissions/content, active runs, and public latest identity.

The active checkout outranks this orientation. Symbol renames, receipt-version advances, or a later
evidence-only commit are details to reconcile. A changed product owner, target policy, publication
model, maintained consumer, or release identity that invalidates the final state requires a safe
stop and exact report.

Unrelated login prompts, optional-provider outages, notices, and deprecation warnings are not
automatic blockers. Do not run interactive credential setup. Complete unaffected local work and
classify only the unavailable external step; use existing noninteractive credentials solely for the
actions below.

## 4. Load-bearing current state and campaign selection

At the orientation revision, the root package owns source snapshot 0.1.13 while immutable public
latest remains 0.1.12. The source-only delta is already vertical: it adds one deployment-bound
outbound HTTPS capability, one closed NIP-11 relay-information application recipe, exact TLS and
address policy, migrated standard and `lkjournal` identities, generated projections, and an
implementation-disjoint copied-binary oracle.

The predecessor terminal record reports fresh focused, product, service, and full profiles; an exact
16,144,608-byte static-musl candidate; four target-admission oracles; and successful local release
prepare/verify. Current `docs/release.md` and `.github/workflows/release.yml` already route outbound
HTTP through target admission, a no-checkout transferred pre-publication job, and exact/latest
anonymous acceptance. Those reports are strong readiness evidence but are not proof for the final
`R`, hosted run, tag, release, assets, or anonymous downloads.

Candidate scorecard, 1 (weak) through 5 (strong):

| Candidate | User/public value | Architectural closure | Independent proof | One-campaign probability | Future leverage | Risk/cost |
|---|---:|---:|---:|---:|---:|---:|
| publish immutable 0.1.13 with outbound acceptance | 5 | 5 | 5 | 5 | 5 | 3 |
| Nostr WebSocket/NIP-01 vertical slice | 5 | 4 | 5 | 3 | 5 | 5 |
| first-party data/history scale and retention evidence | 4 | 4 | 5 | 3 | 4 | 4 |
| bounded owner-detail projection | 2 | 4 | 4 | 4 | 2 | 2 |

Publication closes a ready public capability at the supported binary boundary and resolves the
recurring one-snapshot lag with the highest completion probability. Starting NIP-01 first would add
a second unreleased delta; no maintained data/history failure justifies delaying this release.

## 5. Target state, fixed decisions, and success conditions

### 5.1 Authority and identity

- The root `lkjscript` package version remains the sole human-facing product-release owner and owns
  the matching annotated tag. Do not bump it or any CLI, graph, package, artifact, deployment,
  adapter, runtime, data, queue, compiler, or tooling contract merely for publication.
- `R` is the exact source selected by the annotated tag. It must include all release inputs and only
  necessary release-truth corrections. A changed source, workflow, target, verifier, generated
  document, normative document, lockfile, or release procedure forms a new `R` and invalidates every
  affected receipt.
- Program meaning remains selected only by the typed graph repository. Operational data, object
  storage, deployment descriptors, secrets, and queues remain separate authorities. Tags, assets,
  manifests, checksums, attestations, and receipts are derived distribution identities and evidence.
- The current 0.1.13 standard package, `lkjournal`, recipes, outbound client, deployment policy, and
  generated assets are the product content to distribute. Do not redesign them in a release
  campaign. A material semantic or adapter defect terminates publication until it is fixed and
  freshly re-proved as a new source input.
- Current-public README/status/release claims advance only after independent exact and latest
  acceptance. Historical campaigns and all predecessor releases remain unchanged.

### 5.2 Public scope and security boundary

The release includes the closed `nostr-relay-info` recipe and deployment-bound `HttpClient.get`.
Admission must prove the exact candidate in both pinned userlands and through the maintained command,
distributed, stateful, outbound HTTP/TLS/DNS, and standalone-service boundaries. Outbound proof uses
only deterministic loopback fixtures: no live relay, Nostr publication/subscription, deployment,
ambient proxy authority, or endpoint-policy widening. Claim no new WebSocket, NIP-01, signing,
inbound-TLS, universal-portability, reproducibility, sandboxing, privacy, DNSSEC, or production
behavior.

### 5.3 Outcome-based success conditions

- **REL13-1 — exact final source:** the active campaign is uniquely reconciled; root version remains
  0.1.13; `R` contains coherent source, lockfile, generated assets, release workflow/procedure, this
  mandate, and root instructions; `R == origin/main`; the tree is clean except exactly named
  preserved work; `v0.1.13` and its release remain unused immediately before tag push.
- **REL13-2 — fresh source and consumer proof:** the authoritative final full profile passes freshly
  for `R` with no required reuse, skip, unavailability, failure, or unrun result. Executable
  discovery, generated documents, the maintained standard and `lkjournal` assets, all closed
  recipes, predecessor rejection, and product/contract identities agree.
- **REL13-3 — exact target and application admission:** first-party target tooling builds the exact
  static `x86_64-unknown-linux-musl` candidate from `R`; ELF inspection and both pinned userlands
  pass; target admission freshly passes the complete command lifecycle and all four maintained
  application/service oracles, including outbound HTTPS policy, failure, cancellation, restart,
  authority-unchanged, no-live-relay, and cleanup assertions.
- **REL13-4 — deterministic package and hosted dry run:** release prepare/verify binds `R`, target,
  candidate, verifier, capabilities digest, fresh full and target receipts, notices, archive,
  checksum, manifest, and canonical inventory; repeated packaging is byte-equal. One hosted
  `publish=false` run for exact `R` freshly passes build, full, target, package, no-checkout handoff,
  and all three transferred application oracles while every publication/post-publication job skips
  and no tag, release, draft, or asset is created.
- **REL13-5 — immutable publication:** after final external reconciliation, one exact annotated
  `v0.1.13` tag object selects `R`; the immutable-release setting and exact confirmation variable are
  verified; only the canonical tag-triggered, isolated no-checkout publication path creates one
  immutable, latest, non-draft, non-prerelease release with exactly the canonical asset inventory
  and matching GitHub asset digests.
- **REL13-6 — independent exact/latest acceptance:** separate anonymous exact-tag and
  `releases/latest` downloads each freshly pass checksum, attestation where owned, strict
  extraction, source/product/target/candidate binding, static inspection, distributed HTTP,
  first-party stateful HTTP, and outbound HTTP/TLS/DNS behavior against fresh isolated roots. Byte
  equality is checked only after both behavioral runs. Failed startup emits no readiness, changes no
  semantic authority, and cleans resources.
- **REL13-7 — repository and external closure:** bounded structured evidence and the terminal record
  identify every exact source, tag, workflow, handoff, release, asset, receipt, digest,
  classification, resource observation, retry, recovery, cleanup, push, and external mutation.
  Current public documentation is truthful; the evidence/current-truth commit `E` changes no release
  input, is normally pushed, and no predecessor publication, user data, deployment, or external
  recovery state is left unresolved.

### 5.4 Narrow empirical questions for Codex

1. What exact active branch, `HEAD`/tree, worktree/index/untracked state, remotes/upstream,
   divergence, stashes, supplied-file identities, and later work exist on entry?
2. What exact `R`, current gate inventory, toolchain/native inputs, target-policy digest, verifier
   identity, candidate digest, capabilities digest, handoff/receipt schemas, and generated owners
   bind release admission after reconciliation?
3. Does the current workflow already bind every outbound receipt and negative assertion through
   target, transferred pre-publication, publication dependency, and exact/latest post-publication
   boundaries, or is a minimal release-blocking correction required?
4. Are immutable releases enabled; are the exact tag and release unused; can the established
   confirmation variable be read and set exactly; and do current permissions, concurrency, checkout
   separation, artifact handoffs, and isolated publication still match `docs/release.md`?
5. What exact tag object, peeled source, workflow run/attempt/jobs, handoff artifact IDs/digests,
   release/assets/attestations, anonymous receipts, and public observations prove closure? Did any
   retry or additive recovery execute?

### 5.5 Implementation latitude and non-goals

Codex may choose focused filters, private roots, commit count, release-note wording, and a minimal
verifier/workflow correction proved necessary. Reuse first-party owners; do not refactor a passing
release path for aesthetics.

Out of scope: WebSocket or NIP-01 semantics; new language or authoring operations; owner-detail
projection; data/history scale, packing, compaction, or deletion; another data provider; a new
target; installer, package-manager, signing, or provenance work; package publication; deployment;
live-relay traffic; hosted or user-data migration; and documentation-system reorganization.

## 6. Dependency-ordered work

1. **Reconcile and establish `R`.** Complete section 3, preserve unrelated work, audit the delta
   from `v0.1.12`, incorporate this mandate and the supplied root instructions, and make only exact
   release-blocking or release-truth corrections. Keep current public prose saying 0.1.12 is latest
   until public acceptance. Regenerate owned output only through its owner. Commit coherent release
   inputs, normally fast-forward push them, and prove clean `R == origin/main`.
2. **Prove final source and candidate.** Run narrow checks only for changed owners while iterating,
   then one fresh full profile after all release inputs settle. Build the exact target candidate into
   a private absent path and freshly admit it through every current userland and copied-binary
   oracle. Do not rerun historical semantic-scale or live-relay experiments.
3. **Prepare and verify distribution locally.** Generate notices and deterministic release material
   through first-party tooling, repeat packaging, and strictly verify archive, checksum, manifest,
   extraction, candidate, target, and receipt bindings. Keep binaries, archives, downloaded tools,
   raw logs, and certificate secrets out of Git.
4. **Run the hosted dry run.** Dispatch the canonical Release workflow against exact `R` with
   `publish=false` and intended tag `v0.1.13`. Require all read-only build, package, target,
   handoff, distributed, stateful, outbound, and service dependencies to pass and all
   mutation-dependent jobs to skip. Download and independently verify the exact handoffs. Any input
   correction creates a new `R` and repeats invalidated gates.
5. **Publish once.** Reconcile external state again; prove clean equality with `origin/main`, unused
   tag/release, no conflicting run, immutable-release administration, exact confirmation authority,
   and fresh receipts. Create the annotated tag, verify its object and peeled `R`, bind the exact
   confirmation variable, push only that tag ref, and observe the canonical tag-triggered workflow.
   Do not manually create a release or upload an asset.
6. **Close the public boundary.** Require hosted and independent anonymous exact/latest acceptance;
   verify immutable/latest state and exact inventory; write
   `docs/evidence/202609010158-public-outbound-https-release.json`; then update only current public
   fact owners. Set lifecycle status, append the terminal record, make evidence/current-truth commit
   `E`, run narrow evidence-only checks, and normally push `E`.

## 7. Verification, evidence, and recovery

Use this ladder: minimal predecessor/source orientation; focused checks for actual corrections; one
fresh final full source profile; exact target and four-oracle admission; deterministic local package;
hosted dry run; immutable publication; independent exact/latest anonymous acceptance; evidence-only
closure. Do not rerun broad suites merely because this mandate lists them, but rerun every boundary
whose exact input changed.

Classify every gate as exactly `fresh passed`, `reused passed`, `skipped`, `unavailable`, `failed`, or
`not run`. Final full, target admission, local release verification, hosted dry run, publication
preconditions, and both anonymous public boundaries require fresh proof for exact inputs. Prior
outbound and v0.1.12 receipts are orientation only unless the current executable owner explicitly
proves exact-input reuse is valid; reused evidence is never fresh.

Required negative and containment cases include product/tag mismatch; used tag or release; disabled
immutability; unavailable confirmation authority; concurrent release work; stale, foreign, or
mutated receipts/handoffs; malformed or dynamically linked candidate; target-policy disagreement;
archive path/link/canonicalization/checksum/inventory failure; asset clobber; exact/latest identity or
behavioral disagreement; outbound forbidden destination, TLS, media type, limit, timeout,
cancellation, and malformed-response cases; failed application startup without readiness; and final
cleanup with unchanged semantic authority.

Before tag push, correct a bounded defect, form a new `R`, and rerun all invalidated gates. An
unpushed local tag may be removed only after proving it never crossed the remote boundary. After tag
push:

- never move or delete the tag;
- one exact-tag recovery dispatch is authorized only for an unchanged tag and a proven transient,
  idempotent orchestration failure that cannot create contradictory state;
- bounded read-only propagation, attestation, and download verification may retry;
- any source, workflow, target, verifier, package, candidate, or public-content defect requires the
  smallest unused additive patch, a new source commit, and every affected fresh gate;
- at most one content-changing additive patch is authorized by this mandate; a second distinct
  content defect terminates the campaign with exact externally committed state and the next
  recovery boundary.

Keep raw output in `.artifacts/campaign/202609010158/` or the current ignored verifier-owned roots.
The tracked evidence file contains bounded identities, digests, classifications, resource
observations, cleanup facts, and raw pointers, not full logs, private keys, downloaded binaries, or
inventories.

## 8. Read map

Begin here and expand only for an exact dependency:

- `AGENTS.md`, this mandate, `docs/campaigns/202608311849.md`, and
  `docs/campaigns/202608302224.md`;
- `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`, and `.github/workflows/release.yml`;
- `docs/release.md`, `docs/status.md`, `docs/architecture.md`, `docs/roadmap.md`,
  `docs/spec/verification.md`, `docs/spec/product-surface.md`, and
  `docs/spec/runtime-http-streams.md`;
- `docs/decisions/20260829-release-contract-version-authority.md` and
  `docs/decisions/20260831-deployment-bound-outbound-http.md`;
- first-party release target/build/admit/prepare/verify/verifier owners and check registry/policy;
- distributed HTTP, stateful HTTP, outbound HTTP, standalone service, target-admission, hosted
  handoff, post-release, generated-document, and structured-evidence owners under
  `tools/lkjscript-dev/`, `tests/`, and the executable registry.

Remote names require active-checkout verification. Discover narrow commands from executable help
and `docs/release.md`, not historical spelling.

## 9. Git and external-action policy

Stage explicit paths, inspect every staged diff, pair behavior with proof, and avoid unrelated
formatting, renames, dependency updates, or cleanup. Do not use destructive reset/clean/restore,
rebase, force push, broad staging, or history rewrite. End clean or name preserved work exactly.

| Action | Authorization |
|---|---|
| Edit source, tests, tooling, workflow, generated owners, and documents only as required for this release objective | authorized after reconciliation |
| Run local builds/tests, copied binaries, deterministic loopback HTTP/TLS/DNS fixtures, and private temporary roots | authorized; never ambient, hosted, user, or live-relay data |
| Make coherent local commits | authorized |
| Normal safe fast-forward pushes of `R`, `E`, and one authorized additive recovery | authorized after fetch, proof, staged-diff review, and collision checks |
| Hosted dry-run dispatch with `publish=false` for exact `R` and intended `v0.1.13` | authorized; repeat only when changed `R` invalidates it |
| Read immutable-release administration and the existing confirmation variable | authorized immediately before publication |
| Set `LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA` | authorized only to bind the exact newly verified annotated tag object |
| Create and push annotated `v0.1.13` | authorized once while unused and every precondition passes |
| Canonical tag-triggered immutable publication | authorized only through the existing workflow after all gates |
| One unchanged exact-tag recovery dispatch | authorized only for a proven transient, idempotent orchestration failure |
| Create/push the smallest unused additive patch and publish it | authorized only for the one post-tag content-defect recovery described in section 7 |
| Read-only workflow, artifact, attestation, release, asset, and anonymous-download verification | authorized and bounded |
| Move/delete/replace a tag; edit/unpublish/delete/relabel/clobber a release or asset; manual parallel release | forbidden |
| Force push, history rewrite, destructive cleanup, or mutation of predecessor publications | forbidden |
| Package publication, deployment, migration, database/hosted/user-data mutation, or live-relay traffic | forbidden |
| Issue, PR, settings, permissions, secrets, credentials, or repository-variable mutation other than the exact confirmation binding | forbidden |

If existing credentials do not permit an authorized external action, do not authenticate
interactively or weaken proof. Finish all unaffected local work, preserve coherent commits and
receipts, and report the exact unavailable action and repository relation. No permission here
authorizes destruction of unfamiliar local work.

## 10. Final report and terminal record

Return exact starting and final branch, `HEAD`/tree, worktree/index/untracked state,
remotes/upstream/divergence, stashes and preserved work, deviations, `R`, `E`, commits, and push
results. Report product and independently owned contract/package/application/artifact identities;
toolchain/native inputs; target policy; candidate/verifier/capabilities/archive/checksum/manifest
identities; every check and classification; receipt paths/digests; tag object and peeled source;
workflow run/attempt/jobs and handoffs; release/assets/digests/attestations; exact/latest observations;
resources and cleanup; retries or recovery; tested/observed/not-tested boundaries; limitations; and
every external mutation.

Confirm explicitly that no predecessor tag, release, asset, package registry, deployment, hosted or
user data, issue/PR/settings/secret, history, or published object was mutated. Name the highest
priority deferred candidates: Nostr WebSocket/NIP-01 using the released endpoint policy;
first-party data/history scale and retention; evidence-supported million-owner admission; and owner
detail only when demanded by a maintained authoring workflow.

Append a concise terminal record here with final status and time, starting/final identities,
implementation and closure commits, structured-evidence path/digest, required-gate classifications,
public identities, external actions, recovery branch, push state, preserved work, material
deviations, and deferred limitations. Do not paste raw logs, full manifests, inventories,
measurements, or generated documents.

## Terminal record

Completed at `2026-09-01 04:35:22 JST`. Entry `main` was
`1473532faa23bb8b958157ffe07bb73d422db48f` (tree
`1a258ab4f75a653f88d392b2d1caad2db73c65bd`); release-source commit `R` is
`dadc142c8230c58af6fc7884a3a4e3a5b7267d5e` (tree
`8e546b04de86e45d35bb7c1d6f22d3d26163b1d0`), and the containing evidence/current-truth commit is
`E`, whose exact SHA and normal push result are reported outside its own content. Structured
evidence is `docs/evidence/202609010158-public-outbound-https-release.json`, SHA-256
`2655ef2217c2486b8607d9edc2b9e7fdfafac1c85227eefcf7724b86565f71b1`.

Final full source (24/24 fresh gates), exact static-musl target and four-oracle application
admission (7/7 classifications), deterministic local package verification, hosted dry run,
publication preconditions, canonical publication, and hosted plus independently repeated anonymous
exact/latest boundaries are `fresh passed`. The dry run's mutation-dependent jobs are `skipped` as
required; no required final gate was reused, unavailable, failed, or unrun. The exact 16,152,800-byte
public candidate is SHA-256 `9149dee571ec2703a39e22561b98c840e275f5d0f8ffce7b0515b1ee8910e323`.
Annotated tag object `893dbcb107decd72d9bffbd6ca2d66389be7257a` peels to `R`; canonical
run `33425711515` attempt 1 published immutable latest release `379981013`, archive asset
`538397178` at SHA-256 `75d5fa42fcfeb8b8ad506de570df890de75abbaf9238e081ddb22ce0a8a739a9`,
and checksum asset `538397190` at SHA-256
`3395dcc58faeaff28d848c44d38283e0f784e259d90cd69cda436b6f89f34adb`.

`R`, `main`, and only annotated `v0.1.13` were normally pushed; the authorized confirmation
variable was bound exactly to the tag object, one `publish=false` dry run was dispatched, and the
canonical tag-triggered workflow performed the sole release/asset publication. One expected
read-only draft-discovery poll repeated; no workflow rerun, exact-tag recovery, additive patch, or
content-changing retry ran. Optional local attestation replay was `unavailable` because installed
GitHub CLI 2.45.0 lacks that command; the required hosted attestation gate was `fresh passed` and
its retained statement was inspected. A downloaded verifier's transport mode was normalized after
exact byte verification, one relative-path verifier call was reissued with absolute paths, and one
closure-profile invocation failed before gate selection until the pinned Rust toolchain path was
restored; none changed release content or invalidated required proof.

The final pre-`E` fetch found `R == origin/main`, zero divergence, no active release run, exact
immutable/latest state, and no open issue or pull request. Stash object
`87c2265f6faf0e815d80450ec2480d46c5e774fb` and pre-existing ignored `.artifacts/` and `target/`
contents remain preserved. No predecessor tag, release, asset, package registry, deployment,
hosted/user data, issue/PR, setting, permission, secret, credential, history, or other published
object was mutated. No deployment or live relay was contacted. Deferred limitations are Nostr
WebSocket/NIP-01 using the released endpoint policy, first-party data/history scale and retention,
evidence-supported million-owner admission, and owner detail only when a maintained authoring
workflow demands it.
