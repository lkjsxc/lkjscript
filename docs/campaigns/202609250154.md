# Explicit cumulative quotas for resident execution

## Original mandate (unchanged)

> Continue

## Reconciliation and selected work

Session started 2026-09-25 01:54 Asia/Tokyo on authorized device
`cw-bd235945fff44468a0f41292`. Local and independently read remote main are
`d4f574bac122b464068928c60fe5ed0c444192a3`. The inherited-pipe repair and native
text work are already delivered; do not duplicate them. No active Cargo/product
jobs, other worktrees or stashes were observed. Preserve unrelated untracked
`docs/campaigns/202609242022.md`. Root guidance remains appropriate.

Separate cumulative work quotas from live resource and deadline controls. Make
all four existing VM cumulative quotas explicit nullable deployment choices;
preserve the old numeric descriptor's actual defaults when new fields are absent.
Fresh HTTP recipes should explicitly choose no cumulative quotas, not silently
remove existing deployment limits. Keep complete runtime/execution objects and
positive structural limits, exact grants, deadlines, cancellation and joined cleanup.
Do not change graph/artifact/data formats or add a second VM or application primitive.

Require strict decoding, independent legacy/null/bounded mapping tests, copied
public CLI authoring and detached HTTP success/exhaustion/deadline/recovery tests,
and one dependency-complete fresh full acceptance at stabilized source. Then
normally integrate and independently confirm remote main. No credentials, access
controls, production deployment or immutable release assets are changed.

## Implementation and focused observations

The deployment boundary now carries four nullable cumulative choices. The three
new fields preserve literal predecessor defaults when omitted; fuel remains required.
The existing VM and public policy share one conversion/default owner. Fresh HTTP
recipes select four explicit nulls; no existing descriptor is rewritten. Discovery
contract 5 describes presence, legacy defaults, per-invocation scope and saturation.
Mixed foreground profiles are classified from all four quotas, not fuel alone.

Twelve deployment tests pass on the pinned release test build, including five new
independent legacy/null/invalid/schema tests. No full acceptance is claimed yet.
The external test drivers use literal native source and public plan/apply/build/run;
they do not construct an accepted graph or add a second host-language implementation.

Two combined read-only inspections were blocked before execution and were not
repeated as equivalent batches. Narrower permitted work continued. Two initial
build attempts found no Cargo on PATH and ran no tests. The existing pinned toolchain
was then selected through this process's PATH without changing any system setting.
The first public fixture wrongly converted JSON-encoded Bytes as Text and was
rejected during planning. Its corrected 600,000-iteration case executed successfully
but reported 6,600,010 instructions, below the required predecessor-quota witness.
That test correctly failed its own workload assertion; it is not a passing HTTP run.
The literal workload was enlarged to 1,000,000 iterations. Original failed logs
remain under `.artifacts/202609250154-resident-policy/` with distinct names.

Full acceptance, exact committed source, integration and publication observations
must be appended only after those actions have actually completed.

## Stabilized focused evidence

All 13 deployment tests now pass, including six new tests and explicit HTTP/session/
worker descriptor coverage. The three copied public CLI tests pass. The native work
witness returns 1,000,000, observing 11,000,010 instructions and 440,573,705 cumulative
allocated bytes on the focused product. This is work accounting, not an RSS measurement.
The same HTTP workload passes with four null quotas and fails with an explicit
10,000,000-instruction quota. The allocation, collection and invocation-call cases
report their own exact failure codes; health recovers after each failure. A separate
100 ms operational deadline cancels an unbounded loop, admits a subsequent health
request with only one worker, and joins shutdown. All six owned servers exit zero
with zero active/queued/remaining tasks. Test-local long-work deadlines are 90 seconds
for debug-build portability; the generated 30-second operational default is unchanged.

The first allocation fixture used 16 KiB and correctly also rejected health. The
corrected one uses 1,000,000 bytes and the large workload, preserving recovery as a
real assertion. All original failed logs remain distinct. Foreground reporting and
preflight rejection tests also pass; no older failed run is relabeled successful.
Focused Clippy passes with warnings denied. All eight guides were regenerated and
verified by the copied public product, not edited as generated text.

A same-artifact comparison with accepted predecessor d8dbb865 passes small legacy
execution, preserves its large-work allocation rejection, and rejects the new null
syntax on the predecessor. The new product accepts all four nulls, while fuel-null
alone retains legacy allocation exhaustion. The driver initially expected source
exit 2 for a resource failure; corrected labeled observations preserve actual exit 4.
The original semantic-CLI summary clarification batch was blocked before execution;
that file remains unchanged. The authoritative deployment specification, generated
schema and new resident-policy guide describe the implemented contract explicitly.

## First full acceptance and corrective descendant

Source c789e16bbe54ef5cf0ee264975d0565166dbac5d completed full verification with
24 fresh passes, one Clippy failure, and workspace_tests skipped for that prerequisite.
Inputs stayed stable; elapsed 522.268765612 seconds. The original receipt is
.artifacts/lkjscript-dev/check/1790271524937254318-416497-0/receipt.json.
This is failed source acceptance, not a 26-gate pass. The prior narrow Clippy check
did not cover structural_cli's separate compilation of shared test support.

Extend the existing handshake-based child-join self-test to check the new id and
try_wait methods against std::Child's independently obtained identity/status. This
uses the API in every importing test crate without suppressing dead-code warnings,
adding a second process owner, or relying on timed polling. Test-only deadline
coverage moves from the earlier successful 100 ms to 1,000 ms for debug scheduling
margin; the product's 30-second recipe default is unchanged.

A separate copied-product HTTP observation uses numeric per-invocation limits of
12,000,000 instructions, 500,000,000 allocated bytes and two capability calls.
Two serial and two concurrent large requests each return 1,000,000; two serial and
two concurrent clock requests each perform two permitted calls. A final health check
also passes. Nine requests pass with joined exit zero and zero active/queued/remaining
tasks. This distinguishes per-invocation accounting from a cumulative server budget.
All originals are retained under the campaign's parallel evidence directory.

The initial local commit failed for absent Git identity before any build began.
The successful commit used the repository owner's documented name/email through
per-command environment variables only; no Git configuration or credentials changed.

## Complete recipe transition and independent authority control

The next full source cd9c8b0a736b24b5d769ea4fa916d5e27d889452 completed with
25 fresh passes and one workspace_tests failure; input_stable=true.
Elapsed 544.44893153 seconds. Its original receipt is
.artifacts/lkjscript-dev/check/1790272410869961044-425195-0/receipt.json.
The workspace's library tests reported 788 passed, one failed and eight ignored.
The failure was the captured HTTP recipe projection, which includes its deployment.
It correctly detected the intentional new execution defaults. This was a missed
consumer-test transition, not a reason to discard or overwrite the captured oracle.

The correction keeps every original projection digest. It independently asserts
all six current execution fields, temporarily restores only the literal predecessor
three-field policy in an owned test descriptor, and requires the complete frozen
projection to match. The accepted meaning graph is never edited by that fixture.
Both current and restored policies remain observably different. This proves the
rest of each HTTP and relay-information recipe, including topology, grants and live
limits, stays unchanged. All seven project-creation tests now pass.

The shared child-observation test passes in all four importing integration suites.
No dead-code or lint suppression was added. The verifier binary itself is unchanged
by these test/documentation corrections and is reused as an immutable executable;
fresh full execution still runs every selected gate and captures the actual source.

With every invocation quota null, a separate native clock handler attempting three
calls under its declared two-call allowance fails with normalized_grant_calls, not
normalized_capability_calls. Two such rejections and two subsequent health requests
pass the expected outcome checks. The server joins with exit zero and no remaining
active/queued tasks. Originals are retained in the grant-boundary evidence directory.

The three copied public policy tests also pass with the unoptimized debug executable
(36.60 seconds), including the corrected 1,000 ms cancellation case. Full workspace
all-target/all-feature Clippy passes after the recipe-oracle correction. The complete
fresh acceptance of this final corrective source is still a separate next operation.

## Resumption — 2026-09-25 04:15 Asia/Tokyo

Original user continuation: Continue. Local main is 1cf74cfbc25feb08e4b6aab906b825b4ab307bea,
three commits ahead of independently read remote d4f574ba. No active build or product
process, other worktree, or stash was observed. The unrelated untracked prior campaign
remains untouched. The final attempted full run completed with 25 fresh passes and
one workspace_tests failure, stable inputs, at the exact local source. Its library
suite passed 789 tests with eight ignored, then three copied data-restore tests
reported child crashes without retaining status/stderr in their assertion message.
The original receipt is .artifacts/lkjscript-dev/check/1790273159525576908-436305-0/receipt.json.
Preserve that failed acceptance. Add failure diagnostics before diagnosing the cause;
do not infer a policy or data-format defect from a signal-free summary.

## Resumption — 2026-09-25 06:29 Asia/Tokyo

Original user continuation: Continue. The diagnostic run had completed with three
SIGSEGV failures, empty child output, and seven passing support/data tests. The
local source and independently read remote main are unchanged from the prior
checkpoint; no build or product process was active.

A debugger acquired from the configured Ubuntu package archive was extracted only
under the ignored campaign diagnostic-tools directory. No package was installed,
no publishing credentials were supplied, and no host security setting changed.
A fresh empty backup from the retained working product reproduced the crash before
backup bytes were read: DataStore::restore -> path_exists -> symlink_metadata ->
Path::as_ref. GDB stopped in 22 zero bytes where the final debug ELF declared that
function. The corresponding function in the cached walkdir object archive retained
valid nonzero instructions. The failing debug executable was byte-identical to the
previously retained 451,713,312-byte copy. This establishes a corrupt linked output,
not an identified storage, kernel, compiler or linker root cause.

The original executable, archive, extracted object, symbol inspection and debugger
log remain retained. After reviewing Cargo's package/profile-scoped dry run, only
lkjscript debug build outputs were cleared. Source, dependencies, release artifacts
and campaign originals were not cleaned. Rebuilding the same workspace/all-feature
data_admission_cli target, without changing restore implementation, passes all ten
tests. The rebuilt function has normal instructions. Its retained executable is
451713312 bytes. The only test-source change adds signal/stdout/stderr to a
crash assertion; it changes neither fixture bytes nor any expected outcome.

The early combined stack/probe diagnostic request was blocked before execution and
not retried. The later debugger exercise used a separately created, owned empty
backup and did not change process stack limits. Old failed full receipts remain
failed; the repaired build state now requires fresh complete acceptance.

## Completed full acceptance and mainline delivery — 2026-09-25 06:50 JST

The inherited recovered-build full check completed at exact source
8242f3a71e71eab49c70857f76716c8cc30a72dd: 26 passed, all fresh, zero reused,
740.134247095 seconds, process exit zero. Original receipt:
.artifacts/lkjscript-dev/check/1790285812860383919-458272-0/receipt.json.
Normal push moved remote main from d4f574ba to this exact source, independently
confirmed through GitHub's ref endpoint. No failed predecessor is relabeled.
Public v0.1.44 remains unchanged; successor delivery is selected separately by
[campaign 202609250650](202609250650.md).
