# Executable identity without stale acceptance

## Original mandate (unaltered)

@Remote Desktop Commander https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscriptについて進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。限界まで深く考えてほしい。超長期的な視点からお願いしたい。ChatGPTの開発環境として、cw-bd235945fff44468a0f41292を用意しました。自由に活用ください。remote desktop commanderについて、月ごとにtool callingが1万回までという制限を考慮しつつお願いします。

## Baseline and execution boundary

Campaign timestamp: 2026-09-24 15:35 Asia/Tokyo. The resolved repository is
`lkjsxc/lkjscript`, starting from main
`5e75bd73574196c06e73ae3ce46f28c2b6a37179`, tree
`65b77bf7890199eca4b23dcb0f2e505546fdf1a1`.

The first combined read-only Remote Desktop Commander process request was rejected
by the access safety check. It did not execute. The rejection was not bypassed,
and no further remote-machine commands were attempted. GitHub's authorized
repository connection supplies independent source access and ordinary Git writes.
The provided workspace has not been inspected, modified, synchronized or cleaned.
A disposable local clone also failed because this execution environment could not
resolve GitHub; local Rust compilation is not claimed. Use the existing opt-in
Verify workflow, not the publication pipeline or a weaker replacement verifier.

Read root AGENTS.md, current status/roadmap, existing Verify/release workflows and
the affected evidence, process, snapshot, cache-consumer and original-reader code.
The first inference that development CI was absent was corrected after inspecting
`.github/workflows/verify.yml`. No new CI system is needed. Root AGENTS.md remains
unchanged because its preservation, independent-evidence and mainline rules fit.

## Selected correction

The old executable observation called the ordinary non-following FileProof owner.
For a final symlink it bound only the link text/mode, not the executable's bytes.
Changing a target behind an unchanged link could therefore preserve a successful
gate key and the recorded runtime binding. PATH lookup also interpreted relative
entries in the observer's cwd and selected a regular file without checking whether
the effective identity could execute it. These are concrete acceptance defects,
not a reason to add more hashes indiscriminately or rewrite the language kernel.

Use one typed ExecutableProof in the existing runtime and gate-fingerprint owners.
Keep the selected entry proof and, only for a symlink, a separate resolved regular
file proof. Both receive the same logical command label so immutable temporary
copies retain legitimate reuse. Preserve ordinary source symlink behavior and the
existing VerificationDigest implementation. No parallel receipt/store is added.

Resolve explicit relative/empty PATH entries against the child repository cwd;
Linux effective-identity execution access skips non-executable earlier candidates.
Do not guess an implicit PATH. A selected broken/cyclic/nonregular linked target
fails admission rather than gaining a file certificate. This remains an observation
of the command leaf, not a transitive interpreter/library/plugin closure, executable
pinning against concurrent replacement, or a hostile-filesystem sandbox.

Check/source receipt contract 7 and cache contract 3 explicitly separate the new
binding from predecessor evidence. Old accepted records are not relabeled. Frozen
release artifacts and language, graph, typed-data and public CLI contracts remain
unchanged. No release or production deployment is selected.

## Required evidence

Add seven Linux unit/integration tests covering unchanged-link same-length byte and
mode replacement; link chains and same-content retargeting; broken/cyclic/directory
target rejection; normalized immutable copies; actual Linux PATH execution with
relative/empty entries and an earlier non-executable file; real cached success,
unchanged reuse, predecessor-record rejection, changed-target failure and restored
reuse; and original-runtime rejection/recovery after target replacement. The old
non-following entry proof is retained as a negative control in the mutation tests.
Extend the existing original source reader's forged-evidence matrix for a
consistently rehashed target substitution and predecessor check contract.

Run the maintained fresh full profile against the exact candidate, retain the
original source/run/attempt and all failures, and inspect its complete receipt.
Only then refresh main and normally integrate an accepted candidate without force
or protection changes. Independently read the resulting main ref. A pending CI
run, successful artifact upload or source review alone is not acceptance.

## Deferred finding and explicit limits

`check/snapshot.rs` identity subprocess collection caps retained stream bytes but
waits for child exit and pipe EOF without a deadline. A hanging command or inherited
pipe can stall this preflight path. This campaign does not fix or certify that
lifecycle. A separate correction must cover both process ownership/cleanup and
bounded pipe completion rather than merely adding a timer around an unjoined task.

At this candidate's creation no new tests or full acceptance have executed. Append
actual observations only after inspecting them; never convert this plan into a
passed result. Remote Desktop Commander account-wide remaining quota is unknown.

## Candidate review: selected-path execution

Initial candidate `00282249dbb18ea6611582769dba84f9fc7e42b9` was pushed normally to
`verify/202609241535-executable-evidence`. Following the predecessor workflow guide,
the branch was first created at the base. This also triggered base run 35965360640;
candidate run 35965373009 was pending behind it. Both are attempt 1. The connected
GitHub actions expose no cancellation operation; their permissions/concurrency were
not changed to work around that restriction. The guide is corrected to create new
verification branches directly at the prepared candidate SHA, avoiding this waste.

Independent disposable local Linux experimentation exposed an additional selection
mismatch before candidate acceptance. GNU env/execvp skipped an executable first
PATH script with a missing shebang interpreter and executed the later script. Only
changing that later script changed observed exits from 37 to 7, while the first
regular executable's bytes and permissions stayed identical. This is an observed
OS experiment, not an executed Rust regression or full-profile pass.

The correction now carries one selected absolute pathname alongside its proof.
The gate fingerprint consumes that proof, and fresh execution supplies the same
pathname to the existing process owner. The declared argv[0], remaining arguments,
environment, cwd and process controls are preserved. A missing selection does not
trigger another search; a missing interpreter is unavailable instead of executing
an unobserved later program. The physical selection is not a second evidence store
and is not confused with the normalized logical command label. Existing unbound
process helpers keep their ordinary lookup behavior. File-descriptor/inode pinning
against concurrent replacement remains explicitly outside this correction.

Add an eighth executable regression that independently demonstrates actual Rust
Command PATH fallback, then proves selected execution cannot create the later
program's marker. Three process-launch tests cover declared argv[0], missing and
relative selection refusal, and the inherited timeout/output bounds. Together with
the seven original regressions these are eleven new tests, plus two new forged
source-reader cases. They are requirements until the corrected source actually
executes successfully. Incidental extra derives and a redundant clone from the
initial candidate are removed; no original failure evidence is rewritten.
