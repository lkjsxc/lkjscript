# Immutable 0.1.15 Affine Resource Product Publication

Status: incomplete

Campaign ID: `202609011758-affine-resource-release`

## 1. Identity, audit boundary, and lifecycle

Audit time: `2026-09-01 17:58:33 +09:00` (`Asia/Tokyo`).

Orientation source: remote GitHub repository `lkjsxc/lkjscript`, default branch `main`, exact commit
`a4b4186384476c4773dc679ca86cead86562b3bd`, tree
`3a4b868826f6e8ba5818e251b5a9962b1f9e521a`. This is a remote default-branch observation, not an
active checkout. The active checkout outranks it after reconciliation.

Inspected: repository identity and recent history; the diff from the `v0.1.14` release source; root
`AGENTS.md`; latest complete, release, and terminated campaigns; current status, architecture,
roadmap, release procedure, and relevant semantic and verification owners; root product version;
release workflow; public release and tag state; public issue/PR state; and queued/in-progress
workflow state.

Observed external state:

- source declares product snapshot `0.1.15`;
- remote `main` contains affine implementation commit
  `44a35c3c3490f9734533f35e480617f50a779cd7` and its evidence closure;
- immutable latest public release is `v0.1.14`, release `380190031`, from source
  `b9f37cb86353596d260521924b5986300595450e` through annotated tag object
  `07590034938283fabe48721980290dd24c1f72e3`;
- release run `33464502157`, attempt 1, succeeded;
- no remote `v0.1.15` tag or release, open issue/PR, or queued/in-progress workflow was observed.

Unavailable: local branch/`HEAD`/tree, index, worktree, untracked and ignored files, remotes,
upstream/divergence, stash, supplied files, installed binary, toolchain and execution, private
receipts, workflow artifacts, repository variables, immutable-release administration, credentials,
deployments, hosted data, and production behavior.

Archive: `docs/campaigns/202609011758.md`, absent at the orientation revision.
`docs/campaigns/` remains the sole mandate archive. No mutable current-work ledger was observed; do
not create a competing prompt, plan, work, or campaign tree. Preserve this initial body. Change only
the minimal lifecycle field and append one bounded terminal record. Current facts, semantics,
rationale, proof, generated projections, and raw artifacts stay with their existing owners.

Predecessor disposition:

- `202609011246` is complete. It delivered Graph 6 exact-interface affine resources and parameter
  use, compiler/runtime enforcement, the nine-operation durable-queue cutover, standard and
  `lkjournal` migration, predecessor deletion, public authoring, independent proof, service/worker
  proof, and static-musl target admission. Implementation is `44a35c3c3490f9734533f35e480617f50a779cd7`; closure is the
  orientation commit.
- `202609010950` is complete and published immutable latest `v0.1.14`; no recovery remains active.
- `202609010657` and `202609010158` are complete predecessors. `202608310137` is terminated and
  superseded. Immutable `v0.1.8` is an unchanged historical recovery point, not current work.
- Historical records name stash object `87c2265f6faf0e815d80450ec2480d46c5e774fb` and ignored or
  root-owned artifacts. Preserve anything found without assuming it still exists.
- No active remote source, release, or deployment mandate was observed. Later legitimate checkout
  and external evidence win.

Applications and protocol integrations are proof consumers of the platform, not product-priority
authorities. `lkjournal`, HTTP, and Nostr-related workloads may prove reusable graph/compiler/runtime
behavior; they must not move private domain policy into Rust or determine priority by enthusiasm.

## 2. Implementation mandate and stopping rule

This is release implementation, publication, and proof, not another semantic design or application
campaign.

**Required final state:** the exact completed `0.1.15` source snapshot—including Graph 6 affine
capability resources, unrestricted/borrow/consume parameter use, compiler/runtime move enforcement,
the resource-owned durable-queue interface, migrated maintained consumers, and deletion of raw
attempt-token program authority—is available as one immutable latest `v0.1.15` self-contained
`x86_64-unknown-linux-musl` release whose exact candidate, package, hosted workflow, and anonymous
exact/latest downloads freshly satisfy current source, target, public-authoring, service/worker,
HTTP, stateful-data, outbound-network, integrity, containment, and cleanup contracts, while
predecessor publications remain unchanged and no deployment, hosted data, or live relay is touched.

`R` is the exact release-source commit. It contains this mandate, the reconciled root `AGENTS.md`,
the completed `0.1.15` source, and only changes required to make the existing release path truthful.
Root version remains `0.1.15` before initial publication. `R` must be clean, normally pushed, and
exactly equal to `origin/main` before tag creation.

`E` is the post-publication evidence/current-truth commit. It may update the lifecycle and terminal
record, bounded evidence, status, architecture, roadmap, release procedure, performance summary, and
README. It must not change source, workflow, target/verifier behavior, generated or normative input,
maintained artifacts, lockfile, or anything bound by final receipts. Such a change creates a new `R`
and invalidates affected proof.

Outcome rules:

- `complete`: REL15-1 through REL15-8 pass; publication is immutable/latest; anonymous exact/latest
  acceptance is fresh; pushes close; and the tree is clean or preserved work is exact.
- `blocked`: the objective remains valid but a required gate or external precondition is unavailable.
  Leave status `incomplete` and record the exact reversible or externally committed state.
- `terminated`: a material source, release-path, safety, or recovery defect prevents dependency-closed
  completion.
- `superseded`: a legitimate later campaign or immutable release already owns the final state.

Continue through non-material path, symbol, count, or command drift. Benign warnings, optional cache
failures, missing convenience tools, and absent interactive login are not blockers unless a required
gate or authorized action is affected. Stop before overwriting unfamiliar work, mutating a published
object, taking an unauthorized irreversible action, or claiming completion without a required
boundary.

## 3. Reconciliation gate

Before editing or mutation:

1. Read applicable agent files, this mandate, terminal records `202609011246` and `202609010950`,
   current status/architecture/roadmap/release documents, relevant specifications/decisions, and
   first-party verification/release owners.
2. Capture branch, `HEAD`/tree, index/worktree/untracked/ignored state, remotes/upstream/divergence,
   recent commits, stashes, supplied files, toolchain, product version, and unfamiliar work. Preserve
   unrelated work; do not reset, clean, restore, rebase, or apply/drop a stash.
3. Fetch without rewriting history. Audit the exact delta from release source
   `b9f37cb86353596d260521924b5986300595450e` through active `HEAD`; confirm the affine campaign is
   terminal and no later delta conflicts with publication.
4. Reconcile every plausibly active mandate by lifecycle, commits, consumers, release, and external
   state rather than filename recency. Close or stop for coherent in-flight work first.
5. Locate current owners of full verification, target build/admission, deterministic package,
   verifier handoff, hosted dry run, isolated publication, anonymous acceptance, generated docs, and
   evidence.
6. Confirm the completed resource/use semantics, independent affine oracle, package interface,
   compiler/artifact movement, runtime resource scope, queue adapter, standard graph, `lkjournal`
   worker, copied-binary authoring, generated assets, and predecessor rejection remain intact.
7. Prove the changed worker behavior is bound to public bytes: target-admission service receipt →
   exact candidate digest → release package → strict no-checkout extraction → anonymous exact/latest
   extracted candidate digests. Version, path, or archive-name equality is insufficient.
8. Inspect release predicates for current Graph 6, CLI 17, artifact 11, service receipt 5, and
   capabilities metadata. Correct only an actual stale predicate or missing exact binding; do not
   invent another oracle when the existing candidate chain is complete.
9. Immediately before external mutation, refresh `origin/main`, active workflows, tag/release
   availability, immutable-release administration, confirmation-variable authority, permissions,
   concurrency, public latest, and recovery state. Use only existing noninteractive credentials.

Stop only for a different authority, conflicting objective, used identity, unrecoverable required
gate, unsafe unfamiliar work, or impossible closure.

## 4. Current state and bottleneck

At the orientation revision:

- one accepted typed graph revision is the sole editable program authority;
- source is unreleased `0.1.15`, three commits ahead of and not behind the `v0.1.14` source;
- accepted meaning owns exact-interface resources and unrestricted, borrow, and consume parameter
  use; invalid fabrication, aliasing, post-consume use, foreign authority, branch disagreement,
  function transfer, containment, and durable escape reject before publication;
- compiler, artifact, loader, VM, and task resource scope retain and recheck movement and acquiring
  requirement;
- standard `DurableQueue` exposes nine resource-based operations; raw attempt/worker strings are
  private operational authority, not graph transition rights;
- standard and `lkjournal` graphs/artifacts, public compact authoring, independent affine proof,
  service/worker proof, generated docs, and predecessor deletion are complete; and
- data-store, queue-data, backup, deployment, HTTP-client, object, and release contracts did not
  advance with the source campaign.

Source discovery reports capabilities digest
`855a26b1e6b5ad01be305c6fc8f61b68697cf6fe8e4feddc38d3bf394a1bc23b` at orientation. Prior fresh
receipts are historical only because this mandate and any release-path change produce a new commit.

The bottleneck is distribution authority, not missing semantics. The strongest current resource
result exists only on `main`; supported public `v0.1.14` lacks it. Another feature would deepen the
source/public split that root policy requires closing. The product objective is the reusable affine
mechanism; maintained applications and protocols are proof boundaries.

## 5. Campaign decision

Scores are comparative; value/leverage/closure/proof use `1` weak through `5` strong, while cost and
risk use `1` low through `5` high.

| Candidate | Value | Leverage | Authority | Closure | Completion | Proof | Cost | Risk | Decision |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| Publish immutable `v0.1.15` | 5 | 4 | 5 | 5 | 5 | 5 | 3 | 4 | **Select** |
| Resource transfer across task functions | 5 | 5 | 4 | 3 | 2 | 5 | 5 | 3 | Defer: no maintained need |
| Nostr WebSocket/session slice | 3 | 5 | 3 | 2 | 2 | 4 | 5 | 5 | Defer: proof app; authority unclosed |
| Million-owner/history evidence | 3 | 2 | 2 | 3 | 3 | 4 | 4 | 2 | Defer: separate decision |

Publication closes coherent unfinished public work. It adds no
new graph meaning; report it as distribution of the preceding authority contraction.

Task-function transfer is the strongest semantic alternative, but current work keeps acquisition and
consumption inside one task. Selecting it now would invent need and broaden function, result,
recursion, cancellation, and branch semantics before publishing the existing slice.

Nostr WebSocket/session semantics remains admissible only as a future proving slice with a maintained
consumer and closed connection, framing, subscription, reconnect, signing/key, authentication,
replay, resource, and raw-protocol proof. It is not the platform mission. Scale/history, owner detail,
and worker recipes remain separately evidence-gated.

## 6. Fixed target state and scope

### 6.1 Authority and identity

- Root version stays `0.1.15`; initial intended tag is unused annotated `v0.1.15`.
- `R` is the sole tagged source and must equal `origin/main`. Publication alone does not advance
  semantic, compiler, runtime, data, deployment, or adapter contracts.
- Semantic/package/artifact/target/candidate/tag/release/deployment identities remain separate.
  Manifests, receipts, checksums, handoffs, assets, and attestations are derived evidence.
- Public claims advance only after anonymous exact/latest acceptance. Predecessor public objects are
  immutable and recovery is additive.

### 6.2 Published semantics and public authoring

Preserve, without redesign:

- `CapabilityResource<ExactInterface>` acquired only through its exact requirement;
- unrestricted/borrow/consume use, ordered borrows and one consume, branch agreement, direct nominal
  payload movement, and rejection of function transfer or resource-bearing aggregates;
- task-local, droppable, non-equal, non-serializable resources with no literal, constant, test, data,
  object, queue-payload, cache, or durable escape;
- compiler/artifact/runtime single-consume agreement and capacity reservation before claim/renewal;
- the nine-operation queue interface, with no raw transition authority in program values; and
- one deterministic compact authoring spelling and generated discovery for resource/use forms.

A copied final candidate must discover, inspect, query, plan, apply, review, check, build, serve, and
worker-run supported paths. It must reject fabrication, duplicate/post-consume use, foreign
requirement, illegal containment, branch mismatch, function transfer, predecessor forms, stale
plan/token, malformed request, and hostile artifact movement without semantic advance or resource
leak. Add no aliases, compatibility reader, second authoring form, or private constructor.

### 6.3 Proving consumers

`packages/standard/` and `applications/lkjournal/` remain maintained proof consumers and their final
graph/package/artifact/generated identities must reproduce exactly. Service/worker proof covers
claim, info borrow, renewal, completion, retry/failure, stale replacement, cancellation/cleanup,
restart, failed readiness, backup/restore, unchanged semantic authority, and operational-data
compatibility.

Do not add application-specific public commands or host semantics. HTTP, stateful-from-`minimal`,
outbound HTTPS/NIP-11, and Nostr-related checks are regression/capability oracles only; keep them on
isolated loopback fixtures and never contact a live relay.

The checkout-aware service oracle need not become a fifth no-checkout verifier merely for symmetry.
Instead require exact candidate-digest continuity from service proof through package and anonymous
extraction. Repair only a real break in that chain.

### 6.4 Distribution and nonclaims

The sole target remains `x86_64-unknown-linux-musl`. Use the typed target owner; directly reject an
ELF interpreter, `DT_NEEDED`, GLIBC requirements, foreign machine, malformed linkage, or policy
mismatch. Run both pinned userlands and every current target oracle, including service/worker.

Use first-party release tooling for deterministic prepare/verify. Run one hosted dry run before the
tag; its read-only source/target/package/handoff and transferred distributed/stateful/outbound gates
must pass while mutation jobs skip. Then create one annotated tag, bind the existing non-secret
confirmation variable, and push only that tag. The canonical tag workflow is the sole release writer.
Separate anonymous exact/latest downloads must each pass integrity, attestation, extraction, identity,
static, and current transferred-application checks before byte equality is corroboration.

Claim only the tested source, target, candidate, userlands, archive, integrity, and behavior. Do not
claim generic Linux portability, minimum kernel, reproducibility, signing, provenance, sandboxing,
multi-tenant isolation, encrypted storage, inbound TLS, DNSSEC, privacy, exactly-once work, or service
levels.

### 6.5 Latitude and non-goals

Codex may choose focused checks, commit count, private evidence roots, release notes, and narrow
factoring for an actual release blocker. Do not manufacture source churn or refactor a passing path.
A necessary workflow/verifier/package/metadata/test correction forms a new `R` and reruns affected
proof.

Do not add task-function transfer, affine containers, general linear/session semantics, closures,
WebSocket/NIP-01, key custody/auth/replay, owner detail, worker recipe, package workflow, target,
installer, registry, CI matrix, database/query redesign, retention/compaction, deployment, migration,
or application assets in the product release.

## 7. Success conditions

- **REL15-1 — exact source:** reconcile active work; confirm affine closure; keep root `0.1.15`; form
  clean `R` containing this mandate, current root policy, completed source, and only required release
  fixes; prove `R == origin/main`; preserve exact unfamiliar work; confirm no conflicting run and
  unused `v0.1.15` immediately before tagging.
- **REL15-2 — fresh source:** one fresh full profile for `R` closes Graph 6 semantics, independent
  affine oracle, public direct/file authoring, standard/`lkjournal`, generated and clean/incremental
  equality, service behavior, production/reference agreement, negative cases, and predecessor
  rejection with no required reuse, skip, unavailability, failure, or unrun gate.
- **REL15-3 — exact target:** one exact static-musl candidate from `R` fresh-passes ELF inspection,
  both userlands, command lifecycle, distributed/stateful/outbound oracles, standalone service/worker,
  containment, authority preservation, and cleanup. Service receipt binds its exact digest.
- **REL15-4 — package:** preparation binds source/tag, target, candidate, capabilities, toolchain,
  locked closure, notices, and fresh receipts. Repeated packages are byte-equal; strict verification
  rejects malformed inventory and proves candidate continuity.
- **REL15-5 — dry run:** one `publish=false` Release run for `R` freshly passes build/full, target,
  package, handoff, and no-checkout application gates. Publication/post-publication jobs are `skipped`
  and no public object is created.
- **REL15-6 — publication:** refreshed preconditions pass; annotated `v0.1.15` selects `R`;
  immutability is enabled; confirmation variable equals tag-object SHA; only the tag is pushed; and
  the canonical workflow creates one immutable latest release with canonical assets/digests.
- **REL15-7 — public acceptance:** separate anonymous exact/latest downloads each fresh-pass checksum,
  owned attestation, strict extraction, source/target/candidate/static identity, and current
  distributed/stateful/outbound behavior on fresh roots. Candidate digest equals the service-tested
  digest; compare exact/latest bytes only afterward; cleanup completes.
- **REL15-8 — closure:** bounded evidence records all exact identities, classifications, resources,
  retries/recovery, cleanup, pushes, and mutations. Current docs are truthful; `E` changes no bound
  input; predecessors are unchanged; normal push closes; and the campaign is terminal.

## 8. Dependency-ordered work

1. Reconcile Section 3 and capture the exact source/public baseline, preserved work, and unused
   identity.
2. Install this mandate and supplied complete root `AGENTS.md`; retain only its durable
   proving-consumer clarification, with no policy churn.
3. Audit current release owners against final Graph 6/CLI 17/service-receipt-5 inputs. Correct only a
   release blocker; regenerate only through owners; form, review, push, and prove `R == origin/main`.
4. After inputs stabilize, run one fresh full profile; build/admit one candidate through static
   inspection, both userlands, and all target oracles.
5. Prepare twice, require byte equality, strictly verify into absent private paths, and prove service
   candidate-to-package digest continuity.
6. Run one hosted dry run; inspect exact run/attempt, handoffs, receipts, classifications, and skipped
   mutation jobs.
7. Refresh preconditions, create/verify the annotated tag, bind the confirmation variable, push only
   the tag, and observe canonical publication.
8. Complete hosted and independent anonymous exact/latest acceptance; verify immutable/latest and
   predecessor state; write bounded evidence/current truth, append terminal record, commit `E`, run
   narrow closure checks, push normally, and report state separation.

## 9. Verification, evidence, and recovery

Use first-party owners and checkout-authoritative invocations. Efficient order: minimal
baseline; focused release-fix checks; one fresh full profile; exact target admission including
service/worker; deterministic package twice; hosted dry run; annotated-tag publication; hosted and
independent anonymous acceptance; evidence-only closure. Repeat a broad gate only after changed input
or necessary failure isolation.

Classify every gate exactly `fresh passed`, `reused passed`, `skipped`, `unavailable`, `failed`, or
`not run`. Final full, target, service, package, dry run, publication preconditions/publication, and
both public boundaries require fresh exact-input proof. Dry-run mutation jobs must be `skipped`. A
required unavailable gate blocks completion.

Retain independent affine/reference agreement, copied-binary valid/invalid authoring, authority
comparison around rejection, clean/incremental equality, independent queue-state observation,
loopback HTTP/TLS/DNS fixtures, direct ELF parsing, strict no-checkout handoff/package validation,
exact GitHub object observations, and separate anonymous exact/latest behavior.

Negative release proof covers stale/foreign receipts, source/tag mismatch, used identity, disabled
immutability, bad confirmation binding, concurrency, altered handoff, candidate mismatch, stale
service receipt, dynamic/malformed ELF, policy mismatch, unsafe/corrupt archive, asset clobber,
exact/latest disagreement, predecessor/invalid affine forms, rollback, cancellation, startup without
readiness, unchanged authority, and cleanup.

Record only verifier-owned resource dimensions and their units: process wall/CPU/RSS where available,
bytes, gate/userland/application/queue/workflow counts, retries, cleanup, and publications. Infer no
latency, throughput, memory safety, portability, provenance, isolation, or service level.

Keep large output under `.artifacts/campaign/202609011758/` or current ignored owners. Track bounded
summaries at `docs/evidence/202609011758-affine-resource-release.json` unless current convention
requires an equivalent name.

Before tag push, repair a defect in a new `R`, rerun invalidated proof, and recheck identity. Remove an
unpushed local tag only after proving it never reached remote. After tag push, never move/delete it.
One unchanged exact-tag recovery dispatch is allowed only for proven transient idempotent failure.
A content/input defect requires the smallest unused additive patch and fresh affected proof; at most
one content-changing additive recovery release is authorized. A second content defect terminates for
reassessment. Published objects remain immutable.

## 10. Narrow empirical questions

1. What active Git/worktree/upstream/stash/supplied/later/preserved state exists?
2. What exact `R`, contracts, generated owners, toolchain, target policy, candidate, capabilities,
   verifier, and receipt schemas bind release?
3. Is service-tested candidate identity exact through package, handoff, manifest, and anonymous
   exact/latest extraction?
4. Do current workflow predicates consume Graph 6, CLI 17, artifact 11, and service receipt 5 without
   stale assumptions; if not, what smallest release-only fix closes them?
5. Are immutability, unused tag/release, confirmation authority, permissions, and concurrency ready?
6. Which exact tag/run/handoff/release/asset/attestation/anonymous identities prove closure?

These questions do not reopen semantics, compatibility, consumers, target, or authorization.

## 11. Read map

Start with root `AGENTS.md`; this mandate; campaigns `202609011246` and `202609010950`; Cargo,
toolchain, workflow, and release owners; current status/architecture/roadmap; relevant affine,
effects, package, artifact/runtime, verification, product, and CLI specs/decisions; first-party
check/target/release/verifier/service/application/evidence owners; public and predecessor tests; and
standard/`lkjournal` authorities and generated assets. Expand only for exact dependencies or a
failing boundary. Do not reread every campaign or ingest raw artifact trees.

## 12. Git and external actions

Stage explicit paths, inspect staged diffs, avoid broad staging and unrelated churn, and keep `R`,
optional additive recovery, and `E` distinct. End clean or name exact preserved work.

| Action | Authorization |
|---|---|
| Local scoped edits/generated output | Authorized after reconciliation only for mandate, supplied root policy, necessary release fixes, and truthful evidence/docs |
| Local builds/tests/copied binaries/pinned userlands/loopback services/isolated data | Authorized; no live relay, non-loopback exposure, hosted/user data, or production authority |
| Maintained graph/package/artifact change | Forbidden unless an actual release-blocking inconsistency exists; preserve semantics, use owners, form new `R` |
| Coherent commits | Authorized after review/proof; distinguish `R`, optional recovery, and `E` |
| Normal fast-forward push | Conditional for `R`, `E`, and one additive recovery after fetch, no conflict, proof, review, and existing noninteractive credentials |
| Workflow dispatch | One dry run per final `R`; one exact-tag recovery only under Section 9; otherwise forbidden |
| Annotated tag | Initial `v0.1.15` once after all preconditions; additive recovery only under Section 9 |
| Confirmation variable | Read; set only existing non-secret binding to exact new tag-object SHA immediately before tag push |
| Release/assets/attestations | Only canonical tag-triggered isolated workflow; manual create/upload/edit/replace/unpublish/delete forbidden |
| Read-only repository/workflow/release/download inspection | Authorized and bounded |
| Package/installer publication | Forbidden |
| Deployment/service reconfiguration | Forbidden |
| Schema/data/hosted/user/database/queue/object mutation | Forbidden |
| Issue/PR/settings/permissions/secrets/credentials | Forbidden except exact non-secret confirmation binding |
| Interactive login | Forbidden; absence blocks only affected external action |
| Force push/rebase/reset/clean/restore/retag/published-object destruction | Forbidden |

Permission to commit does not imply push, and source push does not imply workflow, tag, publication,
deployment, migration, or other mutation. Recheck preconditions immediately before each action.

## 13. Final report and terminal record

Return exact start/final Git and preserved-work boundaries; `R`, `E`, recovery commits and pushes;
changed/unchanged semantic, package, artifact, target, release, and deployment identities; no-new-
graph-meaning semantic-leverage result; public authoring and maintained proving-consumer behavior;
every gate/classification and independent/negative/rollback/cleanup result; service-to-public digest
continuity; evidence paths/digests and resource observations; tag object/source, workflow
run/attempt/jobs, handoff IDs/digests, release/assets/attestations and latest state; deviations,
limitations, retries/recovery; every external action performed or not; remaining source/release/
deployment split; and smallest next candidates.

Next candidates should include workload-driven task-function resource transfer, separately bounded
scale/history retention, owner detail or worker recipe only from demonstrated need, and a Nostr
WebSocket/session proving slice only after its general authority and oracle close.

Append one concise terminal record below. On completion set status `complete`; use `terminated` or
`superseded` where applicable; blocked stays `incomplete`. Include terminal time, identities,
commits, REL15-1 through REL15-8, evidence digest, target/package/tag/workflow/release/assets,
classifications, external actions, preserved work, recovery, limitations, and next candidates. Do
not rewrite this body or paste raw logs, inventories, generated documents, or long measurements.

## Terminal record
