# Deliver the numerical-library milestone on main and through the public product

- Repository: https://github.com/lkjsxc/lkjscript
- Invocation/name clock: **2026-09-16T18:09:54+09:00, Asia/Tokyo**; read from the actual environment clock. An independent UTC+09:00 clock read at 18:09:45 agreed.
- Intended initial archive: **`docs/campaigns/202609161809.md`**.
- Companion: the complete replacement candidate for root **`AGENTS.md`** supplied with this mandate.
- Kind: **mainline integration and due-release continuation**, not another language-feature or timeout-repair campaign.
- Selected result: integrate the existing coherent F64/requirement/structural-authoring/transaction-completion lineage into remote main and close its already-due immutable public milestone, preserving the launch, original-evidence and full-lifecycle corrections.

Execute this mandate as the responsible Codex integrator. Normal non-forced pushes to main, ordinary configured CI/release use and the selected repository release are explicitly authorized within actual permissions, protections and higher-priority instructions. The user dislikes abandoned branches. A topic push, local commit or open PR is not completion. This authority does not cover force-pushing published history, protection bypasses, access/credential/billing changes, purchases, production deployment or destruction of operational data. Preserve unrelated work and secrets.

## 1. Starting state: implemented work is still outside main

These are observations, not reset targets. API reads were sequential, not an atomic snapshot. Inspect actual startup state before acting. Source reads use the exact frozen candidate below unless a reporting/main context is explicitly named. Final branch/job refresh recorded at **2026-09-16T18:22:00+09:00** found the same main/candidate/report tips and job 104710914072 still in exact static-candidate build/admission after successful fresh full.

| Role | Observed identity and meaning |
| --- | --- |
| Default branch M | `main` at `0b6a9c08017fa7565408acfda10d3ce3b3c40c04`; includes accepted transaction completion, not the subsequent F64 implementation. |
| Frozen candidate S | `7dce6b73769346f792c7f2c71d77d41dc8695dae`, tree `41104b16607c220e263768be3cf29bfe00bbab8d`, on `codex/full-lifecycle-202609161630`. |
| Mutable reporting descendant R | `fe0d92a728620d8a701ac0600c76c04a8ad1dd0d`, tree `db6ecd828de65d9b93ba9c9855a574577f360bd0`, on `codex/full-lifecycle-report-202609161630`. Direct child of S; changes only the predecessor campaign ending and `docs/status.md`. |
| Previous cutover/report | `a5bad65c14e118e47f774ecaac0f5adce4259f7d` / `b5224018b282ea642e7c6d45c483640ff50782de`. S descends from the latter by two commits. Do not repeat the Artifact 21 consumer or deadline correction. |
| Earlier F64/report handles | `codex/f64-202609152217` at `256edf0018ce1fd9935e9f9af9cbe428a6b93514`; `codex/f64-report-202609152217` at `cccd62fca0ec88adb04ceef06d5db3b284db01bd`. Reconcile their real ancestry/unique changes during cleanup, not from names. |
| Existing rehearsal | **35070583339, attempt 1**, exact S, `workflow_dispatch`, reported `publish=false`, selected `v0.1.38`; running at observation. Job **104710914072** had passed fresh full and was building/admitting the exact static candidate. |
| Published state | Latest release **v0.1.36**, ID **388927116**, immutable, published **2026-09-15T07:00:57Z**. Publication is confirmed; original-public-evidence closure is still reported failed. |
| Intended successor | S's `Cargo.toml` is **0.1.38**. The exact v0.1.38 tag lookup returned 404 during this investigation. This is not a reservation; refresh tag/release/run occupancy. |

The live comparison M...R reported `merge_base_commit=M`, ahead 9, behind 0. The comparison between `b5224018...` and S reported that base as merge base, ahead 2, behind 0. This is actual ancestry plus change inspection, not an inference from branch labels. R's direct-parent/diff inspection establishes that its reporting changes are not a new tested product source. At the observed tips a normal fast-forward path exists; do not assume it will remain unchanged.

There were no open pull requests. The branch API marked main unprotected, the ruleset listing was empty, and repository metadata exposed push/admin permission. These reads are not a guarantee that a future credential can perform every action. The workflow directory at S contained only `release.yml`; its push trigger is `v*` tags, not main. A normal main push does not trigger publication through that inspected workflow. Other external automation was not inspected; check consequential configured effects without inventing a hypothetical deployment approval requirement.

### Freshly inspected original full evidence, not fresh product execution

The architect acquired the original authenticated artifact **10437208580**, `full-verification-evidence-35070583339-1`. The downloaded ZIP independently matched API length **50,747,833 bytes** and SHA-256 **`e4ec653e841f3a6256d1ea7a2ab6918ddf315615d7862c97bf48138f7c828e1d`**. It expires at **2026-09-23T08:31:30Z** according to metadata.

Its original member `lkjscript-dev/check/1789545472245939837-6493-0/receipt.json` records full/check contract 5, exact S, status `passed`, 26 fresh passed gates, zero reused gates, no unrun gates, and stable equal initial/final input digest `verification_883de2c7f81e8a134f92be34ec9353ee383a262ec8e8371c9fd962466526f4c9`. Elapsed time is 2,017.164161148 seconds. The hosted full step ended **2026-09-16T08:31:29Z**, and its evidence upload succeeded. These are authenticated source-bound recorded results, not a controlled performance comparison.

Several cache writes are recorded as failed while the corresponding fresh gate execution passed. Do not convert these into cached passes, a product failure, or an unrelated cache-repair campaign. The archive alone is not a claim that every external path/input referenced by the full reader is present. Complete original-reader admission through the maintained preparation/handoff context; do not invent missing manifests or rewrite paths inside receipts.

The architect performed source/API inspection, ZIP hashing and JSON/log inspection only. No Rust build, lkjscript execution, original lkjscript receipt-reader execution, main integration, branch deletion or publication was performed. A disposable Git clone could not resolve github.com; connector revision-bound reads remained available. Private stashes, ignored retained fixtures, reader-reconstruction notes and the user's checkout were not inspected. Historical/local passing claims below remain predecessor reports until authenticated/reconciled.

### Inherited obligations and disposition

The maintained predecessor is `docs/campaigns/202609161630.md`, including its execution/resumption additions in R. It reports completed allowance/diagnostic engineering, 119 contributor tests plus formatting/affected strict lint, retained old originals, and an exact-source local test harness. Its early "full unrun" label is superseded by the fresh full observation above. Its target/installed/original-reader/fault/main/public obligations are not thereby closed.

The F64 campaign `docs/campaigns/202609152217.md` and cutover continuation `docs/campaigns/202609161407.md` own already implemented numerical semantics, Artifact 21 consumer repair, once-only launch and evidence-retention work. They are not new implementation requests.

v0.1.35 is reported as the last fully closed public milestone. v0.1.36 is published but its public reader rejected eight absent authoring requests/plans; preserve the immutable release and that failure. v0.1.37's tag object `51ecaabf3a6dfbaef0f842e95c4fc480dced0b6d` selects `8baafff7f67b55481cda8d67f8945b78af80be4d`; publisher `34965341260/1` failed before producing a release. Its separate recovery was explicitly superseded by the combined successor, not erased. The subsequent a5bad65c rehearsal `35059467604/1` passed source/target/package work but failed its installed aggregate at the offline child deadline; that failure remains valid for that older source.

**No new feature deferral is available.** Complete the combined milestone rather than selecting another attractive feature. If intervening work has already delivered this exact outcome with required proof and remote-main integration, report satisfaction with identities and complete only actual remaining reconciliation/cleanup. Do not silently choose another campaign.

## 2. Decision and finite result

**Thesis:** the present limiting cause is unfinished delivery of an implemented, substantially verified language increment, amplified by coupling every release gate to main integration and treating frozen candidates as a reason for new reporting branches. Keep exact frozen-source proof and immutable publication, but make the mutable development line reach main as soon as its own dependency-complete integration conditions are satisfied.

Before: the default branch lacks the existing numerical-library implementation; a corrected candidate and successive reports remain on separate branches; anonymous users still obtain a release without the combined milestone and with unclosed evidence retention.

After: remote main contains the coherent implemented result and current guidance; the selected immutable release supports the ordinary numerical/library/transaction workflow through installed bytes; matching original public evidence is admitted; campaign-owned temporary refs are deleted when safe or retained for explicit live dependencies. Main and public readiness are reported separately, never conflated.

The strongest materially different alternative is to profile and reduce repeated public authoring/checking cost, or improve numerical data representation. The existing workload is substantial enough to motivate that work, but no controlled bottleneck experiment here justifies invalidating the healthy exact-source rehearsal and postponing an already consumed delivery trigger. Raising the deadline was an allowance correction, not a speedup; this campaign adds no further blanket timeout increase or verification framework.

Evidence that overturns the chosen route is a concrete current product/compatibility defect, an actually required integration gate, a divergent lineage with materially different accepted work, or a legitimately completed replacement release. Diagnose that evidence and repair/reconcile the responsible owner. A slow healthy job, missing local convenience fixture, stale pending report, or preference for another feature does not overturn the outcome.

After delivery, measure a fresh ordinary numerical/data-processing authoring workload, separating authoring/preparation from steady-state execution and persistent growth. Repeated validation cost or data layout is a candidate next dependency; composable transaction ownership is a different candidate when a real caller needs it. Reconsider using measurements and useful programs, not this provisional outlook. No successor queue is authorized here.

## 3. Preserved product contract and required public witness

No new scalar, graph format, runtime engine, daemon, application or language primitive is selected. Do not reimplement F64 or simplify the already authored library to manufacture a pass.

Retain the current numerical contract at its actual owners: `src/platform/binary64.rs`, kernel/compiler/normalized execution and typed codecs, `docs/spec/language.md`, `docs/spec/semantic-storage.md`, and `docs/spec/data-capabilities.md`. Preserve generic and nominal composition, explicit conversions, unchanged integer behavior, NaN representation versus numerical equality, signed zero, full-domain typed binary values and finite external JSON. Preserve current supported authentic predecessor admission and explicit unsupported-input rejection; do not regenerate historical fixtures as current bytes.

Keep the candidate's Graph 17/compiler 13/bytecode 9/Artifact 21 cutover and affected built-in/standard/lkjournal assets coherent. No additional compatibility generation or application-data migration is warranted by integration or reporting. Release notes must distinguish existing cutover behavior from this delivery-only continuation and explain viable predecessor executable/data recovery. Installation is not migration.

The discriminating designed witness is the existing literal statistics library, not adoption or a new application maintained in this repository:

- A separately authored library owns accumulation/merge behavior, while a separately authored consumer owns a nominal sample type and supplies a bound projection into that generic imported library. The producer disappears before execution. This must not collapse into a host-side computation or a compiler-specific statistics intrinsic.
- The real public path uses product discovery, reviewed `change plan`/`change apply`, check/build, offline export/import, and execution through the copied or installed candidate outside the language checkout. Exact literal request/plan inputs and results are retained. Publicly authored calibration edits change the expected result without a compiler change; opaque typed checkpoints survive a fresh process.
- Keep the numerical owner's independent expectations for ordinary, large-offset, empty/singleton and chunked cases, and its complete currently required long-input cases. Preserve negative input/type/codec and nonfinite-output cases. The granted data witness still proves a committed value remains visible after a later nonfinite JSON output failure.

The executable owner is `tools/lkjscript-dev/src/offline_packages/f64.rs`; literal sources are its sibling `f64.producer.lkjc`, `f64.consumer.lkjc`, `f64.data.lkjc` and `f64.edit.lkjc`. The architect verified the author's public request/plan/apply path and original input/plan retention in that source. Observer code may transport opaque values and inspect storage; it must not replace graph-owned accumulation or application policy. Reuse these maintained cases; do not add another privileged demo generator.

Preserve accepted `transaction-outcome`: `Committed(T)` appears only after actual transaction completion; an aborted completion cannot expose the uncommitted candidate. Independent callback effects can survive failure. No automatic retry or general rollback promise is introduced.

Preserve the already implemented complete-process allowance and diagnostics, owned by `offline_packages.rs`, `check/registry.rs`, `release/mod.rs`, `release/transferred.rs` and `release/transferred/pair.rs`. One explicit 3,600-second offline allowance and the 120-minute application jobs are already selected; do not implement them again. Terminal process success, strict original-reader admission and cleanup are conjunctive. A success-looking intermediate receipt followed by timeout is failure; zero exit with rejected evidence is failure. Maintain once-only execution after successful spawn, per-command bounds, input guards and joined owned process trees.

## 4. Startup and one mutable continuation

Read existing applicable guidance before installing the supplied candidate. Inspect actual HEAD/branch, tracked/untracked/index changes, stashes/worktrees, remotes/divergence, relevant predecessor endings, pinned toolchain/resources and active jobs. Refresh remote state without resetting, cleaning or consuming unrelated work. Preserve the unrelated stash and recovery worktree reported by the predecessor if they still exist.

At the observed state, reuse the existing R lineage for mutable guidance/reconciliation, then integrate it normally into main; it contains S without moving the frozen candidate. Do not create another timestamped implementation/report branch as a ritual. Once main owns the result, continue ordinary reports there. If main is already suitable and contains the lineage, work there directly. If checkout ownership makes isolation necessary, record the specific reason and main integration endpoint.

Archive this initial text unchanged under its intended path, preserve both originals on a real collision using observed seconds or a documented suffix, and append later records only below the initial-text boundary. Reconcile/install `AGENTS.md` in the mutable checkout before substantive work. This changes neither instructions already governing the session nor S's contents.

The first remote-job action is:

```sh
gh run view 35070583339 --repo lkjsxc/lkjscript --json headSha,status,conclusion,jobs
```

Confirm attempt, exact source and selected workflow/tag/publication inputs through run/event metadata. Reconcile matching later jobs before any dispatch. The existing source and workflow permit main to advance while this event SHA remains fixed; an observed main advance alone is not a reason to move S or rerun it.

Acquire authenticated originals by actual artifact ID/digest when available and needed. Retain unmodified ZIPs and metadata in ignored owned storage before operating on reconstructed copies. Do not rely on this chat's local ZIP or the predecessor's private paths being available. Reconstruct from the authenticated handoffs and current source when ignored notes are missing. Preserve original path, mode, environment and reader bindings in owned contexts; do not patch receipt JSON to fit a new location. Do not execute a downloaded verifier until its expected identity is admitted.

## 5. Separate main readiness from publication readiness

### Explicit revision of inherited ordering

The predecessor grouped all installed-pair/original/fault obligations before main integration and prescribed a new frozen branch plus separate reporting. This mandate supersedes that scheduling prescription, **not its substantive proof obligations**. At `docs/release.md` and the relevant `docs/spec/verification.md` guidance, add or reconcile a concise distinction between integration readiness and public closure. Link the current campaign rather than copying its matrix or history into permanent policy. Do not edit the original archived mandate.

For this unchanged candidate, main readiness requires exact-source fresh full, complete exact static-target admission, maintained consumer/generated-asset consistency and the affected verifier correctness checks, with authenticated matching source/target/preparation evidence. Public closure additionally requires the installed/transferred original-reader/fault obligations and the publication/anonymous path below. A known product defect anywhere still blocks acceptance; relabeling a failing test "release-only" is forbidden.

The fresh full result above includes workspace tests, production lints, build, copied public lifecycles, behavioral owners, generated guides, maintained package checks/asset comparisons and policy/diff gates. The predecessor's bounded completion/diagnostic tests are present in S; source inspection confirms they exercise real supervision with provisional output, timeout, reader rejection and cleanup failure. Reconcile these with the current full logs rather than rerunning the same local subset merely for reassurance.

### Integration gate

Use the **existing run** to finish exact musl build/admission and preparation. Require all named target behavioral owners and both pinned userlands, not just ELF inspection. Admit the genuine target and package/verifier handoffs with matching original readers as available; preparation's exact full/target bindings matter. A passing substep is not whole-run/public acceptance, but a complete validated integration prerequisite need not wait for unrelated later release-only work.

Once these integration conditions are established and there is no relevant known failure or actual protection gate outstanding, integrate the accepted lineage into remote main **without waiting solely for the second installed job, publisher, or anonymous acquisition**. Keep their obligations open and continue them. This ordering change preserves fresh source and target proof while eliminating an unnecessary coupling. Do not require a new release rehearsal for guidance/status-only descendants.

Review the exact S-to-integration diff, including R's report and this campaign's guidance/policy updates. Validate its actual scope and links/commands separately; do not claim full was run at the new commit, stamp an old receipt with a new hash, or rely on a filename alone as a proof exemption. If a source, verifier, workflow, generated asset or other relevant execution input changes, freeze the real corrective source and refresh affected proof under the maintained contract. A reporting/guidance descendant can describe S's accepted result without becoming S.

### Normal main delivery

Refresh remote main, protections, permissions and active collaborators before integration. Prefer a fast-forward of the existing lineage where still valid. Incorporate intervening work normally, preserving unrelated history and revalidating consequential interactions. If a PR becomes actually required, complete its permitted merge path. Never force-push or change protections.

Confirm the remote ref, not merely the local branch: fetch/read remote main and verify it contains the accepted result and the exact frozen release source S. Record the actual main SHA, its ancestry to S and any separate proof for integration changes. Preserve S's identity when possible. If a permitted squash/cherry-pick destroys required release ancestry, equivalent patches alone do not satisfy the release owner's source admission; reconcile reachability or choose a correctly proved release source instead of faking the relation.

## 6. Finish the still-real original-evidence and fault obligations

Do not repeat the implementation plan. Complete only the following remaining acceptance on genuine final-source evidence, reusing any already qualifying execution discovered at startup.

| Boundary | Required observation and rejection behavior |
| --- | --- |
| Whole installed invocation | Existing rehearsal's two independent installed route lifecycles plus one fresh complete five-owner suite bound within that pair; actual child exit, owner reread, summary and cleanup all succeed. An inner receipt is insufficient. |
| Target receipt sensitivity | `release::admission::nominal_tests::live_nominal_receipt_omissions_reject_at_target_admission` uses restored exact final-source target originals through `LKJSCRIPT_NOMINAL_ADMISSION_FIXTURE`. Original baseline passes; required omissions/forgeries reject despite enclosing rehashes; originals restore. |
| Pair interruption and recovery | `live_pair_interruptions_preserve_failures_before_fresh_recovery` uses `LKJSCRIPT_PAIR_REHEARSAL_ROOT` with real prepared inputs. Preserve separate failed attempts and unrelated sentinel; all owned processes/resources are cleaned. Follow it with one genuine fresh healthy simulated `pair-run` recovery. |
| Pair/transferred reader sensitivity | `live_pair_receipt_fault_matrix` uses `LKJSCRIPT_PAIR_FIXTURE_ROOT`; transferred `live_receipt_fault_matrix` uses `LKJSCRIPT_TRANSFERRED_FIXTURE_ROOT`. Reuse that same genuine healthy recovery and full-suite child instead of executing applications again for each matrix. |
| Authoring and useful behavior | Original numerical and transaction requests, review plans, inspected definitions, dependency/producer-removal bindings, edited results, restart/checkpoint and commit-after-output-failure observations remain independently required by their maintained readers. Missing originals reject; a new smaller smoke is not a substitute. |

Discover the exact current fully qualified test names and environment contracts in `tools/lkjscript-dev/src/release/admission/nominal_tests.rs`, `tools/lkjscript-dev/src/release/transferred/pair/tests.rs`, and `tools/lkjscript-dev/src/release/transferred/tests.rs` before invoking. The target omission and pair interruption contracts were source-inspected here; the remaining exact command/layout details must be read from their owners rather than invented.

The predecessor reports a prepared copied test harness for S in ignored storage, with source/tree/hash and test inventory. Reuse it only after authenticating that binding. Otherwise prepare the exact S harness with the verified command:

```sh
cargo test --locked -p lkjscript-dev --lib --no-run --message-format=json
```

Select/copy the actual executable reported by Cargo, preserve its identity, list its tests and invoke the exact ignored names sequentially with their documented fixture environments. Do not guess a hashed executable path, conflate a debug test harness with the product/release verifier, or run all ignored tests blindly. Respect the ordinary-user permission-fault environment, pinned Docker/userland prerequisites and immutable copies while other Cargo work occurs.

The real local pair fixture uses `verifier/`, independent `exact-assets/` and `latest-assets/`, genuine preparation `release-receipt.json` and `pair/`, as read by `options_from_fixture` in the current pair tests. Restore hosted readers in their appropriate context; a relocated fabricated "local" receipt is not a recovery execution. Fault edits occur only on owned reconstructed copies with restoration, never on the sole retained original or published assets. Verify restoration and unrelated-state preservation after success and failure.

These are inherited discrimination requirements, not a new permanent case-count quota. Fresh healthy recovery after local faults has a distinct purpose from rereading hosted acceptance; label both honestly. If no compatible local fixture or execution authority is available, finish independent main integration where justified and report the exact remaining **public** gate; do not fake it, restart feature work or dispatch duplicate hosted jobs to avoid diagnosing the missing condition.

## 7. Publish the already selected milestone, once

Default release source remains S; default version remains v0.1.38 subject to actual occupancy. Complete the existing read-only rehearsal and matching original admission, including the fault obligations above, before publication. Reconcile the previous v0.1.37 control owner and competing publishers. Do not replace immutable v0.1.36 assets or move the v0.1.37 tag to repaired content.

Use `docs/release.md`, `tools/lkjscript-dev/src/release/` and the inspected workflow. Before tag push, refresh enabled immutable-release state, the existing non-secret `LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA` selector, exact annotation/source/version agreement and reachability from remote main. Ordinary scoped selector update/readback is part of the delegated maintained release procedure when actual permission allows; changing credentials, access rules or immutable-release settings is not. Do not claim this investigation read or changed that selector.

Create/push the exact annotated tag only after its prerequisites and automatic effects are understood. The publisher runs its own mandated fresh proof; rehearsal results are not promoted across runs in its place. Do not create a second publisher for a shared identity. If legitimate intervening work occupies v0.1.38, reconcile it first: close an already successful equivalent release, resume its actual outstanding obligations, or make an additive correction only if a genuinely defective published result requires one. Changing the package version selects new source/proof inputs; never relabel S's old evidence as new-version proof.

Public closure requires immutable publication, independent anonymous exact/latest acquisition with integrity and attestations, both installed route lifecycles, the maintained one-within-pair full suite, and successful original-public-pair reader admission including the formerly missing literal requests/plans. Simulated acquisition is not anonymous download. Green publication or asset upload alone cannot close this campaign.

Retain original full/target/release/verifier/prepublication/public artifacts and matching context promptly. The previous protocol retains transient release/verifier handoffs for one day and other relevant evidence for longer; inspect actual expiration and copy originals for the real resumption need. Do not build a new archival service or fabricate expired originals. Preserve genuine failed history and keep new acceptance source-specific.

No F64 withdrawal is authorized merely to shorten acceptance. If a newly reproduced F64-specific correctness or compatibility defect appears, correct its owner on a coherent descendant and refresh affected proof. Preserve the work and report an exact blocker if it cannot be safely resolved; do not silently publish a weaker feature set or claim the old combined contract completed. This replaces the predecessor's hypothetical pre-F64 fallback for this already-implemented, fresh-full-passing continuation.

## 8. Failure decisions, cleanup and finite return

For the existing run, use the following finite decision rules:

**Healthy and running:** do not cancel, move its source or dispatch another run. Complete independent reader preparation, scoped faults where genuine inputs exist, guidance, branch reconciliation and integration as its own gates become available. Inspect again at a meaningful completed work boundary, not in a sleep/watch loop.

**Completed successfully:** acquire/admit its genuine handoffs, close missing source-bound obligations, integrate if not already done and perform the single permitted publication route. Do not repeat source/full/target/pair locally solely because the report changed.

**Failed:** acquire the failing originals and identify the responsible phase. Distinguish terminal process failure, reader rejection, cancellation, infrastructure, output bound, cleanup and application defect. Preserve the failure. A correction to relevant source/verifier/workflow inputs requires a new accurately bound candidate/attempt; an infrastructure-only retry needs a specific changed-condition rationale. Do not rerun unchanged deterministic failures or automatically raise deadlines.

**Already superseded or publicly complete:** identify the exact replacement and verify its useful contract, proof and main ancestry. Do only real remaining reconciliation. Do not invent another implementation campaign.

Main integration and safe cleanup are owned work even while public delivery remains pending. Inventory campaign-owned branches/worktrees and test actual ancestry/unique changes. The observed transaction-outcome branch is an alias of M; older finite-callable, stable-release/report, structural-authoring/acceptance/delivery and public-evidence-recovery branches also need disposition, not automatic deletion. Preserve any unique unrelated work. For branches whose changes arrived by squash/cherry-pick, inspect patch equivalence and retained reports explicitly; equivalence alone is not exact-source verification.

Remove a temporary ref/worktree only when its work is integrated or explicitly superseded without loss, its tip has not changed, required objects/reports remain durably reachable and no active job or required reference depends on it. Recheck remote tips before deletion. Preserve the current frozen candidate handle while the live job or release path requires it, then retire it once main/tag/history and retained evidence satisfy its reference needs. Keep immutable tags. Do not leave every historical branch forever merely because it once supplied a release dispatch, and do not delete branches just to reduce a count.

Keep `docs/status.md` concise and truthful: distinguish integrated source, exact verified S, current rehearsal, selected publisher and public acceptance. Link old chronology to its campaign owner. Reconcile the roadmap's consumed trigger without pasting a growing run history. No parallel delivery ledger or new permanent governance machinery is requested.

If only external completion remains, stop with an exact pending record: observed time, source SHA/tree, run/attempt and job, actual completed versus missing gates, retained originals/expiry, main SHA, outstanding refs and the concrete next action. Reuse this outstanding lineage next time. Do not claim complete delivery, promise background monitoring, give a speculative completion time or manufacture another report branch.

The final report must state what became available and to whom; actual changed owners/revisions/actions; fresh/valid-reused/failed/cancelled/skipped/unavailable/unrun proof; compatibility/recovery implications; verified remote-main integration; tag/release/public-reader state; branch/worktree cleanup or retention reasons; exact blockers and resumption. The most consequential finding for the next architect should distinguish a real product limit from accumulated delivery procedure. No arbitrary additional feature, benchmark or document quota belongs beyond this finish line.

## 9. Guidance changes and evidence navigation

The supplied root file preserves the graph/authority distinctions, typed publication/effect/ownership guarantees, safe startup, pinned safe-Rust/no-Python contributor policy, existing command entry points and honest evidence boundaries. It makes ordinary remote-main delivery, one mutable continuation, explicit integration/public readiness, verified remote containment and safe cleanup standing rules. It adds no campaign identity, fixed deadline, case count or report-branch ritual. Reconcile legitimate intervening instructions rather than overwriting them mechanically.

Material inspected sources and durable resumption references:

- [M-to-R ancestry and changes](https://github.com/lkjsxc/lkjscript/compare/0b6a9c08017fa7565408acfda10d3ce3b3c40c04...fe0d92a728620d8a701ac0600c76c04a8ad1dd0d).
- [S-to-R reporting-only change](https://github.com/lkjsxc/lkjscript/commit/fe0d92a728620d8a701ac0600c76c04a8ad1dd0d).
- [Predecessor with execution/resumption](https://github.com/lkjsxc/lkjscript/blob/fe0d92a728620d8a701ac0600c76c04a8ad1dd0d/docs/campaigns/202609161630.md).
- [Current direction at R](https://github.com/lkjsxc/lkjscript/blob/fe0d92a728620d8a701ac0600c76c04a8ad1dd0d/docs/roadmap.md).
- [Release protocol at S](https://github.com/lkjsxc/lkjscript/blob/7dce6b73769346f792c7f2c71d77d41dc8695dae/docs/release.md) and [workflow](https://github.com/lkjsxc/lkjscript/blob/7dce6b73769346f792c7f2c71d77d41dc8695dae/.github/workflows/release.yml).
- [Verification contract at S](https://github.com/lkjsxc/lkjscript/blob/7dce6b73769346f792c7f2c71d77d41dc8695dae/docs/spec/verification.md) and [full/check owner](https://github.com/lkjsxc/lkjscript/blob/7dce6b73769346f792c7f2c71d77d41dc8695dae/tools/lkjscript-dev/src/check/mod.rs).
- [Numerical public authoring and reader owner](https://github.com/lkjsxc/lkjscript/blob/7dce6b73769346f792c7f2c71d77d41dc8695dae/tools/lkjscript-dev/src/offline_packages/f64.rs).
- [Target original/fault owner](https://github.com/lkjsxc/lkjscript/blob/7dce6b73769346f792c7f2c71d77d41dc8695dae/tools/lkjscript-dev/src/release/admission/nominal_tests.rs) and [pair tests/fixture owner](https://github.com/lkjsxc/lkjscript/blob/7dce6b73769346f792c7f2c71d77d41dc8695dae/tools/lkjscript-dev/src/release/transferred/pair/tests.rs).
- [Existing run 35070583339](https://github.com/lkjsxc/lkjscript/actions/runs/35070583339), [attempt-1 jobs](https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/35070583339/attempts/1/jobs), and [original artifact inventory](https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/35070583339/artifacts).
- [Authenticated original full artifact metadata](https://api.github.com/repos/lkjsxc/lkjscript/actions/artifacts/10437208580). The member path and independent hash observation above locate the evidence without relying on this conversation's files.
- [Immutable v0.1.36](https://github.com/lkjsxc/lkjscript/releases/tag/v0.1.36). Its published body is not proof that its later original-reader obligation passed.

---
End of initial mandate. Preserve the text above unchanged when archiving; append concise reconciliation, execution and resumption records below.

## Startup reconciliation and integration admission — 2026-09-16 09:49 UTC

Actual environment clock first read 09:43 UTC (18:43 Asia/Tokyo); the archive name preserves the
mandate's invocation identity. Startup was reporting R `fe0d92a728620d8a701ac0600c76c04a8ad1dd0d`,
with only the supplied replacement `AGENTS.md` modified and this exact mandate untracked. The
initial archive SHA-256 before this append is
`ecdcb1d8bb470c0be16cbf50128f6e241010e5f44a4f43cd1305757195b39b72`.
Existing root guidance was read and reconciled with the supplied candidate; no intervening
instructions or execution inputs were overwritten. The installed candidate's SHA-256 is
`9528c687583ed6e3edd665408e2338be1a59204d99a066671f51509e861edd23`.
No new mutable branch or worktree was created. Rust 1.98.0 is pinned; approximately 661 GiB disk
and 7.8 GiB available memory were observed. No conflicting local Cargo/application job existed.
The unrelated stash `87c2265f` and clean recovery worktree at `8baafff7` are preserved.

The required first remote-job action found exact rehearsal `35070583339/1`, workflow_dispatch,
`publish=false`, `tag=v0.1.38`, source S `7dce6b73769346f792c7f2c71d77d41dc8695dae`.
Build job `104710914072` completed successfully at 09:27 UTC; installed application job
`104739797791` was running. No competing publisher or occupied v0.1.38 tag was found. Fresh
remote main remains `0b6a9c08`, an actual ancestor of R; main is unprotected, rulesets and open PRs
are empty, and actual repository permissions expose push/admin. The current workflow has only a
`v*` tag push trigger plus manual dispatch; a main push does not invoke this release workflow.
Two older workflow registrations have no workflow file in current main or this lineage.

Authenticated original run/job metadata, unmodified ZIPs and extracted originals are retained under
ignored `.artifacts/numerical-delivery-202609161809/originals/`. All lengths and SHA-256 values
matched their actual API metadata:

| Original | Artifact ID | ZIP bytes | SHA-256 | Hosted expiry UTC |
| --- | --- | ---: | --- | --- |
| full | 10437208580 | 50,747,833 | `e4ec653e841f3a6256d1ea7a2ab6918ddf315615d7862c97bf48138f7c828e1d` | 2026-09-23 08:31:30 |
| target | 10439183963 | 51,094,352 | `bc25cc6258a92081c233d3cc747f26eb45f739c1d11392e7700aac80bd45435c` | 2026-09-23 09:24:33 |
| release | 10439806267 | 11,080,576 | `9697bd8ad43834b254cf243fb39bae9484d585900330784718a95632aeb954b9` | 2026-09-17 09:27:10 |
| verifier | 10439776484 | 32,902,540 | `1d8b1a893eac810e160d76a9c35b7f7e6016093c731ee671bc1445b53b39059b` | 2026-09-17 09:27:11 |

Original full contract 5 binds exact S, 26 fresh passes, zero reuse, no unrun gates and equal initial/
final digest `verification_883de2c7f81e8a134f92be34ec9353ee383a262ec8e8371c9fd962466526f4c9`.
Its workspace log contains the 119 passing contributor tests, including actual provisional-child
failure and supervised cancellation/timeout recovery. Maintained assets, consumers, guides,
production lints and all other full owners passed. Fourteen cache writes failed after their fresh
gates passed; these are neither reused passes nor product failures and select no cache repair.

The authenticated build log independently binds the copied verifier to 32,901,768 bytes /
`5cf309a209ef4409f21a79c1049b17f140e2d6418f2a6213512774370388239e`. Actual bytes matched before
execution. Its original verifier and strict package readers pass. The admitted archive is
`24393a6ef8aa425f3bcc01761adf2f8295fa3d47c59857eaea67c0f8ec182351`; candidate executable is
24,295,968 bytes / `98a39bd192c1e98187a1a954f916f5ffc89ef2586317c25e1efa06c61b888c32`.

Original target admission passes the matching reader after restoring exact recorded paths/modes,
packaged candidate, frozen verifier and S's maintained lkjournal asset in an owned mount namespace.
All 3,456 restored original files compare byte-for-byte; no receipt JSON was rewritten. This is
archived-context admission, not live-effect replay. It admits all six named owners and both pinned
userlands. Genuine successful preparation binds the exact full receipt (61,287 bytes,
`b8a3ebbe8ff3eae60de71793e1c3cf14de8129be6d528716afd21688f0445e37`) and target receipt (73,181 bytes,
`cdbaf239674a26b780320e1c5573e8cdb686b72e888349b02a1c466291aab197`). The private full reader ran in
that original preparation context; strict package rereading is not misreported as a new full read.

These satisfy the mandate's integration prerequisites. The only mutable changes are the supplied
guidance, this archive, concise status/roadmap reconciliation and the explicit integration/public
ordering distinction at `docs/release.md` and `docs/spec/verification.md`. No product, verifier,
workflow, compatibility generation, generated asset or numerical workload changed. The installed
pair, required source-bound fault/recovery tests, publisher and anonymous public reader remain
separate open obligations. Target fault admission and the genuine local interruption/recovery
sequence are proceeding with independently owned reconstructed copies and the authenticated S
harness; their results are not yet claimed.

### First fault attempt and narrow correction boundary — 09:55 UTC

The exact S target matrix ran from 09:48:17Z to 09:51:50Z and failed at the intended-owner assertion
for `f64/rehashed-independent-scale-result`. Its baseline passed. Diagnosis against all 2,018
original offline files found exactly three mode mismatches: `f64-producer.lkjplan`,
`f64-consumer.lkjplan` and `f64-edit.lkjplan` changed from 0600 to 0644 after omission/restoration;
all bytes remain equal to authenticated originals. `nominal_tests::OriginalFiles` remembers only
bytes and recreates removed files with the process umask. The production inventory reader correctly
rejects the changed modes before reaching the intended F64 discriminant. This is a required fault
harness defect, not numerical misexecution or defective initial reconstruction. Later omission
attempts may also have rejected for that incidental mode difference, so the entire matrix requires
fresh execution after a narrow restoration fix; no partial matrix is accepted.

The failed test/logs are retained under `target-reader/`; all owned processes/namespaces exited.
The pair interruption suite continues once-only, then holds before healthy recovery while the
corrective source is selected. The required pair/transferred reader matrices use in-place writes
or rename/restore and do not share this deletion/recreation defect. Main's unchanged-product full,
complete target/preparation and affected production-verifier checks still pass. The explicitly
additional public fault gate remains failed, not waived or mislabeled as passed. Integrating the
admitted S lineage does not authorize publication with this unresolved acceptance defect.

Scoped documentation validation passed two fresh maintained `check changed` gates (`diff_check`,
`rust_only_tooling`), with zero reuse; Markdown targets resolve and the initial mandate remains an
unchanged byte prefix. The hosted verifier cannot run local checkout checks because its compiled
repository root is the hosted path; a local pinned Rust 1.98.0 contributor build supplied this
check. These local invocation corrections did not change accepted evidence or replay applications.
