# Review-bound logical change plan campaign

This is the operational index for [`prompts/202608261448.md`](../../prompts/202608261448.md).
Executable contracts, tests, retained evidence, and the active checkout remain the source of
current facts.

## Baseline and reconciliation

| Item | Evidence |
|---|---|
| Starting branch and commit | `main` at `66368e0d4b83aa76cc13fde21cd3a644503daa4a` |
| Checkout relation | Exactly the audited baseline and `origin/main`; no later commit required reconciliation |
| Prior campaign | `202608260032` was complete in `8ea897f`, `ef08d12`, and `66368e0`; no conflicting campaign was active |
| Remote view | `origin` was `lkjsxc/lkjscript`; GitHub reported no open issue or pull request |
| Preserved starting work | User-owned tracked `AGENTS.md` changes and untracked `prompts/202608261448.md` were not overwritten |
| Baseline log | `.artifacts/campaign-202608261448/baseline.txt` |

## Completed cutover

Implementation commits `ae08f5e96b0028e4ca5fc9c7e983a38094094996` and
`b8a1cf3bc8e8a21c8b188f4d6613ec1e4bfb81e4` make the following one public
dependency-closed change. The second commit replaces conservative rounded output allowances with
executable-checked exact canonical maxima:

- `plan_` now carries a 32-byte request commitment followed by a 32-byte prepared logical-plan
  commitment. Its canonical payload is exactly 128 lowercase hexadecimal characters; the old
  request-only payload is rejected.
- Apply compares the request component before project discovery, reprepares the exact base, then
  compares the prepared component before `GraphRepository::publish`.
- Authored preparation retains one typed logical projection from its existing canonical delta,
  relation delta, impact, validation, and test facts without adding another engine or coupling
  generic publication to authored-control labels.
- `change plan` accepts optional `--output PATH` for compact-record and direct adapters. One
  streaming encoder owns both the prepared commitment and canonical
  `lkjscript-logical-change-plan-1` file. The registry owns its exhaustive record vocabulary.
- The file writer rejects project-contained, symlink, and non-regular targets; uses a private
  sibling `create_new` stage, file and parent synchronization, and atomic rename; and reports
  byte-identical output as `unchanged`. Plan files remain non-authoritative and are never apply
  input.
- The strict decoder checks canonical framing, closed records/fields, typed domains, ordering,
  duplicates, optionality, counts, commitment/trailer agreement, and trailing input.

The old `ChangePlanDigest`, `lkjscript.change-plan.v4` domain, 64-hex parser/display, generic
mismatch diagnostic, tests, generated contract entries, and current handwritten claims were
removed. There is no legacy parser, alias, fallback, dual token, persisted plan cache, or plan-file
writer path into accepted authority.

## Evidence index

| Evidence | Path or result |
|---|---|
| Focused unit filter | `cargo test --locked --lib reviewed_change_plan`: 5 passed |
| Focused public filter | `cargo test --locked --test public_cli reviewed_change_plan`: 2 passed, release-scale test explicitly ignored here |
| Complete library suite | 391 passed; `.artifacts/campaign-202608261448/lib-tests-precommit.log` |
| Complete public suite | 16 passed, one explicit release-scale test ignored; `.artifacts/campaign-202608261448/public-cli-precommit.log` |
| Initial focused profile | 6/6 fresh before the exact-limit correction at `.artifacts/lkjscript-dev/check/1787731775107757536-1754377-0/receipt.json`; final-content profiles are returned in the handoff |
| Release scale | 10,655 plan records / 1,491,596 bytes, 513 stdout records / 35,778 bytes, zero repository bytes; `.artifacts/campaign-202608261448/plan-scale-release.log` |
| Machine evidence | `docs/evidence/20260826-review-bound-logical-change-plan.json`; includes commands, topology, timings, counts, digests, and limitations |
| Copied release binary | External plan/export/apply/inspect is covered by `copied_binary_direct_rename_completes_a_normalized_external_workflow`; retained small timing output is under `.artifacts/campaign-202608261448/small-workflow/` |
| Exact closure oracle | `reviewed_change_plan_owned_body_closure_matches_complete_before_after_oracle` reconstructs before/after canonical meaning and relations independently |
| Generated documentation | Registry digest `5c5c4d4a0eda603cedfbc7c190593427ba70c5723fe38251ebfc6e56933b891d`; executable verification current |
| Warnings-denied Clippy | `cargo clippy --workspace --all-targets --locked -- -D warnings`: passed before the implementation commit |
| Final profile | One fresh final `lkjscript-dev check full` receipt is retained under `.artifacts/lkjscript-dev/check/` and returned in the final handoff |

## Independent and adversarial proof

The body-replacement fixture builds a nontrivial binding/expression subtree, reconstructs complete
before/after authority, extracts all canonical relations without the plan encoder, and compares the
exact deleted owners, retirements, removed/added relations, validation sets, tests, and reasons.
Decoder tests mutate, truncate, duplicate, reorder, append, inject an operational record, corrupt
the trailer, use malformed escaping, and use foreign typed domains. Public tests prove direct/
record and label-renaming equality, changed-existing/unchanged output, symlink/directory/missing-
parent/project-contained failures, old-token rejection, early request mismatch, late prepared
mismatch, stale publication, and unchanged HEAD/inventory.

## Deviations and limitations

- The selected `plan_` plus 128-hex spelling fit current typed-token conventions, so no token-shape
  deviation was required.
- Moving the selected relation/impact sets into authored evidence avoided a second complete fact
  copy. Dependency and retirement before-values were retained during lowering because the generic
  semantic diff intentionally carries only their object commitments.
- The 740,018-record/303,377,551-byte file ceilings are independently derived from default change
  admissions and maximum record encodings. Internal callers may select larger admissions, but
  output then fails rather than changing semantic validity or truncating facts.
- The release-scale fixture is explicit/ignored in ordinary debug suites because its 9,125-type
  candidate is evidence work; the required release command runs it directly.
- Current test seams do not provide deterministic process-kill checkpoints for the external output
  rename or a forced random stage-name collision. `create_new`, owned-stage cleanup, sync/rename,
  existing-output preservation, and repository-inventory tests cover the independent boundaries;
  exact kill timing remains unmeasured.
- No accepted-repository fixture currently constructs two different page/pack layouts for an
  end-to-end token comparison. Lower-level logical-state, persistent-map, request-label, plan
  exclusion, and Rust/serde-layout independence tests pass; complete physical-layout comparison is
  unavailable rather than claimed.
- CPU time and filesystem syscall counts were unavailable; filesystem cache state was uncontrolled.
  Peak RSS is process-wide. No provider-token or monetary result is inferred.

## Next dependency-closed candidates

1. public owned-closure deletion consuming the exact plan without implicit cascade or ambiguous
   reference repair;
2. a fresh decomposition of normalized command bootstrap and `check`/`build`/`run`, including
   standard/`lkjournal`, artifact, and execution-tier closure;
3. bounded normalized context traversal with stateless continuation; and
4. the next maintained-workflow authored operation.
