# Immutable Current Product Publication

- **Status:** incomplete
- **Campaign:** `202608292254`
- **Audit time:** `2026-08-29 22:54:03` Asia/Tokyo
- **Repository:** `lkjsxc/lkjscript`
- **Audited source:** remote GitHub default branch; no active local checkout was available to the mandate author
- **Audited branch:** `main`
- **Orientation baseline:** `9ff360b991bfbf2954e234b9414a5b94545a0797`
- **Baseline tree:** `27e260558194b362fe86b3f79e329ed4c957b890`
- **Archive path:** `docs/campaigns/202608292254.md`
- **Distinct current-work location:** none; this file owns only the minimal lifecycle field and append-only terminal record while active
- **Latest completed campaign:** `docs/campaigns/202608291908.md`
- **Intended first release identity:** `v0.1.10`, only while the exact annotated tag and release remain unused
- **Current public release at audit:** immutable latest `v0.1.9`
- **External observation boundary:** GitHub repository, tag, release, issue, pull-request, and Actions state available through the repository connector at the audit time
- **External mutation policy:** normal fast-forward pushes, bounded exact hosted dry runs, one intended annotated-tag publication, the canonical immutable release workflow, bounded verification, and narrowly defined additive recovery are authorized below; every other external mutation is forbidden

The orientation SHA is not authority over later state. The active checkout, complete campaign
records, repository-owned contracts, and refreshed external observations outrank it.

## 1. Implementation mandate and stopping rule

This is an implementation-and-proof mandate for Codex CLI, not another design pass, a release
checklist without execution, a documentation-only claim, or a feature campaign.

**Required final state:** close the completed current source capability as an immutable latest
self-contained Linux x86-64 product release—normally `v0.1.10`—whose exact and latest anonymous
downloads pass the repository-owned integrity, static-linkage, copied-binary, stateful-application,
and maintained-service boundaries, with additive patch recovery only if an already-pushed identity
makes content-changing recovery necessary.

Proceed through reconciliation, release-candidate assembly, fresh source and target proof,
deterministic package verification, hosted dry run, annotated-tag publication, anonymous public
acceptance, current-truth evidence, coherent commits, and safe normal pushes.

Set this campaign to **complete** only when all success conditions in section 5 hold and no
externally committed release state remains unclosed. If a required gate is unavailable or fails
before any tag is pushed and the objective cannot be closed through a bounded correction, leave
public truth unchanged, set the campaign to **terminated**, and report the exact blocker. If a tag or
release becomes externally committed and neither exact-input recovery nor the single authorized
additive patch can close it, preserve every immutable object, set the campaign to **terminated**,
classify the state as externally committed but not closed, and provide the exact additive recovery
boundary.

Prior receipts, host builds, target names, private archives, workflow artifacts, drafts, or
publication without anonymous acceptance are not completion.

## 2. Reconciliation gate

Before editing or running mutation-capable commands:

1. Read applicable agent instructions, this complete campaign, `docs/campaigns/202608291908.md`,
   `docs/release.md`, current status/architecture, `docs/spec/product-surface.md`, and
   `docs/decisions/20260829-release-contract-version-authority.md`.
2. Capture the actual branch, `HEAD`, working tree, remotes, upstream relation, recent commits,
   tags, releases, open work, and queued or running workflows. Fetch without rewriting history.
3. Reconcile every plausibly active campaign from its top-level status and terminal record. Do not
   select by filename recency. Preserve unrelated work and any user-supplied replacement
   `AGENTS.md` or campaign file.
4. Inspect commits after the orientation baseline. Use the actual current checkout when later work
   remains coherent with this objective; do not silently release an unknown or conflicting change.
5. Refresh the root product version, intended tag and release availability, immutable-release
   setting, release workflow, exact repository-variable access, and current latest release
   immediately before the hosted dry run and again immediately before tag push.
6. Identify the exact final release-source commit `R`, the exact static candidate derived from `R`,
   the first-party verifier bytes, target-policy identity, pinned external inputs, and every receipt
   that must bind them.
7. Confirm that the supplied mandate and root instructions are repository work, not proof that the
   campaign has already run.

Load-bearing read map after reconciliation: `Cargo.toml`, `rust-toolchain.toml`,
`.github/workflows/release.yml`, `tools/lkjscript-dev/src/release/`, the target-admission,
distributed/stateful HTTP and service owners, the documents named above, `README.md`, and current
release evidence. Remote names remain orientation until verified locally.

Stop safely before mutation when any of these holds:

- another coherent active or externally committed campaign must be closed first;
- `v0.1.10` already exists remotely, a release with that identity exists, or current source has
  deliberately advanced to another release identity;
- later commits invalidate the selected objective rather than merely changing local symbols;
- immutable releases are disabled and cannot be confirmed through the established administrative
  boundary;
- required publication authority is unavailable; or
- the current workflow no longer preserves the read-only build/verification and isolated
  publication boundaries.

Do not ask the user to choose routine commit layout, filters, paths, release-note wording, or
implementation details. Report only genuinely objective-changing evidence.

## 3. Load-bearing current state

At the audit boundary:

- remote `main` selects `9ff360b991bfbf2954e234b9414a5b94545a0797`, the evidence-only closure
  of the product-version-surface campaign;
- the latest campaign is complete, and no independent incomplete mandate, open issue, open pull
  request, queued Action, or in-progress Action was observed;
- the root package owns product version `0.1.10`, and no `v0.1.10` tag was found;
- immutable `v0.1.9` remains latest and supported;
- current source has two completed but unreleased public changes: explicit named higher-order
  generic authoring with the graph-owned standard fold, and the single product-version projection
  that removes subsystem generation numbers from current public surfaces;
- predecessor implementation `54af61a9448b0f4ee5fe2e31ffeff4fc040e6b08` passed fresh full,
  static target, surface-audit, dry-run, and archive gates; this is readiness orientation, not
  release proof for final `R`;
- the current release path already owns one static
  `x86_64-unknown-linux-musl` target, deterministic packaging, a no-checkout pre-publication
  boundary, an isolated publication job, and anonymous exact/latest application acceptance; and
- the immutable unclosed `v0.1.8` recovery point is historical and was superseded additively by
  closed `v0.1.9`; it does not require another recovery campaign.

`docs/generated/` guides and project-local `generated/` artifacts are derived outputs, not another
executable or authority. This campaign does not rename those roles.

The present bottleneck is therefore distribution, not missing source implementation: users of the
supported binary cannot access the already completed `0.1.10` capability and product-surface
cutovers.

## 4. Campaign selection

| Candidate | User/workload value | Closure and proof | Cost/risk | Decision |
|---|---|---|---|---|
| Publish the current snapshot through the existing immutable static release path | High: makes completed work usable through the supported binary | Strongest: exact source, target, package, hosted, and anonymous owners already exist | Moderate irreversible effect, tightly bounded by established recovery | **Selected** |
| First-party semantic data engine and complete PostgreSQL cutover | Very high architectural contraction and advances the one-binary stateful goal | Dependency closure spans the standard interface, 982-record BBS, 1,313-owner `lkjournal`, queue, migration, backup/restore, descriptors, and release admission | High context and failure cost; one campaign is presently much less likely to close | Defer first |
| Bounded context traversal | Useful public authoring improvement with a clear independent oracle | Bounded feature slice | Adds more unreleased surface before distributing completed work | Defer |
| Documentation or generated-directory reorganization | Low direct product value; current roles are already explicit | Easy to prove | Would not close a public workflow | Reject |

Publication closes the existing source-to-user split with independent proof. The data cutover
would add a new semantic and operational authority before the current snapshot is available.

## 5. Target state, decisions, and success conditions

### 5.1 Authority and identity decisions

- The root `lkjscript` package version remains the sole product-facing version and owns the matching
  annotated tag.
- `v0.1.10` is the intended identity only while unused. Do not bump an internal contract, standard
  package, graph revision, contributor tool, or artifact format merely for publication.
- `R` is the exact source commit selected by the annotated tag and must already equal
  `origin/main` before tag push.
- The candidate, archive, manifest, checksums, assets, attestations, receipts, workflow artifacts,
  and release are derived distribution evidence. None becomes semantic authority.
- Current-public prose may advance from `v0.1.9` only after anonymous exact and latest acceptance
  completes.
- Published tags, releases, and assets are immutable. Recovery is exact-input retry or a new patch,
  never mutation.

### 5.2 Success conditions

- **PUB-1 — Release source.** Final source commit `R` contains all release inputs and equals
  `origin/main`. Initially it owns unused `v0.1.10`. After authorized additive recovery, final `R`
  instead owns the smallest unused patch and records the failed predecessor unchanged.
- **PUB-2 — Fresh source proof.** The authoritative final full profile passes freshly for `R` with
  every required gate accurately classified and no required reuse, skip, unavailability, failure,
  or unrun result.
- **PUB-3 — Exact target admission.** The repository-owned builder produces the exact static musl
  candidate from `R`; direct ELF inspection, pinned userland command lifecycles, distributed HTTP,
  stateful HTTP, and maintained `lkjournal` service/worker acceptance all pass through the current
  target-admission owner.
- **PUB-4 — Deterministic package.** Local release preparation and strict verification bind `R`,
  candidate bytes, target policy, full receipt, target-admission receipt, notices, canonical public
  metadata, archive, and checksum. Repeated packaging agrees exactly.
- **PUB-5 — Hosted pre-publication proof.** One hosted dry run for exact `R`, workflow content, tag,
  candidate, verifier, and pinned inputs passes every required build/package/application job while
  all publication and post-publication jobs are skipped and no public object is created.
- **PUB-6 — Immutable publication.** The exact annotated tag object selects `R`; its administrative
  confirmation is exact; the tag-triggered canonical workflow publishes one immutable, latest,
  non-draft, non-prerelease release with only the canonical asset inventory and verified digests.
- **PUB-7 — Anonymous acceptance.** Separate anonymous exact-tag and `releases/latest` downloads
  freshly pass checksums, attestations, strict extraction, product/source/target/candidate binding,
  static inspection, distributed HTTP, and stateful HTTP. Exact/latest byte equality is checked in
  addition to, not instead of, both behavioral runs.
- **PUB-8 — Closure.** Evidence, current public truth, terminal status, commits, pushes,
  classifications, limitations, and effects are accurate; closure changes no release input; remote
  `main` contains it; predecessors remain unchanged; and no external release state is unclosed.

### 5.3 Narrow empirical questions for Codex

- **Q1 — Entry and supplied work.** What are the exact local branch, `HEAD`, worktree, remotes,
  divergence, later commits, active campaign disposition, and identities of the supplied
  `AGENTS.md` and this mandate?
- **Q2 — Final release inputs.** What exact `R`, target-policy digest, toolchain/native inputs,
  verifier bytes, candidate digest, capabilities digest, and current full/target gate inventory
  own release admission after local reconciliation?
- **Q3 — Hosted authority.** Are immutable releases enabled; is the exact tag/release unused; can
  the established confirmation variable be set and read; and do workflow permissions, checkout
  separation, artifact handoffs, and publication isolation still match `docs/release.md`?
- **Q4 — Public closure.** What exact tag object, peeled commit, workflow run/attempt/jobs, release,
  assets, digests, attestations, exact/latest downloads, receipts, and observations prove closure?
- **Q5 — Recovery.** Did any retry or additive recovery branch execute, why was it safe, what
  external identity had already committed, and what obsolete state remains historical?

### 5.4 Implementation latitude

Codex owns exact filters, temporary paths, commit count, release-note phrasing, and narrow
pre-tag fixes. Use first-party owners rather than copying release logic. Any changed release input
creates a new `R` and invalidates affected proof.

### 5.5 Out of scope and deliberately deferred

Do not change the data engine or database interface; remove PostgreSQL; add a provider, traversal,
topology, worker recipe, outbound HTTP, target, installer, signing/provenance claim, directory or
documentation migration, or unrelated optimization. Do not publish a package, deploy, migrate
production, or change user data.

## 6. Dependency-ordered work

### 6.1 Reconcile and assemble `R`

1. Complete the reconciliation gate and record exact deviations from the orientation baseline.
2. Audit all changes from immutable `v0.1.9` through current `HEAD`; confirm that the intended
   public delta is coherent and that current release notes/metadata can describe it without
   exposing internal contract ladders.
3. Preserve or install the supplied complete root `AGENTS.md` and this initial mandate. Do not
   rewrite the mandate body to predict the outcome.
4. Make only release-blocking or release-truth corrections. Keep current prose stating that
   `v0.1.9` is latest until public acceptance proves otherwise. Regenerate executable-owned output
   only through its owner and only when an owned input changed.
5. Run narrow changed-path checks, `git diff --check`, and the product-surface audit as relevant.
6. Commit coherent final source content as `R`, normally fast-forward push it, then prove a clean
   `R == origin/main`.

### 6.2 Establish fresh local release proof

Follow the exact current commands and prerequisites in `docs/release.md` and first-party tooling:

1. build the contributor verifier and inspect `release target`;
2. run the authoritative fresh full profile for `R`;
3. build the exact target candidate into a private create-new path;
4. run exact target admission, including both copied-binary HTTP owners and maintained service
   acceptance;
5. prepare the dry-run release from the fresh full and target receipts; and
6. strictly verify the produced archive, checksum, public metadata, extraction, candidate, and
   deterministic repetition.

Keep raw output under `.artifacts/campaigns/202608292254/`; report bounded paths, identities,
digests, classifications, resources, and cleanup. Do not commit binaries, tools, database state,
archives, or raw logs.

Any implementation, workflow, target, dependency, generated, specification, release-procedure, or
verification-owner change after these proofs invalidates affected final evidence and creates a new
`R`.

### 6.3 Prove the hosted dry run

1. Recheck external state and dispatch the canonical `Release` workflow against exact `R` with
   publication disabled and the intended tag.
2. Require fresh source, exact target, deterministic package, transferred distributed HTTP, and
   transferred stateful HTTP evidence. Mutation-dependent jobs must be skipped.
3. Download and verify the exact handoffs by run ID, attempt, artifact ID/digest, file digest, mode,
   source, tag, candidate, verifier, target policy, and cleanup.
4. If the dry run reveals a content defect, fix only that defect before tag push, produce and push a
   new `R`, and rerun every invalidated boundary. Do not reuse a receipt from another identity.

### 6.4 Publish exactly once

Immediately before tag creation:

- prove `R == origin/main` and the worktree is clean apart from explicitly preserved unrelated work;
- prove the intended root version, tag, and release are exact and unused;
- prove no relevant workflow is queued or running;
- verify the hosted dry run and every local release receipt still bind `R`;
- verify immutable releases and exact confirmation-variable authority; and
- confirm the publication job remains no-checkout, write-isolated, and dependent on all required
  pre-publication evidence.

Then follow `docs/release.md`: create the one annotated tag, verify its object and peeled `R`, bind
the established repository variable to the tag-object SHA, push only the tag ref, and observe the
tag-triggered workflow. Do not manually create a release or upload an asset.

### 6.5 Close the public boundary

1. Require the hosted post-release exact and latest jobs to complete their independent anonymous
   integrity, attestation, static, distributed, and stateful checks.
2. Perform the repository's additional token-free transport verification when available.
3. Verify release immutability, latest selection, exact asset inventory, asset digests, tag object,
   peeled source, release state, and exact/latest byte agreement.
4. Write `docs/evidence/202608292254-public-product-release.json` as the concise structured evidence
   owner. Link raw artifacts rather than embedding them.
5. Only after public acceptance, update current release facts in `README.md`, `docs/status.md`,
   `docs/architecture.md`, `docs/release.md`, and measurements/evidence indexes that actually own
   changed facts. Do not alter normative semantics merely to record publication.
6. Update only this campaign's top-level status and append its concise terminal record. Preserve the
   initial mandate body.
7. Create an evidence/current-truth closure commit `E`, run the narrow evidence-only checks,
   normally push it, and report `E` externally. `E` must not change source, Cargo inputs, workflow,
   target policy, generated public behavior, specification, or verification logic.

### 6.6 Recovery

Before tag push, corrections create a new `R` and invalidate affected evidence. An unpushed local tag
may be deleted only after proving it never crossed the remote boundary.

After tag push:

- never move or delete the tag;
- for an unchanged exact tag and purely transient/idempotent orchestration failure, one bounded
  exact-tag recovery dispatch is authorized only after proving it cannot clobber or contradict
  existing state;
- read-only propagation, download, attestation, and behavior verification may retry within current
  repository policy;
- any source, workflow, target, verifier, package, candidate, or public-content defect requires the
  smallest unused additive patch, normally `v0.1.11`, with a new `R` and every affected fresh gate;
- at most one content-changing additive patch is authorized in this campaign; a further distinct
  content defect terminates the campaign with exact externally committed state and recovery needs.

Never edit, delete, unpublish, relabel, replace, or clobber a published release or asset.

## 7. Verification and evidence

Use the verification ladder rather than repeatedly running the broadest suite:

1. establish only the exact predecessor/source baseline needed for comparison;
2. use focused tests and policy audits while correcting release inputs;
3. run the final fresh full source profile once stable;
4. build and admit the exact candidate from that source;
5. prepare and strictly verify the deterministic local package;
6. run the hosted dry run;
7. publish through the tag-triggered workflow; and
8. prove anonymous exact and latest boundaries independently.

| Boundary | Required evidence |
|---|---|
| Source | Exact `R`, root version, capabilities/product surface, clean generated owners, fresh full receipt |
| Target | Candidate SHA-256 and mode, direct ELF static facts, target policy, pinned userlands, command lifecycle |
| Maintained applications | Distributed HTTP, stateful BBS with fresh isolated PostgreSQL, and `lkjournal` service/worker behavior; accepted graph authority unchanged |
| Package | Deterministic archive repetition, exact inventory, notices, canonical metadata, checksum, strict extraction, hostile-input rejection |
| Hosted dry run | Exact run/attempt and handoffs; all required jobs fresh passed; publication jobs skipped; no public mutation |
| Publication | Annotated tag object and peeled `R`, confirmation binding, immutable latest release, canonical assets/digests, isolated job |
| Public exact/latest | Separate anonymous checksums, attestations, extraction, static inspection, application runs, then byte equality |
| Closure | Structured evidence, current truth, terminal status, allowed-path evidence commit, clean/preserved tree, remote relation |

Required negative or containment checks include version/tag mismatch, an existing tag/release,
disabled immutability, concurrent release work, stale or foreign receipts, dynamic or malformed
candidate bytes, target-policy disagreement, archive/link/path/canonicalization/checksum failures,
handoff mutation, publication dependency bypass, asset clobber attempts, exact/latest identity or
behavior disagreement, failed application startup without readiness, database failure with cleanup
and no authority mutation, and premature current-public documentation.

Classify every check exactly as `fresh passed`, `reused passed`, `skipped`, `unavailable`, `failed`,
or `not run`. Final full, target admission, local release verify, hosted dry run, publication
preconditions, and anonymous exact/latest boundaries require fresh evidence for their exact inputs.
A required unavailable or failed gate prevents completion.

Do not claim a minimum kernel, universal Linux portability, code signing, reproducible build,
hostile-code isolation, multi-tenant security, or general supply-chain provenance. Report only the
tested target, pinned environments, exact identities, and measured resource observations.

## 8. Git and external-action authorization

| Action | Authorization and conditions |
|---|---|
| Edit, test, and commit in the active checkout | Authorized within this objective; preserve unrelated work and stage explicit paths |
| Normal push of `R` and `E` to `main` | Authorized only as safe fast-forwards after fetch/reconciliation and required checks |
| Hosted `Release` dry-run dispatch | Authorized for exact `R` with `publish=false`; no public objects |
| Set `LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA` | Authorized only to the exact verified new annotated tag-object SHA through the established administrative boundary |
| Create and push an annotated tag | Authorized once for unused `v0.1.10`; once more only for the single additive patch recovery described above |
| Tag-triggered immutable GitHub Release | Authorized only through the canonical workflow after every named precondition |
| Exact-tag recovery dispatch | Authorized once only for unchanged exact input and proven transient/idempotent orchestration |
| Read-only release/download/attestation verification | Authorized and repeatable only within bounded current policy |
| Force push, history rewrite, retag, tag deletion, release/asset mutation, manual parallel release | Forbidden |
| Package-registry publication, deployment, production migration, user-data mutation, installer/update publication | Forbidden |
| Workflow, settings, secrets, permissions, or repository-variable changes other than the exact confirmation binding | Forbidden |

No permission in this section authorizes destructive cleanup of unfamiliar local work.

## 9. Documentation, evidence, and final report

The initial mandate above records intent and must not be rewritten to resemble the outcome. Codex may
change only the top-level `Status` field and replace the terminal paragraph below with a concise
completion or termination record. Large evidence belongs in the structured evidence owner and
ignored artifacts.

The final report must provide:

- entry and final branch/`HEAD`, worktree, remotes, divergence, preserved work, `R`, `E`, commits,
  pushes, and campaign reconciliation;
- product/tag/release/workflow/asset identities, sizes, digests, attestations, and exact/latest state;
- verifier, target policy, candidate, capabilities, and every source/target/package/hosted/public
  receipt with paths, digests, and classifications;
- documents changed, historical records retained, external mutations/retries/recovery, resources,
  tested environments, limitations, nonclaims, and deviations;
- confirmation that no predecessor publication, deployment, package, production data, user data, or
  history was mutated; and
- the smallest next candidates: semantic data cutover, bounded traversal, and remaining maintained
  authored operations.

Do not paste raw logs, manifests, archive inventories, database dumps, complete generated output, or
full diffs.

## Terminal record

No terminal record exists. The final completion or termination commit must update only the
authoritative top-level status and replace this paragraph with a concise record naming `R`, `E`,
verification and public identities, push state, external mutations, preserved work, and any recovery
boundary.
