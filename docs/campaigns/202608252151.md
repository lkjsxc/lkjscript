# Self-describing semantic change interface campaign

This is the operational index for [`prompts/202608252151.md`](../../prompts/202608252151.md).
Executable contracts, tests, receipts, and the active checkout remain the source of current facts.
The campaign supersedes `prompts/202608250938.md` only as active implementation scope.

## Baseline

| Item | Evidence |
|---|---|
| Starting branch and commit | `main` at `7f05ea8907a84f0ae2920a201be7be2850b7f164` |
| Checkout relation | Exactly the audited baseline; no later commits to reconcile |
| Preserved starting work | User-owned `AGENTS.md` update and untracked campaign prompt |
| Initial change discovery | 65 records, 4,426 bytes, 13 operations, 2 operation fields; SHA-256 `48f4d860838e46cc1b4759fa270a5d3e270d5dba3c9e7314446b9509b2783ea9` |
| Initial discovery artifact | `.artifacts/campaign-202608252151/change-capabilities-baseline.txt` |

## Completed cutover

- Commit `e5ecf02d` makes one exhaustive typed descriptor inventory own all 13 public
  operation names and all 49 ordered operation fields. Record recognition, field validation,
  registry projection, generated documentation, and direct-operation discovery consume it.
- The registry exposes all field forms and closed values, validates descriptor structure and
  decoder coverage, and advertises only direct `rename.owner` with its exact plan/apply usage.
- Direct and record adapters construct one transport-neutral typed request and publication options
  before plan comparison, project discovery, repository access, preparation, response rendering,
  or publication. The CLI contract is 6; `lkjscript-change-records-3` and
  `lkjscript.change-plan.v4` are unchanged.
- Public differential and failure tests cover typed and byte-level equality, optional controls,
  exact-owner validation, malformed and duplicate arguments, reviewed-plan mismatch before project
  discovery, stale base, absent owner, unchanged authority, and a copied-release-binary workflow.
- The prior campaign's unrelated compiler, runtime, query, package, application, storage, and scale
  phases remain deferred.

## Evidence index

| Evidence | Path or result |
|---|---|
| Final change discovery | `.artifacts/campaign-202608252151/change-capabilities-final.txt`: 133 records, 10,304 bytes, 13 operations, 49 fields; SHA-256 `ab2481e1c37a20659d1747d4df837f93cf4f701993335035100ac3a8db0fdbe7` |
| Direct/record workflow | `.artifacts/campaign-202608252151/workflow.ZnCQmn`: equal 1,252-byte plan responses; SHA-256 `f1594e96bd5afa21325eee539774df94d16ca21b3132d3e3e22ec48c8ad13a69`; 7 commands, 0 retries |
| Generated documentation | `.artifacts/campaign-202608252151/verify-generated-final.txt`; registry digest `b658f5a5b62e635aa70367040fa4ae3ffc75d4ec4d75d6e5be218e442e21aa26` |
| Intermediate focused profile | `.artifacts/lkjscript-dev/check/1787668422106756179-1472625-0/receipt.json`; 6/6 checks were fresh and passed before the final field-syntax wording correction |
| Maintained identities | `.artifacts/campaign-202608252151/{standard,lkjournal}-{review-receipt,check,build}.json` and `builtin-inspect.json` |
| Final complete profile | Fresh `lkjscript-dev check full` receipt is retained under `.artifacts/lkjscript-dev/check/` and returned in the final handoff |

## Remaining dependency-closed candidates

1. normalized command template plus `check`/`build`/`run` closure;
2. bounded normalized query, context, and impact cutover;
3. remaining public authored operations selected from maintained application workflows; and
4. normalized standard package and `lkjournal` migration after their prerequisites close.
