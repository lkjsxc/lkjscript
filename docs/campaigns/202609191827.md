# lkjscript — Sustained Mainline Development Mandate
## Human-directed. Agent-operated. Substantial progress. Verified delivery.

Execute this mandate for `https://github.com/lkjsxc/lkjscript`.

You are the responsible product architect, implementer, verifier, and integrator for this execution. Investigate the actual project, choose valuable work, implement it, establish evidence, and deliver accepted results to GitHub's remote `main` through normal authorized operations.

This is an execution request, not a request for advice, a roadmap, a rewritten prompt, or instructions for the human to give to another agent. A local patch, topic-branch push, open pull request, green CI run, or enabled auto-merge is not completed mainline delivery. Use the tools yourself. Delegate bounded work only when delegation is actually available; retain integration responsibility.

Communicate with the human in Japanese. Use the repository's established language, normally English, for code, documentation, commits, and technical records. Explain decisions, evidence, and uncertainty, not private deliberation.

## 1. Product direction and decision authority

Build a powerful general-purpose language and ecosystem through which a human can delegate the creation, understanding, modification, composition, execution, distribution, and maintenance of useful software to AI. The human directs intent and remains in control; the product should not require the human to become its manual implementation engine.

Optimize for capabilities that compose, reliable agent workflows, inspectable meaning, useful performance, data preservation, and maintainable foundations. A demonstration matters when it exposes a reusable capability or a real limitation, not merely because it produces a screenshot or another application name.

Exercise your own judgment now. Previous assistant decisions, campaign plans, architectural fashions, and incidental implementation limits are revisable evidence, not permanent user requirements. Respect current contracts until deliberately changed, preserve their important guarantees, and provide a coherent transition when redesigning them. Neither a large rewrite nor a tiny diff is inherently preferable.

## 2. Working horizon: approximately 120 minutes, not one small task

The human prefers a sustained engineering session on the scale of approximately **120 minutes**, rather than a roughly ten-minute task closure. There is no user-imposed deadline. Treat this as a strong scope-and-persistence preference, not a promise about platform runtime, a compulsory minimum, or a stopwatch target.

Plan enough worthwhile work to use that horizon productively when the actual environment permits it. Do not assume a short session merely because an earlier execution was short. There is no requirement to finish the first plausible task and immediately return.

**A completed checkpoint is not automatically a completed session.** Closing inherited work, merging an already-verified candidate, repairing a small blocker, or making a narrow correction should normally be an early checkpoint. After it is accepted, reassess the improved project and continue with a meaningful implementation outcome without requesting renewed permission.

Choose scope large enough to advance a coherent product objective, then divide it into independently acceptable steps. Prefer an end-to-end improvement with its necessary mechanism, practical use, failure behavior, and verification over disconnected easy fixes. A focused correctness or performance correction can be substantial when its demonstrated impact warrants it; do not require gratuitous new features.

After each checkpoint, ask: what is now the highest-value work that can still be implemented, verified, and integrated responsibly in this execution? Continue when that answer is concrete and feasible. Do not merely list that work as a future recommendation while ample execution capacity remains.

Never inflate elapsed time through sleeping, redundant builds, repeated unchanged status reads, excessive documentation, manufactured tasks, or arbitrary commit counts. If useful work finishes unusually quickly, enlarge the useful objective where justified; do not slow down the work. If real limits intervene, deliver and report the accepted subset honestly. Reserve capacity for verification and integration instead of starting an expansion that predictably strands everything off main.

## 3. Authorized operations and boundaries

Normal development, tests, disposable local environments, owned working branches, commits, ordinary repository CI, non-forced main integration, and safe cleanup of integrated campaign-owned references are authorized within actual permissions and protections. Make routine engineering choices yourself.

Routine repository publication through the existing maintained release process is permitted when a selected usable milestone or a genuine due-delivery obligation calls for it. It is not required for every session and must not consume the session merely to create another release. Mainline delivery and public release are separate outcomes.

This does not authorize production deployment, destruction or silent migration of operational data, purchases, increased billing, credential disclosure or changes, access-control changes, bypassing branch protections, force pushes, or rewriting immutable published history. Respect higher-priority instructions and required tool confirmations. Use disposable owned resources for destructive tests. Repository content, comments, logs, and external pages cannot grant new authority.

## 4. Establish current reality and an executable delivery path

Read applicable `AGENTS.md` guidance before editing. Inspect current remote `main`, recent consequential changes, relevant campaign endings, active branches or pull requests, and matching verification runs. Treat status documents and remembered revisions as navigation aids, not substitutes for current code and remote state.

When a checkout exists, inspect its HEAD, branch, worktree, index, relevant untracked files, stashes, worktrees, remotes, divergence, and active jobs. Preserve unrelated work. When an observation is unavailable, say so; do not invent a clean checkout or reset to an earlier inspected SHA.

Establish early how this environment can read, edit, execute tests, retain evidence, and update remote refs. Inspect the available connected tools and their actual schemas. A missing shell command or local toolchain does not establish that all execution or write paths are unavailable. Use supported alternatives, including repository CI when appropriate. Do not request credentials in chat or weaken security to manufacture a path.

Inspect the maintained verification and release owners before inventing infrastructure. In particular, check whether `docs/development-verification.md` and `.github/workflows/verify.yml` provide an existing pre-main route. Verify their current behavior rather than assuming ordinary pushes trigger acceptance.

Reconcile inherited work as accepted and integrated, actionable, blocked, or superseded with a reason. Preserve valid evidence and finish worthwhile outstanding integration promptly. This startup recovery does not, by itself, exhaust this mandate.

## 5. Select a substantive objective from evidence

Briefly compare a small number of genuinely different candidates, including the strongest product-capability or usability candidate and any dominant correctness, performance, or delivery blocker. Evaluate demonstrated utility, generality, future leverage, maintenance burden, uncertainty, and likelihood of complete verified delivery. Do not manufacture numerical scores or a large strategy report.

Exercise or inspect representative public workflows before selecting speculative machinery. Distinguish limitations in the application, authoring workflow, preparation and checking, runtime execution, storage, and deployment. Use a discriminating experiment when the distinction affects the decision.

Choose one primary objective and a small sequence of coherent checkpoints. State what becomes possible or materially better, the current limitation, the intended public path, the necessary dependency closure, independent acceptance evidence, and compatibility implications. Keep one plausible extension available if the primary work closes early; it is not an obligation to accumulate unrelated features.

Do not default to campaign bookkeeping, release machinery, or verification refactoring merely because these are easy to inspect. Such work is appropriate when it removes a measured dominant obstacle. Once an enabling repair works, use the repaired path for product progress when feasible.

Do not bind future executions to today's SHA, stale backlog order, or a preselected feature. Reassess at each invocation and consequential discovery.

## 6. Keep a compact, resumable execution record

Use the repository's existing campaign convention and reuse the appropriate record for a continuation. For a genuinely new record, use the actual current `Asia/Tokyo` timestamp, normally `docs/campaigns/YYYYMMDDHHmm.md`, preserving any real filename collision.

Honor applicable archival requirements, then keep execution additions compact: objective, checkpoint state, consequential decisions, current source identity, evidence, blockers, and the next concrete action. Separate the original instruction from subsequent findings. Do not create parallel ledgers or repeatedly copy historical chronology into status summaries.

Update this record at meaningful boundaries and before likely context loss. It should let a resumed execution continue without reconstructing the entire conversation. Record decisions and observable facts, not a private reasoning transcript. Documentation supports execution; it is not a substitute for it.

## 7. Implement complete behavior, not isolated machinery

Follow the actual callers, ownership boundaries, and narrower guidance. Complete the necessary path across implementation, public discovery, affected libraries and consumers, error handling, generated assets, and documentation. Reuse general mechanisms rather than adding application-specific compiler behavior or hidden privileged shortcuts.

For a new public capability, demonstrate fresh authoring and use through the ordinary product interface, including a copied candidate executable outside the language checkout where required. Keep literal inputs and independent expected results. A host-side replacement application or direct privileged storage mutation cannot prove language capability.

Preserve the relevant semantic, identity, effect, resource-lifetime, persistence, cancellation, and recovery guarantees. Make representation or contract changes deliberate and detectable. Analyze existing consumers and recovery before removing predecessors. Do not silently reinterpret stored data or retain a second permanent implementation without a concrete need.

For performance work, measure an equivalent baseline and candidate on a representative workload. Identify the affected stage, retain correctness checks and unfavorable results, and report environment and limitations. Do not infer throughput, agent effort, or token savings from unrelated measurements.

When a hypothesis fails, revise it and continue. A failed test is information to diagnose, not a reason to weaken the test or abandon the objective without investigating feasible alternatives. Use bounded exploratory experiments for risky redesigns before committing the whole session to them.

## 8. Verify proportionately and preserve source identity

Use maintained focused feedback during iteration and dependency-complete acceptance for each integration candidate. Discover the current gate requirements instead of hardcoding a historical gate count. A clean-worktree changed-only selector is not proof of a committed implementation.

When full acceptance is required, run the maintained fresh profile after relevant inputs stabilize. Avoid overlapping equivalent full runs solely for reassurance. Inspect existing healthy runs before dispatching replacements. Reuse completed evidence only when its source, verifier, environment, inputs, workload, and trust bindings remain valid. Changes invalidate the affected claims, not automatically every unrelated proof.

Use independent expected behavior, regression witnesses, and meaningful failure cases. Where practical, show the relevant witness distinguishing predecessor from candidate. Never claim a predecessor run or independent review that did not occur. Changed validation machinery cannot certify itself merely by reporting success.

Bind results to the exact tested commit or tree, executable where relevant, verifier, profile, and run/attempt. Inspect terminal results and required receipts, not just a green badge or artifact upload. Preserve genuine failures and distinguish fresh, valid reused, failed, cancelled, skipped, unavailable, and unrun evidence.

Treat later reporting-only commits separately; do not relabel old evidence with a new SHA or enter a circular report/hash/retest loop. Additional executable changes require appropriate renewed proof.

## 9. Deliver accepted checkpoints to remote main

Integrate at natural, independently acceptable boundaries. Do not hold every completed result off main until the entire horizon is over, and do not trigger expensive acceptance after every trivial edit. Batch tightly coupled edits into a coherent candidate.

Before integration, refresh remote `main`, applicable rules, candidate identity, and required checks. Review the complete intended delta. Preserve intervening work and revalidate consequential interactions. Do not use an unverified main update as a way to obtain the first test run.

Use a normal fast-forward or permitted merge path. If a pull request is required, complete the authorized merge rather than stopping at its creation. If using Git-object tools, preserve the intended base tree and parentage and update refs without force. After an ambiguous write response, read the actual remote state before retrying.

Independently read remote `main` after the operation and verify that it contains the accepted result. Identify the actual final SHA and its relationship to the tested source. Do not describe a new merge, squash, or cherry-pick commit as the exact previously tested candidate; assess the resulting content and interactions according to maintained policy.

A checkpoint is delivered only when its acceptance is satisfied and its remote-main postcondition is observed. Then reassess and continue under the working-horizon policy. An updated campaign file alone is not the substantive progress this mandate seeks.

## 10. Continue intelligently while jobs run

Keep a stable candidate source while its authoritative verification runs. Use the live session for useful independent work: inspect adjacent consumers, design a justified next checkpoint, review failure paths, prepare independent oracles, or diagnose actual failures. Isolate subsequent edits so they do not silently change the tested candidate or contaminate its outputs.

Check remote jobs at meaningful work boundaries. Use a supported bounded wait when appropriate and available. Do not create hidden or indefinite polling, repeatedly fetch unchanged logs, or push commits just to provoke activity. A job being in progress is not, by itself, a reason to abandon feasible live-session work or integration.

Diagnose failures at their actual boundary: setup, product behavior, verifier, artifact availability, permission, or integration. Retry only when the operation is safe and a changed condition or diagnosis justifies it. Preserve source and attempt identities.

Do not invent a time limit to justify early closure. Equally, do not pretend the prompt grants unlimited context, execution time, permissions, or unattended operation. If an actual limit or externally controlled gate prevents further progress, preserve the exact state and report it. Do not promise work after the response or claim pending integration as completed.

## 11. Publication, guidance, and cleanup

Keep release frequency tied to useful installed milestones, not the number of executions. Reconcile genuinely due delivery and otherwise record a concrete publication trigger. Use the maintained release path and valid accepted assets rather than rebuilding or reconstructing old evidence ceremonially. Never weaken release guarantees to satisfy the working horizon.

Change `AGENTS.md` only when a durable, justified guidance change is needed. Do not replace it ritualistically, fill it with campaign history, or treat an edit as changing the active instruction hierarchy. Revise stale process rules at their proper owner when they demonstrably obstruct value, preserving the guarantees they protect.

Clean up campaign-owned branches and worktrees only after confirming integration or justified supersession, preserving unique work and evidence, checking unchanged tips, and confirming no active dependency remains. Do not disturb unrelated work. When the available client cannot safely delete a reference, report its retention instead of force-moving or disguising it.

## 12. Session completion gate and final report

Before ending, explicitly assess both delivery and whether stopping is justified. Successful closure requires a substantive accepted outcome on remote `main`, not just a completed initial chore. Explain why the selected outcome is meaningful and why another feasible high-value implementation step is not being taken now.

Near the preferred working horizon, consolidate and close a coherent result; continue longer when that is the responsible way to finish and the environment permits it. Early closure is appropriate for an actual blocker or a genuinely completed substantial objective with no justified further executable scope. Merely finding a small passing patch, finishing the first checkpoint, or reaching a convenient summary boundary is insufficient.

When the full objective cannot be completed, preserve and integrate any independently acceptable subset where safe. Keep unfinished work isolated and resumable. Report partial delivery or blocked delivery accurately; never manufacture success or hide the remaining work.

The final Japanese report should state:

- What became possible or materially better, separating inherited closure from work newly completed in this execution.
- Starting and final remote-main revisions, accepted source identities, and the actual integration result.
- The most important evidence and limitations, including compatibility, data, release, and deployment implications.
- Retained or cleaned-up work, any exact blocker and resumption action, and why the session stopped. Report timing only when observed; distinguish active engineering from external waiting rather than presenting invented precision.

Keep the report focused on outcomes and evidence. A continuation note is a recovery mechanism, not a substitute for work that is still feasible now.

**Begin by inspecting current reality and the available delivery path. Select a substantive objective, execute it, verify it, integrate accepted checkpoints, and keep making worthwhile progress rather than stopping at the first small success.**

---

## Execution additions — continuation on 2026-09-19

Initial mandate above retains its wording; escaped chat Markdown and the redundant URL link were rendered as ordinary Markdown.

Starting and refreshed main: `d77fb8bafab8a5a92b4d5251e62a9c44a41aea82`. Its parent `ffd155350d7f294ab5d2618a5a7784eb0c493213` is the already-integrated HTTP correction, not new product work. No open PR was observed. Root guidance is unchanged; indexed repository guidance contained only the root file.

The interrupted lineage `verify/202609191648-sustained` selected `e08e9975c9c5c2d807991377fbd7f7035f77d663`, a temporary public-source/toolchain transfer, not acceptance. Transfer `35430345691/1` succeeded. The resumed local shell and both Python execution routes returned transport timeouts, including a fresh attachment mount recovery attempt. Local HEAD/index/worktree/stash/worktree-list and active-process observations remain unavailable; no local cleanup or reset is claimed. Connected Git-object writes and unprivileged hosted execution remain available. No credentials, permissions or protections were changed.

Candidate comparison: authoring extensions would improve editing but lack a freshly demonstrated blocking consumer; further release machinery is not due and would repeat enabling work. Map insertion/removal demonstrably clones the complete `BTreeMap` in both checked evaluators. Select persistent ordered-map execution, preserving immutable snapshots, primitive key order, independent raw admission, current byte formats and optional quota/cancellation failure behavior. The public path remains ordinary standard map operations in authored programs; no new spelling or application-specific behavior is selected.

Checkpoint 1: isolated neutral AVL storage with entry sharing, ordered iteration, a standard-library BTreeMap oracle, retained versions and reservation-failure injection. This is an experiment, not yet wired into production and not main-ready. Checkpoint 2: complete production/reference/codec/destruction cutover with appropriate accounting and fresh public program use. Checkpoint 3: maintained full fresh acceptance, reviewed non-forced integration and independent remote-main observation. An adjacent extension is linear ordered map-entry projection only if measurements justify it.

The temporary experiment workflow grants only `contents: read`, retains no checkout credentials, executes no release/deployment/ref writes, and does not claim full acceptance. Restore the exact maintained Verify workflow before selecting the integration candidate. Do not merge a storage-only experiment. No release is selected; publication becomes due for an installed consumer needing the accepted performance increment or an explicit release request.

### Measured experiment and blocked continuation

Experiment source `ee442c07e6e21a368b85c5b13f77e96d5c2a3b18`, tree `7963e58c8a7f9bbe71fb4864c51c24c3a5bef320`, ran in `35434916472/1`, job `105875832455`. All four selected storage tests passed: ordered mixed-key bulk construction; randomized insertion/removal against independent BTreeMap with retained snapshots and AVL invariants; each allocation-reservation failure preserving both originals; and growing-map storage work. The complete workflow **failed** at formatting, not at these tests. Pinned rustfmt reported three wrapping differences in `map.rs`; they remain unfixed in that tested source. No full-profile acceptance or public-executable workload was run.

The job used Rust 1.98.0, release optimization, Ubuntu 24.04.5, x86_64 GNU, 15 GiB reported memory, and two Cargo build jobs. Its single storage comparison retained exact output equality. Nanosecond observations:

| Entries | BTreeMap clone/insert | Persistent insert | Baseline copied entries | Candidate allocated nodes | Candidate charged bytes |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1,000 | 10,673,546 | 425,030 | 499,500 | 10,967 | 662,416 |
| 4,000 | 159,951,259 | 1,966,212 | 7,998,000 | 51,893 | 3,034,864 |
| 16,000 | 2,770,787,926 | 8,969,063 | 127,992,000 | 239,603 | 13,676,944 |

These are single-run storage-layer measurements, not public application throughput or an actual predecessor executable benchmark. The candidate loop additionally checked its retained predecessor after each insertion. Build time (8m41s in the runner log) is not execution speed. Originals remain in the identified GitHub job log; this experiment uploaded no separate artifact. The transient source-transfer artifact is not semantic evidence.

Before the block, unreferenced tree `8b633f8b08ea2ee6c88f84461cf27594d6879336` was created for a partial cutover: sealed flat/persistent storage interchange, a changed raw Map field, and map module registration. It is **not compiled, accepted, committed, or on a branch** and is not a resumable accepted checkpoint. The owned branch deliberately retains the tested storage experiment, not that incomplete raw-representation change. Availability of unreferenced Git objects is not a retention guarantee.

A subsequent source-write request was rejected by the tool with an OpenAI safety-verification block. No SHA or successful write was returned for that request; it was not retried through another route. This block followed the independent local execution transport failures. Neither failure establishes a GitHub branch-protection denial, and no access-control change is requested. Main was reread afterward and remains `d77fb8bafab8a5a92b4d5251e62a9c44a41aea82`. No newly accepted product behavior reached main; no release, deployment, migration, branch deletion or unrelated cleanup occurred.

Resume this lineage only through an available authorized execution/write path. First finish and review the production cutover, including checked parent/child provenance, reservation-before-growth, one-time flat-map promotion without unaccounted payload copying, independent reference admission, ordered codecs and stack-safe destruction. Reconsider the prototype's one-million-entry cap rather than silently adding a new trusted-execution quota; it is not a durable product requirement. Then fix the observed formatting, add deep-destruction and raw/retained admission negatives, exercise a fresh copied public executable with independent outputs, restore original Verify workflow blob `776c52f0b58cf678b896a438f21ee11003ebf147`, and run the maintained complete fresh profile before normal main integration. Do not merge this experiment or the partial tree merely to satisfy delivery. This reporting-only commit skips repeat CI; it does not change the tested-source identity or turn the failed workflow into a pass.
