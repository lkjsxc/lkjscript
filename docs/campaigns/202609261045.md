# Bounded native form serialization and mainline completion

## Original user mandate

https://github.com/lkjsxc/lkjscriptlkjsxc/lkjscript  について進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。最も筋の良い方向に進むために、限界まで深く考えてほしい。超長期的な視点からお願いしたい。どれだけ時間がかかっても構いません。開発環境として、cw-bd235945fff44468a0f41292という名前で、lxcのコンテナを用意しました、自由に活用ください。安全性チェックも気にかけつつ、なんとか進んでほしい。

## Reconciliation and selected work

Work only as the existing unprivileged coder user in the supplied container
cw-bd235945fff44468a0f41292, confirmed through Home Coder workspace tomato-ocelot-73
(bd235945-fff4-4468-a0f4-1292e6333578). Repository:
`/home/coder/workspace/lkjscript`. Initial local main is
588995ef63da46d2fc9a14bf5ce40acb1f2f2380; refreshed origin/main is
3a34c305119ca7c1dc34ee7326ebe5b960d2a0b2, eight commits behind with no divergence.
Preserve unrelated untracked `docs/campaigns/202609242022.md`, the existing
corrective worktree and all original evidence. No stashes or active local
compiler/test jobs were observed. Cargo 1.98.0 is installed; invocation PATH needs
`/home/coder/.cargo/bin`. No host, credentials, protection or production changes.

Original Verify 36206083541/1 is still executing its full profile for exact source
e5847f1e568ad823325dd3a1e56c7a6fd64fa3fa. Reuse that run without queuing a duplicate.
Its deployment acceptance and the 8da50e4e relocation supplement retain their
own source identities. Public v0.1.47 is complete; candidate v0.1.48 remains a
separate publication decision after required mainline proof.

The existing ordinary form library parses input but cannot construct form bodies
or query values for another native application. Add bounded UTF-8 serialization
beside that decoder, with no compiler primitive, package resolver or external
runtime. Preserve ordered pairs and duplicate names, distinguish spaces from
literal plus signs, and charge the exact encoded bytes including separators
before output growth. Reject negative bounds, too many fields and overflow
without exposing a partial successful result. A rejected input remains distinct
from operational allocation/cancellation failure.

Use the URL Standard's UTF-8 form serializer as the interoperability contract:
https://url.spec.whatwg.org/#urlencoded-serializing
This is not general URL/path/HTML escaping, line-ending normalization, MIME
parsing, authentication or a transaction protocol. Existing exact packages do not
silently update; readers explicitly author/export/stage the new library.

Prove literal native authorship through public plan/apply/check and exact offline
transport. Add independent expected bytes, inclusive limit/Unicode/duplicate
cases, negative type/review checks, source-free command use and a real loopback
POST to the existing ordinary receiver. Verify the unchanged durable editor
against the updated exact supplier. Keep focused descendant proof separate
from the original hosted full source receipt. Integrate only with complete
applicable evidence and a refreshed normal mainline preflight.

## Original deployment acceptance completed

Verify 36206083541/1 (job 108302807605) completed successfully at
2026-09-26T01:50:22Z. Downloaded original artifact 10894503910, named
`verify-e5847f1e568ad823325dd3a1e56c7a6fd64fa3fa-36206083541-1`, to this
campaign's ignored `original-verify/` directory. Hosted artifact digest:
`sha256:0eb4aa69d2ec0fa35d09678f1bd247cbe4fc606ecb6dfafcc4f5746eb9eed636`.
The full summary records all 26 selected gates passed fresh, zero reused,
3334.373119559 seconds and no failure. Original receipt binds exact source
e5847f1e568ad823325dd3a1e56c7a6fd64fa3fa, identical initial/final input digests
and `input_stable=true`. Receipt SHA-256:
`82e7fb3769a3f3e4951fdfa08fe2578ade89db08a30551427e233edcdd9dd413`.

The relocation supplement's existing focused receipt independently records
8da50e4ea6d7bf5e9a96f68d413dd7d84128d5d4, step complete and exit zero. Its
seven workflow tests, clippy and separate real-browser proof are detailed in
202609261015.md. Descendants since e5847f1e change tests and documentation,
not the deployment implementation. Keep those identities distinct rather than
claiming a fresh full run against a later documentation commit.

Read-only preflight found main still at 3a34c305, no divergence, no active branch
rules and no branch protection. Normal fast-forward delivery of the original
eight commits plus this acceptance record is authorized. The native form
encoding implementation remains a separate, uncommitted workstream under
focused verification; it is not included in this deployment candidate source.

## Native form encoding and affected consumers

The literal `form-codec.lkjc` now exports `encoding = valid(Bytes) | invalid(Text)`
and `encode(List<field>, I64, I64)`. Ordinary functions own UTF-8 traversal,
uppercase percent bytes, safe characters, space/plus distinction, ordered duplicate
fields and exact separator accounting. Private pending output cannot escape as a
successful prefix. Negative bounds reject before field count; each append checks
remaining capacity, and text length is checked before conversion to Bytes.
No kernel, intrinsic, package resolver, configuration grant or runtime limit changed.
The existing strict decoder is unchanged. Its structural consumer gains individual
and batch encoding ports; the guide shows an actual JSON-boundary command and
explains that native applications receive Bytes directly.

Initial native review found a missing closing parenthesis and a literal test with
an incorrect 19-byte budget for a 21-byte safe-character result. Both were corrected
before acceptance; the temporary owner-print diagnostic was removed. The first
complete focused run then passed the existing decoder, real POST and review/mutation
workflows, but the large matrix failed with `normalized_collection_items`: multiple
near-8192-byte forms shared one command's unchanged cumulative collection quota.
Original `forms-1.log` and `focused-1.json` retain the 3-pass/1-fail result. The
correction splits large fixtures into individual invocations rather than relaxing
runtime bounds, dropping cases, changing expected bytes or catching resource failure
as malformed form data. One large individual valid form still has to pass.

The corrected focused run began 2026-09-26T01:59:06Z and completed at 02:04:01Z.
`focused-2.json` records format/forms/editor/clippy exit codes all zero. Native and
host test inputs were frozen during execution. The exact inputs are bound by:

| Source path | SHA-256 |
| --- | --- |
| docs/guides/examples/form-codec.lkjc | 6f19c99a2db692ab57c8937246869b71ed7066a54f3e3fb24771584f248894b4 |
| docs/guides/examples/form-consumer.lkjc | 214cbaf9b1a723c4835b81130143742311dab3dfe5cd92d216e55f8e8fee9ff5 |
| tests/public_cli/native_forms.rs | 7f11eacf6e692701f62539f7bf796e4fceb642c6d66144a2fa57ef54df29cb2d |
| tests/public_cli/native_form_encoding.rs | 9f335ea4e82e087791a7642b2fc44e2d2224fe26a67fddf2651c1620171149f2 |

`cargo test --locked --test public_cli native_forms -- --nocapture --test-threads=2`
passes all four workflows in 174.97 seconds. Eighteen added literal native tests
raise library/consumer/receiver closure totals to 124/125/129 (shared standard tests
are not new). The independent Rust encoder oracle compares 674 cases in both pure
project and source-free command paths: 1348 comparisons, plus 205 detached roundtrips.
Expected bytes are checked independently before trusting a roundtrip. Cases include
all ASCII, UTF-8 scalar boundaries, combining marks/BOM/control data, ordered duplicate
and empty fields, negative/I64-extreme bounds, encoded expansion, exact 8192-byte
output and early rejection of a 500,000-byte value. Every copied product invocation
has an empty PATH and the reference/VM pure paths agree.

Two source-free server lifecycles receive twelve real loopback requests: four
concurrent successful POSTs per lifecycle, then one malformed UTF-8 body and one
foreign content type. Statuses and full decoded ordered fields agree with independent
expectations; joined shutdown and command cleanup leave no remaining resources.
The Rust test client transports native-produced bytes; this is not a new native
outbound HTTP-client or browser witness. Two ill-typed native proposals and a stale
review token reject without changing accepted revision. A separately accepted,
well-typed raw-space mutation fails the original native expectations.

`cargo test --locked --test public_cli native_editor -- --nocapture --test-threads=2`
passes its existing end-to-end composition workflow in 99.42 seconds with the new
exact form supplier. It retains admission, shared authentication, conditional save,
conflict and durable-state assertions. `cargo clippy --locked --test public_cli --
-D warnings` and format pass. These are focused native-library/affected-consumer
proofs, not a relabeled full-26 or static-release acceptance. The compiler, runtime,
release controller and deterministic generated assets are unchanged by this increment.

## Independent public-runtime interoperability

Downloaded the existing v0.1.47 archive and checksum using authenticated GitHub
release access, checked `SHA256SUMS`, inspected archive membership, and extracted
only its executable and manifest under the campaign's owned ignored directory.
The copied executable reports 0.1.47 and has SHA-256
`8fb8afa22ea7cd7c2105c0781dc7a6bb77c4fc189ea414c64285624452422e98`.
No product rebuild, installed-runtime replacement or public asset mutation occurs.

The literal-input driver `public-oracle.mjs` only binds observed base/package metadata;
it does not generate native meaning. Its expected wire bytes use Node 24.18.1's
independent built-in `URLSearchParams`. Between 2026-09-26T02:05:08.070Z and
02:05:22.755Z it completed 54 public commands, including fresh plan/apply/check of
124 library and 125 consumer tests, exact offline transport and bundle building.
After removing both authoring graphs, proposals and transport, all 642 detached
encoding/limit cases matched that oracle with successful joined cleanup. All product
subprocesses had an empty PATH. Node is solely a one-off external test oracle;
neither the shipped ordinary library nor maintained Rust tests depend on Node.
This is compatible-runtime proof, not fresh anonymous acquisition or every-version
compatibility. It establishes no speedup or hardware-independent timing guarantee.

Original stdout/stderr and command metadata remain under
`.artifacts/202609261045/public-oracle-WKRpFr/logs/`. Its final `result.json` has SHA-256
`e10598607b750067152d1954d41ee5efd50074d44add1fb581473bdda5e553b8` and records exact
source hashes, package identity, transport and artifact. The owned driver itself
has SHA-256 `a5b43229dbc52b429d426c3b183d6a9279cbb4c413d0ee5abe96c73b0ce049b7`.
The guide's sample is independently confirmed as the 32 bytes
`tag=%E7%8C%AB+%2B+Rust&tag=a%26b`. The current public binary can use this extension
without waiting for a runtime release, but existing exact package imports require
an explicit reviewed update.

## Publication boundary and integration preflight

The original deployment work and acceptance record were normally delivered as
`e2c0d1f58ed0b347c7956d2af248b944ccf422b9`; an independent GitHub branch read confirmed
that exact main. A first local commit attempt lacked author identity and made no
commit; the successful retry used the existing repository author identity only for
that invocation, without modifying global or repository configuration.

With the v0.1.48 tag absent and no active prior Release run, candidate
36210073260/1 was dispatched once at exact product/controller e2c0d1f5, at
2026-09-26T01:56:21Z. The later native-library extension is not in those frozen inputs.
At the feature-integration preflight, main still equals e2c0d1f5 with no divergence,
no protection and no active branch rules. Candidate job 108314571815 is still in
step 8, source-specific acceptance with the immutable verifier. No tag or scoped
publication selection was changed and no v0.1.48 release is claimed.

The native-library increment has complete applicable focused and published-runtime
proof. It can be delivered without restarting or falsely inheriting the frozen
candidate. After that candidate's terminal success, follow `docs/release.md`, retain
its finalized archive and acceptance, and run the existing seven snapshot public
CLI workflows against its exact extracted executable before selecting unchanged
assets for promotion. Preserve original source/run/attempt and publication identity.
Until then public v0.1.47 remains complete. Unrelated untracked
`docs/campaigns/202609242022.md`, the corrective worktree and original failed evidence
remain untouched. No production listener, host configuration, credentials or
application data outside owned disposable fixtures were changed.

## Completed mainline delivery and retained handoff

The native form implementation, affected tests and guides were normally pushed as
`c99800d47bd91090ffb7facc907b0f94e009ec52`, tree
`062c07fee385d80066a114e06addc652f22bd445`. An independent GitHub read confirmed
remote main at that exact commit. Its four implementation/test byte hashes match
the focused and public-runtime observations above. This final appended section is
reporting only; it does not assign a later documentation SHA to those executions.

The completed Verify source/run/attempt and the unchanged remote tip were reread
before normal deletion of `verify/202609260831-deployment-snapshots`.
Its exact source e5847f1e is an ancestor of main, original acceptance artifacts are
retained, and no unique changes were discarded. The separate detached corrective
worktree remains preserved rather than risking its original local evidence.

For the pending finalized-candidate check, an immutable copy of Cargo's actually
reported public CLI test executable is retained at
`.artifacts/202609261045/candidate-public-cli`, SHA-256
`a5873658d323ba6cc1a2414c8735e0816f80ad85b3f60e1a286e0d270be0249c`.
Its listing confirms the seven `native_declarations::deployment_build::` tests.
After authenticating and admitting the selected finalized archive, set the existing
`LKJSCRIPT_RELEASE_CANDIDATE` selector to that exact extracted product and invoke
this test filter with `--nocapture --test-threads=2`; retain its output separately.
The candidate-specific execution is not performed yet and is not a pass claim.

At the closing independent GitHub observation, Release 36210073260/1 remains
in progress at exact source e2c0d1f5. Candidate job 108314571815 still executes
source acceptance, step 8; product finalization, archive acceptance and uploads are
pending. No duplicate producer, promotion, tag or selection write was started.
All local focused checks and the public-v0.1.47 oracle are finished, with no remaining
owned test/server process. The only visible untracked repository file remains the
unrelated `docs/campaigns/202609242022.md`.
