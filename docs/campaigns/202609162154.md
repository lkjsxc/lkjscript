# Close the numerical-library milestone without resetting its verified candidate

- Repository: https://github.com/lkjsxc/lkjscript
- Invocation/name clock: **2026-09-16T21:54:00+09:00, Asia/Tokyo**, obtained from the live UTC+09:00 clock. The execution environment independently read **2026-09-16T12:54:22Z**.
- Intended initial archive: **`docs/campaigns/202609162154.md`**.
- Companion: the supplied complete replacement candidate for root **`AGENTS.md`**.
- Kind: **continuation of existing mainline/public delivery**, not another numerical implementation, fixture-repair or timeout campaign.
- Selected outcome: admit the completed corrective rehearsal's actual originals, integrate its existing correction and reports into remote main, and close the already-due immutable **v0.1.38** public milestone at the existing exact candidate.

Execute this mandate as the responsible Codex integrator. The user explicitly authorizes normal non-forced pushes to main and ordinary configured CI/release use within actual permissions, repository protections and higher-priority instructions. Accepted work must reach remote main; a local commit, topic push or open PR is not completion. Reuse the existing continuation rather than abandoning it for another branch.

This delegation does not authorize rewriting published history, force-pushing, protection bypasses, credentials/access-control changes, purchases/billing changes, production deployment or destruction of operational data. The existing release-scoped non-secret selector operation described below is authorized solely for the selected repository publication. Preserve unrelated work and secrets.

## 1. Starting state and reconciliation

### Observed sources and lineage

These are sequential read-only observations, not reset targets or an atomic snapshot. Source reads below are pinned to T unless a main/reporting context is explicitly identified. Refresh live state at startup and before consequential writes.

Final live-state reconciliation recorded at **2026-09-16T22:08:13+09:00, Asia/Tokyo**: the branch refresh still returned the M/R/H tips below; the newest-run refresh still returned completed successful `35083514716/1`; the latest-release endpoint still selected v0.1.36. Those refreshes were sequential and preceded this recording time. The invocation/name clock above remains unchanged.

| Role | Observed identity and meaning |
| --- | --- |
| M: default branch main | `14de12ea708530c4c2c0bfe7e0cb0547a56a5efe`, tree `71cd4326e807a48fd31446cc6b3ec949e1ca5c6f`. The numerical/requirement/structural-authoring/transaction-completion implementation is already integrated. |
| S: previously admitted numerical source | `7dce6b73769346f792c7f2c71d77d41dc8695dae`, tree `41104b16607c220e263768be3cf29bfe00bbab8d`. Already reachable through main; do not reimplement or reintegrate its features. |
| T: selected corrective/release source | **`7083f9a6d56ed702017942e100c3696fc6f35308`**, tree **`e12b89965176dcb166f69c9e32441f71588b7b9f`**. Direct child of M. The correction preserves file permissions as well as bytes in the target fault helper and its existing regression. Root product version is **0.1.38**. |
| R: mutable continuation/report tip | `6db51c912dd2f845850817e3969bc4d1357cc121` on **`codex/full-lifecycle-report-202609161630`**. Descends from T and retains the predecessor's later reconciliation. Its report is not a new tested candidate. |
| H: retained old recovery source | `8baafff7f67b55481cda8d67f8945b78af80be4d` on `codex/public-evidence-recovery-202609151412`. Exact ancestor of M, also retained by the historical v0.1.37 tag. No tracked integration debt remains at that observed tip. Local ignored evidence/worktree ownership still needs inspection before cleanup. |

The live M...R comparison has merge base M, two commits ahead and none behind. The H...M comparison has merge base H and none behind. This is ancestry, not a branch-name inference or merely patch equivalence. The remaining T implementation is already written; its later report must not be confused with its source-bound proof. [1][2]

The API listed only these three branches, no open PRs and no repository rulesets; main was marked unprotected. Repository metadata exposed push/admin permission, which does not guarantee the next session's credential scope. The inspected release workflow triggers on `v*` tag pushes or explicit dispatch, not a main push. Inspect actual consequential automation/protections at startup; do not invent a production-deployment approval requirement for this normal main integration. [3][4]

The root guidance blob is unchanged between M and R (`7e5e53db0316728f01ece2a42dfcf16c1fb82fbd`). The supplied replacement is a reconsidered standing guide, not a frozen-source modification.

### The pending report is stale; the actual corrective rehearsal is complete

**Run 35083514716, attempt 1**, selects exact T. It is a read-only rehearsal, not a publisher. The live run and job APIs report:

| Boundary | Live observation |
| --- | --- |
| Build, verify, and package | Job **104752845576**, success, completed **2026-09-16T11:27:51Z**. Its fresh-full, exact-static-target, strict-prepare and both handoff-upload steps succeeded. |
| Transferred packaged application candidate | Job **104775233342**, success, completed **2026-09-16T12:15:18Z**. The complete installed application invocation and its evidence upload succeeded. |
| Whole rehearsal | **Success**, updated **2026-09-16T12:15:19Z**. |
| Immutable publisher / anonymous public job | **Skipped**, not successful public delivery. |

Five artifacts are available; their observed metadata is in section 5. Do not start another rehearsal because R's last narrative observation still says T is running. Do not turn this completed attempt into another waiting campaign. [5][6]

**Evidence boundary:** this architect inspected source, Git ancestry, API metadata and reported predecessor results. No archive was downloaded or independently hashed, no original receipt reader or Rust/product test was run, and no repository write, integration, cleanup, workflow dispatch or publication was performed. A disposable clone attempt failed because the container could not resolve github.com; revision-bound connector reads remained available. Private stashes, ignored originals, reconstructed reader contexts and the user's checkout were unavailable. A green job is therefore a live service observation here, not fresh architect-executed acceptance or completed local original-reader admission.

### Inherited obligations

Read `docs/campaigns/202609161809.md`, especially its **Corrective source, complete regression and serialized rehearsal** and **Original installed admission and finite resumption** additions at R. It reports:

- The numerical implementation reached remote main through `a5b2d62bcbecc2975b71b4cb450747a3b0c968d2`; M records that integration. The integration/public-readiness separation is already installed in `docs/release.md` and `docs/spec/verification.md`. Do not implement that policy separation again.
- T's correction passed its reproduced regression, contributor tests, formatting and affected strict lint. A T-built harness passed the complete target matrix against **S originals and S's verifier**. That proves the correction against those originals, not T's final-source matrix.
- S rehearsal `35070583339/1` and its matching full/target/preparation/installed original readers passed. S's twelve local interruption cases passed, but its fresh healthy recovery was intentionally not run after T superseded that proof source. Neither S's results nor the T/S mixed-context regression close T's own final fault/recovery obligations.
- Failed histories and genuine originals are retained under ignored `.artifacts/numerical-delivery-202609161809/`. The unrelated stash `87c2265f6faf0e815d80450ec2480d46c5e774fb` and a recovery worktree at H were expressly preserved. Verify actual local state; do not assume those paths/objects exist in a fresh session. [7]

The latest public release remains immutable **v0.1.36**, release **388927116**. Its failed original-public-reader closure is preserved; v0.1.35 is reported as the last fully closed milestone. The v0.1.37 tag object **`51ecaabf3a6dfbaef0f842e95c4fc480dced0b6d`** selects H; its publisher `34965341260/1` failed and produced no release. Its separate recovery was superseded by this combined successor. The exact v0.1.38 tag lookup returned 404 and the release list showed no successor at observation. Refresh occupancy; absence is not a reservation. Preserve all historical tags/assets/failures. [7][8]

## 2. Decision, result and finite finish line

**Architectural thesis:** useful language power is not delivered while its accepted mechanism remains behind an old public executable and its completed verification is repeatedly treated as a reason to select another source. Preserve exact-source proof, but consume the successful existing attempt, finish the remaining discriminating checks and publish through the maintained owner.

**Before:** main contains the general numerical/library/transaction capability, but not the final test-fixture correction; the correction and report remain on an otherwise integrable branch; anonymous users receive an older release. T has a successful complete rehearsal whose original-evidence admission and final-source fault/recovery closure are not yet established by the available record.

**After:** remote main contains T, its reconciled reports and current guidance; immutable v0.1.38 at T has its required source/target/installed/fault proof, anonymous exact/latest installed behavior and original-public-reader closure. Temporary campaign references are removed when safe, or retained with a specific remaining dependency. No new language feature, format generation or permanent verification subsystem is added.

The strongest materially different alternative is to measure and reduce recurring preparation/authoring cost, or introduce a more suitable numerical data representation. Those can materially expand useful programs. They are not selected now: the current candidate has already completed expensive hosted acceptance, the single release deferral has been consumed, and no bottleneck experiment here justifies invalidating those inputs. A completed attempt awaiting admission/action is not an algorithmic performance defect. Do not add a blanket timeout increase, parallel acceptance framework or another round of feature-driven deferral. [9]

The ordinary useful workload being delivered is a transported statistics library: an independently authored consumer owns its nominal sample type and supplies a bound projection into an imported generic accumulator, edits calibration through review and resumes from typed checkpoint bytes in a fresh process. This is reusable library composition, not a compiler-owned statistics demo or a claim of third-party adoption.

**Finite completion:** close the acceptance rows in section 7, verify remote main and public state, reconcile temporary refs and return. If startup proves that the same promised outcome, required proof and selected delivery are already complete, report satisfaction/supersession with identities and finish only actual remaining cleanup/reconciliation. Do not silently choose a different campaign.

A concrete product/compatibility defect, materially different accepted concurrent work, an actual protection or inaccessible required original can alter the execution route. Diagnose its owner and apply section 6's bounded rules. A stale pending report, lack of a convenient local fixture, unchanged healthy work or a more attractive feature does not overturn the selected outcome.

After closure, use an ordinary AI-authored numerical/data-processing workload to separate editing/admission/preparation cost, steady-state execution, allocation and persistent growth. If repeated preparation dominates, investigate its owner; if representation prevents useful algorithms, compare an appropriate successor layout against the same program. Further transaction composition is justified only by a concrete caller. This is a revisable outlook, not an authorized queue or an additional completion gate.

## 3. Resolved contract and ownership

### Preserve the already implemented product

There is no new semantic subset to design in this continuation. Preserve F64 composition, explicit conversions, unchanged integer behavior, normalized representation versus numerical equality, signed zero, full-domain typed binary values and finite external JSON. Preserve generic/imported/nominal/callback composition, graph-owned algorithms and current authentic predecessor admission. Do not regenerate historical inputs as modern bytes to make them pass.

Keep **Graph 17, compiler 13 / bytecode 9 and Artifact 21** and their affected built-in/standard/`lkjournal` assets coherent. Integration, reports and this test-helper correction require no further compatibility generation. Installation and runtime selection are not application-data migration. Release notes must describe the existing cutover, supported versus unsupported predecessor inputs, retaining matching predecessor executables/projects and viable backup/restore recovery; do not promise automatic downgrade or universal data compatibility.

Preserve accepted `transaction-outcome`: `Committed(T)` appears only after actual completion, while abort does not expose the uncommitted candidate. Independent callback effects can survive subsequent failure. The numerical granted-data witness must still observe its committed value after later nonfinite JSON output fails. There is no new retry policy or general rollback guarantee.

The public workflow remains product discovery, reviewed change plan/apply, check/build, offline library transport, copied/installed execution and recovery outside the language checkout. Retain literal inputs, not just resulting hashes. The existing numerical witness covers imported consumer-owned sample types, bound projection, public calibration edit, merging and restart from typed bytes, with independent expected results. Preserve its required scale and failure cases rather than substituting a smaller example. Observer drivers can stimulate and inspect behavior, not implement the graph library's computation.

Public distribution remains the single maintained Linux x86-64 static-musl target. A shared installed runtime is not a mandatory daemon and does not turn application artifacts into self-contained executables. No deployment, operational-data migration or maintained exploratory application is selected.

### Relevant owners

| Responsibility | Maintained owner |
| --- | --- |
| Existing restoration correction and target falsification | `tools/lkjscript-dev/src/release/admission/nominal_tests.rs` |
| Source profiles, changed-path selection and evidence | `tools/lkjscript-dev/src/check/`, `docs/spec/verification.md` |
| Original target/package/verifier admission | `tools/lkjscript-dev/src/release/`, `docs/release.md` |
| Installed pair, interruptions and receipt matrices | `tools/lkjscript-dev/src/release/transferred/pair.rs`, its `pair/` modules, `tools/lkjscript-dev/src/release/transferred/fault_tests.rs` |
| Numerical public workflow | `tools/lkjscript-dev/src/offline_packages/f64.rs` and sibling `f64.producer.lkjc`, `f64.consumer.lkjc`, `f64.data.lkjc`, `f64.edit.lkjc` |
| Numerical semantics and compatibility | `src/platform/binary64.rs`, kernel/compiler/normalized execution, `docs/spec/language.md`, `docs/spec/semantic-storage.md`, `docs/spec/data-capabilities.md` |
| Publication and current summaries | `.github/workflows/release.yml`, `docs/release.md`, `README.md`, `docs/status.md`, `docs/roadmap.md` |

The inspected correction records regular-file permissions with bytes and restores both after omission/recreation and assertion unwinding. Its regression distinguishes mode 0600 from an intentionally recreated 0644 file independently of umask. Do not rebuild this already present fix, loosen the production inventory reader, or treat rejection for incidental mode damage as proof that the intended numerical forgery was rejected. [10]

Keep the already selected 3,600-second complete offline-process allowance and 120-minute application-job limits. Terminal process success, reader acceptance and cleanup are separate conjunctive obligations. A provisional success-looking receipt followed by timeout is failure; zero exit with rejected evidence is failure. An allowance correction is not a speedup. Do not replay any child that successfully started merely because its result was inconvenient.

### One bounded operator-policy clarification

`docs/release.md` currently says to reconcile the selector's previous value to a **completed release**. The actual predecessor selector reportedly names v0.1.37, whose owning publisher failed without producing a release. Resolve this wording explicitly: the previous binding must be reconciled to its **terminal owning attempt and actual publication state**, with no queued/running/waiting/foreign owner, not necessarily a successful release. A terminal failed attempt whose successor was explicitly selected is a legitimate previous owner.

Clarify that sentence in the mutable maintained procedure, retaining prior-value reread, unchanged-tip/ownership checks, readback, non-atomicity warning and immutable historical identities. Do not waive an active owner, infer terminality from a branch name, change the immutable-release setting or weaken the workflow's exact new tag-object check. This clarification changes no product/verifier/workflow byte at T and must not trigger a new frozen candidate solely for wording. The workflow checks the new exact annotated tag object; it does not require that the old selector produced a successful release. [11][12]

## 4. Execution order and remaining work

### A. Reconcile first; preserve the available originals

Inspect actual checkout/index/worktree, relevant untracked/ignored material, stashes, worktrees, remotes/divergence, narrower guidance, pinned tools/resources and active local/remote jobs before editing. Read the predecessor's ending rather than its original pending-state introduction. Do not reset or rewind to M, S or T. Preserve unrelated changes and redact secrets from observations.

The first release-state reads are:

```sh
gh run view 35083514716 --repo lkjsxc/lkjscript --json headSha,status,conclusion,jobs
gh api repos/lkjsxc/lkjscript/actions/runs/35083514716 \
  --jq '{id,run_attempt,head_sha,status,conclusion}'
gh api 'repos/lkjsxc/lkjscript/actions/runs/35083514716/artifacts?per_page=100'
```

Inspect matching newer attempts, tag/release occupancy and actual controls before dispatching anything. The existing T rehearsal is completed successfully at observation: **the next action is original acquisition/admission, not another workflow dispatch**.

Acquire the five original ZIPs using their authenticated artifact identities. Match actual ZIP length and SHA-256 to metadata before extraction; preserve metadata/run/job logs and unmodified ZIPs in ignored owned storage. Prioritize the one-day release/verifier handoffs. The existing evidence owner `.artifacts/numerical-delivery-202609161809/` can hold a clearly source/run-labeled T subdirectory; do not create another permanent evidence ledger. The predecessor's ignored notes may help, but the command/source owners and authenticated inputs must suffice without them.

Reconcile/install the supplied `AGENTS.md` in the mutable lineage before substantive implementation. Archive this initial mandate unchanged at its intended path; on a real collision preserve both originals using an observed-seconds or documented suffix, not an invented future time. Append concise additions only below the initial-text boundary. These mutable instructions must not modify T's frozen contents.

### B. Admit matching original source/target/preparation evidence

Authenticate the actual verifier handoff and independently supplied verifier length/digest from the producing job before executing it. Use the exact T verifier and package, not a newly built mutable-main binary or a self-asserted hash inside an unauthenticated identity file.

Reconstruct the required original reader context in owned isolated storage/namespaces using recorded paths, file modes, selections and environment. Never rewrite receipt JSON, manufacture absent requests/plans, alter sole original ZIPs or represent reconstructed historical environment values as new live host observations. Keep original evidence distinct from writable extraction/scratch needed by the reader. Do not modify the host's global environment, filesystem or kernel identity to mimic a runner.

Admit matching verifier/package/target/preparation through their maintained readers. Inspect the actual full receipt and its dependency-complete preparation binding: exact T, fresh required gates, stable relevant inputs, no required failure/skips/reuse substitution, correct source/target linkage and maintained consumers/generated assets. Retain original output. A strict archive check alone does not establish a new direct full-reader invocation; report whether full was admitted through its genuine preparation context or directly through its maintained reader.

Read the original workspace proof for the affected helper regression and existing correctness gates. The earlier local T/S matrix remains focused corroboration, not T's final matrix. Cache-write failure after a genuinely fresh passing gate is not a reused pass or automatically a product failure.

The completed hosted T installed pair also needs original `pair-verify` admission; perform it independently or after C. It is a public-closure prerequisite, not an additional main gate unless it reveals a product or compatibility defect. This reads evidence without replaying applications and is separate from the one fresh local recovery required below. All required route request/plan originals must exist. Source/job metadata success does not replace this step.

### C. Deliver the existing correction to remote main promptly

At the observed graph, reuse R and preserve exact T ancestry. Do not cherry-pick a reimplementation of its patch from M or create a new campaign/report branch. Incorporate the supplied guidance, archive, the bounded procedure clarification and concise actual status reconciliation on the mutable line. Keep the old initial mandates and failed history unchanged. Point the current status/roadmap to the active continuation rather than appending another chronology.

Main readiness remains the already adopted contract: exact-source fresh full, complete exact static target including all six maintained owners and both pinned userlands, consumer/generated consistency and affected verifier correctness, with authenticated matching source/target/preparation admission and actual repository protections. Once B's source/target/preparation admission establishes this, integrate T and the reviewed mutable changes without waiting solely for later local installed/public-release gates. A discovered product or compatibility defect still blocks acceptance.

Use the local pinned contributor executable for local checkout checks; a hosted verifier may embed a different repository root. The inspected toolchain is Rust **1.98.0** with clippy/rustfmt and the musl target. Inspect `rust-toolchain.toml` rather than changing the toolchain to match a convenience environment. [13][14]

Relevant verified entry points are:

```sh
cargo build --release --locked -p lkjscript-dev
cargo run --release --locked -p lkjscript-dev -- check changed --machine
target/release/lkjscript-dev release target
```

Run the changed-path check while the actual mutable documentation/guidance changes are represented in Git status; inspect its selected closure. A clean-tree changed check cannot prove the already committed correction. [15] Use T's admitted exact-source proof for T and separately validate the descendant's actual changes. Do not run another local full plus another hosted rehearsal merely to recertify a report.

Stage intended paths, review the staged diff, refresh remote main/protections, incorporate concurrent work without overwriting it and revalidate material interactions. Prefer ordinary fast-forward integration and a normal push to main. If a PR is genuinely required, complete that permitted integration path. Verify the fetched remote main revision contains exact T and the intended reconciled result; record the actual revision and relation to T. A reporting descendant is not retrospectively the tested source.

### D. Complete the still-required final-T fault/recovery contract

Reuse a retained exact-T test harness only after confirming its source/build/executable bindings. Otherwise build in an owned clean checkout of T, without changing the user's checkout or moving main:

```sh
cargo test --locked -p lkjscript-dev --no-run --message-format=json
```

Select the test executable from Cargo's emitted `compiler-artifact` records for the test profile, copy/hash it before other builds, and list its actual test names. Never guess a hashed executable path. Choose build concurrency from available memory and existing observations; concurrent feature-unified outputs must not replace an in-use verifier/harness.

The required sequence is finite:

1. Run the **entire** `live_nominal_receipt_omissions_reject_at_target_admission` test from the exact T harness against owned restored **T originals with T's original verifier**, using `LKJSCRIPT_NOMINAL_ADMISSION_FIXTURE`. Discover its complete qualified name from that harness and use `--ignored --exact`. Require healthy baseline and restored admission. In particular, the rehashed numerical-scale forgery must reach the intended independent `F64 scale:` rejection, not incidental damaged-mode admission. Compare required original bytes/modes afterward and preserve failures. The previous T-harness/S-originals pass is not this proof.
2. Prepare genuine T package/verifier inputs in the layout required by `docs/release.md`: separate `exact-assets` and `latest-assets`, `verifier`, and the actual preparation `release-receipt.json`. Run `release::transferred::pair::tests::live_pair_interruptions_preserve_failures_before_fresh_recovery` with `LKJSCRIPT_PAIR_REHEARSAL_ROOT`, `--ignored --exact`. Complete the existing twelve interruption cases, retaining their deliberate failures, accurate terminal/child diagnostics, unaffected sentinel/input observations and joined cleanup. S's earlier cases do not become T cases.
3. After these T interruptions, perform **one genuine fresh healthy simulated `release transferred pair-run`** into a new owned `pair` root with that exact T context. Require both real installed route lifecycles, the full five-owner suite once within the pair, summary acceptance, terminal success and cleanup. The earlier hosted pair is not a newly executed post-interruption recovery. Do not add another healthy run per fault case or another independent demonstration suite.
4. Against that same completed healthy pair, run `release::transferred::pair::tests::live_pair_receipt_fault_matrix` with `LKJSCRIPT_PAIR_FIXTURE_ROOT`; run `release::transferred::fault_tests::live_receipt_fault_matrix` with `LKJSCRIPT_TRANSFERRED_FIXTURE_ROOT` naming its `full-suite`. Use the actual exact names and explicitly include ignored tests. These falsify evidence and restore it; they are not fresh application executions. Require original-reader recovery and preserved bytes/modes/owned-state cleanup after each matrix.

Use `docs/release.md`'s **Anonymous public acceptance** owner for exact pair arguments and fixture layout; derive absolute create-new paths and verifier identity from the admitted inputs, not guessed filenames or JSON rewrites. Current matrix enumeration comes from the exact test source, not an arbitrary case-count target. No gate is waived by classifying the helper as contributor-only. [11]

Do not repeat S's abandoned healthy recovery: it was deliberately superseded by this final-T sequence. T's source/target/rehearsal results stay frozen while this work runs. Do not modify production readers, budgets or canonical expected results merely to make a fault invocation succeed.

## 5. Original artifact acquisition record

These are **API metadata observations**, not architect-verified download hashes. All belong to run **35083514716/1**, head SHA T, and were reported unexpired. Confirm actual metadata and bytes when acquiring them. Artifact API endpoints are `repos/lkjsxc/lkjscript/actions/artifacts/` followed by the actual ID; the returned `archive_download_url` identifies the ZIP. [6]

| Role / actual artifact name | ID | ZIP bytes | SHA-256 | Hosted expiry (UTC) |
| --- | ---: | ---: | --- | --- |
| `full-verification-evidence-35083514716-1` | 10442542279 | 50,759,464 | `1d806c6c0901ad5e605fac053ccd295856bee457dd10561d8d56286403bea470` | 2026-09-23T10:42:57Z |
| `target-admission-evidence-35083514716-1` | 10443787044 | 51,083,069 | `5e128afa9f5fdf8a3d119a0c8773aedf60d41012c1f37dd525c4eb55b52b97e0` | 2026-09-23T11:25:34Z |
| `release-handoff-35083514716-1` | 10444565020 | 11,080,566 | `28ab119920979d421e8d7950a632b4a635c24deb0512cf32dece1a0bfe240e9a` | 2026-09-17T11:27:47Z |
| `verifier-handoff-35083514716-1` | 10444032256 | 32,902,540 | `33d223a7d16bda38bf717fb2bcb8e48018890ff56059cf7f1ce5538885cbc559` | 2026-09-17T11:27:48Z |
| `pre-publication-application-evidence-35083514716-1` | 10445143066 | 162,948,123 | `cf718fe7e5a7bfa8be38f52b88ea00b5ed4d0f3aa9fb1cf5a7d7bbbe84e51365` | 2026-09-23T12:15:04Z |

The first expiring handoffs expire at **2026-09-17 20:27:47/48 Asia/Tokyo**. Preserve authenticated originals now instead of increasing permanent retention or changing T's workflow just to append this mandate. Subsequent publisher originals have their own actual IDs/digests/expiries; do not reuse this table for another run.

## 6. Publication, bounded adaptation and cleanup

### Publish the already selected milestone

**Disposition: complete due v0.1.38 publication at T; there is no further feature deferral.** Do not publish R or the new guidance commit merely because it is newer. All substantive shipped work and the correction are already at T. After original rehearsal admission, required final-T faults/recovery and verified main integration, use the maintained tag-push publication path. It intentionally performs its own fresh source/target/package/installed proof; rehearsal receipts are not promoted across runs.

Prepare/reconcile the useful release annotation in an owned file outside tracked inputs. The predecessor reports an ignored prepared annotation; verify its actual content and T/version binding before reuse. Explain the shipped numerical library, requirement/structural authoring and transaction-completion capability, compatibility/recovery boundaries and delivery correction. Do not state that still-pending anonymous acceptance has passed. The workflow uses the annotation as immutable release notes.

Immediately before publication, refresh remote main, exact v0.1.38 tag/release occupancy, all relevant publisher attempts, actual immutability and the existing selector's value/owner. Confirm T remains reachable from remote main. The prior selector is reported as the preserved v0.1.37 tag object; reread its real value and terminal owning attempt rather than blindly trusting that report.

Use only the established administrator read of enabled immutability and the scoped existing non-secret variable **`LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA`**. Create the exact annotated v0.1.38 tag at T in a clean owned source context, bind this variable only to that new tag-object SHA with prior-value reread and readback, then normally push that tag once. Never change immutability, credentials, access or other settings. Read/check/write is not atomic: reject a changed or still-owned binding. The historical failed v0.1.37 owner is handled by the explicit terminal-owner clarification in section 3, not by deleting its tag or pretending it published.

The existing tag push owns the publisher. Do not also dispatch a manual publisher, create a parallel release, overwrite an asset or cancel an unrelated run. The isolated configured write job performs immutable publication after its own gates; ordinary use of that existing service is authorized. Inspect its exact source/tag-object/run/attempt and required stages, not merely an upload or green subjob. [4][11][12]

Public closure requires independent anonymous exact-tag and latest acquisition of the archive/checksum/bootstrap triples, authenticated metadata/attestation admission, both fresh installed small lifecycles, the one-within-pair full five-owner suite, terminal cleanup and the matching original public `pair-verify`. The routes must retain the actual create/change requests and review plans, including all eight originals required by the current owner. Missing requests/plans are not reconstructible evidence. Reconcile original publisher full/target/package/installed evidence as well. Advance current-public README/status claims only when these boundaries actually pass.

### Bounded decisions for actual blockers

- **Matching work already exists:** adopt the exact healthy/completed owning attempt and its authentic inputs. If a matching tag is already pushed, do not recreate it or rewrite its annotation. If delivery is already closed, finish only real remaining integration/cleanup/reporting.
- **An original is unavailable:** first inspect retained authenticated downloads and the actual producing artifact, not just a convenience path. A relocated reconstruction error is not a product defect. If no recoverable authentic original exists, record the exact lost binding and perform only the maintained replacement proof boundary genuinely required. Do not manufacture evidence or rerun an unchanged deterministic failure. Keep old evidence failed/unavailable rather than restamping it.
- **A genuine failing product/verifier/test owner is found before tagging:** retain the failed attempt, reproduce/locate its cause and implement the smallest correct repair on the same mutable lineage. Review its actual invalidation cone; a real source-changing repair selects a new explicitly identified frozen source and the required refreshed proof. A still-unused v0.1.38 identity need not be bumped solely for a pre-tag repair. No documentation-only, smaller-example or weakened-reader substitute is acceptable.
- **Failure after tag publication:** preserve immutable identity. Only a genuinely transient, idempotent orchestration failure may use the maintained same-tag recovery dispatch after ownership reconciliation. Changed product/workflow/verifier/package inputs or a published content defect require the smallest unused additive patch and accurate fresh bindings, not an overwritten v0.1.38.
- **Actual integration permission/protection/conflict:** preserve useful work on the existing continuation, complete the allowed path when possible, and report the exact source/gate/resumption action when impossible. Do not create another report branch or invent a permission problem from the API's empty-status aggregate.

If only a healthy external job remains, do useful independent accepted work without invalidating its inputs, inspect status at meaningful work boundaries, then return the exact pending state when nothing actionable remains. Do not run watch/sleep/status loops or hide polling in scripts. Name the source, tag object, run/attempt, last observed job, missing gate, retained artifact location/expiry and next concrete action. Do not call pending publication complete or promise unattended monitoring.

### Retire temporary references without losing work

After normal integration, refresh the actual tips and determine whether any live job or required reference still depends on the named continuation. The rehearsal selects immutable event SHA T; its historical branch label alone is not proof of an ongoing named-ref dependency. The later publisher uses the annotated tag. Retire the campaign-owned continuation branch/worktree when the integrated result and required evidence are safely retained and no actual dependency remains. Verify remote absence after deletion.

H is already an exact ancestor of observed main and is durably identified by the preserved v0.1.37 tag. It does not need another merge or publisher. Inspect the recovery worktree's ignored originals and ownership; retain or safely relocate required evidence before removing a disposable worktree. Remove its obsolete campaign branch when safe, or state the concrete continuing reason for retention. Never delete unrelated work/stashes, erase failed history or destroy unique ignored material just to reduce branch count. Keep required frozen tags permanently unchanged.

## 7. Acceptance: implementation, main and public boundaries

Use existing evidence owners. This table describes required claims, not new test-count quotas or a new receipt system.

| Claim | Decisive proof and failure boundary |
| --- | --- |
| The correct existing candidate is being delivered | Exact T/tree/version, M-to-T-to-R ancestry and actual run/event/attempt identities. T is not S, R or a later reporting commit. Authenticate originals before use. |
| T is ready for main | Original exact-T fresh full and complete static-target/preparation admission, all maintained owners/userlands/consumer/generated checks, affected helper correctness and actual protections. Validate mutable descendant changes separately. A main API status alone or a clean-tree changed check is insufficient. |
| The final target reader discriminates real missing/forged evidence | Entire exact-T target matrix on T originals; required baselines/restored originals pass, faults reject for intended reasons, numerical forgery reaches its independent oracle, original bytes/modes survive. Mixed T/S proof cannot close this row. |
| Failure and recovery compose | Exact-T interruption cases preserve non-success, terminal diagnostics, unrelated sentinel/inputs and joined cleanup; one genuine subsequent healthy simulated pair succeeds. S's interruptions and an earlier hosted healthy pair cannot be relabeled as that recovery. |
| Evidence readers are trustworthy at the selected boundary | Exact pair and transferred matrices against the same recovered pair reject missing/forged observations and recover to admitted originals; all required literal route requests/plans remain. These are rereads/fault tests, not new live application claims. |
| The meaningful program is available through the ordinary product | Maintained copied/installed library workflow preserves imported generic/nominal/bound-projection composition, public edits, restart/checkpoint behavior and independently expected numerical/data outcomes outside the checkout. No hidden host library implementation or smaller substituted workload. |
| Remote-main ownership is complete | Fresh remote observation contains T and the reconciled work; source/evidence relation is accurate. Temporary lineage is retired or retained for an exact dependency. A topic push/PR is not completion. |
| Public delivery is closed | Exact immutable selected tag/source and three assets; successful owning publisher and anonymous exact/latest installed behavior; matching original source/target/package/installed/public reader admission, required authoring originals and terminal cleanup. Uploaded bytes, simulated acquisition or a skipped public job do not close this row. |

No new application-data migration is applicable: this campaign changes no application format and authorizes no operational data alteration. No additional performance acceptance is applicable: no speedup or token/billing claim is made. No new library API/semantic cutover is applicable beyond preserving the already implemented milestone. No new exploratory application is required.

## 8. Guidance changes and final return

The replacement `AGENTS.md` retains the current authority model, canonical-meaning guarantees, safe Rust/no-Python gates, ordinary budget-free trusted execution, distributed-product proof, mainline integration and truthful publication. It consolidates standing instructions, keeps transient IDs and matrices here, emphasizes reusing one mutable lineage and immutable executable evidence, and makes ignored-evidence preservation explicit before worktree cleanup. It does not add a permanent rule about this particular mode-restoration incident or force a new report branch.

Read current applicable guidance first, reconcile legitimate intervening changes and install the candidate in the mutable checkout. The current session still follows instructions already in force. The supplied root file neither changes T's frozen source nor overrides higher-priority instructions.

Preserve this initial mandate text unchanged in the archive. Append concise source-bound reconciliation/execution/resumption notes; link detailed existing evidence rather than copying growing histories into status/roadmap/release introductions. Keep report changes on the normal mutable lineage and deliver them to main with proportionate checks. No circular obligation requires a report commit to certify its own source hash.

Return in Japanese with:

1. What ordinary users can now do and whether that result is on main, public, or still pending.
2. Actual source/tree, remote-main revision, tag object, release/run/attempt identities and actions performed; distinguish candidate verification from descendant validation.
3. Fresh execution, valid original rereading/reuse, failed/cancelled/skipped/unavailable/unrun claims and compatibility impact.
4. Branch/worktree cleanup or exact retention reasons, preserved unrelated work and concrete blockers with the next resumption action.
5. The single most consequential finding for the next architect. Do not inflate an unconsumed successful attempt into another architectural project.

Do not claim a repository write, archive acquisition, reader execution, integration, cleanup or publication that did not occur.

## Revision-bound evidence and live observation sources

The live API links are retrieval locations, not immutable evidence snapshots; retain actual responses with observation times when executing. Source links pin the inspected revision.

[1]: https://github.com/lkjsxc/lkjscript/compare/14de12ea708530c4c2c0bfe7e0cb0547a56a5efe...6db51c912dd2f845850817e3969bc4d1357cc121
[2]: https://github.com/lkjsxc/lkjscript/compare/8baafff7f67b55481cda8d67f8945b78af80be4d...14de12ea708530c4c2c0bfe7e0cb0547a56a5efe
[3]: https://api.github.com/repos/lkjsxc/lkjscript/branches?per_page=100
[4]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/.github/workflows/release.yml
[5]: https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/35083514716/jobs?per_page=20
[6]: https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/35083514716/artifacts?per_page=20
[7]: https://github.com/lkjsxc/lkjscript/blob/6db51c912dd2f845850817e3969bc4d1357cc121/docs/campaigns/202609161809.md
[8]: https://github.com/lkjsxc/lkjscript/releases/tag/v0.1.36
[9]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/docs/roadmap.md
[10]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/tools/lkjscript-dev/src/release/admission/nominal_tests.rs
[11]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/docs/release.md
[12]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/.github/workflows/release.yml#L755-L780
[13]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/Cargo.toml
[14]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/rust-toolchain.toml
[15]: https://github.com/lkjsxc/lkjscript/blob/7083f9a6d56ed702017942e100c3696fc6f35308/tools/lkjscript-dev/src/check/mod.rs

---

**End of the initial mandate.** Preserve everything above unchanged when archiving. Append clearly labeled execution/reconciliation/resumption records below this boundary only after actual work or observation.


## Original acquisition and main admission — 2026-09-16 14:02 UTC

Startup reused R `6db51c912dd2f845850817e3969bc4d1357cc121` on the named continuation;
remote main remains M and exact T is its child. The supplied replacement AGENTS.md and this unchanged
initial mandate were the only initial worktree changes. Existing root guidance and predecessor
endings were read; no conflicting intervening root guidance or narrower AGENTS.md was found.
The unrelated stash remains `87c2265f6faf0e815d80450ec2480d46c5e774fb`. Pinned Rust 1.98.0 is
installed; its bin directory is supplied only to local commands. No active release run, open PR,
main protection or ruleset was observed. Latest remains immutable v0.1.36; v0.1.38 is unused.

All five ZIPs for `35083514716/1` were downloaded through authenticated artifact IDs. Every actual
length and SHA-256 equals section 5 and fresh API metadata. Unmodified ZIPs, metadata and both job
logs are retained in ignored `.artifacts/numerical-delivery-202609161809/T-35083514716-1/originals/`.
This preserves the two one-day handoffs before expiry. The producing build log independently binds
the actual verifier to 32,901,768 bytes / `5cf309a209ef4409f21a79c1049b17f140e2d6418f2a6213512774370388239e`;
its bytes were hashed before execution. Matching verifier and strict package readers exit 0.
The archive is 11,071,815 bytes / `bb3e843c0cd8d0c27dd1bfe429dc1a396971f75e079006de9df470d5a72e740b`.

Original full contract 5 binds exact T, all 26 selected gates freshly passed, zero reuse, no unrun
gates and stable relevant inputs. Full receipt is 61,290 bytes /
`02368e94c302f50bcdcc88dfa440f360f9aa85a639f3f1c11bab6487463a8835`; target receipt is 73,159 bytes /
`661bcaf01a5d503709a8282b225442c301560bd1066452e0ddcd16b088d66d9c`. Genuine successful preparation
binds both. The private full reader ran in that original preparation context; this session does not
claim a new direct full-reader invocation. Workspace originals include the helper regression,
119 passing contributor tests and all required consumer/generated/lint gates.

Matching target admission exits 0 in an owned reconstructed namespace, admitting all six owners
and both pinned userlands. All 3,456 original files compare byte-for-byte. An initial setup copy
used an incorrect flattened extraction path and started no reader; the first actual target reader
then rejected a manually placed maintained asset at mode 0640 instead of recorded 0644. Correcting
only that reconstructed asset's mode yields admitted originals. Failed setup/reader observations
remain; receipt JSON, production readers and sole originals are unchanged. This is original
rereading, with no application replay. Source/target/preparation details are retained in
`source-target-preparation-admission.json` and `target-reader/` under that ignored owner.

The matching hosted installed reader also exits 0 (14:00:23–14:00:25Z). It reproduces the original
summary byte-for-byte, including both routes, five terminal-success owners and all eight actual
request/plan originals. All 3,316 original files / 174 JSON, 62 recorded modes and two restored
selection symlinks survive; namespaces are joined, scratch absent and the host kernel unchanged.
Recorded hosted context was supplied only inside the reader namespace. This is simulated hosted
original admission, not anonymous publication or the later fresh post-interruption recovery.
`installed-reader/original-reader-summary.json` retains the independent details.

T's exact retained harness was independently rehashed and its actual test inventory listed.
The complete final-T target matrix is now running against T originals and T's original verifier.
The later interruption/recovery and public gates are not yet claimed. Main admission is established
without substituting S proof or waiting solely for public closure. The mutable procedure now
reconciles the prior selector to its terminal owning attempt and actual publication state, retaining
ownership checks, prior-value reread, readback, non-atomicity warning and immutable identities.
