# Complete native public-guide ownership

## Original mandate (unchanged)

https://github.com/lkjsxc/lkjscript@Remote Desktop Commander  lkjsxc/lkjscriptについて進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。限界まで深く考えてほしい。超長期的な視点からお願いしたい。ChatGPTの開発環境として、cw-bd235945fff44468a0f41292を用意しました。自由に活用ください。

## Reconciliation and selected responsibility

Observed clean local main `59437dfa`, three commits ahead of remote main
`9db66daa`. The accepted predecessor implementation is `7b7a54ce`; its original
20-gate source acceptance and public-v0.1.44 reproduction remain historical proof,
not fresh acceptance of this successor. No other worktrees, stashes or build jobs.
Pinned Rust 1.98.0 is installed under the existing Cargo home. The authorized
workspace has 8 GiB memory and sufficient disk. Root guidance needs no change.

Complete the maintained native guide tool rather than retaining two formatting
implementations. Native meaning will own all eight pages' prose, layout and
escaping. Rust retains read-only product metadata, ordinary pure execution and
file publication, not a second page renderer. Preserve current semantic facts,
record format changes explicitly, and test independently against literal expected
content. Exercise fresh authoring, canonical re-entry and detached use through a
copied public binary. Do not introduce a new language intrinsic, external semantic
generator, document-specific compiler behavior or a compiler-self-hosting claim.

Normal Git dry-run currently fails without authentication. The existing Coder
GitHub provider also reports authentication unavailable. Preserve accepted local
lineage and pursue normal integration only through actually authorized access;
never use credential changes, a workflow write bypass or force-pushing.

## Implemented and focused evidence

The native extension adds 551 owners without changing an existing owner or the
exact standard dependency. Accepted native revision is
`rev_041777aca045e140294031405c4594101c10605b08dcfd26f980d5fea6d0bb09`.
Its ordinary build is 1,079,297 bytes, versus the previous 794,397 bytes. It owns
six additional commands, typed observation records, parent-aware exact-reference
selection and a checked Document result. Host metadata code reports all interface
owners rather than choosing page-specific references. The old six Rust renderers
and their prose/selection loops are deleted. The public operation/diagnostic pages
remain byte-identical; other pages intentionally use escaped HTML tables/code blocks.
No graph/artifact/data encoding, compiler intrinsic or standard dependency changes.

Fresh focused checks pass: 61 native differential graph tests, ten host/native
adapter tests, and both copied-product public lifecycle tests. The latter creates
the two literal proposals from a fresh minimal project, checks them, drafts and
edits accepted meaning, reproduces the embedded artifact and all eight generated
pages, and runs both old/new bundles after deleting project and authoring inputs.
Both diagnostic and newly native function-definition targets run detached with
an empty PATH. The original two-target proposal remains preserved unchanged.

Independent output decoding preserves four complete registry/interface payloads,
the grammar prose/examples, all 18 stateful and two relay exact references,
composition/closed-behavior/lifecycle records and all ten numeric defaults. Table
layout and block separators are deliberately not asserted byte-identical. The
first comparison exposed one extra blank separator, not lost prose; its failed
original and the corrected semantic comparison remain retained.

## Stable source acceptance

Implementation commit `ee2c60797ad5e9ab3a5d02472dcf97cd07e5121a` passes the
existing **full** profile: 26 selected, 26 freshly passed, zero reused, zero unrun.
The run took 744.882 seconds after building the verifier. The receipt records the
same worktree-input digest before and after execution and `input_stable=true`.
This includes all source gates plus the six existing behavioral owners, rather
than promoting a release-source-only run to full acceptance. Main library tests
report 780 passed and eight pre-existing ignored cases; no ignore policy changed.
Focused native/adapter/public tests are included evidence, not extra gates added
to inflate the total. The actual accepted candidate executable is retained outside
Cargo's mutable output paths. No source/test/native-asset edit follows this commit;
this campaign and current-status/cost updates are documentation-only descendants.

Receipt: `.artifacts/lkjscript-dev/check/1790183045759123488-46291-0/receipt.json`.
Session evidence: `.artifacts/202609240132-native-guides/`.

A second Git-only reproduction using the frozen public v0.1.44 executable checks
all 61 native/standard tests and rebuilds the 1,079,297-byte artifact identically.
Its untouched canonical draft is a no-op. Editing the newly native function-guide
heading produces a different accepted bundle; after moving the copied authoring
tree out of its original path, detached old/new runs preserve their respective
headings. All product invocations use an empty PATH. This independently establishes
that no new compiler privilege or host interpreter is required for the extension.

## Matched generation cost

Seven alternating fresh-process pairs, after the full verifier exited, generated
all eight pages into new directories with an empty PATH. Every output was checked
byte-for-byte against that revision's expected pages. The before binary exactly
matches the retained accepted `7b7a54ce` executable; the after binary is the accepted
`ee2c6079` candidate. Filesystem caches were not flushed. These are same-machine
wall times for complete generation, not a language-throughput or application-runtime
benchmark; they do not prove performance on other systems.

| Observation | Two native pages / six Rust pages | Eight native pages |
| --- | ---: | ---: |
| Median complete generation | 389.797 ms | 458.215 ms |
| Minimum / maximum | 372.432 / 414.261 ms | 444.290 / 507.724 ms |
| Host executable bytes | 25,715,360 | 25,969,632 |
| Native guide artifact bytes | 794,397 | 1,079,297 |

The measured cost is about 68 ms, or 18%, more for this explicit development
command, and 254,272 more executable bytes. There is no speedup claim. Peak memory
was not measured: an initial optional GNU-time probe found `/usr/bin/time` absent;
its failed original is retained and the actual comparison measures monotonic wall
time only. The one-time
literal-prose transfer and comparison helpers are disposable text/test operations,
not shipped semantic generators or dependencies of native authoring or execution.

## Delivery boundary and recovery

After acceptance, both the connected GitHub read and normal `git fetch` still
observe remote main at `9db66daaf91767d3c1504a6ce03d20cfa12777af`, an ancestor of
the accepted local history. An actual normal `git push origin HEAD:main` fails
with exit 128 because HTTPS username/credential acquisition is unavailable.
The underlying GitHub account is not proven unlinked: more precise diagnosis of
the Coder helper is `CODER_AGENT_URL must be set`, in both the current and normal
login shell. It cannot reach its agent context here. Normal SSH, with the official
GitHub Ed25519 host key verified and strict checking retained, separately fails
`Permission denied (publickey)`. No credential, permission or protection setting
was changed; connected GitHub reads do not establish shell Git authentication.

All predecessor commits remain on local main. The full-history recovery bundle
location is `/home/coder/workspace/lkjscript-native-guides-202609240132.bundle`;
bundle verification and clean-clone checks use the session evidence directory.
The source acceptance remains `ee2c6079`, independent of the documentation-only
reporting descendant. Local acceptance is not remote integration. Public v0.1.44
remains the latest frozen release and does not acquire these changes; no release,
remote branch or workflow dispatch was created to work around the blocked push.

Resume with a supported authenticated Git connection in this workspace, re-read
remote ancestry and integrate the preserved history normally. Do not substitute
a release-workflow write channel, force-push, or a new credential/access grant.
The original failed native request plans and unavailable measurement probe remain
in session evidence; none was relabeled successful or used to weaken acceptance.
