# Native typed HTML composition through the public product

## Original user mandate (unchanged)

> https://github.com/lkjsxc/lkjscript lkjsxc/lkjscriptについて進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。限界まで深く考えてほしい。超長期的な視点からお願いしたい。

## Reconciliation and selected work

Start from independently observed remote/local main `2db59cc3`. PR #3 is merged.
Preserve the untracked `202609242022.md` and all prior evidence. No active project
jobs, extra worktrees or stashes were observed. Existing root guidance is retained.
An additional combined terminal source-inspection request was blocked by the tool
safety check. Do not repeat it or acquire the same blocked inspection through an
alternative route. Runtime metering and host-process changes are not undertaken.

Select an independent, ordinary native library workload: typed HTML composition.
Keep inline and block nodes distinct, restrict element names to closed native
variants, and reuse the exact standard's existing HTML text escaping. Do not add
raw-markup, arbitrary-attribute, URL-policy, compiler or runtime behavior. Supply a
separately authored transported consumer and literal reproducible public inputs.

Use the anonymous immutable v0.1.44 download, matched to authenticated release
asset metadata. Author and test outside the compiler checkout. Prove exact output,
composition, type rejection, detached use and independently parsed HTML structure.
Keep launch/assertion tooling outside the product dependency path. Integrate only
accepted native inputs and documentation through normal mainline delivery. This
is a library/example increment, not a new binary release or compiler self-hosting.

## Execution record

The observations below supersede the initial pending state. Unsuccessful attempts
remain recorded separately from accepted native source.

### Implemented native slice and exact execution context

The [literal library](../guides/examples/html.lkjc) defines separate recursive
`inline` and `block` variants, fixed rendering cases, private balanced text joining,
public render functions and a complete document function. It reuses the existing
standard `html-escape-text`; it neither duplicates that implementation nor creates
an intrinsic. The [separate consumer](../guides/examples/html-consumer.lkjc) composes
its own nominal `entry` records with transported library blocks. The
[guide](../guides/native-html.md) supplies the public operations and limitations.

All product executions use the anonymous public v0.1.44 musl download, not a local
Rust build with the same version string. The 11,443,454-byte archive matched the
independently authenticated release asset `583363930` and its published digest.
This is exact public-asset acquisition, not a new installation/latest-selection or
release-verifier acceptance. No publishing credentials were passed to the detached
command processes. No compiler checkout, Python or other semantic generator was
used to author the programs. Disposable Node drivers substituted observed identity
placeholders, launched public operations and asserted results; they are not added
to the repository or required to use the library. The maintained literals retain
placeholders rather than embedding these particular project identities.

Scratch and all originals are retained at
`/home/coder/workspace/lkjscript-native-html-202609242152`.
The empty library template was first created in ignored scratch, then moved outside
the compiler checkout before any library meaning was authored or checked. The
consumer was created outside it. Their accepted source identities are:

- Library package `pkg_05530437007b9ce8388a11b29768a611`, semantic revision
  `rev_e3f555a4c0658e030866aae39bd6ee2792e68d250bd7a8059851f8e8ef261893`,
  package revision
  `package_revision_c192f06954284a2f1e02a58c8aa59a2a13ae995df86c1e0a257bc64df082147d`.
- Consumer package `pkg_01d069c8421b5047b9ba2f49bc903694`, semantic revision
  `rev_97e6b9198af523ba712fbb58cad241e3b28a71f273fee82829e03379c6ee2cf1`.
  Its built standalone artifact is 797,250 bytes.

### Completed evidence, without adding overlapping test totals

| Observation | Actual result |
| --- | --- |
| Fresh public library check | 61 passed, zero failed, production/reference equal; 16 library plus 45 exact-standard tests. |
| Fresh transported consumer check | 64 passed, zero failed, production/reference equal; three consumer plus the same 61 dependency tests. |
| Public project commands | A nonempty Japanese article and a heading containing markup text return the expected escaped output with evaluator agreement. |
| Invalid native paragraph and strong payloads | Both reject with `kernel_type_payload`. |
| Unrelated nominal inline lookalike | Rejects with `kernel_type_root`. |
| Unexposed script case and private wrapping helper | Both reject with `change_reference_not_exposed`. |
| State after rejected plans | Full public status equals its original bytes after each of the five rejected plans. |
| Detached production command | One empty-text tree containing all supported tag kinds succeeds after authoring roots are parked. Exact output and parse5 8.0.0 tag/attribute/text assertions complete before the driver fails on its later cleanup assertion. |

The 61 and 64 checks overlap; there are 19 newly authored tests, not 125 new tests.
The pure project commands run differentially. The detached command explicitly
reports production execution with verification not performed; it is not relabelled
a differential run. Its original cleanup record reports admission stopped, drained
before cancellation, zero remaining tasks and an empty cleanup-failures array.
An independent reread of the retained output confirms those actual fields; the
incorrect external assertion still makes the overall driver a failed run.

Library/consumer native checks cover all five escaped punctuation characters,
existing entity text, Japanese/astral text, NUL serialization, empty inputs, order,
inline/block nesting, title/language contexts and code-block leading newline.
This is not independent browser confirmation for those nonempty inputs. The only
completed independent parsed-DOM case uses empty leaf text and `lang="ja"`.

### Genuine failures and uncompleted work

The first project creation incorrectly requested a `library` template; public
capabilities instead provide `minimal`. One extra parenthesis in the last library
test was corrected before acceptance. The initial minimal-project request lacked
staged standard source and correctly rejected; exporting and staging the exact
standard closure preceded the accepted plan. A relative result-file parent with
`..` correctly rejected and the next invocation used an absolute destination.
Original failed logs remain retained, separate from successful native checks.

The first external driver completed the five negative native checks and its first
detached result/DOM assertions, then wrongly expected the literal field
`cleanup=joined`. The product actually emits an encoded structured cleanup record.
A subsequent driver edit contained an invalid JavaScript regular expression and
failed before running any test. A command to correct that harness was blocked by
the OpenAI tool safety check and was not retried through another command or tool.
The first failed driver snapshot, second source, both failure logs and all actual
result bytes remain in scratch. No successful full external summary exists.

The planned 88 detached render cases, six malformed runtime inputs and subsequent
article recovery were **not completed**. There is no breadth/depth performance,
nonempty adversarial DOM, HTTP integration or general browser-security claim.
The guide carries this boundary visibly rather than treating intended tests as
passes. Complete those independent checks before expanding this into a broader
web-framework promise. Fixed tags, private helpers and escaped text do not make
ordinary output `Text` an opaque trusted-HTML or hostile-code security boundary.

### Integration scope and outstanding boundaries

Only the two literal inputs, the new guide, its index entry and this campaign are
selected for main. The pre-existing untracked `202609242022.md` is untouched and
not staged. Existing root guidance, Rust, standard generated assets and release
configuration are unchanged. This example-only descendant has its own public
native-program evidence; no source-wide fresh full suite was run and prior
`6d2c6df1` hosted acceptance is not stamped with a new source identity.

The larger runtime-mandate implementation is not complete: resident optional
metering and the inherited-output host-process lifecycle defect remain unresolved.
No blocked inspection or harness correction was rerouted. No server was started,
no production state was changed and no release was dispatched. There is no new
binary-publication obligation for these inputs: the already immutable v0.1.44 can
execute them. The author's source projects remain under `parked/`; neither deletion
nor containment against deliberately rediscovered paths is claimed.

Before normal integration, independent GitHub reads still showed main `2db59cc3`,
protection disabled and no rulesets. Final integration must preserve any intervening
work and independently confirm the exact resulting main; a local commit alone is
not delivery. Preserve scratch as the resumption point for the unfinished external
matrix rather than inventing a successful verification receipt.
