# Complete the accepted requirement-library and structural-authoring release

- Mandate timestamp: **2026-09-15T14:12:40+09:00 — Asia/Tokyo (JST, UTC+09:00)**.
- Clock read: 2026-09-15 05:12:40 UTC; the filename uses that actual minute.
- Repository: https://github.com/lkjsxc/lkjscript
- Intended immutable initial archive: **docs/campaigns/202609151412.md**.
- Companion: complete replacement candidate for root **AGENTS.md**.
- Observed default branch: **main**, **e65e02a441adc96eb81fa876b4926d0c93554274**.
- Selected frozen product source: **eea968b910ac62c3feab95a8896fff1a91904a17**.
- Selected outcome: complete combined **v0.1.36** repository publication and anonymous installed-product acceptance.

Execute this mandate as Codex. Complete the remaining delivery of the accepted capability. This is
a finite continuation of docs/campaigns/202609150900.md, not a new parser, library, runtime or release
framework project. You have authority for ordinary repository integration and this release through
the existing configured services, subject to actual permissions and gates. Do not ask for routine
approval. Production deployment, operational-data destruction, new spending, credentials/permission
changes and rewriting published history are outside this mandate.

## 1. Starting point: accepted implementation, unfinished delivery

The architect inspected current GitHub source and live metadata on 2026-09-15, beginning at
05:07 UTC. Related source reads use main at e65e02a441adc96eb81fa876b4926d0c93554274. Git comparison
confirms main is exactly one reporting commit after the frozen source: only the structural campaign,
release procedure, roadmap and status documents changed. Main is not a replacement release candidate.

| Item | Established position and disposition |
|---|---|
| Requirement parameters | Implemented, accepted and integrated at 104654edf866d8f5531555b0aa0731fd496d72a8. Explicit T/E/R applications, minimum operation constraints, exact grants, transported typed-cell and queue libraries exist. Preserve their semantics and compatibility. |
| Structural expression authoring | Implemented in 4d39101f75d73c7f93abb0c84a29e3b7498efc8d, with diagnostic correction 1d0599fe8e8a95ec56e255e8f3bbab1468c2c4b1 and installation-lock correction eea968b910ac62c3feab95a8896fff1a91904a17. Frozen combined source is already normally integrated. Do not reimplement it. |
| Local acceptance | The predecessor reports final-source fresh full acceptance, exact static target acceptance, strict deterministic packaging, installed-pair proof and reader fault sensitivity. Its 108 target and 107 pair cases, failed attempts and restored originals remain at their existing owners. These are reported results, not tests freshly run by the architect. |
| Successful hosted rehearsal | **34918782215 / attempt 1**, exact event source eea968b910ac62c3feab95a8896fff1a91904a17, branch codex/structural-delivery-202609150900, publish=false, tag v0.1.36. Live state is **completed / success**, updated **2026-09-15T03:38:14Z**. Build job 104222098922 and pre-publication job 104235605973 succeeded. Publish job 104243316463 and anonymous-public job 104243316701 were correctly skipped. |
| Stale pending report | The predecessor ending observed that rehearsal still running at 02:58 UTC. The live successful result supersedes that pending observation. Append reconciliation; do not erase the historical observation. |
| Earlier attempts | Rehearsals 34915386161/1 and 34917057693/1 are cancelled. Preserve their actual source/context and reasons. Do not relabel them successful or restart them. |
| Already delivered predecessor | Immutable v0.1.35, release 388511109, source 4306ef64783462a8dda48f2a855a88482d1faec8; published 2026-09-14T15:34:40Z. Its publisher and public acceptance are closed in the retained records. It does not contain this combined increment. |
| Still open | Authenticate/admit original hosted rehearsal evidence, publish the combined increment, and complete publisher/anonymous-public evidence admission. Live release listing still ended at v0.1.35; the v0.1.36 tag lookup returned not found. Refresh before mutation. |
| Delivery commitment | The requirement campaign allowed one substantive campaign before publication. Structural authoring consumed it. **The deferral cannot be renewed.** |

The architect read source, metadata and reports. No product test, build, binary download,
original-receipt execution, integration or publication was performed in this investigation.
Current administrator access, local retained files, worktree state and intervening work remain
startup checks. A successful hosted status does not establish independent admission of its bytes.

### Startup and bounded branches

Before changes, inspect actual HEAD/branch, index/worktree, relevant untracked files, stashes,
remotes, divergence, applicable guidance, toolchain/resources and active local/remote jobs. Read
the relevant endings of 202609140938.md and 202609150900.md. Preserve unrelated changes and operational
state. Use an owned mutable development checkout for documents and an isolated clean checkout/clone
of the selected source for release operations that require it. Do not rewind main.

Archive this initial text unchanged, then append clearly separated concise execution/resumption
records. The intended filename was absent from the inspected tree. On an actual collision preserve
both originals with observed seconds or a documented collision suffix. Reconcile the supplied full
AGENTS.md with legitimate intervening guidance and install it in the mutable checkout before
substantive work; replacing it does not override already applicable session instructions. Never
change frozen release inputs to include this mandate, new guidance or reporting prose.

Resolve startup into one of these branches:

1. **No publication yet, matching rehearsal usable:** admit the originals and proceed below.
2. **Matching tag/publisher already exists:** identify its exact source, tag object, attempt and
   selector ownership; resume that lane. Do not push another tag or dispatch a parallel publisher.
3. **Combined delivery already complete:** verify exact publication and public-acceptance evidence,
   close real reconciliation/archive obligations and report this mandate satisfied. Do not invent
   another feature or release.
4. **Evidence inaccessible/expired:** first inspect retained authenticated copies. Expiration does
   not invalidate retained originals. If necessary originals are actually missing or fail admission,
   record the exact missing/failed boundary and run one justified replacement maintained rehearsal
   at the unchanged frozen source. A new attempt owns its own evidence; do not splice attempts.
5. **Actual candidate defect:** make the smallest correction required to deliver the same useful
   capability and refresh affected proof. Before tag push select a new clean source honestly; after
   tag push follow additive unused-patch recovery. Do not use a document-only update as a reason
   to replace the candidate.
6. **Actual permission/service blocker:** finish independent authorized work, retain exact evidence,
   name the blocked action and resumption. Never bypass protections, alter permissions or invent
   credentials. A speculative side effect is not a blocker.

## 2. Decision and finite outcome

**Thesis:** finish the proven language increment through the maintained distribution path, keeping
accepted product bytes separate from later development and reporting.

Before this campaign, the published product stops at v0.1.35 while main contains a composable
requirement-library mechanism and substantially easier expression authoring. After completion, a
user can install the new immutable Linux x86-64 static runtime, author and revise structural bodies,
and build/run transported libraries whose operations act through caller-supplied exact requirements.
The user needs the distributed product and its ordinary application inputs, not the language checkout.

This is the highest-value unfinished outcome because implementation, integration and a successful
rehearsal already exist, while the agreed delivery trigger has matured. A new source redesign would
not solve this remaining gap. The strongest evidence-backed alternative is observable transaction
completion: the current typed-cell witness demonstrates a true primary conditional result even when
the transaction publishes nothing. That deserves a future language decision, but is not necessary
to ship the accepted honest UpdateAttempt API.

A short revisable direction after closure is to compare an ordinary committed-update library's
needs against the remaining declaration/type/effect/requirement prelude and edit burden. Broader
numeric and data-processing work remains available; this investigation did not establish its full
current implementation status. These are future hypotheses, not authorized follow-on work here.

Reconsider this source only if evidence establishes a release-blocking defect or incompatible
intervening work. If a successor has already delivered the same capabilities and required proof,
reconcile that successor as satisfaction. Do not lower the outcome to a tag, a draft, rehearsal
success or a local binary.

**In scope:** authentic evidence retention/admission; exact-source annotated publication; existing
publisher/public acceptance; concise public notes and documentation; normal report/guidance integration.

**Out of scope:** new transaction semantics, automatic retry, numerical primitives, further authoring
syntax, new platforms, new applications, generalized CI, proof-framework redesign, storage/history
changes and production deployment. Implementation/migration design fields are otherwise inapplicable:
the feature implementation and its transition are already accepted. Only a demonstrated defect
activates the bounded correction branch.

## 3. Preserve the accepted product contract

Do not change these properties to obtain green delivery:

- The typed meaning graph remains the sole accepted editable program authority. Structural blocks
  are change-input notation lowered to existing authored intent. Flat inputs remain supported;
  descriptive projections are not editable source.
- Structural syntax covers the existing public expression forms, sequential/nested lexical
  shadowing, explicit T/E/R application and reviewed substantial body replacement. Preserve
  canonical/review parity for supported equivalent flat/block requests with matching authored
  order, Names and annotations; signature-owner continuity, strict scope, complete admission
  and located failures.
- Requirement parameters preserve exact interface/minimum-operation constraints, whole-requirement
  substitution, producer-owned capability calls/transactions, task/pure distinctions, exact grants
  and local affine provenance. A minimum set does not attenuate the whole supplied requirement.
- UpdateAttempt<T> reports candidate data and the primary conditional result. It does not certify
  durable transaction publication. Keep the true-primary/no-publication and trap rollback cases;
  no implicit retry or cross-effect rollback guarantee is introduced.
- Current implemented boundaries are compact-change 20, authored codec 15, Graph 15, TypeObject 10,
  compiler 11/bytecode 7 and Artifact 19. The existing authentic predecessor Graph 14 / compiler 10 /
  bytecode 6 / Artifact 18 compatibility and rejection rules stay with their owners. Publication
  creates no migration, operational-data deletion or graph-history rewrite.
- The product remains one static Linux x86-64 runtime with separate runtime-dependent application
  bundles. Sharing installation bytes introduces no daemon. Ordinary trusted execution does not
  acquire a mandatory instruction budget.
- The installation-lock correction is part of the frozen source. Keep native locking, immutable
  version slots, explicit selection, pinned executables and joined cleanup.

The existing public witnesses already discriminate composition: transported generic typed cells
for I64 and Text; effectful callbacks; a separate queue helper; direct and bound invocation;
supplier body/constraint replacement; insufficient-consumer rejection and repair. Literal structural
and flat requests and independent typed-data/queue expectations reside in the existing offline-package
owner. These are designed witnesses, not established user adoption. Their host observers stimulate
and inspect; lkjscript implements the algorithms.

The actual installed route in pair/lifecycle.rs performs structural creation returning **42** and
body replacement returning **43**, preserves signature owners, and also checks/builds/runs the
original template with expected **"hello"**. Mention both in mutable release documentation: its
existing "hello" description is correct but omits the added structural work. Do not alter frozen
source to fix that prose.

## 4. Execution, in dependency order

### A. Retain and admit the successful rehearsal

Start from https://github.com/lkjsxc/lkjscript/actions/runs/34918782215 . Confirm attempt 1, exact
source and successful required jobs. Do not dispatch another rehearsal merely because this mandate
is new. The original metadata advertised these artifacts, all unexpired when observed:

| Artifact name | ID | Original ZIP SHA-256 | Expiration UTC |
|---|---:|---|---|
| release-handoff-34918782215-1 | 10378323499 | dbfbf6ffcc3d0dccd15a28fdab601ead119e6bc5a64233fcd7eba9e8810eb316 | 2026-09-16T02:58:24Z |
| verifier-handoff-34918782215-1 | 10377899845 | 4ec5162de96a2c2ff324d6bac00ffce6edc23d32b3367c777b74910e6f9b1420 | 2026-09-16T02:58:25Z |
| full-verification-evidence-34918782215-1 | 10377349927 | cf33075fed7e6ba062b0649028f1e7163fb759e20507e52936c34c103a78507e | 2026-09-22T02:19:29Z |
| target-admission-evidence-34918782215-1 | 10378807891 | 9c4dc5ec774cfec9cad0d1a85faec4d6285df740fb297d9728a09486b6f154eb | 2026-09-22T02:56:17Z |
| pre-publication-application-evidence-34918782215-1 | 10380161760 | bf75c7d07af052f29f5c7c2d257dd540417e813a9f4b8c0b0460a7ee35d3d394 | 2026-09-22T03:37:58Z |

The first two expire **2026-09-16 around 11:58 JST**. Acquire promptly while available. Refresh
authenticated metadata and retain original ZIPs, artifact IDs/digests, logs, source, attempt and
reader context before interpretation. An API ZIP digest is not an executable or release-archive
digest. Do not require hosted bytes to equal local builds merely because source matches.

Use authenticated handoffs and independent externally obtained digest/length bindings to admit the
verifier before executing it. Preserve its exact source/version/role identity. Use the matching
original readers for strict package, target children and installed-pair evidence. Reconstruct
required original paths/modes/context only in owned disposable surroundings; do not edit receipts,
weaken readers, fabricate GitHub execution or overwrite another user's paths. Record archived-context
rereading explicitly.

Reconcile the original full receipt with its authenticated artifact and the original preparation
receipt's exact source, digest, length, fresh-gate classification and retained logs. The inspected
release dispatcher has **no exported full-receipt-only verify command**: its full inspection is
private to prepare. Do not invent such a command or rerun prepare and describe that as original
receipt rereading.

Source-verified reader interfaces below list required argument names. Supply actual retained paths
and values from the relevant original context; they are not commands with fabricated filenames:

| Existing reader | Required options |
|---|---|
| release verifier verify | --executable, --identity, --tag, --commit, --expected-sha256, --expected-bytes |
| release verify | --archive, --checksums, --receipt, --extract-to, --expected-tag, --expected-publication |
| release admission-verify | --candidate, --receipt, --commit |
| release transferred pair-verify | --exact-assets, --latest-assets, --tag, --commit, --publication, --acquisition, --evidence-root, --verifier-identity, --expected-verifier-sha256, --expected-verifier-bytes |

Invoke those subcommands through the admitted original lkjscript-dev. pair-verify accepts the
pair-run option shape but reads existing evidence rather than rerunning applications. Rehearsal
uses publication=dry-run and acquisition=simulated; public acceptance uses publication=release
and acquisition=anonymous. Keep exact and latest asset directories independent. release verify
uses an absent extraction destination; a read-only pair check uses the completed evidence root.

The matching source dispatchers and docs/release.md are the detailed procedure, including source
and runtime environment admission. If an original context cannot safely be reconstructed, retain
that precise limitation and use the missing-evidence branch; never claim a reader passed.

### B. Prepare useful notes and the exact publication

Use the retained release-notes draft if it exists under the predecessor's ignored
.artifacts/structural-202609150900/proof-eea968b9/ owner. It is a lead, not an assumed local file.
If unavailable, use this complete proposed annotated-tag message, adjusting only facts if startup
reconciliation requires it:

> lkjscript v0.1.36
>
> Author structural expression bodies through reviewed changes, including lexical bindings,
> explicit type/effect/requirement applications and substantial body replacement that preserves
> declaration and unchanged parameter identities. Existing flat requests remain supported;
> accepted typed meaning remains the program authority.
>
> Build ordinary transported task libraries with explicit requirement parameters. Typed-cell
> and queue witnesses compose producer-owned operations with caller-supplied exact requirements,
> callbacks and grants, including direct/bound invocation and supplier replacement. UpdateAttempt
> reports candidate data and the primary conditional result, not a transaction-commit certificate.
>
> This increment also fixes native installation-lock release at the owning operation's end.
> Linux x86-64 static-musl distribution and explicit runtime installation/selection continue.
> Installation does not migrate programs or operational data.
>
> Source: eea968b910ac62c3feab95a8896fff1a91904a17.
> Publication and anonymous exact/latest installed acceptance have separate evidence boundaries.

Write the exact text to an owned temporary file and create the annotation with git tag -a and
-F (the --file option). The inspected workflow reads the annotated tag object's message into release-notes.txt and
passes it to gh release create --notes-file. Do not leave the public notes as a generic version
line or try to edit an immutable release later. Do not claim pending public proof passed in the notes.

Follow docs/release.md, especially "Exact tag and immutable publication":

1. Refresh current main, intended tag/release and matching active jobs. Prove selected source is
   clean, normally pushed and reachable from main. Main descendants do not invalidate it.
2. Confirm v0.1.36 is unused, or enter the existing-tag branch. Do not increment versions merely
   because a release is awaiting completion.
3. Create the exact annotated tag locally at the selected source, with useful notes. Verify its
   tag-object SHA and peeled commit.
4. Read the actual immutable-release setting; it must already be enabled. Read and retain the
   previous value of the existing non-secret selector
   **LKJSCRIPT_IMMUTABLE_RELEASE_TAG_OBJECT_SHA**, reconcile its owner, and exclude queued,
   running, waiting or foreign publishers.
5. Compare the prior selector again immediately before changing it. Bind only this annotated
   tag-object SHA and verify the written value; this read/check/write is not atomic compare-and-swap.
   This established release-attempt binding is within the authorized release workflow. It does not
   authorize changing immutability, permissions, credentials or any unrelated setting.
6. Push only the annotated tag. The tag push owns publication. Do not manually create another
   release. If abandoning this attempt before tag push, restore the prior selector only if this
   attempt still owns the field and no tag/run has acquired it. On the normal path keep the new
   binding for tag push; after push retain it for the owning run.

Actual source configuration has only version-tag push and explicit-dispatch workflow triggers;
ordinary main/report/guidance pushes do not publish. The isolated contents-write job consumes verified
handoffs without checking out or executing repository code; it creates/resumes the exact draft,
uploads missing matching assets without clobber, verifies digests and publishes immutable latest
state. This workflow does not deploy applications.

### C. Finish publisher and anonymous acceptance

The publisher performs its own maintained fresh full, exact static target, package and transferred
installed acceptance, followed by publication and anonymous exact/latest installed acceptance.
**Reusing the successful rehearsal does not waive those fresh publisher gates.** It removes an
unnecessary second rehearsal and local replay.

Track the owning run/attempt and exact source; obtain original authenticated evidence at useful
completion boundaries. The current target owner selects one Linux x86-64 static-musl archive,
SHA256SUMS and install.sh. Require source/tag/manifest consistency, strict asset and attestation
admission, both anonymous acquisition routes, both installed small lifecycles, two-version recovery
where required, and the one actual five-owner aggregate with valid within-pair latest binding.

Fresh structural 42-to-43 creation/edit and transported requirement-library behavior must be present
through the existing route and offline-package owners. Do not replace them with a version string,
the template alone, a host-generated body or a handwritten success flag. Keep the original template
journey as well. Reconcile the publisher's original full evidence through the authenticated
preparation bindings described in A. Reread its package/target/installed evidence and anonymous
pair using their matching original readers and authenticated bindings.

An immutable release without successful public evidence is published but incomplete. For a transient
orchestration failure, inspect tag/control/assets and owning attempts before using the existing
bounded manual recovery. For a content/source/verifier defect after tag push, select the smallest
unused additive patch and refresh invalidated proof. Never move a pushed tag, replace an asset,
relabel an old attempt or rerun unchanged deterministic failure without diagnosis.

### D. Integrate concise reporting and guidance

Normal integration of intended documentation/guidance is selected, with actual protections honored.
The feature source is already integrated; do not present a reporting commit as new feature integration.

Update current-public README/status/release claims only after the corresponding public boundary
passes. Include the structural route in the incomplete release-procedure prose while retaining its
valid template example. Review nearby current-version claims against the accepted capability map;
do not perform a repository-wide historical version substitution.

Keep one concise current delivery summary linking the predecessor campaign and this reconciliation.
Append a short closure or resumption record to the relevant predecessor and this mandate. Remove
obsolete pending wording from current summaries, preserving historical attempts at their existing
owner. Do not copy another chronology into the roadmap, create parallel ledgers or introduce a new
permanent proof framework. Generated assets need no changes for a delivery-only continuation; if
a factual update affects a generated owner, use its source generator instead of hand-editing output.

The supplied root candidate preserves mission/authority, startup protection, current semantic
guarantees, pinned safe-Rust/no-Python policy, public workflow evidence, actual gate selection,
frozen sources and truthful delivery/waiting. Its changes modestly group navigation and move
adapter-specific details back to narrow owners. It adds no release identity or campaign objective.
Reconcile legitimate newer guidance rather than replacing it mechanically.

## 5. Acceptance and cost control

| Material claim | Required observable evidence and disposition |
|---|---|
| The accepted combined implementation is the selected product | Git source/ancestry and version agree; retain valid existing semantic, compatibility, full, target and fault evidence at eea968b9. Classify it as reused/reported, not newly executed here. |
| Hosted rehearsal is genuinely usable | Successful 34918782215/1 plus authenticated original ZIPs; exact full/preparation bindings and original verifier/package/target/pair readers pass. A metadata flag alone is insufficient. |
| Public artifact is the intended immutable product | Owning publisher passes its maintained gates; exact annotated tag, source, selector, release and three asset identities agree; required authenticity/attestation checks pass. |
| Users can obtain and use the capability | Fresh anonymous exact/latest installed journeys exercise structural create/edit with 42 then 43 and unchanged signature owners, plus original template behavior; existing within-pair suite exercises transported requirement libraries and independent failure/oracle cases. Both route admission and joined cleanup pass. |
| Recorded acceptance means what it says | Matching original public readers admit retained originals; distinguish rereading from execution, simulated from anonymous acquisition, and exact route's fresh aggregate from latest's within-pair binding. |
| Integration and reporting are complete | Intended mutable changes are normally integrated; current-public claims match passed boundaries; old frozen source and unrelated state are preserved; exact pending blockers, if any, remain visible. |

No new local full/target/pair run, fault matrix or extra application is required merely to archive
this mandate or replace root guidance. Reuse final-source 108/107 fault evidence when its bindings
remain valid. Additional testing needs a changed input, a real failure or an uncovered claim.
Do not run every overlapping contributor profile.

For mutable documentation/guidance changes, the inspected check/snapshot.rs selects diff_check and
rust_only_tooling through check changed; check the actual selected paths before running it.
Source-verified contributor entry points include:

    cargo build --release --locked -p lkjscript-dev
    cargo run --release --locked -p lkjscript-dev -- check changed --machine
    cargo run --release --locked -p lkjscript-dev -- check full --fresh --machine
    target/release/lkjscript-dev release target

Use the narrow changed check while the relevant changes remain visible in Git status, plus review
of the intended/staged diff. A clean-tree check is not campaign acceptance. If a correction changes
source, tests, workflow, verifier or other proof inputs, use the appropriate dependency-complete
maintained gates after freezing them; source/test changes widen to full. Avoid concurrent builds
that replace copied verifier/candidate outputs. Choose compilation concurrency from actual memory
rather than repeating the predecessor's resource failure.

A later report/root commit does not retarget release bytes or force circular source retesting.
Archive originals and exact trust bindings; a recreated receipt is not the original. Separate fresh,
valid reused, failed, cancelled, skipped, unavailable and unrun proof. No performance, authoring-time,
model-token or provider-cost improvement is claimed by this delivery campaign.

**Finite completion:** combined capability available in the immutable release; owning publisher and
required anonymous exact/latest acceptance complete; original evidence admitted; intended current
reports/guidance integrated; inherited delivery obligation closed. If only remote completion or an
actual access barrier remains, report incomplete delivery with a precise resumption instead of
lowering this finish line.

## 6. Waiting, return and evidence anchors

Do useful evidence admission, notes preparation and mutable documentation work while a remote job
runs. Inspect initially and at meaningful work boundaries, or to diagnose a concrete failure.
Do not use watches, sleep/status loops, repeated unchanged logs or polling hidden in a script.
If no productive work remains, one short bounded wait is reasonable only when observed progress
makes near-term completion plausible. Otherwise finish with exact source/tag/run/attempt, observed
state, missing gate, retained evidence location and one precise next action. Do not promise monitoring.

Return a concise Japanese report separating engineering, verification, integration, immutable
publication and anonymous public acceptance. Identify actual starting/frozen/tested/integrated
revisions, tag object, release, runs/attempts, actions, capabilities now distributed, evidence
classification, compatibility/nonclaims, preserved work and cleanup. Name any exact blocker and
resumption action. The key finding for the next architect is whether the combined milestone is
actually delivered; only after closure should a new capability be selected.

These are durable revision-bound source anchors. Their reports are not fresh executions:

- [Predecessor structural ending](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/docs/campaigns/202609150900.md) and [requirement ending](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/docs/campaigns/202609140938.md).
- [Current status](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/docs/status.md), [roadmap](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/docs/roadmap.md) and [existing root guidance](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/AGENTS.md).
- [Structural lowering](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/src/platform/control/change/structural.rs) and [installation locking](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/src/platform/installation/mod.rs).
- [Literal library/oracle contract](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/tools/lkjscript-dev/src/offline_packages/requirements.md) and [installed route](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/tools/lkjscript-dev/src/release/transferred/pair/lifecycle.rs).
- [Release procedure](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/docs/release.md), [frozen workflow](https://github.com/lkjsxc/lkjscript/blob/eea968b910ac62c3feab95a8896fff1a91904a17/.github/workflows/release.yml), [release dispatcher and full-receipt binding](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/tools/lkjscript-dev/src/release/mod.rs), [verifier admission](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/tools/lkjscript-dev/src/release/verifier.rs) and [pair reader](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/tools/lkjscript-dev/src/release/transferred/pair.rs).
- [Gate registry](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/tools/lkjscript-dev/src/check/registry.rs), [Git-status impact selection](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/tools/lkjscript-dev/src/check/snapshot.rs), [manifest](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/Cargo.toml) and [toolchain](https://github.com/lkjsxc/lkjscript/blob/e65e02a441adc96eb81fa876b4926d0c93554274/rust-toolchain.toml).
- Live sources observed: [successful rehearsal](https://github.com/lkjsxc/lkjscript/actions/runs/34918782215), [artifact metadata](https://api.github.com/repos/lkjsxc/lkjscript/actions/runs/34918782215/artifacts), [v0.1.35](https://github.com/lkjsxc/lkjscript/releases/tag/v0.1.35). These can change; refresh only for the stated startup and decision boundaries.

---

## Execution record — 2026-09-15 UTC

### Startup reconciliation and preserved authority

Actual mutable main and origin/main both began at `e65e02a441adc96eb81fa876b4926d0c93554274`,
with zero divergence. The only worktree changes were the supplied root guidance and this exact
initial mandate, already placed at the intended path. Both supplied originals were preserved;
the mandate's initial 32,474 bytes have SHA-256
`c0ea1eea25de423c26b862471046534ff736a187e801d47b632bacefde1479ed`.
The installed AGENTS.md matches the supplied candidate, SHA-256
`3166e99f1a229936de2d819c99b1f7f39ed4e1e1f995f69767b1d7f3330688a2`.
There was no intervening guidance to reconcile. The unrelated August 31 stash is preserved.

The selected source is already normally integrated and is exactly one reporting commit behind
initial main. An owned separate clone keeps detached HEAD at `eea968b9`; this mandate and new
guidance are absent from its frozen inputs. Mutable reporting uses
`codex/combined-release-202609151412`. Rust 1.98.0 is installed under `/home/coder/.cargo/bin`;
the initial shell omitted that directory from PATH. No Cargo or product job was active locally.
The 12 GiB host and retained memory-related failure were inspected; no new product build or full
test was needed to admit the hosted originals.

Startup selected branch 1: no v0.1.36 tag/release or active publisher existed, and the matching
rehearsal was usable. Administrator/push authority was present; main was unprotected, with no
required checks or rulesets. The only workflow triggers are version tags and explicit dispatch.
The immutable-release setting was already enabled. Its selector still belonged to completed
v0.1.35 object `c90758848312705af6bb2f896831fbed27d353e5`; no setting or permission was changed
except the explicitly authorized release-attempt selector binding described below.

### Authenticated rehearsal admission

Rehearsal `34918782215/1` completed successfully at 03:38:14Z. Build job `104222098922` and
pre-publication job `104235605973` succeeded; publish/public jobs were correctly skipped. This
supersedes the predecessor's 02:58 pending observation while preserving that historical record.
Cancelled `34915386161/1` and `34917057693/1` remain cancelled at their original sources.

All five original ZIPs were acquired through authenticated GitHub APIs before expiration. Every
ID, length, source/run binding and SHA-256 matches the refreshed metadata and initial table above.
The original run/attempt metadata, jobs, complete job-log ZIP, artifact metadata and extracted
originals are retained under `.artifacts/release-202609151412/rehearsal-34918782215-1/`.

| Boundary | Actual disposition and retained identity |
|---|---|
| Verifier | Authenticated job log supplies independent executable SHA-256 `7cab40031fb390ed0fcd408876978744630682aa026803e453e81097799acffe`, 30,012,128 bytes. Both were checked before restoring executable mode or execution. Original `release verifier verify` passed with handoff 4 and all six exact roles. |
| Original full | Preparation binds the authenticated 61,287-byte full receipt, SHA-256 `b710127da368625fda29bc71cb394bdfaa89d29d86656f9c88d8609e0458a2d2`, exact `eea968b9`, stable inputs, 26 fresh gates and zero reuse. All 52 original gate logs were retained and their lengths checked. This reconciles original preparation; no nonexistent full-only reader or new prepare/full execution is claimed. |
| Strict package | Matching original `release verify` passed, dry-run archive 10,907,614 bytes / `0c5a7d8c0389fca73410b6eda885c45ea4fe1ffd70d42b766084bcb62cf3eb1f`. Extracted static candidate is 24,033,824 bytes / `840ceecfc5c09ed24f3974726a20a386001c1c01c3b3b314a8ade58d71d8a652`. These are hosted dry-run bytes, independently bound from the different local build. |
| Six-target reader | Original `release admission-verify` passed all six named owners. Receipt is 73,161 bytes / `de17d3ca76f8f584454ba8f3d4201b6703239dd6860a4b5b3b63ac1e2a4ce9c5`. |
| Installed-pair reader | Original `pair-verify` passed receipt `verification_c13aa2ce1d7e3dce3049b89762c796d218b128017d36d545e5acd2f2a003efcb`, 164,046 bytes / `f7be27a3fe320f17b3b40d39b193326d1ea6cf0d13c9160c4dda8a02c8b85ea2`. Its one five-owner aggregate is `verification_efdb64ad7b4a6a2d0758d7e2e4274c181c6a27eaa5eb975958a48f815e59cda9`, SHA-256 `0c0f15f9306492bb5cd103429b5fe310e8c0a3fd1640ae589f8c2b88c66d42c4`. |

The pair is original hosted rehearsal evidence with simulated acquisition and dry-run publication.
Both original 19-command routes created 42, replaced the body to return 43 with unchanged signature
owners, and checked/built/ran the template with `"hello"`. The exact route executed the full suite;
latest bound that same aggregate within the pair. The requirement-library owner and two-version
recovery remain required. Cleanup passed. This invocation only reread evidence; it replayed no
application effects and claims no new anonymous acquisition.

Archived absolute paths, modes, selected relative symlinks, filesystem owner IDs and recorded
kernel were reconstructed only inside owned private mount/network namespaces. Exact frozen
source supplied the maintained service artifact. Initial context failures (a path guard, missing
source artifact, user-namespace UID mapping and local extraction umask) are retained separately;
correction changed only disposable context, never product readers or original receipts. Both
final readers passed. All 9,133 restored artifact files were compared to authenticated originals
after reading and retained identical content. Namespaces and reader processes were joined.

The predecessor's final-source local full/target/package/pair and 108/107 fault evidence remains
valid reused evidence, not fresh execution here. Its original full, target, pair, aggregate and
fault-summary hashes were checked against the predecessor's recorded values and still match.
No additional rehearsal, local full/target/pair run or fault matrix was launched.

### Exact publication started

Immediately before publication, all release runs were inspected: none was active. Authenticated
v0.1.36 release lookup returned HTTP 404 and the remote tag was absent. Clean selected source
`eea968b910ac62c3feab95a8896fff1a91904a17` remained reachable from refreshed origin/main.
The retained release-note draft was used, adding explicit static distribution and separate
publication/public-acceptance boundaries; exact text remains in the artifact root.

`git tag -a -F` created object `a8cacb9c574005079a7532e4f9e7d140c51ccc40`, peeling exactly to
that source. The prior selector was read again immediately before changing it, then the written
new object value was independently read and verified. This is the prescribed non-atomic
read/check/write, not a permission or immutability change. Only `refs/tags/v0.1.36` was pushed.
Remote tag object/source match. The selector is retained for its owning tag-push run.

Publisher [34933268953/1](https://github.com/lkjsxc/lkjscript/actions/runs/34933268953) began at
05:34:09Z, event `push`, exact source `eea968b9`, and was observed in progress. This run owns its
fresh full, target, package, pre-publication pair, immutable release and anonymous exact/latest
acceptance. Rehearsal reuse waives none of those gates. No release was manually created or asset
replaced. Immutable publication and public acceptance remain incomplete at this boundary.

### Mutable reporting and resumption

The public documentation now consistently identifies already accepted v0.1.35 while identifying
v0.1.36 as the active combined delivery. Release-procedure prose includes both structural 42-to-43
authoring/editing and the existing `"hello"` journey. Current status owns the concise delivery
summary; the roadmap links it and retains the consumed trigger. Predecessor endings are appended,
and the initial mandate remains unchanged above. No generated asset or product source changed.

When only this remote dependency remains, resume the same run with
`gh run view --repo lkjsxc/lkjscript 34933268953 --json headSha,status,conclusion,jobs` and confirm
attempt 1 before acting. Retain authenticated publisher originals at completion, admit its exact
full/preparation, verifier, package, target and simulated-pair evidence, then immutable release,
all three asset identities/attestations and anonymous exact/latest originals through matching
readers. Advance public claims and close the inherited delivery obligation only after those
boundaries pass. Do not dispatch a parallel publisher, move the tag or renew the deferral.

### Reporting boundary — 2026-09-15T05:40Z

Owning publisher remains `34933268953/1`, exact event source `eea968b9`, in progress. Job
`104265741936` passed exact checkout, workflow/source/tag identity, pinned toolchain, notice-tool
and locked-dependency setup; **Build the host verifier** was running. Fresh full/static-target,
package, pre-publication pair, immutable publication and anonymous exact/latest acceptance had
not yet completed. There is no diagnosed candidate, permission or service failure. The exact
remaining dependency is this remote job chain and admission of its resulting original evidence.
Metadata is retained as `publisher-{run,jobs}-report-boundary.json` under the campaign artifact root.

The documentation/guidance changed profile freshly passed both selected gates, `rust_only_tooling`
and `diff_check`, with zero reuse and stable inputs. The matching copied local verifier remains
SHA-256 `9ad0453cfe15647d9e4285dc390652e7bbbf29d69bceed8d5f8863fac16ccc4a`; no compilation or
source proof rerun was necessary. The final check and staged diff review precede the normal
reporting push; `report-check-final.json` retains that command's result. Refreshed origin/main
still equals initial `e65e02a4`, with zero divergence, push authority, no protection/required checks
and no rulesets. This reporting commit is separate from tested/frozen product source; its exact
identity belongs to Git history and the completion report, avoiding a self-hash/retest cycle.

The accepted combined behavior and compatibility contracts remain unchanged: structural input
adds no graph migration, `UpdateAttempt<T>` does not certify transaction publication, and installation
migrates no operational data or settings. No new performance, authoring-time, token or cost claim is
made. Existing stashes, operational state, prior immutable releases and failed evidence are preserved.
Owned readers and namespaces are joined; authenticated originals and their disposable reconstruction
are retained as evidence. Only remote completion and its dependent evidence/report closure remain.
No unattended monitoring or renewed release deferral is promised.

## Publisher admission and additive recovery — 2026-09-15T09:20Z

The transaction-completion mandate resumed the independent publisher without changing its
source or selector. Run `34933268953/1`, event `push`, completed successfully at 07:40:37Z.
Build/package job `104265741936`, pre-publication job `104278764431`, publication job
`104284885816` and anonymous-public job `104284977771` all succeeded. Immutable v0.1.36,
release `388927116`, was published at 07:00:57Z. Annotated object
`a8cacb9c574005079a7532e4f9e7d140c51ccc40` still peels to frozen
`eea968b910ac62c3feab95a8896fff1a91904a17`; the original selector remains bound to that object.

All six authenticated original artifact ZIPs and their external ID/digest/length/source bindings,
complete job logs, tag/selector/release metadata, original attestations and new attestation-reader
results are retained in `.artifacts/release-202609151412/publisher-34933268953-1/`. Artifact IDs are
`10383386925` (full), `10383807261` (target), `10383977017` (verifier), `10384011813` (package),
`10384811568` (pre-publication) and `10386575269` (anonymous public). Every ZIP matches its
authenticated SHA-256 and byte length; no handoff expired before acquisition.

| Original boundary | Actual admission |
|---|---|
| Verifier | Authenticated job-log binding is 30,012,128 bytes / `7cab40031fb390ed0fcd408876978744630682aa026803e453e81097799acffe`. Checked before mode restoration and execution; original handoff-4 reader passes all six roles. |
| Full/preparation | Preparation binds 61,283 bytes / `a66b2321e5ea96bf9c0387cde4ade43d7851932179f0803bde2a674d91e6630f`, exact frozen source, stable inputs, 26 fresh gates and zero reuse. All 52 gate logs are retained and length-checked. No prepare/full execution or nonexistent full-only reader is claimed. |
| Strict package | Original reader passes the release-mode archive, 10,907,638 bytes / `3cdaaef45164e870a1a0562c42d505aa53f90cff176ce025c02710d6f2779ed0`; extracted static executable is 24,033,824 bytes / `840ceecfc5c09ed24f3974726a20a386001c1c01c3b3b314a8ade58d71d8a652`. |
| Six-target reader | Passes receipt 73,152 bytes / `e12dbcedd14650b1733cc3f2304844a7c3b9e6fe8dba002637c9b06fa552ca12`, including the maintained service owner. |
| Pre-publication pair | Original reader passes `verification_6638c55829a7cd5ae30c3e6bd90475d68bd85a28961c18585088a11a5d28f1ce`, 164,076 bytes / `fcf9f304f00cc84504beeddd43068703f2396d598c3967f927265f980d6a76b9`. Its five-owner aggregate is `verification_f475cd683f304637dabc99ab33c409c98081110deebb41380e33c7a8cb5cd0b1`. Acquisition is simulated, publication mode is release. |
| Immutable assets/authenticity | Archive asset `565119322`; checksum asset `565119355`, 109 bytes / `e8e6baf55535c65d32d52770eea75b14a13dc88a300453050939893d7c168516`; installer asset `565119377`, 3,566 bytes / `f698c0848dceb812ffcff5b9427d3854031df977fdcd4f6d61ba0c42e3f94683`. Both route triples match all external bindings. Release and all six exact/latest asset attestation rereads pass with retained gh 2.98.0. |
| Anonymous original pair | Hosted execution reports both 19-command structural 42-to-43 and original `"hello"` lifecycles, signature continuity, one fresh five-owner aggregate and cleanup. Original public receipt is 157,317 bytes / `de3166088e4fa2e4efce80130efdecfc81c459b8416a9dd3407cd638ea60c932`, aggregate `verification_233e04f74b5190ddeaa80c9695567169ad514079c555a0ff8c87fa37cc9fe05f`. **Original-reader admission fails because required authored material is missing from the uploaded artifact.** |

The exact failure is `inspect bounded file
'/home/runner/work/_temp/public-verification/pair/exact/create.lkjc': No such file or directory`.
Frozen `.github/workflows/release.yml` retained the public logs, lifecycle, built artifact,
extraction, installation and suite, but omitted the eight files below. The pre-publication upload
retained them through its existing route inventories. This is a workflow evidence-retention defect,
not a transient service failure or a missing claim that a successful subjob can satisfy.

| Route | Missing original | Bytes | SHA-256 required by its authenticated public lifecycle |
|---|---|---:|---|
| exact | create.lkjc | 1,064 | `1dd75f3fa6ab058a284d68ccce380423309d2024b95704de0bcf70c4a5b584c7` |
| exact | create.logical-plan | 29,588 | `ccf431f1a569552a2137ce91df7120039baf070d3cc3d53c9ab92052f501509f` |
| exact | replace.lkjc | 566 | `6a07cfd8b392f112ef8402dbd5117e11e1c635e75e16a880e7a5f1178de10895` |
| exact | replace.logical-plan | 37,049 | `0a5d3c57d3dc5dcfb2dc3b6e031531f7b5cf558da353393a29766009b75bcb48` |
| latest | create.lkjc | 1,064 | `1e2bf10b0ad6ab0c15f85b4c2a0c37831bdf79d820126b469e1f1ba9e275d3a8` |
| latest | create.logical-plan | 29,588 | `cc4591e7260540f08481416b391ea236ecae448ae43b46fdc3af9c5d3fd93282` |
| latest | replace.lkjc | 566 | `066e4371e83e60cb3a83eaad3016deb807862fedd4e985972042925a2997cbac` |
| latest | replace.logical-plan | 37,049 | `c50024b4d2e5aedc67896ac7c119bba97926f8398025efb97bf781f3ecb2e166` |

The retained-original search covered all six publisher roles, all five authenticated rehearsal
roles, exact frozen source and local structural-campaign evidence: 35,118 files, 86 matching-length
candidates, zero matching originals. Pre-publication's eight files have different hashes. No
request or plan was regenerated and no receipt was modified. The complete failing reader output,
search scope and individual expected bindings remain at the evidence owner above. The successful
anonymous job therefore does not close original-evidence/public acceptance for v0.1.36.

Context-only failures were diagnosed and corrected separately: historical documentation had
unrelated old absolute-path bindings; git archive extraction gave the maintained service artifact
mode 0664 instead of its authenticated 0644; the anonymous reader additionally required its
recorded hosted variables and server-derived run URL. Restored paths, modes, original UID/GID,
selection symlinks and kernel exist only in owned private mount/network namespaces. All 12,111
restored existing artifact files remained byte-identical to authenticated originals afterward.
Every reader/namespace was joined. The system gh 2.45.0 lacked the attestation subcommands;
retained gh 2.98.0 performed the seven successful rereads. These context/tool failures and the real
missing-material rejection remain distinct. No local applications or live effects were replayed.

This contradicts the new mandate's observation that no workflow repair was identified. Its
authorized smallest additive recovery is reserved as **v0.1.37**, after refreshed tags/releases
ended at v0.1.36 and all existing attempts were terminal. An isolated branch based on exact
`eea968b9`, `codex/public-evidence-recovery-202609151412`, adds only the eight required public
upload paths, their existing Rust workflow regression assertions and the product manifest/lock
version. The transaction increment and new guidance are absent from that frozen repair source.
Focused validation and normal integration precede a new exact-source rehearsal/publication under
the unchanged maintained gates. No v0.1.36 tag, asset, receipt, selector or old history is replaced.
This due repair precedes the publication lane for the independently developed transaction outcome.

### Frozen repair source — 2026-09-15T09:26Z

The four-path repair is committed as `3b32f2fc1221fb129950dacc983f1f54423b7b1b`, based directly
on `eea968b9`. Its isolated worktree is clean. Pinned release-profile compilation freshly ran the
existing workflow regression: one passed, 133 filtered. Formatting and locked metadata checks
pass. The copied matching library test executable, 19,513,944 bytes / SHA-256
`c15c155c74a7cd12f8b59a8d6f69197cead710911bf397781350fcf2be171afb`, rejects the authentic
pre-repair workflow with `missing pair/exact/create.lkjc` and passes after restoration of the
repaired workflow. All four intended staged paths and their staged diff were reviewed before
commit. No production behavior, live effects, local full/target suite, tag or selector was changed.

Normal integration must preserve this frozen source as an ancestor of main before dispatching
the exact repair branch with `publish=false`, `tag=v0.1.37`. The hosted rehearsal owns fresh full,
six-target, package and installed-pair proof; its authenticated originals must pass matching
readers before the additive tag publisher starts. Integration/rehearsal/publication remain
distinct pending boundaries at this record. Existing v0.1.36 and its missing-material evidence
remain preserved.

### Version-derived guide correction — 2026-09-15T09:35Z

Root normally integrated `3b32f2fc` through main merge
`7590a121d2db6d9f1703c36175b16329c7281dec` and dispatched repair rehearsal `34952573731/1`
at exact `3b32f2fc`, `publish=false`, `tag=v0.1.37`. The first branch push returned a remote
500; root checked that the branch was absent before its successful bounded retry. No history
was rewritten. Immediately after dispatch, root identified an omitted version dependency:
`generated_header()` binds the product version and capabilities digest, so the eight old
generated guides could not pass the exact generated-document gate. The initial version review
was incomplete. Root cancelled that owned invalid-input attempt; the run completed cancelled at
09:30:08Z, during host-verifier compilation. Full, static target, package and later acceptance
were skipped. It produced no artifacts. Authenticated run/jobs metadata and its original log ZIP
are retained with the recovery evidence. This was a cancellation, not an executed full-gate failure.

The matching pinned v0.1.37 host product was built in the isolated recovery checkout and copied
outside both language checkouts. It is 23,893,088 bytes / SHA-256
`30d7b062b01031760e2864694119e5fe6f54bb8bfa399527c284194148818f7a`.
Its actual `capabilities --verify-generated` first rejected the old guides with
`capabilities_generated_drift` (exit 2). The same copied product then generated all eight guides
through `capabilities --generate-docs`; the fresh exact verification passed every file (exit 0).
Only their first lines changed: product version 0.1.37 and capabilities digest
`4f8e8b3dce7a9f8120fd713cb3ec9b20ae892296a0a3a64961fe2072a63e97e3`.
All remaining guide bytes are unchanged. No guide grammar was maintained by hand. Historical
version references and the unrelated tracing-core dependency retain their existing meaning.

The explicit eight generated paths and staged diff were reviewed, then committed as
`8baafff7f67b55481cda8d67f8945b78af80be4d`, a direct child of `3b32f2fc`. This is the corrected
frozen v0.1.37 source. Its production code is unchanged from the local document-generating
binary; the earlier workflow regression remains valid reused narrow evidence. Full/target/
package/installed proof must come from the corrected exact-source rehearsal after normal
successor integration. The host product and before/generate/after records are retained; its
owned external temporary directory was removed and all build/product processes were joined.
The transaction increment remains excluded, and v0.1.36's immutable release and missing-original
failure remain unchanged.

### Corrected integration and rehearsal — 2026-09-15T09:40Z

Root normally integrated corrected source `8baafff7` through main merge
`3372cb3ef7d215e419e8720a8b122c8e93d3b6c5`, and pushed both main and its frozen source branch.
Remote identities and source ancestry agree. Corrected read-only rehearsal
`34953636796/1` was created at 09:38:40Z, event `workflow_dispatch`, exact
`8baafff7f67b55481cda8d67f8945b78af80be4d`. Its initial build job `104330334858` was running
the host-verifier build. Fresh full, static-target, package and pre-publication acceptance remain
pending at that observation. The cancelled first repair attempt is preserved and not reused.

The original-evidence owner is `.artifacts/release-202609151412/rehearsal-34953636796-1/`.
It retains initial run/jobs metadata, remote source/tag identities, immutable-setting and selector
snapshots, a checked exact source archive, a useful release-note draft and concrete reader/
publication resumption instructions. v0.1.37's tag remained absent and its authenticated release
lookup returned 404; immutability remained enabled and the selector still bound terminal
v0.1.36 object `a8cacb9c`. These are preparation observations, not permission or freshness for a
later write. No tag, selector or publication mutation was made by this audit.

At the next meaningful implementation/proof boundary, inspect `34953636796/1` once. On success,
acquire its five authenticated original roles promptly and admit its own matching verifier,
full/preparation, strict package, six-target and installed-pair originals. Actual artifact metadata
must supply all IDs, hashes, lengths and recorded context; old v0.1.36 bindings cannot substitute.
Only after those readers pass may refreshed serialized publication checks authorize the new
annotated v0.1.37 tag at exact `8baafff7`. Its fresh publisher and anonymous original-reader gates
remain mandatory. The repair lane stays independent of the transaction increment, version 0.1.38.

### Corrected rehearsal original admission — 2026-09-15T11:45Z

Corrected rehearsal `34953636796/1` at exact `8baafff7` completed successfully at 11:33:54Z.
Build job `104330334858` and prepublication job `104355785357` passed; publication and anonymous
jobs were skipped as required for this dry run. All five original ZIPs are retained with
authenticated ID/source/digest/length bindings: full `10391752071`, target `10392949371`, verifier
`10393392804`, package `10393626716` and prepublication `10394580498`. The final ZIP has
153,417,596 bytes and SHA-256
`8de9af90b281a74645cab156bf9e788dc46a407f01107fcdd7e097fdbb41e824`.

The matching frozen verifier has 30,012,128 bytes and SHA-256
`30ba047512207f293b455905ff7760c08a47cccf4063a38d953b9ed081caea44`, independently bound by
authenticated original build/prepublication logs. Its verifier, strict package, six-owner target
and pair readers pass. Original preparation binds the original full receipt's 26 fresh gates,
zero reuse and all 52 logs. No prepare/full or application effects were rerun locally.
Pair receipt `verification_ce843b1dc8dfd411a1163e0758703d48c6d39787618e4c58decac035653600ea`
has 164,022 bytes, SHA-256
`fa428e03aa8a1078f5b6773b3aaddee61c71647b66502d4f744918c6f53cce64`.
Both routes retain all eight structural originals and pass 42-to-43 editing, original hello,
recovery and cleanup; the within-pair five-owner suite is fresh. This is simulated prepublication
acceptance, not anonymous public acceptance.

Private mount/network reader contexts restored only original paths, modes, selection symlinks
and recorded environment. All 9,135 restored files match authenticated originals afterward;
readers/namespaces were joined. `rehearsal-original-admission.json` and the role-level records in
the existing ignored rehearsal owner retain the full bindings and original command contexts.

### Serialized corrected publisher startup — 2026-09-15T11:50Z

Root refreshed the exact source/main ancestry, permissions, protections, all 74 visible Release
attempts (none active), unused v0.1.37 tag/release and enabled immutability. The prior selector was
compared immediately before its authorized write: it still contained terminal v0.1.36 object
`a8cacb9c574005079a7532e4f9e7d140c51ccc40`. The new annotated object
`51ecaabf3a6dfbaef0f842e95c4fc480dced0b6d` peels to frozen
`8baafff7f67b55481cda8d67f8945b78af80be4d`. The selector was written once and independently read
back; only `refs/tags/v0.1.37` was pushed. No immutability/permission settings were changed.
The prior tag still resolves to original `eea968b9`; no old assets or failed evidence were replaced.
The newer transaction increment is normally integrated at `3fdddef3`, but excluded from this tag.

Owning tag-push publisher **34965341260/1** began at **11:49:02Z**. At the bounded startup read
11:49:54Z, build job **104368343435** had passed workflow identity, toolchain, notice tool and
dependency fetch, and was building the host verifier (step begun 11:49:24Z). No artifact or
v0.1.37 release existed yet. Startup metadata/selector/tag and source archive are retained at
`.artifacts/release-202609151412/publisher-34965341260-1/`, with the root's mutation evidence in
`publisher-v0.1.37-startup/`. The owning selector stays bound to this run.

Fresh publisher full/target/package, prepublication installed pair, immutable release assets,
attestations and anonymous exact/latest installed pair with original-reader admission remain due.
At resumption inspect this exact run/attempt once; acquire each available authenticated original
handoff promptly and use its matching frozen verifier/source context, without repeating the
already admitted rehearsal. Preserve healthy work and dispatch no competing publisher. Missing
or failed material requires diagnosis under the existing bounded/additive recovery protocol.
v0.1.35 remains the last fully closed public milestone until the actual anonymous original-reader
boundary passes. This is an external-completion handoff, with no promise of unattended monitoring.
