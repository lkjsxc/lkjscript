# Bounded verifier process completion

## Original user mandate (unchanged)

> https://github.com/lkjsxc/lkjscript lkjsxc/lkjscriptについて進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。限界まで深く考えてほしい。超長期的な視点からお願いしたい。自律的に次のやることを考え、進み続けてほしい。

## Reconciliation and decision

At 22:48 Asia/Tokyo on 2026-09-24, local main and an independent GitHub read
both identify 89c2d883. The authorized cw-bd235945fff44468a0f41292 checkout
has no other worktree, stash or active project job. Preserve the pre-existing
untracked 202609242022.md and all previous evidence. Existing root guidance is
appropriate. PR #3 and the native HTTP/HTML examples are already on main; the
HTML external matrix, resident metering and host-process completion remain open.

Repair the shared contributor process owner first. Keep the direct child waitable
until the last group signal, drain both pipes without blocking completion, retain
deadline/cancellation/output accounting after direct-child exit, and clean up sampled
owned descendants. Use that owner for bounded Git/toolchain identity commands.
Use safe Rust and the existing dependencies; do not introduce a second executor,
new receipt system, language feature, production deployment or containment claim.
A process escaping ancestry sampling into a separate session is not completely
contained by procfs observation. Missing stream completion must fail, not hang.

Prove the defect with finite independent fixtures, then retain regression tests for
output boundaries, inherited descriptors, controls, descendant cleanup, child reaping
and healthy recovery. Use focused feedback, followed by one stable-source fresh full
acceptance through the maintained owner. Deliver through ordinary main ancestry and
independently reread GitHub. Record failures separately from accepted observations.

## Execution record

Pending. Evidence belongs in `.artifacts/202609242248-process/`; later reporting must
not relabel this starting source or any failed run as accepted implementation.

### Implementation and focused observations

The shared process owner now uses nonblocking standard-library pipes, finite fair
stream turns and waitid NOWAIT. The direct child remains waitable through all group
signals; the consuming cleanup owner reaps only afterward. All shared routes sample
owned descendants and continue traversing known branches after reparenting. Closed
stdout is closed before spawn, not in a race with the child's first write. Every
ordinary drive error still enters cleanup and both log synchronization attempts.
Stream exhaustion fields survive a later infrastructure failure. Git/toolchain
identity commands reuse this owner, with the existing byte bounds, a 30-second
execution allowance and temporary logs outside the observed checkout.

Two independent finite tests ran before the production edit. Both failed: an
80-millisecond deadline was exceeded by inherited stdout/stderr writers, but the
old owner reported Passed at 607,811,479 and 609,637,656 nanoseconds. Original
`baseline.log` retains these actual failures. No indefinite fixture was launched.

The first compile attempt failed on two explicit rustix Errno conversions. After
correcting them, `cargo test --locked -p lkjscript-dev --lib -- --nocapture` passed
181 tests with 19 ignored and zero failures (6.29 seconds test execution). Ten new
tests were included in that run. A subsequently added eleventh test independently
exercised a sampled reparented branch creating a later child and passed separately.
Focused all-target contributor Clippy with denied warnings passed. These observations
are not full acceptance; ignored tests remain unrun. Original logs are retained.

A later grouped request to optimize retained descendant inventory and inject a
root-only traversal fault was blocked by the tool safety check before execution.
It was not retried or rerouted. Read-only inspection confirms neither that proposed
optimization nor the mutation was applied, and no mutation result exists. The
already-applied healthy reparenting test remains. The inherited bounded inventory
is not advertised as unlimited process churn or complete hostile containment.

Root guidance, dependency versions, language runtime, public binary assets and
production services are unchanged. The pre-existing untracked predecessor campaign
is preserved and excluded from this change. Fresh full acceptance and integration
are recorded separately below only if actually completed.

### Fresh dependency-complete source acceptance

Implementation source: `4ead1716977319c72dcef7cb3843a1e0272ade77`, based directly on
`89c2d883`. The release contributor executable built successfully in 2m 52s and was
copied to the campaign's evidence directory before use. The maintained command was
`check full --fresh --machine --jobs 2`; no Git/source/doc edit occurred during it.
All **26/26** gates passed freshly, reused **0**, with `input_stable=true` and no
failure. Elapsed time is **677,448,550,703 ns** (677.449 seconds). The retained receipt
is `.artifacts/lkjscript-dev/check/1790258692506862210-349731-0/receipt.json` and its
worktree-start/end digests agree. This is full, not release-source or an unchanged
`check changed` result. It includes the workspace, strict Clippy, checker self-test,
source policy/surface, generated guides, exact package/artifact rebuilds, release
command lifecycle, detached applications, pure tail and stateful HTTP acceptance.
The public release was not rebuilt or replaced by this acceptance.

A later grouped read-only resident-policy/progress request was also blocked before
execution. That additional investigation stopped; it produced no source change or
new resident-policy result. Completion of the already-started full command was
received normally through its original process session and reported exit zero.

### Complete the independently detached HTML boundary

The separate workspace `/home/coder/workspace/lkjscript-html-acceptance-202609242248`
contains a new independent `verify.mjs`, raw inputs/results/commands and `summary.json`.
The old failed drivers and authoring roots are retained. GitHub's authenticated v0.1.44
release 394461526 still identifies asset 583363930, 11,443,454 bytes. The retained
archive matches that asset's published digest, and the runtime executable matches the
archive's exact executable bytes. This reused the earlier accepted library/article
artifact, not fresh authorship or a development-source product binary.

All **88** rendering cases pass exact serialization and independently constructed
full ordered parse5 8.0.0 DOM trees, including attributes, namespaces and normalized
text. Cases comprise ten explicit leaves, 64 seeded recursive inputs (`0x5eeda11`),
eight breadths through 2,048 paragraphs, and six depths through 24 sections. Exactly
23 inputs intentionally produce only the parser's null-character diagnostic; NUL
replacement/removal and newline normalization match independent expectations.
These are not 88 differential VM tests or a sanitization/security certification.

All **six** malformed runtime values reject with `normalized_json_type` and create no
result file. The separate article target then succeeds. Each of the 89 successful
invocations has stopped admission, drained work, zero remaining tasks and no cleanup
failures. There are **95** public product commands in this matrix. The prior five
rejected native plans remain prior evidence, not new repetitions or inferred passes.

### Actual typed HTML / HTTP library composition

The separate workspace `/home/coder/workspace/lkjscript-html-http-202609242248` retains
all literal requests, logical plans, public CLI logs, bundles, descriptors, independent
HTTP/DOM checks and failed configurations. A fresh response library passes 53 tests.
A fresh HTTP site imports that library and the retained exact HTML transport; its
76-line literal declaration is now `docs/guides/examples/html-site.lkjc`.
Its final public-binary check passes **74** tests with evaluator agreement: 45 standard,
16 HTML, seven response, two starter and **four newly authored composition tests**.
The final bundle has four exact packages and is 856,152 bytes. No semantic graph is
constructed by the external JS driver: the driver substitutes observed identities
into the literal native request and launches the public product.

The final detached HTTP acceptance passes **22** requests: **19** successful HTML
responses and **three** expected 404s (missing path, POST and HEAD). Eight clients
execute simultaneously. Default/repeated/empty values, Unicode, entity-looking and
HTML-looking text, plus decoding, NUL/line endings and a wider query have exact status,
header, content-length and UTF-8-body expectations with independent parsed HTML checks.
Two server lifecycles, including restart on the same immutable bundle, exit zero after
SIGINT and report stopped admission, zero queued/active/remaining tasks and no cleanup
failures. Only the public executable, artifact and descriptor are present in runtime;
both fresh authoring roots were moved away before service execution. No server was
exposed beyond its ephemeral loopback listener and both owned servers were stopped.

Failures remain distinct from this result. The first site had no stream requirement
or grant; its 74 pure graph tests passed, but HTTP preparation rejected with
`normalized_http_stream_grant`. A separately authored corrected site explicitly
selects the generated exact ByteStream grant and declares its one-call read-all
allowance; the handler itself does not invoke read-all. Readiness confirms the actual
`streams: byte-stream` binding. The first successful HTTP driver's summary still
contained a stale empty-grants literal; that report and its event logs were retained
under `evidence/first-success-stale-summary/`. After correcting the reporter to read
the actual descriptor and assert readiness grants, the full 22-request/two-lifecycle
matrix passed again; `http-summary.json` and `http-verification-final.log` are final.

A replay probe for the original missing-owner failure initially used an absolute
artifact path and correctly rejected with `deployment_path`, not the intended error.
That failed probe is retained. The canonical relative-path replay then rejects with
`normalized_http_stream_grant` before any ready event. Neither failed probe nor the
stale summary is relabeled as the final accepted HTTP execution.

### Delivery boundary and next architectural work

The later guide/example/status/report changes do not modify the Rust source accepted
at `4ead1716`. Their product behavior is instead supported by the explicit v0.1.44
native and detached HTTP observations above. Do not relabel a later reporting commit
as the exact source of the earlier full receipt. Normal mainline delivery is recorded
only after Git confirms it; there is no force push, protection change, new release,
production deployment or claim of unattended future work.

The next substantial runtime decision remains coherent optional metering for trusted
resident execution while preserving request deadlines, cancellation and admission
ownership. The currently demonstrated resident descriptor still carries 10,000,000
instructions and a 30-second request deadline. The procfs process inventory remains
bounded and conservative; high-churn inventory optimization and the blocked mutation
experiment were not completed. Native web growth should continue through ordinary
libraries and independent consumers, not application-specific Rust intrinsics.
