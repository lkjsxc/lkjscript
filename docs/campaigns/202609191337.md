# Status-aware bounded outbound HTTP responses

## Selected execution mandate — 2026-09-19 13:37 Asia/Tokyo

Starting remote main: `44cc0dadd4ff4325a940664976a5592b8fbb6552`.
Base tree: `c1266754008e8d9630a5611a62991414029f383a`.

Deliver correct, bounded response admission through the existing deployment-bound
`HttpClient.get`. Preserve the ordinary executable, exact destination/grant
boundary, independent application policy and once-only request execution. Complete
normal remote-main integration only after the maintained acceptance succeeds.

The inspected response reader selects Content-Length or Transfer-Encoding before
its 204/304 branch. Consequently, a legitimate 304 metadata length selects a body
read or a body-limit failure; a prohibited framed 204 can return content. Its
chunk-line reader also accepts a discovered CRLF before checking the line bound,
so an overlong line can be accepted or rejected depending on read fragmentation.
These are source-derived counterexamples, not an executed predecessor trial.
RFC 9110 section 8.6 and RFC 9112 sections 6.1/6.3 provide independent protocol
expectations. Numerical/preparation measurements and transaction-joining were
considered, but neither has a reproduced limiting workload in this execution;
repairing these concrete existing guarantees takes priority.

### Success contract

A 304 returns its original ordered headers and an empty body immediately after
complete headers, without reading a representation or applying its metadata
length to the response-body allocation limit. Its canonical decimal metadata
may exceed a machine integer. A valid 204 also ends at headers; prohibited 204
Content-Length (including zero) and Transfer-Encoding reject. Existing duplicate,
ambiguous, malformed and unsupported framing rejection remains. Already buffered
bytes after a bodyless response reject; this does not promise detection of future
packets after the client has completed and closed its non-reused connection.

Every chunk size/extension line is bounded to 8,192 wire bytes including CRLF,
regardless of buffering or fragmentation. Exact-fit lines work; one-over lines
reject before body publication. Ordinary sized, chunked and close-delimited
responses, truncation, body limits, trailers and recovery retain their contracts.

Use deterministic literal-wire tests with independently expected status, headers,
body and diagnostic classes. A reader that forbids another read after the header
block makes bodyless completion observable without timing assumptions. Cover
fragmented/coalesced reads, metadata integer overflow, exact/one-over line bounds,
and successful reads after rejection. Exercise the prepared adapter against an
owned raw loopback peer where useful; keep test adapters distinct from graph
application logic. The existing copied-executable outbound owner supplies the
unchanged public creation/build/run/TLS/lifecycle regression boundary. Do not call
source-bound parser tests a new copied-binary workload or fresh-agent trial.

Implementation is confined to the outbound response reader, focused Rust tests,
its normative HTTP specification and this record. No language operation, graph,
artifact, installation or application-data format changes. No cache, redirect,
retry, outbound method, endpoint authority, credential, deployment or limit
increase is selected. No operational data migration is needed. Malformed 204 and
overlong chunk lines become deliberate rejections; valid existing response
behavior remains. Failed requests are never automatically replayed.

Use one coherent Git-object candidate derived from the exact base tree and one
owned `verify/` lineage. Inspect the complete delta and execute the existing
`check full --fresh --jobs 2 --machine` workflow without changing its verifier,
permissions, triggers or acceptance. The full registry includes library tests,
workspace tests and the copied outbound HTTP owner. Require exact-source, stable,
fresh acceptance and actual original-receipt admission before non-forced main
integration. Refresh main/rules, preserve concurrent work, and independently read
the delivered main SHA and content. Keep original failures and reporting-only
changes separate from the accepted implementation.

Publication is not selected. A concrete consumer needing this corrected behavior
in an installed release, an explicit release request, or a deliberately selected
usable milestone makes publication due; another feature is not a prerequisite.
No production deployment is authorized. `AGENTS.md`: unchanged.

## Startup reconciliation

The pre-main verification bootstrap is already accepted and integrated; its
retained `verify/202609191047-pre-main` branch is not unfinished product work.
Its original source/run evidence is not rerun or relabeled here. Existing
input-evolution publication remains subject to its recorded consumer trigger.
No open pull request or in-progress Actions run was observed. Main was reported
unprotected and the repository ruleset collection was empty at inspection.

This session exposes coherent Git-object creation and non-forced ref updates,
unlike the preceding session's available operation inventory. No local checkout
or Rust toolchain is present; native GitHub acquisition fails DNS resolution.
Use the existing read-only hosted verifier, not a new execution service or release
credentials. This does not describe or modify the user's unseen working machine.
Effective candidate write and hosted execution results will be recorded separately.

## Candidate review and continuation

Git-object writes created coherent candidate
`7703622ad67c7d9a69add44fcf03b050eb8d2498`, tree
`75d649a8a2a50fcced56717e2bb07e209658d1ee`. Its inspected diff contains exactly
four intended files and preserves unrelated source. Creating the owned branch
`verify/202609191337-http-response` actually produced push-triggered Verify run
`35422125835`, attempt 1, job `105841623403`. This is execution-route evidence,
not product acceptance. At review the job was provisioning its immutable verifier;
its live log endpoint returned BlobNotFound, so no test result is inferred.

Post-freeze review found a test-fixture execution defect: the raw TCP witness used
`Handle::block_on` with an otherwise undriven current-thread runtime. Tokio 1.53.1
requires an independently active I/O driver for that call. The successor uses one
multi-thread runtime worker, matching the public executable's runtime family.
The in-memory parser witnesses still use `Runtime::block_on` and need no I/O driver.
This is a test correction, not a product/runtime policy change. Preserve the
initial run and never transfer its result to the corrected source.

The normalized adapter passes the client's status, ordered headers and body into
the existing structural result without cache policy; the existing relay recipe
still deliberately maps non-200 responses to 502. No automatic cache behavior is
introduced. No cancellation or branch-ref deletion operation was exposed by the
connected tool inventory. Use the same verification lineage and its existing
serialized concurrency; retain needed original evidence and report exact later
run/source identities rather than inventing completion or forcing cleanup.

## First hosted result and correction

Run `35422125835`, attempt 1, finished with failure at the maintained `fmt` gate.
The release verifier build succeeded. Full selection was 26 gates: 3 fresh passes,
0 reused, and a formatter nonzero exit; dependent product tests did not execute.
The exact-source acceptance step was skipped, not passed. The original receipt is
`1789793565468566627-6309-0/receipt.json`, digest
`verification_9ade84096620112ee3ec83ecbc2b93ec340f6034a794fc4d2aa216ddff23ad78`.

Artifact `10577129428` was downloaded through the authenticated connector and its
16,104,309 bytes matched the hosted SHA-256
`ba7668ac18e8ecde8b3aca2189d6147c742566b54a5849cd3b3e5b443a904338`.
Its context binds the original source, tree, workflow, event, run and attempt.
The original formatter stdout identifies only three layout hunks in the new
response test module; apply that exact formatting together with the reviewed
I/O-driver correction. Do not alter the product fix or verification expectations.
The hosted artifact expires `2026-10-03T04:52:50Z`; its original ZIP is also retained
in this execution's isolated evidence. No product acceptance is reused from it.
