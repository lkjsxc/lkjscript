# Immutable 0.1.21 Consolidated Distribution Release

Status: incomplete

Campaign ID: `202609041625-v0.1.21-consolidated-release`

## 1. Identity, audit boundary, and lifecycle

Audit time: `2026-09-04 16:25:11 +09:00` (`Asia/Tokyo`).

Intended archive: `docs/campaigns/202609041625.md`.

Repository/source: read-only remote GitHub repository `lkjsxc/lkjscript`, not an active checkout.
Default and inspected branch: `main`.
Orientation commit: `43a982c4858eae5694e8f8efe0f4d3deb635ec87`.
Orientation tree: `1ca6847ed8c11baa13c508959488ba8e8fc58113`.

The baseline is orientation, not authority to rewind. The reconciled checkout, applicable instructions,
legitimate later commits, preserved work, and refreshed external state win.

Inspection covered repository metadata and recent diffs; root `AGENTS.md` and its policy provenance;
the five completed source campaigns after `v0.1.16`; current status, architecture, roadmap, release,
verification, maintained-consumer, generated, and evidence owners; the release workflow; public
tags/releases/workflow metadata; and open issue/pull-request state.

Unavailable and unclaimed: local branch/`HEAD`, index/worktree, untracked/ignored files,
remotes/upstream/divergence, stashes, supplied work, toolchain execution, installed binaries,
private artifacts, credentials, repository variables and immutable-release settings, deployments,
running services, and hosted application/queue/object/data state. The deployments collection was
unavailable through the connector.

Observed remotely:

- source declares product `0.1.21`;
- `main` is 31 commits ahead of the `v0.1.16` release-source commit and zero behind;
- immutable latest is `v0.1.16`, release `381033538`, published `2026-09-02T06:54:38Z`;
- annotated tag object `fd9879e967512e9269faf111d462140572fec137` selects
  `b49d78e862b7cbba02c639f06ca4bd2e11db1f2d`;
- release run `33599176438` and dry run `33597094514` succeeded;
- no `v0.1.21` tag, queued/running recent workflow, open issue, or open pull request was observed.

Refresh all volatile identities before use. Historical evidence does not prove current checkout,
credential, setting, deployment, or service state.

`docs/campaigns/` remains the sole observed mandate archive; no mutable active-work ledger was found.
Do not create another campaign, prompt, plan, or work tree. Preserve this initial mandate body.
Codex may change only the minimal lifecycle field allowed by current convention and append one bounded
terminal record. Keep current facts, semantics, architecture, rationale, proof, generated projections,
and raw output in their existing owners.

Predecessor disposition:

- `202609040425` exact HTTP routes: complete.
- `202609032212` structured sessions: complete.
- `202609031354` incremental catalog and million-owner capacity observation: complete.
- `202609022319` identity-preserving `extract.function`: complete.
- `202609021736` requirement-bound affine task handoff: complete.
- `202609021420` immutable `v0.1.16`: complete.
- older blocked, terminated, superseded, recovery, and release records: historical.
- no active remote source, publication, deployment, migration, or recovery mandate was found.

Reconcile any checkout-local work before proceeding. The source/public split was deliberate historical
state, not automatic publication work. This mandate deliberately activates one consolidated release
after five complete source-only slices; it creates no recurring feature-to-release rule.

The supplied root `AGENTS.md` is byte-identical to the inspected file, blob
`57a9241e23cd355c7a2272621c679d2955f24e73`. Preserve legitimate later policy rather than overwriting it.

## 2. Objective, final state, and outcomes

Mode: **implementation vertical slice at the distribution boundary**.

This is release implementation, proof, publication, and closure, not another planning pass.

**Final state:** the exact reconciled product `0.1.21` source snapshot, containing the already completed
`0.1.17` through `0.1.21` slices and no independent product change, is the one immutable latest
`v0.1.21` `x86_64-unknown-linux-musl` release; its anonymous copied binary freshly satisfies current
authoring, maintained application/service, target, package, integrity, and cleanup contracts through
both exact and latest downloads, while `v0.1.16` and every predecessor remain unchanged and no
deployment or hosted application state is touched.

The bottleneck is public reach. Source contains affine task handoff, identity-preserving extraction,
the incremental catalog, structured interactive sessions, and exact HTTP routes; anonymous latest
still selects `v0.1.16`. This campaign adds no graph meaning. Tags, archives, manifests, receipts,
assets, and attestations remain derived distribution identity and evidence.

Result meanings:

- `complete`: REL21-1 through REL21-7 freshly pass; immutable latest is exactly `v0.1.21`;
  post-publication truth is committed and pushed; the tree is clean or preserved work is exact.
- `blocked`: the objective remains coherent but a required local, hosted, publication, anonymous,
  credential, setting, or recovery gate cannot close safely. Preserve the repository's blocked-status convention.
- `terminated`: `0.1.21` is incoherent, needs an unrelated semantic repair, cannot preserve immutable
  predecessors, or cannot meet the supported distribution contract in this campaign.
- `superseded`: later work already published an equivalent or stronger immutable release and closed proof.

Continue through non-material path, symbol, count, command, or contract drift after correcting current
owners. Stop before changing product meaning to rescue publication, weakening proof, overwriting
unfamiliar work, moving a public identity, or performing an unauthorized mutation.

Non-goals: new graph/language/CLI/compiler/runtime/storage/application/deployment semantics; a version
beyond `0.1.21`; backfilled `v0.1.17`–`v0.1.20`; another target; installers or registry packages;
signing/reproducibility claims; deployment or hosted migration; live relay access; parameterized
routes or middleware; Nostr or outbound WebSocket; million-owner compiler/build admission; or a
follow-on patch after a content defect crosses the remote-tag boundary.

## 3. Reconciliation and feasibility gate

Before production edits:

1. Read applicable instructions, this complete mandate, the six predecessor terminal records,
   and current status, architecture, roadmap, release, verification, security, generated,
   maintained-consumer, and evidence owners relevant to distribution.
2. Capture branch, `HEAD`, tree, index/worktree/untracked/ignored state, remotes, upstream/divergence,
   recent commits, stashes, supplied/unfamiliar work, toolchain, target facilities, credentials
   boundary, and root-owned artifact constraints.
3. Fetch without rewriting history. Refresh `main`, tags, releases, open work, queued/running workflows,
   and any partial `v0.1.21` state.
4. Reconcile later source, release, recovery, deployment, or implicit in-flight work. Preserve all
   unrelated work and stashes.
5. Compare the supplied `AGENTS.md` with the checkout. Install it only if it remains the current
   constitutional file; otherwise preserve and report legitimate later policy.
6. Confirm root version, expected tag, executable-owned target/archive policy, release workflow,
   immutable-release setting, and current receipt identities.
7. Freeze the exact maintained standard and `lkjournal` semantic `HEAD`, package, generated artifact,
   and deployment-descriptor identities before release-only edits.
8. Inspect every live release consumer of current distributed, stateful, outbound, service, session,
   route, extraction, affine, catalog, and target receipts. Historical evidence may keep predecessors;
   live predicates may not.
9. Confirm that current full and target-admission owners exercise every public feature advertised for
   `v0.1.21`, including copied-binary `extract.function`, affine handoff, structured sessions, exact
   routes, and bounded catalog recovery.
10. Discover authoritative commands from current owners. Current executable policy outranks examples.

The gate may refine contract numbers, predicate fields, evidence shape, release-note wording, commands,
and test placement. It may not reopen product semantics or add a second verifier/publication path.

A release-path correction is in scope only when a workflow, verifier handoff, receipt predicate,
release document, or package metadata lags an already accepted owner. Cut all live consumers over
once, prove material predecessor rejection, and delete stale release-only compatibility. Do not accept
both generations merely to pass CI, and do not advance a contract without an encoding or behavior change.
If all live owners are current, make no ceremonial source change beyond archiving the mandate and later closure.

Stop before production edits if current evidence reveals conflicting active authority, an unclosed
irreversible campaign, unrecoverable partial publication, or a product defect requiring a separate
semantic campaign.

Narrow checkout questions:

- What exact local work, stash, and upstream relation must be preserved?
- What are the live receipt identities, and are any release predicates stale?
- Do current full/target owners already bind every required copied-binary feature?
- What exact candidate, package, verifier, artifact, receipt, and workflow identities result?
- Are immutable releases enabled, the existing tag-confirmation variable available, credentials
  noninteractive, and release concurrency clear?
- Does any local or remote `v0.1.21` tag, draft, release, asset, attestation, or run already exist?

These questions refine facts, not architecture.

## 4. Release contract

The typed graph remains sole editable program authority; `GraphRepository` remains the normal
accepted-authority writer. Operational data, queues, objects, deployment descriptors, and release
objects remain separate authorities.

Do not reimplement the terminal source slices:

- `0.1.17`: exact requirement-bound consume-only affine handoff into a private task helper.
- `0.1.18`: bounded identity-preserving `extract.function` and maintained migration.
- `0.1.19`: rebuildable incremental segmented catalog with packs and `HEAD` still canonical.
- `0.1.20`: graph-owned structured interactive sessions over bounded WebSocket transport.
- `0.1.21`: finite stable exact method/path route owners replacing the universal HTTP target port.

Current product metadata, specifications, decisions, generated projections, maintained graphs,
artifacts, descriptors, and verification policy are release inputs. Release-only work must preserve
their accepted meaning and exact bytes unless their executable owner requires deterministic
regeneration. Unexpected semantic or maintained-artifact drift is a stopping condition.

The sole supported target remains executable-owned `x86_64-unknown-linux-musl`. The exact candidate
must prove static linkage by inspection, run in both pinned userlands, and pass all current target
oracles. A target name or linker flag is not evidence.

First-party tooling owns target admission, deterministic packaging, strict extraction, verifier
handoff, application acceptance, metadata, publication, and recovery. Workflow shell only orchestrates.
The release contains only canonical inventory and checksums. Its annotated `v0.1.21` tag selects the
exact release-source commit `R`; the release is non-draft, non-prerelease, immutable, and latest.

Before publication, a hosted no-checkout job binds exact handoff/verifier/candidate/source identities
and freshly runs current transferred application oracles. After publication, anonymous exact-tag and
latest paths each perform independent extraction and behavioral proof. Byte equality supplements,
never replaces, both runs.

The prior one-million-owner result remains source capacity evidence. Do not rerun it ceremonially or
relabel it as candidate, compiler, build, service, deployment, or production proof. The final candidate
must still pass current bounded catalog healthy/recovery verification.

All runtime and release acceptance leaves semantic authority unchanged and cleans temporary projects,
binaries, archives, roots, listeners, workers, sessions, objects, queues, logs, and credentials.

Claims remain limited to observed target and behavior. Do not claim sandboxing, multi-tenant isolation,
encrypted storage, signing, reproducibility, general provenance, universal Linux portability,
production deployment, plaintext confidentiality, or a service level.

## 5. Implementation and cutover order

1. Reconcile checkout/external state and classify clean release, legitimate partial state, additive
   recovery, supersession, or unsafe conflict.
2. Archive this exact mandate and reconcile `AGENTS.md`.
3. Freeze semantic, package, generated, application, artifact, target, workflow, and release identities.
4. Audit live release consumers against executable receipt owners and normative verification.
5. Apply only necessary release-path corrections and narrow tests; change no accepted graph,
   maintained application, target behavior, or public operation.
6. Regenerate only owner-required outputs; require equality for maintained semantic artifacts expected stable.
7. Assemble release-source commit `R`. Root version remains `0.1.21`; prose still says unreleased;
   every release-bound input is stable.
8. Run narrow iteration checks, then one fresh full source profile.
9. Build/admit exact `R` candidate; prepare twice; strictly verify deterministic package and negatives.
10. Push `R` safely and require refreshed `R == origin/main`.
11. Run one canonical `publish=false` dry run for `v0.1.21`; require publication jobs skipped.
12. Refresh irreversible preconditions; create one annotated tag, bind the existing non-secret
    tag-object confirmation variable, and push only that tag.
13. Observe canonical immutable publication and post-release acceptance; never upload manually.
14. Perform any independently required anonymous transport repetition from fresh roots.
15. Create evidence/current-truth commit `E`; update only owners changed by public availability.
16. Append the terminal record, safely push `E`, and finish clean or name preserved work.

Any change to source, workflow, verifier, target policy, package, normative verification, generated,
or maintained inputs invalidates downstream receipts that bind it. `E` must not change such inputs.
If it must, it is not evidence-only closure.

## 6. Success conditions

### REL21-1 — Exact release source

One commit `R` contains this mandate, strictly necessary release corrections, and proving tests.
Product remains `0.1.21`; expected tag is `v0.1.21`; five source campaigns remain terminal;
maintained semantics/artifacts are unchanged; live release predicates require current owners;
material predecessor inputs reject; no intermediate tag exists; and the tree is clean except named work.

### REL21-2 — Fresh source and maintained proof

After `R` stabilizes, one authoritative full profile is `fresh passed` with no reused, skipped,
unavailable, failed, or unrun required gate. Current owners prove public discovery/authoring,
copied-binary lifecycle and extraction, affine transfer, sessions, routes, catalog recovery,
standard and `lkjournal` check/build/service behavior, independent oracles, negatives, unchanged
authority, deterministic generated output, and cleanup.

### REL21-3 — Admitted candidate and deterministic package

The exact `R` candidate is freshly built, directly proven static, admitted in both pinned userlands,
and passed by every current candidate-bound oracle. Candidate, verifier, manifest, archive, checksums,
notices, and receipts agree on source/target/capabilities/digests. Two preparations are byte-identical.
Foreign, malformed, extra, missing, duplicate, nonregular, symlink, traversal, corrupt, stale,
mismatched, and extraction-conflict inputs reject without partial output; cleanup completes.

### REL21-4 — Hosted no-publication proof

After `R` equals refreshed `origin/main`, one canonical dry run with `publish=false` and tag
`v0.1.21` is `fresh passed`. It rebuilds/admit/packages exactly `R`, validates handoffs by artifact
identity/digest, and runs current no-checkout application oracles. Publish/post-release jobs are
skipped, and no tag, release, asset, attestation, variable, deployment, or hosted application state changes.

### REL21-5 — Immutable publication

Immediately before mutation: `R == origin/main`; work is reconciled; dry run is fresh; no conflicting
run exists; `v0.1.21` is unused; immutable releases are enabled; the existing non-secret confirmation
variable and noninteractive credentials are available. One annotated tag selects `R`; only that
variable is set to its tag-object SHA; only that tag is pushed. The canonical isolated workflow creates
one immutable non-draft/non-prerelease latest release with canonical assets/attestations and no predecessor mutation.

### REL21-6 — Anonymous exact/latest proof

Fresh anonymous exact and latest downloads independently verify checksums, GitHub asset digests,
attestations, strict extraction, manifest/source/target/candidate identity, static linkage, inventory,
current transferred behavior, and cleanup. Exact/latest bytes agree only after both behavioral runs.
Authenticated or previously extracted bytes cannot substitute.

### REL21-7 — Evidence and closure

Track bounded evidence at `docs/evidence/202609041625-v0.1.21-consolidated-release.json` or the current canonical equivalent.
Record source/tree/tag/workflow/release/asset/attestation/candidate/package/verifier/receipt identities,
classifications, material resources, negatives/recovery/cleanup, external actions, limitations, and
ignored raw pointers. Then update current status/release/README/architecture/roadmap/generated or
maintained prose only where public state requires it; append the terminal record; create evidence-only
commit `E`; push it safely; close source/public state while deployment remains unchanged and unclaimed.

## 7. Verification and evidence

| ID | Real boundary | Independent oracle or negative | Retained proof |
|---|---|---|---|
| REL21-1 | reconciled checkout and staged `R` | owner/version search, identity freeze, predecessor rejection | Git identities, staged diff, changed/stable owners |
| REL21-2 | fresh full profile, copied binary, maintained repositories/services | semantic/reference, extraction, affine, route, session, catalog and protocol oracles | receipts, classifications, digests |
| REL21-3 | ELF, two userlands, target oracles, strict package verifier | linkage inspection, hostile inputs, twice-built equality | candidate/package/verifier/manifest bytes and SHA-256 |
| REL21-4 | hosted dry-run and downloaded handoffs | artifact metadata/digests, no-checkout execution, no-publication observation | run/attempt, artifact IDs/digests, receipts |
| REL21-5 | remote tag and isolated publication | refreshed preconditions, tag target, asset digest and immutability | tag, run, release, assets, attestations |
| REL21-6 | independent anonymous exact/latest downloads | checksums, attestations, strict extraction, separate behavior and byte compare | download digests, receipts, cleanup |
| REL21-7 | evidence/current-truth commit and terminal record | release-input exclusion and final reconciliation | evidence digest, `E`, final worktree/upstream |

Classify every gate exactly `fresh passed`, `reused passed`, `skipped`, `unavailable`, `failed`, or
`not run`. Completion requires `fresh passed` for every required gate. Historical evidence stays
historical.

Use implementation-disjoint reference proof for pure semantics. Execute live effects once through
production and compare through protocol/state oracles. Prove catalog/cache rebuild where current
owners require it without treating derived state as authority. Distinguish source, target, workflow
artifact, immutable release, anonymous binary, deployment, and running service.

Raw logs, binaries, archives, downloads, projects, roots, inventories, and receipts belong under
ignored storage, preferably `.artifacts/campaign/202609041625/` and established first-party roots.
Tracked evidence contains bounded summaries, exact identities, classifications, digests, resource
units, and pointers. Observe only material bytes, times, CPU/RSS when owned, bounded logs, temporary
storage, requests/tasks/sessions, transfers, publication count, and cleanup; infer no broader claims.

## 8. Read map and authorization

Start with applicable `AGENTS.md`; this mandate and six predecessor terminal records; manifests and
toolchain; README; current status/architecture/roadmap/release/security/verification owners; relevant
release-identity, exact-route, session, extraction, affine, catalog, and deployment decisions/specs;
`.github/workflows/release.yml`; first-party check/release/target/distributed/stateful/outbound/service/
extraction/catalog/raw-WebSocket/evidence owners; public CLI/service tests; generated projections;
`packages/standard/`; and `applications/lkjournal/`. Expand only by exact live dependency or failure.

Use explicit staging, inspect each staged diff, fetch before push, and end clean except exact preserved work.

| Action class | Authorization |
|---|---|
| scoped release-only edits/tests/docs and owner-generated output | **authorized after reconciliation** |
| local builds/tests, copied binaries, pinned containers/userlands, loopback services, temporary data and cleanup | **authorized** |
| coherent local commits `R` and `E` | **authorized** |
| normal fast-forward push of `R` and `E` | **conditional** on fetch, fresh proof, staged review, safe upstream, preserved work, and existing noninteractive credentials |
| workflow dispatch | **conditional** for one final-`R` `publish=false` dry run and one unchanged-input transient retry |
| exact annotated `v0.1.21` and canonical immutable release | **conditional** on REL21-1–4 and refreshed preconditions |
| existing non-secret tag-confirmation variable | **conditional** only to bind the exact `v0.1.21` tag-object SHA immediately before tag push |
| unchanged-input publication recovery dispatch | **conditional** only after a pushed tag and proven idempotent transient failure |
| intermediate/other tags or releases, manual assets, installers, registry packages, or new targets | **forbidden** |
| deployment/reconfiguration, hosted schema/data/queue/object/database mutation, or live relay contact | **forbidden** |
| issues/PRs/general settings/permissions/secrets/credentials/other variables or interactive authentication | **forbidden** |
| reset/clean/restore, rebase/history rewrite, force push, stash mutation, retag, or published/unfamiliar-state mutation | **forbidden** |

Before remote tag creation, repair by a new `R` and rerun invalidated proof. An unpushed local tag may
be removed only after proving it never reached a remote. After tag push, never move/delete it.
Retry unchanged input only for a proven idempotent orchestration failure. A content/source/workflow/
verifier/package/target defect after tag push leaves state preserved and this campaign blocked;
additive patch recovery requires another mandate. Never mutate an immutable release or asset.
Missing authentication blocks only the external action, not safe local proof or an honest terminal result.

## 9. Final report, terminal record, autonomy, and stopping

The final report must give: start/final Git, worktree, upstream, stash, and preserved-work boundaries;
commits and purposes; every changed/stable product, semantic, package, application, artifact, target,
candidate, verifier, package, receipt, tag, workflow, release, asset, attestation, deployment, and
operational identity; public copied-binary path; confirmation of no new graph meaning; every gate and
classification; required oracle/negative/recovery/interruption/deletion/cleanup/clean-incremental
results; evidence paths/digests and resource observations; deviations and nonclaims; every external
action performed or not; final source/release/deployment split; and smallest next candidates.

Append one concise `## Terminal record`. Use `complete`, `blocked`, `terminated`, or `superseded`,
following current top-status convention. Record time, start/final non-self-referential identities,
REL21 dispositions, evidence classifications/pointers, publication or recovery result, limitations,
preserved work, and next candidates. Do not rewrite this body, paste raw logs, or require the record
to contain its own commit/blob identity.

Codex owns routine factoring, tests, command discovery, release-note wording, and coherent commit
shape. Continue through benign warnings, optional cache failures, unavailable convenience tools,
and non-material drift when required gates and safety are unchanged. Do not ask for routine confirmation.

Stop safely if checkout authority conflicts, a required gate remains unavailable/failed after
reasonable isolation, irreversible preconditions are absent, unfamiliar work would be overwritten,
an existing public identity differs from intended source, or dependency closure would require a new
product campaign.
