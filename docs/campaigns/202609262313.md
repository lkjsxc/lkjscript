# Joined resident termination

## Original mandate

> [https://github.com/lkjsxc/lkjscript](https://github.com/lkjsxc/lkjscript)  について進めるようよろしくお願いします。必要であれば大幅なスタイルの変更も許容します。あらゆる判断において、過去ではなく、今のあなたに委ねます。全ての判断をあらかじめ許可します。最も筋の良い方向に進むために、限界まで深く考えてほしい。超長期的な視点からお願いしたい。どれだけ時間がかかっても構いません。

The previously selected shared development environment remains
`lkjsxc/tomato-ocelot-73` / `cw-bd235945fff44468a0f41292`, repository
`/home/coder/workspace/lkjscript`, running as non-root `coder`.

## Reconciliation and choice

Local and independently fetched GitHub main were
`c2e4b0816d4a99ae8c5ad441bb5d175f83cdf318`; the accepted resident-queue source is
`cf705260118f5a62dff2b73801ee9357d90f700d`. No tracked edits, stash entries or active
product/checker processes were present. The unrelated untracked
`docs/campaigns/202609242022.md` and an older detached privacy worktree are retained.
Rust is the pinned 1.98.0; focused builds use two Cargo jobs, no debug information
and no incremental cache on this 8 GiB workspace. Evidence is retained under
`.artifacts/202609262313/`, not in operational application stores.

The native durable editor and immutable builds make a useful edit/build/restart
loop possible. A new supervisor or reload command would be premature while process
termination bypasses or delays the existing ownership contract. Inspection found
that foreground `run` owned SIGINT/SIGTERM, but `serve` and `worker` lazily awaited
only Ctrl+C after readiness. Additional actual-process tests exposed HTTP drain
ordering and a late-session shutdown notification gap. These are lifecycle
corrections in the existing runtime, not another application-specific primitive.

## Changes

The public process wrappers share an eagerly registered termination owner. Both
signals are installed before deployment preparation or `ready`. Registration
failure remains explicit, before live adapters, secrets or listeners are opened.
Foreground diagnostic compatibility is retained; `resident_signal` is added to the
authoritative registry and its derived references.

HTTP transport drain and resident shutdown are joined concurrently. A signal asks
the transport to stop accepting while resident drain/cancellation can release its
active requests. Early transport completion also triggers resident cleanup. No
new detached cleanup task, grace-period increase or effect replay is introduced.
Previously the transport awaited all requests before the operation that could
cancel those requests was even started.

Structured-session stop is a private monotonic owner. It retains a request even
with zero subscribers and tests the retained value for each new wait. An upgrade
or initial writer can no longer require a second stop request merely because it
subscribed late. Parent ownership, phase validation and reader/writer joining are
unchanged. The five direct unit cases cover absent and late subscribers, pending
clones, cancelled waits and repeated requests after completed waits.

The public suite uses an ordinary native fixture, copied product executables,
empty PATH, removed authoring graphs, loopback ephemeral listeners and signals
sent only to owned unreaped child PIDs. It covers SIGINT and SIGTERM immediately
after readiness for HTTP, worker and interactive runners, admitted infinite HTTP
work, and an actual upgraded peer left connected during shutdown. Successful
cases require one stopped event, successful exit and zero remaining tasks,
permits and session scopes. Connected sessions must complete, not merely fail
and disappear. A separate native web-editor case retains committed revisions and
stale-write rejection through two SIGTERM stops and a final SIGINT control.

## Observations retained while developing the tests

`before.log` is a harness encoding error: the newly transferred native fixture was
not valid UTF-8. It is not a product counterexample. After correcting only that
fixture, `before-utf8.log` reproduces the unchanged HTTP process exiting on signal
15 after `ready`, with no joined `stopped` event (0 passed / 1 failed).

The first signal-only repair passed six idle cases. Its initial active-HTTP probe
raced the sole worker's admission with its own health request. The revised probe
first rules out an early busy-request response and still requires an independent
503 `resident_overloaded` before signalling. With that positive admission witness,
both signals reached the test's 15-second shutdown deadline against a 60-second
application deadline. This exposed the transport-before-cancellation ordering;
the test deadline and application quotas were not weakened to hide it.

The initial session fixture returned a phase-invalid `continue` on shutdown. Its
terminal cases were corrected to `finish` with absent state, and the public oracle
now requires one completed session and zero failed sessions. An initial copied
predecessor SIGINT session run exited successfully but reported one failed session;
it is retained in `before-session.log` and is not a successful-session claim.
The first signal-only repair also produced `normalized_session_shutdown_incomplete`
under the upgrade race. Deterministic stop-owner tests supplement that scheduling-
sensitive observation; a single successful network run is not proof against races.

`stop-state.log` records the first five passing monotonic-stop unit tests.
`after.log`, `after-state.log` and subsequent focused logs retain unsuccessful as
well as corrected process observations. Final source acceptance is recorded below
only after the complete frozen-input check has actually finished.

## Compatibility and delivery boundary

No graph, package, artifact, deployment or durable-data format changes. No quota,
stack, concurrency, session phase, authorization, origin, secret or conditional-save
rule is relaxed. Synchronous preparation is not made preemptible. Forced process
termination and crashes cannot promise cleanup; stopping does not imply rollback,
retry, automatic replacement, migration or live backup.

At reconciliation and a later independent read, the public latest release remained
v0.1.48 (`397062403`). The abandoned v0.1.49 promotion, immutable tags/assets and
previous release-selector state are untouched. The previous session's v0.1.50
candidate dispatch had not executed. This campaign does not treat version text,
a local passing subset or a main push as finalized-candidate/public acceptance.

## Acceptance and integration

`joined.log` records 11/11 public process cases passing in 86.89 seconds. The two
admitted infinite-HTTP cases each reported one cancellation, zero remaining tasks
and permits, and approximately 101.4 ms in the shutdown receipt under the selected
100 ms drain grace. This is a bounded local witness, not a universal latency claim.
Both connected-session cases reported one completed session and zero failures,
active sessions or pending handshakes. The copied native editor retained revisions
1 and 2, rejected a stale save with 409, and joined all three process stops.

Two isolated negative controls were detected by the unchanged unit tests:
`mutation-send.log` records 2 passed / 3 failed after replacing retained publication
with receiver-dependent send; `mutation-changed.log` records 1 passed / 4 failed
after retaining publication but waiting only for a subsequent change. Each failed
with exit 101 at its one-second observation deadline. Both mutations were reverted;
no weakened assertion or larger timeout was used.

`restored.log` records 5/5 unit tests passing again after both reversions,
workspace/all-targets Clippy with warnings denied, a successful product build,
and public generation followed by exact verification of all eight maintained
guides. No intentional mutation remains in the source. The existing branch read
showed main unprotected; the allowed repository rulesets collection, including
parents, independently returned an empty array. No protection setting was changed.

Frozen-input full verification and mainline delivery of this additional source
are still pending at this source-recording stage.
