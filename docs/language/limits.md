# Limits

## Purpose

Hardcoded AI-friendly source budgets for this language version.

## Constants

Hardcoded in the runtime (`MAX_*` in core `limits` module):

- `MAX_NEST_DEPTH`: 8
- `MAX_CHILDREN`: 16 (raised for typed `name Type` param pairs)
- `MAX_TOKENS_PER_FILE`: 384 (raised for mandatory `sig/` forms)
- `MAX_DIR_CHILDREN`: 8
- `MAX_TOPLEVEL_FORMS`: 8
