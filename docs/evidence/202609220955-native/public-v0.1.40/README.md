# Public v0.1.40 native-authoring observation

On 2026-09-22 UTC, the official exact v0.1.40 bootstrap was acquired anonymously from
`https://github.com/lkjsxc/lkjscript/releases/download/v0.1.40/install.sh` and installed into an
owned prefix. The installed executable, SHA-256
`bfe2f8bf643930db1d3d37d73093391d332d2fdce0d8814d3fa0ca463e53bdd7`, was the only product used
for this lifecycle. Product source is `1cdaf72888a1f46359a6d38956050747335f8e32`;
the [delivery campaign](../../../campaigns/202609221813.md) owns authenticated producer,
immutable publication and configured exact/latest verification. The installer reports payload
integrity and neutral publication metadata; that receipt alone is not publication authentication.

This is a bounded final-public-binary observation. It supplements the maintained installed pair
without changing its frozen verifier. It is not a new independent-agent usability experiment,
maintained application adoption, lkjscript-implemented tooling or compiler self-hosting.
Shell scripts only launch public operations, copy literal inputs and assert independent expected
results. They neither generate declarations nor write accepted storage.

All consumer commands ran outside the compiler checkout in the pinned Debian image
`sha256:70509c95d1857a3704c0a5d92ee2e0adac95f612a9386889d70760bfd7c1ebba`, with no Python,
Node, Rust or Cargo, cleared environment, no network, read-only image, no capabilities,
and user 1001:1001. The installed prefix was mounted read-only. Each foreground container was
joined and removed. The actual host kernel was Linux 7.2.3-arch1-2, x86_64; this does not extend
the release's admitted platform boundary.

## Creation, reconstruction and reviewed change

[create-packing.lkjc](create-packing.lkjc) keeps the predecessor's literal units body byte-for-byte;
only its request base came from public status on a fresh minimal project. It creates `Parcel`,
`recommend`, four graph tests, a component/port and the `packing-advice` target.
Creation was planned and accepted; all four initial tests passed with differential equality,
and the initial bundle built successfully. Every independent initial case matched the literal
[expected table](expected.tsv).

The original input and complete creation plan were moved outside every later consumer mount.
The old bundle also moved to the separate detached directory. Only the accepted project and
generated public guides remained available during canonical reconstruction. Public module and
target selectors produced [the fresh draft](draft-untouched.lkjc). Untouched planning reported
`outcome=unchanged`, `semantic-change=false`, `owner-recreation=false`, `publication=none`.
Initial HEAD, untouched-plan HEAD and rejected-edit HEAD were all
`rev_c551e2952ab75367fa651ed3b7ed9ad5a5be5e0b000be7a60b53688ec86b1ea9`.

[The explicit edit](edit-packing.lkjc) changes the function and corresponding fragile/chilled test
together. Comparing it with the literal fresh draft shows the entire authored change. The public complete review plan
contains seven affected non-expression owners, but only the function and intended test have
different before/after stored objects. Five affected dependents have identical objects.
[The comparison](023-reviewed-owner-assertions.out) records these disjoint sets.

Before acceptance, [a copy](tampered-packing.lkjc) replaced only the newly introduced result
literal with `Tampered recommendation.` while presenting the original review token.
[Apply rejected](024-tampered-preacceptance.log) with `change_request_commitment_mismatch`, exit 2.
Public status retained the exact original HEAD and the distinguishing original case still executed
as `Use an insulated carton.` The unchanged reviewed request was then accepted for the first time;
this was not an already accepted idempotent retry.

Edited HEAD is `rev_23127fe5bf62deddd42bc97349990b878f34350a317d13e9d7534212c61a086f`.
Its four graph tests passed with differential equality, its bundle built, and each of the four
independently specified cases matched:

| Fragile | Chilled | Initial | Edited |
|---|---|---|---|
| false | false | Use a standard carton. | Use a standard carton. |
| true | false | Use a padded carton. | Use a padded carton. |
| false | true | Use an insulated carton. | Use an insulated carton. |
| true | true | Use an insulated carton. | Use a padded insulated carton. |

Public owner snapshots compare equal for all 13 named identities, including all seven declarations,
module, fields, port, target and function parameter. The before/after public function parameter
definition is also identical. Expressions were replaced/retired as expected; they are not a claim
of unchanged expression identity.

After the authoring project moved to an unmounted retained directory, deployment execution with
only old/new runtime-dependent bundles and their literal descriptors returned the distinguishing
old/new strings. [Old](035-detached-old.log) and [new](036-detached-new.log) runs report production
execution, no differential replay, `remaining_tasks=0` and empty cleanup failures. The final owned
container query was empty and bootstrap temporary storage was empty. The accepted project,
bundles, installation, literal inputs and all full logs remain retained for diagnosis; no application
data migration or rollback occurred.

## Actual failures and recovery

Two observation-harness invocations used unsupported command combinations and were repaired:

- [The initial function lookup](014-find-function.log) omitted the required module `--parent`:
  `query_parent_required`, exit 2. Fresh public module and function IDs supplied the correct
  declaration and parameter lookup parents. Existing accepted creation was not replayed.
- [Untouched planning with `--output`](019-untouched-plan.log) returned `cli_usage`, exit 2,
  because unchanged requests have no publishable plan to export. Omitting that option produced
  the required unchanged result.

A host resume-script mistake also reached the launcher's transcript-overwrite guard, exit 65,
[before a product command ran](resume-create-and-draft.log). The already recorded version
invocation was removed from the resume suffix. All failed originals remain outside checkout;
the small failure transcript presentations above preserve their commands and results. These are separate from the intentional
tampered-request negative and are not hidden in a successful aggregate. No product repair or new
candidate was required. No other acceptance suite, Orbloam experiment or application migration
was run for this witness.

## Retained evidence

[commands.md](commands.md) gives the exact command route, isolated environment probe and recovery
lineage. Tracked literal inputs, descriptors, small output presentations and launch/assertion scripts
are sufficient to inspect the bounded observation without a new evidence format. Complete public
plans, generated guides, unabridged transcripts, script originals and binaries remain at
`/home/coder/workspace/lkjscript-native-public-20260922`; no configured expiry was observed for
that local directory. Only trailing whitespace was removed from tracked `.log` presentations and
named-identity output presentations for repository whitespace policy; their external originals are
unchanged. Literal requests, descriptors and executed scripts are unchanged copies. Hosted handoff
expiry is a separate campaign fact. The corrected creation
script is a reproduction aid, not a claim that the original one-shot script passed unchanged.
