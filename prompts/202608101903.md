# lkjscript — Complete the Source-Free Early-Return Vertical Without Reintroducing Platform Complexity

## Task status
This file is an execution prompt for a coding agent.
It is not a repository authority artifact.
Do not commit this prompt.
Do not copy it into repository documentation.
Do not create a prompt archive, checkpoint file, handoff file, completion capsule, or task ledger from it.
Read the current checkout before trusting any implementation detail written here.
The observed baseline while this prompt was prepared was:
- repository: `lkjsxc/lkjscript`;
- default branch: `main`;
- observed commit: `24677150795990b356d1f4e6d26e1508acf7e17a`;
- observed commit title: `Complete source-free imperative loop authoring`;
- observed date: 2026-08-10;
- no open GitHub issues were found;
- no open GitHub pull requests were found.
The checkout may have advanced.
Current code, tests, manifests, and accepted specification files override stale observations in this prompt.
If the checkout has advanced, preserve the objective and invariants rather than mechanically reproducing an obsolete patch shape.

## Executive objective
Complete one dependency-closed product vertical:
> Allow a program to be constructed source-free through the semantic workspace with an early `return`, then inspect, compile, execute, and clean it up through the same canonical semantics as the source importer.
The retained current consumer is:
`crates/lkjscript-app/tests/fixtures/ownership-control.lkjscript`
That fixture:
- creates an owned byte vector;
- borrows it;
- observes the borrow;
- performs an early return of `7`;
- requires the owned value to be cleaned exactly once;
- is part of the representative runtime baseline;
- currently cannot be authored completely through the source-free expression draft solely because source-free `return` is absent.
The principal implementation goal is therefore narrow:
- add source-free early-return construction;
- reuse the existing HIR `Return` semantics;
- reuse existing ownership, cleanup, memory-plan, SSA, bytecode, VM, and baseline-native behavior;
- expose truthful semantic workspace inspection;
- prove source-import and source-free convergence;
- update current documentation;
- stop before unrelated control-flow or platform work.

## Required result
At completion, a coding agent must be able to:
1. create an empty semantic workspace;
2. create `main` or a function with a declared result type;
3. fill its body using a flat `ExpressionDraft` that contains an early return;
4. place that return inside ordinary source-free constructs such as a sequence, `let`, conditional, mutable-local body, or `while` body where language semantics permit it;
5. receive deterministic rejection for invalid return values;
6. inspect the resulting return node through existing workspace APIs;
7. project the resulting semantic program without source text;
8. compile the complete snapshot directly without rendering or parsing source;
9. execute it through the selected production path;
10. observe the exact expected result;
11. observe exactly-once cleanup of owned values on the return path;
12. compare the source-free result with the imported fixture through semantic and executable evidence;
13. retain stable identities for unaffected semantic entities and nodes across ordinary edits;
14. preserve transaction failure atomicity;
15. run the repository's complete verification boundary.

## Strategic intent
The long-term project is an AI-primary, statically typed, memory-safe, collector-free, high-performance programming language and implementation.
For this task, AI-primary means:
- semantic construction does not require source text;
- the agent proposes typed semantic operations;
- deterministic implementation machinery decides validity;
- incomplete state remains explicit;
- errors are structured and actionable;
- queries expose semantic facts rather than parser artifacts;
- projections are reviewable but non-authoritative;
- compilation consumes semantic authority directly;
- check and run remain distinct;
- output remains compact and deterministic;
- the implementation does not create a speculative service or protocol merely because an agent is the consumer.
AI-primary does not mean:
- model inference inside correctness boundaries;
- probabilistic validation;
- one-model-specific token tricks;
- a new agent orchestration platform;
- a daemon without measured need;
- JSON wrappers around display strings;
- exposing every private compiler fact;
- preserving textual notation as semantic authority;
- adding abstractions merely because future agents might use them.

## Autonomy
You are authorized to make autonomous technical decisions.
You may:
- make incompatible changes;
- revise provisional Rust APIs;
- revise provisional source notation;
- revise specifications when a better coherent semantic rule is required;
- reorganize modules or crates when directly justified;
- delete obsolete code and tests;
- replace representations;
- remove compatibility layers;
- rename public or private APIs;
- change fixtures;
- change package locks or generated contract data when the active implementation requires it.
Backward compatibility is not an objective.
Do not preserve:
- old source syntax;
- old serialized bytes;
- old command shapes;
- old internal Rust APIs;
- old module topology;
- old fixtures;
- old names;
- old projection spelling;
- old diagnostic wording;
- old package locks;
- old documentation;
merely because they existed.
This authorization is not permission for gratuitous redesign.
Prefer the smallest coherent root-cause correction.
Preserve unrelated user work, credentials, external data, remote history, and host state.

## One-turn scope
This prompt is deliberately multi-turn aware.
Complete one coherent vertical in this turn.
The default vertical is source-free early return.
Do not attempt to solve the entire language.
Do not automatically begin the next roadmap item after this vertical passes.
Do not use remaining context or time as a reason to expand scope.
Stop after:
- the vertical is complete;
- displaced unsupported claims are removed;
- tests and documentation agree;
- final verification has completed or an exact environment blocker is recorded;
- the next highest-leverage problem is identified.
A smaller complete vertical is better than a larger partial redesign.

## Explicit non-goals
Do not implement source-free `break`.
Do not implement source-free `continue`.
Do not implement a general control-transfer target framework.
Do not add labels.
Do not add arbitrary return target identities.
Do not add nested-function control routing.
Do not add exceptions.
Do not add effect handlers.
Do not add continuations.
Do not add generators.
Do not add coroutines.
Do not add async semantics.
Do not add an interpreter just for semantic drafts.
Do not add a second mutable semantic AST.
Do not add a second production execution engine.
Do not add a source renderer merely to compile the new draft.
Do not render and reparse semantic state.
Do not add a wire protocol.
Do not add a CLI semantic editing protocol.
Do not add persistence.
Do not add a transaction journal.
Do not add a semantic database.
Do not add a daemon.
Do not add a warm service.
Do not add sessions.
Do not add a scheduler.
Do not add process cells.
Do not restore deleted runtime-platform crates.
Do not add a resource topology.
Do not add collaboration.
Do not add a CRDT.
Do not add incremental compilation.
Do not add incremental semantic analysis.
Do not add a cache framework.
Do not add invalidation machinery beyond truthful existing domains.
Do not add a query engine.
Do not add a generic graph database.
Do not add a universal traversal framework.
Do not add a generic control-flow builder.
Do not add a rewrite DSL.
Do not add a plugin system.
Do not add a descriptor registry.
Do not add a diagnostic framework solely for return.
Do not add a new identity namespace solely for return.
Do not add a return entity.
Do not add a return handle.
Do not add a return plan.
Do not add a return witness.
Do not add a return schema version.
Do not add a return compatibility mode.
Do not add a `v2` draft tree.
Do not add an edition.
Do not add a feature flag for old versus new behavior.
Do not add an engine-selection option.
Do not add a JIT threshold.
Do not add tier transition state.
Do not add deoptimization.
Do not add PGO.
Do not add an optimizing runtime.
Do not add forced native execution.
Do not modify lower runtime layers preemptively when they already support return.
Do not broaden the source-free operation catalog unless the early-return convergence fixture proves a current missing operation.
Do not redesign generics.
Do not redesign ownership.
Do not redesign package semantics.
Do not redesign the file format.
Do not rename the language.
Do not self-host.
Do not add broad platform targets.
Do not optimize around Brainfuck.
Do not create arbitrary file-length, directory-width, module-count, or plan-count limits.
Do not replace an arbitrary language quota with a larger arbitrary quota.
Do not weaken a test to accommodate an invalid design.

## Authority order
Resolve conflicts by claim dimension.
Use this practical order:
1. the active task owns the immediate objective;
2. the root `AGENTS.md` owns repository engineering procedure;
3. accepted files under `docs/spec/` own intended language and semantic-workspace contracts;
4. code, tests, manifests, schemas, and command definitions own current checkout behavior;
5. `docs/status.md` summarizes current implementation and gaps;
6. `docs/architecture.md` explains current responsibilities, flow, ownership, and trust boundaries;
7. `docs/performance.md` owns performance method and compact evidence;
8. `docs/roadmap.md` owns planned ordering only;
9. sparse accepted decisions own durable rationale;
10. Git history owns superseded implementation and prose.
A roadmap item is not an architectural commitment.
A previous prompt is not a specification.
A previous assistant answer is not a specification.
A benchmark establishes only its workload and protocol.
A test proves only the behavior it exercises.
When two claims disagree:
- classify the claim;
- inspect the owning artifact;
- inspect executable evidence;
- decide which artifact is stale;
- update or delete stale material in the same coherent change.
Do not create a new authority layer to resolve the disagreement.

## Permanent project constraints relevant to this task
The `.lkjscript` extension is the only fixed source-format property.
Text is an importer and presentation convenience.
Text is not semantic authority.
Paths are not semantic identity.
Spans are not semantic identity.
Formatting is not semantic identity.
Parser nodes are not semantic identity.
Dense compiler indexes are not stable public identity.
One semantic representation owns mutable program facts.
Derived HIR is ephemeral.
Derived HIR is not a second mutable authority.
Compilation must not require source rendering.
Compilation must not require source parsing after semantic construction.
Incomplete semantic state is valid editing state.
Executable fallback meaning must never hide behind a hole.
Transactions publish one coherent final state or nothing.
Failure must preserve the prior published snapshot.
Failure must preserve allocator state.
Failure must preserve deterministic future identities.
Public survivor identities remain stable when semantic meaning survives private compaction.
Removed identities are tombstoned.
Old immutable snapshots remain queryable.
Ordinary execution is collector-free and non-tracing.
Copy, move, borrow, and deterministic cleanup remain semantic laws.
Program validity follows semantic laws, not arbitrary project-selected size quotas.
Coarse host resource policy is distinct from language validity.
One complete generic production execution route must remain available.
Optional native execution may decline only before effects.
Once native entry begins, its result or failure is final.
Effects must never be replayed through fallback.

## Observed current architecture
At the observed baseline, the active product flow is:

```text
Workspace::empty OR verified text/path import
    -> one partial-capable SemanticProgram authority
    -> optional source/presentation attachments
    -> immutable WorkspaceSnapshot
    -> revision-checked atomic semantic transactions
    -> compile_snapshot completeness gate
    -> one source-optional complete HIR
    -> ownership and memory planning
    -> typed SSA lowering and verification
    -> bytecode lowering and validation
    -> ExecutableProgram
        -> check: discard before execution state or effects
        -> run: one baseline-native attempt, otherwise unchanged VM execution
```

At the observed baseline:
- `ExpressionDraft` is flat and non-recursive;
- physical node order is non-semantic;
- draft child identities are transaction-local dense handles;
- public semantic nodes receive stable workspace identities;
- `DraftNode` supports literals, loads, moves, borrows, calls, selected operations, conditionals, sequences, immutable locals, mutable locals, assignment, `while`, products, enums, and matches;
- source-free `return` is absent;
- HIR already has `ExprKind::Return { value }`;
- workspace `NodeKind` already has `Return`;
- source import already validates and lowers `return`;
- ownership, memory planning, SSA, bytecode, VM, and native paths already execute imported early return;
- source-free sequence already rejects an expression after a divergent expression;
- `Type::Never` already represents non-normal control;
- the ownership-control fixture already runs through the production baseline and requires unique cleanup.
Treat those as observations to verify, not as instructions to duplicate code blindly.

## Core hypothesis
The working hypothesis is:
> Source-free early return is an authoring-surface gap, not a missing runtime architecture.
A minimal correct change should therefore primarily touch:
- the public flat draft node model;
- draft shape traversal;
- draft lowering and validation;
- focused semantic workspace tests;
- convergence tests;
- query/projection assertions where existing generic indexing is insufficient;
- current status, architecture, workspace specification, performance evidence, and roadmap wording.
It should not require a new subsystem.
It should not require a new lower IR.
It should not require a new runtime instruction.
It should not require a new native mechanism.
It should not require a new ownership model.
It should not require a new process boundary.
Falsify this hypothesis with focused evidence before expanding scope.

## Reversal condition
Reverse or redesign the proposed local approach if focused evidence proves any of the following:
- existing HIR return semantics cannot represent the source-free case without fabricated meaning;
- imported and source-free return semantics genuinely differ in an intended way;
- current return cleanup is incorrect;
- current return typing is inconsistent across source and semantic paths;
- return nodes cannot preserve semantic identity under existing reconciliation;
- a lower-layer correctness defect blocks the selected fixture;
- the flat draft model cannot represent return without recursive or source-shaped authority;
- direct compilation would require render/reparse;
- a proposed helper would duplicate semantic authority;
- a proposed abstraction is larger than the selected vertical.
If a lower-layer defect is exposed:
- fix it only when it is a prerequisite of this vertical;
- keep the fix dependency-closed;
- add a focused regression test;
- do not use the defect as permission for unrelated rewriting.

## Stop condition
Stop work when all of the following are true:
- source-free `return` can be proposed through one flat draft;
- invalid return drafts fail deterministically and atomically;
- imported and source-free ownership-control programs converge on selected semantic facts;
- both produce result `7`;
- owned cleanup is exactly once;
- direct source-free compile records zero source loading and zero parsing;
- workspace queries and projection report return truthfully;
- no new parallel authority exists;
- no unrelated control-transfer form was added;
- documentation is current;
- the roadmap advances to measurement rather than speculative machinery;
- full relevant verification has completed or exact environment limits are documented;
- the repository is left coherent;
- one cohesive commit exists if committing is permitted;
- publication occurs only if explicitly requested.

## Initial repository inspection
Before editing, inspect:
- current branch;
- current `HEAD`;
- working-tree status;
- untracked files;
- upstream/tracking state;
- recent commits;
- root `AGENTS.md`;
- `docs/authority.md`;
- `docs/spec/language.md`;
- `docs/spec/workspace.md`;
- `docs/status.md`;
- `docs/architecture.md`;
- `docs/performance.md`;
- `docs/roadmap.md`;
- workspace manifests;
- current compiler module topology;
- current tests around source-free drafts;
- current tests around return;
- current ownership-control runtime baseline.
Do not destroy unrelated work.
Do not reset another agent's changes.
Do not clean untracked files.
Do not checkout over local edits.
Do not force a branch.
If the worktree is dirty:
- classify each change;
- preserve unrelated changes;
- include only dependency-closed task changes in any commit;
- avoid rewriting files merely to normalize formatting around unrelated work.

## Required code tracing
Trace the current producer-consumer chain for early return.
At minimum, locate and understand:
- source parsing of `return`;
- source resolution of `return`;
- the declared callable result type;
- HIR `ExprKind::Return`;
- HIR expression-child traversal;
- iterative clone and destruction behavior;
- effect calculation;
- ownership checking of return;
- memory-plan production for return;
- memory-plan verification for return;
- SSA lowering of return;
- bytecode representation of return;
- bytecode validation;
- VM execution;
- baseline-native lowering and verification;
- workspace `NodeKind::Return`;
- workspace index classification;
- node expected-type facts;
- node actual-type facts;
- node effect facts;
- child containment;
- projection rendering;
- semantic diff behavior;
- replacement and hole lifecycle;
- source-free draft shape validation;
- source-free draft scope validation;
- source-free draft lowering order;
- final draft-root expected-type validation;
- transaction failure atomicity;
- parser/source-load counters;
- production execution metrics.
Document the trace only in working notes unless an owning repository document genuinely needs the result.
Do not add a committed architecture inventory.

## Required fixture trace
Read the exact current contents of:
`crates/lkjscript-app/tests/fixtures/ownership-control.lkjscript`
Verify its semantics.
At the observed baseline, its structure is equivalent to:

```text
main returns i64
let b = new-byte-vector(2)
do
    byte-slice-length(borrow b)
    return 7
```

Do not assume this summary replaces the file.
Verify:
- which byte-vector constructor operation is canonical;
- how the shared borrow is represented;
- when the borrow ends;
- when the owner must be released;
- whether native execution is expected;
- how unique cleanup is measured;
- which semantic facts existing convergence helpers compare;
- whether source import assigns source origin where source-free construction must assign semantic origin.
The origin difference is expected.
Do not normalize away truthful provenance.

## Primary design constraint
Represent return as a normal flat draft node.
The likely shape is conceptually:

```rust
DraftNode::Return {
    value: DraftNodeId,
}
```

The exact Rust spelling is yours to choose.
Do not add:
- a return target entity;
- a return target node;
- a return label;
- a return continuation;
- a return plan;
- a return destination handle;
- a return identity allocator;
- a return registry;
- a generic control-transfer enum merely for symmetry;
- a `BreakOrContinueOrReturn` abstraction without multiple current consumers and clear simplification.
The lexical return target is the callable whose body owns the draft.
The current root/callable context should provide that meaning.

## Canonical return semantics
Source-free return must converge with source-imported return.
Verify the accepted language semantics, then preserve at least these rules:
- return has exactly one value;
- the return value is evaluated before control transfers;
- the return value itself must not already have type `never`;
- the return value must exactly match the callable's declared result type;
- no implicit numeric conversion is introduced;
- no source-only inference is introduced;
- no display-string type comparison is used;
- the return expression has type `never`;
- the return expression carries the child expression's effects;
- control transfer itself does not fabricate a host effect;
- cleanup occurs for live owned values exited by the return;
- active borrows are ended consistently with canonical ownership rules;
- cleanup is attempted on all relevant return exits;
- no owned value is released twice;
- no effect before return is repeated;
- no expression after return in the same ordered sequence is reachable;
- an `if` branch of type `never` may join with a normally returning branch according to canonical control typing;
- a return nested in a `while` body exits the callable, not merely the loop;
- a return nested in an immutable or mutable local body exits through the canonical cleanup path;
- a return in a generic function checks against the canonical binder-bearing result type;
- a return in `main` checks against `main`'s declared result type.
Do not invent a second interpretation.

## Callable result-type authority
A nested return cannot use only the immediate contextual expected type.
The immediate context may be:
- `unit` for a non-final sequence item;
- a branch join;
- a loop body;
- a local body;
- a match arm;
- another control context.
Return must validate against the declared result type of the owning callable.
Locate the existing canonical callable signature facts.
Use one local helper only if it removes real duplication and has a clear owner.
The helper should:
- accept the current semantic program and callable/root identity;
- distinguish `main` from ordinary functions;
- retrieve the canonical declared result type;
- handle generic function result types correctly;
- fail deterministically for stale or wrong-kind roots;
- avoid parsing a rendered function type;
- avoid depending on source text;
- avoid exposing private dense identities publicly.
Do not cache this fact without measurement.
Do not store a duplicate mutable return type on every draft node.

## Final body compatibility
Inspect how a complete draft body is checked against its expected result.
Do not simply weaken exact type checking so that any mismatch passes.
Use existing canonical control compatibility.
A body whose normal result has the declared type is valid.
A body that diverges through return may be valid when language control typing permits `never`.
A body that produces the wrong non-divergent type is invalid.
A return value with the wrong type is invalid even if the outer body is divergent.
A return whose child is already divergent is invalid if that is the canonical source rule.
Keep source and source-free behavior equal.

## Draft graph requirements
The draft remains flat and non-recursive.
Adding return must preserve:
- checked conversion from `DraftNodeId` to host index;
- one connected root;
- no cycles;
- no reused child;
- no disconnected node;
- physical node order independence;
- iterative traversal;
- stack-safe validation;
- deterministic lowering order;
- deterministic child order;
- no semantic meaning in vector position after publication.
Update all exhaustive handling of `DraftNode`.
At minimum, inspect:
- child count;
- child enumeration;
- shape validation;
- lowering action construction;
- scope traversal;
- definition-event traversal;
- debugging or formatting;
- public exports;
- tests with exhaustive matches.
Do not suppress compiler exhaustiveness as a shortcut.
Do not add wildcard branches that hide future missing handling.

## Scope requirements
Return introduces no binding.
Return opens no scope.
Return closes no scope by itself.
Its value sees the same lexical scope as the return site.
A return value may refer to:
- callable parameters;
- visible immutable locals;
- visible mutable locals where a legal load or move is used;
- visible payload bindings;
- visible functions and type parameters according to current rules.
A return value may not refer to:
- a local declared after the return;
- a local outside its lexical scope;
- a binding from another match arm;
- a foreign workspace identity;
- a stale identity;
- a hidden compiler match temporary;
- a deleted entity;
- an invisible type parameter.
Reuse existing draft scope validation.
Do not add a return-specific scope table.

## Effects requirements
Return must participate in current effect analysis.
Verify:
- child effects are preserved;
- `never` does not erase effects;
- source-free snapshots with holes retain unknown effects;
- filling a hole with return recomputes effects;
- replacing return with a pure value recomputes effects;
- replacing a pure value with return recomputes effects;
- node query effects equal compiler semantic facts;
- projection reports existing named effects consistently;
- imported and source-free selected effects converge;
- no new effect bit named `return` is added unless the language specification already defines control transfer as one.
Do not confuse control divergence with a host capability effect.

## Identity requirements
Return is a semantic node, not an entity.
It receives an ordinary stable `NodeId` when published.
Its value child receives an ordinary stable `NodeId`.
No new public identity type is required.
Verify:
- filling a body hole preserves the hole/root node identity according to current fill semantics;
- new descendants receive new identities;
- unaffected ancestors and siblings preserve identities;
- replacing the return value follows ordinary targeted replacement semantics;
- introducing a hole over the return removes its subtree;
- deleted descendants are tombstoned;
- stale removed node identities fail;
- old snapshots remain queryable;
- private compaction does not appear as public movement;
- no dense preorder index escapes as identity;
- projection labels are never accepted as identity.

## Transaction requirements
Every source-free return edit must use the existing transaction boundary.
Success publishes exactly one revision.
Failure publishes nothing.
On failure, verify preservation of:
- the exact prior `Arc` where current tests expose it;
- revision;
- namespace;
- entity allocator;
- node allocator;
- hole allocator;
- tombstone generations;
- free lists;
- diagnostics;
- projection;
- deterministic next successful IDs;
- imported attachments;
- current semantic program.
Invalid return cases must not partially create semantic nodes.
Allocation failure, where current test mechanisms support it, must remain atomic.
Do not add a secondary staging transaction for return.
Do not mutate published indexes in place.

## Query requirements
Existing query APIs should expose return through ordinary node semantics.
Verify or add focused coverage for:
- `workspace_node` reports `NodeKind::Return`;
- containment orders the return value as its child;
- actual type is `never`;
- expected type is truthful for the return site;
- the child actual type is the callable result type;
- the child expected type is truthful;
- effects are reported;
- owner is the correct callable or containing semantic node according to existing model;
- references inside the return value are indexed;
- calls inside the return value are indexed;
- generic substitutions inside the return value are indexed;
- dependencies inside the return value are indexed;
- pagination remains deterministic;
- no private HIR or SSA identity is exposed.
Do not add a dedicated `ReturnView` unless existing generic node facts cannot express a current required fact.
A dedicated view is probably unnecessary.
Prove that before adding one.

## Projection requirements
The concise semantic projection must remain truthful.
Verify:
- return is rendered distinctly from an ordinary sequence value;
- its child is rendered in semantic order;
- `never` is not misrepresented as the callable result;
- the return value type remains inspectable;
- source-free projection needs no source attachment;
- source and source-free projections may differ in provenance or review labels without differing in semantic meaning;
- projection is deterministic;
- projection remains iterative on user depth;
- projection failure on allocation is explicit;
- projection does not become compiler input.
Do not build a complete source renderer for this task.

## Diff requirements
Semantic diffs should follow existing structural rules.
Verify:
- filling a body hole with a return reports the expected hole transition;
- the return node and value descendants are reported as created where current diff semantics require;
- replacing the return value reports targeted descendant changes;
- replacing an ordinary value with return reports control replacement truthfully;
- introducing a hole over return reports removed descendants and the new hole;
- references and calls in the return value are rewired in the diff where applicable;
- private compaction emits no fabricated movement;
- unrelated entities do not appear changed;
- deterministic ordering is preserved.
Do not add a return-specific diff protocol.

## Ownership requirements
The selected fixture exists primarily to test ownership control.
Trace the exact live-owner and loan state around return.
The source-free path must preserve:
- byte-vector ownership;
- shared borrow legality;
- end of the borrow before or during the canonical return cleanup boundary as required;
- one release of the owned byte vector;
- no release while a borrow remains illegally active;
- no leak;
- no double release;
- no fallback re-execution;
- no cleanup omission on native entry;
- no cleanup omission in VM fallback;
- equal observable result.
Use the canonical ownership checker.
Use the canonical memory plan.
Use the canonical cleanup plan.
Do not special-case cleanup in the workspace layer.
Do not insert explicit synthetic `drop` source-free operations merely to make the fixture pass.
Do not hide an ownership defect by moving the return outside the owner scope.

## Runtime requirements
The source-free snapshot must compile through the production compiler path.
Do not invoke a test-only evaluator as the product result.
The SSA evaluator may remain an independent test oracle if already used.
The selected production execution behavior must remain:
- one complete generic VM route;
- one optional baseline-native attempt;
- decline before effects;
- no VM retry after native entry;
- exactly-once effects.
The ownership-control fixture is expected at the observed baseline to use baseline-native execution.
Verify current behavior rather than hard-coding a stale expectation.
If a valid source-free form causes native decline but the imported equivalent enters native:
- investigate semantic or lowering divergence;
- do not force native execution;
- do not weaken native validation;
- do not add an engine flag;
- do not claim convergence until the reason is understood.

## Source-free purity requirement
A source-free early-return construction and compile must invoke:
- source loading zero times;
- parsing zero times.
Use existing deterministic counters or test hooks.
Do not infer this from call topology alone when counters exist.
Do not create dummy source text.
Do not create a placeholder path.
Do not create a source hash.
Do not render the draft.
Do not feed the source importer.
Do not install fabricated source provenance.
Use honest semantic origin.

## Import convergence requirement
Build imported and source-free forms of the ownership-control program.
Compare semantic outcomes, not textual spelling.
At minimum compare selected facts that current convergence infrastructure can own truthfully:
- program completeness;
- callable kind;
- callable declared result type;
- normalized entity kinds;
- normalized stable semantic types;
- containment order;
- return node kind;
- return child type;
- references;
- dependencies;
- operation identities;
- borrow/move facts;
- named effects;
- selected memory obligations;
- bytecode for `main`, where existing tests compare it;
- runtime result;
- runtime path;
- native entry fact where stable for the selected baseline;
- cleanup result;
- retained-owner/loan state after teardown.
Do not require provenance equality.
Imported nodes may have source origin.
Source-free nodes must have semantic origin.
Do not normalize identity values across independent workspaces as if they were globally equal.
Use existing normalization helpers.

## Error requirements
Invalid return proposals must fail with existing structured error machinery.
Cover at least:
- wrong return value type;
- return child type `never`;
- disconnected return child;
- cyclic draft involving return;
- reused return child;
- stale binding in return value;
- foreign binding in return value;
- invisible binding in return value;
- deleted callee in return value;
- wrong-kind callee in return value;
- expression after return in one sequence;
- return embedded in a context whose surrounding normal type remains invalid;
- stale target node for replacement;
- stale hole for fill;
- stale base revision;
- allocation failure where supported.
Prefer existing variants such as:
- `TypeMismatch`;
- `InvalidDraft`;
- `InvalidTransaction`;
- `InvisibleEntity`;
- `WrongEntityKind`;
- `UnsupportedEdit`;
- `Host`;
- `Validation`.
Do not create a broad control-flow diagnostic taxonomy for one constructor.
A more specific structured error is justified only if:
- the producer knows a stable semantic fact;
- a current consumer uses it;
- existing error variants cannot express it without string parsing;
- tests demonstrate the value;
- the new shape does not duplicate type facts.

## Diagnostic wording
Keep messages:
- deterministic;
- concise;
- truthful;
- free of fabricated source ranges;
- free of parser terminology for source-free failures;
- free of private dense IDs;
- explicit about expected and actual semantic types;
- stable only where the public API promises stability.
Do not preserve obsolete wording for compatibility.
Do not parse display messages in tests when structured fields exist.

## Stack-safety requirements
Return adds one unary node.
Its implementation must not reintroduce native recursion on user-controlled depth.
Inspect all affected operations:
- draft shape validation;
- draft child traversal;
- lowering-order construction;
- scope validation;
- draft lowering;
- semantic clone;
- index construction;
- projection;
- diff;
- HIR derivation;
- ownership traversal;
- memory planning;
- destruction.
Reuse iterative infrastructure.
A focused deep test is justified only when it exercises a newly affected path not already protected.
Do not add a duplicate 20,000-level full-pipeline fixture merely for numerical symmetry.
Prefer:
- extending an existing deep draft geometry to include one return;
- adding a small-stack focused case;
- or proving the existing generic traversal covers the new unary child through code and a modest regression.
Add an ignored release stress only if it detects a real otherwise-uncovered recursion risk.

## Scale requirements
Do not add a return nesting limit.
Do not add a maximum number of returns.
Do not add a maximum sequence length.
Do not add a maximum control depth.
Do not add a maximum ownership cleanup count.
Do not add a maximum draft node count.
Do not use recursion depth as validation.
Use checked arithmetic.
Use checked conversion before host indexing.
Return count is not a semantic validity rule.
Resource exhaustion remains a host result.

## Performance requirements
Do not claim a performance improvement merely because the change is small.
Do not add incremental machinery.
Do not add caching.
Do not add parallelism.
Do not add a daemon.
The selected task should preserve complexity shape.
Where existing deterministic work counters exist, verify:
- each draft node is visited once by the relevant order traversal;
- return does not cause a whole-body rescan;
- return type lookup is constant or bounded by one existing callable lookup, not repeated per descendant;
- index construction remains one ordinary pass;
- ownership planning remains canonical;
- no source parsing occurs;
- no duplicate complete-HIR derivation occurs.
If a new scan is proposed:
- identify its input size;
- identify how often it runs;
- identify whether an existing map or root fact can replace it;
- reject an `O(nodes × returns)` design;
- reject an `O(functions × returns)` design when the owning callable is already known;
- prefer one direct root lookup.
Do not add a benchmark framework for this feature.
Use current harnesses.

## Documentation requirements
Update only owning active documents.
Likely files are:
- `README.md` if its concise implemented authoring surface lists the gap;
- `docs/status.md`;
- `docs/architecture.md`;
- `docs/spec/workspace.md`;
- `docs/performance.md`;
- `docs/roadmap.md`;
- root `AGENTS.md` only when this task also installs the supplied replacement policy and it remains accurate.
Do not change `docs/spec/language.md` merely to report implementation.
Early return is already normative intended language behavior at the observed baseline.
Change the language specification only if a genuine semantic correction is required.
For each document:
- describe current as current;
- describe target as target;
- remove the now-stale statement that source-free return is absent;
- retain `break` and `continue` as explicit gaps;
- do not imply a broad control-transfer framework;
- record convergence and cleanup evidence;
- record zero source loading/parsing;
- update roadmap `Now` to the next measured task only after return is complete;
- do not promote unmeasured incrementality;
- keep persistence and daemonization evidence-gated.
Do not add:
- a task report file;
- a completion document;
- an implementation diary;
- a global revision;
- a capability registry;
- duplicate status tables;
- a prompt archive;
- a handoff document.

## Roadmap transition
After the vertical is complete, the likely roadmap transition is:
`Now`:
- measure full recomputation under representative imperative edits and queries;
- record edit latency, query latency, allocation counts/bytes where practical, retained memory, invalidated work, and output volume;
- retain full recomputation unless measurements justify more machinery.
`Next` may continue to include:
- deliberate unresolved, ambiguous, conflict, and recovery states;
- one concrete public movement vertical only when a current use case exists;
- evidence-based representation and crate-boundary reassessment.
`Later` remains evidence-gated:
- persistence;
- crash recovery;
- collaboration;
- a warm semantic service;
- daemonization;
- broader platform products.
Do not implement those items in this task.

## File-change discipline
Do not assume every listed file must change.
Change a file only when:
- it owns an affected fact;
- exhaustive matching requires it;
- a focused test belongs there;
- stale documentation must be corrected;
- a generated lock or contract must be regenerated.
Avoid churn.
Do not reorder unrelated declarations.
Do not mass-rename unrelated symbols.
Do not reformat untouched modules manually.
Let Rustfmt own formatting.
Delete displaced unsupported checks rather than retaining dead compatibility branches.

## Expected primary code touchpoints
Verify current paths before editing.
Likely touchpoints include:
- `crates/lkjscript-compiler/src/workspace/draft.rs`;
- `crates/lkjscript-compiler/src/workspace/transaction.rs`;
- `crates/lkjscript-compiler/src/workspace/index.rs`;
- `crates/lkjscript-compiler/src/workspace/projection.rs`;
- `crates/lkjscript-compiler/src/workspace/query.rs`;
- `crates/lkjscript-compiler/src/workspace/tests.rs`;
- compiler public re-exports if the draft enum is exposed elsewhere;
- app or compiler convergence fixtures;
- performance harness assertions only if the existing workload needs a source-free companion.
Do not edit lower layers merely because they contain return handling.
First prove that they are missing something.

## Implementation sequence
Use this order unless evidence justifies a safer dependency order.

### Step 1 — establish baseline
Run focused existing tests for:
- source return;
- ownership-control execution;
- source-free imperative counted loop;
- source-free byte-vector borrow then move;
- source-free query/projection;
- transaction atomicity;
- source/parser counters.
Record exact commands and statuses in working state.
Do not commit raw logs.

### Step 2 — add the flat constructor
Add one return variant to the flat draft model.
Update child count.
Update child enumeration.
Compile immediately to discover exhaustive handling sites.
Do not silence those errors with wildcards.

### Step 3 — integrate traversal and scope
Update shape validation through generic child traversal where possible.
Update lowering order.
Update scope validation.
Confirm no definition event is introduced.
Confirm physical draft order remains irrelevant.

### Step 4 — resolve callable result type
Locate the canonical result type for the owning root.
Avoid display parsing.
Avoid source provenance.
Avoid duplicate mutable facts.
Handle both `main` and ordinary functions.
Handle generic result types.

### Step 5 — lower return
Take the child exactly once.
Reject a divergent child if source semantics reject it.
Require exact declared result type.
Construct canonical HIR `Return`.
Set type to `never`.
Preserve child effects.
Use semantic origin.
Do not lower through source syntax.

### Step 6 — preserve outer control typing
Verify:
- root return;
- return as non-final sequence terminator;
- branch return;
- local-body return;
- loop-body return;
- match-arm return if current source-free match construction supports it.
Use canonical `never` joining.
Do not weaken unrelated expected-type checks.

### Step 7 — inspect semantic facts
Exercise:
- node kind;
- actual and expected type;
- effect facts;
- containment;
- references;
- calls;
- projection;
- diff.
Modify generic indexing only if a fact is missing.

### Step 8 — prove ownership convergence
Construct the source-free ownership-control equivalent.
Compare it with the imported fixture.
Compile directly.
Execute.
Assert result `7`.
Assert unique cleanup.
Assert no retained owner or loan after teardown where current APIs expose it.

### Step 9 — prove failure atomicity
Attempt invalid return transactions.
Confirm the prior snapshot and deterministic future allocation remain unchanged.

### Step 10 — prove source independence
Assert zero source loading.
Assert zero parsing.
Assert one production compiler derivation after completeness.

### Step 11 — update documentation
Update owning current documents.
Remove stale gap claims.
Advance roadmap ordering only after implementation evidence exists.

### Step 12 — run final verification
Run focused tests first.
Then run the complete relevant boundary once after the final relevant edit.

### Step 13 — commit coherently
If repository policy and task permissions allow committing:
- exclude this prompt;
- exclude raw logs;
- exclude scratch files;
- include one dependency-closed change;
- use a result-oriented message such as `Complete source-free early return authoring`.
Do not push unless explicitly requested.

## Test design principles
Tests protect semantic meaning.
Do not test only enum variant presence.
Do not test only that code compiles.
Do not test only a display string.
Do not test only imported syntax.
Do not test only the happy path.
Do not test private dense IDs except in focused compaction invariants.
Do not duplicate a full fixture where a focused helper can express the same semantic comparison.
Prefer:
- one end-to-end convergence test;
- several narrow validation tests;
- one identity/lifecycle test;
- one ownership cleanup test;
- one source-independence test;
- one modest stack-safety test if needed.

## Required end-to-end convergence test
Add or extend a test with a name that communicates the result, for example:
`source_free_early_return_executes_cleans_up_and_matches_imported_semantics`
Use repository naming conventions.
The test should:
- import the ownership-control fixture;
- construct an equivalent source-free workspace from empty;
- create `main` with result `i64`;
- fill the body with a flat draft;
- create the byte vector through the canonical operation;
- bind it through the canonical immutable-local constructor;
- borrow it through the canonical borrow constructor;
- call or apply the canonical byte-slice length operation;
- place a return of `7` after the borrow observation;
- compile the complete snapshot;
- execute through the production path;
- assert exact result;
- assert selected semantic convergence;
- assert selected bytecode convergence when existing helpers own that comparison;
- assert cleanup;
- assert zero source loading and parsing for the source-free path;
- avoid comparing truthful source versus semantic origin as equal.

## Required simple return test
Construct the smallest source-free callable with:

```text
main returns i64
return 42
```

Assert:
- transaction success;
- snapshot completeness;
- return node kind;
- return node type `never`;
- return value type `i64`;
- direct compile;
- exact result `42`;
- no parser/source load.
This test isolates authoring from ownership geometry.

## Required ordinary function test
Create:

```text
function answer() -> i64:
    return 42
main() -> i64:
    answer()
```

Assert:
- function return validates against function result;
- main normal result validates independently;
- call indexing is correct;
- direct compile and execution return `42`;
- no source import is used.
This guards against accidentally validating every return against `main`.

## Required generic function test
Use the smallest already-supported generic declaration.
Conceptually:

```text
identity<T: copy>(value: T) -> T:
    return value
main() -> i64:
    identity<i64>(42)
```

Use current legal load semantics for a copy parameter.
Assert:
- the return value matches the canonical binder-bearing result type;
- no display or binder-name comparison is used;
- stable type-parameter identity remains queryable;
- exact call substitutions and witnesses remain correct;
- direct compile executes;
- source loading/parsing remain zero.
Do not broaden generic ownership/reference instantiation.

## Required branch test
Construct a conditional in a callable returning `i64`.
One branch should return early.
The other branch should produce the normal result or also return, according to the focused case.
Assert canonical control join behavior.
Useful cases include:

```text
if condition:
    return 1
else:
    2
```

and:

```text
if condition:
    return 1
else:
    return 2
```

Do not add implicit coercion.

## Required sequence test
Construct:

```text
do:
    observe()
    return 7
```

Assert the sequence has type `never`.
Then construct:

```text
do:
    return 7
    observe()
```

Assert deterministic rejection for an expression after a divergent expression.
The rejected transaction must be atomic.

## Required local cleanup test
Use the retained owned-byte-vector geometry.
Assert:
- the owner is initialized;
- the borrow is valid;
- return exits the local scope;
- cleanup occurs exactly once;
- no owner remains;
- no loan remains;
- result is `7`.
Do not replace this with a copy-only value.

## Required while-body test
Construct a supported `while` whose body returns from the callable under a reachable condition.
Assert:
- return exits the callable;
- it does not act like `break`;
- the loop itself is not given a new public target;
- ownership cleanup is canonical;
- control typing remains valid.
Keep the fixture finite and deterministic.
Do not add `break` or `continue` to support the test.

## Required match-arm test
Only when current source-free match construction makes the test local and non-duplicative:
- construct a non-generic enum;
- match it exhaustively;
- return from one or both arms;
- assert arm-local payload visibility;
- assert return targets the callable;
- assert match usefulness and exhaustiveness remain canonical;
- assert no hidden match local becomes public.
Skip this test if it would force unrelated pattern expansion.

## Required wrong-type test
For a callable declared `i64`, attempt:

```text
return true
```

Assert:
- structured expected type is `i64`;
- structured actual type is `bool`;
- no revision publishes;
- no IDs are consumed;
- later valid return receives deterministic expected IDs.

## Required divergent-child test
Attempt a return whose value already has type `never`.
Use an existing control form or nested return if the draft shape can express it without fabricating invalid shared children.
Assert the canonical source rule.
Do not allow:

```text
return (return 7)
```

merely because both outer and inner body contexts diverge.

## Required malformed-graph tests
Cover return in existing generic draft-shape test helpers:
- child index out of range;
- disconnected value;
- reused value child;
- cycle;
- root omission;
- impossible host index conversion where current constructors permit it.
Do not duplicate every graph test if a table-driven variant already covers all `DraftNode` child shapes.
Extend the table.

## Required stale and foreign tests
Use return value references to test:
- foreign workspace binding;
- stale deleted binding;
- wrong-kind entity;
- invisible local;
- cross-arm payload binding.
Assert existing structured failures.
Do not create return-specific identity validation.

## Required replacement test
Publish a return.
Replace only its value child.
Assert:
- return node identity survives if ordinary targeted replacement semantics say the parent survives;
- value descendant identity changes;
- result changes;
- diff is truthful;
- old snapshot still returns the old value;
- new snapshot returns the new value.

## Required hole lifecycle test
Publish a return subtree.
Introduce a typed hole over the return or its value according to current operation contracts.
Assert:
- removed descendants are tombstoned;
- no hidden executable return remains;
- snapshot becomes incomplete;
- compile stops before memory/SSA/bytecode;
- filling the hole restores completeness;
- direct execution uses only the filled semantic meaning.

## Required deletion interaction test
Where return value calls a user function:
- delete that callee without removing the return call;
- assert dependency rejection;
- replace the return value or delete the owning callable in the same final-state batch;
- assert order-independent dependency closure where current deletion contracts promise it.
Do not silently cascade independent callable deletion.

## Required query test
Select the return node by stable identity.
Assert:
- `NodeKind::Return`;
- actual type `never`;
- expected type is truthful;
- one child;
- child type;
- effects;
- enclosing callable;
- references from child;
- deterministic response;
- stale identity failure after removal.

## Required projection test
Project the source-free callable.
Assert stable semantic content rather than the entire accidental formatting string.
Where snapshot tests conventionally use exact projection, update the exact expected projection intentionally.
Ensure return is visible.
Ensure no source path is fabricated.

## Required diff test
Inspect the transaction outcome that fills a body hole with return.
Assert only stable public semantic changes.
Do not assert private preorder values.
Do not report private compaction as movement.

## Required production-path test
Run through the same compiled `ExecutableProgram` route used by the product.
Do not satisfy this requirement with only the SSA evaluator.
Assert native/VM selection only to the extent the current baseline owns it.
Assert no effect replay.

## Required quietness preservation
This feature should not make successful high-frequency commands noisy.
Verify existing:
- silent `check` success;
- deterministic `check --json`;
- intentional `run` output;
- complete failure detail.
Do not add progress logs.
Do not print semantic drafts during normal compilation.

## Test helper discipline
Reuse existing test helpers for:
- workspace creation;
- entity discovery;
- hole discovery;
- source import;
- semantic normalization;
- compile counters;
- VM execution;
- native metrics;
- cleanup observations;
- diff inspection;
- small-stack workers.
Add a helper only when:
- at least two focused tests need it;
- it owns one clear test concept;
- it is smaller than duplicated setup;
- it does not obscure semantic assertions.
Do not create a test DSL for one constructor.

## Lower-layer modification gate
Before editing any of these areas, produce focused evidence that the current implementation is wrong or incomplete for the source-free return vertical:
- HIR expression model;
- ownership checker;
- memory-plan producer;
- memory-plan verifier;
- SSA IR;
- SSA verifier;
- bytecode schema;
- bytecode validator;
- VM;
- native lowering;
- executable installer;
- host interfaces;
- package model.
A compile error caused only by adding a new exhaustive draft variant is not evidence that lower semantics need redesign.
A convergence failure may be evidence.
Fix the root cause, then add the smallest regression.

## Unsafe-code gate
The workspace currently forbids unsafe code except narrow mechanism crates with explicit local policy.
This feature should require no new unsafe code.
If you believe unsafe code is necessary:
- stop;
- explain the exact invariant;
- explain why safe code cannot express it;
- inspect repository unsafe boundaries;
- reconsider the design.
Do not broaden unsafe permissions for early return.

## Dependency gate
This feature should require no new third-party dependency.
Do not add one unless:
- it removes substantial audited machinery;
- local implementation would be materially riskier;
- a current consumer requires it;
- license and maintenance are acceptable;
- the dependency does not create a new authority.
A unary draft variant does not justify a dependency.

## Crate-boundary gate
Do not split or merge crates merely because this feature crosses modules.
A crate change is justified only by:
- a real trust boundary;
- an unsafe boundary;
- an independently useful public library;
- measured compile isolation;
- clear low coupling;
- removal of substantial adapter overhead.
Early return probably needs none.

## Representation gate
Do not introduce:
- a separate semantic control-flow graph;
- a return table;
- a per-return cleanup plan in the workspace;
- a serialized return descriptor;
- a return opcode if existing lower representations already encode it;
- a return-specific stable ID.
Use existing semantic and compiler representations.

## Naming guidance
Prefer direct names:
- `Return`;
- `return_type`;
- `callable_return_type`;
- `declared_result_type`.
Avoid inflated names:
- `SemanticControlTransferReturnDescriptor`;
- `ReturnPublicationWitness`;
- `ReturnLifecycleCoordinator`;
- `EarlyExitSemanticPlan`;
- `ControlDestinationRegistry`.
Use repository conventions.

## Comments guidance
Comments should explain:
- non-obvious semantic invariants;
- why callable result type, not local expected type, owns return validation;
- why no control target identity is needed;
- why a lower-layer path is reused;
- any subtle ownership cleanup boundary.
Do not narrate obvious syntax.
Do not copy this prompt into comments.
Do not add speculative future plans to code comments.

## Error-path audit
For each fallible allocation or conversion introduced:
- use `try_reserve` where repository policy requires it;
- propagate host allocation failure;
- avoid panic;
- avoid unwrap;
- avoid expect outside tests where lints forbid it;
- use checked arithmetic;
- use checked narrowing;
- preserve staged atomicity.
Do not turn allocation failure into semantic invalidity.

## Determinism audit
Verify deterministic:
- node traversal;
- child order;
- error selection;
- diff ordering;
- query ordering;
- projection;
- generated bytecode;
- cleanup order where promised;
- future identity allocation after failed edits.
Avoid dependence on `HashMap` iteration order in public results.
Use explicit sorting where current semantics require it.
Do not sort semantically ordered children.

## Exactness audit
Return type validation must not use:
- type display strings;
- debug strings;
- source names where stable nominal identity exists;
- product shape equality;
- enum shape equality;
- binder spelling;
- inferred source substitutions;
- private dense IDs presented as stable identities.
Use canonical internal type equality and exact semantic conversion.

## Origin audit
A source-free return node has semantic origin.
Its source-free descendants have semantic origin unless they preserve an existing imported subtree under ordinary edit rules.
Do not attach:
- dummy `SourceId`;
- line zero;
- empty path;
- fabricated hash;
- synthetic module;
- fake span.
Diagnostics without source facts must omit source fields.

## Capability audit
Return itself requires no capability.
Its value may contain operations with existing capabilities.
Preserve:
- exact effect propagation;
- package capability validation;
- no ambient authority;
- check without effects;
- run with intentional effects.
Do not add a control capability.

## Native boundary audit
If baseline-native supports imported return, source-free return should feed equivalent verified SSA.
Do not branch native eligibility on source origin unless source origin is semantically required, which would be suspect.
Inspect any divergence in:
- HIR origin handling;
- memory origins;
- SSA normalization;
- native group eligibility;
- cleanup metadata;
- generated entry result handling.
Fix only proven semantic divergence.

## VM boundary audit
The VM must receive validated bytecode.
Do not bypass validation for source-free programs.
Do not add a semantic-workspace execution shortcut.
Do not execute an incomplete snapshot.
Do not retry after native entry.

## Check-command audit
`lkjscript check` must remain effect-free.
The new source-free API is in-process at the observed baseline.
Do not add a CLI route merely to expose return.
If current tests compile a source-free snapshot directly, use that API.
No host environment or execution mapping should be constructed for check.

## Package audit
The source-free development snapshot does not need fabricated package files.
The required-package source fixture retains its current package validation.
Do not weaken package locks.
Do not make source-free tests pretend to be verified package imports.
Compare equivalent post-import semantics at the correct boundary.

## Performance evidence update
In `docs/performance.md`, record compact reproducible evidence.
A suitable addition or update should state:
- hypothesis;
- selected source and source-free workloads;
- semantic equivalence criteria;
- source/parser counters;
- cleanup criterion;
- production result;
- stack-safety evidence if newly measured;
- exact focused commands;
- no unsupported latency claim.
Do not paste raw benchmark logs.
Do not claim end-to-end speedup.
Do not claim lower API cost without measured bytes, lines, commands, or round trips.

## Focused commands
Discover exact current test names.
Likely focused commands will resemble:

```sh
cargo test --locked -p lkjscript-compiler \
  workspace::tests::<new_source_free_return_test> -- --exact
```

```sh
cargo test --locked -p lkjscript-compiler \
  workspace::tests::source_free_imperative_counted_loop_executes_and_matches_imported_semantics -- --exact
```

```sh
cargo test --locked -p lkjscript-compiler \
  workspace::tests::source_free_byte_vector_borrow_then_move_executes_and_cleans_up -- --exact
```

Use actual names.
Do not copy stale names without checking.
Run the smallest falsifying test during iteration.

## Product fixture commands
Verify the source fixture through current product commands.
Likely forms include:

```sh
./target/debug/lkjscript check \
  crates/lkjscript-app/tests/fixtures/ownership-control.lkjscript
```

```sh
./target/debug/lkjscript run \
  crates/lkjscript-app/tests/fixtures/ownership-control.lkjscript
```

Use the current CLI contract.
A successful check must not execute the fixture.
A successful check should remain silent in human mode.
The run should return the expected result through product metrics or current test harness.

## Full verification boundary
After the final relevant edit, run at least:

```sh
cargo fmt --all -- --check
cargo clippy --quiet --workspace --all-targets --all-features --locked -- -D warnings
cargo test --quiet --workspace --all-targets --all-features --locked
cargo build --quiet --workspace --release --locked
```

Then run retained container verification when available:

```sh
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
```

Run additional relevant checks when justified:
- focused ignored locked-release small-stack test;
- Miri for changed unsafe-adjacent code, though no unsafe change is expected;
- sanitizer or fuzz only if a malformed-boundary defect is touched;
- runtime baseline workload if existing harness can select ownership-control without excessive unrelated cost;
- documentation link or example checks if present.
Do not rerun an unchanged full suite merely to produce another summary.

## Verification failure handling
If a command fails:
- record the exact command;
- record exit status;
- preserve complete relevant diagnostics;
- distinguish code failure from environment failure;
- fix code failure before claiming completion;
- do not hide output with `|| true`;
- do not broadly filter diagnostics;
- do not claim success when the process did not complete.
If container verification is unavailable because of environment:
- report the exact blocker;
- report native verification that did pass;
- identify residual risk;
- do not fabricate a pass.

## Commit requirements
Commit only after the repository is coherent.
The commit should include:
- implementation;
- focused tests;
- convergence evidence;
- documentation updates;
- regenerated locks only when required.
The commit should exclude:
- this prompt;
- scratch notes;
- raw logs;
- raw benchmark samples;
- temporary generated fixtures;
- credentials;
- unrelated local changes.
Use one result-oriented message.
Do not create multiple partial commits merely to checkpoint.
Do not push unless explicitly requested.

## Final report requirements
The final coding-agent report must state:
- completed objective;
- root cause;
- principal design;
- why callable result type owns validation;
- why no new control target abstraction was added;
- files changed;
- paths deleted or displaced;
- source/import convergence result;
- ownership and cleanup result;
- source loading/parser counters;
- production execution result;
- relevant performance/work evidence;
- exact focused test commands and outcomes;
- exact full verification commands and outcomes;
- container verification outcome;
- environment-limited checks;
- documentation changes;
- commit hash and message if committed;
- push/publication state;
- next highest-leverage problem;
- explicit reason work stopped before beginning it.
Keep the report factual.
Do not paste full successful logs.
Do not reproduce this prompt.

## Next-turn queue
Do not implement this queue now.
After early return, the next evidence-oriented task is likely:

### Candidate A — measure full recomputation
Measure representative semantic edits and queries using source-free imperative programs.
Capture:
- transaction latency;
- index rebuild latency;
- query latency;
- projection latency;
- compile latency after edit;
- allocations and bytes where practical;
- retained snapshot memory;
- copied bytes;
- invalidated domains;
- output bytes and lines;
- agent command count;
- repeated work.
Keep full recomputation if results are acceptable.
Do not presuppose an incremental architecture.

### Candidate B — incomplete recovery states
Add unresolved, ambiguous, conflict, or recovery state one vertical at a time.
Each must be:
- explicit;
- queryable;
- editable;
- non-executable;
- free of fabricated fallback meaning.
Do not build a universal recovery framework first.

### Candidate C — public movement
Add movement only when a current semantic owner/order use case exists.
Do not confuse private compaction with public movement.
Do not create universal coordinates.

### Candidate D — crate and representation reassessment
Use profiles, dependency edges, and actual change coupling.
Merge only where boundaries no longer pay for themselves.
Do not reorganize for symmetry.

### Candidate E — broader products
Persistence, warm services, daemonization, GUI, web, game, database, scheduler, and distributed products remain later.
Require current consumers and measurements.
Do not restore deleted components as compatibility layers.

## Completion checklist
Before declaring completion, answer every item.

### Objective
- [ ] One source-free return constructor exists.
- [ ] It is flat and non-recursive.
- [ ] It targets the owning callable lexically.
- [ ] It reuses canonical HIR return.
- [ ] It does not add break or continue.
- [ ] It does not add a control framework.

### Typing
- [ ] Return child cannot already be `never`.
- [ ] Return child exactly matches the callable result.
- [ ] Generic binder-bearing results work.
- [ ] Main and ordinary functions are distinguished.
- [ ] Return expression type is `never`.
- [ ] Outer control joins remain canonical.
- [ ] Wrong non-divergent body types still fail.

### Effects
- [ ] Child effects are preserved.
- [ ] No fabricated return capability exists.
- [ ] Hole effects remain unknown until filled.
- [ ] Query effects are truthful.
- [ ] Imported/source-free selected effects converge.

### Ownership
- [ ] Owned local cleanup occurs on return.
- [ ] Borrow legality remains canonical.
- [ ] Cleanup is exactly once.
- [ ] No leak remains.
- [ ] No double release occurs.
- [ ] No effect is replayed.

### Semantic authority
- [ ] No source is rendered.
- [ ] No source is parsed.
- [ ] No dummy path exists.
- [ ] No fake source hash exists.
- [ ] Semantic origin is honest.
- [ ] Direct compilation consumes the snapshot.

### Identity
- [ ] Return uses ordinary `NodeId`.
- [ ] No return entity exists.
- [ ] Unaffected IDs remain stable.
- [ ] Removed descendants tombstone.
- [ ] Old snapshots remain queryable.
- [ ] Dense indexes remain private.

### Transactions
- [ ] Success publishes one revision.
- [ ] Invalid type fails atomically.
- [ ] Malformed graph fails atomically.
- [ ] Stale revision fails atomically.
- [ ] Future identity allocation remains deterministic.
- [ ] No partial index publication occurs.

### Inspection
- [ ] Node kind is `Return`.
- [ ] Actual type is `never`.
- [ ] Child type is correct.
- [ ] Containment order is correct.
- [ ] Effects are visible.
- [ ] References and calls are indexed.
- [ ] Projection is deterministic.
- [ ] Diff is truthful.

### Convergence
- [ ] Imported ownership-control fixture is retained.
- [ ] Source-free equivalent exists in tests.
- [ ] Selected semantic facts agree.
- [ ] Result is `7`.
- [ ] Cleanup evidence agrees.
- [ ] Parser/source-load counters are zero for source-free.
- [ ] Provenance differences remain truthful.

### Complexity
- [ ] No arbitrary quota was added.
- [ ] No recursion on user depth was added.
- [ ] No per-return whole-body scan was added.
- [ ] No cache was added.
- [ ] No daemon was added.
- [ ] No new dependency was added without proof.
- [ ] No lower subsystem was rewritten without evidence.

### Documentation
- [ ] Current status lists return as implemented.
- [ ] Architecture lists return in the source-free draft surface.
- [ ] Workspace specification current section is updated.
- [ ] `break` and `continue` remain gaps.
- [ ] Performance evidence is compact and reproducible.
- [ ] Roadmap advances to measurement.
- [ ] No duplicate authority document was added.

### Verification
- [ ] Focused happy-path test passes.
- [ ] Focused wrong-type test passes.
- [ ] Focused atomicity test passes.
- [ ] Focused convergence/cleanup test passes.
- [ ] Rustfmt passes.
- [ ] Clippy passes.
- [ ] Workspace tests pass.
- [ ] Release build passes.
- [ ] Container verification passes or exact environment blocker is recorded.

### Repository state
- [ ] Prompt is not committed.
- [ ] Raw logs are not committed.
- [ ] Unrelated work is preserved.
- [ ] One coherent commit exists if allowed.
- [ ] No push occurred unless requested.
- [ ] Final report identifies the next problem and stops.

## Final instruction
Exercise judgment.
Do not mechanically edit every file named here.
Do not optimize for the number of changed files.
Do not optimize for architectural novelty.
Do not optimize for preserving provisional APIs.
Optimize for:
- semantic coherence;
- memory safety;
- exact cleanup;
- direct source-free authorship;
- deterministic machine usability;
- stack-safe scale;
- one active architecture;
- minimal current machinery;
- truthful evidence;
- a complete dependency-closed result.
Complete the early-return vertical.
Prove it.
Update the owning truth.
Then stop.
