# Coding-Agent Prompt: Complete Source-Free Generic Enum Declaration Authoring

**Repository:** `lkjsxc/lkjscript`

**Inspection baseline:** `efcf802f77a4d69bba03ab466011db50176e12f1` (`Complete exact generic enum authoring`)

**Primary vertical:** source-free creation of exact, non-recursive, unbounded generic enum declarations, followed by direct value construction, exhaustive matching, compilation, and execution without source text.

**Execution mode:** multi-turn, evidence-gated, direct-cutover development.

## 0. Read This First

You are the lead coding agent for this repository.

The user grants broad authority to inspect, redesign, delete, and replace repository code.

Use that authority to make one coherent decision, not to expand the task.

Backward compatibility with prior repository revisions is irrelevant.

Past source bytes, Rust APIs, fixtures, package bytes, lock bytes, schemas, commands, and internal representations may break.

When you replace a representation, update every maintained producer and consumer and delete the old path.

Do not add compatibility aliases, adapters, migrations, dual readers, dual writers, editions, `v2`, `next`, feature flags, or legacy modes.

The user explicitly warns that previous work sometimes overengineered the repository by projecting distant ambitions into current code.

Treat that warning as a hard design constraint.

Do not build a daemon, persistence layer, protocol, generalized editor, generalized planner, generalized cache, generic declaration framework, or speculative platform component in this task.

The objective is not maximum diff size.

The objective is the smallest complete semantic cutover that materially improves AI-first authoring.

Long-term performance matters.

Do not pursue performance through speculative caching or services.

Preserve the complete generic VM route.

Preserve exact type, ownership, effect, capability, identity, and cleanup semantics.

This prompt is intentionally detailed so that you can spend less model context rediscovering settled facts.

Use it as a map, then verify every fact against the actual checkout.

Do not treat this prompt as repository authority when code or normative specifications disagree.

Install or use the companion root `AGENTS.md` as the durable repository policy.

Do not commit this timestamped prompt into the product repository unless the user explicitly asks for that artifact to be maintained there.

## 1. Immediate Operating Rules

1. Record the starting commit, branch, and `git status --short` before changing anything.
2. Read the applicable `AGENTS.md` once.
3. Verify whether the current checkout still contains the gap described here.
4. If HEAD differs from the inspection baseline, inspect the intervening commits before deciding that this task still applies.
5. If the vertical is already complete, verify it, repair stale documentation if necessary, report evidence, and stop.
6. Do not select an unrelated task merely because this prompt has become stale.
7. Preserve unrelated work in the worktree.
8. Never reset, clean, overwrite, or force-push work you did not create.
9. Search exact symbols before opening large files.
10. Read narrow line ranges and recent diffs instead of dumping entire compiler files.
11. Run one focused characterization before implementation.
12. Select one representation and stop comparing alternatives after evidence decides.
13. Implement one dependency-closed vertical.
14. Run focused tests before broad tests.
15. Run the complete verification boundary once near completion.
16. Create one cohesive local commit when the vertical is complete and verification permits it.
17. Do not push, open a pull request, or alter remote state unless explicitly requested.
18. At every turn boundary, leave a compact evidence-based handoff rather than a transcript.

## 2. Repository Facts to Verify

The following facts were observed at the inspection baseline.

Confirm them against the current checkout before relying on them.

- The active product is local package checking and execution plus an in-process semantic workspace.
- The `.lkjscript` extension remains the current source/package extension.
- The current line-oriented source notation is a provisional import format, not semantic authoring authority.
- Source import resolves once into the same partial-capable semantic workspace used by source-free construction.
- `SemanticProgram` is the staged semantic representation.
- `WorkspaceSnapshot` is the immutable published semantic authority.
- A successful transaction publishes one revision.
- A failed transaction publishes nothing and preserves allocator state.
- Public identities are opaque, namespace-scoped, generation-aware, and stable across private compaction.
- Old immutable snapshots remain valid after later revisions.
- Complete snapshots compile through `compile_snapshot` without rendering or reparsing source.
- The compiler path remains HIR, memory planning, verified SSA, validated bytecode, and generic VM execution.
- Baseline-native execution is bounded specialization and may decline before entry.
- The VM is the complete generic route.
- No VM retry is allowed after native entry begins.
- Speculative Phase 2 services and platform components were deliberately deleted.
- There is no supported daemon, RPC layer, persistence layer, journal, collaboration engine, scheduler, or generalized cache.
- Current measurements do not justify general incrementality or a warm semantic service.
- Source-free generic function declaration creation is implemented.
- Source-free non-generic enum declaration creation is implemented.
- Imported generic enum declarations are implemented by the canonical HIR/compiler/runtime path.
- Exact source-free values for existing generic enum declarations are implemented.
- Exact source-free enum-variant tests for existing generic enum declarations are implemented.
- Exact source-free exhaustive patterns for existing generic enum declarations are implemented.
- Those existing generic enum value and pattern paths compile directly and execute through the generic runtime route.
- Source-free generic enum declaration creation remains an explicit known gap.
- Broader generic pattern families remain a separate gap.
- Generic product declaration creation remains a separate gap.
- Generic ownership/reference instantiation remains a separate gap.
- Forwarding an unresolved caller type parameter remains a separate gap.
- Recursive source-free enum declaration creation remains outside the current `CreateEnum` shape.
- Nominal member addition, deletion, and reordering remain separate gaps.
- Broader movement remains evidence-gated and separate.
- Parser recovery remains evidence-gated and separate.

## 3. Concrete Evidence for the Selected Vertical

The selected vertical is not based on a distant platform idea.

It is based on a direct current authoring asymmetry.

- `crates/lkjscript-compiler/src/hir/enums.rs` already represents `EnumDefinition::type_parameters`.
- `EnumDefinition` already stores generic type parameter names and typed variant fields.
- The source analyzer already imports generic enum declarations.
- The source analyzer already validates generic enum parameter names and duplicates.
- The source analyzer already lowers enum field occurrences of those parameters to canonical types.
- The source analyzer already derives generic enum layout and recursion facts.
- `crates/lkjscript-compiler/src/workspace/model.rs` already has `EntityAddress::EnumTypeParameter`.
- `EntityKind::TypeParameter` already covers enum-owned binders.
- `crates/lkjscript-compiler/src/workspace/index.rs` already creates stable enum type-parameter entities.
- The index already owns containment from enum to type parameters and variants.
- The index already maps private enum type variables to stable public type-parameter entities.
- Deletion and compaction already understand `EnumTypeParameter` addresses.
- Queries and projections already expose imported generic enum binders.
- The latest completed vertical already proves exact generic enum values and patterns against imported declarations.
- The remaining dependency on imported source is declaration creation itself.
- A multi-turn AI author currently cannot start from `Workspace::empty` and create the generic enum used by that completed path.
- Closing that gap requires no new runtime, solver, protocol, process, or persistence boundary.
- Closing that gap reuses existing generic enum semantics rather than inventing another feature family.

## 4. Selected Product Operation

Implement this exact agent workflow:

1. Create an empty semantic workspace.
2. Submit one transaction that creates an unbounded generic enum declaration with ordered declaration-local type parameters.
3. Receive a published incomplete snapshot and deterministic semantic diff.
4. Obtain stable enum, type-parameter, variant, and field identities from the transaction result and ordinary queries.
5. Create `main` in a later transaction using ordinary stable identities where needed.
6. Fill the main body with an exact generic enum value keyed by the stable enum type-parameter identity.
7. Match the value with exact exhaustive enum patterns.
8. Compile the complete snapshot through `compile_snapshot`.
9. Execute the validated program through the complete generic runtime route.
10. Observe the expected result and exact cleanup behavior.
11. Perform all post-empty-workspace authoring, querying, compilation, and execution with zero source loading and zero parsing.

The reference semantic example is equivalent to an unbounded generic `maybe<t>` declaration with `none` and `some(value: t)` variants.

The complete source-free program should construct `some<i64>(42)`, exhaustively match it, and return `42`.

A second focused example should cover a nullary or phantom generic variant and prove that the exact concrete enum instantiation is retained even when no payload carries the parameter.

## 5. Exact Scope

- Add source-free creation of generic enum declarations to the existing semantic transaction path.
- Support zero or more ordered enum type parameters in the one canonical `CreateEnum` operation.
- Keep non-generic enum creation as the zero-parameter case of the same operation.
- Allow enum variant field declaration types to reference declaration-local enum type-parameter handles at arbitrary type depth.
- Resolve those local handles to stable enum-owned type-parameter entities before publication.
- Lower the resulting declaration into the existing canonical `EnumDefinition` representation.
- Reuse the existing generic enum type, value, pattern, substitution, usefulness, exhaustiveness, memory, SSA, bytecode, and VM paths.
- Expose the created binders through existing entity, containment, type, query, projection, diff, deletion, and old-snapshot behavior.
- Preserve current ownership/reference restrictions for source-free enum fields unless the focused source equivalence test proves the existing restriction itself is a blocking inconsistency.
- Preserve current generic ownership/reference instantiation restrictions.
- Preserve current direct compilation and generic runtime behavior.
- Use a direct API cutover.
- Update all maintained call sites, tests, examples, and documentation in the same change.
- Delete any displaced non-generic-only representation rather than wrapping it.

## 6. Explicit Non-Goals

- Do not implement generic product declaration creation.
- Do not implement enum type-parameter trait bounds.
- Do not invent an enum-bound witness model.
- Do not modify the language to add enum bounds.
- Do not implement self-recursive source-free enum creation in this vertical.
- Do not add a declaration-local nominal self handle.
- Do not implement mutually recursive source-free declarations.
- Do not implement generic declaration groups.
- Do not implement generic aliases.
- Do not implement associated types.
- Do not implement higher-kinded types.
- Do not implement implicit workspace type inference.
- Do not implement unresolved generic forwarding.
- Do not implement ownership/reference-bearing generic instantiation.
- Do not implement broader generic pattern families.
- Do not implement unresolved type names.
- Do not implement unresolved variants or fields.
- Do not implement parser recovery.
- Do not implement conflict state.
- Do not implement collaboration.
- Do not implement persistence or a journal.
- Do not implement a wire protocol or RPC interface.
- Do not implement a daemon or warm service.
- Do not implement a scheduler.
- Do not implement a plugin platform.
- Do not implement a generalized edit planner.
- Do not implement a generalized generic-declaration framework.
- Do not implement a registry for declaration kinds.
- Do not implement a serializer for same-build workspace values.
- Do not implement a compilation cache.
- Do not implement a general incremental dependency engine.
- Do not add async infrastructure.
- Do not add a new crate unless an existing hard boundary demonstrably requires it.
- Do not reorganize unrelated modules.
- Do not optimize unrelated compiler phases.
- Do not expand baseline-native eligibility merely because the new program is generic.
- Do not narrow the generic VM route.
- Do not preserve the old Rust `CreateEnum` shape for compatibility.
- Do not introduce `CreateGenericEnum`, `CreateEnumV2`, `next`, or an edition.
- Do not add a compatibility adapter for existing test call sites.
- Do not begin the next vertical after this one passes.

## 7. Required Initial Audit

Perform a bounded audit before editing.

The audit exists to verify dependency closure, not to reopen architecture.

1. Inspect the current root `AGENTS.md`.
2. Inspect `docs/status.md`, especially the semantic workspace and known-gap sections.
3. Inspect `docs/roadmap.md`.
4. Inspect the generic-enum portions of `docs/spec/workspace.md`.
5. Inspect the latest relevant commit diff beginning at `efcf802f77a4d69bba03ab466011db50176e12f1` or its current successor.
6. Locate `Edit::CreateEnum` and every maintained construction site.
7. Locate `EnumFieldDraft`, `EnumVariantDraft`, `TypeParameterDraft`, `DraftTypeParameterId`, and `DeclarationType`.
8. Locate `create_enum` and `create_function` staging code.
9. Locate the existing iterative declaration-type collection and conversion helpers.
10. Locate staged type-parameter resolution used by source-free generic functions.
11. Locate the source generic enum collector and field resolver.
12. Locate enum type-parameter indexing and public type projection.
13. Locate enum deletion ownership and compaction remapping.
14. Locate imported/source-free generic enum value and pattern convergence tests.
15. Locate source parser and source-load counters used by workspace tests.
16. Locate the smallest focused test command for workspace compiler tests.
17. Record exact files and symbols in a compact task ledger.
18. Do not read unrelated runtime, host, database, scheduler, or deleted platform history.

## 8. Characterization Before Implementation

Add or first write the smallest failing characterization that proves the gap.

The preferred characterization starts from `Workspace::empty_deterministic`.

- Attempt to describe `maybe<t>` through the current `CreateEnum` API.
- Demonstrate that the current enum field draft cannot name a transaction-local binder.
- Avoid adding a temporary fake implementation solely to make the test compile.
- Use the intended final API shape in the test if the migration is straightforward.
- Keep the first test focused on declaration publication and stable query facts.
- Do not begin with a large integration fixture.
- Do not build a new test harness.
- Use existing deterministic workspace constructors and helper conventions.

The characterization should make the missing producer obvious:

the canonical compiler and generic enum consumers already exist, but the source-free declaration producer cannot supply them.

## 9. Representation Decision

Make one direct representation decision.

The preferred minimal shape is:

```rust
pub struct EnumTypeParameterDraft {
    pub id: DraftTypeParameterId,
    pub name: String,
}

pub struct EnumFieldDraft {
    pub name: String,
    pub ty: DeclarationType,
}

Edit::CreateEnum {
    name: String,
    type_parameters: Vec<EnumTypeParameterDraft>,
    variants: Vec<EnumVariantDraft>,
}
```

This shape is preferred because enum parameters are unbounded in the current language model.

It does not expose a meaningless `bounds` field that callers must populate and the transaction must reject.

It reuses the existing `DraftTypeParameterId` local identity domain.

It reuses `DeclarationType` for binder-bearing declaration types.

It leaves published types as `SemanticType` with stable identities.

You may choose a smaller exact equivalent if the current checkout makes this shape unnecessarily duplicative.

Any alternative must satisfy all of the following constraints.

- Enum-bound syntax must not appear in the public API unless the language and canonical HIR support it end to end.
- Function-only bound semantics must not leak into enum creation.
- Creation-only binder handles must not enter published state.
- There must be one canonical `CreateEnum` operation.
- Non-generic and generic enums must use the same operation.
- Field declaration types must support local binder references without source strings.
- Published field types must use stable semantic type-parameter entities.
- No parallel generic declaration AST may be retained after staging.
- No generalized declaration framework may be introduced merely to share a few validation loops.
- A small local helper is acceptable when it removes actual duplicate validation while preserving owner-specific policy.

Do not rename existing public draft types solely for aesthetic symmetry if the rename creates broad unrelated churn.

Backward compatibility is not a reason to avoid a useful rename.

Diff size alone is not a reason to perform one.

Choose based on semantic clarity and search cost.

## 10. Enum Type-Parameter Contract

- Enum type parameters are ordered.
- Declaration order is semantic for type argument position.
- Submitted local draft IDs are transaction-local handles.
- A local draft ID must appear at most once in the enum parameter list.
- A parameter name must be a valid semantic identifier.
- A parameter name must not be reserved by the language.
- Parameter names must be unique within the enum declaration.
- The parameter list may be empty.
- The parameter list may contain more than one parameter.
- Enum parameters carry no trait bounds in this vertical.
- Do not synthesize empty bound records in published semantic state.
- Do not create trait witnesses for enum declarations.
- A binder may occur at arbitrary supported depth in a variant field declaration type.
- An unknown draft binder referenced by a field rejects before publication.
- A binder owned by another draft declaration is not implicitly visible.
- A stable type-parameter entity owned by another declaration must obey the existing owner and visibility rules.
- Do not infer a local binder from matching text.
- Do not resolve a local binder by name after validation.
- Do not store draft binder IDs in HIR.
- Do not store draft binder IDs in a snapshot.
- Do not store draft binder IDs in queries.
- Do not store draft binder IDs in projection.
- Do not store draft binder IDs in diffs.
- Do not store draft binder IDs in package or runtime artifacts.
- Allocate one stable public `TypeParameter` entity per enum binder.
- The stable binder entity is owned by the stable enum entity.
- Stable binder entities follow declaration order in containment and query results where order is defined.
- Field types published through the workspace refer to those stable binder entities.
- Compiler-local field types use the canonical private type-variable representation expected by existing generic enum code.

## 11. Phantom and Unused Enum Parameters

Do not copy the generic-function unused-parameter rule into enum declarations without evidence.

The source enum collector observed at the baseline validates names and duplicates but does not impose function-signature usage semantics.

Enum type parameters may be semantically relevant through an enum instantiation even when a selected variant carries no field of that parameter.

A nullary or phantom variant must retain the exact concrete enum type arguments.

Determine whether the source importer accepts an enum whose type parameter is unused by every variant.

Use that canonical source behavior as the oracle.

Do not reject a source-free declaration merely because a function-oriented helper currently rejects unused function binders.

If the source importer accepts a fully phantom enum, source-free construction must converge with it.

If the source importer rejects it through a canonical rule, preserve that rule and cite the focused evidence in the final report.

## 12. Field Declaration-Type Contract

- Use the existing creation-only declaration type model rather than strings.
- Support primitive and builtin types already accepted by non-generic enum creation.
- Support stable product identities already accepted by non-generic enum creation.
- Support stable enum constructors and exact type arguments already accepted by non-generic enum creation.
- Support stable visible type-parameter entities only where the existing type boundary permits them.
- Support local enum draft binders through the creation-only binder case.
- Support nested list and function type shapes to the same extent as the existing declaration type boundary.
- Preserve exact enum arity validation.
- Preserve wrong-kind validation.
- Preserve namespace validation.
- Preserve generation and liveness validation.
- Preserve owner validation for stable type-parameter entities.
- Preserve current lexical-reference result and field restrictions.
- Preserve current ownership-bearing field restrictions.
- Do not weaken type resolution because a declaration is source-free.
- Do not duplicate enum type substitution logic in workspace staging.
- Do not infer field types.
- Do not coerce field types.
- Do not silently replace an unsupported type with a placeholder.
- Reject `never` or other invalid field shapes only according to the canonical current declaration rules.
- Use iterative traversal for user-controlled declaration type depth.
- Check host-index conversions.
- Propagate allocation failure as a host error before publication.

## 13. Recursive Enum Boundary

Source import can already collect enum headers before resolving fields and can therefore represent recursive enums.

The current source-free `CreateEnum` operation does not expose a transaction-local nominal self identity.

Do not solve that separate problem in this vertical.

The selected operation covers exact non-recursive generic enum declarations.

Do not add:

- a draft enum self handle;
- a declaration group handle;
- a local nominal namespace;
- a two-phase public creation protocol;
- a partially published enum header;
- a generalized recursive declaration planner; or
- a hidden source-style name-resolution pass.

Document recursive source-free enum declaration creation as a remaining gap after this vertical.

Do not call the completed feature universally complete if recursion remains unsupported.

## 14. Staging Order

The transaction must stage enough stable identity context before resolving binder-bearing fields.

A sound staging sequence is:

1. Validate the global enum name.
2. Reject current declaration collisions.
3. Validate that at least one variant exists under the current enum contract.
4. Validate local type-parameter handles and names.
5. Validate variant names and field-name uniqueness.
6. Clone or reserve the staged allocator, never the published allocator.
7. Determine the private future enum address.
8. Reserve the stable enum entity in staged state.
9. Reserve stable enum type-parameter entities in declaration order.
10. Build the temporary map from draft binder IDs to stable binder entities.
11. Build the staged map from stable binder entities to canonical private binder names.
12. Convert every field `DeclarationType` to a stable `SemanticType` using the draft map.
13. Resolve each stable semantic type through the canonical workspace type boundary with the enum as owner.
14. Apply existing field ownership/reference restrictions.
15. Reserve stable variant and field entities.
16. Build the canonical private `EnumDefinition` using existing enum IDs, variant IDs, field IDs, layout identity, and field types.
17. Set non-recursive layout facts honestly for this supported route.
18. Run existing staged program consistency and complete validation as applicable.
19. Build canonical indexes.
20. Reconcile identities.
21. Build blockers, diagnostics, diff, and invalidated domains.
22. Publish one revision only after every fallible step succeeds.

This list describes required invariants, not a demand for one monolithic function.

Reuse existing helpers where they own the same invariant.

Do not create a generic transaction pipeline abstraction merely to mirror these steps.

## 15. Canonical HIR Requirements

- Use the existing `EnumDefinition` representation.
- Populate `type_parameters` in exact declaration order.
- Use canonical private type-variable names consistently with imported generic enums.
- Do not make the type-variable spelling a public identity.
- Use existing stable enum nominal identity derivation.
- Use existing stable runtime layout identity derivation.
- Use existing stable variant identity derivation.
- Use existing stable field identity derivation.
- Preserve variant declaration order.
- Preserve field declaration order.
- Set `Origin::Semantic` for source-free declarations.
- Do not fabricate source IDs, source spans, paths, or digests.
- Do not create synthetic source provenance.
- Do not add a second generic enum representation.
- Do not add a workspace-only generic enum solver.
- Do not bypass canonical enum consistency validation.
- Do not bypass canonical substitution when values or patterns use the new declaration.

## 16. Identity Contract

- The created enum receives one stable public entity identity.
- Each enum type parameter receives one stable public entity identity.
- Each variant receives one stable public entity identity.
- Each field receives one stable public entity identity.
- The enum owns its type parameters and variants.
- Each variant owns its fields.
- Identity allocation is deterministic for equivalent successful transactions in equivalent deterministic workspaces.
- Failure consumes no stable identity.
- Failure does not advance any generation.
- Failure does not alter free lists or tombstones.
- Retrying a corrected transaction after failure produces the same identities as a clean control workspace.
- Unrelated later edits preserve all created identities.
- Private compaction preserves all surviving created identities.
- Renaming the enum preserves enum, binder, variant, and field identities.
- Renaming a variant or field preserves all generic type facts and identities.
- Deleting the enum tombstones the enum, its type parameters, variants, and fields.
- Old snapshots preserve the deleted declaration and remain queryable.
- Later same-name recreation receives fresh generations.
- Private enum vector relocation emits no semantic movement.
- Binder spelling is not continuity evidence.
- Field type equality is not continuity evidence.
- Declaration order is not sufficient identity evidence after deletion and recreation.

## 17. Transaction Atomicity Contract

- Check the base revision before staging.
- Check every supplied namespace before use.
- Check liveness and kind of every stable type identity.
- Check ownership of stable type-parameter identities.
- Check all local draft handles before publication.
- Stage allocator state privately.
- Stage all created entities privately.
- Stage all converted types privately.
- Stage the complete enum definition privately.
- Stage indexes privately.
- Stage diagnostics and blockers privately.
- Stage the semantic diff privately.
- On any validation error, keep the exact published `Arc`.
- On any host allocation error, keep the exact published `Arc`.
- On any type error, keep the exact published `Arc`.
- On any duplicate declaration error, keep the exact published `Arc`.
- On any ownership-field rejection, keep the exact published `Arc`.
- Do not publish an enum header before fields validate.
- Do not publish binder entities before the enum validates.
- Do not publish a revision with a partially populated type-parameter map.
- Do not consume IDs while formatting an error.
- Do not mutate current diagnostics on failure.
- Do not mutate current blockers on failure.
- Do not mutate current attachments or provenance on failure.

## 18. Semantic Diff Contract

Use the existing semantic diff model unless a focused test proves it cannot honestly report this operation.

- Report the created enum entity.
- Report every created type-parameter entity.
- Report every created variant entity.
- Report every created field entity.
- Use stable identities in diff entries.
- Use deterministic entry ordering.
- Do not expose draft binder IDs.
- Do not expose compiler enum IDs.
- Do not expose private enum vector indexes.
- Do not expose private type-variable names as identity.
- Do not emit reference rewires when no existing reference changed.
- Do not emit movement for private layout changes.
- Do not add a special generic-enum-created diff variant if ordinary entity creation entries fully describe the semantic change.
- Let the multi-turn agent retrieve stable identities from the ordinary transaction result and queries.

## 19. Query and Projection Contract

- The published enum appears in ordinary entity queries.
- The published type parameters appear as `EntityKind::TypeParameter`.
- Each type parameter reports the enum as owner.
- Containment reports enum type parameters and variants in defined semantic order.
- Each field reports its variant as owner.
- The field declared type projects through stable semantic identities.
- A field using local binder `t` projects as `SemanticType::TypeParameter(stable_t)`.
- An enum type projects with the stable enum constructor and exact argument list.
- Exact concrete arguments remain visible for nullary variants.
- Legal-constructor queries for a hole of the exact concrete enum type advertise accepted variants.
- Legal-constructor queries do not advertise unsupported generic instantiations.
- Queries remain revision-labelled.
- Queries remain deterministic.
- Large query results remain paginated through existing continuation rules.
- Projection works without source attachments.
- Projection never becomes semantic authority.
- Projection does not display draft handles.
- Projection does not fabricate source paths or spans.
- Old snapshots project the old declaration after deletion in a later snapshot.

## 20. Multi-Turn AI Authoring Contract

This vertical exists for an actual multi-turn agent workflow.

Test the workflow as identities, not as text reconstruction.

1. Turn-equivalent transaction A creates the generic enum.
2. The outcome returns a new immutable revision.
3. The outcome and ordinary queries reveal stable identities.
4. The agent does not need to parse projection text to recover those identities.
5. Turn-equivalent transaction B creates or edits a callable using the published enum constructor.
6. The agent supplies exact type arguments keyed by stable parameter identity.
7. The transaction path performs canonical validation.
8. Turn-equivalent transaction C constructs and matches a value.
9. The complete snapshot compiles without source.
10. No hidden mutable session state is required beyond the ordinary `Workspace` object.
11. No daemon, RPC, persistence, or journal is required to prove this workflow.

Do not add a process boundary merely because the user mentions multi-turn use.

The current in-process workspace is the consumer for this vertical.

## 21. Generic Value Construction Requirements

- Use the existing `DraftNode::EnumValue` route.
- Select the stable variant identity.
- Supply one exact type argument for every stable enum type-parameter identity.
- Treat submitted type-argument order as non-semantic if the current API keys arguments by stable identity.
- Canonicalize type arguments to declaration order.
- Reject a missing argument.
- Reject a duplicate argument.
- Reject an argument keyed by a parameter owned by another declaration.
- Reject an argument keyed by a wrong-kind identity.
- Reject a foreign or stale parameter identity.
- Retain exact arguments in the resulting enum expression type.
- Retain exact arguments for nullary variants.
- Substitute field types through the existing canonical generic resolver.
- Check payload fields exactly once against substituted types.
- Canonicalize payload fields to declaration order.
- Preserve current ownership/reference generic restrictions.
- Do not add implicit type inference to workspace drafts.

## 22. Exact Pattern Requirements

- Use the existing exact enum-variant pattern route.
- Do not add type arguments to pattern nodes.
- Derive exact enum arguments from the expected scrutinee or enclosing field type.
- Validate that the stable variant belongs to the exact enum constructor.
- Require each declared field exactly once.
- Substitute each nested field type canonically.
- Publish payload binding entities with exact substituted stable semantic types.
- Preserve arm-local lexical visibility.
- Preserve usefulness checking.
- Preserve exhaustiveness checking.
- Preserve deterministic arm order.
- Preserve match-plan ownership and cleanup.
- Do not add a second pattern solver.
- Do not generalize patterns beyond the exact existing family in this vertical.

## 23. Compiler and Runtime Requirements

- Compile the finished source-free snapshot only through `compile_snapshot`.
- Reject incomplete snapshots before deriving HIR.
- Derive source-optional complete HIR directly from semantic state.
- Run canonical HIR consistency validation.
- Run canonical ownership and memory planning.
- Run SSA lowering and verification.
- Run bytecode lowering and validation.
- Execute through the generic VM route when native specialization declines.
- Do not make generic enum declaration support contingent on baseline-native eligibility.
- Do not add a public engine-selection option.
- Do not add a forced-native test mode.
- Do not add VM retry after native entry.
- Do not duplicate host effects.
- Prove the returned value is `42` in the primary example.
- Prove cleanup has no failure in the primary example.
- Use existing runtime observation helpers where possible.

## 24. Source Independence Requirements

- Start the principal source-free test from `Workspace::empty` or deterministic empty workspace.
- Reset parser and source-load invocation counters before source-free authoring begins.
- Create the enum without source import.
- Create `main` without source import.
- Construct and match values without source import.
- Query and project without source import.
- Compile without source import.
- Execute without source import.
- Assert parser invocation count remains zero.
- Assert source-load invocation count remains zero.
- Do not satisfy the test by rendering a temporary `.lkjscript` string.
- Do not satisfy the test by importing an oracle into the same source-free workspace.
- An independent imported oracle may be built in a separate snapshot for semantic convergence comparison.

## 25. Imported/Source-Free Convergence

Use an imported program only as an independent semantic oracle.

Construct an equivalent source-free program from an empty workspace.

Compare normalized public semantic facts and observable behavior.

Do not require private IDs to match across independent namespaces or import routes.

- Compare declaration kinds and ownership.
- Compare ordered type-parameter count and names as presentation facts.
- Compare stable public type shapes after route-local identity normalization.
- Compare variant and field order.
- Compare field types.
- Compare exact enum instantiation at value sites.
- Compare pattern types and payload binding types.
- Compare calls and references where applicable.
- Compare selected memory obligations where applicable.
- Compare normalized bytecode behavior or existing canonical observation helpers.
- Compare VM result.
- Compare traps and cleanup behavior if the selected helper already covers them.
- Do not compare source provenance.
- Do not compare private enum IDs or layout addresses.

## 26. Error Model Requirements

Use structured existing error variants when they already carry the facts callers need.

Generalize function-specific display wording only where sharing the same structured error with enum creation makes the old wording false.

Do not build a new diagnostic framework.

- Duplicate local draft binder identity rejects deterministically.
- Duplicate enum binder name rejects deterministically.
- Invalid enum binder name rejects deterministically.
- Reserved enum binder name rejects deterministically.
- Unknown local binder in a field declaration type rejects deterministically.
- Wrong-kind stable identity in a field declaration type rejects deterministically.
- Foreign stable identity rejects deterministically.
- Stale stable identity rejects deterministically.
- Stable type parameter owned by the wrong declaration rejects deterministically.
- Invalid generic enum arity rejects deterministically.
- Duplicate variant name rejects deterministically.
- Duplicate field name within a variant rejects deterministically.
- Empty variant list retains the existing rejection.
- Unsupported ownership/reference field types retain the existing rejection.
- Nonexistent enum-bound semantics cannot be silently accepted.
- Host allocation failure remains a host error.
- User-controlled invalid input must not panic.
- Error formatting must not depend on draft map iteration order.
- Do not stringify a typed error and parse it in a caller.

## 27. Focused Failure Atomicity Matrix

For each applicable failure below, assert the exact published snapshot remains unchanged.

Where existing test infrastructure supports it, assert `Arc::ptr_eq` with the pre-transaction snapshot.

After representative failures, apply a corrected transaction and compare its stable identities with a clean deterministic control workspace.

### 27.1 duplicate draft binder ID

- Setup: two enum parameters reuse one `DraftTypeParameterId`.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: structured duplicate-draft rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.2 duplicate binder name

- Setup: two distinct handles use the same semantic name.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: structured duplicate-name rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.3 invalid binder name

- Setup: one binder name is empty or syntactically invalid.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: owning-boundary identifier rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.4 reserved binder name

- Setup: one binder uses a language-reserved name.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: owning-boundary reserved-name rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.5 unknown binder reference

- Setup: a field type refers to an undeclared draft handle.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: structured unknown-binder rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.6 wrong-kind stable type

- Setup: a field type uses `main` or a value entity as a type parameter.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: wrong-kind rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.7 foreign stable type

- Setup: a field type uses an identity from another workspace namespace.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: foreign-namespace rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.8 stale stable type

- Setup: a field type uses a later generation for an existing slot.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: stale-identity rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.9 wrong-owner stable binder

- Setup: a field type uses a stable function binder where the enum owner is required.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: owner rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.10 bad enum type arity

- Setup: a nested enum type supplies the wrong argument count.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: invalid semantic type rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.11 duplicate variant

- Setup: two variants have the same name.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: duplicate-variant rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.12 duplicate field

- Setup: two fields in one variant have the same name.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: duplicate-field rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.13 empty variants

- Setup: the declaration has no variants.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: existing empty-enum rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.14 ownership field

- Setup: a field uses a currently unsupported ownership-bearing type.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: existing unsupported-edit rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.15 reference field

- Setup: a field uses a currently unsupported lexical reference type.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: existing unsupported-edit rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

### 27.16 global collision

- Setup: the enum name collides with an existing or reserved declaration.
- Action: submit one `CreateEnum` transaction against the current revision.
- Expected error: global-name rejection.
- Publication assertion: current snapshot pointer, revision, diagnostics, blockers, and attachments remain unchanged.
- Allocation assertion: the corrected retry receives the same identities as a clean control workspace.
- Non-goal: do not add special rollback machinery outside the existing staged allocator route.

## 28. Success Test Matrix

### 28.1 one-parameter maybe declaration

- Scenario: create `maybe<t>` with `none` and `some(value: t)`.
- Required result: enum, binder, variants, and field publish with exact ownership.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.2 two-parameter pair declaration

- Scenario: create `pair<a, b>` with `both(first: a, second: b)`.
- Required result: parameter and field order remain exact.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.3 nested binder occurrence

- Scenario: place a draft binder under an existing nested type constructor.
- Required result: iterative conversion resolves the stable binder at depth.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.4 nullary generic variant

- Scenario: create a generic enum with one variant carrying no fields.
- Required result: concrete type arguments survive value construction.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.5 phantom parameter oracle

- Scenario: mirror the importer behavior for a parameter unused by the selected or all variants.
- Required result: source-free and imported validity agree.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.6 query after creation

- Scenario: query the incomplete snapshot immediately after enum creation.
- Required result: all stable entities and field types are available without main.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.7 diff-driven continuation

- Scenario: obtain created identities from the transaction outcome.
- Required result: later edits use identities without parsing text.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.8 complete source-free program

- Scenario: create main, construct `some<i64>(42)`, and match it.
- Required result: compile and VM execution return 42.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.9 import convergence

- Scenario: build an independent imported equivalent.
- Required result: normalized semantic observation and runtime result agree.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.10 rename lifecycle

- Scenario: rename enum, variant, or field after use sites exist.
- Required result: identity and generic type facts remain stable.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.11 delete lifecycle

- Scenario: delete the enum after removing independent dependents.
- Required result: owned binders, variants, and fields tombstone.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.12 old snapshot lifecycle

- Scenario: retain the pre-delete snapshot.
- Required result: old declaration remains queryable and executable where complete.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.13 recreation lifecycle

- Scenario: recreate the same name after deletion.
- Required result: fresh generations are allocated.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.14 private compaction

- Scenario: delete an unrelated declaration that relocates private enum storage.
- Required result: all generic enum public identities survive.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

### 28.15 zero parser path

- Scenario: reset counters before empty-workspace authoring.
- Required result: all source-free operations leave parser/load counters at zero.
- Identity oracle: use stable public entities and ordinary queries.
- Compiler oracle: use the canonical complete snapshot route where the scenario reaches completeness.
- Runtime oracle: use the generic VM path or the existing production runner without forcing a tier.
- Source oracle: no render/reparse step is permitted.

## 29. Determinism Requirements

- Equivalent deterministic workspaces and equivalent successful transactions produce equivalent stable identity allocation.
- Type-parameter entity order follows declaration order.
- Variant entity order follows declaration order.
- Field entity order follows declaration order.
- Submitted map or vector order that is explicitly non-semantic is canonicalized before publication.
- Diff order is deterministic.
- Entity query order is deterministic.
- Containment order follows semantic declaration order.
- Projection order is deterministic.
- Error selection is deterministic when multiple invalid conditions exist under the owning validation order.
- No observable order may depend on `HashMap` or `HashSet` iteration.
- No observable order may depend on allocator addresses.
- No observable order may depend on private enum vector relocation.

## 30. Stack-Safety Requirements

The field declaration type is user-controlled depth.

The new path must remain stack-safe.

- Reuse the existing iterative `DeclarationType` traversal.
- Reuse the existing iterative declaration-type-to-semantic conversion.
- Reuse the existing iterative semantic-type resolution and projection.
- Do not add recursive convenience conversion for enum fields.
- Do not add recursive debug formatting in a failure path.
- Do not add recursive equality or cloning specific to enum creation.
- Use checked counts and host-index conversion.
- Add one focused ordinary-depth test for the new route.
- Reuse an existing ignored deep-type fixture if it can exercise the route with little new machinery.
- Add a new ignored 20,000-level test only when existing deep tests do not reach the new producer and the fixture can reuse current helpers.
- Do not create a general stress framework for this task.

## 31. Performance Requirements

This is primarily a semantic authoring vertical.

Do not invent a benchmark requirement without a complexity question.

- Do not add a cache.
- Do not add a daemon.
- Do not add parallelism.
- Do not add serialization.
- Do not add a digest or version token.
- Do not clone the entire workspace more times than the existing transaction path requires.
- Do not scan all declarations once per enum field when a local map already resolves the owner.
- Build the draft-binder map once.
- Build the staged stable-binder map once.
- Traverse each field declaration type with the existing linear iterative route.
- Avoid name-based repeated lookup after local handle validation.
- Avoid repeated conversion of the same field type.
- Do not retain temporary maps after publication.
- Do not add permanent measurement counters unless a measured regression question requires them.
- If a representation cleanup changes transaction work materially, use the existing measurement harness rather than a new one.
- Do not claim a performance improvement without retained evidence.

## 32. API-Cost and Context Discipline

The user asked for reduced coding-agent API cost where practical.

Reduce cost by reducing rediscovery, log volume, and failed architectural branches.

Do not reduce cost by skipping correctness or final verification.

- Read the companion `AGENTS.md` once.
- Keep a compact task ledger.
- Use `rg` for exact symbols.
- Use `git show` on the latest relevant commit instead of rereading history broadly.
- Open narrow line ranges around `CreateEnum`, `create_enum`, and existing generic tests.
- Do not dump the whole 40,000-line workspace test file.
- Do not dump complete compiler logs repeatedly.
- Capture long command output once.
- Inspect the first causal error and the final summary.
- Use compiler errors as a mechanical migration inventory after changing the API shape.
- Batch straightforward `CreateEnum` call-site updates.
- Run one focused test after each semantic milestone.
- Do not rerun a command when inputs did not change.
- Do not run the full workspace suite after every edit.
- Do not run Docker until the local full boundary is ready.
- Do not ask the user to choose between internal Rust representations.
- Do not use subagents for overlapping implementation.
- Use at most one bounded subagent audit only if a genuinely independent inventory would save context.
- Do not request competing designs from multiple subagents.
- Stop exploring after the direct representation satisfies the contract.
- At handoff, cite exact files, symbols, tests, commands, and remaining blocker.
- Do not copy source files into the handoff.

## 33. Likely Code Locations

These paths are likely in scope.

Verify exact ownership before editing.

- `crates/lkjscript-compiler/src/workspace/transaction.rs` for edit API and staging.
- `crates/lkjscript-compiler/src/workspace/error.rs` for declaration-neutral structured error wording where required.
- `crates/lkjscript-compiler/src/workspace/mod.rs` or the actual public re-export owner for new draft types.
- `crates/lkjscript-compiler/src/workspace/tests.rs` for focused source-free and convergence evidence.
- `crates/lkjscript-compiler/src/workspace/recompute_measurement.rs` only for mechanical API call-site migration or an existing representative fixture.
- `crates/lkjscript-compiler/src/workspace/index.rs` only if the current generic enum index assumes source-only production.
- `crates/lkjscript-compiler/src/workspace/query.rs` only if a focused test reveals a producer-specific assumption.
- `crates/lkjscript-compiler/src/workspace/types.rs` only if the staged owner-aware type resolver cannot already serve enum creation.
- `docs/spec/workspace.md` for the intended semantic contract.
- `docs/status.md` for implemented facts and remaining gaps.
- `docs/architecture.md` for the source-free declaration flow and authority.
- `docs/roadmap.md` for completion and next evidence gates.
- `README.md` only if its concise current capability statement becomes false or materially incomplete.

The following paths should not be touched unless a focused failing dependency proves it is necessary:

- host capability implementations;
- database or SQLite host code;
- native code installation;
- bytecode encoding unrelated to enum types;
- scheduler or deleted platform history;
- package transport unrelated to changed maintained fixtures;
- Docker infrastructure except verification fixes caused by the actual change;
- unrelated examples and benchmarks;
- crate topology;
- CI topology.

## 34. Implementation Sequence

Use the following sequence unless the actual dependency graph proves a smaller order.

1. Record checkout state and inspect instructions.
2. Verify the gap and baseline facts.
3. Write the focused failing declaration-publication characterization.
4. Choose the exact public draft shape.
5. Change `CreateEnum` directly.
6. Change enum field declaration types directly.
7. Update all maintained call sites mechanically.
8. Compile the affected crate to inventory missed sites.
9. Implement enum binder validation.
10. Reserve stable enum and binder identities in staged state.
11. Convert local declaration types through the existing iterative route.
12. Resolve fields with enum owner context.
13. Construct the canonical generic `EnumDefinition`.
14. Run the focused declaration test.
15. Add diff, query, and projection assertions.
16. Add representative failure-atomic tests.
17. Add the complete empty-workspace value/match/compile/execute test.
18. Add imported/source-free convergence.
19. Add lifecycle coverage only for behavior the new producer could break.
20. Run affected module or crate tests.
21. Inspect warnings and simplify the representation.
22. Update normative and descriptive documentation.
23. Run formatting.
24. Run full Clippy, tests, and release build once.
25. Run Docker verification once when available.
26. Inspect status and full diff.
27. Run `git diff --check`.
28. Commit one cohesive local change.
29. Inspect final worktree state.
30. Report exact evidence and stop.

## 35. Focused Test Construction Guidance

- Prefer one helper that creates the generic enum and returns stable identities from the transaction result.
- Do not rediscover identities by suffix matching when the diff or containment query already returns them.
- Use deterministic workspace namespaces for allocator rollback comparison.
- Use independent imported fixtures only for convergence.
- Keep the primary source-free test self-contained and source-independent.
- Use existing `ExpressionDraft`, `TypeArgumentDraft`, `PatternDraft`, and match helpers.
- Do not create a second expression builder DSL.
- Do not create a second canonical observation format.
- Extend an existing normalized observation helper only when the generic declaration facts are currently omitted and the extension benefits the actual convergence assertion.
- Do not compare presentation names where stable semantic type identity is the intended fact.
- Do compare parameter and member order where declaration order is semantic.
- Use exact error variants rather than substring matching when the variant is part of the API.
- Use error-class matching only when message details are intentionally not contract.
- Assert both parser and loader counters remain zero.
- Compile through production API, not a test-only HIR surrogate.
- Execute validated bytecode or the production run helper, not only the SSA oracle.

## 36. Detailed Declaration Acceptance Checklist

- [ ] A001: one canonical `CreateEnum` supports zero parameters.
- [ ] A002: one canonical `CreateEnum` supports one parameter.
- [ ] A003: one canonical `CreateEnum` supports multiple parameters.
- [ ] A004: parameter order is retained.
- [ ] A005: variant order is retained.
- [ ] A006: field order is retained.
- [ ] A007: local binder IDs are unique.
- [ ] A008: local binder names are unique.
- [ ] A009: local binder names are valid.
- [ ] A010: local binder names are non-reserved.
- [ ] A011: unknown local binders reject.
- [ ] A012: local binders resolve without spelling lookup.
- [ ] A013: local binders may occur at nested type depth.
- [ ] A014: local handles do not publish.
- [ ] A015: stable enum identity publishes.
- [ ] A016: stable parameter identities publish.
- [ ] A017: stable variant identities publish.
- [ ] A018: stable field identities publish.
- [ ] A019: enum owns parameter entities.
- [ ] A020: enum owns variant entities.
- [ ] A021: variant owns field entities.
- [ ] A022: field types project through stable parameter entities.
- [ ] A023: compiler HIR uses canonical private variables.
- [ ] A024: HIR origin is semantic.
- [ ] A025: no source provenance is fabricated.
- [ ] A026: no parser call occurs.
- [ ] A027: no source load occurs.
- [ ] A028: failed validation publishes nothing.
- [ ] A029: failed validation consumes no IDs.
- [ ] A030: corrected retry is allocation-stable.
- [ ] A031: old snapshots remain valid.
- [ ] A032: private compaction preserves public IDs.
- [ ] A033: deletion tombstones owned binders.
- [ ] A034: same-name recreation uses fresh generations.
- [ ] A035: enum rename preserves nominal identity.
- [ ] A036: variant rename preserves variant identity.
- [ ] A037: field rename preserves field identity.
- [ ] A038: rename does not rewrite binder identity.
- [ ] A039: exact value construction accepts the new declaration.
- [ ] A040: exact value construction requires every type argument.
- [ ] A041: exact value construction rejects duplicate arguments.
- [ ] A042: exact value construction canonicalizes argument order.
- [ ] A043: exact value construction substitutes payload types.
- [ ] A044: nullary variant retains concrete arguments.
- [ ] A045: exact pattern construction accepts the new declaration.
- [ ] A046: pattern types derive from expected concrete enum type.
- [ ] A047: payload binding receives substituted field type.
- [ ] A048: match usefulness remains canonical.
- [ ] A049: match exhaustiveness remains canonical.
- [ ] A050: complete snapshot compiles directly.
- [ ] A051: generic VM route remains complete.
- [ ] A052: runtime returns the expected value.
- [ ] A053: cleanup remains exact.
- [ ] A054: imported and source-free observations converge.
- [ ] A055: non-generic enum creation remains the empty-parameter case.
- [ ] A056: old Rust call shape is deleted.
- [ ] A057: no compatibility wrapper remains.
- [ ] A058: no new crate is added.
- [ ] A059: no new dependency is added without demonstrated need.
- [ ] A060: no generic declaration framework is added.
- [ ] A061: no enum bound model is added.
- [ ] A062: no recursive declaration handle is added.
- [ ] A063: no persistence or service machinery is added.
- [ ] A064: documentation states the exact supported boundary.
- [ ] A065: roadmap removes the completed gap.
- [ ] A066: roadmap retains recursive generic enum creation as separate only if evidenced.
- [ ] A067: full repository verification is run or exact environment failure is reported.
- [ ] A068: final worktree contains no prompt, log, cache, or build artifact.

## 37. Rejected Design Alternatives

Reject these alternatives unless the current checkout provides concrete evidence that the preferred direct shape is impossible.

- Reject `CreateGenericEnum` beside `CreateEnum`: creates two authorities and compatibility burden.
- Reject `CreateEnumV2`: encodes revision history in the product API.
- Reject an enum `edition` flag: preserves obsolete behavior without a consumer.
- Reject reusing bound-bearing function parameter drafts without constraint: exposes unsupported enum-bound states.
- Reject silently discarding enum bounds: makes caller intent dishonest.
- Reject rendering enum source and importing it: restores text as semantic authoring authority.
- Reject resolving draft binders by name: makes presentation spelling semantic identity.
- Reject publishing an enum header and filling fields later: breaks one-transaction atomicity.
- Reject storing draft handles in snapshots: leaks creation-local identity.
- Reject adding a generic declaration registry: adds a second metadata authority.
- Reject adding a generalized declaration builder framework: increases search space for one producer.
- Reject adding a daemon for multi-turn state: adds an unmeasured process boundary.
- Reject adding persistence for the test: adds a durable format without a durable consumer.
- Reject adding a cache around type resolution: optimizes unmeasured work.
- Reject adding parallel field resolution: adds overhead before independent work is measured.
- Reject expanding native lowering: is unrelated to declaration authoring correctness.
- Reject implementing recursive enums simultaneously: requires a separate local nominal identity contract.
- Reject implementing generic products simultaneously: combines separate declaration semantics.
- Reject generalizing all errors into a new diagnostic system: expands beyond the owning failures.

## 38. Documentation Changes

Update maintained documentation in the same change.

- Update `docs/spec/workspace.md` to define source-free unbounded generic enum declaration creation.
- State that enum declaration-local binder handles exist only during one creation edit.
- State that published enum type parameters are stable entities.
- State that field declaration types may reference those local binders and publish stable semantic types.
- State that enum values require exact stable-parameter-keyed arguments.
- State that patterns derive arguments from the expected exact enum type.
- State that enum bounds are not supported.
- State that recursive source-free enum declarations remain unsupported in this vertical.
- Update `docs/status.md` only with behavior actually implemented and tested.
- Remove the generic enum declaration creation gap from implemented-gap wording.
- Retain precise remaining gaps without expanding them into speculative plans.
- Update `docs/architecture.md` if its source-free declaration flow currently says enums are non-generic only.
- Update `docs/roadmap.md` so completed work leaves `Now`.
- Do not invent a new `Now` item without a concrete consumer and focused evidence.
- Do not add a permanent task report.
- Do not archive this prompt in repository documentation.
- Update `README.md` only if its concise capability statement would otherwise be false.
- Do not add performance claims without measurements.

## 39. Verification Ladder

Use the smallest command that answers the current question.

1. Compile the affected workspace module or crate after the direct API migration.
2. Run the one focused generic enum declaration publication test.
3. Run the focused failure-atomic test set.
4. Run the focused source-free compile/execute convergence test.
5. Run the affected crate test suite.
6. Run formatting after representation stabilizes.
7. Run full Clippy once.
8. Run full workspace tests once.
9. Run the locked release build once.
10. Run Docker verification once when available.
11. Run `git diff --check`.

Do not repeatedly run the full boundary while the API shape is still changing.

Do not omit final full verification because focused tests passed.

## 40. Required Final Commands

Run these commands before completion unless the environment prevents one.

```bash
cargo fmt --all -- --check
cargo clippy --quiet --workspace --all-targets --all-features --locked -- -D warnings
cargo test --quiet --workspace --all-targets --all-features --locked
cargo build --quiet --workspace --release --locked
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
git diff --check
```

If formatting fails, run `cargo fmt --all`, then rerun the check.

Use `--locked` exactly as shown.

Do not claim Docker verification if Docker is unavailable.

Report the exact environment error.

Distinguish product failures from environment failures.

## 41. Final Diff Inspection

- Inspect `git status --short`.
- Inspect `git diff --stat`.
- Inspect the complete substantive diff.
- Inspect all API call-site migrations.
- Confirm the old `CreateEnum` shape no longer exists.
- Confirm no compatibility alias exists.
- Confirm no prompt file entered the repository.
- Confirm no raw log entered the repository.
- Confirm no build artifact entered the repository.
- Confirm no cache entered the repository.
- Confirm no unrelated formatting sweep occurred.
- Confirm documentation matches code and tests.
- Confirm no speculative service, persistence, planner, registry, or cache appeared.
- Confirm the generic VM path remains unchanged or only narrowly fixed for a demonstrated bug.
- Confirm every new public type earns its existence by making invalid states unrepresentable or carrying required semantics.

## 42. Commit Rules

- Create one cohesive local commit after the selected vertical passes.
- Use a subject that describes the semantic result.
- A suitable subject is `Complete source-free generic enum declarations` if that accurately describes the final scope.
- Do not mention the prompt in the commit subject.
- Do not include unrelated cleanup.
- Do not push.
- Do not open a pull request.
- After committing, inspect `git status --short` and record the ending commit.

## 43. Multi-Turn Handoff Rules

This task may span multiple agent turns.

Do not force all future decisions into the first turn.

At the end of a turn, leave a compact handoff containing:

- starting commit;
- current commit;
- worktree status;
- selected vertical;
- representation decision;
- files changed;
- focused tests added;
- commands run;
- exact failures remaining;
- next exact symbol or test to address;
- non-goals still enforced; and
- whether any verification is unrun.

Do not create a committed handoff document.

Do not repeat repository history.

Do not hand off vague instructions such as “continue implementation.”

Name the exact next operation.

Commit only coherent verified work.

Do not commit a knowingly broken intermediate API solely to save context.

## 44. Stop Conditions

Stop implementation immediately after all selected acceptance criteria pass.

Do not continue into adjacent work.

- Stop before generic product declarations.
- Stop before recursive source-free enums.
- Stop before enum bounds.
- Stop before unresolved type names.
- Stop before broader generic patterns.
- Stop before member editing.
- Stop before generalized movement.
- Stop before parser recovery.
- Stop before persistence.
- Stop before daemon work.
- Stop before protocol work.
- Stop before caching.
- Stop before parallelism.
- Stop before unrelated performance optimization.

If the implementation begins to require one of those systems, narrow the vertical or stop with exact evidence.

Do not solve uncertainty by building infrastructure.

## 45. Completion Definition

The task is complete only when all statements below are true.

- [ ] An empty workspace can publish an unbounded generic enum declaration through one atomic transaction.
- [ ] The one canonical `CreateEnum` operation covers generic and non-generic declarations.
- [ ] Enum variant field types can refer to declaration-local enum binders without source text.
- [ ] Local binder handles do not escape staging.
- [ ] Stable enum-owned binder entities publish in declaration order.
- [ ] Published field types use stable semantic binder identities.
- [ ] Failure consumes no public identity and preserves the exact snapshot.
- [ ] A corrected retry is allocation-stable.
- [ ] Ordinary queries and projection expose the declaration without source provenance.
- [ ] A later transaction constructs an exact concrete value using stable parameter identities.
- [ ] A later transaction exhaustively matches that value using existing exact pattern semantics.
- [ ] The complete snapshot compiles through `compile_snapshot`.
- [ ] The generic runtime route executes the program and returns `42`.
- [ ] Parser and source-load counters remain zero after empty workspace creation.
- [ ] An imported equivalent converges on normalized semantic facts and observable behavior.
- [ ] Representative invalid declarations reject deterministically and atomically.
- [ ] Lifecycle behavior for deletion, old snapshots, and private compaction remains correct.
- [ ] The implementation is stack-safe at user-controlled declaration type depth.
- [ ] No compatibility layer remains.
- [ ] No speculative subsystem appears.
- [ ] Documentation precisely states the implemented boundary and remaining gaps.
- [ ] Focused and full verification pass, or exact environment-only failures are reported.
- [ ] One cohesive local commit records the semantic result.

## 46. Final Report Format

Use this exact shape in the final agent report.

```text
Starting commit:
Ending commit:
Selected vertical:
Current consumer:
Gap evidence:
Semantic result:
Representation decision:
Direct compatibility breaks:
Deleted obsolete path:
Identity behavior:
Atomicity behavior:
Source-independence evidence:
Compiler/runtime evidence:
Focused tests:
Focused commands:
Full verification:
Docker verification:
Measurements:
Documentation:
Remaining gaps:
Worktree state:
```

Report exact facts.

Do not claim unrun verification.

Do not report hidden reasoning or a transcript.

## 47. Compact Decision Ledger Template

Keep this in ignored scratch space or working memory while implementing.

```text
Operation: create an unbounded generic enum declaration from semantic operations
Consumer: multi-turn in-process AI authoring from Workspace::empty
Gap: generic enum value/pattern consumers exist, declaration producer is source-only
Authority: SemanticProgram published through WorkspaceSnapshot
Public inputs: stable identities plus transaction-local binder handles
Authoritative output: canonical EnumDefinition and stable workspace entities
Survivors: all unrelated entities and nodes
Created: enum, enum type parameters, variants, fields
Tombstoned: none on successful creation
Atomicity: staged allocator and one-revision publication
Compiler route: compile_snapshot -> HIR -> memory -> SSA -> bytecode -> VM
Source work: zero after Workspace::empty
Non-goals: recursion, bounds, generic products, services, persistence, generalized machinery
Stop: source-free generic maybe declaration/value/match compiles and executes with all checks passing
```

## 48. Final Instruction

Implement the selected vertical now.

Be decisive about the direct API cutover.

Be conservative about scope.

Reuse the existing generic enum semantics.

Do not create another semantic authority.

Do not preserve obsolete APIs.

Do not restore deleted platform ambitions.

Do not trade correctness for API cost.

Save API cost by searching narrowly, testing incrementally, and stopping when the evidence-backed vertical is complete.
