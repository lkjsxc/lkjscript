# Complete One Identity-Preserving Public Movement Vertical

Repository: `lkjsxc/lkjscript`
Grounding branch: `main`
Grounding commit: `501d132757f1cb15c6c000ca7fa34c1279ded06c`
Grounding date: 2026-08-12
Primary implementation language: Rust
Task mode: autonomous, evidence-first, multi-turn engineering

## Assignment

Implement one complete public semantic movement operation for the in-process workspace.
The preferred and presumed vertical is reordering one existing direct child inside one existing semantic sequence.
The operation must preserve the stable public identity of the moved subtree and every surviving descendant.
The operation must change semantic evaluation order directly, without rendering source, reparsing source, copying the subtree, deleting and recreating it, or introducing a second mutable representation.

Treat this as one dependency-closed product vertical.
Complete its public edit contract, validation, identity reconciliation, query behavior, projection behavior, semantic diff, compilation behavior, execution evidence, failure atomicity, documentation, measurement, and verification.
Do not expand this turn into a general tree editor, generic movement framework, persistence project, daemon, collaboration layer, incremental compiler, or broad language-surface campaign.

Backward compatibility is not a constraint.
You may make incompatible changes to public Rust APIs, internal representations, tests, fixtures, documentation, and provisional source notation whenever the resulting design is simpler and more coherent.
Do not retain aliases, adapters, deprecated variants, compatibility shims, dual paths, migrations, or feature flags for displaced designs.

The user grants broad architectural authority.
Use that authority to make the smallest correct root-level change, not to maximize scope.
A large rewrite is justified only when the existing architecture demonstrably prevents a coherent one-vertical result and the rewrite deletes more complexity than it adds.

## Required Outcome

- One public typed workspace edit can move one existing direct child to another position within the same existing semantic sequence.
- The edit addresses semantic objects through stable public identities, not source spans, parser nodes, dense IDs, vector indexes, preorder numbers, textual paths, or debug labels.
- The moved root keeps its existing `NodeId`.
- Every structurally surviving descendant of the moved root keeps its existing `NodeId`.
- The containing sequence keeps its existing `NodeId`.
- Unmoved surviving siblings keep their existing `NodeId` values.
- No moved or surviving node is spuriously reported as deleted and recreated.
- Private preorder, addresses, indexes, dense bindings, places, slots, plans, or compiler-local identities may change as needed.
- Old immutable snapshots remain valid and retain their original order.
- The new snapshot exposes the new semantic child order deterministically.
- The edit produces a compact explicit movement diff expressed through stable semantic identities.
- The edit causes canonical type, effect, control-flow, ownership, match, cleanup, index, and completeness validation at the same authority boundaries as other semantic edits.
- A complete valid moved snapshot compiles directly through `compile_snapshot`.
- A complete valid moved snapshot executes through the selected production route.
- An invalid move publishes nothing and consumes no allocator state.
- Imported and source-free snapshots use the same movement operation after semantic construction.
- The movement implementation invokes neither source loading nor parsing.
- The implementation remains stack-safe for user-controlled subtree depth.
- The implementation introduces no arbitrary semantic size or depth quota.
- The result is documented as current behavior only after executable evidence passes.

## Why This Vertical

- The current roadmap explicitly calls for one concrete public movement vertical only when a real owner/order use case exists.
- A sequence is already an ordered semantic owner whose child order directly defines evaluation order.
- Agents routinely need to reorder existing statements or expressions during iterative refactoring.
- Replacing an entire sequence for a reorder destroys useful identity continuity and produces unnecessarily large diffs.
- Stable identity across a reorder improves multi-turn references, review, focused queries, and compact state transfer.
- A same-sequence direct-child move has an exact owner, exact order semantics, and a bounded public contract.
- The existing workspace already has stable nodes, immutable revisions, explicit transactions, private relocation, and identity reconciliation.
- The existing compiler already validates final semantic order through canonical type, effect, ownership, control-flow, and cleanup paths.
- This vertical tests whether public identities truly survive deliberate semantic movement rather than only private compaction.
- This vertical does not require cross-scope reparenting, generic coordinates, arbitrary paths, or a universal graph editor.
- This vertical creates an immediately useful agent operation without requiring a daemon or persistence layer.
- The operation can reduce future model context and review volume by describing one move instead of a replacement subtree.
- Any API-cost claim beyond measured output or explicit estimates must remain labelled unknown.

## Current Repository Facts to Preserve

- The active product is local package `check` and `run` plus an in-process semantic workspace API.
- Text and paths are importer conveniences, not semantic authority.
- `WorkspaceSnapshot` is immutable and revision-labelled.
- `SemanticProgram` is the one partial-capable program authority used while staging a transaction.
- Stable public identities are separate from compiler-dense placement.
- Removed identities tombstone and generations protect reuse.
- A failed transaction publishes no new snapshot.
- A failed transaction must not alter deterministic future identity allocation.
- Incomplete snapshots are valid editing states.
- Incomplete snapshots are never executable states.
- `compile_snapshot` is the sole semantic snapshot to complete-HIR compiler boundary.
- Complete HIR is derived, ephemeral, and not a second mutable authority.
- Source-free compilation must not render or reparse source.
- The check path compiles but does not enter program effects.
- The run path attempts one eligible baseline-native group before effects and otherwise uses the unchanged validated VM program.
- Once native entry begins, no VM retry occurs.
- The VM remains the complete generic production route.
- The SSA evaluator remains a test-only oracle.
- Full semantic recomputation remains the accepted default for semantic edits.
- Only the already-measured metadata-only hole-refinement path shares unchanged semantic and index state.
- No general incremental framework is justified by current measurements.
- No warm service or daemon is justified by current measurements.
- Successful high-frequency verification uses quiet native command modes.
- Raw measurements remain outside Git.
- Current documentation authority is split intentionally among specification, status, architecture, performance, roadmap, and executable evidence.

## Grounding Evidence

- The grounding `main` commit is `501d132757f1cb15c6c000ca7fa34c1279ded06c`, titled `Complete source-free typed loop control`.
- Recent work completed nominal lifecycle, semantic binding lifecycle, exhaustive enum matches, nominal ownership authoring, source-free construction, generic calls, generic function authoring, imperative loops, early return, unresolved value references, measured recomputation, verification reduction, and typed loop control.
- The recent trajectory is one source-free semantic vertical at a time, followed by convergence, ownership, stack-safety, performance, and workflow evidence.
- There are no open issues or pull requests defining a competing immediate objective at the grounding snapshot.
- The roadmap `Now` item is one concrete public movement vertical only when a present owner/order use case exists.
- The roadmap rejects generic coordinates, paths, and tree-editing machinery added for symmetry.
- The roadmap `Next` item is evidence-based retained representation and crate-boundary reassessment.
- The roadmap permits `merge nothing` when evidence does not justify consolidation.
- Current public movement is absent even though private identity-preserving compaction is complete.
- Current source-free editing already supports sequence, mutable locals, assignment, while, typed loop, break, continue, early return, products, enums, matches, generic calls, holes, and unresolved copy-load references.
- Current transaction staging clones the semantic program, applies edits, compacts, infers effects, validates complete state, rebuilds indexes, reconciles identities, and publishes once.
- Existing structural edits replace roots and force selected survivor identities.
- Existing identity reconciliation uses semantic structure and private placement; a reorder must not be mistaken for delete/create churn.
- The compiler workspace contains large owned modules, including approximately 665 KiB of workspace tests, approximately 109 KiB of recomputation measurement code, approximately 75 KiB of query code, and approximately 63 KiB of index code.
- Large files are a navigation and context signal, not proof of a compile-time defect or a reason to shard by line count.
- A prior warm `cargo test --no-run` measurement was approximately 0.104 seconds while the corresponding full test command was approximately 141 seconds before retained test reduction.
- That evidence identified test execution, not warm compilation, as the selected verification bottleneck.
- Retained verification work was reduced by moving excess scale geometry out of the default boundary while preserving a larger ignored locked-release stress.
- The measured native four-command verification median fell from approximately 142.335 seconds to 98.245 seconds.
- Successful aggregate verification output was reduced from approximately 79,329 bytes to 5,152 bytes without a wrapper, daemon, or custom runner.
- A prior architecture deletion reduced the selected cold release-build median by approximately 3.73 percent and the stripped binary by approximately 4.95 percent on the measured host.
- Representative semantic-workspace measurement selected full recomputation for semantic edits.
- Only a narrow metadata-only hole-refinement path was retained because it shared unchanged semantic and index state honestly.
- At representative medium geometry, compilation dominated the measured agent check loop more than transaction, query, or projection work.
- Do not reopen general incrementality merely because movement changes order.
- The application crate directly composes several internal crates, and the internal dependency graph is shallow but fanned out.
- That graph is a later audit target, not evidence that this movement turn should merge crates.
- The current workspace has one CLI composition root, one semantic workspace, one compiler boundary, one generic VM route, and one optional baseline-native route.
- Preserve that one-route architecture.

## Authority Order

1. The active task owns this turn's objective and acceptance criteria.
2. The repository root `AGENTS.md` owns repository-wide engineering procedure.
3. `docs/spec/workspace.md` owns the intended semantic-workspace contract.
4. `docs/spec/language.md` owns intended language semantics.
5. Executable code, tests, manifests, lock files, command definitions, and schemas own current checkout behavior.
6. `README.md` owns product introduction and first successful use.
7. `docs/status.md` owns current implementation status and known gaps.
8. `docs/architecture.md` owns current responsibilities, flows, ownership boundaries, and trust boundaries.
9. `docs/performance.md` owns measurement methods, retained evidence, selection criteria, and reversal conditions.
10. `docs/roadmap.md` owns ordering and intent only.
11. Git history owns superseded implementation and prose.
12. This task prompt is transport, not durable repository authority.

When these sources disagree, identify the precise dimension of disagreement.
Inspect the owning artifact and executable behavior.
Update or delete stale material in the same coherent change.
Do not preserve contradictory prose merely because it predates the implementation.

## Operating Mode

- Work autonomously.
- Do not ask the user to choose among internal Rust API shapes.
- Do not ask the user to approve a necessary breaking change.
- Do not ask the user whether to add tests required for correctness.
- Do not ask the user whether to update stale documentation.
- Ask only when a genuinely external product requirement cannot be inferred safely.
- Use the current checkout, not assumptions from older conversations or obsolete branches.
- Inspect branch, worktree, upstream, and recent history before editing.
- Preserve unrelated tracked and untracked work.
- Do not reset, clean, overwrite, or rewrite unrelated work.
- Do not force push.
- Do not push unless explicitly instructed by the execution environment or user.
- Create cohesive commits when repository policy and environment permit.
- Do not commit this task prompt, raw logs, raw samples, scratch notes, or subagent reports.
- Use English for code, APIs, diagnostics, tests, documentation, commits, and reports.
- Prefer focused searches and focused file ranges before opening large files.
- Prefer executable evidence over architectural speculation.
- Prefer deletion and reuse of existing invariants over new machinery.
- Stop after this vertical is coherent and verified.
- Name the next problem without beginning it merely to appear ambitious.

## Multi-Turn Boundary

- This turn is not required to complete the entire roadmap.
- This turn is not required to implement all possible movement operations.
- This turn is not required to redesign the source format.
- This turn is not required to persist workspace snapshots.
- This turn is not required to expose movement through a CLI.
- This turn is not required to add a wire protocol.
- This turn is not required to optimize every transaction.
- This turn is not required to merge or split crates.
- This turn is not required to add direct nominal-member mutation.
- This turn is not required to add unresolved moves, borrows, calls, type names, members, patterns, or imports.
- This turn is not required to add parser recovery, ambiguity state, conflict state, or collaboration.
- This turn should leave one clear next highest-leverage problem.
- The expected next reassessment is retained representation and crate boundaries, but do not start it unless movement work proves inseparable.

## Primary Scope

- One existing semantic sequence node.
- One existing direct child of that sequence.
- One destination inside the same direct-child list.
- Stable public sequence identity.
- Stable public moved-subtree identity.
- Stable public surviving-sibling identity.
- Revision-checked atomic transaction publication.
- Canonical final semantic validation.
- Deterministic query and projection order.
- Compact stable-identity semantic diff.
- Source-free and imported semantic snapshots.
- Complete and incomplete snapshots where the existing workspace contract permits edits.
- Focused scale, stack-safety, and failure-atomicity evidence.

## Explicit Non-Goals

- Do not implement arbitrary cross-parent reparenting.
- Do not implement cross-function movement.
- Do not implement cross-main/function movement.
- Do not implement cross-owner movement.
- Do not implement cross-workspace movement.
- Do not implement cross-revision identity translation.
- Do not implement move-by-source-span.
- Do not implement move-by-text range.
- Do not implement move-by-file path.
- Do not implement move-by-parser-node identity.
- Do not implement move-by-private preorder.
- Do not expose vector indexes as durable identity.
- Do not expose raw dense node addresses.
- Do not expose compiler-local IDs.
- Do not create a generic path language.
- Do not create JSON Pointer-like semantic coordinates.
- Do not create a universal tree patch format.
- Do not create a graph rewrite DSL.
- Do not create a command journal.
- Do not create an event stream.
- Do not create an observer framework.
- Do not create a transaction planner framework.
- Do not create a generic ownership-aware movement framework.
- Do not create a mutable parent-pointer graph unless current invariants require it and it deletes more machinery than it adds.
- Do not create persistent sibling IDs.
- Do not create stable order keys.
- Do not create fractional indexing.
- Do not create CRDT positions.
- Do not create conflict-free replicated movement.
- Do not create multi-writer conflict state.
- Do not create a daemon.
- Do not create a service.
- Do not create a protocol.
- Do not create a database.
- Do not create persistence.
- Do not create a cache framework.
- Do not create a general incremental compiler.
- Do not create parallel transaction staging.
- Do not create speculative self-hosting support.
- Do not create compatibility variants for a provisional edit API.
- Do not retain a replace-based movement fallback after the real operation exists.
- Do not add hidden copy/delete/reinsert behavior.
- Do not infer that sequence movement generalizes automatically to call arguments, match arms, product fields, enum fields, loop bodies, or declarations.
- Do not add another movement kind for symmetry.
- Do not split files merely to satisfy a line-count aesthetic.
- Do not merge crates merely to reduce workspace member count.
- Do not add a dependency merely to implement one vector reorder.
- Do not add a benchmark framework for one focused measurement.
- Do not add a runner, broker, or logging layer to reduce output.
- Do not weaken canonical validation to make moved programs publish.
- Do not silently coerce an invalid moved sequence into a valid one.
- Do not leave a moved complete snapshot executable when ownership or control flow is invalid.

## Preferred Public Edit Shape

The following shape is a strong starting point, not an instruction to preserve exact spelling against better evidence:

```rust
#[non_exhaustive]
pub enum Edit {
    // Existing variants...
    MoveSequenceChild {
        sequence: NodeId,
        child: NodeId,
        before: Option<NodeId>,
    },
}
```

- `sequence` identifies the existing semantic sequence owner.
- `child` identifies the existing direct child to move.
- `before: Some(anchor)` requests insertion immediately before another existing direct child of the same sequence.
- `before: None` requests insertion at the end of the sequence.
- The destination anchor is a stable semantic identity.
- No public numeric position is required.
- The operation does not accept a replacement draft.
- The operation does not accept source text.
- The operation does not accept a private address.
- The operation does not manufacture a new subtree.

You may select another equally narrow stable-identity API if inspection proves it materially simpler or more precise.
Examples include an explicit `after` anchor or a closed destination enum.
Do not broaden the semantic capability while changing the spelling.
Record the reason for any material deviation in the final report and owning documentation.

## Evidence Gate Before Implementation

- Locate the public `Edit` enum and every exhaustive match over it.
- Locate `SemanticDiffEntry` and every consumer of semantic diffs.
- Locate structural-edit preflight and overlap detection.
- Locate the canonical mapping from `NodeId` to private `NodeAddress`.
- Locate the canonical expression-child order for `ExprKind::Do` or the current semantic sequence representation.
- Locate the code that clones the semantic program for staging.
- Locate the code that mutates or replaces an expression root.
- Locate identity reconciliation and forced survivor mapping.
- Locate subtree node-key construction and parent/child-ordinal matching.
- Locate hole and unresolved-reference address refresh.
- Locate completeness blocker regeneration.
- Locate partial effect inference.
- Locate complete-HIR derivation.
- Locate ownership validation.
- Locate match-plan validation and refresh.
- Locate index construction and containment ordering.
- Locate selected projection rendering.
- Locate node, containment, and legal-constructor query code.
- Locate source-loading and parser test counters.
- Locate failure-atomicity tests and deterministic allocator retry tests.
- Locate source-free/imported convergence helpers.
- Locate existing generated wide-sequence tests.
- Locate existing small-stack tests.
- Locate existing transaction work counters and measurement harness.
- Locate documentation statements that say public movement is absent.
- Locate roadmap statements governing public movement.

Write a compact temporary evidence note before editing.
The note must identify the current representation, the exact mutation point, the identity-reconciliation gap, the canonical validators that can be reused, and the minimal public surface.
Do not commit the note.

## Falsifiable Hypothesis

- A same-sequence direct-child reorder can be implemented by one local semantic mutation plus explicit survivor relocation.
- The existing final-state validation pipeline can decide type, effect, control-flow, ownership, cleanup, and match legality after the reorder.
- No new semantic representation is required.
- No new persistent identity type is required.
- No new general-purpose movement abstraction is required.
- No parser or source renderer is required.
- No general incremental framework is required.
- The moved subtree can preserve all stable node identities through an explicit old-node to new-private-address relocation.
- The operation can produce a compact movement diff without delete/create noise.
- Representative movement transaction cost can remain linear in the traversed semantic root or better.
- The implementation can remain smaller than a replace/copy/delete/reinsert compatibility mechanism.

## Reversal Conditions

- Revert or redesign if the operation requires a second mutable semantic authority.
- Revert or redesign if the operation requires source rendering and reparsing.
- Revert or redesign if the operation cannot preserve the moved subtree's stable identities.
- Revert or redesign if unaffected siblings lose identity.
- Revert or redesign if old snapshots become invalid.
- Revert or redesign if failed movement consumes identities or changes future allocation.
- Revert or redesign if complete invalid programs can publish.
- Revert or redesign if program effects can execute during checking.
- Revert or redesign if native fallback can duplicate effects.
- Revert or redesign if movement is represented as delete plus create in the public diff.
- Revert or redesign if a generic tree framework is larger than the movement vertical.
- Revert or redesign if a new cache has no explicit owner, lifetime, invalidation rule, and measured benefit.
- Revert or redesign if default verification becomes materially slower without a justified coverage gain.
- Revert or redesign if a performance claim cannot be reproduced under equivalent semantics.
- Keep the existing architecture and report the evidence if the movement vertical is disproved by a fundamental semantic conflict.

## Stop Conditions

- Stop when one movement vertical is complete and verified.
- Stop before adding movement to another ordered owner.
- Stop before adding cross-parent movement.
- Stop before adding persistence.
- Stop before adding daemonization.
- Stop before adding collaboration.
- Stop before adding generic conflict handling.
- Stop before starting the crate-boundary audit.
- Stop before redesigning syntax unless movement exposed a direct correctness defect there.
- Stop before optimizing unrelated runtime code.
- Stop before broadening pattern construction.
- Stop before implementing direct nominal-member mutation.
- Stop before adding unresolved-reference kinds unrelated to this movement.

## Semantic Contract: Sequence

- Use the repository's existing semantic sequence representation.
- At the grounding commit, source-free `DraftNode::Sequence` lowers to the HIR sequence form represented as `ExprKind::Do`.
- Do not add a second sequence node kind.
- A sequence owns an ordered vector of direct semantic child expressions.
- Child order is evaluation order.
- An empty sequence yields `unit` under the existing language contract.
- A non-empty sequence yields the value of its final child under the existing language contract.
- A move changes order only.
- A move does not clone the child.
- A move does not change child contents.
- A move does not rename an entity.
- A move does not alter stable identity.
- A move may change program behavior because evaluation order is semantic.
- A move may change the sequence result when it changes the final child.
- Any resulting type must satisfy the canonical owning context.
- Any resulting divergence must satisfy canonical reachability rules.
- Any resulting ownership order must satisfy canonical ownership validation.
- Any resulting effect order must be represented honestly.
- Any resulting cleanup order must be derived canonically.
- Any moved child containing holes or unresolved references remains incomplete rather than becoming executable.

## Semantic Contract: Identity

- The sequence `NodeId` is stable across the move.
- The moved child `NodeId` is stable across the move.
- Every surviving descendant `NodeId` below the moved child is stable across the move.
- Every surviving sibling `NodeId` is stable across the move.
- Every surviving descendant of unmoved siblings is stable across the move.
- The containing callable `EntityId` is stable.
- Stable binding entities inside moved subtrees remain stable when their semantic ownership remains unchanged.
- Stable payload bindings inside moved match subtrees remain stable when their semantic ownership remains unchanged.
- Stable hole identity remains stable if a hole subtree is moved.
- Stable unresolved-reference identity remains stable if an unresolved subtree is moved.
- Private `NodeAddress.preorder` values may change.
- Private parent/child ordinals may change.
- Private dense bindings may compact or relocate only through existing canonical rules.
- Public identity continuity must be explicit, not inferred from content fingerprints.
- Equal content does not establish identity.
- Order position does not establish identity.
- Source span does not establish identity.
- Path does not establish identity.
- Name does not establish node identity.
- Hash does not establish node identity.
- A moved subtree must not receive fresh generations.
- A moved subtree must not tombstone its prior identities.
- No stale old address may remain accepted as a current public identity.
- The new snapshot's index must resolve every preserved identity to its new private address.
- The old snapshot's index must continue resolving the same identities to its old private addresses.
- Identity reconciliation must reject duplicate or colliding forced mappings before publication.
- Identity reconciliation must reject missing moved descendants before publication.
- Identity reconciliation must reject wrong-kind moved mappings before publication.

## Semantic Contract: Destination

- The destination is inside the same direct-child list.
- The destination anchor, when present, must be a direct child of the same sequence.
- The moved child must be a direct child of the sequence in the base snapshot.
- The sequence must be the actual direct parent of the child.
- A descendant is not a direct child.
- An ancestor is not a direct child.
- A sibling from another sequence is not a valid anchor.
- A node from another callable is not a valid anchor.
- A node from another namespace is not a valid anchor.
- A stale node generation is not a valid anchor.
- A stale base revision is rejected before movement staging.
- A wrong-kind sequence node is rejected.
- A hole node is not a sequence owner.
- An unresolved-reference node is not a sequence owner.
- A call argument list is not silently treated as a sequence.
- A loop body is not silently treated as a sequence unless it is explicitly represented by a selected sequence node.
- A while body is not silently treated as a sequence unless it is explicitly represented by a selected sequence node.
- A match-arm list is not a sequence-child destination.
- Product fields are not sequence children.
- Enum fields are not sequence children.
- Function order is not sequence order.
- Appending must have one deterministic meaning.
- Moving before an anchor must have one deterministic meaning.
- The API must define or consistently reject self-anchoring.
- The API must define no-op movement consistently with existing transaction no-op behavior.
- No-op behavior must be tested.

## Transaction Semantics

- Movement participates in the existing `Transaction` type.
- Movement is revision-checked.
- Movement is namespace-checked.
- Movement is generation-checked.
- Movement is kind-checked.
- Movement is owner-checked.
- Movement is direct-parent-checked.
- Movement is destination-checked.
- Movement is final-state validated before publication.
- One successful transaction publishes one revision.
- One failed transaction publishes nothing.
- The allocator is staged, not mutated eagerly.
- The base snapshot remains semantically unchanged after failure.
- Diagnostics remain unchanged after failure.
- Blockers remain unchanged after failure.
- Continuations remain unchanged after failure.
- Future deterministic ID allocation remains unchanged after failure.
- Movement may coexist with independent edits only when semantics remain simple and explicit.
- Multiple movements in disjoint sequence roots may be supported if existing preflight can establish disjointness simply.
- At most one movement per sequence in one transaction is an acceptable first contract.
- Reject overlapping movements rather than building a movement planner.
- Reject a parent sequence move combined with a descendant structural edit when overlap cannot be expressed safely.
- Reject movement of a child whose containing root is deleted in the same transaction.
- Reject movement against a sequence deleted in the same transaction.
- Reject movement against an anchor deleted in the same transaction.
- Do not make edit-list order an undocumented semantic dependency.
- If movement order within a batch matters, define it explicitly or reject the ambiguous batch.
- Prefer final-state validation for independent deletion dependencies.
- Do not weaken existing order-independent deletion semantics.
- Do not create hidden intermediate revisions.
- Do not publish an intermediate order.
- Do not execute code while staging.

## Structural Preflight

- Extend structural preflight intentionally rather than bypassing it.
- Represent movement conflicts separately if replacement overlap logic cannot describe them cleanly.
- Do not encode movement as a synthetic replacement draft.
- Detect duplicate movement of the same child.
- Detect duplicate movement of the same sequence when the first contract disallows it.
- Detect movement plus replacement of the same child.
- Detect movement plus replacement of an ancestor.
- Detect movement plus replacement of the sequence.
- Detect movement plus hole introduction at the same child.
- Detect movement plus unresolved-reference introduction at the same child.
- Detect movement plus fill of a hole inside the moved subtree if address stability cannot be established safely.
- Detect movement plus deletion of the owning callable.
- Detect movement plus deletion of a dependency when final validation requires it.
- Allow genuinely disjoint edits when existing transaction semantics support them.
- Use stable identities for conflict reporting.
- Produce deterministic errors.
- Do not leak private addresses in public errors.
- Do not silently drop one conflicting edit.
- Do not select the first valid interpretation.

## Mutation Strategy

- Clone the semantic program through the existing fallible staged path.
- Resolve sequence, child, and anchor against the base snapshot.
- Capture the moved subtree's complete stable identity mapping before mutation.
- Capture the containing sequence's stable identity.
- Capture stable identities for surviving siblings as required by reconciliation.
- Mutate the existing sequence child vector directly in staged semantic state.
- Use checked indexing only after stable identities resolve to verified private positions.
- Remove the child exactly once.
- Adjust destination position correctly after removal.
- Insert the same owned expression value exactly once.
- Do not clone the moved expression merely to reorder it.
- Do not drop the moved expression during the operation.
- Do not duplicate effects.
- Do not duplicate ownership.
- Do not create a temporary semantic hole.
- Do not create a temporary unresolved reference.
- Do not render source.
- Do not parse source.
- Do not serialize the semantic program.
- Do not hash the semantic program to establish continuity.
- Refresh derived sequence and ancestor result types through existing canonical code or a small shared local function.
- Refresh semantic match result facts only when affected.
- Run existing compaction only if the general transaction path requires it; do not add movement-specific compaction.
- Run existing partial effect inference.
- Run complete validation when the staged snapshot is complete.
- Build canonical indexes once.
- Reconcile identities once.
- Publish only after all fallible copies and outputs succeed.

## Identity Relocation Strategy

- Do not rely on existing parent/child-ordinal matching alone for the moved subtree.
- A reorder intentionally changes child ordinals.
- Create an explicit one-to-one relocation from each old public moved-subtree identity to its new private address.
- Preserve the sequence identity explicitly if normal reconciliation cannot infer it safely.
- Preserve unmoved siblings through normal explicit structural matching or forced mappings as appropriate.
- Use one iterative traversal to enumerate the moved subtree before or after mutation.
- Use one iterative traversal to enumerate the corresponding moved subtree after mutation.
- Prove shape correspondence without recursive native-stack traversal.
- Require equal node counts for the relocated subtree.
- Require matching semantic child structure for the same moved value.
- Require matching node kinds where continuity is asserted.
- Do not use expression equality as the sole continuity proof.
- Do not use a fingerprint.
- Do not use source origin.
- Do not use source span.
- Do not use debug rendering.
- Do not allow a forced moved identity to collide with another survivor.
- Do not allow two old identities to map to one new address.
- Do not allow one old identity to map to two new addresses.
- Do not preserve identities for nodes actually replaced by another simultaneous edit.
- Keep moved hole and unresolved records attached by stable identity and refreshed address.
- Keep old snapshots queryable.

## Type and Result Semantics

- Recompute the sequence's semantic result from the final ordered child list.
- An empty sequence remains `unit`.
- A non-empty sequence takes its result type from the final child under the existing language law.
- Do not store a stale pre-move sequence type.
- Do not silently insert a conversion.
- Do not silently wrap the sequence.
- Do not silently discard a new final value.
- Validate the sequence result against its owning expression context.
- Validate an entry body result against `main` return type.
- Validate a function body result against its declared return type.
- Validate a branch result against its enclosing conditional.
- Validate a local body result against its enclosing context.
- Validate a match arm result through canonical match facts.
- Validate loop-related result types through existing control semantics.
- Reject `never` placement that makes later sequence entries invalid.
- Reject a moved final child whose result violates the expected type.
- Allow a final-child change only when the canonical final program remains valid.
- Do not create a general bidirectional type-repair system.

## Control-Flow Semantics

- Sequence order determines reachability.
- Moving `return` earlier may make later entries unreachable.
- Moving `break` earlier may make later loop-body entries unreachable.
- Moving `continue` earlier may make later loop-body entries unreachable.
- Moving a divergent expression earlier may make later entries unreachable.
- Canonical validation must reject invalid later entries.
- Do not delete newly unreachable entries automatically.
- Do not reorder around divergence automatically.
- Do not treat movement as formatting.
- Preserve existing nearest-loop target semantics.
- Do not expose private loop IDs.
- Do not retarget `break` or `continue` merely because order changed.
- Do not create label syntax.
- Do not add control-flow normalization to make a move pass.
- Ensure complete-HIR consistency independently checks the moved result.

## Ownership and Cleanup Semantics

- Evaluation order is ownership-significant.
- Moving a borrow after a move may be invalid.
- Moving a move before its final use may be invalid.
- Moving a drop-producing operation may change cleanup order.
- Moving an allocation may change live-owner intervals.
- Moving an early return may change cleanup edges.
- Moving within a loop body may change backedge ownership state only when the selected sequence is inside that body.
- Use the canonical HIR ownership checker.
- Use the canonical SSA ownership and dataflow laws.
- Use the canonical memory plan.
- Do not add a movement-specific ownership approximation.
- Do not declare a move legal from local syntactic checks alone.
- Do not suppress ownership errors.
- Do not preserve stale loan state.
- Do not duplicate owners.
- Do not leak moved affine values.
- Do not double-release moved affine values.
- Do not rerun effects through native fallback.
- Test normal return cleanup.
- Test invalid reordered ownership atomically.

## Effect Semantics

- Reordering effectful children is an intentional semantic change.
- Preserve each child's effect facts.
- Recompute enclosing effect summaries through existing inference.
- Do not sort effects.
- Do not commute effects automatically.
- Do not assume pure children unless canonical facts say so.
- Unknown effects remain unknown while holes or unresolved references remain.
- Do not execute effects during transaction staging.
- Do not execute effects during check.
- Run tests whose observable result proves changed order.
- Run tests whose would-be host effect remains unreachable during check.

## Incomplete-State Semantics

- An incomplete snapshot may be edited.
- A moved hole remains a real hole.
- A moved unresolved value reference remains a real unresolved value reference.
- The moved incomplete node keeps its stable identity.
- The moved incomplete node's private address refreshes.
- The moved incomplete node's owner remains correct.
- The moved incomplete node's context remains correct.
- Visible entities refresh from final semantic position when position can affect scope.
- Expected type refreshes from final semantic context when context can affect type.
- Unknown effects remain explicit.
- Diagnostics remain revision-correct.
- Blockers remain revision-correct.
- Projection shows the incomplete node in its new order.
- Queries return the incomplete node in its new order.
- Compilation rejects before complete-HIR derivation.
- No hidden executable expression survives behind the incomplete node.
- No movement operation resolves an incomplete node implicitly.
- No candidate is selected implicitly.
- Old snapshots retain the old incomplete position.

## Imported-State Semantics

- Text import still occurs only at importer entry.
- After import, movement acts on the semantic snapshot.
- Movement does not edit source text.
- Movement may drop presentation or source attachments under the existing semantic-edit policy.
- Do not fabricate updated source spans.
- Do not fabricate a source digest.
- Do not render a replacement source file.
- Do not retain source as a sibling semantic authority.
- Compilation after movement derives complete HIR directly from semantic state.
- Measure or assert zero source-loading and parser invocations during movement and post-move direct compilation where existing counters permit.
- Imported and source-free equivalent semantic programs should exhibit the same movement rules.

## Query Contract

- Entity and node queries remain revision-labelled.
- Containment queries expose the new child order.
- Node semantics for the moved node retain its stable identity.
- Definitions and references remain attached to the same stable nodes and entities.
- Calls remain attached to the same stable call sites and callees unless semantics independently change.
- Match views retain stable match and arm-body identities.
- Hole context queries locate moved holes correctly.
- Unresolved-reference state queries locate moved unresolved nodes correctly.
- Legal-constructor queries use final control and expected-type context.
- Old continuations remain bound to the old revision.
- Using an old continuation against the new revision fails deterministically.
- Pagination order remains deterministic.
- No query returns private preorder as semantic identity.
- No query silently truncates moved children.
- Projection and query agree on containment order.

## Projection Contract

- The selected projection renders the moved child at its new semantic position.
- The projection retains review-local labels consistently within one rendering.
- The projection does not use labels as input identity.
- The projection does not fabricate source text.
- The projection remains deterministic.
- The projection remains stack-safe.
- The projection remains fallible on allocation failure.
- The projection shows holes and unresolved nodes explicitly.
- The projection does not dump all candidates merely because a node moved.
- The projection does not emit private addresses.
- The projection should not gain a new general patch syntax.

## Semantic Diff Contract

Add one explicit movement diff entry or an equally compact stable-identity representation.
A recommended shape is:

```rust
SequenceChildMoved {
    sequence: NodeId,
    child: NodeId,
    old_predecessor: Option<NodeId>,
    old_successor: Option<NodeId>,
    new_predecessor: Option<NodeId>,
    new_successor: Option<NodeId>,
}
```

- Use stable `NodeId` values.
- Do not expose numeric indexes.
- Do not expose preorders.
- Do not expose private addresses.
- Describe the old neighborhood.
- Describe the new neighborhood.
- Keep the entry deterministic.
- Do not emit `DescendantDeleted` for the moved subtree.
- Do not emit `DescendantCreated` for the moved subtree.
- Do not emit `ExpressionReplaced` merely because order changed.
- Do not emit reference rewires when reference targets did not change.
- Do not emit call rewires when callees did not change.
- Continue emitting independent entries for genuinely independent edits in the same transaction.
- Define no-op diff behavior.
- Sort or construct entries under existing deterministic diff rules.
- Keep invalidation conservative if no consumer justifies finer invalidation.
- Do not add an incremental cache merely because movement has a distinct diff.

## Error Contract

- Use existing `WorkspaceError` categories where they express the failure accurately.
- Add a narrow error fact only when existing categories cannot distinguish a public contract violation.
- Reject foreign sequence identity.
- Reject stale sequence identity.
- Reject wrong-kind sequence identity.
- Reject foreign child identity.
- Reject stale child identity.
- Reject child not owned directly by sequence.
- Reject foreign anchor identity.
- Reject stale anchor identity.
- Reject anchor not owned directly by sequence.
- Reject self-anchor when the chosen API defines it as invalid.
- Reject conflicting movement edits.
- Reject movement against a deleted owner.
- Reject final type inconsistency.
- Reject final control-flow inconsistency.
- Reject final ownership inconsistency.
- Reject final match inconsistency.
- Reject allocation failure without publication.
- Do not include private IDs in messages.
- Keep diagnostics deterministic.
- Test errors structurally where a structured type exists.

## No-Op Policy

- Inspect current transaction behavior for semantic no-ops.
- Define moving a child to its current position.
- Define moving the final child to append.
- Define moving a child immediately before its current successor.
- Define a movement included beside another real edit.
- Prefer one consistent policy over ad hoc special cases.
- An acceptable policy is to accept the transaction, omit the movement diff, and follow existing revision publication rules.
- Another acceptable policy is to reject a movement-only no-op as invalid if that matches established API policy.
- Do not publish a misleading movement diff.
- Do not create and tombstone identities for a no-op.
- Document and test the selected policy.

## Batching Policy

- Support independent non-overlapping edits when simple.
- Allow movement in separate callable roots if preflight can prove disjointness without a planner.
- Reject two movements targeting the same sequence in the first implementation unless a small deterministic algorithm already falls out naturally.
- Reject movement of a sequence child combined with replacement of that child.
- Reject movement of a sequence child combined with replacement of the sequence.
- Reject movement of a child combined with replacement of an ancestor.
- Reject movement of an ancestor sequence combined with movement inside its descendant subtree unless exact final semantics are simple and tested.
- Resolve movement identities against the base revision unless the transaction contract explicitly and coherently says otherwise.
- Do not make later edits refer to private post-move addresses.
- Do not add transaction-local movement handles.
- Do not add a topological sorter for edit lists.
- Do not create a generic edit dependency graph.
- Use conservative rejection for ambiguous overlap.
- Test reversed independent edit order when the contract promises order independence.

## Implementation Phases

1. Inspect worktree, branch, upstream, and current commit.
2. Read root `AGENTS.md`, workspace specification, status, architecture, performance, and roadmap.
3. Map the edit enum, transaction staging, sequence representation, index, reconciliation, diff, query, projection, and tests.
4. Write the falsifiable hypothesis, completion criteria, reversal condition, and stop condition in temporary working state.
5. Add the smallest focused failing API and identity test.
6. Add public edit and diff shapes.
7. Add validation and conflict preflight.
8. Add staged direct sequence mutation.
9. Add explicit moved-subtree identity relocation.
10. Refresh incomplete records and derived facts through existing paths.
11. Add focused success, invalidity, and failure-atomicity tests.
12. Add imported and source-free convergence evidence.
13. Add modest scale and small-stack evidence.
14. Measure representative movement cost only after correctness is stable.
15. Remove temporary instrumentation without a continuing consumer.
16. Update owning documentation and delete stale statements.
17. Run focused verification.
18. Run final native verification once final relevant inputs are stable.
19. Run retained container verification because compiler and executable behavior changed.
20. Inspect the complete diff for duplicate paths and stale compatibility code.
21. Create cohesive commit or commits if permitted.
22. Produce a factual final report and stop.

## Likely Files and Symbols

- `crates/lkjscript-compiler/src/workspace/transaction.rs`: public edit, diff entry, preflight, staging, mutation integration, and movement-specific forced identity mapping.
- `crates/lkjscript-compiler/src/workspace/identity.rs`: reconciliation rules if current forced-node support needs a narrow extension.
- `crates/lkjscript-compiler/src/workspace/index.rs`: final containment and private address derivation.
- `crates/lkjscript-compiler/src/workspace/model.rs`: private record or address facts only if required.
- `crates/lkjscript-compiler/src/workspace/query.rs`: ordered query behavior and moved incomplete context.
- `crates/lkjscript-compiler/src/workspace/projection.rs`: selected-order rendering tests or implementation.
- `crates/lkjscript-compiler/src/workspace/tests.rs`: focused semantic, identity, atomicity, query, projection, and convergence tests.
- `crates/lkjscript-compiler/src/workspace/recompute_measurement.rs`: only if extending the retained measurement corpus is justified.
- `crates/lkjscript-compiler/src/hir/expressions.rs`: existing iterative child traversal and `ExprKind::Do` semantics; change only when necessary.
- `crates/lkjscript-compiler/src/lib.rs`: public re-exports if required.
- `crates/lkjscript-app/src/engine.rs`: product-path execution evidence only if compiler tests cannot prove the selected route.
- `docs/spec/workspace.md`: normative movement contract.
- `docs/status.md`: current implementation and remaining gaps.
- `docs/architecture.md`: staging, mutation, identity relocation, and validation flow.
- `docs/performance.md`: compact reproducible measurement only when performed.
- `docs/roadmap.md`: mark the one public movement vertical complete and advance the next item.
- `README.md`: update only if first-use or public summary materially changes.

These are investigation targets, not a mandate to touch every file.
Change only files that own a changed fact or executable behavior.
Do not create an additional movement module merely for symmetry.

## Implementation Design Review

- Confirm whether existing `force_surviving_nodes` can accept a movement relocation without semantic abuse.
- Confirm whether node keys include child ordinals and therefore cannot infer movement continuity.
- Confirm whether sequence mutation should happen before or after other structural replacements.
- Confirm whether incomplete record pruning assumes replacement-only structural edits.
- Confirm whether match-plan refresh accepts a changed order without a synthetic replacement action.
- Confirm whether sequence result recomputation already exists in `replace_expression` or a reusable helper.
- Confirm whether moved nested local bindings require dense binding relocation.
- Confirm whether source attachments are dropped or remapped under existing semantic edits.
- Confirm whether movement invalidates provenance.
- Confirm whether all semantic domains should remain conservatively invalidated.
- Confirm whether semantic diff consumers are exhaustive.
- Confirm whether public re-exports need updating.
- Confirm whether tests can isolate movement in a dedicated coherent submodule without artificial sharding.
- Confirm whether existing generated sequence fixtures can be reused.
- Confirm whether current measurement schema can represent movement without becoming a public protocol.

## Focused Test Matrix: Basic Success

- Move the first of three direct sequence children before the third.
- Move the middle of three direct sequence children to the end.
- Move the last of three direct sequence children before the first.
- Move a child one position left.
- Move a child one position right.
- Move a child across multiple siblings.
- Move a pure child.
- Move an effectful child.
- Move a child with nested descendants.
- Move a child containing a call.
- Move a child containing a product construction.
- Move a child containing an enum match.
- Move a child inside a function body sequence.
- Move a child inside `main`.
- Compile the final snapshot.
- Execute the final snapshot.
- Assert the changed semantic outcome.
- Assert no cleanup failure.

## Focused Test Matrix: Identity

- Record the sequence ID before movement.
- Record every direct child ID before movement.
- Record every descendant ID below the moved child.
- Record selected descendants below unmoved siblings.
- Apply the movement.
- Assert the sequence ID is unchanged.
- Assert the moved child ID is unchanged.
- Assert every moved descendant ID is unchanged.
- Assert every unmoved sibling ID is unchanged.
- Assert selected unmoved descendants are unchanged.
- Assert each preserved ID resolves in the new snapshot.
- Assert each preserved ID resolves in the old snapshot.
- Assert old and new private order differs.
- Assert no moved identity appears in deleted entries.
- Assert no moved identity appears in created entries.
- Assert the movement diff names the stable moved child.
- Assert the movement diff names stable neighboring anchors.
- Assert no generation changes.
- Assert a later unrelated edit preserves the moved identity.
- Assert private compaction after a later deletion still preserves the moved identity.

## Focused Test Matrix: Observable Order

- Use a mutable local whose body is an explicit sequence.
- Use two assignments with distinct values.
- Keep a final load as the sequence result.
- Run the base snapshot and record the base value.
- Move the two assignments into the opposite order.
- Run the new snapshot and assert the new value differs exactly as predicted.
- Ensure the final load remains final when testing assignment order.
- Assert the same selected execution route where eligibility is unchanged.
- Assert no duplicated assignment effect.
- Assert no skipped assignment effect.
- Assert exact cleanup observations when an owned value participates.

## Focused Test Matrix: Result Type

- Move non-final children while preserving the final child and result type.
- Move a same-typed child into the final position and accept when the owning context remains valid.
- Move a differently typed child into the final position and reject when the owning context becomes invalid.
- Assert failure publishes no revision.
- Assert failure preserves IDs and allocator state.
- Move the prior final child earlier while another exact-typed child becomes final.
- Assert the final sequence type is recomputed rather than retained stale.
- Test an empty-sequence edge only if the API can reach it without another operation.
- Do not add a special coercion to satisfy the test.

## Focused Test Matrix: Control Flow

- Move an early return before a later ordinary child and assert canonical rejection.
- Move an early return later when the final program is valid and assert success.
- Move `continue` before a later loop-body child and assert canonical rejection where a child becomes unreachable.
- Move `break` before a later loop-body child and assert canonical rejection where a child becomes unreachable.
- Move a divergent nested expression and assert derived sequence divergence refresh.
- Assert private loop target identity is unchanged.
- Assert no public loop identity is introduced.
- Assert failure is atomic.
- Assert the old snapshot still executes.

## Focused Test Matrix: Ownership

- Construct a sequence with an owner-producing operation.
- Construct a shared borrow use.
- Construct a later move of the owner.
- Reorder only when the resulting borrow/move order remains valid and assert success.
- Reorder the move before the borrow and assert canonical ownership rejection.
- Assert no new snapshot is published on rejection.
- Assert no owner leaks.
- Assert no double drop.
- Assert no live loan remains after valid execution.
- Assert no release backlog remains.
- Assert the old snapshot remains executable.
- Assert retrying the same invalid move preserves deterministic future identity behavior.

## Focused Test Matrix: Incomplete State

- Create an explicit sequence containing a typed hole as a direct child where existing type rules permit it.
- Move that hole within the same sequence.
- Assert the hole ID is unchanged.
- Assert the hole context ID is unchanged where context is the hole itself.
- Assert the private address changes.
- Assert the blocker remains present.
- Assert the diagnostic is revision-correct.
- Assert projection order changes.
- Assert legal-constructor context is correct.
- Assert compilation rejects before complete-HIR derivation.
- Fill the moved hole and compile successfully.
- Create an unresolved copy-load reference inside a sequence where the existing contract permits it.
- Move the unresolved node.
- Assert unresolved identity and requested spelling are unchanged.
- Assert visible candidates are recomputed from the final snapshot.
- Assert explicit resolution after movement uses the same stable target rules.
- Assert the old snapshot retains the old unresolved position.

## Focused Test Matrix: Imported and Source-Free Convergence

- Construct one representative program through source import.
- Construct its semantic equivalent through source-free transactions.
- Select corresponding semantic sequence and child identities in each independent workspace.
- Apply the same logical movement.
- Compare normalized semantic child order.
- Compare node kinds and structured types.
- Compare effects.
- Compare references and dependencies.
- Compare calls and instantiations where present.
- Compare selected memory obligations.
- Compare normalized bytecode.
- Compare VM or selected product outcomes.
- Compare cleanup observations.
- Assert source-free movement invokes source loading zero times.
- Assert source-free movement invokes parsing zero times.
- Assert post-import movement invokes source loading zero times.
- Assert post-import movement invokes parsing zero times.
- Do not normalize away meaningful semantic differences.

## Focused Test Matrix: Invalid Identity and Ownership

- Foreign namespace sequence.
- Foreign namespace child.
- Foreign namespace anchor.
- Stale generation sequence.
- Stale generation child.
- Stale generation anchor.
- Wrong-kind sequence.
- Child that is not a direct child.
- Grandchild presented as direct child.
- Ancestor presented as child.
- Anchor from another sequence.
- Anchor from another callable.
- Deleted sequence in the same transaction.
- Deleted child owner in the same transaction.
- Replaced sequence in the same transaction.
- Replaced child in the same transaction.
- Hole-introduced child in the same transaction.
- Duplicate movement of the same child.
- Two moves in the same sequence when unsupported.
- Self-anchor when invalid.
- Stale base revision.
- Malformed no-op form under the chosen contract.

## Focused Test Matrix: Failure Atomicity

- Capture the exact base `Arc`.
- Capture base revision.
- Capture diagnostics.
- Capture blockers.
- Capture projection.
- Capture selected queries.
- Capture allocator-derived next identity through the existing deterministic test method.
- Submit an invalid wrong-parent move.
- Assert the exact current `Arc` is retained.
- Assert revision is unchanged.
- Assert diagnostics are unchanged.
- Assert blockers are unchanged.
- Assert projection is unchanged.
- Assert selected queries are unchanged.
- Assert future ID allocation is unchanged.
- Repeat for a final-type-invalid move.
- Repeat for an ownership-invalid move.
- Repeat for a conflicting batch.
- Repeat for a forced allocation failure if an existing harness can target the new path.

## Focused Test Matrix: Diff

- Assert exactly one movement entry for a movement-only transaction.
- Assert the entry uses stable sequence identity.
- Assert the entry uses stable moved-child identity.
- Assert old predecessor is exact.
- Assert old successor is exact.
- Assert new predecessor is exact.
- Assert new successor is exact.
- Assert append has a deterministic neighborhood representation.
- Assert move-to-front has a deterministic neighborhood representation.
- Assert no delete entry for the moved subtree.
- Assert no create entry for the moved subtree.
- Assert no replacement entry for pure movement.
- Assert no reference rewire when targets are unchanged.
- Assert no call rewire when callees are unchanged.
- Assert independent rename or replacement entries still appear when a supported disjoint batch succeeds.
- Assert diff order is deterministic across equivalent construction order.
- Assert no-op movement follows the documented diff rule.

## Focused Test Matrix: Query and Projection

- Query sequence containment before movement.
- Query sequence containment after movement.
- Assert only order changes.
- Query moved node semantics before movement.
- Query moved node semantics after movement.
- Assert identity and intrinsic kind remain.
- Assert actual or expected type changes only when semantically required.
- Project the selected body before movement.
- Project the selected body after movement.
- Assert deterministic new order.
- Assert no source attachment is required.
- Assert an old revision continuation cannot be reused against the new revision.
- Assert old snapshot projection remains unchanged.
- Assert moved match view remains coherent when a whole match child moves.
- Assert moved hole or unresolved view remains coherent.

## Focused Test Matrix: Scale and Stack Safety

- Generate a wide sequence with at least 512 direct children through existing source-free helpers.
- Move one child from near the start to near the end.
- Assert exact final order.
- Assert every sampled stable identity is preserved.
- Assert no duplicate or missing child.
- Assert compilation and execution succeed.
- Measure deterministic traversal work if existing counters answer complexity directly.
- Require movement-specific work to be linear in the traversed sequence or root, not quadratic in child count.
- Use a 128 KiB worker stack for one moved child containing a deep existing iterative subtree if existing harnesses support it.
- Assert movement staging is stack-safe.
- Assert identity relocation is stack-safe.
- Assert index construction is stack-safe.
- Assert projection or selected query is stack-safe.
- Assert destruction is stack-safe.
- Keep default geometry modest.
- Add a larger ignored locked-release stress only with a clear continuing consumer.
- Do not introduce a child-count quota.
- Do not introduce a depth quota.

## Focused Test Matrix: Product Path

- Compile the final complete moved snapshot through `compile_snapshot`.
- Validate bytecode.
- Execute through the selected product path.
- Record whether the path is baseline-native or VM fallback.
- Do not force an engine.
- Do not add a threshold.
- Do not add a movement-specific runtime path.
- Assert exactly one execution.
- Assert no retry after native entry.
- Assert exact returned value.
- Assert exact cleanup outcome.
- Use VM-only evidence as additional coverage, not a replacement for selected product-path evidence when natively eligible.

## Performance and Complexity Work

- Correctness and semantic coherence come before timing.
- Do not optimize before identifying measured work.
- Reuse existing transaction measurement instrumentation when it answers the question.
- Add a movement workload to the retained workspace measurement corpus only if it will remain useful.
- Otherwise use a focused temporary harness and remove it after recording compact evidence.
- State workload, environment, build profile, cache state, sample count, median, tail orientation, and selection criterion.
- Separate transaction time from query, projection, compile, and execution time.
- Record deterministic work counts when more reliable than timing.
- Compare equivalent semantics.
- Keep raw samples under ignored `target/` paths.
- Commit only compact reproducible evidence.
- Do not create a hard timing gate from one developer machine.
- Do not claim a general speedup from a single sample.
- Do not infer API billing savings from output bytes alone.
- Label model or API-cost effects as measured, estimated with assumptions, or unknown.
- A useful acceptance target is no material regression in representative edit, query, projection, and compile latency.
- A useful algorithmic target is one direct-child lookup, one vector reorder, and one bounded root, index, and reconciliation pass under current full recomputation.
- Do not add incrementality unless movement measurements unexpectedly show recomputation dominates a representative current consumer and the mechanism is smaller than removed work.
- Compilation may remain the dominant agent check phase.
- Full semantic recomputation may remain the selected design.

## Agent and API-Cost Discipline

- Search for symbols before opening entire large files.
- Read focused ranges before full 600 KiB test modules.
- Reuse existing test helpers.
- Do not ask subagents to duplicate broad repository mapping.
- Use subagents only for independent read-only questions or isolated reviews.
- Return compact evidence packets from subagents.
- Do not paste complete successful logs into model context.
- Use native quiet flags.
- Run the smallest test that can falsify the current hypothesis.
- Do not rerun an identical successful command after no relevant input changed.
- Do not repeatedly regenerate complete projections.
- Do not dump full HIR, SSA, bytecode, or machine code without a consumer.
- Prefer stable IDs and compact movement diffs in tests and reports.
- Measure output bytes or lines only when making an output-volume claim.
- Do not add a daemon merely to avoid process startup.
- Do not add persistence merely to survive model turns.
- Use the immutable snapshot and concise final report as the handoff boundary.
- Keep the final report compact enough for the next turn to consume.

## Anti-Overengineering Gate

1. What exact present problem is being solved?
2. Who currently produces the state?
3. Who currently consumes the state?
4. Who owns any new state?
5. What is its lifetime?
6. What invalidates it?
7. How does allocation failure behave?
8. How does transaction failure behave?
9. What existing invariant can be reused?
10. Why is direct local code insufficient?
11. What invalid state becomes unrepresentable?
12. What duplicated work is removed?
13. What maintenance cost is added?
14. What is the reversal condition?
15. What is the deletion condition?

Answer these questions in temporary working state before adding any abstraction beyond the edit variant, a small movement plan, and an explicit relocation map.
If the answers are weak, use direct local code.

## Architecture Constraints

- Keep one semantic authority.
- Keep one identity model.
- Keep one incomplete-state model.
- Keep one compiler route.
- Keep one ownership model.
- Keep one generic production execution route.
- Keep one package model.
- Keep one documentation authority model.
- Keep one movement implementation for this vertical.
- Do not retain a replace-based alternative.
- Do not create a movement service.
- Do not create a movement cache.
- Do not create a movement IR.
- Do not create movement bytecode.
- Do not add runtime movement support; this is an authoring-time semantic edit.
- Do not move code between crates without evidence that the boundary blocks the vertical.
- Do not split `workspace/tests.rs` solely because it is large.
- You may place focused movement tests in a coherent test submodule if that establishes ownership and reduces navigation cost without artificial sharding.
- Any new module must have a coherent responsibility and current consumer.

## Backward-Incompatible Change Policy

- Change the edit variant name if a better exact name exists.
- Change semantic diff shapes if needed.
- Change test helpers if needed.
- Change public re-exports if needed.
- Change documentation examples if needed.
- Delete stale unsupported placeholders.
- Delete obsolete movement-related TODO prose.
- Delete any compatibility adapter discovered on the changed path.
- Do not preserve an old provisional API.
- Do not add `MoveSequenceChildV2`.
- Do not add a `legacy` feature.
- Do not add a migration layer.
- Do not dual-emit old and new diffs.
- Do not keep old tests protecting accidental topology.
- Preserve unrelated current external semantic contracts unless this vertical proves them wrong.

## Documentation Work

- Update `docs/spec/workspace.md` with the normative movement contract.
- Define the exact sequence owner.
- Define direct-child requirements.
- Define stable identity continuity.
- Define destination-anchor semantics.
- Define batch conflict semantics.
- Define no-op semantics.
- Define incomplete-state behavior.
- Define compilation rejection and final validation.
- Define diff behavior.
- Define old-snapshot behavior.
- Define failure atomicity.
- Update `docs/status.md` only with demonstrated current behavior.
- Remove public movement from the known-gap list only for the completed sequence vertical.
- Keep broader movement explicitly unimplemented.
- Update `docs/architecture.md` with staging, direct mutation, relocation, index rebuild, and validation flow.
- Do not describe a generic movement framework.
- Update `docs/performance.md` only with reproducible compact evidence.
- Update `docs/roadmap.md` so `Now` reflects the next real item after completion.
- Likely move representation and crate-boundary reassessment into `Now` after this vertical passes.
- Do not claim crate consolidation occurred.
- Update `README.md` only if first-use or public summary materially benefits.
- Delete stale contradictory prose in the same change.
- Do not create a decision record unless the anchor, no-op, or batch choice is durable, non-obvious, expensive to rediscover, and has a meaningful reversal condition.
- Do not add a task summary document.
- Do not add a prompt archive.

## Focused Verification During Iteration

- Run formatting on changed Rust files or the workspace as appropriate.
- Run the smallest focused workspace movement test.
- Run the focused identity-preservation test.
- Run the focused failure-atomicity test.
- Run the focused ownership-invalid reorder test.
- Run the focused imported and source-free convergence test.
- Run the focused query and projection test.
- Run the affected compiler crate test target.
- Run the affected app or product-path test if product execution changed.
- Run the modest scale and small-stack test.
- Do not launch multiple heavyweight Cargo commands concurrently in one target directory.
- Do not rerun the full boundary after unchanged inputs.

## Final Native Verification

Once final relevant inputs are stable, run the repository's retained native boundary exactly:

```sh
cargo fmt --all -- --check
cargo clippy --quiet --workspace --all-targets --all-features --locked -- -D warnings
cargo test --quiet --workspace --all-targets --all-features --locked
cargo build --quiet --workspace --release --locked
```

Run these serially unless an explicit experiment has already established a faster correct topology.
Do not hide non-zero status or diagnostics.

## Retained Container Verification

Because this task changes compiler semantics, transactions, tests, and executable outcomes, run:

```sh
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
```

- Use the final relevant source state.
- Preserve visible failures.
- Do not accept stale cached test executables.
- Confirm the retained source-mtime correction remains effective.
- Do not weaken container coverage to save time.
- If the environment blocks Docker, report the exact command, failure category, relevant output, successful remaining evidence, and residual risk.

## Additional Verification

- Run the ignored locked-release movement stress only if one is added.
- Run existing deep semantic workspace stresses affected by identity reconciliation when practical.
- Run existing deletion and compaction tests because forced survivor relocation changes may affect them.
- Run existing hole and unresolved-reference lifecycle tests.
- Run existing early-return and typed-loop control tests.
- Run existing ownership-control convergence tests.
- Run existing source-free and imported convergence tests.
- Run machine-output tests only if output schemas change.
- Run Miri, sanitizers, fuzzing, or property tests only when the changed boundary benefits and the environment supports them.
- Do not claim unrun checks passed.

## Code Quality Requirements

- No `unwrap` in production code.
- No `expect` in production code.
- No `panic` in production code.
- No `todo`.
- No `unimplemented`.
- No new unsafe code.
- Use checked arithmetic.
- Use checked narrowing before host indexing.
- Use fallible allocation where the surrounding public boundary is fallible.
- Use iterative traversal for user-controlled depth.
- Keep errors deterministic.
- Keep successful high-frequency paths quiet.
- Keep public recursive types stack-safe to clone, compare, project, validate, convert, rewrite, and destroy.
- Do not introduce arbitrary limits.
- Do not leave dead branches or shadow implementations.
- Do not duplicate semantic validation.
- Do not serialize in-process typed state without a boundary.
- Do not add dependencies unless they remove substantially more machinery or risk.

## Commit Construction

- Commit only cohesive repository changes.
- A single cohesive commit is preferred if the repository remains coherent throughout.
- Multiple commits are acceptable when each leaves a complete coherent state.
- Do not split commits by file type.
- Name the semantic result in the commit subject.
- A suitable subject is `Add identity-preserving sequence movement` if accurate.
- Exclude the task prompt.
- Exclude raw logs.
- Exclude raw benchmark samples.
- Exclude temporary scripts without a continuing consumer.
- Exclude credentials.
- Exclude unrelated work.
- Do not push unless explicitly requested.

## Final Report Contract

- State the completed objective.
- State the demonstrated need and evidence gate.
- State the exact public movement contract.
- State the final API shape.
- State identity continuity behavior.
- State batch and no-op behavior.
- State the principal implementation strategy.
- State any deleted or replaced path.
- State focused tests and exact outcomes.
- State imported and source-free convergence evidence.
- State ownership and cleanup evidence.
- State scale and stack-safety evidence.
- State performance measurements and limitations.
- State output or API-cost evidence only when measured.
- List exact verification commands and outcomes.
- State environment-limited checks.
- State documentation updates.
- State commit IDs.
- State publication status.
- State remaining risk.
- Name the next highest-leverage problem.
- Explain why work stopped before starting it.
- Do not reproduce this prompt.
- Do not paste complete successful logs.
- Do not claim future work is implemented.

## Detailed Acceptance Checklist

- The checkout was inspected before editing.
- Unrelated work was preserved.
- Current authority documents were read.
- The exact sequence representation was confirmed.
- The public edit enum has one movement variant for this vertical.
- The movement variant is non-source-based.
- The movement variant is non-index-based.
- The movement variant uses stable identities.
- The sequence identity is validated.
- The child identity is validated.
- The destination identity is validated.
- The direct-parent relation is validated.
- Foreign namespaces reject.
- Stale generations reject.
- Wrong kinds reject.
- Wrong owners reject.
- Wrong direct-parent relations reject.
- Cross-sequence anchors reject.
- Conflicting structural edits reject.
- Batch semantics are explicit.
- No-op semantics are explicit.
- The staged semantic program is mutated directly.
- The moved owned expression is removed once.
- The moved owned expression is inserted once.
- The moved expression is not cloned as the movement mechanism.
- The moved expression is not rendered.
- The moved expression is not parsed.
- The moved expression is not serialized.
- The moved expression is not deleted and recreated.
- The sequence result is refreshed.
- Ancestor result facts are refreshed.
- Effect facts are refreshed.
- Match result facts are refreshed when needed.
- Incomplete records are refreshed.
- Completeness blockers are refreshed.
- Diagnostics are refreshed.
- Canonical ownership validation runs for complete state.
- Canonical match validation runs for complete state.
- Canonical consistency validation runs for complete state.
- Canonical indexes rebuild once under the accepted full-recompute path.
- Moved-subtree public identity is forced explicitly.
- Moved descendants preserve identity.
- Sequence identity preserves continuity.
- Unmoved siblings preserve continuity.
- No forced identity collision is possible.
- Old snapshots remain valid.
- New snapshot exposes new order.
- Diff exposes movement.
- Diff uses stable identities.
- Diff omits private positions.
- Diff omits false deletion.
- Diff omits false creation.
- Diff omits false replacement.
- Queries expose new order.
- Projection exposes new order.
- Continuations remain revision-bound.
- Complete moved snapshots compile directly.
- Check remains effect-free.
- Run executes once.
- Native decline remains pre-entry.
- No VM retry occurs after native entry.
- Valid ownership reorder succeeds.
- Invalid ownership reorder fails atomically.
- Invalid result reorder fails atomically.
- Invalid control reorder fails atomically.
- Invalid identity reorder fails atomically.
- Allocator state is preserved on failure.
- Diagnostics are preserved on failure.
- Blockers are preserved on failure.
- Projection is preserved on failure.
- Future deterministic identity allocation is preserved on failure.
- Imported movement avoids reparsing.
- Source-free movement avoids parsing.
- Convergence tests compare semantic facts.
- Convergence tests compare compiled outcomes.
- Convergence tests compare cleanup.
- Scale test crosses a useful wide-sequence geometry.
- Movement traversal is not quadratic.
- Deep moved subtrees do not recurse on native stack.
- No semantic quota was added.
- No generic movement framework was added.
- No process boundary was added.
- No persistence layer was added.
- No incremental framework was added.
- No compatibility layer was added.
- No duplicate active route remains.
- Specifications describe the target and current contract accurately.
- Status describes demonstrated behavior.
- Architecture describes actual flow.
- Performance contains only reproducible evidence.
- Roadmap advances after the completed vertical.
- Stale gap prose is removed.
- Focused tests pass.
- Affected component tests pass.
- Native final verification passes.
- Retained container verification passes or an exact environment limitation is reported.
- The final diff was reviewed.
- Cohesive commit state is reported.
- Publication state is reported.
- Remaining risk is explicit.
- The next turn is bounded.

## Review Questions Before Declaring Completion

1. Can an agent identify the moved subtree in the next revision using the same `NodeId`?
2. Can an agent identify every moved descendant using the same `NodeId`?
3. Can an agent understand the move from the semantic diff without reading the whole body?
4. Can deterministic machinery reject an invalid move without model inference?
5. Does the operation work on a source-free snapshot?
6. Does the operation work after source import without reparsing?
7. Does a complete valid result compile directly?
8. Does an incomplete result remain explicitly incomplete?
9. Does check remain effect-free?
10. Can any effect execute twice?
11. Can any owner be duplicated?
12. Can any loan become stale?
13. Can any stable identity collide after relocation?
14. Can any failure alter future allocation?
15. Does any private coordinate escape?
16. Does any old compatibility path remain?
17. Did the implementation add more machinery than the problem requires?
18. Did file or crate topology change for evidence rather than aesthetics?
19. Are performance and API-cost claims labelled honestly?
20. Is there exactly one next highest-leverage problem rather than an unbounded wishlist?

## Execution Evidence Ledger

### Repository State

- Record branch name.
- Record HEAD commit.
- Record dirty tracked paths.
- Record relevant untracked paths.
- Record upstream relationship.
- Record which local changes predate this task.
- Record files owned by the movement change.
- Do not commit this ledger.

### Public Contract

- Record selected edit spelling.
- Record selected destination representation.
- Record direct-child law.
- Record same-sequence law.
- Record no-op law.
- Record batch-overlap law.
- Record failure categories.
- Record movement diff shape.

### Identity Proof

- Record how old moved nodes are enumerated.
- Record how new moved nodes are enumerated.
- Record how one-to-one correspondence is proven.
- Record how collisions are rejected.
- Record how sequence continuity is preserved.
- Record how unmoved siblings are preserved.
- Record how holes are refreshed.
- Record how unresolved references are refreshed.

### Canonical Validation

- Record which code recomputes result types.
- Record which code infers partial effects.
- Record which code derives complete HIR.
- Record which code checks ownership.
- Record which code validates match plans.
- Record which code validates semantic consistency.
- Record which code lowers SSA.
- Record which code validates bytecode.

### Focused Evidence

- Record the first failing test.
- Record the first passing implementation state.
- Record identity assertions.
- Record failure-atomicity assertions.
- Record imported and source-free assertions.
- Record product-path assertions.
- Record scale assertions.
- Record small-stack assertions.

### Performance Evidence

- Record representative workload.
- Record equivalent semantics.
- Record environment.
- Record build profile.
- Record warm or cold state.
- Record sample count.
- Record median.
- Record tail orientation.
- Record deterministic work counts.
- Record selection decision.
- Record reversal condition.

### Final Verification

- Record Rustfmt status.
- Record Clippy status.
- Record workspace test status.
- Record release build status.
- Record container verification status.
- Record additional stress status.
- Record environment limitations.
- Record final diff review.

### Final State

- Record changed public behavior.
- Record changed internal behavior.
- Record deleted behavior.
- Record documentation ownership updates.
- Record commit identifiers.
- Record push status.
- Record remaining risk.
- Record next bounded task.

## Expected Next Turn

After this movement vertical is complete, the expected next high-leverage task is an evidence-based reassessment of retained representation and crate boundaries.
That later task should distinguish compile cost, test execution cost, runtime cost, model navigation and context cost, ownership boundaries, unsafe boundaries, and API cohesion.
It must accept `merge nothing` as a valid measured result.
Do not begin that audit in this turn unless a current crate boundary directly prevents the movement vertical and the smallest correct fix necessarily changes it.

## Final Directive

Implement the complete narrow vertical.
Be decisive about breaking provisional APIs.
Be conservative about adding machinery.
Preserve one semantic authority and one production route.
Use stable identities and deterministic validation to make the operation genuinely useful to agents.
Measure only what can inform a current decision.
Delete displaced paths.
Verify the final state.
Commit coherently when permitted.
Stop before broadening the roadmap.
