# Coding Agent Prompt — Evidence-Gated Semantic Ambiguity and Faster Verification for lkjscript

**Repository:** `lkjsxc/lkjscript`
**Orientation commit inspected while preparing this task:** `755f6a183ceb8aa0cf70dce4a75662ee826360cf`
**Task mode:** autonomous, breaking changes allowed, multi-turn, evidence-first
**Companion repository policy:** replace the root `AGENTS.md` with the supplied companion file
**Publication:** create cohesive local commits when appropriate; do not push unless the active invocation explicitly requests it

## 1. Role

- Act as the lead engineer for this turn.
- Exercise autonomous technical judgment.
- Use the actual checkout, executable evidence, and accepted specifications as the basis for decisions.
- Do not ask the user to choose among internal designs that the repository can decide.
- Do not optimize for appearing ambitious.
- Optimize for one coherent, dependency-closed result.
- Treat this prompt as transport rather than durable repository authority.
- Do not commit this prompt.
- Treat the companion `AGENTS.md` as the intended repository-wide operating contract.
- Install the companion `AGENTS.md` directly at the repository root.
- Do not archive the displaced `AGENTS.md`.
- If the companion policy contradicts executable reality, identify the exact contradiction and correct the policy or implementation in the same coherent change.

## 2. Authorization

- The user authorizes incompatible language changes.
- The user authorizes incompatible source-format changes.
- The user authorizes incompatible Rust API changes.
- The user authorizes incompatible command changes.
- The user authorizes representation replacement.
- The user authorizes crate and module reorganization.
- The user authorizes deletion of obsolete features.
- The user authorizes deletion of obsolete tests.
- The user authorizes deletion of obsolete fixtures.
- The user authorizes deletion of obsolete documentation.
- Backward compatibility is not a project objective for this task.
- Do not preserve a weak design merely because it is already implemented.
- Do not create compatibility adapters, editions, `v2` modules, or permanent parallel routes.
- Broad authorization is permission to make the right change, not an instruction to maximize the diff.
- Preserve unrelated work, credentials, host state, external data, and remote history.

## 3. Long-term mission

- Build `lkjscript` into an AI-primary programming substrate rather than a text-first language with an AI wrapper.
- Make semantic meaning directly inspectable by agents.
- Make complete and incomplete programs directly constructible without source text.
- Make semantic edits typed, deterministic, revision-checked, and failure-atomic.
- Make exact types, effects, ownership facts, references, calls, blockers, and legal next actions queryable.
- Keep local deterministic machinery in every correctness boundary.
- Keep model inference outside parsing, validation, compilation correctness, runtime correctness, and persistence correctness.
- Keep checking effect-free.
- Keep execution intentional.
- Keep incomplete state non-executable.
- Keep one syntax-independent mutable semantic authority.
- Keep one complete generic production execution route.
- Make review output compact, structured, stable, and useful to weak models.
- Reduce agent context and API spend through better local interfaces, not through hidden correctness shortcuts.
- Maintain a path to high final performance without building speculative performance machinery.

## 4. This turn's intended result

- Complete one evidence-gated semantic authoring vertical around a real ambiguous value-reference choice, only if a distinct semantic state is justified.
- Independently characterize the verification critical path and test whether bounded parallelism or deduplication materially improves it.
- Keep the semantic vertical and the verification-throughput experiment architecturally independent.
- Do not make semantic correctness depend on a test runner.
- Do not make verification topology depend on a new semantic state.
- Do not build a general ambiguity, recovery, orchestration, cache, or scheduler framework.
- If evidence disproves one objective, do not manufacture implementation work for it.
- A valid outcome may implement the semantic vertical, the verification improvement, both as separate cohesive changes, or neither mechanism when evidence rejects them.
- Even when a mechanism is rejected, leave durable tests or documentation only when they prevent rediscovery and remain smaller than the rejected mechanism.
- Stop after the selected result is complete, documented, and verified.

## 5. Explicit non-goals

- Do not restore the deleted daemon.
- Do not restore the deleted service database.
- Do not restore process cells, session brokers, schedulers, or topology observation.
- Do not add persistence.
- Do not add a journal.
- Do not add a wire service.
- Do not add collaboration.
- Do not add a CRDT.
- Do not add a general incremental compiler.
- Do not retain HIR in snapshots.
- Do not add a compilation cache without a present measured consumer.
- Do not add a general candidate-set abstraction merely for symmetry.
- Do not add parser recovery.
- Do not make text import tolerate unresolved names as part of this task.
- Do not add unresolved calls, moves, borrows, type names, nominal members, patterns, or imports as collateral scope.
- Do not add public semantic movement without a present owner/order use case.
- Do not redesign the entire language.
- Do not add self-hosting scaffolding.
- Do not add PGO, deoptimization, or another native tier.
- Do not change ownership or memory semantics unless a demonstrated prerequisite requires it.
- Do not optimize one benchmark at the expense of the generic route.

## 6. Repository orientation

- The preparation snapshot used branch `main` and orientation commit `755f6a183ceb8aa0cf70dce4a75662ee826360cf`.
- That commit added first-class unresolved value references.
- Treat this orientation as a starting point, not as a substitute for inspecting the actual checkout.
- The actual checkout may have advanced.
- Read recent history before editing.
- Reconcile this prompt with newer accepted work rather than reverting it.
- The current workspace was observed with eleven Cargo members and one application binary.
- The current product boundary was observed as local package check/run plus an in-process semantic workspace API.
- Text and paths were observed as importer conveniences rather than semantic authority.
- The compiler was observed consuming complete semantic snapshots directly.
- The generic VM route was observed remaining complete.
- One baseline-native group attempt was observed occurring only before program effects.
- The workspace was observed without persistence, a daemon, a semantic wire protocol, or a general compilation cache.
- Revalidate every fact that affects the design.

## 7. Current semantic-workspace orientation

- A `Workspace` owns an immutable current `Arc<WorkspaceSnapshot>`.
- A snapshot owns one partial-capable `SemanticProgram`.
- Snapshots carry opaque namespace, revision, entity, node, hole, and unresolved-reference identities.
- Allocator tombstones preserve stale-generation defense.
- Old immutable snapshots remain queryable.
- Source-free and imported programs converge on the same semantic authority.
- Compilation derives complete HIR directly from one complete snapshot.
- Compilation does not render and reparse source.
- Incomplete compilation rejects before memory planning, SSA, bytecode, native lowering, or execution.
- Transactions publish one revision or nothing.
- Failed transactions preserve the published snapshot and future identity allocation.
- Private dense compaction preserves surviving public identities.
- Current source-free authoring covers a substantial scalar, control, nominal, match, ownership, and generic subset.
- Current source-free authoring does not yet cover every language form.
- Do not widen unrelated authoring surfaces in this turn.

## 8. Current unresolved value-reference orientation

- The observed unresolved value reference is a distinct childless expression node.
- It carries a validated requested-name hint.
- It carries one expected semantic type.
- Its observed intent is a future copy-safe binding load.
- Its effects are unknown.
- It has no selected target.
- It has no executable load.
- It has no hidden fallback.
- It creates no reference edge merely because a candidate exists.
- It creates no candidate dependency merely because a candidate exists.
- It has a revision-labelled public state.
- Its state includes owner, context, expected type, requested name, and visible entities.
- A deterministic paginated candidate query filters visible bindings by current copy-load requirements.
- Candidate results expose stable entity identity, current name, kind, declared type, exact-name-match status, and `RequiresCanonicalValidation`.
- Exact-name matches sort before non-exact candidates.
- Resolution chooses a stable target explicitly.
- Resolution reuses the ordinary canonical load-lowering and validation path.
- Resolution preserves the unresolved root node identity.
- Replacement or owner deletion prunes unresolved state through ordinary containment.
- Text import remains fail-fast on unresolved source names.

## 9. Current roadmap orientation

- The observed `Now` item asks for characterization of one ambiguous value-reference choice from real unresolved-candidate use.
- It asks to preserve a finite explicit choice only when deterministic scope and type filtering leave a concrete authoring ambiguity.
- It explicitly warns against prebuilding a generic candidate-set or recovery framework.
- Treat that roadmap item as a hypothesis to test.
- Do not treat the wording as proof that a new stored state is required.
- Update the roadmap after the evidence resolves the hypothesis.
- Do not leave a completed or disproven item in `Now`.

## 10. Companion AGENTS.md installation

1. Inspect the current root `AGENTS.md` and the supplied companion file.
2. Preserve any newer repository fact that is not represented in the companion file.
3. Replace the root policy with one coherent merged result rather than stacking documents.
4. Do not create `AGENTS.old.md`, `AGENTS.v2.md`, or a policy archive.
5. Keep repository-wide procedure general; do not encode this task's exact semantic design as permanent policy.
6. Ensure the final policy includes evidence-first work selection, anti-overengineering, subagent discipline, test-parallelism discipline, API-cost discipline, and verification tiers.
7. Include the final root policy in a cohesive local commit.

## 11. Initial inspection

1. Run `git status --short --branch` or the equivalent before editing.
2. Identify the current branch and upstream.
3. Inspect recent commits that affect the workspace, tests, verification, and policy.
4. Inspect the actual root `AGENTS.md` after installing the companion policy.
5. Read `README.md` for the active product route.
6. Read `docs/authority.md` if present.
7. Read `docs/spec/workspace.md` around incomplete states, transactions, queries, and compilation.
8. Read `docs/status.md` around the semantic workspace and known gaps.
9. Read `docs/architecture.md` around editing, compilation, and verification flow.
10. Read `docs/performance.md` around recomputation and local agent-loop evidence.
11. Read `docs/roadmap.md`.
12. Inspect the unresolved-reference model, transaction, query, projection, validation, and tests.
13. Inspect CI and retained container verification.
14. Inspect smoke scripts for shared files, ports, databases, and process cleanup.
15. Inspect Cargo profiles and workspace features before proposing concurrent builds.
16. Search before opening large files in full.
17. Record a concise producer-consumer map in temporary working state.

## 12. Worktree safety

- Preserve unrelated tracked changes.
- Preserve unrelated untracked files.
- Do not reset, clean, checkout, or rewrite work you did not create.
- Do not assume a clean worktree.
- Do not overwrite a file changed by the user without first understanding the diff.
- Do not commit prompts, raw logs, raw samples, credentials, or scratch plans.
- Keep measurement outputs under ignored `target/` paths or external temporary directories.
- Use cohesive commits.
- Do not push unless the active invocation explicitly requests publication.

## 13. Multi-turn scope

- One turn does not need to finish all incomplete-state families.
- One turn does not need to finish public movement.
- One turn does not need to finish persistence.
- One turn does not need to finish broader platform products.
- One turn should leave a complete vertical or a clearly justified no-mechanism decision.
- Do not start a second semantic family after completing the selected vertical.
- Do not use verification measurements as permission for an unrelated repository-wide build rewrite.
- Do not use an ambiguity fixture as permission for a generic error-recovery redesign.
- Name the next highest-leverage problem in the final report.
- Stop before beginning it.

## 14. Temporary decision record

- Before implementation, write a concise temporary hypothesis.
- State what current behavior fails to preserve.
- State the concrete authoring scenario.
- State why the existing unresolved state may or may not be sufficient.
- State the smallest candidate mechanism.
- State completion criteria.
- State semantic reversal conditions.
- State performance selection criteria for verification changes.
- State the stop condition.
- Do not commit the temporary record.

## 15. Anti-overengineering test

- For every proposed new public type, name its current producer and current consumer.
- For every proposed stored field, name its owner and invalidation rule.
- For every proposed index, demonstrate repeated work that matters.
- For every proposed cache, demonstrate repeated recomputation that matters.
- For every proposed parallel task, demonstrate separability and resource headroom.
- For every proposed dependency, quantify the machinery it removes.
- For every proposed framework, identify at least two current consumers with genuinely shared semantics.
- For every proposed new incomplete state, prove that an existing state plus query cannot represent the intent.
- For every proposed verification wrapper, prove that native commands cannot provide the same result more simply.
- Delete or reject a proposal that fails this test.

## 16. Lead-agent operating model

- One lead agent owns the architecture.
- One lead agent owns the active worktree.
- One lead agent owns the final integration.
- One lead agent schedules heavyweight verification.
- One lead agent reviews every subagent result.
- Subagents provide evidence or isolated leaf changes.
- Subagents do not independently choose the repository architecture.
- Subagent agreement is not proof.
- Subagent disagreement is a signal to inspect the owning evidence.
- The lead agent must understand every integrated change.

## 17. Suggested subagent decomposition

- Use no subagents when the environment does not support them or when the task is already serial.
- Use the smallest useful number of subagents.
- A read-only semantic investigator may map unresolved-reference producers, consumers, invariants, and gaps.
- A read-only test investigator may identify existing coverage and the smallest real ambiguity fixture.
- A read-only verification investigator may measure command and smoke-test critical paths.
- An optional independent reviewer may inspect the final semantic diff for authority duplication and lifecycle gaps.
- Do not assign two agents to read the same broad files.
- Do not assign two agents to design the same public type.
- Do not assign overlapping write ownership.
- Do not have subagents run full workspace verification concurrently.
- Cancel or stop duplicate investigations once sufficient evidence exists.

## 18. Subagent evidence packet

- Require each read-only subagent to report the assigned question.
- Require the conclusion first.
- Require exact files and symbols inspected.
- Require exact tests or commands that support the conclusion.
- Require unresolved uncertainty.
- Require one recommendation.
- Require one reversal condition when relevant.
- Forbid unrelated redesign proposals.
- Forbid complete file dumps.
- Forbid complete successful logs.
- Forbid generic repository summaries.
- Reuse the packet instead of repeating the same search.

## 19. Parallel writing policy

- Prefer parallel read-only investigation.
- Use parallel writers only for genuinely independent leaf tasks.
- Use separate worktrees or equivalent isolated checkouts for parallel writers.
- Assign explicit non-overlapping file or component ownership.
- Do not allow two writers to edit the same file.
- Do not allow two writers to evolve the same semantic model independently.
- Require each writer to produce a cohesive commit.
- Integrate one commit at a time.
- Review the diff before integration.
- Rerun focused tests after integration.
- Resolve architectural conflicts centrally.
- Do not build an orchestration service or task registry.

## 20. Heavy-command scheduling

- Treat CPU, memory, disk bandwidth, linker resources, Docker resources, and caches as shared state.
- Do not run full `cargo clippy`, full `cargo test`, and release LTO builds concurrently in one target directory.
- Do not have multiple subagents launch Docker builds concurrently.
- Do not run large stress tests while timing ordinary tests.
- Do not run benchmarks while competing compilation distorts the result.
- Separate target directories only for a measured experiment.
- Account for duplicated compilation when comparing separate target directories.
- Schedule the final heavyweight boundary through the lead agent.
- Keep focused read-only tasks parallel while heavyweight commands are serialized.

## 21. API-cost and context discipline

- Search for symbols before reading whole modules.
- Read diffs and focused ranges before complete files.
- Prefer structured test output to prose summaries.
- Prefer stable identities and compact headers to rendered trees.
- Do not ask subagents to repeat repository orientation.
- Do not paste unchanged code into reports.
- Do not emit full successful Cargo logs.
- Capture failure logs outside Git and surface only relevant excerpts.
- Do not infer provider token savings from byte counts.
- Measure command count, output bytes, duplicate work, and round trips when claiming workflow savings.
- Keep the final report concise enough to guide the next turn.

## 22. Primary semantic question

Determine whether an agent needs a distinct semantic state that preserves a finite explicit choice among multiple copy-load targets after deterministic scope, type, and copy-safety filtering.
Do not assume the answer is yes.

- The existing unresolved state preserves a requested spelling and expected type.
- The existing candidate query can already return zero, one, or many candidates.
- An agent can already choose one stable candidate explicitly.
- A distinct state is justified only if it preserves additional author intent.
- The strongest plausible missing intent is: `the eventual target is one of these specific stable entities, and no other current or future candidate`.
- A mere count of two or more query results is derived information, not automatically semantic state.
- A cached query result is not semantic authority.
- A candidate list stored only for convenience is not enough justification.
- A finite explicit disjunction may be semantic intent if later rename, declaration creation, or scope changes must not silently alter the set.
- Establish whether that use case is real and useful to agent authoring.

## 23. Evidence gate for a distinct ambiguity state

1. Construct a source-free fixture that produces a real unresolved copy-load site.
2. Ensure canonical visibility, exact type, and copy-safety filters leave at least two plausible stable candidates.
3. Use a scenario where deterministic language semantics cannot choose one candidate.
4. Use current public authoring and query APIs where possible.
5. Record what the unresolved state preserves.
6. Record what the candidate query derives.
7. Demonstrate the exact intent that is lost when the workspace later changes.
8. Test whether explicit immediate resolution already solves the agent workflow.
9. Test whether a derived `multiple candidates` classification is sufficient.
10. Test whether a stored finite stable set changes observable semantics in a useful way.
11. Reject the new state if no additional durable intent is demonstrated.
12. Do not lower the evidence standard merely because the roadmap mentions ambiguity.

## 24. Required real ambiguity fixture properties

- Use two or more visible copy-safe bindings with the same expected semantic type.
- Include at least one exact requested-name match only when that does not deterministically resolve semantics.
- Alternatively use no exact requested-name match and multiple equally plausible candidates.
- Keep lexical scope honest.
- Do not fabricate candidates outside scope.
- Do not use hidden compiler locals.
- Do not use unsupported affine loads.
- Do not use type mismatches to manufacture candidate count.
- Do not make source text the authority.
- Make the fixture small enough to understand.
- Make the fixture strong enough to exercise stable identity.
- Use stable entity IDs in observations.
- Demonstrate candidate ordering.
- Demonstrate unresolved compilation rejection.

## 25. Compare three possible outcomes

### 25.1 Outcome A — existing unresolved state is sufficient

- Choose this outcome if the real workflow only needs a requested hint, candidate query, and explicit resolution.
- Do not add a new node kind.
- Do not add a new blocker.
- Do not add a stored candidate set.
- Add or improve focused tests only when they protect a previously unproven invariant.
- Clarify query semantics only when current output is misleading.
- Update the roadmap to record that a distinct ambiguity state was not justified.
- Document the reversal condition that would reopen the question.
- Proceed to the independent verification-throughput objective.

### 25.2 Outcome B — derived ambiguity classification is sufficient

- Choose this outcome when agents benefit from a structured classification but no durable candidate set is semantic authority.
- Keep the unresolved node as the only stored semantic state.
- Derive classification from the current revision and query filters.
- Bind the result to the revision and query identity.
- Do not store a duplicate candidate list in the snapshot.
- Do not create dependency edges from derived candidates.
- Use deterministic pagination.
- Do not claim a candidate is canonically legal before resolution validation.
- Keep the classification small and specific to unresolved copy loads.
- Do not generalize it to calls, types, members, patterns, or recovery.
- Demonstrate that the classification materially improves an agent decision.
- Update status, specification, projection, and tests only to the extent the public contract changes.

### 25.3 Outcome C — a finite explicit ambiguity state is justified

- Choose this outcome only when the author deliberately constrains the future target to a finite stable set.
- Treat the set as semantic intent rather than a cached query result.
- Implement one complete lifecycle.
- Keep it specific to the current copy-load value-reference vertical.
- Do not create a generic `Ambiguous<T>` framework.
- Do not create a generic candidate registry.
- Do not create a recovery hierarchy.
- Do not create a protocol or persistence format.
- Complete introduction, inspection, projection, diff, resolution, replacement, deletion, failure atomicity, and compile rejection.
- Define exact behavior under candidate rename, deletion, scope changes, and private compaction.
- Require explicit author action before ambiguity becomes executable.

## 26. Reference hypothesis for a finite explicit state

The following is a reference hypothesis, not a mandatory design.
A strong minimal design may allow an unresolved copy-load site to be constrained to two or more specific stable value-binding identities.

- The operation may preserve the existing root node identity.
- The operation may change the node kind from unresolved to an explicit ambiguous copy-load kind.
- The operation may retain the requested-name hint as non-authoritative intent metadata.
- The operation must store at least two distinct stable candidate identities.
- The operation must validate every candidate against current namespace, generation, kind, visibility, expected type, and copy-safety preconditions.
- The operation must define whether candidates become semantic dependencies.
- If they do not become dependencies, explain how deletion cannot silently rewrite the finite intent.
- If they do become dependencies, final-state deletion validation should require explicit resolution, replacement, or owner deletion in the same transaction.
- Resolution must choose one member of the explicit set.
- Resolution must still run the ordinary canonical load validation.
- No automatic first-candidate selection is allowed.
- No automatic single-survivor selection is allowed.
- The state remains incomplete and non-executable.

## 27. Stored-state justification questions

- What fact cannot be derived from the current snapshot?
- Who creates that fact?
- Who consumes that fact?
- Why does it need to survive later revisions?
- Why is the requested-name hint insufficient?
- Why is a revision-bound candidate page insufficient?
- Does the fact constrain future meaning?
- Does a candidate rename preserve the intended choice?
- Does a new declaration change the intended choice?
- Does candidate deletion invalidate the intended choice?
- Does movement change visibility?
- Does replacement remove the owner?
- Does private compaction preserve every member identity?
- Is the state useful before persistence or collaboration exists?
- Can the same result be obtained by immediately resolving to one stable identity?
- Is deferring the choice useful to weaker models or multi-turn authoring?
- Is the mechanism smaller than the lost intent?

## 28. Semantic identity requirements

- Use one stable node identity for the incomplete expression site.
- Do not expose a dense candidate index.
- Do not use names as candidate identity.
- Do not use vector position as candidate identity.
- Do not use source spans as candidate identity.
- Do not manufacture a separate public ambiguity identity unless it has an independent lifecycle.
- Preserve the root node identity across valid unresolved-to-ambiguous and ambiguous-to-load transitions when meaning permits.
- Tombstone removed descendant identities through the ordinary allocator.
- Preserve unrelated entity and node identities.
- Preserve old immutable snapshots.
- Reject stale, foreign, wrong-kind, or wrong-owner identities before publication.
- Do not consume identities on failure.

## 29. Candidate-set requirements if stored

- Require at least two distinct candidates.
- Reject an empty set.
- Reject a singleton set rather than calling it ambiguous.
- Reject duplicate stable identities.
- Use a deterministic canonical order.
- Do not let caller input order become semantic unless the language explicitly needs preference order.
- Prefer stable-identity order for canonical storage unless another existing semantic order is required.
- Expose current names as revision-derived presentation facts.
- Do not rewrite the requested-name hint on candidate rename.
- Do not add newly visible candidates automatically.
- Do not remove candidates silently because another declaration was added.
- Define final-state validation when a candidate ceases to be visible or type-compatible.
- Do not retain an invalid executable fallback.
- Do not silently truncate large sets.
- Use checked allocation and checked length conversion.

## 30. Candidate dependency requirements if stored

- Decide whether an explicit candidate membership is a semantic dependency.
- A deliberate finite disjunction normally constrains meaning and therefore requires deletion-aware behavior.
- Do not reuse the current unresolved candidate query's non-dependency rule without examining the semantic difference.
- If membership is a dependency, expose it through the existing dependency model only if that model accurately represents the relation.
- Do not invent a second dependency graph.
- Ensure final-state deletion checks are order-independent.
- Allow a same-batch resolution or structural replacement to remove the dependency before deleting a candidate.
- Allow owner deletion to cascade through the incomplete site.
- Reject candidate deletion when a surviving ambiguous site still names it and no explicit edit resolves the conflict.
- Report a deterministic surviving dependent.
- Preserve failure atomicity.
- Document the relation in the owning specification.

## 31. Known and unknown facts

- Expected type remains known.
- Owner remains known.
- Context remains known.
- Candidate identities may be known.
- Current candidate names are revision-derived.
- The final selected target remains unknown.
- Effects remain unknown until resolution.
- Reference edges to one selected target remain absent until resolution.
- Call edges remain irrelevant for a copy load.
- Ownership legality beyond the validated copy-safe intent remains subject to canonical validation.
- Do not claim a complete expression type/effect witness if any fact remains unknown.

## 32. Non-executability

- The state must remain a completeness blocker.
- The state must produce a deterministic diagnostic.
- The state must be queryable and projectable.
- The state must never lower to `unit`.
- The state must never lower to zero.
- The state must never lower to a trap.
- The state must never lower to the requested-name match.
- The state must never lower to the first sorted candidate.
- The state must never retain the displaced executable subtree.
- The state must reject compilation before complete HIR derivation.
- Independent complete-program validation must reject a surviving ambiguous node.
- Downstream HIR, memory, SSA, bytecode, and runtime layers should reject the impossible state where appropriate.

## 33. Introduction semantics

- Define the exact source state from which ambiguity may be introduced.
- Prefer transition from an existing unresolved copy-load node if that preserves a clear lifecycle.
- Do not allow arbitrary expression replacement to smuggle a stored candidate set around canonical preflight.
- Validate the base revision.
- Validate the unresolved node identity.
- Validate expected type equality.
- Validate requested intent kind.
- Validate every candidate in the final staged lexical context.
- Validate copy safety through the same authority used by direct loads.
- Preserve the targeted root identity when the semantic site remains continuous.
- Remove no unrelated subtree.
- Publish one revision or nothing.
- Emit an explicit semantic diff.

## 34. Resolution semantics

- Resolution must name one stable candidate explicitly.
- If a finite set is stored, reject a target outside the set.
- Reuse the ordinary one-node load proposal where practical.
- Reuse canonical namespace validation.
- Reuse canonical generation validation.
- Reuse canonical binding-kind validation.
- Reuse canonical lexical visibility validation.
- Reuse canonical exact-type validation.
- Reuse canonical copy-safety validation.
- Reuse canonical local-storage lookup.
- Preserve the root node identity.
- Remove the incomplete blocker and diagnostic.
- Create the ordinary reference and dependency facts.
- Run whole-program ownership validation when the edit completes the snapshot.
- Publish nothing on failure.

## 35. Rename behavior

- Candidate identity survives rename.
- Current candidate names in queries refresh after rename.
- Requested spelling remains intent metadata and does not silently change.
- Exact-name-match facts refresh after rename.
- A rename must not reorder semantic membership if stored order is canonical by stable identity.
- A rename must not select a target.
- A rename must not remove the blocker.
- Old snapshots retain old names and old exact-match facts.
- Projection must clearly distinguish requested spelling from current candidate names.

## 36. Declaration creation behavior

- Creating a new visible binding must refresh derived unresolved candidate queries.
- Creating a new visible binding must not silently enter a stored finite candidate set.
- Creating a new exact-name match must not select it automatically.
- Creating a new candidate must not rewrite requested intent.
- Hole and unresolved visibility must remain revision-derived.
- A stored finite state must preserve its explicit set.
- Old snapshots remain unchanged.

## 37. Candidate deletion behavior

- Define candidate deletion before implementation.
- Do not silently choose a remaining member.
- Do not silently shrink semantic intent unless the operation explicitly means narrowing and that behavior is accepted.
- Prefer final-state validation over edit-order behavior.
- Allow deletion when the ambiguous site is resolved, replaced, or deleted in the same transaction.
- Allow callable owner deletion to cascade through the incomplete site.
- Reject a surviving invalid finite set deterministically.
- Preserve the published snapshot and allocator after rejection.
- Test deletion of the first, middle, and last canonical candidate.
- Test deletion that would leave one candidate.
- Test deletion of all candidates.

## 38. Replacement and owner deletion

- Replacing the ambiguous node must remove its incomplete record.
- Replacing an ancestor must remove contained incomplete records.
- Deleting the callable owner must cascade through the site.
- Removed identities must tombstone.
- Unrelated replacements must preserve the site.
- Unrelated private compaction must preserve the site and every surviving candidate identity.
- Structural reconciliation must not use content fingerprints as identity authority.
- Old snapshots must remain queryable.

## 39. Scope and movement

- Current public movement is not implemented.
- Do not implement public movement for this task.
- Still define behavior if a candidate becomes out of scope through an already-supported structural change.
- Use final staged scope rather than stale base indexes.
- Reject a surviving explicit ambiguity whose member is invalid in final state unless the same transaction resolves or removes it.
- Do not create generic coordinates or tree-editing machinery.
- Record movement as a later concern only when a real use case exists.

## 40. Private compaction

- Private dense compaction is not public movement.
- Surviving stable entity identities must not change.
- Surviving node identity must not change.
- Stored candidate membership must be rewritten only through existing stable-to-private reconciliation.
- Do not expose private addresses.
- Do not emit semantic diff entries for private relocation alone.
- Test candidate membership across deletion and compaction of unrelated earlier declarations.
- Test old snapshot behavior after compaction.

## 41. Transaction behavior

- Use the existing revision-checked atomic transaction path.
- Do not add a second mutation API.
- Collect relevant structural and deletion intent before order-sensitive application.
- Validate the final staged graph.
- Reject overlapping edits.
- Reject contradictory introduction and resolution.
- Reject duplicate candidate identities.
- Reject foreign candidates.
- Reject stale candidates.
- Reject wrong-kind candidates.
- Reject out-of-scope candidates.
- Reject type-incompatible candidates.
- Reject non-copy-safe candidates.
- Preserve allocator state after every failure.
- Preserve diagnostics and blockers after every failure.
- Preserve deterministic future identities after every failure.

## 42. Same-batch behavior

- Test unresolved-to-ambiguous plus unrelated creation in one batch.
- Test ambiguity introduction plus candidate rename in one batch if operation ordering claims final-state semantics.
- Test ambiguity resolution plus candidate deletion in one batch.
- Test ambiguity replacement plus candidate deletion in one batch.
- Test owner deletion plus candidate deletion in one batch.
- Test contradictory introduction and node replacement.
- Test duplicate introduction edits.
- Test stale revision after a successful prior edit.
- Make success or failure independent of arbitrary edit list order where the transaction contract promises it.

## 43. Semantic diff

- Expose introduction as an explicit semantic change.
- Expose resolution as an explicit semantic change.
- Expose candidate membership if it is semantic authority.
- Use stable identities.
- Do not expose dense indexes.
- Do not expose private addresses.
- Do not emit a massive copied snapshot.
- Report base-to-final meaning for repeated compatible edits where the existing diff model does so.
- Omit net no-ops.
- Include ordinary expression-kind and reference rewiring changes where applicable.
- Keep diff ordering deterministic.
- Test diff equality across equivalent edit orderings.

## 44. Query requirements

- Return the selected snapshot revision.
- Return the incomplete node identity.
- Return requested-name intent.
- Return expected semantic type.
- Return owner and context.
- Return finite candidate identities when stored.
- Return current candidate names as revision-derived facts.
- Return candidate kinds.
- Return declared semantic types.
- Return exact-name-match facts.
- Return honest validation status.
- Do not return a candidate as established legal when canonical resolution validation has not run.
- Do not silently truncate.
- Use deterministic pagination where a list may be large.
- Bind continuations to namespace, revision, and query identity.

## 45. Query ordering

- Keep exact-name matches first only when this remains a presentation ordering rather than semantic preference.
- Use current name and stable identity as deterministic tie breakers for derived candidate pages.
- Use canonical stable membership order for a stored finite set unless accepted semantics need another order.
- Do not let hash-map iteration order escape.
- Do not let private dense placement escape.
- Do not let transaction input order accidentally define meaning.
- Test deterministic ordering across unrelated declaration insertion and private compaction.

## 46. Projection requirements

- Projection must show that the snapshot is incomplete.
- Projection must show the blocker kind.
- Projection must show the selected node.
- Projection must show requested-name intent.
- Projection must show expected type.
- Projection must show owner and context.
- Projection may show a bounded summary of candidate count.
- Projection should not dump every candidate by default when a query already provides pagination.
- Projection must use review-local labels only for local links.
- Projection must not construct identity.
- Projection must remain source-independent.
- Projection ordering must be deterministic.
- Projection must be stack-safe.

## 47. Diagnostic and blocker requirements

- Use one stable diagnostic code for a new state.
- Do not reuse the unresolved code if it would make the public distinction dishonest.
- Attach the diagnostic to the semantic node.
- Include only facts the producer owns.
- Do not fabricate source path or range for source-free state.
- Keep the message compact and actionable.
- Expose the legal next action: choose one stable candidate, replace the node, or remove the owner as appropriate.
- Keep one blocker per surviving incomplete site.
- Keep blocker ordering deterministic by semantic node identity or the established rule.
- Keep diagnostics and blockers consistent.
- Test consistency validation against missing or duplicated records.

## 48. Compilation gate

- Reject the incomplete snapshot before complete-HIR derivation.
- Record a deterministic test counter proving memory planning was not entered.
- Record a deterministic test counter proving SSA was not entered.
- Record a deterministic test counter proving bytecode lowering was not entered.
- Do not construct an executable program.
- Do not install native code.
- Do not construct a VM.
- Do not enter host effects.
- Return revision-labelled blockers.
- Preserve the generic complete route after successful resolution.

## 49. Independent downstream defense

- The partial semantic program must reject a surviving ambiguous node when asked to become complete.
- Complete-HIR validation must reject an impossible leaked incomplete node.
- Memory planning should not accept a new incomplete expression kind.
- SSA lowering should not accept it.
- Bytecode lowering should not accept it.
- Native lowering should never observe it.
- Use exhaustive matching or explicit impossible-state errors rather than a hidden fallback.
- Do not duplicate the full semantic validation in every layer.
- Keep defense proportional to each trust boundary.

## 50. Direct-load convergence

- For each candidate, construct an equivalent program using a direct canonical load.
- Resolve the incomplete site to that candidate.
- Compare stable selected semantic facts.
- Compare node kind and type.
- Compare reference edges.
- Compare dependency edges.
- Compare effects.
- Compare selected memory obligations.
- Compare bytecode.
- Compare VM outcome.
- Compare cleanup behavior.
- Confirm source loading and parsing remain zero for source-free construction.
- Do not require identity equality for facts whose continuity intentionally differs.

## 51. Source importer boundary

- Keep text import fail-fast on unresolved source names.
- Do not introduce parser recovery.
- Do not synthesize ambiguity from parser errors.
- Do not make source spans required for the new state.
- Do not add a renderer.
- Do not render and reparse the semantic snapshot.
- If an imported complete program is edited into ambiguity, preserve honest source and semantic provenance according to existing rules.
- Update specification text so current importer behavior remains explicit.

## 52. Generics and ownership boundaries

- Keep the initial vertical restricted to the currently supported copy-load intent unless evidence requires more.
- Do not add unresolved generic forwarding.
- Do not add ownership/reference-bearing generic instantiation.
- Do not add unresolved moves.
- Do not add unresolved borrows.
- Do not add ambiguous calls.
- Do not add ambiguous type names.
- Do not add ambiguous nominal members.
- Do not add generic patterns.
- Do not weaken copy-safety checks.
- Do not create a generic candidate validator that bypasses canonical load validation.

## 53. Stack safety

- Keep public recursive semantic values stack-safe.
- Keep draft traversal iterative.
- Keep query construction iterative.
- Keep projection iterative.
- Keep validation iterative where user depth controls shape.
- Keep destruction stack-safe.
- Do not add recursion on candidate count.
- Candidate lists are wide data, not recursive structure.
- Use checked allocation.
- Add a deep test only when the changed path is depth-sensitive.
- Do not duplicate the full 20,000-level compiler fixture for a shallow list-only change.

## 54. Complexity discipline

- Prefer one pass over visible candidates.
- Do not add an index before candidate query cost is measured as material.
- Do not cache candidate pages in snapshots.
- Do not add general incrementality.
- Do not clone the complete program for a metadata-only edit if a narrow correct path is clearly justified and measured.
- Do not add a narrow fast path merely for symmetry.
- If a new edit changes semantic program meaning, full recomputation may remain correct and acceptable.
- Add deterministic work counters only when they answer a concrete complexity question.
- Remove temporary instrumentation without a continuing consumer.

## 55. Semantic test matrix — baseline state

- Test that empty workspace remains incomplete only because `main` is absent.
- Test that missing body remains distinct from unresolved value reference.
- Test that typed hole remains distinct from unresolved value reference.
- Test that unresolved copy-load with zero candidates remains queryable and non-executable.
- Test that unresolved copy-load with one candidate remains unresolved until explicit resolution.
- Test that unresolved copy-load with two candidates remains unresolved until explicit resolution.
- Test that unresolved copy-load with many candidates paginates deterministically.
- Test that requested spelling survives unrelated edits.
- Test that expected type survives unrelated edits.
- Test that owner and context remain correct.
- Test that unknown effects remain explicit.
- Test that no selected reference edge exists before resolution.

## 56. Semantic test matrix — candidate filtering

- Test that visible value parameters of the exact type are considered.
- Test that visible immutable locals of the exact type are considered.
- Test that visible mutable locals are considered only when canonical copy-load rules allow them.
- Test that out-of-scope bindings are excluded.
- Test that hidden compiler bindings are excluded.
- Test that wrong-kind entities are excluded.
- Test that wrong-type bindings are excluded.
- Test that affine non-copy-safe bindings are excluded.
- Test that stale identities are rejected on explicit introduction.
- Test that foreign identities are rejected on explicit introduction.
- Test that duplicate candidates are rejected.
- Test that candidate order is stable.
- Test that exact-name-match status updates after rename.

## 57. Semantic test matrix — ambiguity introduction if implemented

- Test that a valid unresolved node can be constrained to two valid candidates.
- Test that the root node identity is preserved.
- Test that the requested name is preserved.
- Test that the expected type is preserved.
- Test that the new state has unknown effects.
- Test that the new state emits one blocker and one diagnostic.
- Test that a singleton set rejects atomically.
- Test that an empty set rejects atomically.
- Test that duplicate membership rejects atomically.
- Test that an out-of-scope member rejects atomically.
- Test that a type-mismatched member rejects atomically.
- Test that a non-copy-safe member rejects atomically.
- Test that a stale member rejects atomically.
- Test that a foreign member rejects atomically.
- Test that a wrong-kind member rejects atomically.
- Test that allocation failure rejects atomically.

## 58. Semantic test matrix — resolution if implemented

- Test that each member can be selected explicitly.
- Test that a non-member cannot be selected.
- Test that resolution preserves the root node identity.
- Test that resolution creates the canonical load.
- Test that resolution creates the ordinary reference edge.
- Test that resolution creates the ordinary dependency relation.
- Test that resolution removes the ambiguity blocker.
- Test that resolution removes the ambiguity diagnostic.
- Test that resolution retains unrelated incomplete blockers.
- Test that resolution runs whole-program ownership validation when it completes the program.
- Test that canonical failure publishes nothing.
- Test that old snapshots retain the ambiguity.
- Test that the new snapshot compiles and executes exactly once.

## 59. Semantic test matrix — rename and creation

- Test that renaming one candidate preserves membership.
- Test that renaming all candidates preserves membership.
- Test that requested spelling does not change.
- Test that current names refresh.
- Test that exact-name-match flags refresh.
- Test that adding a new matching binding does not enter a stored finite set.
- Test that adding a new matching binding appears in a derived unresolved query.
- Test that adding an exact-name match does not auto-resolve.
- Test that old snapshots retain old names.
- Test that continuations from an old revision fail against the new revision.

## 60. Semantic test matrix — deletion and replacement

- Test that deleting a referenced candidate while the explicit ambiguity survives follows the chosen dependency rule.
- Test that same-batch resolution then candidate deletion succeeds when final meaning is valid.
- Test that same-batch replacement then candidate deletion succeeds when final meaning is valid.
- Test that ancestor replacement removes the incomplete record.
- Test that callable deletion removes the incomplete record.
- Test that unrelated callable deletion preserves the incomplete record.
- Test that unrelated nominal deletion preserves the incomplete record.
- Test that private compaction preserves all surviving stable identities.
- Test that deletion failure preserves allocator state.
- Test that deletion failure preserves the exact published snapshot.
- Test that later same-name recreation receives a fresh generation.

## 61. Semantic test matrix — diff, query, projection

- Test that introduction diff names stable identities.
- Test that resolution diff names stable identities.
- Test that candidate membership diff is deterministic when membership is semantic.
- Test that private relocation emits no semantic membership change.
- Test that query results carry the current revision.
- Test that pagination has no loss or duplication.
- Test that stale continuation rejects.
- Test that foreign continuation rejects.
- Test that projection works without source attachments.
- Test that projection is deterministic.
- Test that projection does not dump an unbounded candidate list by default.
- Test that machine-facing structured values do not expose private dense IDs.

## 62. Semantic test matrix — compile and execution

- Test that incomplete compilation reports the correct blocker.
- Test that incomplete compilation enters zero memory-plan work.
- Test that incomplete compilation enters zero SSA work.
- Test that incomplete compilation enters zero bytecode work.
- Test that incomplete checking performs no host effects.
- Test that a resolved program compiles through the production route.
- Test that a resolved program executes through the unchanged generic route when native specialization declines.
- Test that a resolved eligible program may use the existing baseline-native route without changing semantics.
- Test that effects execute exactly once.
- Test that cleanup is identical to a direct-load equivalent.
- Test that source loading and parsing remain zero for source-free authoring.

## 63. Semantic test matrix — failure atomicity

- Test that stale base revision preserves the exact snapshot.
- Test that foreign node identity preserves the exact snapshot.
- Test that wrong-kind node identity preserves the exact snapshot.
- Test that stale candidate identity preserves the exact snapshot.
- Test that duplicate candidate input preserves the exact snapshot.
- Test that type mismatch preserves the exact snapshot.
- Test that scope mismatch preserves the exact snapshot.
- Test that copy-safety mismatch preserves the exact snapshot.
- Test that allocation failure preserves the exact snapshot.
- Test that future entity allocation order is unchanged after failure.
- Test that future node allocation order is unchanged after failure.
- Test that diagnostics and blockers are unchanged after failure.

## 64. Semantic test matrix — scale

- Test that a candidate page larger than one page has no loss or duplication.
- Test that at least 257 visible candidates retain deterministic ordering if the existing generated fixture remains appropriate.
- Test that candidate materialization does not rescan the full callable body per result.
- Test that wide candidate storage uses checked allocation.
- Test that large finite membership does not silently truncate.
- Test that deep surrounding expression structure remains stack-safe when the changed path traverses it.
- Test that destruction remains stack-safe.
- Test that a small-stack test covers the deepest changed lifecycle when necessary.

## 65. Semantic acceptance gate

- Do not accept a new state merely because all tests can be made to pass.
- Accept it only when the evidence fixture demonstrates additional durable author intent.
- Accept it only when the lifecycle is complete.
- Accept it only when no second mutable authority is introduced.
- Accept it only when canonical validation remains authoritative.
- Accept it only when incomplete execution remains impossible.
- Accept it only when the public API is smaller than the problem it solves.
- Accept it only when documentation can explain it without a generic framework.
- Reject it if the existing unresolved state plus explicit resolution is sufficient.
- Reject it if the stored set is merely a query cache.
- Reject it if candidate invalidation semantics cannot be made coherent in this vertical.

## 66. Documentation for the semantic result

- Update `docs/spec/workspace.md` when the intended public contract changes.
- Update `docs/status.md` with only implemented facts.
- Update `docs/architecture.md` with only current flow and ownership.
- Update `docs/roadmap.md` to remove or replace the completed/disproven `Now` item.
- Update `README.md` only if first-use or public surface meaningfully changes.
- Update `docs/performance.md` only when retaining reproducible measurement evidence.
- Delete stale statements in the same change.
- Do not add a separate ambiguity design document unless the decision is durable, non-obvious, and expensive to rediscover.
- Do not commit the task prompt or subagent reports.
- Keep examples mechanically checked where practical.

## 67. Secondary objective — verification throughput

The repository's final verification is substantial enough that faster feedback may materially improve agent productivity and API spend.
Treat parallelization as a measured optimization, not as an assumed improvement.

- Measure the actual critical path before changing topology.
- Separate build time from test execution time.
- Separate host verification from container build and smoke time.
- Measure repeated compilation.
- Measure CPU and memory pressure.
- Identify shared files, ports, databases, and output paths.
- Identify which tasks are truly independent after one product binary exists.
- Distinguish local wall latency from total CI compute and CI cost.
- Keep full correctness coverage.
- Keep failure output complete and deterministic.

## 68. Observed verification orientation

- The observed GitHub Actions workflow used one `verify` job.
- The observed job ran format, Clippy, workspace tests, and workspace release build sequentially.
- The observed retained Docker build first produced a release binary.
- The observed verified Docker stage then ran format, Clippy, workspace tests, product checks, example runs, and smoke scripts sequentially.
- The observed release profile used LTO and one codegen unit.
- Concurrent release links may therefore be expensive.
- The observed HTTP smoke used fixed port `8080`.
- The observed benchmark smoke wrote a shared C binary under `target/`.
- Several observed smoke scripts used isolated temporary directories.
- Revalidate these facts in the actual checkout.

## 69. Verification baseline protocol

1. Record commit and dirty-state digest.
2. Record machine, OS, architecture, CPU, memory, Rust, Cargo, Docker, and filesystem context.
3. Record available logical CPUs.
4. Record whether caches are cold, warm, or mixed.
5. Record relevant environment overrides.
6. Use the same semantic target and feature set across compared cells.
7. Capture exact command status.
8. Capture wall time.
9. Capture CPU time when practical.
10. Capture peak RSS when practical.
11. Capture stdout and stderr byte counts.
12. Separate Cargo/process startup from in-process test execution where practical.
13. Use repeated samples for noisy comparisons.
14. Report median and a tail orientation.
15. Keep raw logs and samples outside Git.

## 70. Baseline command cells

```sh
cargo fmt --all -- --check
cargo clippy --quiet --workspace --all-targets --all-features --locked -- -D warnings
cargo test --quiet --workspace --all-targets --all-features --locked
cargo build --quiet --workspace --release --locked
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
```

- Measure each native command separately.
- Measure the four-command native boundary as a sequence.
- Measure the retained container boundary separately.
- Do not run baseline cells concurrently.
- Repeat unchanged warm cells only as required by the protocol.
- Do not infer one command's critical path from aggregate wall time alone.

## 71. Separate compilation from test execution

- Use current Cargo capabilities first.
- Measure a workspace test compile-only path if practical.
- Measure test-binary execution separately if practical without changing semantics.
- Identify crates or integration targets that dominate compilation.
- Identify test binaries that dominate execution.
- Identify whether all-feature test compilation duplicates release build work.
- Identify whether the release build is required as an independent final artifact check.
- Do not add a new runner before this split is understood.
- Do not use unstable Cargo interfaces without an explicit reason and fallback.

## 72. Candidate verification improvements

- Remove truly duplicate compilation when one artifact can serve the same consumer boundary.
- Reuse the already-built release `lkjscript` binary across post-build smoke tests.
- Run independent post-build smoke tests concurrently only after isolation is established.
- Parameterize fixed ports before parallel HTTP execution.
- Parameterize shared benchmark outputs before concurrent execution.
- Keep benchmark timing separate from resource-contended correctness smokes.
- Consider grouping lightweight isolated smokes behind a small bounded shell harness.
- Consider CI job splitting only after accounting for duplicated compilation and cache behavior.
- Consider mature `cargo-nextest` only if test execution, rather than compilation, dominates.
- Consider cache-key or Docker layer corrections only when current invalidation is demonstrated.
- Consider narrowing local iteration commands without weakening final verification.
- Prefer deletion or reuse before adding concurrency.

## 73. Candidate experiment A — bounded smoke parallelism

- Build the release binary once.
- List every post-build smoke command.
- Classify each command as isolated, parameterizable, serial-only, or benchmark-only.
- Give each concurrent command a unique temporary directory.
- Give each concurrent network command a unique port.
- Give each concurrent database command a unique database path.
- Give each concurrent compiler command a unique output path.
- Capture stdout and stderr separately per command.
- Capture every exit status.
- Use bounded concurrency based on measured host resources.
- Reap every child process.
- Kill remaining children on failure or interruption.
- Report all failures deterministically.
- Compare end-to-end verified-stage wall time.
- Reject the harness if complexity exceeds the saved critical-path time.

## 74. Candidate experiment B — CI job topology

- Measure current single-job latency.
- Estimate or measure cache reuse within the current job.
- Model compile duplication for separate lint, test, release, and smoke jobs.
- Distinguish pull-request latency from total CI minutes.
- Do not optimize only the visual parallelism of the Actions graph.
- A faster wall clock with much higher compute may still be a poor project tradeoff.
- A split may be justified when cached compilation is effective and independent failures become available earlier.
- A split may be rejected when each job recompiles most of the workspace.
- Keep one authoritative required status set.
- Do not make release artifacts depend on a less complete feature shape.
- Do not introduce a matrix without current supported targets.

## 75. Candidate experiment C — Cargo test runner

- Measure how much of `cargo test` wall time is compilation.
- Measure how much is test execution.
- Measure current libtest thread utilization.
- Identify serial tests and global-state constraints.
- Evaluate `cargo-nextest` only when it addresses the measured dominant portion.
- Account for installation and version-pinning cost.
- Account for changed output and filtering semantics.
- Account for ignored locked-release stress tests.
- Account for doctests if any.
- Account for CI and Docker availability.
- Do not adopt a runner for a marginal or unrepeatable result.
- Do not create a custom runner when a mature tool suffices.

## 76. Candidate experiment D — duplicated host and container work

- Map what the native boundary proves.
- Map what the container boundary proves.
- Identify genuine overlap.
- Identify container-only system dependency and packaging evidence.
- Do not remove container verification merely because native checks already passed.
- Do not run both boundaries repeatedly during iteration.
- Consider whether a final container stage can consume a prebuilt artifact without weakening the clean-build guarantee.
- Consider whether Docker cache mounts already provide the intended reuse.
- Reject cross-boundary artifact reuse if it hides missing container dependencies or build inputs.
- Document any retained distinction between host and container evidence.

## 77. Smoke isolation audit

- Audit editor smoke temporary files.
- Audit HTTP fixed port and child cleanup.
- Audit bulk-byte temporary path.
- Audit durable-file temporary path.
- Audit SHA-256 global state.
- Audit SQLite database path.
- Audit benchmark compiler output path.
- Audit Mandelbrot output path and volume.
- Audit hello output.
- Audit environment variables.
- Audit current working directory assumptions.
- Audit signal handling.
- Audit timeout behavior.
- Audit whether any smoke mutates the source tree.

## 78. Parallelism correctness requirements

- Parallel schedule must not change semantic outcomes.
- Parallel schedule must not change test selection.
- Parallel schedule must not change feature selection.
- Parallel schedule must not change target selection.
- Parallel schedule must not hide one failure behind another.
- Parallel schedule must not leave child processes running.
- Parallel schedule must not collide on ports.
- Parallel schedule must not collide on output files.
- Parallel schedule must not corrupt Cargo artifacts.
- Parallel schedule must not make benchmark results meaningless.
- Parallel schedule must not make logs nondeterministically unreadable.
- Parallel schedule must have a serial fallback when the environment cannot support it.

## 79. Resource-aware concurrency

- Do not hard-code concurrency from developer enthusiasm.
- Choose concurrency from the number and resource shape of independent tasks.
- Bound concurrency.
- Reserve memory for the release binary and host processes.
- Avoid simultaneous LTO links.
- Avoid simultaneous full workspace compiles.
- Avoid saturating disk with multiple cold targets.
- Avoid timing cells while unrelated load is high.
- Record starting load when reporting developer-host timings.
- Use deterministic task ordering for launch and reporting.

## 80. Verification selection criterion

- Predeclare a material wall-time threshold before inspecting final results.
- A reasonable default is at least a 15% median reduction in the selected end-to-end boundary or another clearly material absolute critical-path reduction.
- Adjust the threshold only for measured noise and explain the adjustment before selecting the result.
- Require identical semantic coverage.
- Require identical failure visibility.
- Require no new flakiness.
- Require bounded resource use.
- Require understandable maintenance.
- Require the mechanism to be smaller than the saved work.
- Do not keep a change solely because one sample was faster.
- Do not make noisy timing a correctness gate.

## 81. Verification reversal conditions

- Revert or reject a change that hides failures.
- Revert or reject a change that changes selected tests.
- Revert or reject a change that weakens all-feature coverage.
- Revert or reject a change that skips release compilation without an equivalent boundary.
- Revert or reject a change that makes Docker packaging unverified.
- Revert or reject a change that adds intermittent port collisions.
- Revert or reject a change that leaves child processes.
- Revert or reject a change that materially increases total CI cost without a justified latency benefit.
- Revert or reject a change whose maintenance cost exceeds its repeatable benefit.
- Reopen the decision when repository size, CI hardware, or test distribution changes materially.

## 82. Verification changes to avoid

- Do not run `cargo clippy`, `cargo test`, and release build simultaneously in one target directory.
- Do not launch multiple Docker builds against the same mutable context for a local speed claim.
- Do not add a general scheduler.
- Do not add a task database.
- Do not add a logging framework.
- Do not add a custom wire protocol.
- Do not add a persistent daemon.
- Do not add `sccache` without measuring remote or local cache value and operational cost.
- Do not adopt `cargo-nextest` without measuring test-execution dominance.
- Do not split CI into many jobs merely to display parallel boxes.
- Do not parallelize the timing benchmark with competing workloads.
- Do not replace exact exit checking with grep-only success.
- Do not suppress stderr.
- Do not reduce final coverage to improve a number.

## 83. Verification implementation guidance if accepted

- Prefer a small script under `meta/scripts/` when a script is genuinely needed.
- Use POSIX shell where existing environment and requirements permit.
- Use Bash only for features actually required.
- Use `set -euo pipefail` or equivalent explicit status handling.
- Use `mktemp -d` for isolated state.
- Use traps for cleanup.
- Keep per-task log paths outside committed source.
- Use a simple explicit task table rather than a framework.
- Bound jobs through one documented parameter or detected safe default.
- Print nothing on complete success unless an existing retained boundary requires a compact result.
- On failure, print task name, command, status, and log paths.
- Test the harness itself with deliberate failure probes, then remove the probes.

## 84. Verification tests

- Test that all original smoke commands still run.
- Test that a deliberate failing smoke returns nonzero.
- Test that a deliberate failing smoke exposes its stderr.
- Test that multiple failures are reported deterministically when aggregation is promised.
- Test that child processes are reaped.
- Test that temporary directories are removed.
- Test that HTTP uses an isolated available port when parallelized.
- Test that benchmark output does not collide.
- Test that serial fallback runs correctly.
- Test that bounded concurrency is honored.
- Test that successful output remains compact.
- Test that the final result file is produced only after every task succeeds.

## 85. Verification measurement documentation

- Retain the harness command.
- Retain workload definitions.
- Retain environment metadata.
- Retain sample count.
- Retain median and tail orientation.
- Retain exact semantic coverage comparison.
- Retain the selection decision.
- Retain reversal conditions.
- Keep raw samples outside Git.
- Do not claim general speedup across hosts.
- Do not claim API billing savings without provider-specific measurement.
- State unmeasured dimensions as `Unknown`.

## 86. Interaction between semantic and verification work

- Do not let verification experiments obscure semantic failures.
- Establish focused semantic correctness before measuring the final boundary.
- Do not measure a dirty tree that changes during the sample set.
- Use separate cohesive commits when semantic behavior and verification topology both change.
- Measure verification topology on a fixed semantic commit.
- After integration, rerun semantic focused tests under the final verification topology.
- Do not attribute compiler code changes to verification topology speedup.
- Record the exact commit or dirty-state digest for each measurement.

## 87. Representation and crate-boundary restraint

- Do not reorganize crates merely because this task touches many workspace files.
- Do not split the workspace module into tiny files for line count.
- Do not merge crates without profile and dependency evidence.
- Do not create a new crate for one incomplete node.
- Do not create a new crate for one smoke scheduler.
- Keep the semantic state in the existing authoritative workspace model unless a real boundary proves otherwise.
- Keep verification scripts under the existing meta boundary unless another current consumer exists.
- Delete any obsolete helper made redundant by the selected change.

## 88. Performance of the language itself

- Do not conflate test-runner wall time with `lkjscript` runtime performance.
- Do not change the VM or native path merely to shorten repository verification.
- Do not weaken release LTO without separately measuring binary and runtime consequences.
- Do not optimize Mandelbrot I/O as collateral scope.
- Do not add JIT tiers.
- Preserve the complete generic VM route.
- Preserve pre-effect native decline.
- Preserve exactly-once effects.
- Record runtime changes only when the semantic vertical necessarily affects them.

## 89. Local agent-loop economics

- Use focused semantic tests during iteration.
- Do not run Docker after every edit.
- Do not run the full workspace after every edit.
- Run the final native boundary once final relevant code inputs are stable.
- Run the final container boundary according to the companion policy and changed risk surface.
- Reuse successful unchanged evidence.
- Keep successful high-frequency commands quiet.
- Surface failures completely.
- Reduce round trips by returning stable structured facts.
- Do not add a service merely to avoid process startup until process startup dominates representative work.

## 90. Commit strategy

- Create a cohesive policy commit when replacing `AGENTS.md`, or include it with the first coherent task commit when that remains reviewable.
- Keep semantic contract, implementation, tests, and owning documentation in the same semantic commit when practical.
- Use a separate commit for a measured verification-topology change.
- Do not split code and tests into separate incomplete commits.
- Do not split documentation from the behavior it describes when that would leave a false intermediate state.
- Use commit messages that name the result.
- Do not commit raw benchmark samples.
- Do not commit the prompt.
- Do not push unless explicitly requested.

## 91. Focused verification during semantic implementation

- Run the smallest workspace tests that exercise unresolved state.
- Run the smallest tests that exercise candidate queries.
- Run the smallest tests that exercise transaction failure atomicity.
- Run the smallest tests that exercise projection and diff.
- Run the smallest compile-rejection test.
- Run the direct-load convergence test.
- Run small-stack or generated scale tests only when the changed path requires them.
- Do not run unrelated host smokes during every semantic edit.

## 92. Focused verification during throughput implementation

- Run each modified smoke independently first.
- Run the serial baseline.
- Run the candidate parallel path.
- Run deliberate failure probes.
- Run cleanup and port-isolation tests.
- Run repeated comparable samples.
- Remove deliberate probes.
- Run the final retained verification command.
- Do not compare cells with different source or feature sets.

## 93. Final native verification

```sh
cargo fmt --all -- --check
cargo clippy --quiet --workspace --all-targets --all-features --locked -- -D warnings
cargo test --quiet --workspace --all-targets --all-features --locked
cargo build --quiet --workspace --release --locked
```

- Run these against the final relevant state.
- Do not claim success from partial completion.
- Capture complete failure logs outside Git.
- Report exact status for each command.
- Do not rerun an identical passing command after no relevant input changed.

## 94. Final container verification

```sh
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
```

- Run it when the change reaches compiler, executable, VM, native, host, example, smoke, packaging, system dependency, or Docker behavior.
- A semantic workspace compiler change normally reaches the product boundary and therefore normally warrants this verification.
- A documentation-only final change does not require rebuilding an already verified unchanged product.
- If the environment blocks Docker, report the exact failure and remaining risk.
- Do not shorten the command by deleting coverage without equivalent evidence.

## 95. Additional final verification

- Run ignored locked-release stress relevant to the changed incomplete-state lifecycle.
- Run small-stack tests relevant to the changed traversal.
- Run malformed identity and allocation-failure tests.
- Run machine-output tests if public JSON or projection changes.
- Run Miri, sanitizer, fuzz, or property checks only when the changed unsafe or validation boundary warrants them.
- Run the accepted verification measurement protocol if selecting a throughput change.
- Do not run unrelated expensive suites for ceremonial completeness.

## 96. Primary semantic acceptance criteria

- Acceptance requires that the actual checkout and recent history were inspected.
- Acceptance requires that the companion `AGENTS.md` is installed coherently.
- Acceptance requires that a real ambiguity fixture was constructed.
- Acceptance requires that the existing unresolved state was compared against the proposed need.
- Acceptance requires that the evidence decision is explicit.
- Acceptance requires that no generic ambiguity framework was added.
- Acceptance requires that no second semantic authority was added.
- Acceptance requires that incomplete state remains non-executable.
- Acceptance requires that canonical load validation remains authoritative.
- Acceptance requires that identity and revision behavior are defined.
- Acceptance requires that rename, creation, deletion, replacement, compaction, and old-snapshot behavior are defined.
- Acceptance requires that failure atomicity is demonstrated.
- Acceptance requires that queries, projection, blockers, diagnostics, and diffs are deterministic.
- Acceptance requires that direct-load convergence is demonstrated.
- Acceptance requires that source-free construction invokes no source loading or parsing.
- Acceptance requires that owning documentation and roadmap are current.

## 97. Verification-throughput acceptance criteria

- Acceptance requires that the current critical path was measured.
- Acceptance requires that compilation and test execution were distinguished where practical.
- Acceptance requires that shared resources were audited.
- Acceptance requires that candidate parallel work is actually independent.
- Acceptance requires that semantic coverage is unchanged.
- Acceptance requires that failure visibility is unchanged or improved.
- Acceptance requires that child cleanup is correct.
- Acceptance requires that concurrency is bounded.
- Acceptance requires that repeatable wall-time benefit meets the predeclared threshold.
- Acceptance requires that total CI cost is considered.
- Acceptance requires that the mechanism remains small.
- Acceptance requires that raw samples remain outside Git.
- Acceptance requires that compact reproducible evidence and reversal conditions are documented.

## 98. Valid no-mechanism outcomes

- It is valid to conclude that the existing unresolved state is sufficient.
- It is valid to conclude that only a derived classification is useful.
- It is valid to conclude that a finite explicit ambiguity state is useful.
- It is valid to conclude that test parallelism does not improve the critical path.
- It is valid to conclude that CI splitting costs more than it saves.
- It is valid to conclude that `cargo-nextest` is not justified.
- It is valid to leave current verification topology unchanged after a sound experiment.
- Do not force a code change to satisfy the appearance of progress.
- When rejecting a mechanism, update the roadmap or performance decision only when the evidence is durable and reproducible.

## 99. Stop conditions

- Stop semantic expansion once the selected ambiguity decision is complete.
- Stop verification experimentation once the selection criterion is met or all small candidates are rejected.
- Stop adding tests once intended invariants and root causes are covered.
- Stop adding abstractions when direct local code is sufficient.
- Stop using subagents when evidence begins to duplicate.
- Stop running heavyweight commands after the final relevant state is verified.
- Stop before public movement.
- Stop before persistence.
- Stop before a daemon.
- Stop before another unresolved-reference family.
- Stop before a general recovery framework.
- Stop before a general task scheduler.

## 100. Final report format

1. State the completed objective.
2. State the orientation commit and actual final commit.
3. State the demonstrated semantic evidence.
4. State whether the ambiguity mechanism was accepted, narrowed, or rejected.
5. State the principal design and why it is smaller than alternatives.
6. State removed or replaced paths.
7. State focused semantic tests and outcomes.
8. State direct convergence evidence.
9. State verification baseline and candidate results.
10. State whether a throughput mechanism was accepted or rejected.
11. State exact final verification commands and outcomes.
12. State environment-limited checks.
13. State remaining risks and unknowns.
14. State documentation updates.
15. State local commit identifiers.
16. State publication status.
17. State how subagents were used and integrated, if applicable.
18. Name the next highest-leverage problem.
19. Explain why work stopped before beginning it.

## 101. Final prohibitions

- Do not claim a target contract is implemented when it is not.
- Do not claim a performance improvement without equivalent measurements.
- Do not claim API cost savings from intuition.
- Do not hide a failed command.
- Do not weaken tests to pass a redesign.
- Do not preserve obsolete compatibility.
- Do not introduce a hidden executable fallback.
- Do not duplicate semantic authority.
- Do not introduce a generic framework for one state.
- Do not parallelize unsafe shared state.
- Do not push without an explicit publication request.
- Do not begin the next roadmap item after completing this turn.

## 102. Closing instruction

Use the repository to decide the design.
Prefer deletion, directness, and canonical reuse.
Make ambiguity explicit only when it preserves real intent.
Make verification parallel only when the work is truly separable and the critical path improves.
Leave `lkjscript` simpler, more truthful, easier for agents to operate, and no less correct.
