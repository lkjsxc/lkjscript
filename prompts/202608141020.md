# Coding Agent Prompt: Complete Source-Free Generic Enum Declaration

Repository: `lkjsxc/lkjscript`

Prompt date: 2026-08-14, Asia/Tokyo

Observed repository head when this prompt was prepared: `efcf802f77a4d69bba03ab466011db50176e12f1`

Observed head subject: `Complete exact generic enum authoring`

Intended prompt filename: `202608141020.md`

Companion policy artifact: the `AGENTS.md` delivered with this prompt

## 0. How to Use This Prompt

This is a multi-turn implementation directive for a coding agent operating in the repository.

Treat the actual checkout as authoritative for current facts.

Treat the companion root `AGENTS.md` as the repository-wide engineering policy for this task.

Install the companion `AGENTS.md` at the repository root before substantive implementation unless doing so would overwrite unrelated user work.

If the existing root policy has unrelated uncommitted edits, preserve them and integrate the companion policy without losing either set of legitimate instructions.

Do not commit this prompt file.

Do not archive this prompt in the repository.

Do not treat prompt wording as product architecture.

Use this prompt to select and complete one evidence-backed vertical.

Do not attempt to settle every future language, workspace, runtime, persistence, service, or platform question in this turn.

Continue across turns through compact evidence-based handoffs when necessary.

Do not ask the user to choose between internal implementation alternatives that repository evidence can decide.

Backward compatibility with provisional APIs, syntax, fixtures, internal forms, tests, or documentation is not a goal.

Use a direct cutover.

Do not add a compatibility mode, versioned twin API, legacy adapter, migration, deprecated alias, or dual representation.

Preserve unrelated work and external state.

Do not push, force-push, open a pull request, deploy, or alter remote state unless a later explicit user instruction requests it.

## 1. Executive Objective

Close the smallest high-leverage source-free authorship gap that prevents an agent from declaring and then using a simple generic enum entirely through the typed semantic workspace.

The default selected vertical is source-free declaration of a non-recursive generic enum with declaration-local type parameters.

The concrete target is a semantic-workspace program equivalent to a conventional `maybe<t>` declaration with `none` and `some(value: t)` variants.

The program must then instantiate that enum at `i64`, construct a value, exhaustively match it, compile directly from the semantic snapshot, and execute through the existing production route.

The complete source-free path must perform zero source loading and zero parsing after `Workspace::empty`.

The imported and source-free forms must converge on the same canonical compiler and runtime semantics.

The change must reuse the existing generic enum instantiation, substitution, matching, ownership, memory, SSA, bytecode, VM, and bounded native machinery.

The change must not introduce a second generic solver.

The change must not introduce a second semantic authority.

The change must not introduce persistence, a daemon, a wire protocol, or an agent service.

The change must not generalize products, recursive nominal declaration, generic forwarding, enum bounds, or movement merely for symmetry.

## 2. Mandatory Decision Gate

The selected vertical is a strong hypothesis, not permission to ignore the checkout.

Begin by proving the current gap against the actual head.

The observed checkout already supports source-imported generic enum declarations.

The observed HIR already stores enum type parameters.

The observed semantic workspace already supports exact generic enum values and exact generic enum patterns for existing declarations.

The observed semantic workspace already supports source-free generic function declaration through declaration-local type-parameter handles.

The observed `Edit::CreateEnum` remains non-generic and its field draft uses published `SemanticType`.

The observed status document lists generic enum declaration creation as unsupported.

Verify each fact before relying on it.

If the repository head has advanced and source-free generic enum declaration is already complete, do not reimplement it.

If it is already complete, run the concrete consumer proof described below.

If that proof passes in full, stop after verification and report that no implementation vertical remains justified by this prompt.

If that proof exposes a narrower missing operation, select only the earliest dependency-closed blocker needed by the same consumer.

Do not jump from an already-complete consumer to persistence, a CLI protocol, collaboration, generic products, recursive enums, or another unrelated roadmap item.

If the current architecture makes the proposed vertical dishonest, document the exact contradiction and select the smallest representation correction inside the same producer-to-consumer path.

A broad architecture rewrite is not a valid substitute for this gate.

## 3. Why This Vertical Is Currently the Best Candidate

- It closes a real asymmetry between imported programs and source-free programs.

- It extends an already selected semantic-authoring model rather than adding a new product boundary.

- It has a concrete consumer that can be tested end to end.

- It reuses declaration-local binder machinery already proven for generic functions.

- It reuses generic enum value and pattern machinery completed immediately before this task.

- It exercises stable nominal, binder, variant, and field identities.

- It exercises transaction publication, queries, projection, deletion, compaction, direct compilation, and execution.

- It does not require a persistent workspace or process boundary.

- It does not require source rendering or parsing.

- It does not require generic inference in semantic edits.

- It does not require generic ownership or reference instantiation.

- It does not require recursive self-reference during declaration creation.

- It can be completed with a direct incompatible API cutover.

- It materially increases the set of ordinary algebraic data types an AI agent can author without text.

- It remains small enough to reject speculative generalization.

## 4. Concrete Consumer

Construct the following semantic program from `Workspace::empty` without importing any source.

Use the exact existing public workspace API after the selected API cutover.

Create one generic enum named `maybe` with one ordered type parameter named `t`.

Create variant `none` with no fields.

Create variant `some` with one field named `value` whose declaration type is the same edit's `t`.

Create one ordinary function or `main` whose concrete types use `maybe<i64>`.

Construct `some<i64>(42)`.

Exhaustively match the value.

Return `42` from the `some` arm.

Return a distinct fallback such as `0` from the `none` arm.

Compile the complete `WorkspaceSnapshot` through `compile_snapshot`.

Execute the compiled program through the ordinary production execution helper used by existing workspace end-to-end tests.

Prove the observable result is `42`.

Prove source loading count is zero.

Prove parser invocation count is zero.

Prove the VM remains the complete generic route.

Allow baseline native execution to decline before entry if this generic shape is unsupported.

Do not add native lowering merely to make this test enter native code.

Do not weaken the generic VM route.

## 5. Success Definition

- A source-free transaction can declare a non-recursive generic enum.

- The declaration owns stable type-parameter entities.

- The declaration's fields can refer to declaration-local type-parameter handles.

- Declaration-local handles never escape into a published snapshot.

- Published field types refer to stable type-parameter entities.

- The generic enum can be instantiated with exact concrete semantic type arguments.

- Existing generic enum construction accepts the new declaration.

- Existing exact generic enum matching accepts the new declaration.

- Queries expose the declaration, binders, variants, fields, and structured types deterministically.

- Projection exposes enough structured facts for review without becoming authority.

- A failed declaration transaction publishes nothing and consumes no public identity.

- Old snapshots remain valid after success and after later deletion.

- Deleting the enum cascades through its owned type parameters, variants, and fields.

- Independent surviving dependents block deletion.

- Deleting or structurally removing every independent dependent in the same transaction permits deletion.

- Private compaction preserves every surviving public identity.

- The complete source-free program compiles without rendering or parsing source.

- The complete source-free program executes with the expected result.

- Imported and source-free equivalent programs agree on the relevant semantic and runtime facts.

- The final implementation contains no compatibility layer and no speculative adjacent subsystem.

## 6. Explicit Non-Goals

- Generic product declaration.

- A shared generic-nominal declaration framework introduced only for symmetry.

- Recursive self-reference to the enum being created.

- Mutually recursive source-free nominal declaration.

- A public draft nominal identity for a not-yet-published declaration.

- Trait bounds on enum type parameters unless the accepted language importer already gives them exact semantics.

- Generic type-parameter inference in semantic edits.

- Forwarding an unresolved caller type parameter into a generic enum instantiation.

- Ownership-bearing or reference-bearing generic instantiation.

- Weakening existing source-free nominal field ownership restrictions.

- Adding, deleting, or reordering enum members after declaration.

- Renaming enum type parameters unless an existing accepted operation already supports it.

- Cross-parent or generic semantic movement.

- A generic edit planner.

- A source renderer.

- A text round trip.

- A JSON transaction protocol.

- A workspace CLI.

- A daemon.

- Workspace persistence.

- A transaction journal.

- Collaboration or conflict state.

- Incremental recomputation.

- A compilation cache.

- A plugin system.

- A new runtime engine.

- Optimizing native support for generic enums.

- A broad crate or module reorganization.

- API-cost telemetry.

- A new benchmark framework.

## 7. Starting-State Facts to Verify

- `README.md` describes local check/run plus an in-process semantic workspace as the active product.

- `docs/roadmap.md` has no selected `Now` implementation vertical.

- `docs/status.md` lists generic enum declaration creation as a remaining source-free gap.

- `docs/spec/workspace.md` distinguishes current implementation from broader target behavior.

- `docs/architecture.md` defines `SemanticProgram` and `WorkspaceSnapshot` as the semantic path.

- `crates/lkjscript-compiler/src/workspace/transaction.rs` defines `TypeParameterDraft`.

- `crates/lkjscript-compiler/src/workspace/transaction.rs` defines non-generic `Edit::CreateEnum`.

- `EnumFieldDraft` currently stores `SemanticType`.

- `Edit::CreateFunction` already accepts ordered `TypeParameterDraft` values.

- `crates/lkjscript-compiler/src/workspace/draft.rs` defines `DraftTypeParameterId`.

- `DeclarationType` already represents declaration-local type-parameter handles.

- `crates/lkjscript-compiler/src/hir/enums.rs` stores `EnumDefinition::type_parameters`.

- The source importer already accepts generic enum declarations.

- The source importer already rejects duplicate enum type-parameter names.

- The source importer already has exact generic enum instantiation semantics.

- The semantic workspace already assigns stable entities to imported enum type parameters.

- The semantic workspace already supports exact generic enum values.

- The semantic workspace already supports exact generic enum patterns.

- The semantic workspace already derives concrete payload types through canonical substitution.

- Deletion already defines enum-owned type-parameter containment for imported generic enums.

- Compaction already rewrites enum-related private placement while preserving stable identities.

- `compile_snapshot` is the sole semantic-snapshot compilation boundary.

- The VM is the complete generic runtime route.

## 8. Repository Orientation

1. Record the actual branch, starting commit, and `git status --short`.

2. Read the installed root `AGENTS.md` once.

3. Read only the relevant sections of `docs/authority.md`, `docs/status.md`, `docs/spec/workspace.md`, `docs/architecture.md`, and `docs/roadmap.md`.

4. Inspect the latest commit and the immediately preceding generic workspace commits.

5. Search for `CreateEnum`, `EnumFieldDraft`, `TypeParameterDraft`, `DraftTypeParameterId`, `DeclarationType`, `EnumDefinition`, `SemanticEnum`, and `TypeParameter`.

6. Search every active constructor and pattern match of `Edit::CreateEnum` before changing its shape.

7. Search every active constructor and use of `EnumFieldDraft` before changing its field type.

8. Inspect generic function creation rather than rediscovering declaration-local binder publication.

9. Inspect imported generic enum indexing and stable identity reconciliation.

10. Inspect the exact generic enum value and pattern tests added by the latest commit.

11. Inspect deletion and compaction tests for imported generic enums.

12. Run the smallest existing focused tests that establish the current generic enum behavior.

13. Create a temporary or focused characterization proving that source-free declaration is the missing link.

14. Do not open whole large files when exact symbol search and narrow ranges suffice.

15. Preserve unrelated modifications and untracked files.

## 9. Suggested Initial Commands

```bash
git rev-parse --show-toplevel
git branch --show-current
git rev-parse HEAD
git status --short
git log --oneline --decorate -n 20
git show --stat --oneline --decorate HEAD
rg -n "CreateEnum|EnumFieldDraft|TypeParameterDraft|DraftTypeParameterId|DeclarationType" crates docs README.md AGENTS.md
rg -n "generic enum|type_parameters|TypeParameter" crates/lkjscript-compiler/src/workspace crates/lkjscript-compiler/src/hir docs
rg -n "EnumValue|EnumVariant|PatternDraft|TypeArgumentDraft" crates/lkjscript-compiler/src/workspace
rg -n "DeleteEntity|enum deletion|compaction" crates/lkjscript-compiler/src/workspace docs
cargo test --quiet -p lkjscript-compiler workspace --locked
```

Adapt the focused test command to the actual test names.

Do not run the full workspace suite before the characterization is understood.

Do not repeatedly run an unchanged failing command.

Capture a long failure once and inspect the causal section.

## 10. Characterization Deliverable

Before implementation, establish one focused failing consumer.

The preferred characterization begins from `Workspace::empty`.

It attempts to declare `maybe<t>` through the typed transaction API.

It must fail because the public edit cannot currently express the declaration-local field type, not because of an unrelated helper limitation.

Do not preserve a test that merely asserts a Rust compile error.

Convert the characterization into an executable semantic test as the API is implemented.

The final test must exercise declaration, identity publication, exact instantiation, exact pattern matching, direct compilation, and execution.

If a smaller existing test helper already constructs imported generic enums and source-free bodies, reuse it.

Do not create a general scenario DSL.

Do not create a generic test builder crate.

Do not create a serialized fixture language.

## 11. Private Task Ledger

- Operation: create one non-recursive generic enum from a source-free workspace.

- Consumer: a source-free `maybe<i64>` program that constructs, matches, compiles, and executes.

- Current contradiction: imported generic enums are usable, but the source-free declaration edit cannot create them.

- Semantic authority: the staged `SemanticProgram` and published immutable `WorkspaceSnapshot`.

- Creation-time representation: `DeclarationType` plus declaration-local binder handles.

- Published representation: stable semantic type-parameter entities referenced by `SemanticType`.

- Compiler representation: existing HIR enum type-parameter names and `Type::Param` values.

- Surviving identities: every pre-existing unrelated public entity and node.

- Created identities: enum, ordered type parameters, ordered variants, ordered fields.

- Tombstoned identities: none on pure creation; all owned identities on later enum deletion.

- Atomicity boundary: one revision-checked transaction and staged allocator.

- Success oracle: structured queries plus direct compiler and runtime result.

- Rejection oracle: deterministic validation errors with unchanged snapshot and allocator.

- Performance question: whether the new conversion remains one bounded iterative traversal.

- Non-goals: all items in the explicit non-goal section.

- Stop condition: the concrete consumer passes and the full relevant verification boundary is green.

## 12. Recommended Public API Cutover

Prefer the following direct shape unless exact repository evidence shows a smaller and clearer equivalent.

Extend `Edit::CreateEnum` with an ordered `type_parameters` field.

Reuse the existing `TypeParameterDraft` structure.

Change `EnumFieldDraft::ty` from `SemanticType` to `DeclarationType`.

Reuse `DraftTypeParameterId` for enum declaration-local binders.

Update every active producer and consumer in one incompatible cutover.

Do not retain a non-generic overload.

Do not add `CreateGenericEnum`.

Do not add `CreateEnumV2`.

Do not add default type parameters merely to reduce call-site changes.

Do not add conversion traits whose only purpose is preserving old call sites.

Do not add a builder object unless the current transaction representation demonstrably cannot express the validated data directly.

Keep `CreateProduct` unchanged.

Do not generalize `ProductFieldDraft` to `DeclarationType` in this vertical.

```rust
Edit::CreateEnum {
    name: String,
    type_parameters: Vec<TypeParameterDraft>,
    variants: Vec<EnumVariantDraft>,
}

EnumFieldDraft {
    name: String,
    ty: DeclarationType,
}
```

The code block is a design target, not a demand to ignore a better existing naming convention.

Any deviation must be justified by an actual invariant, not aesthetic symmetry.

## 13. Type-Parameter Draft Semantics

- A draft type-parameter handle belongs to exactly one creation edit.

- A draft handle is not a workspace identity.

- A draft handle is not a compiler identity.

- A draft handle is not a name-resolution token after publication.

- A draft handle never appears in a snapshot.

- A draft handle never appears in a query.

- A draft handle never appears in a projection.

- A draft handle never appears in a semantic diff.

- A draft handle may be numerically sparse if the existing function creation contract permits that.

- Duplicate draft handles reject.

- Unknown draft handles in field types reject.

- A handle from another creation edit cannot be represented as valid local intent.

- Type-parameter names must satisfy the existing identifier rules.

- Type-parameter names must be unique inside the enum declaration.

- Type-parameter order is semantic for explicit invariant enum arguments.

- Submitted type-argument maps remain canonicalized to declaration order.

- Published type parameters receive stable `EntityId` values.

- Published type parameters are owned by the enum entity.

- Published type parameters use the existing `EntityKind::TypeParameter` or exact current equivalent.

- Do not create an enum-specific public binder identity domain.

- Do not use names as published binder identity.

- Do not use vector positions as published binder identity.

- Do not use compiler-private parameter strings as published binder identity.

- Do not reuse stable function type-parameter entities as enum-local binders.

- A stable type parameter owned by another declaration must reject in an enum field type.

- An enum field may refer to the current edit's draft type parameters.

- An enum field may refer to already-published valid nominal constructors where existing field semantics allow it.

- An enum field may use ordinary nonownership builtin types where existing field semantics allow them.

- The new enum itself has no stable identity available to field drafts before publication.

- Do not introduce a draft nominal self-reference handle in this vertical.

## 14. Bounds and Phantom Parameters

Investigate source-imported generic enum semantics before copying generic function rules.

The existing HIR enum definition stores type-parameter names but no enum-bound set.

Do not invent enum trait bounds.

If `TypeParameterDraft` contains a `bounds` field, require it to be empty for `CreateEnum` unless the accepted importer already represents and validates enum bounds.

Return a structured owning-boundary rejection for unsupported nonempty bounds.

Do not silently ignore supplied bounds.

Do not extend HIR solely to make the reused draft structure look uniform.

Do not build an enum witness system.

Do not add a second generic constraint model.

Determine whether the importer permits a declared enum parameter that is unused by every field.

The existing exact generic enum value path explicitly supports nullary or phantom variants, but that does not by itself prove whether an entirely unused declaration parameter is accepted.

Mirror importer semantics exactly.

Do not apply the generic function unused-binder rule without evidence.

If phantom enum parameters are valid, preserve them and test exact argument retention.

If they are invalid, reject them through the same semantic rule as import.

## 15. Declaration-Type Conversion

Use one canonical conversion from creation-time `DeclarationType` to compiler-local `Type` and published `SemanticType`.

Extend or reuse the generic function conversion rather than cloning it.

The conversion must receive the declaration-local handle-to-stable-entity mapping.

The conversion must also receive the declaration-local handle-to-private-HIR-parameter mapping when required.

Every occurrence of the same draft handle must resolve to the same stable binder and compiler parameter.

Nested occurrences must work at arbitrary user-controlled type depth.

Clone, equality, hashing where implemented, debug, validation, conversion, query, projection, dependency analysis, compaction, and destruction must remain stack-safe.

Do not introduce native recursion over nested declaration types.

Do not impose a type-depth quota.

Use checked host-index conversion.

Return an explicit error for malformed completion state.

Do not use `unreachable!` on user-controlled draft shape unless prior validation makes the state genuinely internal and exhaustive.

Respect the repository lints that deny panic-oriented shortcuts.

## 16. Enum Creation Validation Order

1. Validate the transaction base revision and workspace namespace.

2. Validate edit overlap and deletion ownership through the existing transaction preflight.

3. Validate the enum name through the canonical declaration-name rule.

4. Validate global declaration-name collision through the canonical namespace rule.

5. Validate the type-parameter draft handles and names.

6. Validate the supported enum-bound policy.

7. Validate that the variant collection satisfies existing enum rules.

8. Validate variant names and sibling uniqueness.

9. Validate field names and per-variant uniqueness.

10. Validate every field declaration type against the local binder environment.

11. Reject foreign, stale, deleted, wrong-kind, or wrong-owner stable identities.

12. Reject unsupported ownership or reference storage through the existing nominal field rule.

13. Reserve all public identities in staged allocator state only.

14. Build the complete private enum definition and stable-to-private ownership mapping.

15. Insert the declaration into staged semantic state.

16. Run the existing effect, index, consistency, deletion-closure, and completeness logic applicable to the batch.

17. Reconcile every surviving and newly created public identity explicitly.

18. Build the deterministic base-to-final semantic diff.

19. Publish allocator and snapshot together only after every fallible finalization step succeeds.

## 17. Identity Allocation

Use the existing public identity allocator.

Do not create a second allocator.

Do not derive public identity from names.

Do not derive public identity from the HIR `EnumId`.

Do not derive public identity from the runtime layout identity.

Do not derive public identity from declaration order alone.

Do not derive public identity from content hashes.

Reserve identities in deterministic semantic order.

The preferred creation order is enum, ordered type parameters, ordered variants, then ordered fields within each variant, unless the current allocator contract establishes another exact order.

Keep the exact current convention if it is already documented and tested.

A failed transaction must leave deterministic future allocation unchanged.

A successful transaction must publish each created identity exactly once.

Old snapshots must retain their own allocator and address mappings.

A later same-name recreation after deletion must receive fresh generations.

A same-batch delete-and-recreate remains invalid unless the existing transaction contract says otherwise.

## 18. Private Compiler and Runtime Identities

- Continue deriving or allocating the private HIR `EnumId` through the existing path.

- Continue using the existing stable enum runtime-layout identity.

- Continue using existing private variant and field identities.

- Do not expose a private HIR type-parameter string as semantic identity.

- Do not expose private vector indexes.

- Do not expose source order as identity.

- Do not expose memory layout offsets.

- Do not expose bytecode operands.

- Do not expose native layout facts as edit inputs.

- Allow private compaction to relocate definitions.

- Require explicit public-identity reconciliation after relocation.

- Do not emit semantic movement for private relocation.

## 19. HIR Construction

Construct the same HIR enum shape that source import constructs.

Use `Origin::Semantic` or the exact existing source-free origin.

Populate ordered private enum type-parameter names through the existing private naming convention.

Lower current-edit binder occurrences to `Type::Param` using those exact private names.

Preserve ordered variants and fields.

Preserve existing field source-order or semantic-order facts without fabricating source coordinates.

Preserve existing runtime layout derivation.

Do not synthesize a source file.

Do not synthesize a `SourceId`.

Do not render the enum and parse it.

Do not call the importer from source-free creation.

Do not retain a second mutable HIR authority in the snapshot.

The published semantic program remains authoritative.

Complete HIR remains derived at `compile_snapshot`.

## 20. Reuse of Canonical Generic Enum Machinery

- Reuse the canonical enum constructor identity.

- Reuse stable enum type-parameter entities in published `SemanticType`.

- Reuse exact type-argument validation.

- Reuse declaration-order canonicalization.

- Reuse structural substitution.

- Reuse invariant enum instantiation semantics.

- Reuse payload field substitution.

- Reuse exact generic enum value construction.

- Reuse exact generic enum variant testing.

- Reuse exact generic enum pattern construction.

- Reuse match usefulness checking.

- Reuse match exhaustiveness checking.

- Reuse match plan construction.

- Reuse ownership validation.

- Reuse memory planning.

- Reuse SSA lowering and verification.

- Reuse bytecode lowering and validation.

- Reuse VM execution.

- Do not add source-free-only generic semantics.

- Do not add an enum-specific substitution cache.

- Do not copy the generic resolver into workspace transaction code.

## 21. Ownership and Reference Restrictions

Preserve the existing rule that source-free nominal declaration fields reject ownership-bearing or reference-bearing storage when that rule currently applies.

Do not weaken that rule merely because a field is written as a type parameter.

Preserve the existing restriction that concrete generic enum instantiation cannot smuggle an unsupported ownership or reference type into such a field.

Use the existing canonical generic instantiation validator to enforce the concrete restriction.

Do not precompute a parallel whitelist in the declaration edit.

Do not claim a type argument is legal from a shallow query filter when canonical validation remains required.

Preserve exact move, borrow, drop, cleanup, trap, and failure behavior.

The selected consumer should use `i64` so this vertical does not require changing ownership semantics.

Add focused rejection evidence for a concrete unsupported ownership/reference instantiation only if the existing exact generic enum test family does not already cover the new declaration path.

Do not duplicate broad ownership test matrices.

## 22. Query Requirements

- The created enum must appear in deterministic entity listing.

- Each created type parameter must appear as a stable entity owned by the enum.

- Variants must retain stable member entities.

- Fields must retain stable member entities.

- The enum type view must expose ordered stable type-parameter identities.

- The field type view must expose the stable type-parameter identity rather than its draft handle.

- Concrete enum value and pattern views must expose exact ordered concrete arguments.

- Generic constructor candidates must remain filtered by the same canonical restrictions.

- Query ordering must not depend on hash-map iteration.

- Pagination continuations must remain bound to namespace, revision, and query.

- Old-revision continuations must fail exactly as before after publication.

- No query should expose private HIR parameter strings as identity inputs.

- No query should require source attachments.

- No new broad query protocol is required.

- Add only a narrow missing view if the existing query model cannot honestly expose an already-published fact required by the consumer.

## 23. Projection Requirements

- Projection must remain deterministic.

- Projection must remain non-authoritative.

- Projection must render the generic enum declaration from semantic facts.

- Projection should show the ordered type parameters.

- Projection should show variant and field structure.

- Projection should show that the `some.value` field refers to the enum's stable `t` binder.

- Projection must not expose draft handles.

- Projection must not fabricate source coordinates.

- Projection labels remain review-local spellings.

- A source attachment must not be required.

- Projection failure must remain fallible rather than panic.

- Do not build a complete source renderer.

## 24. Semantic Diff Requirements

- Report enum entity creation.

- Report each ordered type-parameter entity creation.

- Report each ordered variant entity creation.

- Report each ordered field entity creation.

- Use the existing creation diff form unless a current consumer proves it insufficient.

- Do not create a generic-enum-specific diff registry.

- Do not include draft handles.

- Do not include private HIR IDs.

- Do not include private vector positions.

- Do not report private compaction.

- Do not report synthetic reference rewiring when no published reference changed.

- Keep diff order deterministic.

- Keep the diff base-to-final when multiple related edits occur in one transaction.

- A failed transaction returns no published diff.

## 25. Failure Atomicity

For every relevant rejection below, publication must be all or nothing.

Do not write one separate test per line when a coherent table-driven test can prove the family.

- Reject stale base revision without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject foreign workspace identity without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject deleted nominal identity without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject wrong-kind identity without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject duplicate enum name without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject reserved enum name without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject invalid enum name without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject duplicate type-parameter handle without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject duplicate type-parameter name without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject invalid type-parameter name without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject unknown draft type-parameter handle without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject unsupported nonempty enum parameter bounds without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject duplicate variant name without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject invalid variant name without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject empty or otherwise invalid variant set according to current semantics without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject duplicate field name inside one variant without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject invalid field name without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject foreign stable type parameter in a field without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject stale stable nominal identity in a field without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject wrong-kind stable nominal identity in a field without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject unsupported ownership-bearing field type without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject unsupported reference-bearing field type without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject host index conversion failure without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject allocation failure without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject final consistency failure without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

- Reject complete-program ownership failure caused by another edit in the batch without changing the published snapshot, revision, allocator, diagnostics, blockers, attachments, provenance, or deterministic future IDs.

## 26. Deletion and Lifecycle

A generic enum owns its type parameters, variants, and fields.

Deleting the enum must tombstone all of those public identities.

Do not leave type-parameter entities orphaned.

Do not retain dead field types that reference deleted binders.

Do not retain dead query index entries.

Do not retain dead dependency edges.

Do not retain dead match-plan facts.

Do not retain dead generic substitutions.

A surviving function signature using `maybe<i64>` must block enum deletion.

A surviving expression constructing `maybe<i64>` must block enum deletion.

A surviving exact match over `maybe<i64>` must block enum deletion.

A surviving hole whose expected type is `maybe<i64>` must block enum deletion.

A surviving unresolved-reference expectation involving the enum must block deletion.

Deleting the independent owner of every such dependency in the same batch should permit deletion under the existing final-state closure law.

Private dense compaction may relocate other declarations.

Every unrelated public entity and node must keep identity.

Old snapshots must still query and compile according to their original state.

A later same-name enum recreation must receive fresh public generations.

Do not add a generic dependency planner.

Extend the existing final-state dependency traversal only where the new published binder form reveals an actual missed edge.

## 27. Rename Behavior

Enum declaration creation must not accidentally make names into type identity.

Renaming the enum after creation must leave its nominal identity unchanged.

Renaming a variant or field after creation must leave its member identity unchanged.

Existing exact generic enum values and patterns must continue to resolve by stable identities after rename.

The concrete instantiated `SemanticType::Enum` must not change identity when presentation names change.

Do not add enum type-parameter rename in this vertical unless it is already part of the accepted generic binder lifecycle.

Do not use spelling to reconnect field binder references after rename.

Do not rewrite unrelated declarations on rename.

Include a focused post-creation enum or member rename assertion only if the latest exact-generic tests do not already prove the same path for source-free-created declarations.

## 28. Imported and Source-Free Equivalence

Build one imported generic enum program using the accepted source importer.

Build an equivalent source-free generic enum program.

Compare semantic facts, not private IDs that are intentionally construction-dependent.

Use stable semantic relationships within each snapshot.

Compare the ordered number and kinds of entities.

Compare declaration and member names.

Compare ordered type-parameter counts and bounds facts.

Compare field types after normalization.

Compare concrete enum instantiation.

Compare exact match structure.

Compare relevant dependency and reference shapes.

Compare selected memory-plan obligation kinds.

Compare normalized bytecode for the executable functions where the existing equivalence helpers support it.

Compare VM result.

Compare trap and cleanup behavior when relevant.

Prove the source-free path invokes source loading and parsing zero times.

Do not require imported and source-free public IDs to have the same raw bytes across independent namespaces.

Do not compare private HIR IDs, dense indexes, runtime addresses, or source origins for equality.

Preserve honest origin differences.

## 29. Direct Compilation and Execution

1. Reach a complete source-free snapshot with no blockers.

2. Call `compile_snapshot` directly.

3. Prove no source renderer is involved.

4. Prove no importer is involved after `Workspace::empty`.

5. Prove completeness is checked before HIR derivation.

6. Prove the derived HIR accepts the generic enum declaration.

7. Prove memory planning accepts the concrete `maybe<i64>` value.

8. Prove SSA construction and verification accept the exact match.

9. Prove bytecode lowering and validation accept the program.

10. Execute the ordinary validated program.

11. Accept a typed baseline-native pre-entry decline.

12. Do not retry the VM after native entry.

13. Prove host effects occur zero times for the selected pure consumer.

14. Prove the result is exactly `42`.

## 30. Completeness and Incomplete States

Creating the generic enum alone is a complete declaration and must not introduce a declaration-body blocker.

Creating a new function or `main` in the same transaction may introduce the ordinary body holes.

The enum's type parameters are not holes.

A malformed local binder reference is a transaction error, not a persistent unresolved type parameter.

Do not introduce a new incomplete node kind for generic enum declaration.

Do not publish partially declared variants or fields.

Do not publish a declaration with unresolved local handles.

Do not add automatic repair or fallback types.

A complete final snapshot must pass canonical whole-program validation.

An incomplete final snapshot remains queryable only under the existing transaction rules.

Compilation must still reject before HIR derivation when any blocker remains.

## 31. Stack Safety

- Keep cloning a deeply nested `DeclarationType` containing the enum's draft parameter off the unbounded native stack.

- Keep comparing deeply nested declaration types off the unbounded native stack.

- Keep debugging or projecting a deeply nested declaration type where supported off the unbounded native stack.

- Keep validating a deeply nested field type off the unbounded native stack.

- Keep resolving a deeply nested draft binder occurrence off the unbounded native stack.

- Keep converting a deeply nested declaration type to compiler HIR type off the unbounded native stack.

- Keep converting a deeply nested published semantic type off the unbounded native stack.

- Keep indexing the resulting field type off the unbounded native stack.

- Keep querying the resulting field type off the unbounded native stack.

- Keep projecting the resulting field type off the unbounded native stack.

- Keep deleting and destroying the resulting declaration off the unbounded native stack.

Reuse the existing iterative `DeclarationType` machinery.

Extend its work-item state only as needed.

Do not add a recursive convenience conversion.

Do not add a semantic depth limit.

A focused ordinary-depth test is mandatory.

An ignored locked-release 20,000-level test is appropriate only if the new code path is not already covered by an existing type-depth fixture.

Do not duplicate the full compiler stress geometry when a type-only fixture is sufficient.

## 32. Performance Discipline

- Do not optimize before measuring a suspected repeated cost.

- Do not add incrementality.

- Do not add a cache.

- Do not add a warm process.

- Do not add parallelism.

- Do not add a registry.

- Do not add a digest.

- Do not add a new benchmark runner.

- The expected creation work should be linear in type parameters, variants, fields, and field-type nodes.

- Use existing one-pass work counters when they can prove the complexity shape cheaply.

- Do not record a performance claim from one sample.

- Do not edit `docs/performance.md` unless a retained reproducible measurement or reversal condition changes.

- Compilation may continue to dominate transaction latency.

- Do not optimize compiler phases merely because the new consumer reaches them.

- Do not narrow valid enum shapes to make native specialization easier.

- Preserve the generic VM fallback for every otherwise valid program.

## 33. API-Cost and Agent-Attention Discipline

- Search before reading.

- Use exact symbols.

- Read narrow line ranges.

- Use recent diffs to avoid reopening settled design questions.

- Keep the private task ledger current.

- Do not reread the entire root policy in the same turn.

- Do not dump whole large workspace files into context.

- Do not dump generated fixtures.

- Do not dump full test logs when one causal region is enough.

- Run one focused test before a crate test.

- Run a crate test before a workspace test.

- Do not rerun commands with unchanged inputs.

- Batch mechanical `CreateEnum` call-site updates.

- Use one compiler error pass as a migration inventory.

- Stop generating alternatives after evidence selects the direct representation.

- Use subagents only for independent bounded questions.

- Do not ask multiple subagents to design competing generic enum APIs.

- Do not add API-spend instrumentation to the product.

- Do not trade correctness for fewer tool calls.

- Reserve the full verification boundary for the final relevant state.

## 34. Implementation Sequence

1. Install or reconcile the companion root `AGENTS.md`.

2. Record starting repository state.

3. Verify the gap and concrete consumer.

4. Locate all `CreateEnum` and `EnumFieldDraft` producers and consumers.

5. Inspect generic function declaration-local binder validation and publication.

6. Inspect imported generic enum binder indexing and deletion.

7. Write or adapt the focused failing source-free generic enum test.

8. Change the public edit shape in one direct cutover.

9. Change enum field creation-time types in one direct cutover.

10. Generalize the existing declaration-local binder resolver only as far as enum creation requires.

11. Validate enum-specific binder semantics without copying function-only restrictions blindly.

12. Allocate stable enum type-parameter entities through the existing allocator.

13. Build the existing HIR enum representation.

14. Publish stable field types referring to stable binder entities.

15. Update indexes and queries only where existing generic enum import facts are not already generic.

16. Update projection only where existing imported generic enum rendering is not already generic.

17. Update deletion and compaction only where source-free creation exposes a missed path.

18. Run focused creation and rejection tests.

19. Run exact generic enum value and pattern tests.

20. Run direct compilation and VM execution proof.

21. Run imported/source-free equivalence proof.

22. Run focused lifecycle and atomicity tests.

23. Run a stack-safety fixture if the new conversion path lacks existing coverage.

24. Delete displaced non-generic-only code or validation.

25. Update maintained documentation.

26. Run affected crate tests.

27. Run the full required verification boundary once.

28. Inspect the final diff for overreach.

29. Commit one cohesive change when permitted by the execution environment.

30. Report exact evidence and remaining gaps.

## 35. Likely File and Symbol Inventory

- `crates/lkjscript-compiler/src/workspace/transaction.rs`

  - `TypeParameterDraft`

  - `EnumFieldDraft`

  - `EnumVariantDraft`

  - `Edit::CreateEnum`

  - enum staging and validation

  - created entity diff generation

  - allocator staging

- `crates/lkjscript-compiler/src/workspace/draft.rs`

  - `DraftTypeParameterId`

  - `DeclarationType`

  - iterative conversion helpers

  - debug/clone/equality paths

- `crates/lkjscript-compiler/src/workspace/types.rs`

  - `SemanticType`

  - stable binder conversion

  - enum constructor and argument validation

- `crates/lkjscript-compiler/src/workspace/identity.rs`

  - stable entity allocation and reconciliation

  - tombstone behavior

- `crates/lkjscript-compiler/src/workspace/index.rs`

  - enum type-parameter entity indexing

  - field type facts

  - dependency edges

- `crates/lkjscript-compiler/src/workspace/query.rs`

  - type-parameter views

  - enum and field structured type views

  - legal constructor views

- `crates/lkjscript-compiler/src/workspace/projection.rs`

  - generic enum declaration rendering

  - stable binder rendering

- `crates/lkjscript-compiler/src/workspace/compaction.rs`

  - enum deletion

  - type-parameter pruning

  - field type remapping

- `crates/lkjscript-compiler/src/workspace/validate.rs`

  - complete semantic consistency

  - binder ownership

- `crates/lkjscript-compiler/src/workspace/program.rs`

  - semantic enum storage

  - HIR derivation

- `crates/lkjscript-compiler/src/generic_call.rs`

  - canonical enum substitution and concrete restrictions

- `crates/lkjscript-compiler/src/hir/enums.rs`

  - `EnumDefinition::type_parameters`

  - `EnumVariantField::ty`

- `crates/lkjscript-compiler/src/analyze/tests/enums.rs`

  - importer semantic oracle

  - phantom and duplicate parameter behavior

  - ownership storage behavior

- `crates/lkjscript-compiler/src/workspace/tests.rs`

  - focused source-free creation tests

  - generic enum value and pattern tests

  - equivalence and execution helpers

- `docs/status.md`

  - implemented facts

  - known gap removal

- `docs/spec/workspace.md`

  - current implementation description

  - target/current distinction

- `docs/architecture.md`

  - creation and publication flow

  - stable/private binder boundary

- `docs/roadmap.md`

  - remove a completed item if one was selected

  - do not invent a replacement `Now` item

- `README.md`

  - concise capability summary if materially changed

This inventory is a search guide, not permission to edit every listed file.

Edit only the dependency closure selected by evidence.

Do not split files merely because they are large.

Do not merge modules merely because several are touched.

## 36. Focused Success Test Matrix

- `minimal declaration`

  - Create `maybe<t>` with `none` and `some(value: t)` from an empty workspace.

- `stable ownership`

  - Verify the enum owns one stable type-parameter entity, two variant entities, and one field entity.

- `published field type`

  - Verify `some.value` has published type `SemanticType::TypeParameter` naming the stable enum binder.

- `draft opacity`

  - Verify no draft handle appears in entity, query, projection, or diff output.

- `ordered parameters`

  - Create an enum with two parameters and verify declaration-order argument canonicalization.

- `field canonical order`

  - Submit fields in any currently accepted non-semantic order and verify published declaration order.

- `phantom behavior`

  - Mirror the importer rule for a parameter unused by payload fields.

- `nested safe type`

  - Use the local binder inside a supported nested declaration type and verify canonical conversion.

- `exact value`

  - Construct `some<i64>(42)` through the existing exact generic enum value draft.

- `nullary value`

  - Construct `none<i64>` and retain the explicit concrete enum argument.

- `exact pattern`

  - Match `some(value)` and `none` exhaustively.

- `payload type`

  - Verify the `value` pattern binding has concrete type `i64`.

- `query`

  - Inspect enum, binder, variant, field, and concrete instantiation facts by stable identities.

- `projection`

  - Project the declaration and body without source attachments.

- `rename continuity`

  - Rename an existing supported enum/member name and preserve nominal/member identities.

- `direct compile`

  - Compile the complete snapshot through `compile_snapshot`.

- `zero source work`

  - Assert zero source-loading and parser invocations for the source-free path.

- `VM execution`

  - Execute and obtain result `42`.

- `import equivalence`

  - Compare with an equivalent imported generic enum program.

- `old snapshot`

  - Keep and query the pre-creation and post-creation snapshots independently.

- `delete lifecycle`

  - Delete the enum after removing every independent dependent and tombstone owned identities.

- `recreation`

  - Recreate the same name later and receive fresh generations.

## 37. Focused Rejection Test Matrix

- `stale revision`

  - Use a prior base revision.

  - Assert no publication.

  - Assert no public identity consumption.

- `foreign namespace`

  - Reference a nominal or binder identity from another workspace.

  - Assert no publication.

  - Assert no public identity consumption.

- `duplicate enum`

  - Create a global declaration with an existing name.

  - Assert no publication.

  - Assert no public identity consumption.

- `invalid enum name`

  - Use a reserved or malformed declaration name.

  - Assert no publication.

  - Assert no public identity consumption.

- `duplicate binder handle`

  - Repeat one `DraftTypeParameterId`.

  - Assert no publication.

  - Assert no public identity consumption.

- `duplicate binder name`

  - Declare two enum binders with the same name.

  - Assert no publication.

  - Assert no public identity consumption.

- `invalid binder name`

  - Use a malformed binder name.

  - Assert no publication.

  - Assert no public identity consumption.

- `unknown binder handle`

  - Use a draft binder in a field without declaring it.

  - Assert no publication.

  - Assert no public identity consumption.

- `foreign stable binder`

  - Use a function-owned stable type parameter in the enum field.

  - Assert no publication.

  - Assert no public identity consumption.

- `unsupported bounds`

  - Supply a nonempty enum binder bound when enum bounds have no accepted semantics.

  - Assert no publication.

  - Assert no public identity consumption.

- `duplicate variant`

  - Repeat a variant name.

  - Assert no publication.

  - Assert no public identity consumption.

- `invalid variant`

  - Use a malformed variant name.

  - Assert no publication.

  - Assert no public identity consumption.

- `invalid variant set`

  - Use an empty or otherwise invalid set according to importer semantics.

  - Assert no publication.

  - Assert no public identity consumption.

- `duplicate field`

  - Repeat a field name inside one variant.

  - Assert no publication.

  - Assert no public identity consumption.

- `invalid field`

  - Use a malformed field name.

  - Assert no publication.

  - Assert no public identity consumption.

- `stale nominal`

  - Use a deleted nominal identity inside a field type.

  - Assert no publication.

  - Assert no public identity consumption.

- `wrong kind`

  - Use a function or field entity where a nominal constructor is required.

  - Assert no publication.

  - Assert no public identity consumption.

- `ownership field`

  - Store an unsupported ownership-bearing type.

  - Assert no publication.

  - Assert no public identity consumption.

- `reference field`

  - Store an unsupported borrowed/reference type.

  - Assert no publication.

  - Assert no public identity consumption.

- `unsupported concrete argument`

  - Instantiate the generic enum with a concrete ownership/reference type disallowed by the existing exact resolver.

  - Assert no publication.

  - Assert no public identity consumption.

- `surviving dependency`

  - Delete the enum while a function signature or body still uses it.

  - Assert no publication.

  - Assert no public identity consumption.

- `same-name delete/create`

  - Delete and recreate the enum in one contradictory batch.

  - Assert no publication.

  - Assert no public identity consumption.

- `allocation failure`

  - Use the existing failpoint to prove staged allocation rollback when available.

  - Assert no publication.

  - Assert no public identity consumption.

## 38. Atomicity Assertion Set

- The returned error is deterministic.

- The current `Arc<WorkspaceSnapshot>` is unchanged when the existing tests can compare it directly.

- The workspace revision is unchanged.

- The allocator free lists and generations are unchanged.

- The next successful deterministic creation receives the same identities it would have received without the failed attempt.

- Entities are unchanged.

- Nodes are unchanged.

- Containment is unchanged.

- References are unchanged.

- Dependencies are unchanged.

- Calls are unchanged.

- Holes are unchanged.

- Unresolved references are unchanged.

- Diagnostics are unchanged.

- Completeness blockers are unchanged.

- Attachments are unchanged.

- Captured provenance is unchanged.

- Old query continuations remain valid for the unchanged revision.

## 39. Identity Continuity Table

| Operation | Created | Tombstoned | Must preserve |
| --- | --- | --- | --- |
| enum creation | new enum identity | none | all pre-existing survivors |
| type-parameter publication | new ordered binder identities | none | enum and unrelated survivors |
| variant publication | new ordered variant identities | none | enum, binders, unrelated survivors |
| field publication | new ordered field identities | none | enum, binders, variants, unrelated survivors |
| enum rename | none | none | enum, binders, variants, fields, uses |
| variant rename | none | none | variant, field, constructor and pattern uses |
| field rename | none | none | field and aggregate/pattern uses |
| private compaction | none | none | every semantic survivor |
| enum deletion | none | enum, binders, variants, fields | unrelated survivors |
| later same-name recreation | fresh enum and owned identities | prior identities remain tombstoned | unrelated survivors |

## 40. Query and Projection Assertions

- The enum entity has the correct kind and name.

- The enum type view reports one ordered type parameter.

- The type parameter view reports the correct owner.

- The type parameter view reports the correct presentation name.

- The `none` variant has zero fields.

- The `some` variant has one ordered field.

- The field view uses the stable binder entity.

- The concrete enum type has one exact `i64` argument.

- The concrete `some` payload type is `i64`.

- The exact match view reports both arms in semantic order.

- The payload binding entity belongs only to its arm scope.

- The projected declaration does not need a source path.

- The projection has no draft IDs.

- The projection has no private compiler IDs.

- A stale continuation after publication rejects.

## 41. Source-Free Execution Scenario

Prefer a small pure program.

Do not add a capability merely to make output visible.

Returning an `i64` is enough when the existing execution test helper can inspect it.

If the production `main` contract requires a different result shape, use the smallest existing accepted result and assert it through the current runtime test oracle.

Do not change `main` semantics for this vertical.

Do not add a special testing runtime.

Do not expose the SSA evaluator publicly.

Do not force baseline native execution.

The VM result is the correctness oracle.

The SSA evaluator may remain an opt-in differential oracle where existing tests already use it.

## 42. Conceptual Construction Sequence

```rust
let mut workspace = Workspace::empty()?;

let create = workspace.apply(Transaction {
    base_revision: workspace.current().revision(),
    edits: vec![
        Edit::CreateEnum {
            name: "maybe".into(),
            type_parameters: vec![
                TypeParameterDraft {
                    id: DraftTypeParameterId::new(0),
                    name: "t".into(),
                    bounds: vec![],
                },
            ],
            variants: vec![
                EnumVariantDraft {
                    name: "none".into(),
                    fields: vec![],
                },
                EnumVariantDraft {
                    name: "some".into(),
                    fields: vec![
                        EnumFieldDraft {
                            name: "value".into(),
                            ty: DeclarationType::DraftTypeParameter(
                                DraftTypeParameterId::new(0),
                            ),
                        },
                    ],
                },
            ],
        },
    ],
})?;
```

Treat this as conceptual pseudocode.

Adapt exact result extraction, stable identity lookup, expression drafts, and execution helpers to the current API.

Do not add convenience APIs merely to make this pseudocode compile verbatim.

Use the semantic diff or deterministic query APIs to obtain the published enum, binder, variant, and field identities.

Fill later bodies with the existing exact generic enum value and pattern operations.

## 43. Avoiding Accidental Generalization

- Do not rename `DeclarationType` to a universal schema merely because enums now use it.

- Do not introduce `NominalDeclarationDraft`.

- Do not merge product and enum creation.

- Do not add a generic member table.

- Do not add a generic nominal registry.

- Do not add a generic binder registry.

- Do not add reflection metadata.

- Do not add a visitor framework for one new conversion branch.

- Do not add a trait abstraction for two local conversion call sites unless it removes a real duplicated invariant.

- Do not add a macro to enumerate type forms unless the existing repository already uses one as authority.

- Do not add serialization derives to in-process semantic types.

- Do not add schema version fields.

- Do not add a stable wire encoding.

- Do not add a general recursive declaration planner.

- Do not add placeholder enum identities.

- Do not add a two-phase public declaration lifecycle.

- Do not add an edit that mutates a declared enum after publication.

- Do not add generic enum bounds.

- Do not add generic products.

- Do not add generic forwarding.

## 44. Rejected Alternative Designs

- **Reject:** `CreateGenericEnum` beside `CreateEnum`

  - **Reason:** Creates a compatibility split and two authorities for one operation.

- **Reject:** `CreateEnumV2`

  - **Reason:** Preserves a provisional API without an external compatibility boundary.

- **Reject:** Enum fields continue using `SemanticType` plus a magic binder name

  - **Reason:** Makes names or sentinels semantic and cannot publish stable binder identity honestly.

- **Reject:** Enum fields continue using `SemanticType` plus a placeholder `EntityId`

  - **Reason:** Fabricates a public identity before allocation and weakens validation.

- **Reject:** A new `DraftEnumTypeParameterId`

  - **Reason:** Duplicates the existing declaration-local binder handle domain without a distinct invariant.

- **Reject:** A generic nominal builder graph

  - **Reason:** Introduces a broad framework for one declaration operation.

- **Reject:** A source snippet embedded in `CreateEnum`

  - **Reason:** Restores text as semantic authority and invokes parsing.

- **Reject:** Render the enum to source and import it

  - **Reason:** Creates a source round trip and a second correctness path.

- **Reject:** Persist the workspace to make the consumer durable

  - **Reason:** Adds an unmeasured boundary unrelated to declaration semantics.

- **Reject:** Add a CLI JSON protocol

  - **Reason:** Adds a wire contract before the in-process vertical is complete and measured.

- **Reject:** Add recursive self-reference now

  - **Reason:** Requires a new unpublished nominal-reference lifecycle not needed by the concrete consumer.

- **Reject:** Add enum trait bounds now

  - **Reason:** Extends language semantics and witness machinery without an importer or consumer requirement.

- **Reject:** Generalize products simultaneously

  - **Reason:** Combines a separate roadmap gap because of superficial shape similarity.

- **Reject:** Optimize native generic enum execution

  - **Reason:** Confuses optional specialization with semantic completeness.

## 45. Error Design

Reject malformed input at the transaction boundary.

Use the existing structured `WorkspaceError` family.

Extend it narrowly only when no existing variant can carry the facts a caller needs.

Do not stringify typed facts and parse them in tests.

Do not fabricate source paths or spans.

Do not panic on user-controlled draft data.

Do not silently ignore unsupported bounds.

Do not silently replace an unknown draft binder with `unit`.

Do not silently drop an invalid field.

Do not auto-resolve a stable type parameter by name.

Do not auto-correct duplicate parameters.

Keep deterministic error ordering.

When several malformed facts exist, preserve the current transaction validation precedence unless a change is required for one clear owning-boundary rule.

Do not build aggregate multi-error infrastructure for this vertical.

## 46. Test Organization

Search existing workspace test helpers before adding new helpers.

Prefer extending the latest exact generic enum test family.

Prefer one coherent end-to-end test and one table-driven rejection test over many tiny isolated tests.

Place lifecycle assertions beside existing deletion and compaction coverage when that reduces duplication.

Place type-depth assertions beside existing declaration-type stress coverage when possible.

Do not create a new crate.

Do not create a new integration-test binary unless an existing test boundary cannot access the required instrumentation.

Do not create generated source fixtures for the source-free path.

An imported source oracle may use the existing line-oriented test source.

Keep test names semantic.

Delete tests that preserve the old non-generic-only API shape.

Do not weaken unrelated tests.

## 47. Suggested Test Names

- `source_free_generic_enum_declaration_compiles_and_executes_exact_match`

- `generic_enum_creation_publishes_stable_binders_and_member_types`

- `generic_enum_creation_rejects_malformed_local_binders_atomically`

- `generic_enum_creation_matches_imported_semantics_without_source_work`

- `generic_enum_deletion_cascades_binders_and_preserves_survivors`

- `generic_enum_field_declaration_type_is_stack_safe`

Use existing naming conventions if they differ.

Do not add every suggested test if existing tests already cover the invariant.

## 48. Documentation Changes

- ``README.md``

  - State concisely that source-free creation now covers generic enums if that materially improves the entry-point summary.

- ``docs/status.md``

  - Report exact implemented source-free generic enum declaration behavior and remove the corresponding known gap.

- ``docs/spec/workspace.md``

  - Update only the current-implementation description and any accepted target wording directly affected by the completed operation.

- ``docs/architecture.md``

  - Describe declaration-local binder validation, stable publication, and direct HIR derivation without creating a second flow.

- ``docs/roadmap.md``

  - Remove the completed vertical if it was recorded; leave `Now` empty if no next item has evidence.

- ``docs/performance.md``

  - Change only if retained reproducible performance evidence or a reversal condition changes.

- `root `AGENTS.md``

  - Install the companion policy and keep it aligned with the actual product boundary.

Do not duplicate exact API tables across documents.

Do not present target behavior as implemented before tests pass.

Do not retain the old gap as historical prose.

Git history already preserves superseded behavior.

Do not add a permanent task report.

## 49. Backward Compatibility Policy

- Do not preserve the old `CreateEnum` shape.

- Do not preserve the old `EnumFieldDraft::ty` type through an adapter.

- Do not add `From<SemanticType>` solely to keep old enum creation call sites unchanged.

- Do not add default generic parameters.

- Do not accept both `SemanticType` and `DeclarationType` through an untyped union.

- Do not add a feature flag.

- Do not add an edition.

- Do not add a migration document.

- Do not retain stale tests for the old API.

- Update all repository call sites atomically.

- Delete obsolete helper paths.

- Mention the intentional Rust API break in the final report.

## 50. Multi-Turn Operating Rules

- Complete one coherent vertical across as many agent turns as necessary.

- At the end of a turn, leave the worktree buildable when reasonably possible.

- Do not publish a knowingly invalid semantic snapshot in code.

- Maintain a compact handoff with exact paths, symbols, failing tests, and next command.

- Do not paste large source files into the handoff.

- Do not re-explain settled alternatives in every turn.

- Do not silently broaden scope after a handoff.

- On resume, read the handoff and inspect current git state before acting.

- If a later turn finds contradictory evidence, revise the design explicitly.

- If the selected vertical is complete, stop instead of filling time with adjacent work.

## 51. Handoff Template

```
Starting commit:
Current commit:
Branch:
Worktree:
Selected vertical:
Concrete consumer:
Characterization:
Representation decision:
Public API cutover:
Implemented:
Focused tests passing:
Current failure:
Exact next command:
Files currently modified:
Identity behavior:
Atomicity behavior:
Compiler/runtime evidence:
Documentation status:
Full verification status:
Explicit non-goals:
Remaining blocker inside this vertical:
Stop condition:
```

## 52. Focused Verification Ladder

1. Run the single characterization or new end-to-end test.

2. Run the table-driven malformed-binder and atomicity test.

3. Run the existing generic enum value tests.

4. Run the existing generic enum pattern tests.

5. Run the existing generic function declaration tests to detect accidental binder regressions.

6. Run existing enum deletion and compaction tests.

7. Run the source-free/import equivalence test.

8. Run the direct compile and VM execution test.

9. Run the affected workspace module tests.

10. Run all `lkjscript-compiler` targets and features.

11. Run the workspace tests only after the focused boundary is stable.

12. Format only after the representation stops changing.

13. Run the full repository verification boundary once.

## 53. Required Final Verification

```bash
cargo fmt --all -- --check
cargo clippy --quiet --workspace --all-targets --all-features --locked -- -D warnings
cargo test --quiet --workspace --all-targets --all-features --locked
cargo build --quiet --workspace --release --locked
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
git diff --check
```

Run `cargo fmt --all` first only when formatting is needed.

Do not omit `--locked`.

Do not omit `--all-targets`.

Do not omit `--all-features`.

Do not omit the release build.

Run Docker verification when the environment supports it and the repository retains that boundary.

Report an exact environment failure.

Do not claim an unrun command passed.

Do not repeatedly rebuild Docker with unchanged inputs.

## 54. Final Diff Inspection

1. Inspect `git status --short`.

2. Inspect `git diff --stat`.

3. Inspect the substantive public API diff.

4. Inspect transaction validation and allocator publication.

5. Inspect stable identity reconciliation.

6. Inspect generic substitution reuse.

7. Inspect deletion and compaction.

8. Inspect queries and projection.

9. Inspect tests for root-cause coverage rather than topology preservation.

10. Inspect documentation for stale non-generic-only claims.

11. Search for old `CreateEnum` call shapes.

12. Search for accidental adapters or `v2` names.

13. Search for source rendering or importer calls on the source-free path.

14. Search for new recursion over user-controlled type depth.

15. Search for new panic, unwrap, todo, or unimplemented calls.

16. Search for prompt files, logs, caches, build artifacts, and generated scratch files.

17. Confirm unrelated changes are neither staged nor overwritten.

## 55. Commit Policy

When repository permissions and the execution environment permit commits, create one cohesive commit after verification.

Use a subject describing the semantic result.

A suitable subject is `Complete source-free generic enum declarations`.

Do not mention this prompt in the commit subject.

Do not commit temporary characterization failures.

Do not commit raw logs.

Do not commit benchmark output that is not retained evidence.

Do not push.

Do not open a pull request.

If unrelated user changes prevent one clean commit, leave the worktree intact and report the exact state.

## 56. Completion Criteria

- [ ] The actual checkout gap was characterized.

- [ ] The companion root policy is installed or safely reconciled.

- [ ] One direct public API cutover is complete.

- [ ] A non-recursive generic enum can be declared source-free.

- [ ] Declaration-local binder handles are validated.

- [ ] Stable binder entities are published.

- [ ] Published field types refer to stable binders.

- [ ] Draft handles do not escape.

- [ ] Malformed local binder input rejects deterministically.

- [ ] Failed transactions publish nothing.

- [ ] Failed transactions consume no public identities.

- [ ] Exact generic enum values work with the new declaration.

- [ ] Exact generic enum patterns work with the new declaration.

- [ ] Concrete payload substitution is exact.

- [ ] Queries expose deterministic stable facts.

- [ ] Projection is source-independent.

- [ ] Deletion cascades owned binder/member identities.

- [ ] Surviving dependencies block deletion.

- [ ] Private compaction preserves survivors.

- [ ] Old snapshots remain valid.

- [ ] Direct compilation performs no source round trip.

- [ ] Source loading remains zero.

- [ ] Parsing remains zero.

- [ ] The VM executes the program correctly.

- [ ] Optional native decline does not invalidate the program.

- [ ] Imported and source-free semantics agree.

- [ ] User-depth type handling remains stack-safe.

- [ ] No arbitrary semantic quota is added.

- [ ] No second generic resolver is added.

- [ ] No second semantic authority is added.

- [ ] No compatibility layer remains.

- [ ] No persistence, daemon, protocol, cache, or collaboration machinery appears.

- [ ] Documentation matches the checkout.

- [ ] Focused tests protect the root cause.

- [ ] Required verification passed or exact failures are reported.

- [ ] The final diff contains no unrelated work.

## 57. Stop Conditions

- Stop and reassess if The proposed change would require a second mutable program representation.

- Stop and reassess if The proposed change would require names to serve as binder identity.

- Stop and reassess if The proposed change would require a public placeholder identity.

- Stop and reassess if The proposed change would require rendering and parsing source.

- Stop and reassess if The proposed change would require a generic edit planner.

- Stop and reassess if The proposed change would require a broad nominal-declaration framework.

- Stop and reassess if The proposed change would require persistence.

- Stop and reassess if The proposed change would require a daemon.

- Stop and reassess if The proposed change would require a wire protocol.

- Stop and reassess if The proposed change would require collaboration semantics.

- Stop and reassess if The proposed change would require enum trait-bound semantics.

- Stop and reassess if The proposed change would require recursive unpublished nominal references.

- Stop and reassess if The proposed change would require weakening ownership validation.

- Stop and reassess if The proposed change would require narrowing the VM route.

- Stop and reassess if The proposed change would invalidate old snapshots.

- Stop and reassess if The proposed change would consume public identities on failure.

- Stop and reassess if The proposed change would add user-depth recursion.

- Stop and reassess if The proposed change would add an arbitrary validity quota.

- Stop and reassess if The proposed change would preserve the old API through compatibility machinery.

- Stop and reassess if The concrete consumer already passes completely on the actual head.

Stopping does not mean abandoning the task.

Prefer a focused audit result with exact evidence over speculative infrastructure.

If a smaller correction can preserve the selected consumer and invariants, perform that correction.

If no implementation is justified, report that result plainly.

## 58. Contingency: Head Advanced

1. Record the new head and relevant commits.

2. Run the concrete source-free `maybe<t>` consumer.

3. Compare the implemented API with the invariants in this prompt.

4. Do not overwrite a better completed implementation merely to match the proposed shape.

5. Fix only demonstrated violations such as escaping draft handles, unstable IDs, missing atomicity, or source round trips.

6. If the entire consumer and invariant set pass, run focused verification and stop.

7. Do not select a new vertical merely because the user granted broad authority.

## 59. Contingency: Existing Generic Enum Declaration Is Partial

If a partial implementation exists, do not stack a second path beside it.

Choose one authoritative representation.

Migrate all active producers and consumers.

Delete the displaced partial path.

Preserve accepted semantic behavior, not provisional Rust API shape.

Retain old snapshot validity only within the runtime semantics of a process, not backward compatibility with serialized data that does not exist.

Add tests at the root cause.

Do not create a compatibility reader.

## 60. Contingency: Generic Function Binder Code Is Too Function-Specific

A prerequisite cleanup is allowed only if it removes duplicated binder validation directly blocking enum creation.

Extract the smallest declaration-local binder helper that owns a real shared invariant.

Keep declaration-specific policy at each caller.

Do not extract a general declaration framework.

Do not move unrelated function signature logic.

Do not generalize bound witnesses to enums.

Do not add dynamic dispatch.

Do not add a new crate.

The helper must reduce duplicated validation or make stable publication correct.

The helper must have a narrow typed interface.

The helper must have focused tests through both function and enum creation.

## 61. Contingency: `DeclarationType` Is Not the Right Reuse Boundary

The default is to reuse `DeclarationType`.

A different creation-time type is acceptable only if `DeclarationType` encodes a function-only invariant that would make enum field construction dishonest.

Prove that invariant from current code and tests.

Prefer renaming or narrowing one existing type over adding two overlapping types.

Do not preserve old call sites.

Do not expose compiler `Type`.

Do not use `SemanticType` with magic unresolved binders.

The final design must still have one creation-time binder representation and one published stable semantic type representation.

## 62. Contingency: Enum Parameter Bounds Exist in the Importer

If exact current importer semantics support enum parameter bounds, inspect the owning HIR and generic resolver before implementation.

Do not assume function bounds can simply be copied.

Select the smallest declaration and instantiation path required by the concrete consumer.

The concrete consumer itself requires no bounds.

It is acceptable to reject enum bounds in the source-free operation for this first vertical only if the operation is explicitly honest and the status/spec record the restriction.

It is not acceptable to silently drop or misrepresent bounds.

Do not broaden this vertical into trait implementation authoring.

## 63. Contingency: Phantom Parameters

Characterize the importer with a generic enum whose parameter is not used in any field.

If valid, source-free creation must publish the binder and retain exact concrete arguments for nullary and phantom variants.

If invalid, source-free creation must reject through the same rule.

Do not decide from intuition.

Do not infer the rule from generic functions.

Do not add a new semantic distinction between imported and source-free declarations.

## 64. Contingency: Performance Regression

If focused measurement shows the new path accidentally scans the whole workspace once per field or once per binder occurrence, fix the local complexity defect.

Build one local binder map.

Resolve each occurrence by lookup.

Keep the full compiler path as the oracle.

Do not introduce global incrementality.

Do not introduce persistent caches.

Do not add a service.

Retain before-and-after reproducible evidence only if the regression and correction are material.

Define a reversal condition for any retained fast path.

## 65. Contingency: Docker Verification Is Unavailable

- Run every native verification command.

- Record the exact Docker command attempted.

- Record the exact environment error.

- Distinguish missing Docker, missing permissions, network failure, disk exhaustion, and product failure.

- Do not claim Docker verification passed.

- Do not rewrite the repository to avoid an external environment limitation.

## 66. No-Question Rule

Do not ask the user whether to use one internal API shape or another.

Do not ask the user whether compatibility matters.

The user explicitly does not require backward compatibility.

Do not ask the user whether to add persistence, a daemon, or a protocol.

Those systems are out of scope without evidence.

Ask only if a genuinely external requirement is missing and no safe explicit assumption can complete the selected vertical.

Prefer a documented assumption and bounded implementation over unnecessary interruption.

## 67. Evidence Standard

- A commit subject is orientation evidence, not proof of behavior.

- Documentation is not proof of current behavior when executable evidence disagrees.

- A compiling Rust API is not proof of semantic correctness.

- A query response is not proof of transaction acceptance.

- A transaction acceptance test is not proof of direct compilation.

- A direct compiler test is not proof of VM execution.

- A VM success test is not proof of failure atomicity.

- A small-depth test is not proof of stack safety.

- A code-size reduction is not proof of performance.

- A cache hit is not proof of better architecture.

- A serializer is not proof of agent usability.

- A large diff is not proof of ambition.

- A small diff is not proof of restraint.

- Use focused executable evidence at every owned boundary.

## 68. Architectural Invariants to Recheck

- Recheck: One semantic authority.

- Recheck: Immutable published snapshots.

- Recheck: Revision-checked atomic transactions.

- Recheck: Stable opaque public identities.

- Recheck: Private dense compiler identities.

- Recheck: Explicit continuity reconciliation.

- Recheck: No name-based rebinding.

- Recheck: No source round trip after import.

- Recheck: Queryable incomplete states.

- Recheck: Structured blockers.

- Recheck: Canonical type validation.

- Recheck: Canonical generic substitution.

- Recheck: Canonical match checking.

- Recheck: Canonical ownership and cleanup.

- Recheck: Complete VM route.

- Recheck: Bounded optional native specialization.

- Recheck: No post-entry VM retry.

- Recheck: Stack-safe user-depth traversal.

- Recheck: Deterministic observable ordering.

- Recheck: No arbitrary semantic quota.

## 69. Final Report

```
Starting commit:
Ending commit:
Selected vertical:
Concrete consumer:
Characterization evidence:
Why this vertical was selected:
Representation decision:
Public API cutover:
Deleted obsolete path:
Compatibility breaks:
Created identity order:
Identity continuity:
Deletion and tombstone behavior:
Failure atomicity:
Canonical generic machinery reused:
Source-free/imported equivalence:
Compiler evidence:
Runtime evidence:
Source-loading count:
Parser count:
Focused tests:
Focused commands:
Affected crate verification:
Full native verification:
Docker verification:
Measurements:
Documentation:
Known remaining gaps:
Explicitly deferred systems:
Worktree state:
Commit:
```

Report facts.

Report exact commands.

Report exact failures.

Report exact unrun verification.

Do not reveal hidden chain-of-thought.

Do not provide a chronological transcript.

Do not claim that deferred systems are implemented.

Do not propose an unrelated next implementation merely to continue.

A concise exact handoff is sufficient when the vertical spans another turn.

## 70. End State

At the end of this task, an AI agent using the typed in-process semantic workspace should be able to declare a simple generic enum, receive stable identities for its binders and members, use that enum at an exact concrete type, construct and exhaustively match values, inspect the resulting program, compile directly, and execute it without source text.

The implementation should feel like the smallest missing continuation of the current architecture.

It should not feel like the beginning of a speculative platform rewrite.

It should leave one cleaner authority, fewer asymmetries, exact failure behavior, and stronger executable evidence.

Then stop.

## Appendix A. Transaction Scenario Matrix

| Scenario | Completeness | Publication | Tombstones |
| --- | --- | --- | --- |
| create generic enum only | incomplete only if the prior workspace was incomplete | publish enum and owned identities | none |
| create enum plus complete unrelated product | retain prior completeness state | publish both atomically | none |
| create enum plus new function | ordinary missing-body blocker | publish declaration and body hole atomically | none |
| create enum plus main | ordinary missing-body blocker | publish declaration, main parameters, and body hole atomically | none |
| create enum plus invalid field type | unchanged | nothing | all reserved identities |
| create enum plus colliding declaration | unchanged | nothing | all reserved identities |
| create enum plus unrelated rename | final state according to body blockers | publish both atomically | none |
| create enum plus deletion | final-state validated | publish only if dependency closure is valid | deleted owners |
| delete generic enum with survivor use | unchanged | nothing | none |
| delete generic enum with all dependent owners deleted | final-state validated | publish deletion and compaction | enum-owned identities and deleted owners |

## Appendix B. Review Questions Before Coding

1. What exact source importer code constructs generic enum type parameters?

2. What exact semantic entity kind represents an imported enum type parameter?

3. What owner relationship is indexed for that entity?

4. How does deletion currently discover imported enum-owned type parameters?

5. How are private HIR type-parameter strings generated for source-free generic functions?

6. Can that private naming helper be reused without importing function-bound semantics?

7. Does the importer permit phantom enum parameters?

8. Does the importer permit enum parameter bounds?

9. What field types are rejected as ownership or reference storage?

10. How does the exact generic enum constructor validate concrete arguments?

11. How does the exact generic enum pattern derive concrete field types?

12. What query already exposes imported generic enum binder entities?

13. What projection already renders imported generic enum declarations?

14. What diff already reports imported or created type-parameter entities?

15. What compaction path remaps enum field types containing parameters?

16. What test instrumentation proves no source loading and parsing?

17. What execution helper proves the ordinary VM result?

18. Which latest tests should be extended instead of duplicated?

## Appendix C. Review Questions After Coding

1. Can a draft binder escape through any public response?

2. Can a field store a stable type parameter owned by another declaration?

3. Can a failed transaction advance any generation?

4. Can private enum compaction change a survivor's public identity?

5. Can enum rename change concrete nominal type identity?

6. Can the new path invoke source parsing?

7. Can a deeply nested declaration type overflow the native stack?

8. Can a concrete ownership-bearing type argument bypass canonical restrictions?

9. Can a query advertise a constructor the transaction rejects for structural reasons?

10. Can deletion leave a dead binder in a field type or match plan?

11. Can the VM be bypassed by an unsupported native shape?

12. Can a stale continuation return results from the new revision?

13. Can output ordering depend on a hash map?

14. Did any compatibility shim remain?

15. Did any adjacent speculative subsystem appear?

## Appendix D. Minimal Search Map

- `rg -n "CreateEnum" crates/lkjscript-compiler`

- `rg -n "EnumFieldDraft" crates/lkjscript-compiler`

- `rg -n "TypeParameterDraft" crates/lkjscript-compiler`

- `rg -n "DraftTypeParameterId" crates/lkjscript-compiler`

- `rg -n "DeclarationType" crates/lkjscript-compiler/src/workspace`

- `rg -n "EnumDefinition" crates/lkjscript-compiler/src`

- `rg -n "EntityKind::TypeParameter|TypeParameter" crates/lkjscript-compiler/src/workspace`

- `rg -n "type_parameters" crates/lkjscript-compiler/src/workspace`

- `rg -n "delete.*enum|DeleteEntity" crates/lkjscript-compiler/src/workspace`

- `rg -n "source_loading|parsing" crates/lkjscript-compiler/src/workspace`

- `rg -n "generic enum" docs crates/lkjscript-compiler/src/workspace`

## Appendix E. Scope Audit

- [ ] Does this edit directly serve the `maybe<t>` consumer?

- [ ] Does this edit own an invariant required by that consumer?

- [ ] Does this edit remove an obsolete non-generic-only path?

- [ ] Does this edit duplicate a canonical validator?

- [ ] Does this edit add a new public identity domain?

- [ ] Does this edit add serialization?

- [ ] Does this edit add process state?

- [ ] Does this edit add persistence?

- [ ] Does this edit add caching?

- [ ] Does this edit add a protocol?

- [ ] Does this edit add a framework for a hypothetical second consumer?

- [ ] Can this edit be deleted without breaking the selected vertical?

For the first three questions, an affirmative answer can justify the edit.

For the remaining questions, an affirmative answer normally means narrow or delete the edit.

## Appendix F. Cost Audit

- [ ] Did the agent search before opening large files?

- [ ] Did the agent inspect recent diffs?

- [ ] Did the agent avoid rereading settled documentation?

- [ ] Did the agent use one failing test as the migration anchor?

- [ ] Did the agent batch mechanical call-site changes?

- [ ] Did the agent run focused tests before workspace tests?

- [ ] Did the agent avoid unchanged reruns?

- [ ] Did the agent capture long output once?

- [ ] Did the agent avoid redundant subagents?

- [ ] Did the agent stop exploring after evidence selected the design?

- [ ] Did the handoff name exact symbols and next commands?

- [ ] Did cost reduction preserve correctness and final verification?

## Appendix G. Documentation Truth Audit

- [ ] Does `README.md` remain concise?

- [ ] Does `status.md` state only implemented facts?

- [ ] Does `workspace.md` distinguish current behavior from target behavior?

- [ ] Does `architecture.md` describe responsibility rather than duplicate code tables?

- [ ] Does `roadmap.md` avoid inventing an unevidenced next item?

- [ ] Does `performance.md` contain only reproducible retained measurements?

- [ ] Does root `AGENTS.md` match the actual product boundary?

- [ ] Was the completed generic enum declaration gap removed everywhere it was stated as current?

- [ ] Were unsupported recursive declaration, bounds, forwarding, and ownership cases retained honestly?

## Appendix H. Final Mechanical Searches

- `rg -n "CreateGenericEnum|CreateEnumV2|legacy|deprecated|compat" crates docs README.md`

- `rg -n "render.*source|parse.*CreateEnum|import_source" crates/lkjscript-compiler/src/workspace`

- `rg -n "DraftTypeParameterId" crates/lkjscript-compiler/src/workspace`

- `rg -n "unwrap\(|expect\(|panic!|todo!|unimplemented!" crates/lkjscript-compiler/src/workspace`

- `rg -n "recursive|recursion" crates/lkjscript-compiler/src/workspace docs/status.md`

- `git diff --check`

- `git status --short`
