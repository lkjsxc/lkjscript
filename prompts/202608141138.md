# Complete Exact Source-Free Recursive Enum Declarations

## Task Metadata

Repository: `lkjsxc/lkjscript`

Reference baseline: `bf6f3e56a2235018406a413dc8ed00d87ca7a99d`

Reference baseline subject: `Align generic enum documentation and engineering policy`

Task file: `202608141138.md`

Primary language for all maintained artifacts: English

Compatibility policy: no backward compatibility is required for provisional syntax, Rust APIs, fixtures, tests, documentation, or internal representations

Execution style: autonomous, evidence-driven, multi-turn aware, and deliberately resistant to over-engineering

Primary implementation vertical: exact source-free recursive enum declarations

Preferred completion commit subject: `Complete source-free recursive enum declarations`

Remote policy: do not push or open a pull request unless the user separately requests it

Prompt policy: this task file is an instruction artifact, not repository authority, and should not be committed unless the user explicitly asks for it

Companion policy: the repository root `AGENTS.md` governs engineering procedure and must be followed

## Executive Directive

Implement one dependency-closed vertical.

Allow the in-process semantic workspace to create recursive user enum declarations without source text.

Support both non-generic direct recursion and generic direct recursion.

The minimum required non-generic case is equivalent to:

```text
enum chain {
    leaf(value: i64)
    branch(next: chain)
}
```

The minimum required generic case is equivalent to:

```text
enum tree<t> {
    leaf(value: t)
    branch(left: tree<t>, right: tree<t>)
}
```

Use a narrow creation-only representation for “the enum currently being declared.”

Resolve that representation to the staged enum’s stable published identity during `CreateEnum`.

Reuse the existing canonical enum, generic substitution, recursive memory-planning, SSA, bytecode, evaluator-oracle, and VM paths.

Do not create a second recursive-type implementation.

Do not create a general declaration graph.

Do not add mutual recursion.

Do not add draft identities for arbitrary in-flight declarations.

Do not add a source renderer.

Do not add persistence.

Do not add a daemon.

Do not add a protocol.

Do not add a cache.

Do not change native specialization merely to make this vertical appear broader.

Complete the source-free operation end to end.

Prove exact rejection behavior at the owning boundary.

Preserve transaction failure atomicity.

Preserve stable public identity.

Preserve old snapshots.

Preserve zero source loading and zero parsing after source-free construction begins.

Update maintained documentation to match the checkout.

Run the required verification boundary once after focused iteration.

Stop when this vertical is complete.

Record later gaps without implementing them.

## Why This Vertical Is Selected

The current product is local package check and run plus an in-process semantic workspace.

The workspace already creates non-generic products.

The workspace already creates generic and non-generic functions.

The workspace already creates generic and non-generic non-recursive enums.

The workspace already creates exact generic enum values.

The workspace already constructs exhaustive exact generic enum patterns.

The workspace already compiles complete source-free snapshots directly.

The workspace already executes complete source-free programs through validated bytecode and the VM.

The source importer already accepts recursive enums.

The existing compiler already analyzes recursive enum declarations.

The existing memory planner already owns recursive nominal layout.

The existing SSA and bytecode routes already handle finite values of recursive enums.

The existing VM already executes recursive enum construction and matching.

The explicit missing piece is declaration-time source-free reference to the enum being created.

`DeclarationType` already carries declaration-local type-parameter handles.

`CreateEnum` already allocates the enum and its stable binder entities before field resolution.

The current implementation deliberately has no declaration-local nominal self reference.

That omission blocks a concrete AI-authoring operation.

The omission is bounded.

The omission has an existing imported oracle.

The omission does not require a new subsystem.

The omission does not require a process boundary.

The omission does not require persistence.

The omission does not require a generalized editor.

The omission does not require source syntax changes.

The omission is therefore a strong next vertical.

## Long-Term Product Intent

`lkjscript` is AI-primary.

An agent should be able to construct programs through typed semantic operations.

An agent should not need to synthesize source text merely to express recursive nominal data.

An agent should receive stable identities after publication.

An agent should use those identities for later values, patterns, queries, renames, and deletion.

An agent should not reconstruct private compiler IDs.

An agent should not guess declaration order.

An agent should not rely on names for semantic continuity.

An agent should not invoke a language model in a correctness path.

A source-free recursive enum should have the same language meaning as its imported equivalent.

A source-free recursive enum should enter the same compiler route as its imported equivalent.

A source-free recursive enum should enter the same runtime route as its imported equivalent.

The implementation should remain small enough for weaker coding models to inspect.

The public semantic operation should be explicit enough for weaker coding models to use correctly.

The design should not anticipate unproven mutual-recursion, collaboration, persistence, or service requirements.

## Exact User or Agent Operation

The completed operation must support this lifecycle.

Start with `Workspace::empty`.

Open a revision-checked transaction.

Submit one `Edit::CreateEnum`.

Declare zero or more ordered type parameters.

Declare one or more variants.

Declare fields whose creation-time types may refer to the enum currently being created.

Publish the declaration atomically.

Receive the stable enum entity.

Receive stable type-parameter entities.

Receive stable variant entities.

Receive stable enum-field entities.

Query the published field types.

Observe the recursive constructor through the stable enum entity.

Rename the enum without changing recursive nominal identity.

Construct finite recursive values with exact stable variant and field identities.

Match finite recursive values with exact patterns.

Complete a source-free `main`.

Compile the complete snapshot through `compile_snapshot`.

Execute through the validated generic VM route.

Obtain the same defined result as the imported oracle.

Delete the recursive enum when no independent surviving dependent remains.

Retain the old snapshot and its identities.

Recreate the same display name later and receive a fresh identity generation.

## Current Consumer

The current consumer is the in-process semantic workspace API.

The current caller is an AI coding agent or deterministic host tool.

The caller needs to author recursive nominal data without writing source.

The caller already uses `Edit::CreateEnum`.

The caller already uses `DeclarationType`.

The caller already receives stable entities through `SemanticDiff`.

The caller already uses `SemanticType` after publication.

No CLI authoring command is required.

No RPC client is required.

No durable transaction journal is required.

No concurrent writer is required.

No IDE protocol is required.

No network consumer is required.

No serialization consumer is required.

## Authoritative Semantics

`docs/spec/language.md` owns language meaning.

`docs/spec/workspace.md` owns workspace transaction meaning.

The current source importer and focused recursive enum tests provide executable characterization.

The semantic workspace remains the authoring authority after construction.

The staged `SemanticProgram` remains the only mutable semantic representation.

The published `WorkspaceSnapshot` remains immutable authority.

The complete HIR remains derived.

SSA remains derived.

Bytecode remains derived.

Runtime values remain derived execution state.

The creation-only self reference is not published authority.

The creation-only self reference is not a source name.

The creation-only self reference is not a stable public identity.

The creation-only self reference is not a private compiler identity supplied by the caller.

The creation-only self reference is resolved during staging.

The published recursive type uses the enum’s ordinary stable semantic identity.

## Representation Decision

Prefer a single new creation-only `DeclarationType` variant with semantics equivalent to:

```rust
DeclarationType::CurrentEnum {
    arguments: Vec<DeclarationType>,
}
```

The exact Rust variant name may differ only when repository naming evidence clearly selects a more precise name.

Do not add a numeric draft enum identity merely for the current declaration.

There is exactly one current enum owner inside one `CreateEnum` edit.

A zero-identity current-owner marker is sufficient.

Do not add `DraftEnumId`.

Do not add `DraftDeclarationId`.

Do not add `DraftNominalId`.

Do not add a batch-local symbol table for in-flight declarations.

Do not add a generic nominal placeholder graph.

Do not add an arena solely for the self reference.

Do not add `Rc`, `Arc`, `Weak`, `RefCell`, or interior mutability to model semantic recursion.

The creation-time Rust value must remain a finite owned tree.

Semantic recursion is represented by a nominal reference.

It is not represented by a cyclic Rust object graph.

The variant should carry exact ordered type arguments.

A non-generic current enum uses an empty argument vector.

A generic current enum supplies one argument for each declared enum type parameter.

The arguments use the same `DeclarationType` tree.

The arguments may contain declaration-local type-parameter handles.

The arguments may contain stable existing semantic identities where canonical rules permit them.

The arguments must be traversed iteratively.

The variant must remain confined to declaration input.

It must disappear before publication.

## Why Not a General Draft Declaration Handle

A general draft declaration handle would imply multiple in-flight nominal declarations.

Multiple in-flight nominal declarations would imply cross-edit reference ordering.

Cross-edit reference ordering would imply batch-level name or handle scope.

Batch-level scope would imply collision, visibility, and lifecycle rules.

Mutual recursion would imply strongly connected declaration validation.

Mutual recursion would imply broader deletion and dependency semantics.

None of those behaviors has a current consumer.

None is required for direct self recursion.

Adding them now would enlarge the identity model.

Adding them now would enlarge transaction overlap.

Adding them now would enlarge testing and documentation.

Adding them now would increase agent search cost.

Adding them now would be speculative.

Use the current enum context and stop.

## Public API Semantics

The new variant is part of creation-time input.

It may be public because `DeclarationType` is public.

Its validity is context-dependent.

It is valid in an enum field type of the same `CreateEnum` edit.

It is invalid in `CreateFunction` parameter types.

It is invalid in `CreateFunction` return types.

It is invalid in `CreateMain` parameter types.

It is invalid in any future non-enum declaration unless a separate accepted vertical defines that behavior.

It is not produced by `TryFrom<&SemanticType>`.

A published `SemanticType` cannot contain the creation-only marker.

Queries cannot return the creation-only marker.

Projections cannot render the creation-only marker as semantic state.

Semantic diffs cannot contain the creation-only marker.

Continuations cannot bind the creation-only marker.

Indexes cannot retain the creation-only marker.

Serialized package or lock data cannot retain the creation-only marker.

No source spelling is assigned to the creation-only marker.

No caller-visible numeric identity is assigned to the creation-only marker.

## Explicit Context Passing

Pass the current enum context explicitly through declaration-type validation and resolution.

Do not use a global variable.

Do not use thread-local semantic context.

Do not use ambient mutable state.

Do not infer the current enum from a display name.

Do not infer the current enum from the last allocated enum.

Do not infer the current enum from vector position.

Do not infer the current enum from source order.

Do not infer the current enum from a private layout ID supplied by the caller.

The resolver context should contain only facts required by the selected operation.

Those facts normally include the staged stable enum entity.

Those facts may include the private canonical enum identity already derived by existing staging.

Those facts include the local-to-stable type-parameter mapping.

Those facts include exact declared type-parameter order.

Do not turn the context into a general transaction environment object unless existing code already has that exact responsibility.

Prefer a narrow parameter or local struct.

## Staging Order

Preserve the current atomic creation order.

Validate transaction revision first.

Validate edit shape first.

Validate enum name rules.

Validate type-parameter handle uniqueness.

Validate type-parameter name uniqueness.

Validate variant name uniqueness.

Validate field name uniqueness within each variant.

Reserve all fallible vector capacity before publication.

Use the staged allocator.

Allocate the stable enum entity before resolving self references.

Allocate stable enum type-parameter entities before resolving field types.

Allocate stable variants and fields in semantic declaration order.

Derive private nominal identities through the existing deterministic route.

Build the local type-parameter mapping.

Establish the explicit current enum context.

Resolve every field `DeclarationType` iteratively.

Convert the current enum marker to the ordinary canonical enum type.

Construct the canonical `EnumDefinition`.

Run existing canonical validation.

Run existing ownership and field restrictions.

Insert only the complete valid declaration into staged semantic state.

Build or rebuild derived indexes through the existing route.

Reconcile stable identities through the existing route.

Publish the new immutable snapshot only after every fallible step succeeds.

Do not publish a partial enum declaration.

Do not insert a temporary incomplete enum definition into the published snapshot.

Do not expose a “resolving” declaration state.

Do not add a placeholder enum to make later mutation easier.

Do not consume allocator state on failure.

## Generic Recursive Semantics

The minimum generic success case is regular recursion with the declaration’s own binders.

For `tree<t>`, `branch.left` and `branch.right` must publish as exact `tree<t>`.

The stable enum constructor must be the created enum entity.

The stable type argument must be the created type-parameter entity.

Binder spelling must not define identity.

Submitted type-parameter draft handles must resolve to their stable published entities.

The published field type must not retain `DraftTypeParameterId`.

The published field type must not retain a private binder string.

The published field type must not retain a dense compiler enum ID.

Argument order must follow declared type-parameter order.

Argument count must be exact.

Missing arguments must reject.

Extra arguments must reject.

Unknown draft binder handles must reject.

Duplicate type-parameter declarations must reject.

Foreign stable type-parameter identities must follow existing visibility and ownership rules.

Nested argument types must follow existing canonical restrictions.

Ownership- or reference-bearing arguments must follow existing canonical restrictions.

Do not add inference for current enum arguments.

Do not silently insert the enum’s own binders.

Require exact explicit arguments in the creation-time type.

Explicitness is better for deterministic AI authoring.

The source importer may infer or parse source notation as before.

The semantic edit path remains exact and syntax-independent.

## Characterize Non-Regular Recursion Before Deciding It

Do not assume that every changing recursive instantiation is valid.

Characterize the existing canonical source/compiler behavior for a shape equivalent to:

```text
enum grow<t> {
    end
    next(value: grow<list<t>>)
}
```

Use the smallest focused characterization available.

Do not add public support based only on speculation.

If the current canonical compiler rejects non-regular recursive instantiation, the source-free route must reject it through the same canonical owner.

If the current canonical compiler accepts it correctly and terminates, the source-free route should not add a stricter workspace-only rule.

If the current source format cannot express the characterization cleanly, inspect the canonical enum/type planner tests instead.

Do not build a new recursive-type theorem prover.

Do not redesign recursive layout.

Do not broaden this task into arbitrary higher-kinded or equirecursive types.

The mandatory acceptance cases remain `chain` and `tree<t>`.

A characterization result outside those cases should be documented only when it changes maintained truth.

## Existing Enum References

`DeclarationType::Enum` continues to reference already published enums.

The new current enum marker does not replace ordinary enum references.

Do not reinterpret an ordinary enum reference that happens to name the new enum.

The caller cannot know the new stable enum identity before publication.

That is why the current enum marker exists.

Existing enum references must preserve current namespace checks.

Existing enum references must preserve generation checks.

Existing enum references must preserve kind checks.

Existing enum references must preserve generic arity checks.

Existing enum references must preserve ownership and reference restrictions.

The new path must converge with the existing ordinary enum type after staging.

Downstream code should not need to distinguish how the type was authored.

## Published Type Contract

After publication, a recursive field has an ordinary `SemanticType::Enum`.

Its constructor is the stable user enum identity.

Its arguments are ordinary published `SemanticType` values.

Any declaration-local binder is replaced by its stable type-parameter entity.

The type is finite as a Rust value.

The nominal reference closes the semantic recursion.

`entity_type` must return the ordinary recursive semantic type.

Structured signature or declaration queries must return the ordinary recursive semantic type.

Projection must render the ordinary recursive semantic type.

Dependency indexing must observe the ordinary nominal dependency.

Deletion closure must understand the self dependency as owned by the declaration.

Compaction must preserve the stable constructor identity.

Rename must leave the constructor identity unchanged.

No public response should mention “current enum,” “self marker,” or a transaction-local handle after publication.

## Identity Contract

The enum declaration receives one stable `EntityId`.

Every enum type parameter receives one stable `EntityId`.

Every variant receives one stable `EntityId`.

Every field receives one stable `EntityId`.

Those identities are allocated through the existing staged allocator.

The current enum marker receives no public identity.

The current enum marker receives no generation.

The current enum marker receives no namespace.

The current enum marker receives no diff entry.

The current enum marker receives no tombstone.

Successful creation publishes the ordinary entity-created entries.

Failed creation publishes no entries.

A rename preserves the enum identity.

A rename preserves type-parameter identities.

A rename preserves variant identities.

A rename preserves field identities.

A rename preserves the recursive field constructor identity.

A rename changes presentation only.

Deletion tombstones the enum and its owned members.

Deletion of the enum must not be blocked by its own recursive field edge.

Deletion must still be blocked by independent surviving external dependents.

Old snapshots retain the original enum and recursive type.

Later same-name recreation receives a fresh identity generation.

Private compaction may relocate dense enum storage.

Private relocation must not change public identity.

Private relocation must not emit semantic movement.

## Transaction Failure Atomicity

Every invalid current enum use must fail before publication.

The current `Arc<WorkspaceSnapshot>` must remain pointer-equal when existing tests use pointer equality as the oracle.

The revision must remain unchanged.

The staged allocator must be discarded.

The next successful allocation must remain deterministic.

Diagnostics must remain unchanged.

Completeness blockers must remain unchanged.

Attachments must remain unchanged.

Provenance must remain unchanged.

Indexes must remain unchanged.

Continuations from the old revision must remain valid for that revision.

No stable entity may be consumed on failure.

No tombstone may be created on failure.

No partial enum definition may remain in staged state visible to callers.

No private enum-vector mutation may escape failure.

No global context may remain set after failure.

Allocation failure must follow the existing host-error route.

Do not add a new fault-injection framework solely for this task.

Reuse existing allocator rollback and invalid declaration tests.

Add focused failure cases that prove the new path participates in the existing atomicity model.

## Error Design

Reject invalid input at the declaration-type owning boundary.

Prefer an existing structured `WorkspaceError` variant when it expresses the required fact.

Use `UnknownDraftTypeParameter` for unknown local binders.

Use existing semantic-type or validation errors for canonical type invalidity.

Use existing arity validation where it already owns enum argument count.

Use `UnsupportedEdit` or `InvalidSemanticType` for a current enum marker outside enum declaration context when no caller requires a dedicated discriminant.

Do not add a dedicated error enum variant merely to make one assertion more specific.

Add a new structured error only when an active caller needs machine-readable facts not represented by existing variants.

Keep error text deterministic.

Do not include private compiler IDs.

Do not include allocator addresses.

Do not fabricate source paths or spans for source-free input.

Do not panic on caller-controlled type input.

Do not use `unimplemented!`, `todo!`, `unwrap`, or `expect` in production code.

Respect workspace lint policy.

## DeclarationType Shape Inventory

Adding one `DeclarationType` variant is a type-shape migration.

Search every match on `DeclarationType`.

Use `rg -n "DeclarationType::" crates/lkjscript-compiler`.

Inspect the complete match inventory before editing mechanically.

Update iterative `Clone`.

Update iterative `Drop` child extraction.

Update iterative `PartialEq`.

Update `Eq` behavior.

Update bounded or iterative `Debug`.

Update `Display` if implemented.

Update conversion from `SemanticType`.

Do not make conversion from `SemanticType` invent a current enum marker.

Update declaration-type validation traversal.

Update draft type-parameter use collection.

Update declaration-type resolution to published `SemanticType`.

Update declaration-type resolution to canonical compiler `Type` if that is a separate path.

Update test builders.

Update documentation examples only when they expose the changed API.

Run a compiler check after the representation stabilizes.

Use compiler exhaustiveness failures as a migration inventory.

Do not silence a missing match with a wildcard when the variant requires owned semantics.

Do not replace explicit closed matches with generic reflection.

Do not add a visitor framework for this one variant.

## Stack Safety

Caller-controlled declaration type depth must remain stack-safe.

The new variant’s arguments may contain arbitrarily deep type trees subject only to real host resources.

Clone must not recurse on native stack.

Drop must not recurse on native stack.

Equality must not recurse on native stack.

Debug and display must remain bounded or iterative according to existing policy.

Validation must not recurse on native stack.

Draft-binder collection must not recurse on native stack.

Resolution must not recurse on native stack.

Conversion must not recurse on native stack.

Projection of the published type must remain stack-safe through existing `SemanticType` behavior.

Do not add a semantic type-depth limit.

Do not turn a stress-test depth into a language maximum.

Add a modest 128 KiB stack test that traverses the new variant.

Follow existing deep declaration-type test style.

Add an ignored larger stress case only if the repository’s current convention and runtime cost justify it.

Do not add a new stack-growth dependency.

Do not use native recursion hidden behind a helper.

## Complexity Requirements

Validation should be linear in total declaration-type nodes plus enum members, aside from existing canonical lookups.

Resolution should be linear in total declaration-type nodes plus enum members, aside from existing canonical lookups.

Do not scan all declarations for every self reference.

Do not scan all enum parameters for every argument when one local map already exists.

Do not rebuild the whole semantic program per field.

Do not perform source rendering or parsing.

Do not hash the declaration to recover identity.

Do not add a cache.

Do not add memoization without a measured repeated subproblem.

Do not add parallelism.

Do not add a generalized dependency engine.

Do not optimize unrelated compiler phases.

A few existing linear passes over the finite declaration type are acceptable.

Correctness and simplicity are more important than collapsing every pass.

Measure only when a concrete complexity concern remains after focused implementation.

Do not add performance documentation without retained reproducible evidence.

## Canonical Compiler Integration

The workspace should produce the same canonical enum definition as the importer.

Do not maintain a workspace-only recursive enum definition.

Do not fork recursive layout logic.

Do not duplicate enum substitution.

Do not duplicate memory-plan recursion detection.

Do not duplicate ownership classification.

Do not duplicate field assignability.

Do not bypass HIR consistency validation.

Do not bypass memory planning.

Do not bypass SSA verification.

Do not bypass bytecode validation.

Do not bypass VM validation.

Use `compile_snapshot` as the source-free compilation boundary.

Incomplete snapshots must still reject before HIR derivation.

Complete snapshots must derive HIR directly.

No source load may occur.

No parser invocation may occur.

No synthetic source may be generated.

The generic VM remains the complete route.

Baseline native may decline recursive shapes before entry.

That decline is not a defect in this vertical.

Do not extend baseline native unless a focused production requirement independently proves the need.

Do not add a forced VM or forced native public option.

## Recursive Memory and Ownership

Existing recursive enum memory planning is authoritative.

Existing source recursive enum tests are the oracle.

The source-free path must feed the same nominal definition and field types into that path.

Do not introduce boxing semantics that the language does not define.

Do not require a new explicit pointer wrapper if the current canonical source language does not.

Do not flatten recursive values into a workspace-specific representation.

Do not add garbage collection.

Do not add tracing.

Do not add reference counting.

Do not add raw-pointer exposure.

Do not weaken affine ownership rules.

Do not suppress deterministic cleanup.

Do not reinterpret recursive nominal identity structurally.

Finite runtime values remain finite.

Semantic type recursion through nominal identity does not imply an infinite Rust object tree.

If source-free exposure reveals a canonical memory-plan defect, fix the canonical owner.

Add both imported and source-free evidence for that canonical fix.

Keep any such fix inside the dependency closure of recursive enum creation.

Do not broaden into a memory-model redesign.

## Values and Patterns

Declaration creation is not complete evidence by itself.

Use the published stable enum, variant, field, and binder identities.

Construct a finite non-generic recursive value.

Construct a finite generic recursive value.

Use exact type arguments.

Use exact stable field identities.

Match at least one recursive value.

Exercise payload projection through the canonical match plan.

Return a simple observable scalar result where practical.

Prefer an `i64` result for easy VM comparison.

A branch containing a leaf is sufficient.

Deep runtime recursion is not required for the primary success test.

Do not add a general recursive value builder API.

Use existing `ExpressionDraft` and `PatternDraft`.

Do not add source-shaped pattern identities.

Do not expose compiler-hidden match storage.

## Imported Oracles

Use `crates/lkjscript-app/tests/enum_execution/recursive.rs` as a primary imported behavior oracle.

The existing generic tree fixture proves finite recursive generic structure.

The existing chain fixture proves recursive matching and scalar result.

Do not rewrite those source tests into a new framework without a demonstrated maintenance benefit.

A small shared test helper is acceptable when it removes real duplication and keeps ownership clear.

Do not move broad test suites merely for symmetry.

Compare imported and source-free results at stable semantic and observable boundaries.

Do not compare workspace namespaces.

Do not compare intentionally private dense IDs.

Do compare normalized declaration shape where useful.

Do compare exact published semantic field types.

Do compare runtime result.

Do compare cleanup behavior when the existing oracle exposes it.

Do assert zero parser and source-load work for the source-free side.

## Query Contract

After creation, entity listing must include the enum.

Entity listing must include its type parameters.

Entity listing must include its variants.

Entity listing must include its fields.

Ownership links must be correct.

The enum owns its type parameters.

The enum owns its variants.

Each variant owns its fields.

The recursive field type must name the stable enum constructor.

The generic recursive field type must name stable binder entities in its arguments.

No draft handle may appear.

No private enum ID may appear.

Deterministic ordering must remain unchanged.

Pagination behavior must remain revision-bound.

No new query endpoint is required.

Do not add a “resolve current enum” query.

Do not add a transaction-local query service.

Use existing entity type, declaration, dependency, and projection views.

## Projection Contract

Projection is presentation.

Projection is not identity.

Projection should render the published recursive enum through ordinary nominal type rendering.

The current enum marker must not have a projection syntax.

Renaming the enum may change displayed spelling.

Renaming must not change semantic constructor identity.

Projection order must remain deterministic.

Source-free projection must not fabricate source spans.

Source-free projection must not fabricate source paths.

Source-free projection must not fabricate source hashes.

Do not add a special recursive-type digest.

Do not add a cycle-breaking public token.

Existing bounded or iterative projection behavior remains authoritative.

## Semantic Diff Contract

A successful creation reports ordinary entity creation.

The diff includes the enum entity.

The diff includes stable type-parameter entities.

The diff includes variants.

The diff includes fields.

The diff does not include the current enum marker.

The diff does not include private layout IDs.

The diff does not report self resolution as a public rewire unless existing accepted semantics already classify declaration type publication that way.

Do not invent diff noise for private staging.

A rename reports the ordinary rename.

A rename does not report delete and create.

A rename does not report recursive field rewiring.

A deletion reports ordinary owned entity deletion.

Private compaction does not report movement.

Old and new revisions remain exact.

## Deletion Contract

A recursive self edge is owned within the enum declaration.

Deleting an otherwise unreferenced recursive enum must succeed.

The enum’s own self field must not be treated as an independent surviving dependent.

Owned type parameters, variants, and fields cascade through existing deletion semantics.

Any target implementation owned by current enum deletion follows existing rules.

A surviving function signature that uses the recursive enum must block deletion.

A surviving product field that uses the recursive enum must block deletion.

A surviving other enum field that uses the recursive enum must block deletion.

A surviving expression, hole, generic substitution, witness, or match dependency must block deletion according to existing rules.

Deleting every independent dependent in the same batch may succeed according to existing final-state closure rules.

Do not add a recursive-deletion special case that bypasses dependency validation.

Fix dependency ownership classification at its canonical owner if the self edge is misclassified.

Test old snapshot validity after deletion.

Test later same-name recreation if the focused fixture can do so without excessive duplication.

## Rename Contract

Rename the recursive enum by stable entity identity.

Preserve all owned member identities.

Preserve all recursive field types.

Preserve all generic arguments.

Preserve canonical enum definition identity.

Preserve runtime behavior.

Do not rewrite types by spelling.

Do not reconstruct the declaration.

Do not emit descendant creation or deletion.

Do not infer continuity from the old and new names.

Use existing rename machinery.

Add only the focused assertion needed to prove recursive self references follow identity rather than spelling.

## Source Boundary Contract

Source import remains fail-fast according to current behavior.

Do not modify the parser merely to support source-free creation.

Do not modify source syntax.

Do not add a `self` keyword to source.

Do not add a source token for the creation-only marker.

Do not add source rendering.

Do not add round-trip source tests.

The imported oracle remains valid.

The source-free path begins from `Workspace::empty`.

After construction begins, parser invocation count remains zero.

After construction begins, source-load invocation count remains zero.

`compile_snapshot` must not load or parse source.

Documentation must continue to describe source as an importer and review format.

## Compatibility and Cutover

Backward compatibility for the provisional Rust creation API is not required.

Adding a non-exhaustive enum variant is acceptable.

Changing internal helper signatures is acceptable.

Renaming a provisional helper is acceptable when the new name is materially clearer.

Do not retain a legacy recursive workaround.

Do not add an alias for two self-reference representations.

Do not add a feature flag.

Do not add an edition.

Do not add a `v2` transaction type.

Do not add a compatibility adapter.

Do not add a migration for nonexistent durable workspace data.

Update all active producers and consumers directly.

Delete displaced provisional code.

Keep unrelated public APIs stable when the vertical does not require changing them.

## Explicit Non-Goals

Do not implement mutually recursive enums.

Do not implement enum-to-product mutual recursion.

Do not implement product-to-product recursion through new creation handles.

Do not implement generic products.

Do not implement type aliases.

Do not implement recursive functions as part of this task; they already have their own path.

Do not implement traits.

Do not implement trait creation.

Do not implement implementation creation.

Do not implement direct implementation deletion.

Do not implement enum member insertion.

Do not implement enum member deletion.

Do not implement enum member reordering.

Do not implement cross-declaration movement.

Do not implement a generalized declaration editor.

Do not implement unresolved type names.

Do not implement parser recovery.

Do not implement ambiguous type choices.

Do not implement merge conflicts.

Do not implement collaboration.

Do not implement persistence.

Do not implement crash recovery.

Do not implement a transaction log.

Do not implement a binary workspace format.

Do not implement a semantic wire protocol.

Do not implement RPC.

Do not implement a daemon.

Do not implement a warm semantic service.

Do not implement a scheduler.

Do not implement a plugin system.

Do not implement a general cache.

Do not implement generalized incremental recomputation.

Do not implement parallel compilation.

Do not implement a new JIT tier.

Do not broaden baseline native recursive support.

Do not implement a source renderer.

Do not implement a formatter.

Do not redesign package identity.

Do not redesign lock files.

Do not redesign capabilities.

Do not redesign ownership.

Do not redesign the memory model.

Do not add garbage collection.

Do not add reference counting.

Do not add arbitrary semantic quotas.

Do not reorganize the repository broadly.

Do not split large files solely because they are large.

Do not add a new crate.

Do not add a dependency unless a concrete unavoidable risk proves it necessary.

No new dependency is expected.

## Orientation Procedure

Record the actual starting commit.

Record the current branch.

Record `git status --short`.

Do not assume the worktree is clean.

Preserve unrelated changes.

Do not reset to the reference baseline.

If HEAD differs from the reference baseline, inspect relevant intervening commits.

Reconcile this task with current accepted semantics.

If the vertical is already complete, verify it and stop.

Inspect the companion `AGENTS.md`.

Read only the relevant sections of `docs/spec/language.md`.

Read only the relevant sections of `docs/spec/workspace.md`.

Read the current recursive and generic enum sections of `docs/status.md`.

Read the current enum creation section of `docs/architecture.md`.

Read `docs/roadmap.md`.

Inspect recent commits:

```text
bf6f3e56 Align generic enum documentation and engineering policy
cce0da11 Finish source-free generic enum validation
abe64ef8 Complete source-free generic enum declarations
efcf802f Complete exact generic enum authoring
```

Use recent diffs to avoid rediscovering settled generic enum decisions.

Do not read every historical prompt.

Do not dump the entire huge workspace test file.

Search exact symbols first.

## Initial Search Set

Start with narrow searches equivalent to:

```bash
rg -n "enum DeclarationType|DeclarationType::" crates/lkjscript-compiler/src/workspace
rg -n "CreateEnum|fn create_enum" crates/lkjscript-compiler/src/workspace
rg -n "EnumTypeParameterDraft|EnumFieldDraft|EnumVariantDraft" crates/lkjscript-compiler
rg -n "UnknownDraftTypeParameter|InvalidSemanticType|UnsupportedEdit" crates/lkjscript-compiler/src/workspace
rg -n "recursive" crates/lkjscript-compiler crates/lkjscript-ir crates/lkjscript-app/tests
rg -n "generic_enum|generic enum" crates/lkjscript-compiler/src/workspace/tests.rs
rg -n "parser_invocation_count|source_load_invocation_count" crates/lkjscript-compiler
rg -n "SemanticType::Enum|EnumDefinition" crates/lkjscript-compiler/src/workspace
```

Inspect narrow line ranges around matches.

Inspect `crates/lkjscript-compiler/src/workspace/draft.rs`.

Inspect the `CreateEnum` staging path in `transaction.rs`.

Inspect declaration-type validation and resolution helpers.

Inspect `WorkspaceError`.

Inspect current generic enum creation tests.

Inspect `crates/lkjscript-app/tests/enum_execution/recursive.rs`.

Inspect recursive enum memory-plan tests only as needed.

Do not inspect unrelated native or host files unless a focused failure points there.

## Private Task Ledger

Keep a compact ignored ledger with these fields.

Operation: source-free recursive enum creation.

Consumer: in-process semantic workspace caller.

Gap: no creation-local current enum type.

Authority: workspace spec plus canonical language/compiler path.

Authoritative representation: staged semantic program and published snapshot.

Created identities: enum, binders, variants, fields.

Non-identity input: current enum marker.

Atomicity: staged allocator and one-Arc publication.

Success oracle: imported recursive enum plus source-free VM result.

Rejection oracle: invalid context, arity, binder, and canonical type cases.

Stack-safety question: all new declaration-type traversals remain iterative.

Complexity question: no scan per self occurrence.

Non-goals: general declaration graph and mutual recursion.

Stop condition: both required recursive families compile and run source-free with full verification.

Do not commit the ledger.

## Characterization Phase

Run the smallest existing recursive enum tests first.

Confirm the imported non-generic chain compiles and executes.

Confirm the imported generic tree compiles and executes.

Confirm current source-free non-recursive generic enum tests pass.

Add a failing source-free characterization test before implementation when practical.

The failing test should attempt a direct current enum reference through the intended API shape.

If the public variant does not yet exist, a compile-fail test is not required.

A focused temporary branch or local edit may establish the failure.

Do not build a new test harness.

Characterize non-regular recursive generic behavior only far enough to select parity.

Record the result in code tests if it defines accepted behavior.

Otherwise keep the result in the task report.

Do not retain speculative test cases that test no accepted contract.

## Preferred Implementation Shape

Extend `DeclarationType` with the narrow current enum variant.

Update its manual iterative operations.

Add an explicit optional current enum context to declaration-type validation and resolution.

Use no current enum context for function and main creation.

Use the staged enum context for enum field resolution.

Resolve current enum arguments through the same local binder mapping as ordinary field types.

Construct the ordinary published enum semantic type.

Construct the ordinary canonical compiler enum type.

Let existing canonical enum validation decide recursive layout legality.

Keep the context local to `create_enum` and its helpers.

Do not store the context in `Workspace`.

Do not store the context in `WorkspaceSnapshot`.

Do not store the context in `SemanticProgram`.

Do not store the context in `EnumDefinition`.

Do not store the current marker in indexes.

Do not store the current marker in diagnostics except as a deterministic input-position description on failure.

## Alternative Allowed Only With Evidence

A separate enum-field-only creation type may appear attractive.

Reject that alternative by default because it duplicates the full `DeclarationType` tree.

A draft enum identity may appear future-proof.

Reject that alternative by default because there is only one current declaration.

A name-based self reference may appear simple.

Reject it because names are presentation and collision-prone.

Pre-inserting a partial enum definition may appear convenient.

Reject it because it creates a second staged lifecycle and risks partial authority.

Building a cyclic Rust type graph may appear semantically direct.

Reject it because nominal identity already closes recursion safely.

A generalized SCC planner may appear complete.

Reject it because mutual recursion is out of scope.

Choose a different representation only if concrete current code makes the preferred shape materially less correct or less simple.

Document that evidence in the final report.

Do not spend a turn generating multiple architecture options after the evidence is clear.

## Detailed Implementation Phase A: Type Input

Add the current enum variant.

Document that it is valid only inside the owning enum creation edit.

Document that it is transaction-local semantic intent.

Document that it never enters a snapshot.

Make `Clone` reconstruct its argument vector iteratively.

Make `Drop` drain its argument vector iteratively.

Make `PartialEq` compare argument count and children iteratively.

Make `Debug` distinguish it clearly without leaking private state.

Keep debug bounded according to existing conventions.

Update any `Display` implementation consistently.

Do not assign it a source spelling.

Ensure conversion from published `SemanticType` always produces ordinary `Enum`, never current enum.

Ensure ordinary `Enum` behavior is unchanged.

Ensure deep nested current enum arguments do not recurse on native stack.

Run a compiler check after exhaustive match updates.

## Detailed Implementation Phase B: Validation

Inventory the existing declaration-type validator.

Pass an explicit validation context.

The context distinguishes enum field creation from function or main declaration creation.

When no current enum is available, reject the marker deterministically.

When a current enum is available, traverse all arguments.

Collect draft binder use from current enum arguments.

Validate unknown binders through the existing error.

Validate existing stable identities through the existing type path.

Validate generic argument count against the current enum declaration.

Do not validate by looking up the partially inserted enum in published indexes.

Use the declared type-parameter list already in staging.

Keep traversal iterative.

Reserve work capacity fallibly.

Propagate host allocation errors.

Do not panic on child-count overflow.

Use checked arithmetic.

Do not silently ignore arguments.

## Detailed Implementation Phase C: Resolution

Inventory conversion from `DeclarationType` to published `SemanticType`.

Inventory conversion from `DeclarationType` to compiler `Type`.

Avoid two divergent resolution laws.

If one path already produces both views from one canonical result, extend that path.

When resolving current enum, use the staged stable enum entity as the semantic constructor.

Use the existing private enum identity as the compiler constructor.

Resolve arguments in declaration order.

Resolve draft binder handles through the existing local mapping.

Reject a missing mapping.

Do not infer by binder name.

Do not perform a program-wide name lookup.

Do not create a second enum definition.

Do not recursively expand the enum fields.

The resolved type is one nominal constructor plus finite arguments.

Keep resolution iterative.

Preserve exact host-allocation errors.

## Detailed Implementation Phase D: Enum Creation

Inspect existing `create_enum`.

Retain its stable allocation order unless correctness requires a direct change.

Allocate the enum before field resolution.

Allocate type parameters before field resolution.

Retain phantom type-parameter support.

Retain unbounded parameter count subject to host resources.

Retain exact declaration order.

Retain stable member identities.

Retain deterministic private identity derivation.

Retain current canonical origin.

Retain current canonical layout derivation.

Pass current enum context only while resolving this declaration’s fields.

Do not make current enum available to sibling `CreateEnum` edits.

Do not make current enum available to a `CreateFunction` in the same transaction.

Do not make one newly created enum reference another newly created enum.

Do not change edit-order semantics.

Do not add an overlap planner.

After resolution, continue through ordinary `EnumDefinition` construction.

Run canonical consistency before publication.

## Detailed Implementation Phase E: Indexes and Dependencies

Build indexes through the current snapshot path.

Verify the recursive field creates the ordinary nominal dependency facts.

Inspect whether self dependencies are represented.

Do not add duplicate dependency edges.

Do not add an explicit cycle object unless existing indexing requires it.

Ensure deterministic ordering.

Ensure query code handles the ordinary published type without special cases.

Ensure compaction remaps private enum references as it already does.

Ensure deletion recognizes the self reference as owned containment.

Modify index or deletion code only if a focused recursive test proves a defect.

Do not preemptively redesign dependency representation.

## Detailed Implementation Phase F: Values, Match, and Execution

Create a non-generic recursive enum source-free.

Create `main` with an `i64` result.

Build a finite `branch(leaf(41))` or equivalent value.

Match the value.

Return `42` or another exact oracle result.

Compile with `compile_snapshot`.

Run validated bytecode in the VM.

Create a generic recursive enum source-free.

Instantiate it with `i64`.

Build a finite branch with leaf payloads.

Match or otherwise inspect it through supported exact patterns.

Return an exact scalar or compare structural object count through existing test oracles.

Compile and run.

Compare with imported fixtures.

Reset parser and source-load counters before source-free construction.

Assert both remain zero after compilation.

Do not depend on baseline native acceptance.

Do not add runtime engine selection.

## Focused Success Test Matrix

Cover the following semantics with the smallest coherent set of tests.

Prefer table-driven or helper-backed tests where that reduces duplication.

Do not create one test per bullet when one scenario proves several bullets.

### Non-Generic Declaration

Create `chain`.

Create variant `leaf`.

Create field `leaf.value: i64`.

Create variant `branch`.

Create field `branch.next: CurrentEnum<[]>`.

Publish atomically.

Assert program remains incomplete only for expected missing entry or body blockers.

Find the stable enum entity through the diff.

Find the stable variants through the diff.

Find the stable fields through the diff.

Query `branch.next`.

Assert its type is the ordinary stable `SemanticType::Enum`.

Assert its constructor is the created enum entity.

Assert its argument list is empty.

Assert no parser invocation occurred.

Assert no source load occurred.

### Generic Declaration

Create `tree<t>`.

Use one `DraftTypeParameterId`.

Create `leaf.value: t`.

Create `branch.left: CurrentEnum<t>`.

Create `branch.right: CurrentEnum<t>`.

Publish atomically.

Find the stable enum entity.

Find stable binder `t`.

Find stable variants.

Find stable fields.

Query `leaf.value`.

Assert it is `SemanticType::TypeParameter(stable_t)`.

Query `branch.left`.

Assert it is `SemanticType::Enum { constructor: created_tree, arguments: [TypeParameter(stable_t)] }`.

Query `branch.right`.

Assert the same exact type.

Assert the binder owner is the enum.

Assert declaration order is preserved.

Assert no draft binder escapes.

Assert no parser invocation occurred.

Assert no source load occurred.

### Source-Free Non-Generic Execution

Create a complete main.

Construct a finite chain value.

Use exact stable variant identity.

Use exact stable field identity.

Match leaf and branch cases exhaustively.

Return the expected `i64`.

Compile through `compile_snapshot`.

Run through VM.

Assert exact result.

Compare to the imported chain oracle.

### Source-Free Generic Execution

Instantiate `tree<i64>`.

Construct exact finite values.

Use stable type-parameter-keyed type arguments in value construction as required by existing APIs.

Use exact stable variant and field identities.

Match exact generic enum variants.

Propagate concrete substituted payload types.

Compile through `compile_snapshot`.

Run through VM.

Assert exact result or exact structural oracle.

Compare to the imported generic tree oracle.

### Source-Free Boundary

Reset source-load counter.

Reset parser counter.

Construct the program.

Query it.

Project it.

Compile it.

Execute it.

Assert source-load count remains zero.

Assert parser count remains zero.

Do not accept a test that checks only construction and not compilation.

### Published View

Query entity type for every recursive field.

Inspect generic signature or enum declaration view when available.

Project the enum and fields.

Assert stable semantic identity is visible.

Assert transaction-local marker text is absent.

Assert private IDs are absent.

Assert deterministic order.

### Rename

Rename the recursive enum.

Assert enum `EntityId` is unchanged.

Assert binder IDs are unchanged.

Assert variant IDs are unchanged.

Assert field IDs are unchanged.

Assert recursive field constructor remains the same stable enum ID.

Assert projection uses the new display name where expected.

Assert no reference or call rewiring is fabricated.

Assert runtime behavior remains unchanged if the fixture stays complete.

### Delete

Create an otherwise unreferenced recursive enum in an incomplete workspace.

Delete it.

Assert deletion succeeds despite the self field.

Assert enum and owned members tombstone.

Assert old snapshot still exposes the declaration.

Assert current snapshot does not.

Optionally recreate the same name in a later transaction.

Assert the new enum identity differs.

Assert the old identity remains stale in the new snapshot.

### External Dependency

Create a recursive enum.

Create or retain an independent function or main signature that uses it.

Attempt to delete only the enum.

Assert deterministic dependency rejection.

Remove or delete the independent dependent in the same batch where existing semantics allow it.

Assert dependency-closed deletion succeeds.

Do not weaken deletion closure to pass the self-edge test.

## Focused Rejection Test Matrix

Consolidate these cases into a table-driven atomicity test where practical.

For every case:

- capture the current `Arc`;
- capture the revision;
- capture diagnostics;
- capture blockers;
- capture allocator behavior through the existing retry-stability oracle;
- apply the invalid transaction;
- assert deterministic error;
- assert no publication;
- assert the next valid transaction receives expected identities.

### Invalid Context

Use current enum in a `CreateFunction` parameter.

Reject.

Use current enum in a `CreateFunction` return type.

Reject.

Use current enum in a `CreateMain` parameter.

Reject at the earliest owning rule.

Do not let main capability validation obscure a leaked marker if a direct context check is clearer.

Do not add current enum support to functions.

### Non-Generic Arity

Declare a non-generic enum.

Use current enum with one type argument.

Reject exact arity mismatch.

Do not ignore the argument.

Do not silently erase it.

### Generic Missing Argument

Declare one enum type parameter.

Use current enum with zero arguments.

Reject exact arity mismatch.

### Generic Extra Argument

Declare one enum type parameter.

Use current enum with two arguments.

Reject exact arity mismatch.

### Unknown Draft Binder

Declare binder `t`.

Use a different undeclared `DraftTypeParameterId` in current enum arguments.

Reject with `UnknownDraftTypeParameter`.

Do not fall back to a name.

### Duplicate Binder Declaration

Declare the same draft binder handle twice.

Preserve existing duplicate-handle rejection.

Current enum arguments must not mask the error.

### Duplicate Binder Name

Declare distinct handles with the same name.

Preserve existing duplicate-name rejection.

### Foreign Stable Identity

Nest a product, enum, or type-parameter identity from another workspace inside current enum arguments where the model permits such a case.

Reject at namespace validation.

Do not dereference it first.

### Stale Stable Identity

Use a tombstoned stable identity inside a current enum argument where applicable.

Reject as stale.

### Wrong Kind

Use a stable entity of the wrong kind where a type identity is expected.

Preserve existing kind rejection.

### Ownership or Reference Restriction

Construct a recursive field shape that the canonical enum field rules reject.

Assert the source-free path rejects through the same canonical rule.

Do not add a workspace-only exemption for self recursion.

### Non-Regular Recursion

If characterization proves the canonical compiler rejects changing recursive instantiations, add one exact source-free rejection case.

Assert it rejects without publication.

If the canonical compiler accepts the case, do not add a workspace-only rejection.

### Allocation and Overflow

Do not add enormous allocations to ordinary tests.

Use existing checked child-count and allocation paths.

Ensure new checked additions cannot overflow silently.

Use existing host error propagation.

Do not add a global fault injector.

## Stack-Safety Test Matrix

Add one modest test on a 128 KiB thread stack.

Build a deeply nested type whose deepest nominal leaf is the current enum marker.

For example, wrap `CurrentEnum<t>` in repeated `List`.

Use a depth consistent with existing modest declaration-type tests.

Create the enum.

Query the published field type.

Assert the same nesting depth.

Drop the draft and snapshot on the small-stack thread.

The test must exercise clone or transaction staging.

The test must exercise validation.

The test must exercise resolution.

The test must exercise publication.

The test must exercise projection or equality when practical.

Do not use recursive test helper construction if the helper itself would overflow.

Construct the nested type iteratively.

If existing ignored stress conventions cover 20,000 levels, add an analogous ignored case only when low maintenance.

State the exact command for any ignored stress case.

Do not treat the tested depth as a semantic maximum.

## Regression Matrix

Run current non-recursive generic enum declaration tests.

Run current generic enum value tests.

Run current exact generic enum match tests.

Run current source-free parser-counter tests.

Run current enum deletion and compaction tests.

Run current nominal rename tests.

Run current deep `DeclarationType` tests.

Run imported recursive enum app tests.

Run compiler workspace tests.

Do not change expected behavior outside the selected vertical.

Do not weaken an existing rejection to make recursion pass.

Do not delete unrelated stress coverage.

## Test Organization

The workspace test file is already large.

Do not split it merely because of size.

Add focused helpers near related generic enum creation tests.

Reuse existing entity lookup helpers.

Reuse existing source-free compile and run helpers.

Reuse existing parser and source-load counters.

Reuse existing imported fixtures where ownership is clear.

A separate `recursive_enum_tests.rs` module is acceptable only if it establishes a stable responsibility and reduces search fan-out.

Do not create a broad new test framework.

Do not introduce macros solely to save a few lines.

Prefer explicit semantic assertions.

Avoid brittle full projection string snapshots when structured queries own the fact.

Use projection assertions only for presentation facts.

## Documentation Updates

Update `README.md`.

Remove the claim that source-free enum creation is non-recursive.

Describe recursive enum construction concisely.

Do not turn README into a full recursive-type tutorial.

Update `docs/status.md`.

State that source-free enum creation supports canonical recursive generic and non-generic declarations.

State the exact creation-only self-reference behavior.

Remove the explicit recursive enum declaration gap.

Retain broader gaps.

Do not claim mutual recursion.

Do not claim generic products.

Update `docs/architecture.md`.

Describe explicit current enum context during enum field resolution.

State that the marker does not escape staging.

State that published field types use stable enum identity.

State that canonical recursive memory planning remains shared with imported programs.

Remove the statement that the operation deliberately has no local nominal self handle.

Do not add a new architecture layer.

Update `docs/spec/workspace.md`.

Move recursive enum creation from gap to current implementation.

Specify the current enum marker’s lifetime and exact resolution.

Specify context rejection outside `CreateEnum`.

Specify atomic publication.

Specify generic argument exactness.

Keep the broader target contract unchanged.

Update `docs/roadmap.md`.

After completion, leave `Now` empty unless another concrete vertical is independently selected by current evidence.

State that source-free construction now covers recursive generic and non-generic enum declarations, exact values, and exhaustive patterns.

Do not implement a next item merely to populate the roadmap.

Do not change `docs/spec/language.md` unless characterization exposes a real normative contradiction.

Recursive nominal enum semantics already belong to the language contract.

Update `docs/performance.md` only if retained reproducible measurements are added.

No performance-document change is expected for an ordinary linear type-shape extension.

Do not churn `AGENTS.md` during implementation unless the installed companion policy has a concrete contradiction.

Do not commit this prompt.

## Documentation Precision

Distinguish current implementation from target behavior.

Do not describe planned mutual recursion.

Do not imply source text is semantic authority.

Do not imply the creation-only marker is a language type.

Do not imply a caller can query the marker after publication.

Do not imply generic arguments are inferred.

Do not imply native execution supports every recursive shape.

Do not imply performance was measured when it was not.

Do not copy large test matrices into status documentation.

Use exact names of public APIs that remain after implementation.

Delete stale “non-recursive” and “no self handle” claims in the same change.

## Performance Discipline

This task is not primarily a performance optimization.

Protect asymptotic behavior.

Do not introduce repeated declaration scans.

Do not introduce recursive native-stack traversals.

Do not introduce source round trips.

Do not introduce a generalized cache.

Do not introduce an incremental engine.

Do not benchmark unrelated compile phases.

Use focused work counters only if a suspected superlinear path appears.

If a regression appears, isolate the production phase.

Use the full path as the correctness oracle.

Record a reversal condition for any optimization retained.

Prefer the simple linear explicit-work-stack implementation.

## API-Cost Discipline

Search before reading.

Use exact symbols.

Use narrow line ranges.

Use recent diffs.

Do not reread the full root policy repeatedly.

Do not dump all of `workspace/tests.rs`.

Do not dump all of `transaction.rs`.

Capture long compiler output once.

Inspect the first causal error.

Use compiler exhaustiveness diagnostics as a migration list.

Batch mechanical match updates.

Run one focused test after each semantic milestone.

Do not rerun unchanged full workspace commands.

Do not ask the user to choose internal names.

Do not generate multiple competing designs after the narrow representation is supported by evidence.

Do not delegate overlapping work to multiple subagents.

Use a subagent only for one independent bounded characterization question.

Give any subagent exact paths, one question, required evidence, and a stop condition.

Do not add token counters.

Do not add API-cost telemetry.

Do not add agent registries.

Save cost by reducing rediscovery and rework.

Never save cost by skipping correctness or final verification.

## Suggested Iteration Order

First, orient and record the baseline.

Second, run imported recursive enum tests.

Third, locate all `DeclarationType` matches.

Fourth, add the narrow current enum variant.

Fifth, update iterative type operations.

Sixth, run `cargo check -p lkjscript-compiler --all-features --locked`.

Seventh, add explicit validation context.

Eighth, add enum-field resolution.

Ninth, add the minimal non-generic creation test.

Tenth, add the generic creation test.

Eleventh, run focused workspace tests.

Twelfth, add source-free value and match execution.

Thirteenth, add imported/source-free parity.

Fourteenth, add invalid-context and arity atomicity cases.

Fifteenth, add rename and deletion identity cases.

Sixteenth, add the small-stack test.

Seventeenth, update documentation.

Eighteenth, format.

Nineteenth, run affected crate tests.

Twentieth, run full required verification once.

Twenty-first, inspect the final diff.

Twenty-second, commit one cohesive change if permitted.

Do not treat this order as a reason to publish intermediate incomplete states.

## Focused Commands

Determine exact test names after implementation.

Use narrow commands similar to:

```bash
cargo check -p lkjscript-compiler --all-features --locked
cargo test -p lkjscript-compiler workspace::tests::source_free_recursive --all-features --locked
cargo test -p lkjscript-compiler workspace::tests::recursive_enum --all-features --locked
cargo test -p lkjscript-app --test enum_execution recursive --all-features --locked
```

Do not invent a test filter that runs nothing.

Confirm the number of tests executed.

Run the affected crate boundary after focused tests:

```bash
cargo test --quiet -p lkjscript-compiler --all-targets --all-features --locked
cargo test --quiet -p lkjscript-app --all-targets --all-features --locked
```

Use `cargo fmt --all` only after representation stabilizes.

Fix Clippy warnings immediately.

## Required Final Verification

Run exactly:

```bash
cargo fmt --all -- --check
cargo clippy --quiet --workspace --all-targets --all-features --locked -- -D warnings
cargo test --quiet --workspace --all-targets --all-features --locked
cargo build --quiet --workspace --release --locked
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
git diff --check
```

Do not omit `--locked`.

Do not omit `--all-targets`.

Do not omit `--all-features`.

Do not omit the release build.

Run Docker verification when the environment provides Docker.

If Docker is unavailable, report the exact environment failure.

Do not claim an unavailable command passed.

Do not substitute a narrower command for the required full boundary.

Do not rerun the full boundary without a code or configuration change.

Capture failure output once.

Fix the causal region.

Rerun the affected command.

## Git Procedure

Record `git status --short` before editing.

Preserve unrelated changes.

Do not stage unrelated files.

Do not commit the prompt.

Do not commit raw logs.

Do not commit build artifacts.

Do not commit caches.

Do not commit temporary characterization files.

Inspect `git diff --stat`.

Inspect substantive diffs.

Inspect `git diff --check`.

Inspect documentation claims.

Confirm generated locks changed only when required.

One cohesive verified commit is preferred.

Use the subject `Complete source-free recursive enum declarations` unless the final semantic result is materially different.

Do not push.

Do not force-push.

Do not open a pull request.

Report the ending commit.

Report final worktree state.

## Multi-Turn Rules

This task is intentionally multi-turn aware.

Do not attempt unrelated future platform work in this turn.

A turn may end after a complete verified vertical.

A turn may end after focused characterization proves the selected representation incompatible with accepted semantics.

If characterization blocks implementation, stop with a precise handoff.

Do not fill the gap with speculative infrastructure.

Do not leave the public `DeclarationType` migration half-complete.

Do not leave two self-reference representations.

Do not leave a disabled compatibility path.

Do not leave a partially inserted enum lifecycle.

Do not leave failing focused tests without an exact causal handoff.

A handoff must name the actual commit.

A handoff must name modified files.

A handoff must name exact symbols.

A handoff must name passing tests.

A handoff must name failing tests.

A handoff must state whether any public API is half-migrated.

A handoff must state the next smallest evidence gate.

A handoff should be concise enough that the next agent avoids rereading large files.

Durable progress belongs in code, tests, specs, status, architecture, roadmap, and retained measurements.

Durable progress does not belong in a copied transcript.

## Stop Conditions

Stop implementation when both required recursive declaration families work source-free.

Stop when invalid context and arity reject atomically.

Stop when published types use stable identities.

Stop when rename preserves recursive identity.

Stop when deletion handles owned self dependencies correctly.

Stop when imported/source-free behavior converges.

Stop when zero parser and source-load work is proved.

Stop when stack-safe traversal is proved.

Stop when documentation matches the checkout.

Stop when required verification passes or exact environment failures are reported.

Do not continue into mutual recursion.

Do not continue into product recursion.

Do not continue into persistence.

Do not continue into a daemon.

Do not continue into a protocol.

Do not continue into incremental compilation.

Do not continue into native recursive specialization.

Do not continue into broad refactoring.

Record the next evidenced gap.

Leave roadmap `Now` empty unless evidence independently selects a new vertical.

## Completion Checklist

The task is complete only if every applicable item below is true.

### Operation

- [ ] `CreateEnum` can express direct self recursion.
- [ ] Non-generic recursion works.
- [ ] Generic recursion works.
- [ ] Exact arguments are explicit.
- [ ] No source text is required.
- [ ] Finite values can be constructed.
- [ ] Exact patterns can match them.
- [ ] Complete snapshots compile.
- [ ] VM execution returns the expected result.

### Representation

- [ ] The representation is creation-only.
- [ ] The representation is not name-based.
- [ ] The representation is not an ID-bearing general draft declaration.
- [ ] The representation is finite.
- [ ] The representation does not create a cyclic Rust object graph.
- [ ] The representation disappears before publication.
- [ ] Published types are ordinary `SemanticType`.
- [ ] Canonical compiler types are ordinary enum types.

### Identity

- [ ] Stable enum identity is published.
- [ ] Stable binder identities are published.
- [ ] Stable variant identities are published.
- [ ] Stable field identities are published.
- [ ] The current enum marker has no identity.
- [ ] Rename preserves all semantic identities.
- [ ] Deletion tombstones owned identities.
- [ ] Old snapshots remain valid.
- [ ] Same-name recreation receives a fresh identity.

### Atomicity

- [ ] Invalid context publishes nothing.
- [ ] Wrong arity publishes nothing.
- [ ] Unknown binder publishes nothing.
- [ ] Canonical type failure publishes nothing.
- [ ] Revision remains unchanged on failure.
- [ ] Allocator state remains unchanged on failure.
- [ ] Diagnostics remain unchanged on failure.
- [ ] Blockers remain unchanged on failure.
- [ ] Future identity allocation remains deterministic.

### Generic Semantics

- [ ] Binder order is authoritative.
- [ ] Binder spelling is presentation only.
- [ ] Draft binder handles do not escape.
- [ ] Missing self arguments reject.
- [ ] Extra self arguments reject.
- [ ] Existing generic enum behavior is unchanged.
- [ ] Canonical substitution is reused.
- [ ] Canonical ownership restrictions are reused.
- [ ] Non-regular recursion follows characterized canonical behavior.

### Compiler and Runtime

- [ ] `compile_snapshot` remains the sole boundary.
- [ ] No source render occurs.
- [ ] No source load occurs.
- [ ] No parse occurs.
- [ ] HIR consistency runs.
- [ ] Memory planning runs.
- [ ] SSA verification runs.
- [ ] Bytecode validation runs.
- [ ] Generic VM execution runs.
- [ ] Native decline remains safe and unchanged.

### Stack Safety

- [ ] Clone is iterative.
- [ ] Drop is iterative.
- [ ] Equality is iterative.
- [ ] Debug/display behavior remains bounded or iterative.
- [ ] Validation is iterative.
- [ ] Binder collection is iterative.
- [ ] Resolution is iterative.
- [ ] Small-stack test passes.
- [ ] No arbitrary depth quota is added.

### Queries and Presentation

- [ ] Entity queries show ordinary stable identities.
- [ ] Field type queries show the recursive enum constructor.
- [ ] Generic field queries show stable binder arguments.
- [ ] Projection is deterministic.
- [ ] Projection contains no transaction-local marker.
- [ ] Projection fabricates no source facts.
- [ ] Diff contains no private staging noise.
- [ ] Continuations remain revision-bound.

### Deletion and Compaction

- [ ] Owned self dependency does not block deletion.
- [ ] Independent surviving dependencies do block deletion.
- [ ] Dependency-closed batch deletion still works.
- [ ] Private compaction preserves public identities.
- [ ] Private relocation emits no movement.
- [ ] Existing enum deletion tests remain green.

### Scope Control

- [ ] No mutual recursion is added.
- [ ] No general draft declaration graph is added.
- [ ] No source syntax is added.
- [ ] No renderer is added.
- [ ] No persistence is added.
- [ ] No daemon is added.
- [ ] No protocol is added.
- [ ] No cache is added.
- [ ] No new crate is added.
- [ ] No unrelated architecture is changed.

### Documentation

- [ ] README matches current support.
- [ ] Status matches current support.
- [ ] Architecture describes the narrow staging context.
- [ ] Workspace spec describes the current implementation.
- [ ] Roadmap removes the completed gap.
- [ ] Language spec changes only if genuinely necessary.
- [ ] Performance docs change only with retained measurements.
- [ ] Stale “non-recursive” claims are removed.
- [ ] Stale “no self handle” claims are removed.
- [ ] Mutual recursion is not claimed.

### Verification

- [ ] Focused compiler tests pass.
- [ ] Imported recursive enum tests pass.
- [ ] Affected crate tests pass.
- [ ] Rustfmt check passes.
- [ ] Clippy passes with warnings denied.
- [ ] Full workspace tests pass.
- [ ] Release build passes.
- [ ] Docker verification passes or exact environment failure is reported.
- [ ] `git diff --check` passes.
- [ ] Final worktree contains no accidental artifacts.

## Final Report Format

Use the following exact headings.

```text
Starting commit:
Starting branch:
Starting worktree:
Reference baseline relation:
Selected vertical:
Current consumer:
Characterization evidence:
Canonical non-regular recursion result:
Semantic result:
Public API result:
Representation decision:
Why a current-owner marker was sufficient:
Rejected broader alternatives:
Created identities:
Transaction-local state:
Identity behavior:
Atomicity behavior:
Deletion behavior:
Rename behavior:
Compiler path:
Runtime path:
Source-free parser/load evidence:
Stack-safety evidence:
Complexity result:
Compatibility breaks:
Deleted obsolete paths:
Focused tests:
Focused commands:
Affected crate verification:
Full verification:
Docker verification:
Measurements:
Documentation:
Remaining gaps:
Ending commit:
Ending worktree:
```

Report exact commands.

Report exact test names.

Report exact failures.

Distinguish product failures from environment failures.

Do not claim unrun verification.

Do not provide a transcript.

Do not provide hidden reasoning.

Report evidence, decisions, and remaining facts.

## Final Reminder

Implement exact source-free recursive enum declarations.

Use one narrow current enum marker.

Resolve it to stable identity during atomic enum creation.

Reuse the canonical recursive enum path.

Prove generic and non-generic execution.

Reject invalid use atomically.

Keep every user-depth traversal stack-safe.

Do not build mutual recursion infrastructure.

Do not build a future platform.

Stop after the verified vertical.
