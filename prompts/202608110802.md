# Complete source-free early-return authoring

Repository: `lkjsxc/lkjscript`

Observed baseline: `main` at `24677150795990b356d1f4e6d26e1508acf7e17a`

Prepared: 2026-08-11 08:02 JST

Transport file: do not commit this prompt.

## Executive task contract

> Complete exactly one dependency-closed product vertical: make early return
> a first-class source-free expression draft that converges on the existing
> canonical HIR, ownership, memory-planning, SSA, bytecode, VM, query,
> identity, and documentation paths. Stop before break, continue, general
> control-transfer machinery, incrementality, persistence, daemonization, or
> any broader platform work.

The repository already supports early return in imported source and in the
lower compiler and runtime layers. The missing product capability is
source-free construction through the semantic workspace. The current roadmap
names this as Now, and
`crates/lkjscript-app/tests/fixtures/ownership-control.lkjscript` is the
concrete convergence consumer.

This prompt is deliberately long because the user requested a highly
explicit execution contract. Its length is not permission to widen scope.
Use the detail to remove ambiguity, avoid rediscovery, prevent accidental
overengineering, and make the final change smaller.

- Treat the companion `AGENTS.md` as the requested repository-root
  replacement and durable engineering policy.
- Ensure the checkout root `AGENTS.md` matches the companion file before the
  final commit.
- Do not opportunistically expand `AGENTS.md` with task-specific
  implementation detail.
- Do not commit this timestamped transport prompt.
- Do not preserve backward compatibility for provisional internal or
  external surfaces unless current executable evidence establishes a real
  consumer.
- Do not ask the user to select among technical alternatives that repository
  evidence can decide.
- Use autonomous judgment when the proposed mechanism is inferior, but
  preserve the narrow objective and acceptance criteria.
- If HEAD has advanced, re-audit the live checkout before editing.
- If the exact vertical is already complete and documented, verify it and
  stop rather than inventing adjacent work.
- If the vertical is partially present, finish one active route and delete
  partial or duplicate mechanisms.

## Why this vertical is the correct next move

- The semantic workspace already constructs source-free functions, main,
  literals, calls, selected operations, conditionals, sequences, immutable
  locals, mutable locals, assignment, while, products, enums, and exhaustive
  enum matches.
- The authoritative source-free expression representation is already flat
  and non-recursive.
- The HIR already contains `ExprKind::Return { value }`.
- The source resolver already defines exact early-return typing and
  unreachable-control rules.
- Ownership checking, memory planning, SSA lowering, bytecode validation,
  and VM execution already understand HIR return.
- Public `NodeKind` already contains `Return`.
- Imported source already exercises return through the retained
  ownership-control fixture.
- The missing source-free constructor is therefore a narrow authoring and
  convergence gap, not a language-design gap.
- Closing this gap enables an ordinary resource-owning control-flow program
  without introducing a second semantic path.
- The work reuses mature lower layers instead of adding speculative
  architecture.
- The next roadmap item is measurement of full recomputation under
  representative imperative edits and queries; that measurement should begin
  only after this authoring gap is closed.
- Implementing break or continue now would broaden the proof surface without
  a present retained consumer.
- Building a generic control-transfer abstraction now would optimize for
  hypothetical symmetry rather than current need.
- Building incrementality now would precede the measurements intended to
  justify it.
- Building a daemon or persistence now would recreate systems recently
  deleted for lack of a consumer.

## Observed baseline facts to verify, not blindly assume

- Confirm every baseline fact below against the live checkout before
  editing.
- If current authority contradicts an observed fact, follow current
  authority and record the contradiction once.

- Baseline fact to confirm: The active branch is `main` and the observed
  head commit is the imperative-loop authoring completion.
- Baseline fact to confirm: The current root `AGENTS.md` already rejects
  arbitrary validity quotas and speculative platform machinery.
- Baseline fact to confirm: `docs/roadmap.md` currently names one
  source-free early-return vertical as Now.
- Baseline fact to confirm: `docs/status.md` explicitly lists source-free
  return, break, and continue as absent.
- Baseline fact to confirm: `docs/spec/language.md` already specifies early
  return and defines `never` as control that does not return normally.
- Baseline fact to confirm: `docs/spec/workspace.md` already identifies
  source-free return as an implementation gap.
- Baseline fact to confirm: `ExpressionDraft` stores a vector of `DraftNode`
  plus a root node ID.
- Baseline fact to confirm: `DraftNode` currently has no return variant.
- Baseline fact to confirm: `DraftNode::child_count` owns draft shape arity
  facts.
- Baseline fact to confirm: `DraftNode::for_each_child` owns draft child
  traversal facts.
- Baseline fact to confirm: `lower_draft` validates shape, computes
  iterative lowering actions, validates lexical scopes, lowers children
  before parents, and publishes one HIR expression.
- Baseline fact to confirm: `lower_draft` already knows the callable root
  and stable callable entity.
- Baseline fact to confirm: `lower_draft` already obtains the published
  binding-location index once per draft.
- Baseline fact to confirm: `lower_draft` already rejects unreachable
  expressions in `Sequence` and `While` bodies after a child of type
  `never`.
- Baseline fact to confirm: `lower_draft` currently checks final root
  assignability with the canonical `Type::unify_assignable` relation.
- Baseline fact to confirm: The source resolver rejects a return value whose
  type is already `never`.
- Baseline fact to confirm: The source resolver requires the return value
  type to exactly equal the enclosing callable result type.
- Baseline fact to confirm: The source resolver assigns the return
  expression type `never`.
- Baseline fact to confirm: The source resolver rejects an expression after
  a control terminator in ordered control bodies.
- Baseline fact to confirm: `NodeKind::Return` already exists in the public
  workspace model.
- Baseline fact to confirm: `LegalConstructor` currently advertises
  literals, operations, loads, moves, borrows, calls, aggregates, if,
  sequence, mutable local, assignment, and while, but not return.
- Baseline fact to confirm: The source-free path is expected to invoke
  source loading and parsing zero times.
- Baseline fact to confirm: The complete semantic snapshot is compiled
  directly without rendering or reparsing text.
- Baseline fact to confirm: Transactions stage allocator and semantic state
  and publish one new Arc only on success.
- Baseline fact to confirm: Complete snapshots receive canonical ownership
  validation before publication.
- Baseline fact to confirm: The generic VM remains the complete production
  execution route.
- Baseline fact to confirm: Baseline native execution may decline before
  entry without invalidating the program.
- Baseline fact to confirm: No forced-native route should be introduced for
  this work.
- Baseline fact to confirm: The ownership-control fixture allocates a byte
  vector, borrows it for length, then returns `7` early.
- Baseline fact to confirm: The package manifest retains ownership-control
  as a public target.
- Baseline fact to confirm: The current roadmap intends representative
  recomputation measurement after this vertical.

## Authority reading order for this task

- For every authority surface below, identify the exact return producer and
  consumer before editing.
- Do not rewrite unrelated material for stylistic consistency.

- Read `AGENTS.md` for repository procedure, anti-overengineering,
  verification, and reporting.
- Read `docs/authority.md` for claim ownership and conflict resolution.
- Read `docs/roadmap.md` for current work ordering and the intended stop
  point.
- Read `docs/status.md` for implemented source-free surface and known gaps.
- Read `docs/spec/language.md` for normative early-return, `never`,
  ownership, and control semantics.
- Read `docs/spec/workspace.md` for normative source-free editing,
  transaction, query, and identity contract.
- Read `docs/architecture.md` for current semantic editing and compile flow.
- Read `docs/performance.md` for measurement discipline and current
  baselines.
- Read `crates/lkjscript-compiler/src/workspace/draft.rs` for public draft
  representation and iterative child traversal.
- Read `crates/lkjscript-compiler/src/workspace/transaction.rs` for draft
  validation, lowering, staging, and publication.
- Read
  `crates/lkjscript-compiler/src/analyze/resolution/control_transfer.rs` for
  canonical source return semantics.
- Read `crates/lkjscript-compiler/src/hir/expressions.rs` for canonical HIR
  child traversal and clone behavior.
- Read `crates/lkjscript-compiler/src/workspace/model.rs` for public node
  kinds and identity-bearing views.
- Read `crates/lkjscript-compiler/src/workspace/index.rs` for node type,
  expected type, effect, containment, and reference indexing.
- Read `crates/lkjscript-compiler/src/workspace/query.rs` for semantic facts
  and legal constructor discovery.
- Read `crates/lkjscript-compiler/src/workspace/projection.rs` for
  human-review projection.
- Read `crates/lkjscript-compiler/src/workspace/validate.rs` for complete
  semantic-program validation.
- Read `crates/lkjscript-compiler/src/ownership/` for canonical move,
  borrow, lifetime, and cleanup validation.
- Read `crates/lkjscript-compiler/src/hir/memory_plan/` for return cleanup
  obligations and verification.
- Read `crates/lkjscript-compiler/src/ssa/lowering/` for canonical return
  control-flow lowering.
- Read `crates/lkjscript-bytecode/` and `crates/lkjscript-core/` for return
  encoding and validation boundaries.
- Read `crates/lkjscript-vm/` for generic return execution and cleanup.
- Read `crates/lkjscript-app/tests/fixtures/ownership-control.lkjscript` for
  retained source convergence consumer.
- Read the nearest source-free convergence tests for test style, zero-parser
  counters, identity comparison, and execution evidence.
- Read the latest imperative-loop commit for the nearest implementation
  precedent and scope discipline.

## Reconnaissance questions that must be answered

- Answer before implementation: Which helper returns the enclosing main or
  function result type from the private callable root?
- Answer before implementation: Does an existing helper already compare
  exact HIR types while producing structured workspace type errors?
- Answer before implementation: How does source import represent return
  effects before and after partial effect inference?
- Answer before implementation: Does `EffectSet` intentionally have no
  separate function-return bit?
- Answer before implementation: How does index construction assign expected
  type to the child of `ExprKind::Return`?
- Answer before implementation: How does index construction map
  `ExprKind::Return` to `NodeKind::Return`?
- Answer before implementation: How does projection currently render an
  imported return node?
- Answer before implementation: Does legal-constructor filtering use the
  hole expected type, the actual constructor result type, or both?
- Answer before implementation: Can `LegalConstructor::Return` be added
  without changing the public enum into a descriptor hierarchy?
- Answer before implementation: How does a caller discover the enclosing
  callable result type through existing queries?
- Answer before implementation: Which test helper constructs a deterministic
  workspace and applies a draft transaction?
- Answer before implementation: Which test helper compares imported and
  source-free snapshots semantically?
- Answer before implementation: Which counters prove that a source-free
  transaction did not load or parse source?
- Answer before implementation: Which tests already compare normalized
  bytecode and VM outcomes?
- Answer before implementation: Which tests already inspect memory-plan
  obligation kinds?
- Answer before implementation: Which tests already inspect cleanup exactly
  once on return or trap?
- Answer before implementation: Which fixture or helper can return an affine
  value by move?
- Answer before implementation: Which source behavior rejects nested
  divergent return values?
- Answer before implementation: Which source behavior rejects a statement
  after return in a sequence or loop body?
- Answer before implementation: Does the root `Type::unify_assignable`
  relation accept `never` for any expected expression type?
- Answer before implementation: If it accepts `never`, is that the intended
  existing bottom-type behavior?
- Answer before implementation: Does adding a return child to draft
  traversal preserve postorder lowering without special actions?
- Answer before implementation: Does draft scope analysis need any special
  return handling beyond ordinary child traversal?
- Answer before implementation: Does draft definition-event analysis need
  any special return handling beyond ordinary child traversal?
- Answer before implementation: Does draft shape validation automatically
  cover the new one-child node once arity and child traversal are updated?
- Answer before implementation: Does deletion dependency analysis already
  traverse HIR return children generically?
- Answer before implementation: Does semantic compaction already traverse
  HIR return children generically?
- Answer before implementation: Does stable node reconciliation already
  treat return as an ordinary HIR node?
- Answer before implementation: Does hole replacement preserve the root ID
  when a return fills it?
- Answer before implementation: Do descendant IDs for the return value
  follow existing replacement rules?
- Answer before implementation: Does introducing a hole over a return
  tombstone descendants through generic subtree removal?
- Answer before implementation: Can the native baseline already lower return
  for eligible scalar groups?
- Answer before implementation: Would the ownership-control workload
  intentionally use VM fallback because of byte-vector operations?
- Answer before implementation: Is any current README statement stale after
  the latest while vertical?
- Answer before implementation: Which exact roadmap item should become Now
  after return completes?
- Answer before implementation: What focused command provides the fastest
  falsification loop for workspace draft changes?
- Answer before implementation: What is the smallest release or ignored
  stress check relevant to a new one-child draft variant?
- Answer before implementation: Are any match arms or conditional joins
  already using `never` and therefore useful as return precedents?
- Answer before implementation: Do diagnostics expose semantic type
  identities without falling back to formatted strings?
- Answer before implementation: What exact fields of the prior snapshot and
  allocator are asserted by failure-atomicity tests?

## Required semantic result

- Requirement: A source-free draft can express one early return with exactly
  one value child.
- Requirement: The return belongs to the enclosing main or function
  identified by the draft root context.
- Requirement: The child expression is evaluated before control transfers
  out of the callable.
- Requirement: The child type must exactly equal the enclosing callable
  result type.
- Requirement: A child whose type is already `never` is invalid as a return
  payload.
- Requirement: The return expression itself has type `never`.
- Requirement: The return expression is valid in any expression position
  where the existing bottom-type relation admits `never`.
- Requirement: The return expression is valid as a complete function or main
  body when existing bottom-type assignment admits it.
- Requirement: An ordered sequence rejects every child after a return.
- Requirement: A while body rejects every child after a return within the
  same ordered body.
- Requirement: An if branch may return while the other branch yields the
  surrounding value type when the existing control join permits it.
- Requirement: Both if branches may return, yielding `never` through the
  existing join semantics.
- Requirement: A match arm may return while other arms yield the match
  result when the existing canonical match lowering permits it.
- Requirement: A return may occur inside immutable or mutable local bodies
  without bypassing lexical scope rules.
- Requirement: A return may not use a binding that is out of scope, stale,
  foreign, wrong-kind, deleted, or invisible.
- Requirement: A return payload may move an affine value when canonical
  ownership rules permit transfer to the caller.
- Requirement: A return payload may copy a copy-safe value through the
  existing load rules.
- Requirement: A return must not synthesize an implicit drop of the returned
  affine value.
- Requirement: Locals not transferred by the return must receive canonical
  cleanup exactly once.
- Requirement: Active borrows, moved places, reinitialization state, and
  failure cleanup remain governed by the existing ownership checker.
- Requirement: The return adds no ambient capability and performs no hidden
  host effect.
- Requirement: The return does not mean process exit and must not be
  labelled with the process-exit effect.
- Requirement: Child effects are preserved and combined according to the
  existing canonical effect inference.
- Requirement: The source-free path produces the same complete HIR meaning
  as equivalent imported source.
- Requirement: No source renderer, source hash, path, parser node, or
  fabricated source origin is introduced.
- Requirement: No executable fallback is retained behind an incomplete
  return node.
- Requirement: A malformed return draft rejects before publication.
- Requirement: A semantically invalid return rejects before publication.
- Requirement: A return ownership failure rejects before publication.
- Requirement: Every failure preserves the exact prior snapshot and staged
  allocator state promised by current transaction law.

- Prove each requirement through an existing canonical path or one focused
  regression test.
- Do not create duplicate validation merely to make a requirement easier to
  assert.

## Representation decision

The expected smallest representation is one new child-bearing `DraftNode`
variant. Use another representation only if direct inspection proves it is
smaller, more coherent, and reuses more current authority.

- Prefer `DraftNode::Return { value: DraftNodeId }` or the
  repository-consistent equivalent.
- Keep `ExpressionDraft` flat and non-recursive.
- Add exactly one child to draft arity and traversal facts.
- Reuse the existing draft shape validator.
- Reuse the existing iterative lowering order.
- Reuse the existing child extraction helper.
- Reuse the existing callable root context.
- Reuse the existing exact type model.
- Lower directly to existing `ExprKind::Return`.
- Do not add a parallel semantic return node outside HIR.
- Do not add a `ControlTransferDraft` enum merely for future break and
  continue symmetry.
- Do not add a loop-target abstraction for return.
- Do not add a new identity for return.
- Do not add a return plan, descriptor, registry, witness, or side table.
- Do not add a source syntax change; imported source already supports
  return.
- Do not add a wire representation.
- Do not add serialization.
- Do not add a CLI editing route.
- Do not add persistence.
- Do not add incremental invalidation machinery.
- Do not add caching.
- Do not add a new crate.
- Do not split files merely to keep line counts small.
- Do not generalize the implementation until a second current consumer
  creates real duplication.
- Delete obsolete unsupported-return branches or stale prose once the active
  route exists.

## Callable result-type authority

A nested return cannot use the local expression expectation as its payload
type. The payload type is owned by the enclosing callable signature. Derive
it from the private semantic program and callable root, then expose only
structured semantic facts through public APIs.

- Callable context `main`: use the current main declaration result type.
- Callable context `ordinary non-generic function`: use its canonical
  function result type.
- Callable context `generic function`: use the declared result type
  containing its own stable type-parameter semantics.
- Callable context `source-imported function edited source-free`: preserve
  the imported callable signature while the new return has semantic origin.
- Callable context `source-free function`: use the directly created
  canonical signature.
- Callable context `function scheduled for deletion in the same
  transaction`: reject structural editing through the existing deleted-root
  rule.
- Callable context `foreign root`: reject through namespace and identity
  validation.
- Callable context `stale root`: reject deterministically without
  publication.
- Callable context `wrong-kind root`: reject before constructing HIR.

- Do not reconstruct callable result types from display text, source syntax,
  or public projection.
- Keep result-type lookup local unless a second current use proves a
  reusable helper is warranted.

## Exact typing rules

- Typing case: return `i64` from an `i64` callable; expected outcome:
  accept.
- Typing case: return `f64` from an `i64` callable; expected outcome: reject
  exact mismatch.
- Typing case: return `never` from any callable; expected outcome: reject
  because the payload is already divergent.
- Typing case: return `unit` from a `unit` callable; expected outcome:
  accept.
- Typing case: return `bool` from a `bool` callable; expected outcome:
  accept.
- Typing case: return a copy-safe local of exact type; expected outcome:
  accept through existing load rules.
- Typing case: return an affine local by copy load; expected outcome: reject
  through existing load restrictions.
- Typing case: return an affine local by explicit move; expected outcome:
  accept when ownership permits.
- Typing case: return a lexical byte slice; expected outcome: follow
  existing callable result restrictions and source behavior.
- Typing case: return a nominal product; expected outcome: accept only when
  exact nominal identity matches.
- Typing case: return an enum; expected outcome: accept only when exact enum
  identity and arguments match.
- Typing case: return a generic type parameter value; expected outcome:
  follow current generic ownership and load restrictions exactly.
- Typing case: return from one branch of an if; expected outcome: join with
  the other branch through existing bottom-type semantics.
- Typing case: return from every branch of an if; expected outcome: derive
  `never` without an ad hoc special case.
- Typing case: return from one match arm; expected outcome: use existing
  match result joining and exhaustiveness.
- Typing case: return at draft root; expected outcome: allow final
  assignability from `never` through existing root rules.
- Typing case: return as a local initializer; expected outcome: reject
  because a divergent expression cannot initialize storage.
- Typing case: return as an assignment value; expected outcome: reject
  because a divergent expression cannot fill storage.
- Typing case: return as an ordinary operation argument; expected outcome:
  follow canonical argument typing and divergence rules without special
  coercion.
- Typing case: return as a call argument; expected outcome: follow canonical
  call argument typing and divergence rules without special coercion.

- Use the same exact type identity and comparison semantics as source
  resolution for every typing case.
- Do not introduce implicit numeric promotion, inference, or display-string
  comparison.

## Effects and divergence

- Return control is represented by result type `never`, not by fabricating a
  host effect.
- A return is not process exit.
- A return is not a trap.
- A return is not cancellation.
- A return is not proof of whole-function divergence.
- The payload is evaluated and its effects occur before control transfer.
- The return node preserves payload effects.
- Partial effect inference remains the canonical post-transaction authority.
- Complete effect inference remains the canonical compile authority.
- No second mutable effect fact may be added to the draft.
- No diagnostic should claim `MAY_EXIT` solely because a function returns.
- No diagnostic should claim purity if the payload performs an effect.
- No effect should be inferred from the spelling or projection label
  `return`.
- Unknown effects remain honest while surrounding holes exist.
- A completed source-free and imported return should expose equal named
  effect bits.

## Ownership, borrowing, and cleanup

- Ownership scenario: copy scalar local then return literal; expected law:
  clean the local according to its ordinary copy semantics and return the
  literal.

- Ownership scenario: owned byte vector local, temporary shared borrow, then
  return scalar; expected law: end the temporary borrow and release the
  owned vector exactly once during return cleanup.

- Ownership scenario: owned byte vector local moved into return; expected
  law: transfer ownership to the caller and do not release the moved place
  in the callee.

- Ownership scenario: owned byte vector local dropped before return then
  returned again; expected law: reject use after consume through the
  existing ownership checker.

- Ownership scenario: live affine value overwritten before return; expected
  law: preserve the existing explicit reinitialization law.

- Ownership scenario: mutable affine local moved, reinitialized, then scalar
  return; expected law: clean only the currently initialized epoch.

- Ownership scenario: return payload traps before return commits; expected
  law: perform canonical failure cleanup and do not execute a second path.

- Ownership scenario: return payload calls a function with effects; expected
  law: perform the call once, then transfer control once.

- Ownership scenario: return inside a branch with branch-local owned values;
  expected law: clean only values live on the taken return path.

- Ownership scenario: return inside a match arm with payload bindings;
  expected law: clean arm-local and outer values under the canonical match
  ownership plan.

- Ownership scenario: return inside a while body; expected law: clean the
  active iteration and callable scopes exactly once.

- Ownership scenario: return from nested immutable locals; expected law:
  clean all non-transferred owned locals in reverse canonical lifetime
  order.

- Ownership scenario: return from nested mutable locals; expected law: clean
  initialized places and skip moved or uninitialized places.

- Ownership scenario: return from a function called by the VM; expected law:
  produce one caller-visible result and one callee cleanup sequence.

- Ownership scenario: return from baseline native after entry; expected law:
  treat native outcome as final; never rerun in VM.

- Ownership scenario: native decline before entry; expected law: run the
  unchanged validated program once in the VM.

- Ownership scenario: allocation failure while staging the draft; expected
  law: publish no revision and mutate no allocator state.

- Ownership scenario: ownership validation failure after lowering; expected
  law: publish no revision and preserve prior snapshot identity.

- Ownership scenario: cleanup reporting saturation; expected law: retain
  existing explicitly saturating observation semantics.

- Ownership scenario: host resource return where supported; expected law:
  transfer or clean according to the exact typed resource contract.

- Prove these laws through the existing ownership, memory-plan, bytecode,
  and VM route.
- Do not encode ownership policy in `DraftNode` or duplicate it in workspace
  preflight.
- Add focused tests only where current coverage does not already prove the
  law.

## Transaction, identity, and diff behavior

- Transaction invariant: Filling a body hole with a return preserves the
  hole root node identity under current fill semantics.
- Transaction invariant: Replacing an existing node with a return preserves
  the targeted root identity under current replacement semantics.
- Transaction invariant: The new return payload descendant receives ordinary
  new descendant identity.
- Transaction invariant: Unaffected ancestors and siblings retain identity.
- Transaction invariant: Introducing a hole over a return removes and
  tombstones return descendants rather than hiding them.
- Transaction invariant: Deleting the owning callable removes the return and
  payload identities through containment ownership.
- Transaction invariant: Private compaction does not masquerade as public
  movement.
- Transaction invariant: A failed return edit consumes no stable identity.
- Transaction invariant: A failed return edit leaves allocator generations
  and free lists unchanged.
- Transaction invariant: A failed return edit leaves the published Arc
  unchanged.
- Transaction invariant: A failed return edit leaves revision unchanged.
- Transaction invariant: A failed return edit leaves projection unchanged.
- Transaction invariant: A failed return edit leaves diagnostics unchanged
  except for the returned error value.
- Transaction invariant: A failed return edit leaves future deterministic
  allocation unchanged.
- Transaction invariant: A successful return edit publishes exactly one
  revision.
- Transaction invariant: A successful return edit reports one root
  replacement or hole fill plus created descendants through existing diff
  semantics.
- Transaction invariant: Reference and call rewiring in the payload is
  reported through existing diff entries.
- Transaction invariant: The return node itself does not create a new
  semantic entity.
- Transaction invariant: The return node does not receive a special identity
  domain.
- Transaction invariant: Old snapshots remain queryable after a successful
  return edit.

- Assert public semantic meaning rather than private dense indexes.

## Query, discovery, and projection

Source-free authoring is incomplete if an agent can construct return only by
memorizing a private Rust enum. The existing deterministic discovery and
inspection surfaces must truthfully expose the new capability without
creating a broad descriptor framework.

- Query requirement: A return node reports `NodeKind::Return`.
- Query requirement: A return node reports actual type `never`.
- Query requirement: A return node reports its surrounding expected type
  through existing node facts.
- Query requirement: The return payload node reports the enclosing callable
  result as expected type.
- Query requirement: The return node has exactly one public semantic child
  in evaluation order.
- Query requirement: References and calls inside the payload are indexed
  normally.
- Query requirement: Effects on the return and payload are structured and
  deterministic.
- Query requirement: Projection renders return without source attachment.
- Query requirement: Projection uses review-local labels only and never
  accepts them as identity.
- Query requirement: Imported and source-free return projections normalize
  to equivalent semantic structure.
- Query requirement: Legal constructor discovery advertises return in a
  callable expression context.
- Query requirement: Legal constructor discovery does not advertise return
  outside a callable context.
- Query requirement: Legal constructor discovery remains deterministic and
  stably ordered.
- Query requirement: Legal constructor discovery does not falsely claim the
  payload is the hole expected type when the callable result differs.
- Query requirement: An agent can discover the callable result through the
  smallest existing structured query path.
- Query requirement: No new universal constructor descriptor is introduced.
- Query requirement: No raw HIR type, binder string, dense ID, or private
  root address leaks into public results.
- Query requirement: No machine consumer must parse projection text to learn
  return semantics.
- Query requirement: No machine consumer must infer return from a `never`
  display string alone.
- Query requirement: Pagination and continuation behavior remain unchanged.

- Add or update focused query and projection assertions only where current
  imported-return coverage is insufficient.

## Legal-constructor design constraint

- Prefer one `LegalConstructor::Return` case if the existing enum can
  express truthful availability.
- Do not remove `Copy` or `Ord` from the whole constructor model merely to
  attach a recursive type unless evidence proves that change is worth its
  cost.
- Do not create `ReturnDescriptor`, `ControlDescriptor`,
  `ConstructorRequirements`, or a general schema solely for this
  constructor.
- Use the existing hole owner and function-signature query to expose the
  payload type when that is sufficiently compact and deterministic.
- If one extra query would dominate a measured real workflow, record the
  measurement before changing the public model.
- Do not optimize hypothetical API-token cost with unmeasured descriptor
  proliferation.
- Do not omit return from constructor discovery merely to avoid touching the
  enum.
- Keep stable ordering tests explicit after adding the new variant.

## Source and source-free convergence

- Compare imported and source-free early-return behavior for program state
  and completeness blockers.
- Compare imported and source-free early-return behavior for callable
  identity and declared result type.
- Compare imported and source-free early-return behavior for normalized
  entity kinds and names.
- Compare imported and source-free early-return behavior for node kinds.
- Compare imported and source-free early-return behavior for actual node
  types.
- Compare imported and source-free early-return behavior for expected node
  types.
- Compare imported and source-free early-return behavior for ordered
  containment.
- Compare imported and source-free early-return behavior for references.
- Compare imported and source-free early-return behavior for calls.
- Compare imported and source-free early-return behavior for dependencies.
- Compare imported and source-free early-return behavior for named effect
  bits.
- Compare imported and source-free early-return behavior for return payload
  expectation.
- Compare imported and source-free early-return behavior for ownership
  obligations.
- Compare imported and source-free early-return behavior for memory-plan
  cleanup obligations.
- Compare imported and source-free early-return behavior for main bytecode
  stream or normalized executable meaning.
- Compare imported and source-free early-return behavior for VM result.
- Compare imported and source-free early-return behavior for trap behavior
  where exercised.
- Compare imported and source-free early-return behavior for cleanup
  behavior.
- Compare imported and source-free early-return behavior for source-free
  import counter.
- Compare imported and source-free early-return behavior for source-free
  parser counter.
- Compare imported and source-free early-return behavior for semantic
  projection.
- Compare imported and source-free early-return behavior for old-snapshot
  stability after edit.
- Compare imported and source-free early-return behavior for failure
  atomicity on invalid variants.

- Compare semantic outcomes, not source text or private dense numbering.
- Reuse the nearest existing convergence helper rather than creating a
  second test framework.

## Primary retained convergence fixture

```text
main/
sig/
inputs/
/inputs
output/
i64
/output
/sig
let/
bind/
b
new-byte-vector/
2
/new-byte-vector
/bind
do/
byte-slice-length/
borrow/
b
/borrow
/byte-slice-length
return/
7
/return
/do
/let
/main
```

- Construct a semantically equivalent program from `Workspace::empty`
  without source text, paths, hashes, rendering, or parsing.
- Create parameterless main returning `i64` through the existing source-free
  declaration edit.
- Fill its body with one flat expression draft.
- Create an immutable local `b` initialized by the canonical byte-vector
  operation already supported source-free.
- Evaluate the shared borrow and byte-slice length before return.
- Return literal `7` through the new draft node.
- Ensure the sequence has no child after the return.
- Compile the complete snapshot directly.
- Execute through the selected production route.
- Accept a typed baseline-native decline before entry.
- Require exactly one VM execution if native declines.
- Require result `7`.
- Require the owned byte vector to be released exactly once.
- Require zero source loading and zero parser invocation for the source-free
  path.
- Compare against the retained imported fixture through existing
  normalization helpers.

## Positive test matrix

- Assert the transaction publishes exactly one revision.
- Assert the root and descendant identity behavior promised by current edit
  semantics.
- Assert structured node kind, actual type, expected type, and effects.
- Assert direct compilation without render or parse.
- Assert execution outcome and cleanup when the case executes.

- Positive case: main returns an `i64` literal at body root; purpose: basic
  direct body fill.
- Positive case: ordinary function returns an `i64` literal at body root;
  purpose: ordinary callable context.
- Positive case: main returns `unit`; purpose: unit result semantics.
- Positive case: function returns a loaded copy-safe parameter; purpose:
  parameter visibility and exact result typing.
- Positive case: function returns a loaded immutable local; purpose: local
  identity and scope.
- Positive case: function returns an affine byte vector by explicit move;
  purpose: ownership transfer to caller.
- Positive case: sequence evaluates a pure expression then returns; purpose:
  ordered evaluation before control transfer.
- Positive case: sequence evaluates an effectful supported operation then
  returns; purpose: payload-independent earlier effects.
- Positive case: immutable local owns a byte vector, temporary borrow ends,
  then scalar return; purpose: cleanup exactly once.
- Positive case: mutable local contains a scalar then return; purpose:
  mutable scope and cleanup.
- Positive case: mutable affine local is moved, reinitialized, then scalar
  return; purpose: epoch transition cleanup.
- Positive case: if then branch returns while else branch yields surrounding
  type; purpose: bottom-type join.
- Positive case: if else branch returns while then branch yields surrounding
  type; purpose: symmetric bottom-type join.
- Positive case: both if branches return; purpose: fully divergent
  conditional.
- Positive case: return appears as final child of while body; purpose:
  loop-body early exit.
- Positive case: return appears in one exhaustive enum match arm; purpose:
  match result join.
- Positive case: return appears in every exhaustive enum match arm; purpose:
  divergent match.
- Positive case: return payload calls a source-free function; purpose: call
  indexing and result typing.
- Positive case: return payload calls an imported function; purpose: mixed
  origin convergence.
- Positive case: return payload constructs a nominal copy-safe product;
  purpose: exact nominal result identity.
- Positive case: return payload constructs a non-generic enum; purpose: enum
  result identity.
- Positive case: a source-imported body subtree is replaced by a source-free
  return; purpose: mixed source/semantic origin.
- Positive case: a typed hole is filled with return; purpose: hole root
  identity preservation.
- Positive case: an ordinary expression is replaced with return; purpose:
  replacement root identity preservation.
- Positive case: old snapshot remains queryable after return publication;
  purpose: immutable snapshot law.
- Positive case: legal constructors advertise return at a body hole;
  purpose: agent discovery.
- Positive case: node semantics expose return as `never`; purpose:
  structured query.
- Positive case: projection renders source-free return without source
  attachment; purpose: human review.
- Positive case: source-free ownership-control matches imported
  ownership-control; purpose: end-to-end convergence.
- Positive case: check compiles return without constructing execution state;
  purpose: effect-free check boundary.

## Negative and failure-atomicity test matrix

- For every negative case, assert deterministic structured error category
  and relevant semantic facts.
- For every negative case, assert the published snapshot Arc, revision,
  projection, diagnostics, and allocator remain unchanged.
- Assert future deterministic identity allocation is unchanged where the
  existing harness can prove it.
- Do not assert private dense indexes unless the test is specifically about
  compaction internals.

- Negative case: return `f64` from `i64` callable; rejection reason: exact
  payload type mismatch.
- Negative case: return `i64` from `unit` callable; rejection reason: exact
  payload type mismatch.
- Negative case: return a return expression; rejection reason: payload
  already has type `never`.
- Negative case: return a divergent if; rejection reason: payload already
  has type `never`.
- Negative case: expression follows return in sequence; rejection reason:
  unreachable ordered expression.
- Negative case: expression follows return in while body; rejection reason:
  unreachable loop-body expression.
- Negative case: return is reused as a child by two parents; rejection
  reason: reused draft child.
- Negative case: return payload node is disconnected from root; rejection
  reason: disconnected draft graph.
- Negative case: return points to itself; rejection reason: draft cycle.
- Negative case: return points to stale draft node ID; rejection reason:
  stale transaction-local identity.
- Negative case: return references an out-of-scope local; rejection reason:
  lexical visibility.
- Negative case: return references a future local; rejection reason: forward
  binding use.
- Negative case: return references a local from another match arm; rejection
  reason: cross-arm binding use.
- Negative case: return references a foreign workspace entity; rejection
  reason: namespace validation.
- Negative case: return references a stale entity generation; rejection
  reason: generation validation.
- Negative case: return references a wrong-kind entity; rejection reason:
  kind validation.
- Negative case: return references an invisible declaration; rejection
  reason: visibility validation.
- Negative case: return targets a callable deleted in the same batch;
  rejection reason: deleted-root structural edit.
- Negative case: return payload calls a function deleted in the same batch;
  rejection reason: final staged dependency validation.
- Negative case: return attempts copy load of byte vector; rejection reason:
  affine copy rejection.
- Negative case: return moves an already moved byte vector; rejection
  reason: use-after-move rejection.
- Negative case: return occurs while conflicting mutable borrow is active;
  rejection reason: borrow conflict.
- Negative case: return leaves a required owned place uninitialized
  inconsistently; rejection reason: ownership-state validation.
- Negative case: allocation failure occurs while lowering return payload;
  rejection reason: host failure atomicity.
- Negative case: allocation failure occurs while indexing published return;
  rejection reason: publication atomicity.
- Negative case: stale base revision carries a valid return draft; rejection
  reason: revision precondition.
- Negative case: foreign hole receives a return fill; rejection reason: hole
  namespace validation.
- Negative case: stale hole receives a return fill; rejection reason: hole
  generation validation.
- Negative case: same target receives overlapping return replacements;
  rejection reason: structural edit disjointness.
- Negative case: return fill and owner deletion occur in contradictory
  batch; rejection reason: delete-owned edit rejection.
- Negative case: return is advertised as legal outside callable context;
  rejection reason: truthful constructor discovery.
- Negative case: return is advertised with an incorrect payload type
  implication; rejection reason: truthful constructor meaning.
- Negative case: return node leaks private dense root or HIR type; rejection
  reason: public boundary integrity.
- Negative case: source-free return path invokes parser; rejection reason:
  direct semantic path violation.
- Negative case: native entry begins and VM fallback also executes;
  rejection reason: exactly-once execution violation.

## Stack safety and scale

- The new draft node must participate in existing iterative shape traversal.
- The new draft node must participate in existing iterative lowering
  traversal.
- The new HIR shape already participates in iterative clone and child
  traversal.
- Do not add recursive validation for return.
- Do not add a nesting quota for return.
- Do not add a draft-node count quota.
- Do not add a control-transfer count quota.
- Do not widen integers without a demonstrated representation boundary.
- Use checked conversion before host indexing.
- Inspect the existing 20,000-level source-free expression stress generator.
- Extend an existing generator to include the new variant only if the
  extension is small and proves the shared algorithm.
- Do not create a second giant stress harness solely for return.
- A focused small-stack test is warranted only if the new implementation
  introduces a depth-sensitive path not already covered.
- Do not claim all compiler paths stack-safe from one return-specific stress
  test.
- Keep locked-release stress ignored if that is the existing repository
  policy.

## Performance and work-complexity constraints

- Adding one return node should be O(1) local work plus existing child
  traversal.
- Do not rescan every function to discover the callable result for each
  return node.
- Do not query public indexes from inside the private lowering loop when the
  callable root already identifies the owner.
- Do not build a return index, cache, or side table.
- Do not hash the draft or semantic program for return validation.
- Do not render types merely to compare them.
- Do not serialize HIR to validate return.
- Do not rebuild source provenance.
- Do not add incremental machinery in this turn.
- Do not perform a broad benchmark campaign for a one-node authoring
  feature.
- Run a focused deterministic traversal counter if code inspection reveals a
  risk of repeated scanning.
- Run the existing relevant scale test if the shared traversal changed.
- Record no performance improvement claim without equivalent evidence.
- Preserve the current representative baseline evidence; do not replace it
  with return microbenchmarks.
- Promote representative edit/query recomputation measurement to Now after
  this vertical completes.

## API and model-context cost

- Search for return producers and consumers before opening complete large
  modules.
- Read the smallest code ranges that settle each question.
- Use the latest imperative-loop diff as a precedent rather than
  rediscovering every workspace mechanism.
- Run the nearest focused workspace test during iteration.
- Do not rerun the full workspace suite after edits that cannot affect it.
- Use quiet Cargo flags that preserve failures.
- Capture noisy complete logs outside Git.
- Do not paste complete successful logs into the final report.
- Do not emit complete projections when a selected return subtree is
  sufficient.
- Do not emit full bytecode or HIR unless a convergence mismatch requires
  it.
- On mismatch, report the first semantic dimension that differs before
  dumping deeper representations.
- Prefer one convergence helper over repeated independent assertions that
  duplicate setup.
- Do not add a command wrapper merely to shorten one command.
- Do not add an output framework merely to reduce test log lines.
- Measure output bytes only if command-output cost is changed by this task.
- Keep successful high-frequency checks quiet.
- Keep failures actionable and complete enough to choose the next edit.
- Avoid loading unchanged large documentation repeatedly.
- Do not spend model context debating future break and continue design.
- Stop after the return vertical and record the next item in one concise
  roadmap edit.

## Implementation surfaces and expected treatment

- For every implementation surface below, inspect its current producer and
  consumer first.
- Delete stale unsupported-return statements where the new active route
  displaces them.
- Do not refactor unrelated neighboring code for symmetry.

- Surface `workspace draft enum`: add the smallest one-child source-free
  return representation.
- Surface `draft child count`: report exactly one child.
- Surface `draft child traversal`: visit the payload exactly once.
- Surface `draft shape validation`: reuse existing connected, acyclic,
  single-parent validation.
- Surface `draft lowering order`: reuse existing iterative
  child-before-parent order.
- Surface `draft scope validation`: treat payload references under ordinary
  lexical scope.
- Surface `definition-event analysis`: allow return inside local bodies
  without special binding scope.
- Surface `callable result lookup`: derive exact enclosing result type from
  private semantic authority.
- Surface `draft lowering match`: validate payload and emit canonical HIR
  return.
- Surface `root assignability`: reuse existing bottom-type relation.
- Surface `effect inference`: reuse canonical HIR effect facts.
- Surface `ownership checker`: reuse canonical move, borrow, and cleanup
  validation.
- Surface `memory planner`: reuse existing return cleanup obligations.
- Surface `SSA lowering`: reuse existing return block/control transfer.
- Surface `bytecode lowering`: reuse existing return instruction path.
- Surface `bytecode validation`: reuse existing state and cleanup
  validation.
- Surface `VM`: reuse existing generic return execution.
- Surface `baseline native`: make no expansion unless a pre-existing defect
  blocks equivalent scalar return.
- Surface `workspace index`: ensure child expectation and node facts are
  truthful.
- Surface `node model`: reuse existing public `NodeKind::Return`.
- Surface `legal constructors`: advertise return through the smallest
  truthful enum extension.
- Surface `projection`: reuse existing imported-return rendering.
- Surface `semantic diff`: reuse ordinary root replacement and descendant
  changes.
- Surface `identity reconciliation`: reuse ordinary node continuity rules.
- Surface `hole handling`: reuse fill and introduction semantics.
- Surface `deletion and compaction`: reuse generic expression traversal and
  containment ownership.
- Surface `source importer`: make no syntax change.
- Surface `package manifest`: retain ownership-control target unless a
  current authority intentionally changes it.
- Surface `workspace tests`: add focused positive, negative, query,
  identity, and atomicity coverage.
- Surface `app integration tests`: prove retained ownership-control product
  behavior.
- Surface `status documentation`: move return from absent to current.
- Surface `workspace specification current section`: describe implemented
  return without changing broader target prematurely.
- Surface `architecture documentation`: mention the expanded draft surface
  only where current flow is enumerated.
- Surface `roadmap`: remove completed return Now and promote measurement.
- Surface `README`: update only if its supported authoring summary is now
  materially stale.
- Surface `performance documentation`: change only if new evidence or method
  is produced.

## Documentation result

- Keep Current, Target, and Historical claims distinct across every
  documentation edit.
- Delete superseded prose instead of preserving a compatibility narrative.

- Documentation surface `docs/status.md`: state that source-free expression
  construction now includes early return; keep break and continue explicitly
  absent.
- Documentation surface `docs/status.md`: record the ownership-control
  source-free convergence evidence and zero-parser path if tests prove it.
- Documentation surface `docs/status.md`: keep known gaps concise and remove
  obsolete return wording.
- Documentation surface `docs/spec/workspace.md` current implementation:
  move return from the explicit gap list into the implemented draft surface.
- Documentation surface `docs/spec/workspace.md` target sections: change
  only if the implementation reveals a genuine normative contradiction.
- Documentation surface `docs/spec/language.md`: normally no change because
  early return is already normative.
- Documentation surface `docs/architecture.md`: update the current draft
  constructor list and any relevant convergence statement.
- Documentation surface `docs/roadmap.md`: mark the return vertical complete
  by removing it from Now rather than adding a history log.
- Documentation surface `docs/roadmap.md`: promote representative
  full-recomputation edit/query measurement to Now.
- Documentation surface `docs/roadmap.md`: retain unresolved/recovery
  states, public movement, and crate reassessment in sensible Next order.
- Documentation surface `README.md`: update only the active product summary
  or example if it is materially inaccurate.
- Documentation surface `docs/performance.md`: do not add unmeasured claims
  or raw samples.
- Documentation surface `docs/decisions/`: do not create a decision record
  for the obvious one-node convergence path.
- Documentation surface repository comments: document only non-obvious
  invariants not already encoded by types and tests.
- Documentation surface examples: do not add a redundant showcase when the
  retained fixture already owns the consumer.

## Explicit non-goals

- Do not implement source-free break.
- Do not implement source-free continue.
- Do not implement source-free general loop expressions.
- Do not add labelled control flow.
- Do not add exceptions.
- Do not add try/finally.
- Do not add defer.
- Do not add generators or coroutines.
- Do not add async control flow.
- Do not redesign `never`.
- Do not redesign function result typing.
- Do not add implicit type coercion.
- Do not broaden generic ownership/reference instantiation.
- Do not add generic patterns.
- Do not add Boolean, integer, or product source-free patterns.
- Do not add unresolved references.
- Do not add ambiguity nodes.
- Do not add conflict nodes.
- Do not add recovery nodes.
- Do not add public semantic movement.
- Do not add direct nominal-member mutation.
- Do not add source rendering.
- Do not replace the provisional importer notation.
- Do not add a new package format.
- Do not add a new CLI editing command.
- Do not add a wire protocol.
- Do not add a daemon.
- Do not add a session service.
- Do not add persistence.
- Do not add journaling.
- Do not add a database.
- Do not add collaboration.
- Do not add a CRDT.
- Do not add incremental recomputation.
- Do not add a cache framework.
- Do not add a scheduler.
- Do not add a plugin framework.
- Do not add a universal descriptor system.
- Do not add a rewrite DSL.
- Do not add a proof framework.
- Do not restore deleted Phase 2 components.
- Do not add another runtime engine.
- Do not add a public engine selector.
- Do not add a forced-native option.
- Do not add native fallback after entry.
- Do not broaden baseline native solely for ownership-control.
- Do not add optimizing JIT tiers.
- Do not add deoptimization.
- Do not add PGO.
- Do not add tracing GC.
- Do not add hidden reference counting.
- Do not expose raw pointers.
- Do not add a general `free`.
- Do not add arbitrary source or IR quotas.
- Do not add a nesting limit.
- Do not add a return-count limit.
- Do not add file-size or directory-shape policy.
- Do not split crates or modules to satisfy line-count aesthetics.
- Do not rewrite unrelated tests.
- Do not rewrite documentation for tone alone.
- Do not clean unrelated warnings or formatting outside touched files.
- Do not benchmark unrelated workloads.
- Do not commit raw logs or prompt files.
- Do not push unless the active invocation explicitly requests publication.

## Decision rules when evidence is ambiguous

- Prefer canonical source semantics over a convenient source-free shortcut.
- Prefer existing HIR authority over a new workspace-only semantic rule.
- Prefer one local helper over a new abstraction when only return uses it.
- Prefer a private helper over a public API when no external consumer needs
  it.
- Prefer structured existing type errors over formatted display-string
  errors.
- Prefer exact equality for return payload typing because source semantics
  require it.
- Prefer existing bottom-type assignability for the return expression
  itself.
- Prefer existing generic VM behavior over native expansion.
- Prefer current identity reconciliation over content fingerprints.
- Prefer one convergence test over duplicated source-free-only snapshots.
- Prefer extending an existing stress generator over creating a new one.
- Prefer deleting unsupported-return wording over adding historical
  explanation.
- Prefer advancing the roadmap over adding a completed-work archive.
- Prefer no performance claim over an unrepresentative microbenchmark.
- Prefer stopping over beginning the next roadmap item in the same turn.

## Suggested implementation sequence

1. Inspect worktree, branch, upstream, and recent commits.
2. Install or verify the companion root `AGENTS.md` replacement.
3. Read the authority files listed above.
4. Run the nearest existing focused source-free workspace tests unchanged.
5. Run the retained imported ownership-control target unchanged.
6. Locate current source return resolution and record exact semantics.
7. Locate every current HIR return consumer and confirm lower-layer
   completeness.
8. Locate the draft enum, child count, and child traversal.
9. Locate the lowering match and callable result-type authority.
10. Locate legal constructor filtering.
11. Locate node index and projection handling for imported return.
12. Locate convergence helpers and parser/source-load counters.
13. Form one falsifiable implementation hypothesis.
14. Add the smallest draft return representation.
15. Add child count and child traversal support.
16. Add direct lowering to canonical HIR return.
17. Validate non-divergent payload and exact callable result type.
18. Preserve payload effects and canonical partial effect inference.
19. Add return to legal constructor discovery through the smallest truthful
    model.
20. Compile the narrowest focused test.
21. Add basic main and function positive tests.
22. Add exact mismatch and nested-divergence negative tests.
23. Add unreachable-sequence and unreachable-while tests.
24. Add transaction failure-atomicity assertions.
25. Add query, containment, type, effect, and projection assertions.
26. Add source/import convergence for ownership-control.
27. Assert zero source loading and parsing for source-free construction.
28. Assert VM result and cleanup exactly once.
29. Run focused ownership and memory-plan tests if convergence reveals a
    mismatch.
30. Fix only the narrow lower-layer root cause if one exists.
31. Inspect the diff for accidental control-transfer generalization.
32. Delete stale unsupported-return code and prose.
33. Update status, workspace spec current section, architecture, and
    roadmap.
34. Update README only if active first-use claims are inaccurate.
35. Run formatting.
36. Run focused affected crate tests.
37. Run full Clippy, tests, and release build after the final relevant edit.
38. Run retained container verification when available.
39. Review `git diff --check` and the complete final diff.
40. Commit one coherent change if permitted.
41. Do not push unless requested.
42. Report the next item and stop.

## Focused commands to discover, not blindly copy

Use repository-native exact commands after inspecting package and test
names. The following command shapes are guidance. Do not invent a wrapper
merely to make them look uniform.

```sh
git status --short --branch
git log --oneline -12
rg -n "DraftNode|ExprKind::Return|NodeKind::Return|LegalConstructor" crates docs
rg -n "ownership-control|source.free|zero.*pars|parser.*count|compile_snapshot" crates docs
cargo test --quiet -p lkjscript-compiler <focused-test-filter> -- --exact
cargo test --quiet -p lkjscript-app <focused-test-filter> -- --exact
```

- Replace placeholders with verified current test names.
- Do not use an empty filter that accidentally runs nothing.
- Record expected and actual exit status for deliberate negative probes.
- Do not hide failures with shell operators.
- Do not rerun unchanged expensive commands without a relevant edit.

## Acceptance criteria

- Acceptance criterion: Root `AGENTS.md` is replaced by the supplied durable
  policy.
- Acceptance criterion: The timestamped prompt is not committed.
- Acceptance criterion: One active source-free early-return representation
  exists.
- Acceptance criterion: No parallel control-transfer representation exists.
- Acceptance criterion: Return has exactly one draft child.
- Acceptance criterion: Draft traversal remains flat, iterative, connected,
  acyclic, and single-parent.
- Acceptance criterion: Return payload type exactly equals enclosing
  callable result type.
- Acceptance criterion: A divergent return payload is rejected.
- Acceptance criterion: Return expression type is `never`.
- Acceptance criterion: Unreachable children after return are rejected in
  sequence and while bodies.
- Acceptance criterion: Return lowers directly to canonical HIR
  `ExprKind::Return`.
- Acceptance criterion: No source rendering or reparsing occurs.
- Acceptance criterion: No duplicate ownership or cleanup checker is added.
- Acceptance criterion: Ownership-control can be constructed source-free.
- Acceptance criterion: The source-free ownership-control snapshot compiles
  directly.
- Acceptance criterion: The source-free ownership-control execution returns
  `7`.
- Acceptance criterion: Its owned byte vector is cleaned exactly once.
- Acceptance criterion: Its temporary borrow does not outlive authority.
- Acceptance criterion: The generic VM remains a complete valid route.
- Acceptance criterion: No VM retry occurs after native entry.
- Acceptance criterion: Node query reports Return and `never`.
- Acceptance criterion: The payload query reports callable result
  expectation.
- Acceptance criterion: Legal constructor discovery advertises return
  truthfully.
- Acceptance criterion: Projection renders return without source attachment.
- Acceptance criterion: Imported and source-free semantic outcomes converge.
- Acceptance criterion: Invalid return transactions are failure-atomic.
- Acceptance criterion: Stable identity behavior follows existing
  replacement and hole rules.
- Acceptance criterion: No arbitrary quota is added.
- Acceptance criterion: No recursion on user-controlled draft depth is
  added.
- Acceptance criterion: No daemon, persistence, protocol, cache,
  incrementality, or broad framework is added.
- Acceptance criterion: Break and continue remain explicit gaps.
- Acceptance criterion: Current documentation is truthful and
  non-duplicative.
- Acceptance criterion: Roadmap Now advances to representative
  full-recomputation edit/query measurement.
- Acceptance criterion: Focused and full verification complete or exact
  environment blockers are reported.
- Acceptance criterion: One coherent commit exists when committing is
  permitted.
- Acceptance criterion: Work stops before the next vertical.

## Reversal and change-course conditions

- If HIR return is not actually complete on current HEAD, fix only the
  blocking canonical lower-layer defect required by the source-free
  convergence test.
- If source and source-free return semantics disagree, treat the accepted
  language specification and existing source behavior as evidence to
  reconcile, not as two modes to preserve.
- If adding one draft variant requires a broad public-model rewrite, stop
  and prove why the current model cannot represent truthful return before
  widening it.
- If legal constructor discovery cannot expose return without descriptor
  proliferation, prefer one simple Return case plus existing signature query
  and record the tradeoff.
- If a performance concern is only hypothetical, keep the direct
  implementation and defer optimization to the planned measurement turn.
- If a lower-layer test reveals an unrelated defect, record it for the
  roadmap unless it blocks this vertical.
- If HEAD already completed every acceptance criterion, verify the result
  and stop without beginning measurement or recovery-state work.
- If the environment cannot run required tests, complete all available
  checks and report the exact residual risk.
- If the worktree contains unrelated user changes, preserve them and isolate
  the task diff.
- If the companion AGENTS policy conflicts with a newer explicit user
  instruction, follow the newer instruction and report the conflict.

## Stop conditions

- Stop when all acceptance criteria relevant to the live checkout are met.
- Stop when the complete return vertical is coherent, tested, documented,
  and committed if permitted.
- Stop before implementing break.
- Stop before implementing continue.
- Stop before measuring representative recomputation beyond a small
  regression guard required by this change.
- Stop before introducing unresolved or recovery states.
- Stop before public movement.
- Stop before crate-boundary reassessment.
- Stop before persistence or daemon work.
- Stop rather than polishing unrelated code.
- Stop rather than creating a completed-work archive.
- Stop rather than adding speculative abstractions for the next turn.

## Final verification

After the final relevant edit, run at least:

```sh
cargo fmt --all -- --check
cargo clippy --quiet --workspace --all-targets --all-features --locked -- -D warnings
cargo test --quiet --workspace --all-targets --all-features --locked
cargo build --quiet --workspace --release --locked
```

Run retained container verification when available:

```sh
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
```

- Run the focused source-free return and ownership-control tests before the
  full suite.
- Run relevant ignored or release stress only when the shared iterative
  traversal changed or current policy requires it.
- Run Miri, sanitizer, fuzz, or allocation-failure coverage only when the
  touched boundary makes it relevant.
- Run `git diff --check`.
- Review the complete final diff for unrelated changes, stale prose,
  duplicate routes, prompt files, raw logs, and accidental API expansion.
- Verify exact command status; never infer success from missing output.

## Commit and publication

- Commit the root `AGENTS.md` replacement with the coherent implementation
  when committing is permitted.
- Do not commit `202608110802.md` or any copied transport prompt.
- Do not commit raw logs, raw benchmark samples, temporary plans, or
  generated scratch output.
- Use a semantic result commit message such as `Complete source-free early
  return authoring` if that accurately describes the final change.
- Do not split one dependency-closed vertical into arbitrary mechanical
  commits unless repository workflow requires it.
- Do not force push.
- Do not push unless the active invocation explicitly requests publication.
- If a push is requested, verify the local branch, tracking branch, and
  remote commit afterward.

## Required final report

- State the completed objective in one sentence.
- State the root cause of the missing capability.
- State the exact draft representation chosen.
- State how callable result type was derived.
- State how exact payload typing and divergent-payload rejection were
  implemented.
- State how the implementation converges on existing HIR return.
- State whether any lower-layer defect had to be fixed.
- State which paths or unsupported branches were deleted.
- State how ownership-control was constructed source-free.
- State the imported/source-free convergence dimensions proved.
- State the VM result and cleanup evidence.
- State parser and source-load evidence.
- State query, legal-constructor, projection, identity, and
  failure-atomicity evidence.
- State focused tests and exact outcomes.
- State full verification commands and exact outcomes.
- State environment-limited checks and residual risk.
- State documentation changes.
- State commit hash and publication state.
- Name the new roadmap Now item.
- Name the next highest-leverage problem not started.
- Explain why work stopped before beginning it.

Keep the final report compact and factual. Do not paste the prompt, complete
successful logs, full projections, full bytecode, or speculative future
designs.

## Final reminder

> The best result is not the largest patch. The best result is one direct,
> source-independent, ownership-correct, queryable, failure-atomic
> early-return vertical that removes one real blocker, reuses every mature
> lower layer, leaves one active architecture, advances the roadmap, and
> stops.

## Compact pre-commit audit

Use this checklist only to review the completed return vertical. It does not
authorize adjacent implementation.

- Name the single authoritative source-free return representation after the
  patch.
- Name the single canonical return semantics reused from source resolution.
- Show that no general control-transfer draft abstraction was added.
- Show that break and continue remain explicit gaps.
- Show that the return payload type comes from the enclosing callable
  signature.
- Show that exact payload equality is distinct from bottom-type root
  assignability.
- Show that a divergent payload is rejected before publication.
- Show that ordered children after return are rejected.
- Show that payload effects are preserved without inventing a process-exit
  effect.
- Show that ownership policy remains in the canonical ownership checker.
- Show that non-transferred owned locals are cleaned exactly once.
- Show that a moved return payload is not also cleaned by the callee.
- Show that native decline occurs only before entry and never duplicates
  effects.
- Show that the generic VM still executes the complete valid program.
- Show that source-free construction performs zero source loads and parses.
- Show that imported and source-free node kinds, types, effects, and
  execution converge.
- Show that return and payload identities follow ordinary replacement rules.
- Show that every invalid edit preserves snapshot and allocator state.
- Show that legal constructor discovery advertises return truthfully.
- Show that the payload expectation is discoverable without parsing display
  text.
- Show that public queries leak no dense IDs or private HIR types.
- Show that projection remains a review view rather than semantic authority.
- Show that no arbitrary size or nesting quota was added.
- Show that no user-depth recursion was added.
- Show that no cache, persistence, protocol, daemon, or incremental
  framework was added.
- Show that no crate or module split was performed for line-count
  aesthetics.
- Show that each changed file has a present producer and consumer in this
  vertical.
- Show that stale unsupported-return prose and code were deleted.
- Show that Current and Target documentation remain distinct.
- Show that roadmap Now advances to representative recomputation
  measurement.
- Show that no next-roadmap implementation began in this turn.
- Show the focused regression command and exact outcome.
- Show the full final verification commands and exact outcomes.
- Show any environment limitation without claiming the blocked check passed.
- Show that the timestamped prompt and raw logs are absent from the commit.
- Show one coherent commit and its publication state when committing is
  permitted.
- End the final report after naming the next highest-leverage problem not
  started.

If any answer requires speculative explanation instead of code, tests,
structured queries, or current documentation, simplify or strengthen the
evidence before committing.
