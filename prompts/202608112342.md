# lkjscript: Complete Source-Free Structured Loop Control

Repository: `lkjsxc/lkjscript`
Primary language: English
Task mode: autonomous, evidence-first, multi-turn engineering
Compatibility policy: no backward compatibility
Prompt artifact date: 2026-08-11
Observed repository head when this prompt was authored: `dcacca8ed6d86050c0a6ac40d830ecaa60443715`
Observed head subject: `Make retained verification faster and source-correct`
The observed commit is orientation, not a reset target.
Inspect the actual checkout before relying on any baseline statement below.
Do not reset, clean, rewrite, or discard unrelated work.

A companion replacement `AGENTS.md` is supplied with this prompt.
Install or retain that companion file at the repository root before substantive implementation.
Treat the root `AGENTS.md` as the stable repository-wide operating contract.
Treat this prompt as the task-specific objective and acceptance contract.
Do not commit this prompt.
Do not create a prompt archive, transcript, handoff file, or completion capsule in the repository.

The user grants broad authority to change semantics, APIs, module boundaries, crate boundaries, tests, examples, documentation, and implementation.
The user explicitly does not require backward compatibility.
The user prefers long-term correctness and simplicity over preserving any previous artifact.
That authorization permits decisive cutovers.
It does not require a broad rewrite.
Select the smallest dependency-closed design that completes the product vertical below.
Do not ask the user to select internal implementation details that can be decided from the checkout.
Ask only when a genuinely external requirement is missing and no safe assumption can complete the task.
This task is intentionally one turn in a longer sequence.
Complete this vertical.
Do not attempt the whole roadmap.
Stop before beginning the next independent vertical.

## Executive objective

Complete the source-free semantic-workspace control-transfer surface for lexical loops.

The expected complete vertical is:
- first-class source-free typed `loop` construction;
- first-class source-free `break` construction;
- first-class source-free `continue` construction;
- exact nearest-lexical-loop targeting;
- insertion into both newly drafted loops and already published surrounding loops;
- deterministic legal-constructor discovery with enough exact type context to author the next edit;
- source-import and source-free semantic convergence;
- exact ownership and cleanup behavior across loop exits and backedges;
- failure-atomic transactions;
- stack-safe traversal for user-controlled depth;
- direct snapshot compilation without source rendering or parsing;
- evaluator, VM, and selected product-path agreement where those routes apply;
- current documentation and roadmap truth; and
- no new daemon, service, persistence layer, wire protocol, public loop identity, or generic control graph.

Implement all three constructs together unless current executable evidence proves that one construct is already complete or that a smaller dependency-closed cut is strictly necessary.
Do not implement only a superficial `DraftNode` enum extension.
The result must work through construction, validation, query, projection, diff, compilation, ownership, execution, and documentation.
Do not call the vertical complete while a coding agent still needs to manufacture source text for ordinary typed loop control.

## Required result

At completion, a consumer of the public semantic workspace API must be able to:
1. create an empty workspace;
2. create a main function or ordinary function;
3. obtain a hole;
4. fill the hole with a flat source-free draft containing typed loops;
5. place `break` and `continue` in valid nested lexical positions;
6. receive deterministic rejection for invalid control placement or payload type;
7. query legal control constructors at a hole without reconstructing private HIR ancestry;
8. inspect resulting node kinds, types, effects, containment, projections, and semantic diffs;
9. compile the complete snapshot directly;
10. run the result through the reference evaluator and VM where available;
11. run through the selected product execution path where the existing product supports the program;
12. observe the same semantic result as an equivalent imported source program;
13. observe exact cleanup on `break`, `continue`, early return, and normal loop completion paths;
14. retain all old immutable snapshots;
15. observe no revision or identity allocation after a failed transaction; and
16. perform all source-free compilation with zero source-loading and zero parsing work.

A valid final result does not require:
- a semantic-workspace CLI;
- a JSON transaction schema;
- persistence;
- cross-process stable IDs;
- a daemon;
- an incremental recomputation engine;
- a public control-flow graph;
- user-addressable loop labels;
- arbitrary jumps;
- labelled break;
- a general tree-edit coordinate system;
- a rewrite DSL;
- a new crate;
- a compatibility layer; or
- a second compiler path.

## Why this is the selected vertical

The current checkout already contains the language, HIR, ownership, SSA, evaluator, bytecode, VM, and product-path meaning for loop control.
The source importer already resolves:
- typed `loop`;
- `while`;
- `break`;
- `continue`;
- early `return`; and
- nearest lexical loop targeting.

The semantic workspace already supports:
- source-free immutable snapshots;
- typed failure-atomic transactions;
- flat expression drafts;
- holes;
- direct snapshot compilation;
- source-free literals;
- selected operations;
- calls;
- conditionals;
- sequences;
- immutable locals;
- mutable locals;
- assignment;
- `while`;
- early `return`;
- products;
- enums;
- matches;
- deterministic node kinds;
- queries;
- projections;
- semantic diffs; and
- stable snapshot identities.

The observed source-free draft currently lacks:
- a typed `Loop` form;
- a `Break` form; and
- a `Continue` form.

The observed public `NodeKind` already contains `Loop`, `Break`, and `Continue` because imported programs can contain them.
The observed HIR already carries private `LoopId` values.
The observed source resolver already validates exact break payload types and nearest lexical targets.
The observed HIR traversal already understands the child shape of loop control.
The observed execution tests already establish evaluator and VM behavior for typed loop break, while break, and nested continue.
The missing capability is therefore a bounded semantic-authoring gap.
Closing it reuses existing canonical semantics.
It does not require a new platform layer.

This is higher leverage than inventing public movement, persistence, or a daemon without a current consumer.
It also improves agent efficiency because a model can express ordinary control intent directly instead of rendering provisional source text.
Do not turn that observation into an unmeasured provider-cost claim.
Treat any API-cost benefit as qualitative unless measured with explicit assumptions.

## Baseline evidence to verify

Before editing, verify every material baseline statement against the actual checkout.
Record disagreements in temporary working notes.
Do not create a repository document for the baseline audit.

Expected relevant files include:
- `AGENTS.md`;
- `README.md`;
- `docs/authority.md`;
- `docs/status.md`;
- `docs/architecture.md`;
- `docs/roadmap.md`;
- `docs/performance.md`;
- `docs/spec/language.md`;
- `docs/spec/workspace.md`;
- `crates/lkjscript-compiler/src/lib.rs`;
- `crates/lkjscript-compiler/src/analyze/resolution/control_transfer.rs`;
- `crates/lkjscript-compiler/src/hir/expressions.rs`;
- `crates/lkjscript-compiler/src/workspace/draft.rs`;
- `crates/lkjscript-compiler/src/workspace/model.rs`;
- `crates/lkjscript-compiler/src/workspace/index.rs`;
- `crates/lkjscript-compiler/src/workspace/query.rs`;
- `crates/lkjscript-compiler/src/workspace/projection.rs`;
- `crates/lkjscript-compiler/src/workspace/program.rs`;
- `crates/lkjscript-compiler/src/workspace/transaction.rs`;
- semantic-workspace tests;
- `crates/lkjscript-app/src/engine.rs`;
- `crates/lkjscript-app/tests/never_control.rs`;
- SSA control verification tests;
- ownership and cleanup tests;
- VM control lowering; and
- evaluator control execution.

Expected current source resolver laws to confirm:
- `break` is valid only inside a loop;
- `break` accepts exactly one value;
- a break value cannot already be divergent;
- a break value exactly equals the target loop result type;
- a `while` break carries `unit`;
- `continue` is valid only inside a loop;
- `continue` accepts no values;
- `loop` declares an explicit result type;
- a loop result type cannot contain `never`;
- the nearest lexical loop is selected; and
- an expression after a divergent control terminator in the same control body is invalid.

Expected current draft laws to confirm:
- expression drafts are flat and transaction-local;
- draft node IDs are dense transaction-local handles;
- recursive draft traversal is avoided;
- `While` allocates a private HIR loop ID;
- `Return` is checked against the enclosing callable result, not its immediate child expectation;
- sequences reject expressions after a `never` child;
- while bodies reject expressions after a `never` child;
- root `never` is assignable as divergent control;
- failed staging does not publish; and
- private loop IDs are not public semantic identities.

Expected current query laws to confirm:
- legal constructors are revision-labelled and paginated;
- legal constructors already include `While` and `Return`;
- return legality is context-sensitive;
- a legal-constructor candidate may be provisional where canonical ownership validation remains;
- legal-constructor ordering is deterministic; and
- query responses do not silently truncate.

Expected current documentation facts to confirm:
- source-free `break` and `continue` are listed as missing;
- source-free typed `loop` is absent from the current draft surface;
- direct snapshot compilation is implemented;
- no semantic wire protocol is current;
- no daemon or warm semantic service is current;
- persistence and collaboration are deferred;
- full semantic recomputation remains selected for ordinary edits;
- a narrow hole-refinement path exists; and
- current measurements do not justify general incrementality.

Expected current verification facts to confirm:
- the native workspace boundary is Cargo-based;
- retained container verification exists;
- recent work shortened the default verification critical path;
- the retained container build must be source-correct; and
- default tests should remain bounded while ignored release stress covers larger scale.

## Initial repository procedure

Perform this procedure before substantive edits.

1. Inspect the current branch.
2. Inspect the current worktree.
3. Inspect untracked files.
4. Inspect upstream tracking state.
5. Inspect recent commits affecting semantic workspace control, unresolved references, recomputation, and verification.
6. Read the companion root `AGENTS.md` completely.
7. Read the authority documents relevant to this vertical.
8. Search for every producer and consumer of `ExprKind::Loop`.
9. Search for every producer and consumer of `ExprKind::Break`.
10. Search for every producer and consumer of `ExprKind::Continue`.
11. Search for every match over `DraftNode`.
12. Search for every match over `LegalConstructor`.
13. Search for every match over `NodeKind`.
14. Search for loop-ID allocation and remapping.
15. Search for control-body reachability validation.
16. Search for divergent-replacement admissibility.
17. Search for hole-context ancestry logic.
18. Search for source-free convergence tests.
19. Search for ownership cleanup on loop exits and backedges.
20. Search for stack-safety tests for flat drafts and HIR traversal.
21. Run the smallest current tests that establish imported loop semantics.
22. Run the smallest current tests that establish source-free `while` and early `return` semantics.
23. Capture current behavior for invalid source-free control forms by writing a temporary focused test or experiment.
24. Remove temporary experiments that have no continuing regression value.
25. State the final implementation hypothesis, completion criteria, reversal condition, and stop condition in temporary working state.

Do not begin with a broad crate merge.
Do not begin with a daemon.
Do not begin with a CLI protocol.
Do not begin with persistence.
Do not begin with general incrementality.
Do not begin with performance optimization before correctness and convergence are established.

## Primary hypothesis

Use this as the default falsifiable hypothesis:

`Loop`, `Break`, and `Continue` can be added to the existing flat semantic expression draft and lowered into the existing canonical HIR by introducing one private, iterative lexical-control preflight that is seeded from the target node's already published enclosing loop ancestry.

The preflight should:
- assign private loop facts to draft `Loop` and `While` nodes;
- identify the nearest lexical loop for each draft `Break` and `Continue`;
- reject control transfer outside a loop;
- preserve exact break payload typing;
- avoid public loop IDs;
- avoid source rendering;
- avoid recursive traversal;
- preserve transaction failure atomicity; and
- feed the existing lowerer without creating a second semantic authority.

This hypothesis is guidance, not an instruction to force a particular helper type.
A smaller design is preferred when it satisfies all acceptance criteria.
A different design is allowed when focused evidence proves it is clearer or safer.

Reject this hypothesis and choose another local mechanism when:
- current HIR no longer has canonical loop control;
- a current helper already provides exact lexical-control ancestry;
- the proposed preflight duplicates a canonical validator;
- it cannot handle a draft inserted inside an existing surrounding loop;
- it requires public stable loop identity;
- it weakens failure atomicity;
- it introduces recursive depth dependence;
- it creates inconsistent loop IDs across source import and source-free construction in a way that affects semantics; or
- it is larger than a direct extension of an existing control-context traversal.

Do not reject the product objective merely because the default mechanism changes.

## Binding semantic contract


### Typed loop

Add a first-class source-free typed loop construct.
The public draft must carry enough source-independent semantic information to determine its result type exactly.
The expected default public shape is conceptually:
`Loop { result_type: SemanticType, body: Vec<DraftNodeId> }`.
The exact Rust spelling may differ when a clearer current API convention exists.
Do not use source spelling as the type contract.
Do not use a text type form.
Do not expose a HIR `LoopId`.
Do not require a caller to allocate a loop identity.
Do not infer the result type from an arbitrary first break.
Do not select a result type by candidate order.
Resolve the semantic type through the canonical workspace type resolver.
Validate namespace and generation for nominal and binder identities in the declared type.
Reject a result type containing `never` using the same semantic law as source import.
Reject a result type that is invalid in the enclosing callable context.
Reject a stale or foreign type identity.
Use a private HIR loop identity allocated during transaction staging.
A failed transaction must not publish or reserve that private identity.
A loop body is an ordered control body.
A loop body may be empty when current language semantics permit an infinite loop.
A loop body may end with a divergent child.
An expression after a divergent child in the same loop body is invalid.
The loop expression's semantic type is the declared result type.
The loop expression may diverge.
Its effects include the effects of all body expressions and canonical loop divergence facts.
Do not manually invent an effect rule that conflicts with canonical effect inference.
A complete loop with no statically evident break may still be well typed if current semantics permit it.
Do not add a mandatory-break reachability proof unless the language specification already requires one.
Do not add labelled loops.
Do not add arbitrary loop targets.
Do not add public block identity.
Do not add general jump semantics.

### Break

Add a first-class source-free `break` construct.
The expected default public shape is conceptually:
`Break { value: DraftNodeId }`.
The caller does not provide the target loop.
The target is the nearest valid lexical loop.
The target may be:
- a typed loop created inside the same draft;
- a while created inside the same draft;
- a typed loop already present around the replacement target; or
- a while already present around the replacement target.
A nested draft loop shadows any enclosing published loop.
A nested draft while shadows any enclosing typed loop.
Leaving the nested draft loop restores the previous context.
The break value must be lowered before break publication.
A break value cannot have type `never`.
A break value must exactly equal the target result type.
Do not use lossy conversion.
Do not use broad assignability when the source language requires exact equality.
For a while target, the exact result type is `unit`.
A non-unit break from a while is invalid.
The break expression has type `never`.
The break expression carries the value's effects and canonical divergence/control effects.
A break must be rejected outside any lexical loop.
A break in a value-required child position must be rejected unless existing divergent-control admissibility explicitly permits it.
A break may be used where divergent control is an admissible branch or final control-body result.
An expression after break in the same sequence or control body is invalid.
Do not recover an invalid break by targeting an outer function, returning a value, trapping, or selecting a loop by name.
Do not expose the target loop as a public reference edge.
Do not create a stable semantic identity for the target loop.
Do not serialize private loop identity.

### Continue

Add a first-class source-free `continue` construct.
The expected default public shape is conceptually:
`Continue`.
The caller does not provide a target.
The target is the nearest valid lexical loop.
The same nested and enclosing-loop rules used for break apply.
Continue is valid in both typed loops and whiles.
Continue accepts no value.
Continue has type `never`.
Continue carries canonical divergence/control effects.
Continue is rejected outside any lexical loop.
Continue in a value-required child position is rejected unless divergent control is admissible there.
An expression after continue in the same sequence or control body is invalid.
Continue must execute all cleanup required before the selected loop backedge.
Continue must not clean values whose lifetime crosses the selected loop backedge.
Continue must not target an outer loop when a valid inner loop exists.
Do not add a public target identity.
Do not add labelled continue.
Do not encode continue as break plus a hidden sentinel.
Do not encode continue as a trap.
Do not encode continue as a synthetic call.

### Lexical target context

The semantic-workspace lowerer must know the lexical loop context for each control-transfer node.
This context is private derived staging state.
It is not semantic authority.
It is not persisted.
It is not exposed in the public API.
It is not a workspace identity.
It is not a query continuation.
It is not a source attachment.
It must be deterministically reconstructible from:
- the immutable base snapshot;
- the target node address; and
- the proposed flat draft.

The context must include at least the semantic facts needed to lower control transfer:
- private HIR loop identity;
- exact result type; and
- whether the target is a while when that affects validation.

Seed draft lowering from the existing lexical loop ancestry of the target expression.
Use the immutable base snapshot or another authority-correct pre-edit view.
Do not infer outer context from rendered projection text.
Do not infer outer context from node names.
Do not infer outer context from public node IDs alone without validated ancestry.
Do not create a public path API merely to obtain this private fact.
Do not create generic movement coordinates.
Do not expose private preorder addresses.

Handle replacement inside existing control flow.
A draft `break` or `continue` inserted into an existing loop body must target that existing loop when no nearer draft loop exists.
A draft inserted outside a loop must reject break and continue.
A draft nested inside a newly proposed loop must target the newly proposed loop.
A deeper nested loop must win.
After leaving a nested loop, the previous target must be restored.

The control-context traversal must be iterative for user-controlled depth.
Do not use recursive descent over arbitrary expression depth.
Use checked arithmetic for preorder ordinals, child counts, and work-stack growth.
Return a typed host/allocation failure rather than panicking.
Do not impose an arbitrary semantic nesting quota.

### Existing surrounding edits

Inspect transaction overlap and structural-edit rules before choosing the ancestry source.
Do not assume edit order gives semantic meaning.
Do not allow one edit to replace a loop while another edit relies on the displaced loop as a control target unless the final-graph transaction semantics explicitly support that combination.
Prefer rejecting overlapping or dependency-conflicting structural edits through existing canonical transaction rules.
Do not add edit-order dependence.
Do not add a hidden intermediate revision.
Do not publish a break whose loop target is deleted by the same transaction.
Do not silently retarget such a break to an outer loop.
Validate the intended final semantic graph.
Keep failure atomicity exact.

### Source-import convergence

Source import and source-free construction must converge on the same canonical HIR meaning.
Equivalent programs must agree on:
- expression kinds;
- expression types;
- effect summaries;
- lexical target selection;
- ownership planning;
- cleanup actions;
- verified SSA control flow;
- evaluator outcome;
- validated bytecode outcome;
- reference VM outcome; and
- selected product-path outcome where supported.

Private dense IDs need not be numerically equal across construction routes.
Compare semantics, not incidental dense numbering.
Normalize or compare through existing semantic views and test oracles.
Do not introduce a new canonical serialization solely for convergence testing.
Do not compare only source text.
Do not render the source-free snapshot and reimport it.
Direct `compile_snapshot` must remain the tested compilation route.

## Public draft API

Extend the current flat `ExpressionDraft` model.
Preserve transaction-local `DraftNodeId` semantics.
Do not make draft node IDs stable workspace identities.
Do not introduce recursive public draft boxes merely for loop control.
Do not introduce an AST hierarchy.
Do not introduce a generic control-flow graph.
Do not introduce a builder framework unless multiple current forms prove it is smaller than direct variants.

Update every exhaustive or semantically relevant match over `DraftNode`, including:
- child counts;
- child traversal;
- shape validation;
- connectivity validation;
- acyclicity validation;
- lowering order;
- lexical binding scope analysis;
- lexical control-context analysis;
- stack-safe drop or clone behavior where relevant;
- deterministic work counters where retained;
- test fixture construction; and
- public exports or documentation.

Preserve the flat draft invariant:
- every child ID refers to a node in the same draft;
- the graph is connected from the root;
- no child has more than one semantic parent unless the existing draft contract explicitly permits sharing;
- cycles are invalid;
- lowering order is deterministic; and
- user-controlled depth does not consume the native call stack.

Do not retain obsolete API forms for compatibility.
Update all current callers directly.
Delete any temporary adapters after the cutover.

## Lowering design constraints

Use the existing canonical HIR `ExprKind::Loop`, `ExprKind::Break`, and `ExprKind::Continue`.
Do not create parallel semantic variants.
Use the existing HIR `LoopId` or its current equivalent.
Keep that identity private.
Use existing type conversion and validation helpers.
Use existing effect inference or canonical effect construction.
Use existing divergent-control admissibility rules.
Use existing ownership validation.
Use existing HIR consistency validation.
Use existing index construction.
Use existing semantic diff machinery.
Use existing compiler pipeline.

A likely small design is:
1. derive the enclosing published loop context at the replacement address;
2. perform one iterative enter/exit walk over the flat draft;
3. allocate private loop IDs for draft `Loop` and `While` nodes;
4. record the nearest loop facts for draft `Break` and `Continue` nodes;
5. reject invalid placement before final lowering;
6. lower children in existing deterministic order;
7. lower each loop/control node using the precomputed private facts;
8. run canonical final validation; and
9. discard all private staging facts after success or failure.

This is not the only allowed design.
Do not force it when an existing traversal can be extended more cleanly.

Whichever design is selected must:
- avoid recursive arbitrary-depth traversal;
- avoid a second semantic authority;
- avoid public target identity;
- handle existing outer loops;
- handle newly drafted nested loops;
- make nearest-target selection deterministic;
- preserve exact error classification;
- preserve failure atomicity;
- avoid edit-order dependence;
- avoid re-rendering source; and
- remain smaller than a general control framework.

## Type resolution

Resolve a draft loop result type through the canonical workspace semantic type path.
Do not trust a `SemanticType` merely because its enum shape is valid.
Validate:
- namespace;
- generation;
- owner for type parameters;
- visibility where applicable;
- nominal constructor identity;
- generic argument arity;
- recursive type legality;
- reference-result restrictions where applicable;
- `never` containment; and
- current callable context.

Use the same semantic equality law as imported control transfer.
Do not compare display strings.
Do not compare source spellings.
Do not silently coerce.
Do not default an unknown type to `unit`.
Do not infer a loop result from the root expected type unless that becomes an explicit, documented public draft contract and remains exact for nested loops.
The default design should keep the explicit semantic result type in the loop draft.

When break targets a while:
- required value type is exactly `unit`;
- a `never` value is invalid; and
- a non-unit value is invalid even when the surrounding expression expects another type.

When break targets a typed loop:
- required value type is exactly the declared loop result type;
- nested type identities must be validated;
- generic type parameters must belong to the correct callable; and
- affine payload ownership must follow the canonical ownership path.

Do not add a special break-only type system.
Do not duplicate generic substitution logic.
Do not bypass ownership because break is divergent.

## Effect and reachability semantics

Use canonical effect facts.
Do not make loop control appear pure when it may diverge.
Do not drop child effects.
Do not add host I/O or trap effects that are not present.
Do not infer execution frequency from static effect sets.

Reachability rules must match imported semantics.
Reject an expression after a divergent expression in:
- a sequence;
- a while body;
- a typed loop body;
- any other control body governed by the same canonical rule.

A divergent final child is allowed only where current HIR control reconstruction permits it.
A break or continue used as:
- an operation argument;
- a call argument;
- a local initializer;
- a mutable-local initial value;
- a product field value;
- an enum field value;
- a condition; or
- another value-required child
must be rejected unless the current language and canonical divergent-admissibility logic explicitly permits that position.

Do not add ad hoc per-variant exceptions when an existing HIR control-context validator can own the rule.
Add focused regressions for every newly exposed divergent placement path.

## Ownership and cleanup

Loop control must preserve collector-free ownership semantics.
Test and inspect cleanup across:
- normal loop body completion;
- while backedge;
- typed-loop backedge;
- continue to while backedge;
- continue to typed-loop backedge;
- break from while;
- break from typed loop;
- nested break;
- nested continue;
- early return from inside a loop;
- trap from inside a loop;
- allocation failure inside a loop where supported;
- resource failure inside a loop where supported; and
- enclosing-scope teardown after loop result publication.

At minimum, create source-free convergence coverage using an affine byte vector or another existing ownership-sensitive value.
Demonstrate:
- no leak;
- no double drop;
- no live loan after exit;
- no stale owner;
- no duplicated side effect;
- no missing cleanup; and
- the same cleanup observations as equivalent imported source.

Use existing memory-plan and runtime observations.
Do not add a new ownership instrumentation framework.
Temporary counters are allowed during diagnosis.
Remove temporary counters unless they provide a continuing regression oracle.
Do not add reference counting.
Do not add a tracing collector.
Do not bypass cleanup by lowering break or continue to a raw jump before ownership planning.

## Identity, snapshots, and diffs

Loop control introduces no new public semantic identity category.
Draft loop handles remain transaction-local.
HIR loop IDs remain private and dense.
Public `NodeId` behavior follows existing expression-node identity laws.

Verify:
- replacing a hole root with a loop preserves the intended root identity rule;
- descendant node identities are allocated deterministically;
- old snapshots remain valid;
- deleted descendants become stale only in later snapshots;
- failed invalid control edits publish no revision;
- failed invalid control edits consume no stable entity IDs;
- failed invalid control edits consume no stable node IDs;
- future allocation order is unchanged after failure;
- semantic diffs report the actual new node kinds;
- containment includes loop, break value, and surrounding children correctly;
- no public reference edge is invented for a loop target; and
- source-free control nodes participate in projection exactly like imported control nodes.

Do not add a public loop-target edge.
Do not overload value-reference edges.
Do not expose private preorder positions.
Do not retain old node IDs through a replacement when the existing identity law says the node was replaced.
Follow the current canonical identity reconciliation path.

## Legal constructors and agent-facing query

Extend deterministic legal-constructor discovery so an agent can discover loop control without reconstructing private HIR ancestry.

The expected constructor surface includes:
- `Loop` with an exact proposed result type or an equivalently compact typed view;
- `Break` with the exact required payload type or an equivalently compact typed view; and
- `Continue`.

The exact public Rust shape may differ.
The query must expose enough semantic information to construct the next valid edit.
For break, a bare untyped marker is insufficient when the nearest typed loop may require an arbitrary result type.
Do not require a model to:
- walk private HIR;
- inspect source text;
- infer a loop result from rendering;
- guess whether the target is a while; or
- issue several unrelated queries to recover one exact payload type.

A reasonable result shape is conceptually:
`Break { value_type: SemanticType }`.
A reasonable loop constructor result is conceptually:
`Loop { result_type: SemanticType }`.
The query may normally choose the hole's exact expected type when that is the canonical legal loop result for the position.
A reasonable continue marker is conceptually:
`Continue`.
Use a different shape when it is simpler and equally exact.

Legal-constructor availability must be context-sensitive.
Advertise `Break` only when:
- a nearest lexical loop exists;
- divergent replacement is admissible at the hole;
- the required result type can be represented exactly; and
- the current snapshot facts are sound.

Advertise `Continue` only when:
- a nearest lexical loop exists; and
- divergent replacement is admissible at the hole.

Advertise `Loop` when:
- a typed loop can be authored in the hole's semantic context;
- the selected result type contract is representable; and
- `never` containment rules are satisfied.

Do not advertise an illegal constructor and rely on later rejection when the query already has exact facts.
Where ownership validation necessarily remains proposal-dependent, label provisional status accurately.
Break and continue placement is not inherently provisional when lexical context is already exact.

Keep query output:
- deterministic;
- revision-labelled;
- paginated;
- stably ordered;
- structurally typed;
- compact; and
- free of silent truncation.

Changing `LegalConstructor` from `Copy` to a non-`Copy` exact typed value is allowed.
Backward compatibility is irrelevant.
Do not retain a parallel legacy constructor enum.
Update all current callers and tests directly.
When deterministic ordering can no longer be derived automatically, implement one explicit stable order.
Do not sort by debug text.
Do not sort by pointer address.
Do not rely on hash iteration order.

This query improvement is the main low-risk API-cost optimization in this turn.
It reduces semantic reconstruction work for future agents.
Do not claim a monetary saving without a measured workload and provider assumptions.
Do not add provider-specific token logic.

## Projection and inspection

Inspect whether existing projection logic already handles imported loop-control nodes generically.
Do not duplicate it when it is already correct.
Add only missing semantic details required for exact review.

A projection containing loop control must preserve:
- node kind;
- node identity;
- owner;
- child order;
- actual type;
- expected type where meaningful;
- effect summary;
- break payload child;
- loop condition child for while;
- body order; and
- source-free versus imported semantic equivalence.

Do not expose private loop IDs in projection.
Do not render a synthetic label.
Do not add a second projection format.
Do not return full-program projections when a selected subtree is sufficient.
Verify bounded output and continuation behavior where current projection APIs paginate.

## Diagnostics and errors

Use existing `WorkspaceError` categories when they accurately represent the failure.
Add a new structured variant only when current variants lose material machine-readable meaning.
Do not add a new error taxonomy for three constructs.
Do not return only a debug string when a current typed category exists.

Required deterministic failures include:
- break outside a loop;
- continue outside a loop;
- wrong break payload type;
- non-unit break from while;
- divergent break payload;
- loop result containing `never`;
- stale nominal type in loop result;
- foreign nominal type in loop result;
- wrong-owner type parameter in loop result;
- unreachable expression after break;
- unreachable expression after continue;
- break in an inadmissible value child;
- continue in an inadmissible value child;
- stale draft child ID;
- disconnected draft control node;
- cyclic draft;
- duplicate parent where forbidden;
- allocation failure while building control context;
- arithmetic overflow while counting or indexing;
- stale target node;
- stale base revision;
- foreign hole;
- overlapping incompatible structural edits; and
- final HIR inconsistency.

Diagnostics must not mention source positions for a source-free draft unless a real source attachment exists.
Diagnostics may identify stable entities and nodes.
Diagnostics must not expose private HIR IDs.
Diagnostics must be deterministic.
Do not recover by guessing.
Do not publish a partially lowered control structure.

## Failure atomicity

Every invalid control transaction must be failure-atomic.
Test at least one failure in each major stage:
- draft shape;
- lexical control context;
- semantic type resolution;
- break payload validation;
- divergent placement;
- ownership validation; and
- final consistency validation when a focused injection facility already exists.

After each failure, verify:
- current snapshot pointer or equivalent immutable state is unchanged;
- revision is unchanged;
- entity inventory is unchanged;
- node inventory is unchanged;
- blockers are unchanged;
- diagnostics are unchanged;
- allocator state is unchanged;
- the next successful transaction receives the same stable identities it would have received without the failure;
- old snapshots still answer queries; and
- no derived cache was mutated.

Do not add a rollback journal merely to obtain atomicity.
Use existing stage-then-publish transaction architecture.
If existing code mutates before validation, correct the root architecture rather than adding compensating cleanup.

## Stack safety and complexity

User-controlled nesting must not consume unbounded native stack.
Cover:
- draft shape validation;
- draft control-context analysis;
- draft lowering;
- HIR clone;
- HIR replacement;
- indexing;
- projection;
- semantic comparison;
- compilation; and
- destruction.

Use iterative work stacks.
Use checked capacity growth.
Return host allocation errors.
Do not impose an arbitrary nesting limit.
Do not convert a stack overflow into a language semantic error.

Add a fast default stack-safety case that exercises the same algorithm at moderate depth.
Add or extend an ignored locked-release stress test for substantially deeper control nesting when current test conventions support it.
Do not put an enormous compile workload in the default suite.
Do not claim application performance from generated depth stress.

Use deterministic work counters when practical to establish linear or otherwise intended scale shape.
A useful counter may include:
- draft nodes entered;
- draft nodes exited;
- loop contexts pushed;
- control transfers resolved;
- existing HIR nodes scanned for ancestry; and
- lowered nodes.

Do not retain counters without a continuing test or diagnostic consumer.
Do not add a general instrumentation framework.
Reject a design that repeatedly scans the entire draft once per break or continue.
Reject a design that repeatedly scans the entire enclosing HIR once per control node.
Prefer one ancestry derivation and one draft walk per replacement.
Measure or count enough to detect accidental quadratic behavior.

## No arbitrary validity limits

Do not add semantic quotas for:
- loop nesting;
- break count;
- continue count;
- body length;
- draft node count;
- node depth;
- type depth;
- local count;
- function size; or
- control-context size.

Checked host allocation failure is allowed.
Explicit untrusted product policy is allowed only at a real product boundary.
The semantic workspace is not an excuse to reject a valid trusted program because a fixed vector reserve looks convenient.
Do not raise an existing arbitrary quota and call the problem solved.
Remove any obsolete quota encountered on the selected path when the dependency-closed correction is safe.

## Implementation map

The following map is advisory.
Follow actual ownership discovered in the checkout.
Do not edit every listed file merely to satisfy the prompt.

`crates/lkjscript-compiler/src/workspace/draft.rs`
- add exact flat draft forms;
- update child count;
- update child iteration;
- preserve non-recursive operations;
- update public documentation; and
- update any helper constructors only when they reduce current call-site noise.

`crates/lkjscript-compiler/src/workspace/transaction.rs`
- derive existing enclosing loop context;
- derive nested draft loop context;
- allocate private loop IDs;
- lower typed loop;
- lower break;
- lower continue;
- preserve exact type/effect rules;
- preserve reachability;
- preserve ownership;
- preserve failure atomicity; and
- update retained deterministic work measurement only when justified.

`crates/lkjscript-compiler/src/workspace/query.rs`
- extend legal constructors;
- expose exact break payload type;
- make ordering deterministic;
- reuse divergent-position checks;
- derive nearest loop context without public coordinates;
- update paging/query keys only when shape semantics require it; and
- update tests.

`crates/lkjscript-compiler/src/workspace/model.rs`
- avoid changes unless a public view genuinely belongs here;
- do not add public loop identity;
- preserve existing `NodeKind` when already complete.

`crates/lkjscript-compiler/src/workspace/index.rs`
- verify imported and source-free control nodes index identically;
- update only if new source-free nodes reveal missing expected-type or effect facts.

`crates/lkjscript-compiler/src/workspace/projection.rs`
- verify exact review output;
- update only missing control-specific presentation facts;
- keep output bounded.

`crates/lkjscript-compiler/src/hir/expressions.rs`
- reuse current canonical forms;
- change only when an existing stack-safe helper is missing or incorrect;
- do not fork HIR semantics.

`crates/lkjscript-compiler/src/analyze/resolution/control_transfer.rs`
- treat current imported semantics as an executable oracle;
- refactor shared validation only when it produces one smaller canonical rule;
- do not make source import depend on the semantic-workspace draft API.

Compiler, ownership, SSA, evaluator, bytecode, VM, JIT, and app files
- change only when convergence exposes a real missing consumer;
- do not broaden the native backend merely because a fixture declines;
- preserve pre-entry fallback laws.

Documentation
- update current and target truth;
- remove closed gaps;
- do not add a task report.

Tests
- prefer focused tests near the owner;
- add end-to-end convergence at the existing product boundary;
- avoid duplicate fixtures that assert the same layer.

## Test strategy

Use the smallest test at the layer that owns each invariant.
Do not place every assertion in one giant integration test.
Do not assert private topology when semantic outcomes suffice.
Do not weaken existing tests.
Delete obsolete tests that preserve an old public API shape.

Required test classes:
- public draft shape;
- lexical context;
- exact typing;
- divergent placement;
- source-free completion;
- query discovery;
- projection;
- semantic diff;
- source convergence;
- ownership convergence;
- evaluator convergence;
- VM convergence;
- selected product-path convergence;
- failure atomicity;
- old-snapshot validity;
- stale identity;
- foreign identity;
- wrong owner;
- deep stack safety;
- deterministic work shape; and
- zero parser/source-loading work.

## Detailed acceptance cases


### Case 1: Source-free typed loop returns an integer

Purpose: Establish the minimal complete typed-loop authoring route.
Setup: Create an empty workspace and a main returning `i64`.
Action: Fill the main hole with a typed loop whose body breaks with `42`.
Required assertions:
- The transaction succeeds and publishes one revision.
- The loop node has semantic type `i64`.
- The break node has semantic type `never`.
- The break payload has semantic type `i64`.
- Direct snapshot compilation succeeds.
- The evaluator returns `42`.
- The VM returns `42`.
Primary layer: workspace unit plus compiler/app convergence.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 2: Source-free while break carries unit

Purpose: Establish the canonical while exit rule.
Setup: Create a main returning `i64` with a sequence containing a while and a final literal.
Action: Use `break unit` as the final while-body expression.
Required assertions:
- The while has semantic type `unit`.
- The break targets the while.
- The break payload is exactly `unit`.
- The final sequence result is the trailing `i64` literal.
- Evaluator and VM outcomes agree.
Primary layer: workspace and end-to-end.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 3: Source-free continue targets the nearest while

Purpose: Establish ordinary continue lowering.
Setup: Create a mutable integer counter inside a while.
Action: Increment the counter and continue while the condition remains true.
Required assertions:
- Continue has semantic type `never`.
- Continue targets the while private loop identity.
- No expression follows continue in the same body.
- The program terminates through the condition.
- Evaluator and VM outcomes agree.
Primary layer: workspace, SSA, evaluator, VM.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 4: Nested continue targets the inner loop

Purpose: Prove nearest lexical target selection.
Setup: Create a typed outer loop containing an inner while.
Action: Place continue inside the inner while and break the outer loop after the while.
Required assertions:
- Continue targets the inner while.
- Continue does not target the outer typed loop.
- The outer break remains reachable after the inner while terminates.
- The final result matches imported source.
Primary layer: workspace convergence.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 5: Nested break targets the inner typed loop

Purpose: Prove lexical shadowing for break.
Setup: Create an outer typed loop returning `i64` and an inner typed loop returning `bool`.
Action: Break the inner loop with `true`, use its result, then break the outer loop with an integer.
Required assertions:
- The inner break requires `bool`.
- The outer break requires `i64`.
- No public target identity is exposed.
- Both loop result types are exact.
- The final result is correct.
Primary layer: workspace and query.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 6: Break inserted into an existing published while

Purpose: Prove seed context from the base snapshot.
Setup: Import or construct a complete while whose final body expression is a replaceable unit node.
Action: Replace that unit node with a source-free `break unit` draft.
Required assertions:
- The break resolves to the surrounding published while.
- No new public loop identity is created.
- The transaction succeeds.
- Direct snapshot compilation succeeds.
Primary layer: workspace replacement.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 7: Continue inserted into an existing published loop

Purpose: Prove seed context for continue.
Setup: Use an existing loop with a replaceable final body expression and a terminating condition or outer exit.
Action: Replace the expression with source-free continue.
Required assertions:
- Continue resolves to the nearest existing loop.
- The replacement does not use source text.
- The resulting control flow validates.
- The outcome matches an equivalent imported program.
Primary layer: workspace replacement and convergence.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 8: Draft loop shadows existing published loop

Purpose: Prove nested draft context takes precedence over seeded context.
Setup: Choose a replacement target inside an existing outer loop.
Action: Insert a new typed draft loop containing break and continue.
Required assertions:
- Inner control transfers target the new draft loop.
- Control after the inner loop can still target the published outer loop.
- Context push and pop are deterministic.
Primary layer: workspace lexical context.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 9: Break outside any loop is rejected

Purpose: Protect lexical validity.
Setup: Create a main hole outside loop control.
Action: Attempt to fill it with a break draft.
Required assertions:
- The transaction fails with a deterministic control-context error.
- No revision is published.
- No stable identity is consumed.
- The original hole remains queryable.
Primary layer: workspace failure atomicity.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 10: Continue outside any loop is rejected

Purpose: Protect lexical validity.
Setup: Create a main hole outside loop control.
Action: Attempt to fill it with continue.
Required assertions:
- The transaction fails deterministically.
- No revision is published.
- No stable identity is consumed.
- The original snapshot remains valid.
Primary layer: workspace failure atomicity.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 11: Wrong typed-loop break payload is rejected

Purpose: Protect exact break typing.
Setup: Create a typed loop declared to return `i64`.
Action: Break with a boolean.
Required assertions:
- The transaction fails with expected `i64` and actual `bool` facts.
- No coercion occurs.
- No revision is published.
Primary layer: workspace type validation.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 12: Non-unit while break is rejected

Purpose: Protect the while-specific result law.
Setup: Create a while body.
Action: Break with an integer.
Required assertions:
- The transaction fails.
- The error identifies the exact unit requirement.
- The break is not retargeted to an outer typed loop when the while is nearer.
Primary layer: workspace lexical typing.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 13: Divergent break value is rejected

Purpose: Protect canonical never semantics.
Setup: Create a loop and a nested expression that already has type `never`.
Action: Use that expression as the break payload.
Required assertions:
- The transaction fails before publication.
- The error states that the break value is already divergent or the current structured equivalent.
- No hidden payload is fabricated.
Primary layer: workspace control validation.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 14: Loop result containing never is rejected

Purpose: Match imported type-form semantics.
Setup: Construct a semantic loop result type such as `list never` or another type containing never.
Action: Attempt to author the loop.
Required assertions:
- The transaction fails deterministically.
- The result is not defaulted or rewritten.
- The failure is atomic.
Primary layer: workspace type validation.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 15: Foreign nominal loop result is rejected

Purpose: Protect namespace identity.
Setup: Create two workspaces and a product in the first.
Action: Use the first workspace's product identity as a loop result type in the second.
Required assertions:
- The transaction fails with a foreign-namespace error.
- No revision is published.
- Both workspaces remain usable.
Primary layer: workspace identity.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 16: Stale nominal loop result is rejected

Purpose: Protect generation validity.
Setup: Create and delete a nominal type, retaining its old identity.
Action: Use the stale identity in a loop result type.
Required assertions:
- The transaction fails with stale identity.
- Slot reuse does not make the stale identity valid.
- No revision is published.
Primary layer: workspace identity.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 17: Wrong-owner type parameter loop result is rejected

Purpose: Protect generic binder ownership.
Setup: Create two generic functions with distinct type parameters.
Action: Use one function's type parameter as a loop result in the other.
Required assertions:
- The transaction fails with wrong owner or the canonical equivalent.
- The type parameter is not matched by name.
- No revision is published.
Primary layer: workspace generic type validation.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 18: Unreachable expression after break is rejected

Purpose: Protect control-body reachability.
Setup: Create a loop body with break followed by a literal.
Action: Apply the draft.
Required assertions:
- The transaction fails.
- The later literal is not silently dropped.
- No partial loop is published.
Primary layer: workspace reachability.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 19: Unreachable expression after continue is rejected

Purpose: Protect control-body reachability.
Setup: Create a while body with continue followed by assignment.
Action: Apply the draft.
Required assertions:
- The transaction fails.
- The assignment is not silently dropped.
- No partial while is published.
Primary layer: workspace reachability.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 20: Break in an operation argument is rejected

Purpose: Protect divergent-child admissibility.
Setup: Create a loop and an arithmetic operation.
Action: Use break as an operation argument.
Required assertions:
- The transaction fails unless current canonical semantics explicitly permit the position.
- The operation is not published with a missing operand.
- Failure is atomic.
Primary layer: workspace control context.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 21: Continue in a local initializer is rejected

Purpose: Protect storage from never.
Setup: Create a loop with an immutable or mutable local.
Action: Use continue as the initializer.
Required assertions:
- The transaction fails.
- No local binding entity is published.
- No allocator state is consumed.
Primary layer: workspace binding and control.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 22: Break as a divergent if branch is accepted

Purpose: Protect valid never joins.
Setup: Create a typed loop with an if expression.
Action: Use break in one branch and a value of the loop's body context in the other where canonical joining permits.
Required assertions:
- The if type joins according to current never semantics.
- The break targets the surrounding loop.
- The program matches imported source behavior.
Primary layer: workspace type join.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 23: Continue as a final conditional branch is accepted

Purpose: Protect valid divergent branch placement.
Setup: Create a while with a conditional as its final body expression.
Action: Use continue in one branch and unit in the other.
Required assertions:
- The conditional joins to unit through never semantics.
- Continue targets the while.
- The program validates and executes correctly.
Primary layer: workspace control join.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 24: Break payload moves an affine value

Purpose: Protect ownership across typed-loop exit.
Setup: Create a typed loop returning an affine value supported by current language semantics.
Action: Move the value into break.
Required assertions:
- The move occurs exactly once.
- The exited loop does not drop the moved value.
- The receiving scope owns the result.
- No leak or double drop occurs.
Primary layer: ownership, memory plan, VM.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 25: Continue cleans an iteration-local affine owner

Purpose: Protect cleanup before backedge.
Setup: Allocate an iteration-local byte vector inside a loop.
Action: Continue after using it without moving it out.
Required assertions:
- The iteration-local owner is dropped before the backedge.
- No loan remains live.
- Repeated iterations do not accumulate live owners.
- Imported and source-free observations agree.
Primary layer: ownership convergence.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 26: Continue preserves an outer affine owner

Purpose: Avoid over-cleaning across a backedge.
Setup: Create an owner outside the loop and borrow or use it inside according to current ownership rules.
Action: Continue from the inner loop body.
Required assertions:
- The outer owner remains live after continue.
- Only iteration-local obligations are discharged.
- The owner is dropped exactly once at its real scope exit.
Primary layer: ownership liveness.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 27: Break cleans inner locals but preserves break result

Purpose: Protect cleanup ordering.
Setup: Create inner locals and a separate break payload.
Action: Break from the typed loop.
Required assertions:
- Inner locals are cleaned.
- The payload remains valid.
- Cleanup occurs exactly once.
- No use-after-move occurs.
Primary layer: memory planning and execution.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 28: Early return inside source-free loop remains correct

Purpose: Ensure new loop context does not corrupt return context.
Setup: Create a function with a loop and a return inside the loop.
Action: Take the return path.
Required assertions:
- Return targets the callable, not the loop.
- Loop-local cleanup runs.
- The returned value has the callable result type.
- Existing early-return convergence remains green.
Primary layer: workspace and ownership regression.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 29: Nested return and break choose distinct targets

Purpose: Protect independent control contexts.
Setup: Create a loop containing a conditional with return in one branch and break in another.
Action: Execute both through separate inputs or fixtures.
Required assertions:
- Return exits the callable.
- Break exits the nearest loop.
- Cleanup is exact on both paths.
- No shared target representation confuses them.
Primary layer: control and ownership.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 30: Legal constructors advertise typed loop

Purpose: Make loop authoring discoverable.
Setup: Create a hole with a non-never expected type.
Action: Query legal constructors.
Required assertions:
- A typed loop constructor is present when legal.
- Its proposed result type is exact and source-independent.
- The result is revision-labelled.
- Ordering is deterministic.
- Pagination is correct.
Primary layer: workspace query.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 31: Legal constructors advertise break payload type

Purpose: Avoid private-context reconstruction.
Setup: Create a hole at a divergent-admissible position inside a typed loop returning a nominal or generic type.
Action: Query legal constructors.
Required assertions:
- Break is present.
- The exact required payload type is returned.
- The type uses stable semantic identities.
- No private loop ID is exposed.
Primary layer: workspace query.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 32: Legal constructors advertise unit break inside while

Purpose: Expose the while-specific law.
Setup: Create a hole at a legal final position inside a while body.
Action: Query legal constructors.
Required assertions:
- Break is present.
- Its required payload type is unit.
- Continue is present.
- No outer typed-loop payload type leaks through.
Primary layer: workspace query.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 33: Legal constructors omit break outside loops

Purpose: Prevent false guidance.
Setup: Create a hole outside loop control.
Action: Query legal constructors.
Required assertions:
- Break is absent.
- Continue is absent.
- Return remains governed by its own context.
Primary layer: workspace query.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 34: Legal constructors omit control in inadmissible child position

Purpose: Respect divergent placement.
Setup: Create a hole used as a value-required operation or initializer child inside a loop.
Action: Query legal constructors.
Required assertions:
- Break is absent when divergent replacement is inadmissible.
- Continue is absent when divergent replacement is inadmissible.
- The query does not advertise a proposal known to be invalid.
Primary layer: workspace query.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 35: Legal-constructor pagination remains deterministic

Purpose: Protect machine-facing query behavior after non-Copy typed variants.
Setup: Create enough visible constructors to require multiple pages.
Action: Page through legal constructors twice.
Required assertions:
- Both traversals return the same order.
- No item is duplicated.
- No item is omitted.
- Continuation rejects stale revision.
Primary layer: workspace query pagination.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 36: Projection shows source-free loop control

Purpose: Make review exact.
Setup: Construct a complete source-free nested-loop program.
Action: Request a bounded projection around the control subtree.
Required assertions:
- Loop, while, break, and continue node kinds are visible.
- Child order is correct.
- Types and effects are correct.
- No private loop ID is present.
Primary layer: workspace projection.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 37: Semantic diff reports loop-control replacement

Purpose: Protect review and multi-turn editing.
Setup: Start from a hole or replaceable expression.
Action: Publish a loop-control draft.
Required assertions:
- The root replacement kind is correct.
- Created descendants include loop-control node kinds.
- Deleted descendants are exact.
- No synthetic reference rewire is reported for loop targets.
Primary layer: workspace semantic diff.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 38: Old snapshots remain valid after loop publication

Purpose: Protect immutable revision semantics.
Setup: Retain the pre-edit snapshot.
Action: Publish a valid loop-control edit.
Required assertions:
- The old snapshot still answers entity, node, hole, and projection queries.
- The new snapshot has a new revision.
- The old snapshot does not observe new nodes.
Primary layer: workspace revision.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 39: Failed break edit preserves future node allocation

Purpose: Prove allocator failure atomicity.
Setup: Prepare two equivalent workspaces.
Action: Apply an invalid break edit to one, then apply the same valid edit to both.
Required assertions:
- The valid edits receive equal stable identities under the existing deterministic law.
- The failed edit consumed nothing.
- Revisions agree after corresponding successful operations.
Primary layer: workspace allocator.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 40: Source-free loop compilation performs no parsing

Purpose: Protect semantic authority.
Setup: Construct a complete source-free loop-control snapshot.
Action: Compile it with current metrics.
Required assertions:
- Source loading is zero.
- Parsing is zero.
- No source attachment is required.
- Compilation succeeds directly from the snapshot.
Primary layer: compiler metrics.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 41: Imported and source-free typed loop converge

Purpose: Protect one semantic compiler path.
Setup: Build equivalent imported and source-free programs returning the same integer through break.
Action: Compile and execute both.
Required assertions:
- Semantic node kinds and types agree modulo private identities.
- Verified SSA outcomes agree.
- Evaluator outcomes agree.
- VM outcomes agree.
- Selected product-path outcomes agree.
Primary layer: end-to-end convergence.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 42: Imported and source-free nested continue converge

Purpose: Protect nearest-loop semantics.
Setup: Use the existing imported nested-continue fixture and a source-free equivalent.
Action: Compile and execute both.
Required assertions:
- Both return the same value.
- Control-flow verification agrees.
- No extra source-free cleanup occurs.
- No parser work occurs for the source-free route.
Primary layer: end-to-end convergence.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 43: Imported and source-free ownership loop converge

Purpose: Protect cleanup exactness.
Setup: Create equivalent programs with iteration-local affine ownership and continue or break.
Action: Execute both through the strongest existing observable route.
Required assertions:
- Allocation counts agree where deterministic.
- Drop counts agree.
- Live owners and loans reach zero.
- Cleanup failures agree.
Primary layer: product path ownership convergence.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 44: Deep flat nested loops are stack safe

Purpose: Protect user-controlled depth.
Setup: Generate a flat draft with many nested loops and valid breaks.
Action: Validate, lower, index, query, compile, and drop it on a small native stack.
Required assertions:
- No stack overflow occurs.
- No arbitrary semantic depth limit rejects it.
- Failure, if any, is a typed host resource failure.
Primary layer: stack-safety regression.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 45: Deep invalid control is stack safe

Purpose: Protect failure paths.
Setup: Generate deep nested structure ending in an invalid out-of-context or wrong-type control transfer.
Action: Apply the draft on a small native stack.
Required assertions:
- The error is deterministic.
- No stack overflow occurs.
- The failure is atomic.
Primary layer: stack-safety failure.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 46: Control-context work is linear

Purpose: Prevent accidental repeated scans.
Setup: Generate drafts with increasing loop and control-transfer counts.
Action: Read retained deterministic work counters or focused instrumentation.
Required assertions:
- Work grows with intended node and ancestry shape.
- One break does not trigger a full-draft scan after preflight.
- Instrumentation is removed unless retained by a regression.
Primary layer: complexity measurement.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 47: Malformed draft child identity remains rejected

Purpose: Protect flat-draft integrity after adding variants.
Setup: Create loop, break, and continue drafts with stale or out-of-range child IDs.
Action: Apply each malformed draft.
Required assertions:
- Each fails before publication.
- No panic occurs.
- The error is deterministic.
Primary layer: draft shape validation.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 48: Disconnected loop-control draft is rejected

Purpose: Protect connected semantic graph.
Setup: Include an unreachable break or loop node outside the draft root.
Action: Apply the draft.
Required assertions:
- The draft is rejected as disconnected.
- The extra node is not silently ignored.
- Failure is atomic.
Primary layer: draft graph validation.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 49: Cyclic loop-control draft is rejected

Purpose: Protect acyclicity.
Setup: Create a cycle through loop body or break payload child IDs.
Action: Apply the draft.
Required assertions:
- The draft is rejected without recursive overflow.
- No partial state is published.
- No panic occurs.
Primary layer: draft graph validation.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 50: Shared child violating single-parent rules is rejected

Purpose: Protect canonical containment.
Setup: Reuse one control or value node from multiple parents where sharing is forbidden.
Action: Apply the draft.
Required assertions:
- The draft is rejected.
- No ambiguous containment is published.
- Failure is deterministic.
Primary layer: draft graph validation.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 51: Stale legal-constructor continuation is rejected

Purpose: Protect revision-labelled query state.
Setup: Obtain a continuation from a legal-constructor page inside a loop.
Action: Publish another revision and reuse the old continuation.
Required assertions:
- The query rejects the stale continuation.
- It does not silently restart.
- The old snapshot can still use its own continuation when current API law permits.
Primary layer: query revision.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

### Case 52: No source-free control compatibility layer remains

Purpose: Enforce direct cutover.
Setup: Complete the implementation and update all callers.
Action: Search for temporary old variants, aliases, feature flags, or adapters.
Required assertions:
- Exactly one public draft form remains per construct.
- No `legacy`, `v2`, or transitional path remains.
- Documentation describes only the active route.
Primary layer: final diff review.
Keep the fixture as small as possible while preserving the invariant.
Prefer existing helpers and oracles.
Do not duplicate this case at every layer unless each layer owns a distinct failure.

## Focused implementation sequence

Use this sequence unless current evidence shows a smaller dependency order.

Phase 1: establish current behavior.
- run imported loop-control tests;
- run current source-free while and return tests;
- add one focused failing source-free typed-loop test;
- add one focused failing source-free break test;
- add one focused failing source-free continue test;
- confirm failures arise from the missing workspace surface rather than a deeper compiler defect.

Phase 2: extend the public flat draft.
- add exact variants;
- update child shape;
- update iterative traversal;
- update direct callers;
- compile the affected crate before broader work.

Phase 3: establish lexical context.
- identify or add one private enclosing-loop fact representation;
- seed it from the target's published ancestry;
- extend it through newly drafted loop and while nodes;
- resolve break and continue targets;
- reject invalid placement;
- test nested and existing-loop cases.

Phase 4: lower canonical HIR.
- resolve loop result type;
- allocate private loop IDs;
- lower loop;
- lower break;
- lower continue;
- preserve effects and reachability;
- run canonical final validation.

Phase 5: expose exact authoring guidance.
- extend legal constructors;
- expose exact break payload type;
- keep deterministic ordering and pagination;
- add focused query tests.

Phase 6: prove semantic convergence.
- imported versus source-free typed loop;
- imported versus source-free nested continue;
- imported versus source-free ownership-sensitive loop control;
- direct snapshot compilation;
- zero parser/source loading;
- evaluator and VM agreement;
- selected product path where supported.

Phase 7: prove failure and scale behavior.
- invalid placement;
- invalid types;
- failure atomicity;
- old snapshots;
- deep stack safety;
- deterministic work shape.

Phase 8: update documentation.
- remove closed current gaps;
- update architecture facts only when responsibilities changed;
- update roadmap ordering;
- update performance evidence only when new retained measurement exists;
- keep README concise.

Phase 9: final verification and diff review.
- run focused tests;
- run affected component tests;
- run native workspace boundary;
- run retained container boundary when relevant;
- inspect all changed files;
- search for obsolete variants and prose;
- verify no task prompt or raw log is staged.

Do not start a crate merge after Phase 9.
Do not start a CLI protocol after Phase 9.
Do not start persistence after Phase 9.
Report those only as possible future work when evidence supports them.

## Performance and complexity evidence

Correctness is primary in this turn.
Do not add an optimization before the complete control vertical works.

After correctness, measure only the work that can plausibly regress:
- draft shape traversal;
- lexical control-context traversal;
- target ancestry derivation;
- draft lowering;
- index construction;
- legal-constructor query;
- direct snapshot compilation; and
- default verification critical path if test topology changes.

Predeclare a simple criterion before retaining optimization machinery.
A reasonable criterion for control-context traversal is:
- one pass over the relevant existing ancestry;
- one enter/exit pass over draft nodes;
- constant-time target lookup during lowering; and
- no scan of all loops for each control transfer.

Use deterministic counters before noisy timing when they answer the question.
Do not create a benchmark crate.
Do not add Criterion solely for this vertical.
Do not add a persistent profiler dependency.
Do not commit raw timing output.
Do not claim speedup from one sample.

When the new tests materially affect default verification time:
- measure the previous focused target;
- measure the new focused target;
- keep default cases representative but bounded;
- move only genuinely large stress to ignored locked-release coverage;
- do not remove semantic coverage; and
- do not weaken retained container source correctness.

Do not optimize private loop-ID numbering.
Do not cache loop ancestry without measured repeated demand.
Do not add incremental indexes for this vertical.
Do not parallelize one draft traversal.
Do not add a warm service.
Do not add PGO.
Do not add deoptimization.
Do not broaden native specialization.

Record compact continuing evidence in `docs/performance.md` only when:
- the workload is retained;
- the method is reproducible;
- the result affects a selected design; and
- a reversal condition is meaningful.

Otherwise keep the measurement in the final report only.

## Coding-agent API-cost discipline

The user asked for reasonable effort to reduce the API cost of agents developing `lkjscript`.
Treat this as a secondary constraint.
Do not compromise semantics, safety, determinism, or maintainability.

Apply the following low-risk measures during this task:
- use the companion shorter procedural `AGENTS.md` instead of retaining duplicated current-status prose;
- search for symbols before opening entire large files;
- read focused ranges and diffs;
- use quiet Cargo flags;
- run focused tests before broad verification;
- avoid repeating successful commands after unchanged inputs;
- give subagents narrow evidence questions;
- avoid duplicate subagent repository summaries;
- keep generated projections bounded;
- avoid printing complete HIR, SSA, bytecode, or machine code;
- expose exact break payload type in legal-constructor results;
- reuse stable semantic identities instead of requiring source reconstruction;
- prefer one exact query over several model-mediated inference steps;
- keep final reporting compact; and
- avoid committing task context that every future agent would need to reread.

Do not add:
- provider-specific token accounting;
- prompt caching machinery;
- an LLM gateway;
- an agent scheduler;
- a semantic daemon;
- a database;
- a wire schema;
- a prompt compression model;
- an embeddings index;
- a vector database;
- a model-specific shorthand language; or
- lossy diagnostics.

When making an API-cost claim, classify it as:
- measured;
- estimated with assumptions; or
- unknown.

The exact legal-constructor result is a product improvement even without a monetary claim.
It reduces private-context reconstruction and future tool round trips.
Keep that improvement provider-independent.

## Documentation changes

Update documentation in the same coherent change.

`docs/spec/language.md`
- change only when intended language semantics change;
- ordinary source-free exposure of existing loop semantics should not invent a new language rule;
- correct any discovered contradiction directly.

`docs/spec/workspace.md`
- add typed loop, break, and continue to the current source-free expression surface;
- describe nearest lexical targeting;
- describe exact break payload typing;
- state that private loop IDs are not public semantic identities;
- state that a draft inserted inside an existing loop can use that lexical context when valid;
- update legal-constructor discovery facts;
- remove closed gap text; and
- preserve target/current labelling.

`docs/status.md`
- state the implemented source-free control surface;
- remove the missing break/continue gap;
- mention typed loop when implemented;
- preserve remaining gaps accurately;
- do not claim persistence, CLI authoring, daemonization, or general incrementality.

`docs/architecture.md`
- update only if a new private staging responsibility or data flow is material;
- name the owner and lifetime of lexical control facts;
- state that they are derived and transaction-local;
- do not draw a new platform architecture.

`docs/roadmap.md`
- remove a completed control-transfer item if present;
- do not leave a contingent public-movement item as an implied active implementation when no current owner/order consumer exists;
- keep `Now`, `Next`, and `Later` only;
- make evidence-driven crate/representation reassessment the next likely vertical when still justified;
- keep public movement contingent on a real use case;
- keep persistence, collaboration, and daemonization later and measured.

`docs/performance.md`
- add only compact retained evidence;
- do not paste raw command output;
- do not convert generated stress into an application claim.

`README.md`
- update only the concise current capability summary and a first-use example when the existing introduction would otherwise be false;
- do not turn README into a semantic-workspace reference manual.

Delete stale prose.
Do not retain old wording in an appendix for compatibility.
Do not create a new task-specific document.
Do not add a decision record unless the selected private control-context design is durable, non-obvious, expensive to rediscover, and governed by a meaningful reversal condition.
A normal local implementation choice should remain in code and tests.

## Non-goals

The following are explicit non-goals for this turn.

- A semantic-workspace CLI.
- A JSON, CBOR, MessagePack, protobuf, Cap'n Proto, or custom transaction protocol.
- Persistent workspace storage.
- Crash recovery.
- A journal.
- Cross-process revision identity.
- Collaboration.
- Concurrent writers.
- Conflict nodes.
- CRDTs.
- A daemon.
- A warm compiler service.
- A background scheduler.
- A session server.
- Remote execution.
- Distributed compilation.
- A plugin system.
- A rewrite DSL.
- A generic graph database.
- A public tree-coordinate API.
- Public loop labels.
- Arbitrary jumps.
- Labelled break.
- Labelled continue.
- Exceptions.
- Coroutines.
- Async lowering.
- Generators.
- A general effect system redesign.
- A general ownership redesign.
- Garbage collection.
- Reference counting.
- A general incremental compiler.
- A cache framework.
- A query database.
- A new parser.
- Source rendering.
- Formatter work.
- A source edition.
- A compatibility mode.
- A legacy API.
- A broad crate merge.
- A broad module reorganization.
- A native backend expansion.
- A new target architecture.
- PGO.
- Deoptimization.
- A self-hosting step.
- A package ecosystem redesign.
- A GUI, web, game, database, scheduler, or service framework.

A non-goal may be touched only when it is an unavoidable dependency-closed prerequisite for correct loop control.
When that occurs, implement the smallest prerequisite and explain why it was unavoidable.
Do not use incidental proximity as justification.

## Verification commands

Use focused commands during iteration.
Discover actual test names before invoking filters.
Prefer the narrowest command that exercises the changed owner.

Expected focused examples include conceptually:
```sh
cargo test --quiet -p lkjscript-compiler <focused-workspace-test-filter>
cargo test --quiet -p lkjscript-app --test never_control
cargo test --quiet -p lkjscript-app <focused-source-free-product-test-filter>
```
Use actual package and target names from the checkout.

At the final relevant code state, run:
```sh
cargo fmt --all -- --check
cargo clippy --quiet --workspace --all-targets --all-features --locked -- -D warnings
cargo test --quiet --workspace --all-targets --all-features --locked
cargo build --quiet --workspace --release --locked
```

Run retained container verification when the change reaches compiler or product behavior:
```sh
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
```

This task changes compiler and product behavior.
The default expectation is therefore to run the retained container verification.
Omit it only when the environment prevents it or a concrete current repository rule proves it irrelevant.
Report the exact reason.

Run additional relevant:
- ignored locked-release deep control stress;
- small-stack tests;
- ownership convergence;
- evaluator/VM differential tests;
- source-free parser-zero metrics; and
- any existing malformed-control verification suite.

Do not run multiple heavyweight Cargo commands concurrently in one target directory.
Do not have subagents run the full final boundary independently.
Do not rerun the full boundary after documentation-only edits when no compiled input changed.
Do rerun any command whose relevant input changed.
Never claim a command passed when it timed out, was cancelled, was skipped, or failed for environment reasons.

## Verification evidence format

For each final command record:
- exact command;
- exit status;
- whether it completed;
- relevant duration when already available;
- failure category when non-zero;
- whether the failure is caused by the change;
- relevant concise output; and
- residual risk.

Do not paste complete successful logs.
Do not hide warnings.
Do not hide container cache behavior.
Do not hide source-staleness evidence.
When a command is unavailable, continue with all independent evidence.
Do not replace execution evidence with confidence language.

## Git discipline

Inspect the diff before every commit.
Preserve unrelated work.
Do not use destructive reset.
Do not clean untracked files you did not create.
Do not rewrite remote history.
Do not force push.

Commit cohesive changes when the environment allows and the task wrapper does not prohibit commits.
One commit is reasonable when the vertical is tightly coupled.
Multiple commits are reasonable when each leaves a coherent build and separates a real semantic step.
Do not split commits by file extension.
Do not commit:
- this prompt;
- raw logs;
- raw benchmark samples;
- scratch notes;
- temporary profiles;
- generated target output;
- credentials;
- subagent packets; or
- unrelated files.

A suitable commit subject should name the product result.
Examples of the level of specificity:
- `Complete source-free lexical loop control`;
- `Add typed loop break and continue authoring`.
Choose the subject that accurately matches the final diff.
Do not push unless the active task wrapper explicitly requests publication.

## Contingency branches

The actual checkout may have advanced after this prompt was authored.
Handle that without asking the user to restate the task.

When `Loop`, `Break`, and `Continue` are already fully implemented:
1. prove completion with the acceptance cases above;
2. identify the first failing or absent dependency-closed requirement;
3. repair that requirement;
4. avoid reimplementing complete code; and
5. stop when the vertical is genuinely complete.

When only break and continue are missing but typed loop already exists:
- reuse the existing loop draft;
- do not add a second loop form;
- complete lexical context, queries, convergence, ownership, and docs.

When break and continue exist only for newly drafted loops:
- complete insertion into existing surrounding loops;
- do not call the feature complete before that path works.

When legal constructors cannot carry an exact type without changing a public enum:
- change the enum directly;
- update callers;
- delete the old shape;
- keep deterministic ordering explicitly.

When an existing private helper already derives loop ancestry:
- extend or reuse it;
- do not add a parallel traversal.

When a deeper canonical HIR or ownership defect is revealed:
- fix the smallest root cause in the same vertical;
- add a regression;
- do not create a source-free workaround.

When current semantics intentionally differ from the baseline:
- inspect the owning specification;
- preserve the accepted current law;
- adapt the source-free route to converge with it;
- update stale prompt assumptions only in temporary reasoning, not in a committed prompt.

When the retained container cannot run:
- run the full native boundary;
- run all focused product tests;
- report the exact environment failure;
- state unverified risk.

When the task becomes larger than one coherent turn:
- finish the smallest dependency-closed control result;
- leave no half-cutover;
- document current truth;
- report the exact remaining sub-vertical;
- stop.

Do not replace completion with a speculative architecture document.

## Architecture restraint

Do not merge `contracts`, `core`, `ir`, `compiler`, `native`, `jit`, `vm`, `host`, `sys`, `executable`, or `app` merely because this task touches several layers.
Crate-boundary reassessment is a separate evidence-driven vertical.
A crate merge is allowed in this turn only when:
- the current boundary directly blocks the control result;
- the boundary has no independent trust, unsafe, target, library, or measured compile-isolation value;
- the merge is smaller than adding adapters; and
- the full control vertical remains the central product result.

Do not create a new `control` crate.
Do not create a new `workspace-control` crate.
Do not create a generic `semantic-graph` crate.
Do not create a protocol crate.
Do not create a test-support crate for a few fixtures.

Local private helper modules are allowed when they establish clear ownership and reduce change coupling.
Do not split one traversal into many tiny one-child modules for visual symmetry.

## Security and trust boundaries

This task should not require new unsafe code.
Do not add unsafe code for loop context, draft traversal, or identity mapping.
Do not cross FFI.
Do not expose raw pointers.
Do not weaken validation because the input is in-process.

Treat public draft input as untrusted enough to require complete shape and identity validation.
Treat canonical internal HIR as trusted only after existing validation.
Use checked arithmetic.
Handle allocation failure.
Do not panic on malformed drafts.
Do not use `unwrap`, `expect`, `todo`, or `unimplemented` in production paths under current lint policy.
Test-only expectations may follow current repository conventions.

## Final acceptance checklist

- [ ] The root companion `AGENTS.md` is installed and internally consistent.
- [ ] The prompt itself is not committed.
- [ ] The actual checkout baseline was inspected.
- [ ] Unrelated work is preserved.
- [ ] A focused failing test demonstrated the original source-free gap.
- [ ] The public flat draft can represent typed loop.
- [ ] The public flat draft can represent break.
- [ ] The public flat draft can represent continue.
- [ ] Draft child traversal covers all new variants.
- [ ] Draft shape validation covers all new variants.
- [ ] Draft traversal is iterative.
- [ ] Loop result types use `SemanticType`, not text.
- [ ] Loop result types are canonically resolved.
- [ ] Loop result types containing never are rejected.
- [ ] Foreign type identities are rejected.
- [ ] Stale type identities are rejected.
- [ ] Wrong-owner type parameters are rejected.
- [ ] Private loop IDs remain private.
- [ ] No public loop identity category was added.
- [ ] Nearest lexical targeting works for newly drafted loops.
- [ ] Nearest lexical targeting works for newly drafted whiles.
- [ ] Nearest lexical targeting works inside existing published loops.
- [ ] Nested draft loops shadow existing loops.
- [ ] Context is restored after nested loops.
- [ ] Break outside a loop is rejected.
- [ ] Continue outside a loop is rejected.
- [ ] Break payload type is exact.
- [ ] While break requires unit.
- [ ] Divergent break payload is rejected.
- [ ] Break has semantic type never.
- [ ] Continue has semantic type never.
- [ ] Expressions after break are rejected.
- [ ] Expressions after continue are rejected.
- [ ] Divergent child admissibility matches canonical HIR law.
- [ ] Return context remains independent from loop context.
- [ ] Ownership cleanup is exact on break.
- [ ] Ownership cleanup is exact on continue.
- [ ] Outer ownership is not over-cleaned.
- [ ] Affine break payloads remain valid where supported.
- [ ] No leak occurs.
- [ ] No double drop occurs.
- [ ] No live loan remains after exit.
- [ ] Legal constructors advertise typed loop when legal.
- [ ] Legal constructors advertise break only when legal.
- [ ] Legal constructors expose the exact break payload type.
- [ ] Legal constructors advertise continue only when legal.
- [ ] Legal constructors omit divergent control in inadmissible positions.
- [ ] Legal-constructor ordering is deterministic.
- [ ] Legal-constructor pagination is correct.
- [ ] Stale continuations are rejected.
- [ ] Projection shows exact control node kinds.
- [ ] Projection exposes no private loop IDs.
- [ ] Semantic diff reports exact control changes.
- [ ] No synthetic loop-target reference edge was added.
- [ ] Old snapshots remain valid.
- [ ] Failed edits publish no revision.
- [ ] Failed edits consume no stable identities.
- [ ] Future allocation order is unchanged after failure.
- [ ] Source-free direct compilation succeeds.
- [ ] Source loading is zero for source-free compilation.
- [ ] Parsing is zero for source-free compilation.
- [ ] No render-and-reparse path was introduced.
- [ ] Imported and source-free typed loops converge.
- [ ] Imported and source-free nested continue converge.
- [ ] Imported and source-free ownership-sensitive control converge.
- [ ] Evaluator outcomes agree.
- [ ] VM outcomes agree.
- [ ] Selected product-path outcomes agree where supported.
- [ ] Deep valid control is stack safe.
- [ ] Deep invalid control is stack safe.
- [ ] No arbitrary nesting quota was added.
- [ ] Control-context work does not scan the full draft per transfer.
- [ ] Temporary instrumentation without a consumer is removed.
- [ ] No daemon was added.
- [ ] No persistence layer was added.
- [ ] No wire protocol was added.
- [ ] No generic graph engine was added.
- [ ] No compatibility layer remains.
- [ ] No broad crate merge was started.
- [ ] Current documentation is accurate.
- [ ] Closed gap text is removed.
- [ ] Roadmap ordering is honest.
- [ ] Focused tests pass.
- [ ] Affected component tests pass.
- [ ] Formatting passes.
- [ ] Clippy passes.
- [ ] Workspace tests pass.
- [ ] Release build passes.
- [ ] Retained container verification passes or an exact environment limitation is reported.
- [ ] The complete diff was reviewed.
- [ ] Cohesive commit state is reported.
- [ ] No push occurred without explicit request.
- [ ] The next independent problem is named but not begun.

## Final engineering report

The final response to the user or task wrapper must be concise but complete.
Include:
- the completed objective;
- the demonstrated original gap;
- the selected private lexical-context design;
- why no public loop identity was added;
- the public draft API cutover;
- legal-constructor query changes;
- convergence evidence;
- ownership and cleanup evidence;
- stack-safety and complexity evidence;
- documentation changes;
- exact focused tests;
- exact final verification commands and outcomes;
- environment-limited checks;
- commit identifier when committed;
- publication state;
- remaining risks; and
- the next highest-leverage independent problem.

State why work stopped.
The expected reason is that the source-free loop-control vertical is complete and the next issue is independent.
Do not paste complete logs.
Do not reproduce this prompt.
Do not describe non-implemented future systems as current.
Do not claim a monetary API saving without measurement.

## Stop condition

Stop when all dependency-closed requirements of source-free typed loop, break, and continue authoring are complete, documented, tested, and verified.
Do not begin the next roadmap item in the same turn merely because time remains.
Do not implement public movement without a current owner/order consumer.
Do not merge crates without profiles and Cargo evidence.
Do not expose the semantic workspace through a CLI protocol without a concrete one-shot consumer and explicit lifetime semantics.
Do not add persistence, collaboration, or a daemon without representative retained-scale and process-boundary measurements.

A strong next-turn candidate, after this task is complete, is an evidence-driven retained-representation and crate-boundary reassessment.
That next turn should inspect compile isolation, dependency direction, trust boundaries, unsafe boundaries, public consumers, and change coupling.
It should merge nothing merely to reduce a crate count.
It is not part of this task.

Complete the control vertical.
Leave one active architecture.
Leave the repository better than it was.
Then stop.
