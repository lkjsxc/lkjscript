# lkjscript post-reset stabilization, runtime evidence, and the next coherent vertical

You are working on `lkjsxc/lkjscript`.

This file is an external task prompt. Do not commit this prompt, copy it into repository documentation, or create a task archive from it. A replacement root `AGENTS.md` is supplied separately. Install or reconcile that file first, then follow it for the entire task.

You have broad authority to make autonomous technical decisions, break every provisional interface, delete obsolete code, replace representations, merge or split crates, rewrite specifications, and remove compatibility scaffolding. Backward compatibility is not a goal. The objective is the strongest long-term design, not preservation of recent work.

That authority is not permission to add machinery without evidence. The repository has recently recovered from substantial overengineering. Prefer one direct system, deletion, measured correction, and a complete vertical over a broad platform, generalized framework, or speculative future architecture.

## 1. Audited orientation

The repository was inspected at commit:

```text
d758243b6b8777f29f93077bae82981840bb3ac5
Record completed borrow-call scale correction
```

Treat this SHA only as orientation. The checkout you receive is authoritative and may have advanced. Begin by inspecting the actual branch, worktree, upstream state, recent history, code, tests, manifests, and authority documents. Never reset or overwrite unrelated work merely to match the audited SHA.

At the audited revision, the active design is approximately:

- one experimental, AI-primary, statically typed, memory-safe language implemented in Rust;
- one syntax-independent immutable semantic workspace snapshot as the direct compiler input after import;
- one local product path: package compile/run plus an in-process workspace API;
- one production execution policy: synchronously attempt one eligible baseline-native group before effects, otherwise execute the unchanged validated program in the VM;
- no public engine selector, no runtime tier transition, no optimizing tier, no forced-native CLI path, and no VM retry after native entry;
- no daemon, session broker, process-cell system, scheduler, resource topology, service database, or semantic wire service;
- direct SQLite, filesystem, network, terminal, clock, stdio, and related language capabilities through the retained VM/host/system path;
- collector-free ordinary execution under the accepted language semantics;
- recently repaired compiler and VM scale pathologies in ownership liveness, bytecode local analysis, physical frame sizing, bytecode validation, and cleanup-range lookup.

Recent simplification is intentional. Do not restore a deleted subsystem merely because the user has long-term interest in a daemon, adaptive optimization, or a native database.

The audited repository still reports important gaps:

1. the representative post-reset runtime baseline remains incomplete, especially peak RSS, allocation/copying behavior, generated-code size, broader workload coverage, native admission/fallback coverage, and application-scale steady-state behavior;
2. several compiler transaction and runtime structural-value paths still lack complete deep-stack evidence;
3. semantic workspace edits remain narrow: declaration and node create/delete/move, local-storage construction, generic calls, matches, unresolved or ambiguous states, conflicts, and broader recovery states are not complete;
4. incremental recomputation is not implemented or selected;
5. retained crate and representation boundaries may still contain ceremony that has not earned its cost;
6. the native attempt still contains finite backend, executable, diagnostic, and compile-time admission values that can create performance cliffs even when the VM preserves semantic validity;
7. the long-term daemon, adaptive optimizer, and custom database have no present implementation mandate, workload contract, or measured admission case.

Verify every item against the current checkout before relying on it.

## 2. Primary objective for this turn

Complete one dependency-closed **post-reset runtime evidence vertical**.

The required outcome is:

1. establish a reproducible, representative baseline for the selected product execution path;
2. trace and classify every material native-attempt admission, decline, installation, preparation, and fallback gate;
3. make at most one coherent runtime-selection, native-admission, measurement, or representation correction when the evidence justifies it;
4. otherwise keep the runtime architecture unchanged and record that result honestly;
5. leave a ranked, evidence-backed next queue, with stack safety and semantic-workspace completion ahead of speculative platform work.

Measurement is part of the product work here, not a ceremonial benchmark exercise. A valid result may be that the current one-shot baseline-native attempt should remain unchanged. Do not invent a runtime redesign merely to produce code churn.

Do not combine this turn with a daemon, persistence layer, custom database, scheduler, optimizing tier, general incremental framework, broad language expansion, or repository-wide cleanup.

## 3. Durable long-term intent

Preserve the following user interests as long-horizon objectives, not immediate architecture:

### 3.1 Runtime-informed optimization

The eventual runtime should be able to use observed execution information to improve later execution. Any future profiling or specialization must be semantically invisible: the same complete semantic program, inputs, capabilities, target, and options must retain the same meaning regardless of profile state, cache state, address layout, or optimization schedule.

The likely path is incremental:

1. obtain representative product measurements;
2. make the generic VM and baseline-native path correct, scale-safe, and observable;
3. identify a repeated workload and a specific optimization opportunity;
4. add the smallest profile or specialization mechanism that serves that opportunity;
5. bound code memory, compile cost, retained state, invalidation, and failure behavior;
6. retain it only if end-to-end evidence pays for its complexity.

Do not jump directly to a multi-tier JIT, speculative deoptimization framework, global profile database, or compiler daemon.

### 3.2 One shared local daemon

The user is interested in one daemon eventually coordinating local `lkjscript` work rather than one heavyweight runtime stack per application. Preserve that hypothesis, but do not implement it now.

A daemon becomes admissible only after the in-process semantic workspace is substantially complete, incremental recomputation has a measured design, warm-state value is quantified, and a process boundary demonstrably improves startup, repeated compile/edit latency, retained artifact reuse, or whole-system resource use.

The first admissible daemon should be one small process per explicit trust domain, probably per OS user unless evidence requires another scope. It should expose thin deterministic operations over existing in-process APIs. It must not resurrect process cells, a scheduler platform, resource topology, service-database control planes, protocol revisions, or broad orchestration machinery.

### 3.3 A performance-oriented native database

The user is interested in an eventual database designed with `lkjscript` performance and semantics in mind. Preserve that direction, but do not replace the current direct SQLite capability without a defined workload and equivalent evidence.

Before custom storage work:

1. define target workloads and semantics: durability, transactions, isolation, recovery, concurrency, indexing, query model, value layout, and capability boundaries;
2. instrument and benchmark the current direct SQLite path;
3. identify a concrete bottleneck or semantic mismatch;
4. decide whether the answer is a better SQLite integration, a narrow embedded storage primitive, or a new engine;
5. build one storage vertical with crash and corruption tests before generalizing it.

A renamed SQLite wrapper, service database, metadata registry, or daemon control store is not the desired native database.

## 4. Phase A: establish repository truth

Before editing:

```sh
git status --short --branch
git remote -v
git log --oneline --decorate -n 40
git branch -vv
cargo metadata --locked --no-deps --format-version 1
```

Inspect at least:

- `AGENTS.md`;
- `docs/authority.md`;
- `docs/spec/language.md`;
- `docs/spec/workspace.md`;
- `docs/status.md`;
- `docs/architecture.md`;
- `docs/performance.md`;
- `docs/roadmap.md`;
- the application compile/run path;
- compiler metrics;
- native/JIT planning, verification, encoding, installation, and invocation;
- VM fallback and failure cleanup;
- relevant release and scale harnesses.

Confirm whether the branch is clean, whether upstream has diverged, and whether recent commits already overlap this task. Preserve unrelated changes.

Replace the root `AGENTS.md` with the supplied companion file, or reconcile it when the checkout has newer, non-conflicting policy improvements. Do not maintain two policy files.

Write a concise scratch plan in agent working state. Do not create a hierarchy of committed plan files.

## 5. Phase B: trace the selected product path

Trace the complete producer/consumer and failure path, not only public function names:

```text
package/path import
    -> semantic snapshot
    -> compile_snapshot
    -> HIR memory planning
    -> verified SSA
    -> validated bytecode
    -> native preflight
    -> native lowering/encoding
    -> executable installation
    -> invocation preparation
    -> native entry
or
    -> unchanged validated bytecode VM fallback
```

For every stage, record privately:

- authoritative input and output type;
- ownership and lifetime;
- validation already performed;
- allocation and copying;
- whole-program or whole-function traversal;
- generated or retained bytes;
- cancellation and resource behavior;
- failure atomicity;
- whether an effect can already have occurred;
- whether the result has a current consumer.

Prove the fallback invariant: every pre-entry native decline must leave the original validated bytecode, inputs, policy, and observable effect state suitable for exactly one VM execution. Every post-entry result or failure must remain final and must not execute the VM.

Audit whether compilation, preflight, lowering, installation, preparation, or metrics reconstruct, clone, hash, serialize, or independently verify information that is already authoritative inside the same synchronous trusted pipeline.

Delete redundant work only when the full producer/consumer audit proves it has no current boundary or consumer.

## 6. Phase C: classify native admission and performance cliffs

Enumerate all ways the baseline-native attempt can decline or stop before entry. Classify each as exactly one of:

- unsupported language operation or type;
- unsupported target/ABI representation;
- executable-memory or relocation safety boundary;
- explicit host resource policy;
- private tuning choice with a generic fallback;
- diagnostic-retention policy;
- measured compile-cost guard;
- accidental fixed ceiling or implementation artifact;
- internal correctness failure.

At the audited revision, candidate values included, but were not limited to:

- native backend defaults around 64 functions, 1,024 blocks, 16,384 values, 1,024 locals per function, 4 MiB code, 4 MiB metadata, and 100,000 work units;
- executable defaults around 4 MiB per-object code and metadata, 64 MiB total code and metadata, 1,024 objects, and associated work-unit limits;
- native object and total compile-time defaults around 250 ms and 2 s;
- diagnostic byte retention limits.

Re-read current code; do not assume these values or types remain.

These values are not automatically semantic bugs because the complete VM route may remain available. They are still performance and observability concerns. Determine:

- whether a limit is reachable on ordinary valid programs;
- whether decline happens before expensive work or only after most work is paid;
- whether the decline reason is precise and measurable;
- whether a static value is justified by executable-memory safety, host resource control, or only history;
- whether the current default causes native coverage cliffs;
- whether increasing or removing it merely moves an asymptotic problem;
- whether a wide generic representation already exists;
- whether a host-sized or workload-derived policy would be simpler;
- whether no limit is necessary in the trusted local product;
- whether the native attempt should decline earlier, later, or not exist for that shape.

Do not “fix” a performance cliff by converting it into a language-validity limit. Do not silently widen every integer. Do not add a generic quota framework.

## 7. Phase D: build the representative measurement corpus

Use the selected production path. Do not restore a public engine selector or keep two product runtimes for benchmarking.

A test-only or measurement-only harness may invoke the VM and baseline-native machinery independently to establish equivalent reference measurements. It must not become a public execution architecture or a permanent forced-engine CLI.

The workload corpus should cover representative semantic families, not only one synthetic loop:

1. scalar arithmetic and comparisons;
2. branches and loops;
3. direct calls and a wider reachable call group;
4. products, enums, and matching where currently implemented;
5. strings, bytes, byte vectors, lists, and structural operations;
6. ownership, borrowing, movement, cleanup, early return, and failure;
7. an entered native trap or checked failure;
8. at least one deterministic filesystem or SQLite host-boundary workload when practical;
9. current examples such as hello, scalar, bench, and Mandelbrot where they remain relevant;
10. generated scale geometries that cross at least one current native admission boundary without becoming the sole basis for a product decision.

Prefer existing examples and tests when they already express equivalent semantics. Add the smallest missing fixtures. Do not name normal examples after engines or optimization strategies.

For each workload, establish expected output or typed outcome and verify equivalence. When a workload is unsupported by native execution, report the precise fallback reason and the cost paid before decline.

## 8. Phase E: measurement protocol

Use a locked release build and record:

- exact commit and dirty-tree identity;
- OS, kernel, architecture, CPU, logical CPU count, memory, Rust and Cargo versions;
- build profile and relevant environment;
- cold versus warm state;
- sample count and selection protocol;
- workload input and expected result.

Measure as applicable:

- fresh-target release build time;
- incremental no-op build time;
- final stripped binary size;
- process startup and time to first result;
- total compile time and every retained compiler phase;
- native preflight, lowering/encoding, installation, preparation, and execution;
- VM execution;
- total selected-product time;
- selected path and exact fallback reason;
- peak process-tree RSS with the method clearly stated;
- allocation count/bytes or a defensible substitute when practical;
- generated code bytes, metadata bytes, executable object count, and retained executable usage;
- bytes copied, serialized, or hashed when a suspected cost makes that relevant;
- output correctness and failure behavior.

Use enough samples to distinguish signal from obvious noise. Five samples is a reasonable default for orientation; use more when conclusions are close. Report median and a tail statistic, while stating when the sample count makes the tail weak.

Keep raw logs, profiles, and samples under ignored `target/` paths or CI artifacts. Commit the harness, workload definitions, compact results, and decisions only.

Do not turn developer-machine timings into brittle CI pass/fail thresholds. Prefer structural work counters for complexity regressions.

## 9. Phase F: make one evidence-backed decision

After the baseline, choose the smallest coherent outcome.

Acceptable outcomes include:

- retain the one-shot baseline-native attempt unchanged because it wins on supported shapes and declines cheaply elsewhere;
- remove or replace one accidental native ceiling while preserving a generic fallback and bounded failure behavior;
- move one cheap eligibility check earlier to avoid expensive doomed lowering;
- remove one redundant pass or representation discovered in the traced product path;
- expose one missing metric required to understand a real decision;
- expand native support for one common operation only when the measured workload and implementation simplicity justify it;
- delete the baseline-native attempt if representative equivalent evidence shows that its total cost and maintenance burden consistently lose;
- retain the architecture and make no runtime code change when evidence does not justify one.

Do not perform more than one architectural correction in this turn.

Any correction must:

- preserve one active product path;
- keep semantics independent of selected execution path;
- preserve pre-entry failure atomicity and post-entry no-retry behavior;
- retain the complete generic route for otherwise valid programs;
- remove the displaced mechanism rather than feature-flagging it;
- include focused correctness, decline, cleanup, and scale tests;
- include before/after equivalent evidence when performance is claimed;
- have an explicit reversal condition.

Do not add:

- a multi-tier JIT;
- runtime call counters or thresholds without a demonstrated consumer;
- deoptimization, on-stack replacement, speculative guards, or profile persistence;
- a compilation cache or artifact identity without a real repeated computation and ownership boundary;
- a general scheduler or parallel compiler;
- a public forced-engine option;
- benchmark-specific branches;
- arbitrary validity quotas;
- a second SSA, bytecode, or semantic authority.

If the runtime baseline yields no justified code correction and the harness/documentation work is small, you may instead complete exactly one independently coherent stack-safety correction. Do so only after reproducing or structurally proving a user-controlled recursion risk under a small-stack or deep generated test. Do not combine a runtime redesign and a stack-safety campaign.

## 10. Secondary stack-safety triage

Whether or not you implement a stack correction, inspect and rank the remaining risk.

Audit:

- every `stack::grow` or equivalent segmented-stack call site;
- recursive compiler type, match, effect, HIR, SSA, memory-plan, and lowering paths;
- semantic transaction staging, HIR cloning, indexing, diffing, projection, and destruction;
- runtime structural values, snapshots, equality, cloning, cleanup, and destruction;
- recursive serialization/deserialization paths, including unbounded-depth JSON handling;
- user-controlled drops whose Rust destructor chain can recurse.

Distinguish:

- recursion bounded by a small fixed compiler structure;
- recursion proportional to user program depth;
- recursion proportional to runtime data depth;
- heap-backed segmented recursion with current evidence;
- an iterative algorithm already covered by deep tests;
- an untested path.

Do not impose a nesting limit. Prefer an explicit work stack when it simplifies ownership and failure. A localized heap-backed stack segment may remain when conversion would be more complex and small-stack evidence demonstrates safety.

## 11. AI-primary design constraints

Do not weaken the semantic-workspace direction while working on runtime evidence.

Preserve:

- one semantic authority after import;
- opaque stable logical identity;
- revision-labelled deterministic queries;
- typed atomic transactions;
- explicit incomplete states;
- concise selective projections;
- direct compilation from complete snapshots;
- parser-free semantic edits and compile;
- human-reviewable projections and diffs;
- deterministic local correctness.

Do not add a daemon, protocol, persistence layer, cache, or collaboration system merely because snapshots exist.

Do not expand the workspace operation surface in this turn unless it is the only dependency-closed correction selected after the primary evidence work. If that exceptional path is chosen, implement one complete operation family through validation, stable identity, transaction publication, semantic diff, invalidation, queries, projection, direct compile, failure atomicity, and tests. Do not add broad enum variants that merely return “unsupported.”

## 12. Overengineering and deletion audit

At every touched boundary, ask:

- Is this value consumed now?
- Is the same fact represented elsewhere?
- Is this validation at a real trust boundary or duplicated inside trusted typed flow?
- Is this abstraction smaller than the problem?
- Does this crate boundary isolate unsafe code, platform code, a supported target, an independently useful library, or measured compile isolation?
- Would deleting or inlining it improve the producer/consumer path?
- Is a name such as `contract`, `witness`, `authority`, `identity`, `plan`, `registry`, or `runtime` hiding a simple local fact?

Do not launch a repository-wide renaming or crate-merging project from these questions. Apply deletion or consolidation only where it is required by the selected vertical or where the evidence is unambiguous and the change is small.

Do not create a ledger proving that you avoided ledgers.

## 13. Documentation requirements

Update only the documents that own changed claims.

- `README.md`: first-use/product summary only.
- `docs/spec/language.md`: intended language semantics.
- `docs/spec/workspace.md`: intended semantic-workspace contract.
- `docs/status.md`: checkout behavior and known gaps.
- `docs/architecture.md`: current responsibilities, data flow, ownership, and trust boundaries.
- `docs/performance.md`: protocol, reproducible workloads, compact evidence, and reversal conditions.
- `docs/roadmap.md`: concise `Now`, `Next`, and `Later`.
- `docs/decisions/`: only a durable, non-obvious, costly-to-rediscover decision with a real reversal condition.

For the roadmap, preserve this broad order unless evidence from the current checkout requires a change:

1. representative selected-product runtime evidence and any direct correction;
2. complete deep-stack evidence and corrections;
3. expand one semantic-workspace vertical at a time;
4. measured incremental recomputation;
5. representation and crate-boundary reassessment;
6. only then evaluate a warm daemon/process boundary;
7. evaluate a custom database only after the direct SQLite path and real workloads establish a need;
8. add adaptive runtime optimization one measured mechanism at a time.

Do not create a vision document, platform revision, evidence ledger, fact registry, prompt archive, completion capsule, or duplicated roadmap.

## 14. Required tests and verification

Run focused tests while iterating. After the final relevant change, run:

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
cargo test --workspace --all-targets --locked
cargo build --workspace --release --locked
```

Run the retained container verification when the environment supports it:

```sh
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
```

Also run the relevant subset of:

- the representative release measurement harness;
- exact generated scale tests;
- small-stack/deep-input tests;
- VM/native differential tests;
- malformed bytecode, machine-plan, relocation, executable-image, and transaction tests;
- cancellation, deadline, resource, cleanup, and failure-atomicity tests;
- Miri for changed unsafe-adjacent or ownership-sensitive code;
- ASan, LSan, or TSan when the changed boundary warrants them;
- fuzzing or property tests for a parser/decoder/validator change;
- package and smoke scripts.

Do not claim a command passed if it did not run after the final relevant edit. Report environmental failure precisely and do not silently substitute a weaker command.

## 15. Commit and publication discipline

Keep commits cohesive and descriptive. Do not commit raw benchmark output, this prompt, scratch plans, or generated task history.

Before committing:

```sh
git diff --check
git status --short
git diff --stat
git diff
```

Inspect the final diff specifically for:

- a second architecture;
- stale documentation;
- accidental compatibility;
- hidden count limits;
- unchecked narrowing;
- duplicated validation or reconstruction;
- effects before native commit;
- VM retry after native entry;
- benchmark-specific code;
- speculative daemon/database/cache/tier machinery;
- unrelated reorganization.

Commit the completed vertical. Push the current branch without force when credentials and repository policy permit. Verify branch/upstream divergence after the push. Never rewrite unrelated history.

## 16. Definition of done

This turn is complete only when:

- the actual checkout was re-audited rather than assumed;
- the selected product path has a reproducible representative baseline or the exact environmental blocker is documented;
- native admission and fallback gates are classified;
- any performance claim has equivalent before/after evidence;
- at most one coherent correction was made;
- no arbitrary language-validity quota was introduced;
- no speculative daemon, database, scheduler, cache framework, or optimizing tier was added;
- failure atomicity, deterministic semantics, and exactly-once observable effects remain protected;
- focused regression tests cover the root cause;
- active documentation matches the checkout;
- full relevant verification ran after the final edit;
- changes are committed and push state is verified when possible;
- the repository is coherent and usable for the next independent turn.

Stop after this vertical. Do not automatically begin the next roadmap item.

## 17. Final report

Report in English with explicit separation of:

1. audited starting state and any divergence from this prompt;
2. measured workloads, protocol, environment, and compact results;
3. native admission/fallback findings;
4. implemented correction, or the reason no correction was justified;
5. important deletions or retained mechanisms;
6. tests and verification commands actually run;
7. commit SHA and push/upstream state;
8. untested paths and environmental limits;
9. remaining risks;
10. deliberately deferred long-horizon work;
11. the single highest-leverage next vertical.

Do not describe a plan as implementation, a hypothesis as measurement, or a future daemon/database/optimizer as current architecture.
