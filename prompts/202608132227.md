# Complete Source-Free Generic Enum Declaration Authoring

## Task Metadata

Repository: `lkjsxc/lkjscript`

Observed target branch at prompt preparation: `main`

Observed target commit at prompt preparation: `efcf802f77a4d69bba03ab466011db50176e12f1`

Observed target commit subject: `Complete exact generic enum authoring`

Prompt preparation time: `2026-08-13 22:27 Asia/Tokyo`

Maintained-language requirement: English

Accompanying repository instruction file: `AGENTS.md`

Expected work style: autonomous, evidence-driven, multi-turn-aware, and deliberately
narrow

Compatibility policy: no backward compatibility for provisional syntax, Rust APIs,
internal forms, tests, or documentation

Remote-state policy: do not push, force-push, open a pull request, or otherwise change
remote state

Local-commit policy: one cohesive verified local commit is permitted when the
environment and current repository instructions permit it

## Primary Mandate

Complete one dependency-closed vertical:

> An AI agent must be able to create a user-defined generic enum declaration from
> `Workspace::empty`, discover its stable enum and type-parameter identities,
> construct exact instantiated enum values, author exact patterns, compile the
> resulting source-free program through the production snapshot pipeline, and
> execute it on the generic VM without parsing or loading source after
> construction begins.

The concrete current gap is generic enum declaration creation.

Do not reinterpret this task as a general redesign of nominal declarations.

Do not reinterpret this task as a request for generic products.

Do not reinterpret this task as a request for enum trait bounds.

Do not reinterpret this task as a request for recursive declaration syntax.

Do not reinterpret this task as a request for persistence, a daemon, RPC, a wire
protocol, collaboration, or a service.

Do not reinterpret this task as a broad performance project.

Do not reopen recently completed generic enum value and pattern authoring unless a
focused regression or integration failure requires a bounded correction.

The user grants broad authority to make the cleanest long-term decision.

That authority must be used to remove contradictions and complete the selected
operation.

It must not be used to accumulate speculative infrastructure.

## Install and Obey the Accompanying `AGENTS.md`

This task is accompanied by a replacement root `AGENTS.md`.

Before implementation, ensure the repository root uses that file.

Treat it as active repository instruction for this task and later turns.

Do not commit this prompt into the repository.

Do not commit chat transcripts, copied tool output, or temporary plans.

If the checkout already contains an identical or stronger `AGENTS.md`, compare
substantive rules rather than file timestamps.

Keep the accompanying version unless current repository evidence requires one small
correction.

Do not weaken its correctness, identity, atomicity, verification, evidence, or
anti-overengineering rules.

## Starting Protocol

Begin in the actual checkout.

Record:

```text
git rev-parse HEAD
git branch --show-current
git status --short
```

Preserve all unrelated work.

Do not reset.

Do not clean.

Do not discard changes you did not create.

Do not assume the observed target commit is still current.

If `HEAD` differs from `efcf802f77a4d69bba03ab466011db50176e12f1`, inspect the
intervening commits before changing code.

If the selected capability is already complete at the current `HEAD`, do not reimplement
it.

Instead, verify it against the acceptance contract in this prompt, repair only actual
defects, update stale documentation if needed, and stop.

If the current checkout has a conflicting uncommitted change in the selected files,
preserve it and integrate carefully.

Do not ask the user to choose internal implementation alternatives that code, tests,
specifications, and current consumers can decide.

## First Audit Commands

Use narrow searches before opening large files.

Recommended initial commands:

```bash
rg -n "CreateEnum|EnumFieldDraft|EnumVariantDraft|TypeParameterDraft" \
  crates/lkjscript-compiler/src/workspace
rg -n "DraftTypeParameterId|DeclarationType" \
  crates/lkjscript-compiler/src/workspace
rg -n "EnumTypeParameter|FunctionTypeParameter" \
  crates/lkjscript-compiler/src/workspace
rg -n "resolve_with_staged_type_parameters" \
  crates/lkjscript-compiler/src/workspace
rg -n "generic enum declaration|generic enum" \
  docs README.md crates/lkjscript-compiler/src/workspace/tests.rs
rg -n "Edit::CreateEnum" crates
```

Inspect these recent commits before reopening settled design:

```bash
git show --stat --oneline efcf802
git show --stat --oneline d014603
git show --stat --oneline 48da409
git show --stat --oneline d983ffc
git show --stat --oneline fdff9e2
git show --stat --oneline 09f6635
```

Read only the relevant parts of:

```text
AGENTS.md
docs/spec/workspace.md
docs/spec/language.md
docs/status.md
docs/architecture.md
docs/roadmap.md
docs/performance.md
```

Do not dump every file into context.

Search symbols first.

Open narrow ranges around owning definitions and focused tests.

## Observed Repository State

The following facts were confirmed at prompt preparation.

Revalidate them against the actual checkout.

The current product is an experimental Rust implementation of an AI-primary, statically
typed, memory-safe, collector-free language and semantic workspace.

The workspace supports both imported source and source-free construction.

`SemanticProgram` is the mutable transaction-staging representation.

`WorkspaceSnapshot` is the immutable published authority.

`compile_snapshot` is the supported semantic-snapshot compilation boundary.

Complete snapshots proceed through canonical HIR validation, ownership and memory
planning, verified SSA, validated bytecode, and the generic VM.

The baseline-native path is bounded specialization and is not the target of this task.

The workspace already exposes stable opaque semantic identities.

Those identities are namespace-scoped and generation-aware.

The workspace already supports transaction-local expression and pattern handles.

The workspace already supports transaction-local function type-parameter handles.

The workspace already supports source-free generic function declaration creation.

The workspace already supports source-free non-generic product declaration creation.

The workspace already supports source-free non-generic enum declaration creation.

The workspace already supports exact source-free values for existing generic enum
declarations.

The workspace already supports exact source-free patterns for existing generic enum
declarations.

The workspace already supports exact stable parameter-keyed generic enum type arguments.

The workspace already canonicalizes generic enum arguments to declaration order.

The workspace already preserves concrete instantiation for nullary generic enum
variants.

The workspace already exposes legal generic enum variant constructors for typed holes.

The workspace already compiles and executes source-free generic enum values and patterns
through the production VM route.

The current generic enum tests still begin from an imported generic enum declaration.

That import is the remaining source dependency in the generic enum authoring workflow.

The roadmap deliberately has no selected `Now` item.

The roadmap requires a current consumer or focused failure before selecting new work.

The existing generic enum value and pattern authoring tests provide that consumer.

They prove that the declaration is actively consumed.

They also prove that source import is the only missing step for fully source-free
generic enum authoring.

No open issue or pull request selected a different vertical at prompt preparation.

No attached GitHub status check selected a different failure at prompt preparation.

Do not create a competing task merely to populate the roadmap.

## Recent Completed Foundations

Treat these recent results as completed foundations unless current executable evidence
proves a regression:

- exact generic enum value authoring;
- exact generic enum pattern authoring;
- source-free capability entry points;
- identity-preserving nominal rename;
- source-free closed pattern construction;
- direct external contracts without a global registry;
- removal of redundant in-process native image contracts;
- identity-preserving sequence movement.

Do not broaden this change into movement semantics.

Do not broaden this change into capabilities.

Do not broaden this change into contract registries.

Do not broaden this change into native image contracts.

Do not rebuild the exact generic enum value or pattern API from scratch.

Reuse it as the end-to-end consumer of the new declaration path.

## Why This Vertical Is Justified

The operation is concrete.

An agent begins with an empty workspace.

The agent wants to define `maybe<T>`.

The agent wants `none` and `some(value: T)`.

The agent wants to construct `some<i64>(42)`.

The agent wants to match the value.

The agent wants the program to return `42`.

The existing semantic workspace can perform every step except creating `maybe<T>`
itself.

The existing `CreateEnum` path can create only a non-generic enum.

The existing generic enum value and pattern paths require a preexisting generic enum
declaration.

The generic enum declaration currently arrives through source import.

That contradicts the source-free construction objective for this current, already-tested
workflow.

The contradiction has a narrow producer-to-consumer closure:

```text
CreateEnum draft
    -> staged EnumDefinition with ordered binders
    -> stable enum/type-parameter/variant/field entities
    -> published WorkspaceSnapshot
    -> exact enum value and pattern drafts
    -> compile_snapshot
    -> VM execution
```

The compiler and runtime already understand generic enums.

The importer already defines accepted generic enum semantics.

The workspace index already understands enum type-parameter entities.

Deletion and compaction already understand enum type-parameter addresses.

The missing work is therefore a declaration-authoring seam.

It is not a request for a new generic runtime.

It is not a request for a second type system.

It is not a request for a generic nominal framework.

## Exact Product Result

After this change, an agent can perform the following sequence without source input:

1. create an empty deterministic workspace;
2. create `maybe<T>` with `none` and `some(value: T)`;
3. receive a published incomplete snapshot;
4. discover the stable enum entity;
5. discover the stable `T` type-parameter entity owned by that enum;
6. discover stable variant and field entities;
7. create a `main` whose result or body uses `maybe<i64>`;
8. fill a hole with `some<i64>(42)`;
9. author an exhaustive match over `some` and `none`;
10. compile the complete snapshot;
11. execute it on the generic VM;
12. observe `42`;
13. observe zero source loads and parser invocations after source-free
    construction begins.

The agent must not need to know:

- the compiler's dense enum index;
- the HIR `EnumId`;
- the HIR `VariantId`;
- a private ordinal as identity;
- source syntax;
- source spans;
- runtime tags;
- bytecode registers;
- native layout internals.

The agent submits declaration-local binder handles.

The workspace publishes stable semantic identities.

The existing exact value and pattern APIs consume those stable identities.

## Stop Condition

Stop when generic enum declarations can be created source-free and the existing generic
enum value and pattern authoring route consumes the newly created declaration end to
end.

Stop after:

- focused success tests pass;
- focused rejection tests pass;
- failure atomicity is proven;
- identity behavior is proven;
- imported/source-free semantic convergence is proven;
- production compilation succeeds;
- generic VM execution succeeds;
- documentation matches the checkout;
- required repository verification passes or exact environment failures are
  reported;
- one cohesive local commit is created when permitted;
- the worktree is inspected.

Do not implement an adjacent roadmap item after this stop condition.

## Explicit Non-Goals

The following are outside this vertical:

- generic product declarations;
- product type parameters;
- enum trait bounds;
- higher-kinded type parameters;
- variance;
- default type arguments;
- inferred declaration binders;
- implicit type arguments at the public workspace boundary;
- generic forwarding changes;
- type aliases;
- recursive source-free enum self-reference;
- mutually recursive declarations;
- adding enum members after creation;
- deleting individual enum members;
- reordering enum binders;
- renaming enum binders;
- changing enum binder bounds;
- generic nominal declaration frameworks;
- a unified product/enum/function declaration builder;
- a generic AST or tree editor;
- source syntax changes;
- parser changes unless a shared error contract must be corrected;
- package format changes;
- persistent workspace encoding;
- compatibility migration;
- daemonization;
- RPC;
- wire protocols;
- collaboration;
- scheduling;
- warm services;
- caches;
- generalized incremental recomputation;
- native/JIT feature expansion;
- public engine selection;
- broad repository reorganization;
- file-size or line-count refactors without a stable responsibility boundary.

If one of these becomes unavoidable, first prove why the selected operation cannot
remain honest without it.

Prefer stopping with an exact handoff over smuggling a second architecture into this
change.

## Confirmed Current Data Model

At the observed target commit, `crates/lkjscript-compiler/src/hir/enums.rs` contains an
`EnumDefinition` with these semantic facts:

```rust
pub struct EnumDefinition {
    pub id: EnumId,
    pub name: String,
    pub origin: Origin,
    pub type_parameters: Vec<String>,
    pub variants: Vec<EnumVariant>,
    pub layout: EnumLayoutFacts,
}
```

The HIR already stores ordered enum type-parameter names.

The HIR does not store enum trait bounds.

Do not invent enum bounds in the workspace API.

The importer already accepts an ordered `forall` parameter list for enum declarations.

The importer validates each enum parameter name.

The importer rejects duplicate enum parameter names.

The importer resolves variant field types in the enum binder scope.

The importer permits an enum parameter that does not occur in a field.

Therefore phantom enum binders are valid language semantics.

Do not copy the generic function rule that rejects an unused draft type parameter.

The importer rejects `Never` as an enum field type.

The importer rejects ownership and reference types stored in enums.

The importer computes stable enum, variant, field, and layout identities.

The importer determines whether the enum is directly recursive.

The source-free `CreateEnum` path currently creates only a non-generic `EnumDefinition`.

The source-free path currently installs:

```rust
type_parameters: Vec::new()
```

That is the seam to complete.

## Confirmed Current Workspace Identity Model

At the observed target commit, the workspace model already has:

```rust
EntityAddress::EnumTypeParameter {
    enumeration: u64,
    ordinal: u64,
}
```

The workspace index already publishes each non-builtin enum type parameter as:

```text
EntityKind::TypeParameter
owner = enum EntityId
address = EnumTypeParameter { enumeration, ordinal }
```

The index already adds enum-to-binder containment edges.

The index already keeps declaration order through the private ordinal.

The private ordinal is not the public identity.

The stable `EntityId` is the public identity.

The type conversion layer already maps internal `Type::Param(name)` values to stable
type-parameter entities in the declaration context.

The query layer already treats function and enum type parameters as
`EntityKind::TypeParameter`.

The generic enum authoring tests already find enum binders through entity kind and
owner.

Do not add another public enum-binder identity type.

Do not expose `EnumTypeParameter` addresses.

Do not expose raw ordinals as public identities.

## Confirmed Current Draft Model

At the observed target commit, `crates/lkjscript-compiler/src/workspace/draft.rs`
defines `DraftTypeParameterId`.

Its documentation currently describes a function declaration draft.

Its semantic role is a transaction-local declaration binder handle.

It does not enter a published snapshot.

It already supports exact, dense, caller-chosen local identity.

`DeclarationType` already distinguishes:

- a published stable type parameter through
  `DeclarationType::TypeParameter(EntityId)`;
- a same-declaration local binder through
  `DeclarationType::DraftTypeParameter(DraftTypeParameterId)`.

`DeclarationType` already supports:

- scalar types;
- capabilities;
- resources;
- products;
- enums with nested declaration types;
- lists;
- function types.

Its clone and conversion paths are iterative.

Do not add a second recursive declaration type for enums.

Do not add a string-based type parameter reference.

Do not add a positional-only binder placeholder.

Do not add an AST type expression.

Reuse `DraftTypeParameterId`.

Reuse `DeclarationType`.

Update their documentation so that it accurately covers declaration-local binders rather
than claiming they are function-only.

## Confirmed Current Transaction API

At the observed target commit, the public transaction API contains:

```rust
pub struct TypeParameterDraft {
    pub id: DraftTypeParameterId,
    pub name: String,
    pub bounds: Vec<SemanticTrait>,
}

pub struct EnumFieldDraft {
    pub name: String,
    pub ty: SemanticType,
}

pub struct EnumVariantDraft {
    pub name: String,
    pub fields: Vec<EnumFieldDraft>,
}
```

The observed `CreateEnum` shape is:

```rust
Edit::CreateEnum {
    name: String,
    variants: Vec<EnumVariantDraft>,
}
```

The observed `CreateFunction` shape includes:

```rust
type_parameters: Vec<TypeParameterDraft>
```

The function type-parameter draft includes bounds because function HIR and language
semantics include bounds.

Enum HIR does not include bounds.

Using `TypeParameterDraft` unchanged for enum parameters would expose a `bounds` field
with no honest enum meaning.

Do not accept and ignore that field.

Do not reject every non-empty bound while still advertising the field as an ordinary
enum parameter input.

Prefer an enum-specific boundless draft.

## Strong Default Representation Decision

Use this public shape unless the actual checkout proves a smaller equally honest
representation:

```rust
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct EnumTypeParameterDraft {
    pub id: DraftTypeParameterId,
    pub name: String,
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub struct EnumFieldDraft {
    pub name: String,
    pub ty: DeclarationType,
}

Edit::CreateEnum {
    name: String,
    type_parameters: Vec<EnumTypeParameterDraft>,
    variants: Vec<EnumVariantDraft>,
}
```

Keep `EnumVariantDraft` as the ordered variant container.

Keep `DraftTypeParameterId` as the local binder handle.

Use `DeclarationType` for enum fields so a field can refer to a binder declared by the
same edit.

A zero-length `type_parameters` list is the direct non-generic case.

Do not preserve the old `CreateEnum` shape through an overload, alias, default wrapper,
or compatibility edit variant.

Update every in-repository caller directly.

A bounded mechanical rename of the function-only `TypeParameterDraft` to
`FunctionTypeParameterDraft` is allowed only if the actual API becomes materially
clearer and the migration remains inside this vertical.

That rename is not required.

Do not perform it merely for symmetry.

Do not introduce a shared `GenericParameterDraft` with optional bounds.

Optional bounds would represent invalid states for enum parameters.

Do not introduce a generic nominal declaration builder.

Products and enums do not currently share the same generic semantics.

## Why the Strong Default Is Narrow

It reuses the existing local binder domain.

It reuses the existing declaration type.

It reuses the existing stable type-parameter entity domain.

It reuses the existing staged type resolver.

It reuses the existing enum HIR.

It reuses the existing enum index path.

It reuses the existing generic enum value and pattern consumers.

It adds no new compiler identity.

It adds no new runtime form.

It adds no new generic solver.

It adds no new trait system.

It adds no new query schema.

It adds no process boundary.

It adds no persistence boundary.

It avoids a false `bounds` field.

It avoids string-based rebinding.

It avoids a generic framework.

## Public API Semantics

`EnumTypeParameterDraft.id` identifies one binder only inside one `CreateEnum` edit.

The ID domain resets for each declaration edit.

The caller may submit dense IDs.

The caller need not submit them in numeric order.

Declaration order is the vector order of `type_parameters`.

The local ID is a key.

It is not the declaration-order ordinal.

The local ID must be unique within the declaration.

The binder name must be unique within the declaration.

The binder name must satisfy the same semantic naming contract as an imported generic
enum binder.

The binder name must not collide with a reserved builtin type, contextual form, or
operation when the importer rejects that name.

The binder may be phantom.

The binder has no bounds.

The binder becomes one stable published `EntityId`.

The published binder is owned by the published enum entity.

The published binder appears in enum containment.

The published binder appears in normal entity discovery.

The published binder survives unrelated edits and private compaction.

Deleting the enum tombstones the binder.

Recreating a same-name enum creates a fresh binder identity.

An old snapshot continues to resolve its old binder.

## Enum Field Draft Semantics

`EnumFieldDraft.ty` is a `DeclarationType`.

It may refer to:

- ordinary builtin types;
- a published product;
- a published enum with exact arguments;
- a published type parameter only when that parameter is visible in the
  declaration context;
- a same-edit enum binder through `DraftTypeParameter`;
- nested lists;
- nested function types when the language permits them as stored enum fields.

It may not contain:

- an unknown draft binder;
- a duplicate local binder declaration;
- a stale published identity;
- a foreign workspace identity;
- a wrong-kind identity;
- an invisible published type parameter;
- `Never`;
- a universal type;
- a stored ownership or reference type;
- a malformed generic enum instantiation.

The same-edit draft binder must resolve to the stable enum type-parameter entity before
publication.

The internal HIR field type must remain `Type::Param(name)` in the enum binder scope.

The public field type query must expose
`SemanticType::TypeParameter(stable_binder_entity)`.

Do not expose the local draft ID after publication.

## Error Model

Reuse existing structured draft-binder errors when their semantic facts match.

The current workspace already has structured variants for:

- unknown draft type parameter;
- duplicate draft type parameter;
- duplicate type-parameter name;
- duplicate type-parameter bound;
- unused draft type parameter.

The first three are relevant to enum declaration authoring.

The bound and unused variants are function-specific semantics.

Do not produce `UnusedDraftTypeParameter` for an enum.

Do not produce bound-related errors for an API that has no enum bound input.

If shared error display text currently says `function declaration`, make the wording
accurate for shared declaration use.

Prefer wording such as `declaration` when the structured variant itself already carries
the relevant IDs.

Do not add parallel enum-specific error variants merely to change prose.

Add an enum-specific structured variant only if a caller needs a fact that the existing
variant cannot represent.

Do not stringify and reparse errors.

Do not collapse stable, foreign, wrong-kind, wrong-owner, and invalid-type failures into
one opaque string.

## Canonical Naming Semantics

Audit the importer naming rules rather than copying only the function binder rules.

At minimum, preserve:

- identifier validity;
- builtin type exclusion;
- contextual name exclusion;
- operation-name exclusion;
- duplicate binder-name rejection.

Do not reject a legal imported enum binder in source-free creation.

Do not accept an enum binder that imported source would reject.

When a shared helper can express the same language contract, use it.

Do not create a workspace-only vocabulary table.

Do not create a second reserved-name registry.

If the importer owns a useful pure helper but its visibility is too narrow, move or
expose that helper at the smallest correct module boundary.

Do not duplicate the list.

## Generic Enum Arity

The created `EnumDefinition.type_parameters.len()` is the generic arity.

Every instantiated `SemanticType::Enum` must supply exactly that many arguments.

Existing generic enum value authoring already requires exact parameter-keyed arguments.

Existing nullary variants still require explicit concrete type arguments when the enum
has type parameters.

Do not infer missing public type arguments.

Do not erase phantom instantiations.

Do not use variant payload fields to infer the enum's generic arity.

Do not reduce arity when a binder is phantom.

## Phantom Binder Requirement

A declaration such as:

```text
enum maybe<t> {
    none,
    some(value: t)
}
```

uses `t`.

A declaration such as:

```text
enum marker<t> {
    marker
}
```

does not use `t`.

Both are valid if imported language semantics accept them.

The source-free API must accept both.

The exact instantiated type of `marker<i64>::marker` must remain `marker<i64>`.

A nullary variant must not lose its concrete type arguments.

Do not call the function-only used-binder collector as a mandatory enum validation step.

If a shared collector is reused, parameterize the unused rule explicitly and keep the
enum path permissive.

Prefer a smaller enum-specific validation loop over a configurable framework when the
shared framework would increase invalid states or search cost.

## Trait-Bound Requirement

Enum declaration binders have no trait-bound model in current HIR.

Do not add a `bounds` vector.

Do not add implicit `Copy`, `Clone`, `Drop`, `Send`, or `Sync` bounds.

Do not call function witness validation for enum declaration creation.

Instantiation restrictions already enforced by canonical generic enum value construction
remain in force.

For example, do not weaken existing rejection of unsupported ownership-bearing generic
arguments.

Declaration authoring and value instantiation are separate validation boundaries.

## Recursive Enum Requirement

Source-imported enums may be recursive.

The current source-free `CreateEnum` API cannot name the not-yet-published enum inside
its own field draft through a stable identity.

This task does not add a recursive self-reference handle.

Do not invent one.

Do not use the enum's name as a self-reference.

Do not reserve and leak a public enum identity to the caller before publication.

Do not add mutually recursive declaration staging.

Continue to compute correct layout facts for the declarations the API can honestly
represent.

For this vertical, a newly created enum may reference already-published enum types.

That does not make the new enum directly recursive.

The new enum's `recursive` layout fact remains false unless the actual representation
already supports an exact same-enum reference without violating publication semantics.

Record recursive source-free enum declaration as a remaining gap only if a current
consumer later proves it.

Do not add it to roadmap `Now` automatically.

## Required Staging Algorithm

Preserve the repository's existing transaction architecture.

Use a cloned or staged allocator.

Use a staged semantic program.

Perform all fallible work before publication.

A recommended algorithm follows.

Treat it as a semantic checklist, not a demand for identical helper names.

### Phase 1: Validate the Transaction Envelope

Check the base revision.

Check the workspace namespace.

Check overlapping edits according to current transaction rules.

Ensure `CreateEnum` is legal in the transaction ordering model.

Do not add a generalized edit planner.

Do not allow a later edit in the same transaction to guess the stable enum ID that
creation will allocate.

If same-transaction consumption is not already supported, keep the normal multi-revision
workflow:

```text
create declaration
    -> inspect published identities
    -> create consumers
```

That workflow is appropriate for an AI-primary semantic workspace.

### Phase 2: Validate the Enum Declaration Name

Validate the enum declaration name using the current nominal declaration rules.

Reject invalid declaration names.

Reject builtin type collisions.

Reject contextual form collisions.

Reject operation collisions.

Reject collisions with current nominal declarations according to the existing namespace
contract.

Do not add a compatibility suffix.

Do not silently rename.

Do not accept a no-op duplicate declaration.

### Phase 3: Validate the Binder Draft List

Allocate no published state.

Build a local set of draft binder IDs.

Build a local set of binder names.

For each binder in declaration order:

- validate its local ID;
- reject a repeated local ID;
- validate its name;
- reject a repeated name;
- preserve vector order;
- do not require numeric local-ID order;
- do not require use in a field;
- do not validate bounds because the API has no bounds.

Use checked allocation.

Return a structured error.

Do not mutate the current snapshot.

### Phase 4: Validate Variant and Field Shape

Require at least one variant if current source-free and imported semantics require it.

Validate each variant name.

Reject duplicate variant names.

Validate each field name.

Reject duplicate field names within a variant.

Preserve variant order.

Preserve field order.

Use the existing name contracts.

Do not infer fields from later values.

Do not sort declaration order by name.

### Phase 5: Compute Checked Entity Counts

Compute the number of new public entities with checked arithmetic.

The count includes:

- one enum entity;
- one entity for each enum type parameter;
- one entity for each variant;
- one entity for each field.

Do not omit binder entities from capacity planning.

Do not treat private HIR identities as public entities.

Do not preallocate a persistent identity table.

### Phase 6: Reserve the Stable Enum Entity

Reserve the enum's stable public entity in the staged allocator.

Reserve or derive the internal HIR `EnumId` through the existing nominal identity path.

Do not expose the internal ID.

Do not derive public continuity from the name alone.

Record one semantic `NewEntity` for the enum according to existing transaction
machinery.

### Phase 7: Reserve Stable Enum Binder Entities

For each binder in declaration order:

1. compute the private ordinal with checked conversion;
2. reserve a stable entity at
   `EntityAddress::EnumTypeParameter { enumeration, ordinal }`;
3. publish it as `EntityKind::TypeParameter` in the staged identity map;
4. assign the enum entity as owner;
5. preserve the binder name;
6. insert a mapping from local `DraftTypeParameterId` to stable `EntityId`;
7. insert the stable `EntityId` to binder-name mapping expected by the existing
   staged type resolver;
8. record a semantic new-entity diff entry through the existing mechanism.

Do not use the draft ID as the ordinal.

Do not use the binder name as identity.

Do not allocate binders after field type resolution.

Field type resolution needs the staged binder map.

### Phase 8: Convert Declaration Types

For each enum field:

1. convert `DeclarationType` into a stable-identity-bearing `SemanticType`;
2. replace each `DraftTypeParameter(local_id)` with
   `TypeParameter(stable_entity)`;
3. reject an unknown local ID;
4. preserve already-published semantic identities;
5. process nested type structure iteratively;
6. preserve exact enum type argument order;
7. keep all work in staging.

Reuse the existing `declaration_type_to_semantic` or equivalent conversion.

Do not add a recursive enum-only converter.

Do not map draft binders to names at the public boundary.

Use names only when lowering the stable semantic type into internal HIR `Type::Param`.

### Phase 9: Resolve Canonical Internal Field Types

Use the existing iterative staged type resolver.

The observed helper is `workspace::types::resolve_with_staged_type_parameters`.

It already accepts:

- the base snapshot;
- the staged semantic program;
- a public semantic type;
- an optional declaration context;
- a map from staged stable type-parameter entity to binder name;
- `allow_never`;
- `allow_forall`;
- a subject string.

Use the new enum entity as declaration context.

Use the staged binder map.

Set `allow_never` to false.

Set `allow_forall` to false.

Do not weaken stable identity validation.

Do not bypass enum arity validation.

Do not bypass product and enum kind validation.

Do not bypass type-parameter owner validation.

Do not duplicate this resolver.

If its current name or documentation claims it is function-only, make the smallest
honest generalization.

Do not turn it into a pluggable solver framework.

### Phase 10: Validate Stored Field Restrictions

Preserve imported/source-free parity for stored enum fields.

Reject `Never`.

Reject ownership and reference types that cannot be stored in enums.

Audit whether the current source-free helper checks nested ownership-bearing types as
thoroughly as the importer.

If a focused parity test exposes a mismatch, fix the shared or owning validation.

Do not broaden this task into a redesign of ownership types.

A field of `Type::Param("t")` is valid at declaration time.

Concrete instantiation restrictions remain enforced later.

Do not reject every generic field merely because some future argument would be invalid.

### Phase 11: Reserve Variant and Field Identities

Reuse the current stable nominal member identity logic.

Reserve each variant entity.

Reserve each field entity.

Preserve declaration order.

Record owners and containment.

Do not derive continuity from names.

Do not expose HIR member identities.

Set each field's internal type to the canonical resolved HIR type.

Compute field `indirect` using the existing enum-containing type logic.

Do not alter runtime layout conventions in this task.

### Phase 12: Build the Enum Definition

Create one semantic-origin `EnumDefinition`.

Populate:

- the stable internal enum ID;
- the declaration name;
- semantic origin;
- ordered type-parameter names;
- ordered variants;
- layout facts.

Do not store public `EntityId` values in HIR type-parameter names.

Do not store local draft IDs.

Do not store bounds.

Do not store an extra binder table when the existing ordered names are authoritative
internally.

The workspace index will reconstruct stable public binder entities from the forced
identity map and enum binder addresses.

### Phase 13: Run Canonical Recompute and Validation

Use the existing transaction recompute path.

Rebuild indexes through the current authority.

Run consistency validation.

Run ownership validation where the transaction boundary currently requires it.

Derive diagnostics and blockers.

Derive the semantic diff.

Do not publish a partially indexed enum.

Do not insert a special-case query cache.

Do not skip validation because the draft was typed.

### Phase 14: Publish Atomically

On success:

- publish one new immutable revision;
- publish the staged allocator;
- publish the staged program;
- publish the rebuilt indexes;
- publish deterministic diagnostics and blockers;
- publish the semantic diff.

On failure:

- publish nothing;
- preserve the original snapshot pointer;
- preserve the original revision;
- preserve the original allocator;
- preserve future identity allocation;
- preserve attachments;
- preserve source provenance;
- preserve old snapshots.

## Allocation and Retry Determinism

Add a focused allocator-control test.

Construct one invalid generic enum declaration.

Apply it to a workspace.

Confirm failure.

Confirm the current snapshot is pointer-identical to the pre-failure snapshot when
current tests use `Arc::ptr_eq` for this invariant.

Then apply a valid declaration.

In a clean control workspace cloned from the same pre-failure snapshot, apply the same
valid declaration.

Compare:

- semantic diff;
- new entity identities;
- enum entity;
- binder entities;
- variant entities;
- field entities;
- observable snapshot facts.

They must match.

The failed attempt must not consume a slot or generation.

Do not make identity allocation depend on hash-map iteration.

## Identity Continuity Requirements

Creating a generic enum allocates fresh identities for:

- the enum;
- each binder;
- each variant;
- each field.

An unrelated later edit preserves all of them.

Private vector compaction preserves all surviving public identities.

Deleting an earlier enum preserves identities of a later generic enum and all its
binders and members.

Deleting the generic enum tombstones:

- the enum;
- its binder entities;
- its variants;
- its fields.

Old snapshots retain the old identities and facts.

A same-name recreation receives new generations.

Do not treat a same-name binder in the recreated enum as the old binder.

Do not emit a semantic movement merely because private enum indexes shift.

## Deletion and Dependency Requirements

Audit existing deletion behavior for generic imported enums.

Reuse it for source-free generic enums.

A field type that references the enum's own binder is containment-internal.

Deleting the enum removes that contained binder.

A declaration that depends on the enum remains an independent dependent.

Do not delete independent dependents transitively unless the existing deletion contract
explicitly includes them in the same requested edit.

If deletion requires the caller to delete a dependent `main` in the same transaction,
preserve that behavior.

After deletion, queries using tombstoned enum or binder identities reject as stale.

An old snapshot still answers those queries.

## Compaction Requirements

The private `program.enums` vector may compact.

The private enum index in `EntityAddress::Enum` may relocate through reconciliation.

The public enum `EntityId` must not change.

The public binder `EntityId` must not change.

The public variant and field `EntityId` values must not change.

The internal HIR `EnumId` continuity rules remain the repository's current rules.

Do not expose private movement in the semantic diff.

Add a focused test only if current generic enum compaction coverage does not already
include a source-free declaration.

Prefer extending an existing compaction test over duplicating a large fixture.

## Query and Discovery Requirements

After `CreateEnum` succeeds, an agent must be able to discover:

- the enum entity;
- ordered binder entities or at least binder entities with stable owners and
  names;
- variants;
- fields;
- field declared types;
- containment;
- dependencies;
- revision;
- completeness state.

Use existing entity and owner queries where they are sufficient.

Do not add `EnumSignatureView` merely because `FunctionSignatureView` exists.

Add a dedicated enum query only if a focused consumer test proves that existing typed
queries cannot recover required facts without private compiler knowledge or parsing
presentation text.

A scan over the current compact entity list in a focused test is not by itself evidence
for a permanent new query.

If a query is added, it must be revision-labelled, deterministic, stable-order, and
typed.

Do not add serialization.

Do not add machine-readable CLI output.

## Type Query Requirements

The enum type-parameter entity has `EntityKind::TypeParameter`.

Its owner is the enum entity.

The field declared type for `some.value: T` must be observable as:

```rust
SemanticType::TypeParameter(type_parameter_entity)
```

An instantiated value of `maybe<i64>` must be observable as:

```rust
SemanticType::Enum {
    constructor: SemanticEnum::Entity(enum_entity),
    arguments: vec![SemanticType::I64],
}
```

Do not expose `Type::Param("t")` at the public boundary.

Do not expose the draft binder handle.

Do not expose a private ordinal.

## Dependency Requirements

The declaration dependency index must include real nominal dependencies.

A field that references an already-published product or enum creates the same dependency
as imported source.

A field that references its own enum binder must not create a false dependency on an
unrelated entity.

Containment of the binder under the enum is not an external dependency.

Do not add a parallel dependency graph for source-free declarations.

Use the existing index builder.

Add a focused parity assertion when useful.

## Projection Requirements

Projection must remain deterministic.

Projection text must not become the only way to discover enum binders.

The new source-free enum may appear in existing entity, type, and body projections
according to current conventions.

Do not add a new projection grammar solely for this task.

If current projection omits binder details, that is acceptable when typed entity queries
expose them and no current consumer requires formatted binder lists.

Update projection only when an existing source/import parity oracle requires it or
current documentation promises it.

## Completeness Requirements

Creating an enum declaration does not by itself complete an empty program.

The snapshot remains incomplete until a valid `main` exists and all blockers are
resolved.

The generic enum and its binder entities must still be queryable in the incomplete
snapshot.

Do not create placeholder `main`.

Do not fabricate a default enum value.

Do not suppress the missing-entry-point blocker.

The later source-free consumer can create and fill `main` through ordinary transactions.

## Source-Free Requirement

The core end-to-end test starts from:

```rust
Workspace::empty_deterministic(seed)
```

It must not call `import_source` for the program under test.

It must not render a source string.

It must not invoke the parser.

It must not load a source file.

Reset parser and source-load counters before construction.

Assert that both remain zero after:

- generic enum creation;
- identity discovery;
- main creation;
- hole filling;
- compilation;
- VM execution.

The test may create a separate imported oracle in a separate setup phase.

If it does, reset the counters after constructing the oracle and before the source-free
path.

Do not confuse oracle import with source-free construction.

## Compiler Boundary Requirement

Call the production `compile_snapshot`.

Do not invoke a test-only direct HIR compiler.

Do not manually lower the enum.

Do not fabricate bytecode.

A complete source-free generic enum program must pass:

- workspace consistency;
- complete-snapshot gate;
- HIR validation;
- ownership validation;
- memory planning;
- SSA verification;
- bytecode validation.

The returned executable must run on the generic VM.

## Runtime Requirement

Use the existing VM execution helper or production `run_chunk`.

The primary end-to-end example returns `42`.

Assert no cleanup failure.

Assert the returned type and payload are correct.

For a payload variant, assert the payload field contains `42` when the runtime test
helper exposes that fact.

For a match, assert the selected arm returns `42`.

Do not require baseline-native execution.

Do not alter native eligibility.

Do not add a native specialization for generic enums.

The VM is sufficient proof for this vertical.

## Effect and Ownership Requirement

Generic enum declaration creation is compile-time semantic authoring.

It does not add runtime effects.

Do not alter operation effect contracts.

Do not alter capability requirements.

Do not weaken ownership restrictions for enum fields or type arguments.

Existing exact enum value construction must continue to reject unsupported
ownership-bearing instantiations.

Existing cleanup behavior must remain exact.

## Stack-Safety Requirement

`DeclarationType` processing is user-shaped.

Keep clone, conversion, and resolution iterative.

Do not introduce recursive descent over nested declaration types.

If the new enum path adds a binder-usage traversal, do not use one for enums.

Phantom binders make such a traversal unnecessary.

If a new traversal is unavoidable for another exact invariant, implement it iteratively.

Use checked counts and checked host-index conversion.

Do not add an arbitrary type-depth limit.

Consider a focused small-stack test if this path was previously untested with a deep
enum field declaration type.

Do not add a huge stress test merely to increase coverage.

Reuse existing deep-type helpers when available.

## Performance Requirement

This task is not justified by a measured performance failure.

Do not add caches.

Do not add incremental recomputation.

Do not add parallel staging.

Do not add a warm service.

Do not add a new benchmark unless the type-shape change creates a concrete complexity
question.

The current full recompute path is acceptable for this vertical.

Avoid obvious accidental quadratic work in binder and field validation.

Use local hash sets or maps where current patterns already use them.

Preserve declaration order separately from uniqueness checks.

Do not sort where vector order is semantic.

If a focused regression is suspected, measure the smallest relevant transaction path
before and after.

Record a retained measurement only when it answers a stated question.

Do not update `docs/performance.md` with guesses.

## No Backward Compatibility

Change the public Rust API directly.

Update all repository callers.

Delete the old enum field type shape.

Do not add:

- `CreateGenericEnum`;
- `CreateEnumV2`;
- `GenericEnumFieldDraft`;
- deprecated constructors;
- conversion shims;
- dual edit variants;
- a defaulted hidden binder list;
- feature flags;
- edition gates.

A zero-length binder vector is the non-generic case.

That is the one API.

Tests for the old Rust shape are obsolete and must be updated.

Documentation for the old limitation must be removed.

No migration format is needed because the workspace has no supported persistent edit
encoding.

## Architecture Restraint Review

Before adding any new type besides the narrow boundless enum binder draft, answer
privately:

- Which current producer constructs it?
- Which current consumer reads it?
- Which invalid state does it remove?
- Why is a local helper insufficient?
- Does it create another authority?
- Does it create another identity domain?
- Does it require serialization?
- Does it increase agent search fan-out?
- Can it be deleted after this vertical?

The expected answer for a generic declaration framework is unfavorable.

Do not build it.

The expected answer for a dedicated `EnumTypeParameterDraft { id, name }` is favorable:

- `CreateEnum` produces and consumes it;
- it prevents dishonest bound fields;
- it reuses the existing draft identity domain;
- it has no serialization;
- it has no new public identity;
- it directly closes the current consumer gap.

## File and Symbol Inventory

Inspect these paths and symbols before editing.

### Root and Documentation

```text
AGENTS.md
README.md
docs/spec/workspace.md
docs/spec/language.md
docs/status.md
docs/architecture.md
docs/roadmap.md
docs/performance.md
```

### HIR and Importer

```text
crates/lkjscript-compiler/src/hir/enums.rs
crates/lkjscript-compiler/src/analyze/declarations/enum_collection.rs
crates/lkjscript-compiler/src/analyze/declarations/enum_fields.rs
crates/lkjscript-compiler/src/analyze/mod.rs
```

Inspect:

```text
EnumDefinition
collect_enum_names
collect_enums
collect_variant_fields
valid_enum_parameter
enum_layout
type_contains_enum
```

### Workspace Draft and Type Boundary

```text
crates/lkjscript-compiler/src/workspace/draft.rs
crates/lkjscript-compiler/src/workspace/types.rs
```

Inspect:

```text
DraftTypeParameterId
DeclarationType
TryFrom<&SemanticType> for DeclarationType
declaration_type_to_semantic or equivalent
resolve_with_staged_type_parameters
SemanticType::TypeParameter
```

### Workspace Transaction

```text
crates/lkjscript-compiler/src/workspace/transaction.rs
```

Inspect:

```text
TypeParameterDraft
ParameterDraft
ProductFieldDraft
EnumFieldDraft
EnumVariantDraft
Edit::CreateEnum
Edit::CreateFunction
Transaction
Workspace::apply
create_enum
create_function
collect_declaration_type_parameters
reserve_forced_entity
reject_ownership_field
type_contains_enum
reconcile
diff construction
deletion staging
```

Do not assume all helper names remain identical.

Search by semantics if the checkout moved.

### Workspace Model and Index

```text
crates/lkjscript-compiler/src/workspace/model.rs
crates/lkjscript-compiler/src/workspace/index.rs
crates/lkjscript-compiler/src/workspace/identity.rs
crates/lkjscript-compiler/src/workspace/compaction.rs
crates/lkjscript-compiler/src/workspace/validate.rs
crates/lkjscript-compiler/src/workspace/program.rs
```

Inspect:

```text
EntityKind::TypeParameter
EntityAddress::EnumTypeParameter
build_entities
enum_type_parameters
type_parameter_entities
declaration dependency indexing
identity reconciliation
enum deletion
private enum compaction
```

### Workspace Queries and Projection

```text
crates/lkjscript-compiler/src/workspace/query.rs
crates/lkjscript-compiler/src/workspace/projection.rs
```

Inspect:

```text
EntityHeader
TypeParameterView
entity_type
definition
entity_page
search_entities
legal_constructors
type view conversion
projection of types and declarations
```

### Tests and Measurement Callers

```text
crates/lkjscript-compiler/src/workspace/tests.rs
crates/lkjscript-compiler/src/workspace/recompute_measurement.rs
```

Inspect:

```text
canonical_workspace_observation
imported_nominal_local_and_ownership_programs_converge
generic_maybe_match_source
generic_maybe_none_source
generic_pair_value_source
exact_generic_enum_value_draft
exact_generic_maybe_match_draft
exact_generic_enum_values_and_patterns_compile_execute_and_converge
exact_nullary_generic_enum_value_retains_its_concrete_instantiation
generic_enum_arguments_and_fields_canonicalize_to_declaration_order
generic_enum_authoring_failures_are_atomic_and_retry_stable
generic_enum_holes_advertise_exact_source_free_constructors
```

Use these existing tests as consumers and oracles.

Do not duplicate their large source fixtures without reason.

## Required Focused Tests

Prefer extending the existing workspace test module.

Use existing helper patterns.

Keep fixtures readable enough to diagnose semantics.

Do not build a new test framework.

### Test 1: Fully Source-Free Generic Enum Program

Start from `Workspace::empty_deterministic`.

Reset source-load and parser counters.

Create:

```text
maybe<t>
    none
    some(value: t)
```

Use one `CreateEnum` edit with:

- one enum binder draft;
- one nullary variant;
- one payload variant;
- one field whose type is
  `DeclarationType::DraftTypeParameter(local_t)`.

After publication, discover:

- `maybe`;
- stable `t`;
- `none`;
- `some`;
- `value`.

Assert:

- the enum is `EntityKind::Enum`;
- `t` is `EntityKind::TypeParameter`;
- `t.owner == Some(maybe)`;
- `none.owner == Some(maybe)`;
- `some.owner == Some(maybe)`;
- `value.owner == Some(some)`;
- field type is `SemanticType::TypeParameter(t)`;
- containment includes enum-to-binder;
- the snapshot is incomplete only for legitimate remaining blockers.

Create `main`.

Use existing exact generic enum value and match drafts.

Construct `some<i64>(42)`.

Match:

```text
some(x) -> x
none -> 0
```

Compile through `compile_snapshot`.

Run through the VM.

Assert return value `42`.

Assert no cleanup failure.

Assert parser invocation count remains zero.

Assert source-load invocation count remains zero.

This is the primary acceptance test.

### Test 2: Imported and Source-Free Declaration Convergence

Create an imported oracle for the same program.

Create the source-free program from an empty deterministic workspace.

Use `canonical_workspace_observation` or the current semantic-equivalence helper.

Compare semantic facts rather than private identities.

The comparison should include the relevant subset of:

- declaration paths;
- entity kinds;
- owner paths;
- declared types;
- node kinds;
- actual types;
- expected types;
- containment;
- references;
- calls;
- dependencies;
- effects;
- diagnostics;
- observable runtime result.

If the existing canonical observation omits enum binder facts, extend it only as needed
to compare them honestly.

Do not make private HIR IDs part of the oracle.

Do not require source and semantic origins to be equal when the existing oracle
intentionally ignores origin.

Assert both programs execute identically.

### Test 3: Multiple Binders and Declaration Order

Create:

```text
pair<a, b>
    both(first: a, second: b)
```

Use local draft IDs submitted in an order that differs from numeric ID order.

For example:

```text
type_parameters vector order = [a(id=9), b(id=2)]
```

Field types refer to the matching local handles.

After publication, assert:

- public binder order follows vector declaration order;
- `a` and `b` have distinct stable identities;
- `first` uses stable `a`;
- `second` uses stable `b`;
- generic arguments are keyed by stable parameter identity;
- a caller may submit type arguments out of order;
- canonical stored instantiation follows declaration order;
- a value `pair<i64, bool>` carries fields in declaration order;
- VM observation is correct.

Do not sort binder declarations by local ID.

Do not sort them by name.

### Test 4: Phantom Binder and Nullary Variant

Create:

```text
marker<t>
    marker
```

Do not reference `t` in any field.

Creation must succeed.

Construct the nullary variant with an explicit `t = i64` type argument.

Assert the exact value type is `marker<i64>`.

Assert the concrete instantiation survives node semantics, compilation, and runtime
representation as far as current APIs expose it.

Do not produce `UnusedDraftTypeParameter`.

### Test 5: Duplicate Local Binder ID

Submit two enum binder drafts with the same `DraftTypeParameterId`.

Use different names.

Creation must reject with the existing structured duplicate-draft-ID error or a strictly
better equivalent.

The transaction must remain atomic.

### Test 6: Duplicate Binder Name

Submit two distinct local IDs with the same binder name.

Creation must reject with the existing structured duplicate-name error or a strictly
better equivalent.

The transaction must remain atomic.

### Test 7: Unknown Draft Binder Reference

Declare one binder.

Use a different undeclared draft handle in a field type.

Creation must reject with `WorkspaceError::UnknownDraftTypeParameter` or the current
precise equivalent.

The error must identify the local handle.

The transaction must remain atomic.

### Test 8: Invalid or Reserved Binder Names

Use a compact table of representative names.

Include cases already rejected by imported generic enum semantics.

At minimum cover:

- an invalid identifier;
- a builtin type name;
- a contextual name;
- an operation name when current semantics reserve it.

Do not duplicate a giant vocabulary matrix.

Use the shared naming contract.

### Test 9: Invalid Field Types

Use focused cases for:

- `Never`;
- direct `ByteVector` when enum storage forbids it;
- direct reference or slice ownership type when forbidden;
- `Forall`;
- malformed generic enum arity;
- stale product or enum identity;
- wrong-kind identity.

Test only cases relevant to this changed boundary.

Reuse existing structured errors.

Do not weaken imported semantics.

### Test 10: Foreign and Wrong-Owner Type Parameters

A field draft may contain `DeclarationType::TypeParameter(existing_entity)`.

Submit:

- a type parameter from another workspace;
- a type parameter owned by a function;
- a type parameter owned by another enum;
- a stale type-parameter identity;
- a non-type-parameter entity.

Each must reject through the canonical namespace, owner, liveness, or kind check.

Do not silently capture an external binder by name.

The same-edit binder must use `DraftTypeParameter`.

### Test 11: Failure Atomicity and Retry Stability

Choose one invalid declaration that reaches staging after some identities would have
been reserved in the staged allocator.

Apply it.

Assert failure.

Assert current snapshot pointer and revision remain unchanged.

Then apply a valid generic enum declaration.

Apply the same valid declaration to a clean control workspace from the same base
snapshot.

Assert identical:

- diff;
- enum identity;
- binder identities;
- variant identities;
- field identities;
- semantic facts.

This proves allocator rollback.

### Test 12: Old Snapshot and Tombstones

Create a generic enum.

Retain the pre-deletion snapshot.

Delete the enum according to current dependency rules.

Assert current snapshot rejects the enum and binder identities as stale.

Assert the old snapshot still returns:

- enum entity;
- binder entity;
- owner relation;
- field type;
- variant and field entities.

Recreate an enum with the same name.

Assert the new enum and binder have fresh generations.

### Test 13: Private Compaction

Create an earlier nominal declaration.

Create the generic enum after it.

Record all stable generic enum identities.

Delete the earlier declaration.

Assert:

- generic enum survives;
- binder identities survive;
- variant identities survive;
- field identities survive;
- private dense positions may compact;
- semantic diff does not claim movement;
- generic value construction still works;
- compiled execution still works when a complete program is present.

Extend an existing compaction test if possible.

### Test 14: Legal Constructors from a Newly Created Generic Enum

After source-free declaration creation, create a hole whose expected type is
`maybe<i64>`.

Query legal constructors.

Assert the new `none` and `some` variant entities are advertised.

Fill the hole through the existing exact enum value draft.

Assert canonical validation accepts it.

This proves that the query and transaction paths agree.

### Test 15: Deterministic Projection and Query Facts

Repeat the relevant entity, type, constructor, or projection query at the same revision.

Assert identical output.

If pagination is involved, assert stable continuation behavior.

Do not create pagination only for this test.

Use current query contracts.

### Test 16: Deep Declaration Type Path

Add this only if the changed implementation introduces or materially extends a
user-depth traversal.

Construct a moderately deep nested declaration type on a small-stack thread.

Create the generic enum.

Query the resulting field type.

Delete the declaration.

Drop all snapshots.

Assert the thread completes.

Use an existing depth helper when possible.

Do not impose a semantic depth limit.

Do not add a 20,000-node default test.

## Existing Test Preservation

The following existing semantics must continue to pass:

- generic enum value construction;
- generic enum pattern construction;
- nullary generic enum instantiation;
- generic enum argument canonicalization;
- generic enum value failure atomicity;
- generic enum pattern failure atomicity;
- legal generic enum constructors;
- generic function creation;
- non-generic enum creation;
- non-generic product creation;
- nominal rename;
- declaration deletion;
- private compaction;
- source-free compile and VM execution;
- parser/source-load counters;
- ownership and cleanup validation.

Do not weaken or delete those tests because the API changed.

Update only their construction syntax where `EnumFieldDraft.ty` changes from
`SemanticType` to `DeclarationType`.

Delete tests only when they protect the obsolete Rust API shape rather than semantic
behavior.

## Mechanical Migration

Search every `Edit::CreateEnum` caller.

Search every direct `EnumFieldDraft` constructor.

Convert non-generic field types directly:

```rust
SemanticType::I64
```

becomes:

```rust
DeclarationType::I64
```

Convert published nominal references through the corresponding `DeclarationType`
variants.

Do not add `.into()` chains merely to conceal the new boundary unless the conversion is
already canonical and unambiguous.

Prefer explicit draft types in tests.

Update recompute measurement fixtures mechanically.

Do not change benchmark semantics.

Run one compiler check after the representation migration to find missed callers.

Fix warnings immediately.

## Suggested Implementation Order

Use this order unless current checkout evidence selects a smaller sequence.

### Step 1: Characterize the Gap

Add or temporarily sketch the smallest failing test that attempts generic enum
declaration creation.

Do not commit a permanently ignored test.

Confirm current API cannot represent the field binder.

### Step 2: Change the Draft Boundary

Add the boundless enum binder draft.

Change enum field types to `DeclarationType`.

Add `type_parameters` to `CreateEnum`.

Update public exports.

Update accurate documentation comments.

Do not implement a compatibility layer.

### Step 3: Use the Compiler as a Migration Inventory

Run:

```bash
cargo check --quiet -p lkjscript-compiler --all-targets --all-features --locked
```

Update all direct callers.

Do not run the full workspace yet.

### Step 4: Implement Enum Binder Staging

Validate binder IDs and names.

Reserve stable binder entities.

Build local-to-stable and stable-to-name maps.

Resolve field declaration types.

Populate ordered HIR type-parameter names.

Preserve atomicity.

### Step 5: Add the Primary Source-Free Test

Create `maybe<T>` from an empty workspace.

Construct and match `some<i64>(42)`.

Compile and run.

Assert zero parser and source loading.

### Step 6: Add Rejection and Retry Tests

Cover duplicate ID, duplicate name, unknown handle, and one canonical wrong-owner or
invalid-type case.

Prove allocator rollback.

Do not create dozens of redundant tests.

### Step 7: Add Identity and Phantom Coverage

Prove phantom binder support.

Prove deletion or compaction continuity where current coverage is insufficient.

### Step 8: Run Focused Verification

Run the exact affected tests.

Then run the compiler crate tests.

Format after the representation stabilizes.

### Step 9: Update Documentation

Update normative workspace semantics.

Update implemented status.

Update architecture only if responsibility or flow changed.

Leave the roadmap without speculative `Now` work.

Update performance documentation only with retained measurements.

### Step 10: Run Full Verification Once

Run the required commands.

Capture long output once.

Fix causal failures.

Do not rerun unchanged commands.

### Step 11: Inspect and Commit

Inspect status and diff.

Remove prompt copies, logs, caches, and build artifacts.

Create one cohesive local commit when permitted.

Suggested subject:

```text
Complete source-free generic enum declarations
```

Do not push.

## Focused Commands

Adapt exact test names to the implementation.

Recommended iteration commands:

```bash
cargo test --quiet -p lkjscript-compiler \
  source_free_generic_enum_declarations_compile_execute_and_converge \
  --all-features --locked
```

```bash
cargo test --quiet -p lkjscript-compiler \
  generic_enum_declaration_failures_are_atomic_and_retry_stable \
  --all-features --locked
```

```bash
cargo test --quiet -p lkjscript-compiler \
  phantom_generic_enum_binder_retains_exact_instantiation \
  --all-features --locked
```

```bash
cargo test --quiet -p lkjscript-compiler \
  --all-targets --all-features --locked
```

Use substring filters only when they select the intended tests.

Do not claim a focused command passed when the filter ran zero tests.

Inspect the test count.

## Required Full Verification

Before completion, run:

```bash
cargo fmt --all -- --check
cargo clippy --quiet --workspace --all-targets --all-features --locked -- -D warnings
cargo test --quiet --workspace --all-targets --all-features --locked
cargo build --quiet --workspace --release --locked
docker compose -f meta/docker-compose.yml --profile verify run --build --rm verify
git diff --check
```

If formatting is needed, run:

```bash
cargo fmt --all
```

Then rerun the formatting check.

Do not omit `--locked`.

Do not omit `--all-targets`.

Do not omit `--all-features`.

Do not omit the release build.

Run Docker verification once after local verification succeeds.

If Docker is unavailable, report the exact command and exact environment failure.

Do not replace Docker verification with a claim that local tests are equivalent.

## Documentation Changes

### `docs/spec/workspace.md`

Update the normative creation contract.

State that user-defined generic enum declarations can be created source-free.

State that enum binders use transaction-local draft handles during creation.

State that publication yields stable type-parameter entity identities owned by the enum.

State that enum field drafts use declaration types.

State that enum binders have no trait bounds.

State that phantom enum binders are valid.

State that a zero-binder enum is the non-generic case.

State failure atomicity and allocator rollback.

Retain the current unsupported boundaries:

- recursive same-edit self-reference;
- generic products;
- enum member mutation;
- persistence and remote transport.

Remove the stale claim that generic enum declaration creation is unsupported.

Do not document a future design as current.

### `docs/status.md`

Record implemented facts after tests pass.

State the exact source-free end-to-end generic enum declaration capability.

State production compilation and VM execution evidence.

State parser/source-load avoidance.

State identity and atomicity behavior.

Remove the corresponding current gap.

Do not claim recursive generic declaration support.

Do not claim enum bounds.

Do not claim a daemon or persistence.

### `docs/architecture.md`

Update only if the creation-time binder flow is not already described generally.

A concise flow may state:

```text
declaration-local enum binder handles
    -> staged stable type-parameter entities
    -> canonical enum field type resolution
    -> published immutable snapshot
```

Do not add a new subsystem diagram for one helper.

### `docs/roadmap.md`

Do not populate `Now` merely because this task completes.

Remove a roadmap mention of the completed gap if present.

Leave future work behind evidence gates.

Record recursive source-free enum creation only when a current consumer exists.

Do not select generic products from symmetry.

### `README.md`

Change only if it currently states the old source-free limitation at the product-summary
level.

Do not expand the README into a design document.

### `docs/performance.md`

Do not change unless a focused retained measurement was made.

No speculative performance claim.

## API-Cost Discipline

The coding-agent model's API cost matters.

Reduce it through disciplined work.

Do not reduce it through skipped validation.

Use these rules:

- search exact symbols before reading files;
- read narrow ranges;
- inspect the recent commit diff that introduced generic enum value and pattern
  support;
- reuse existing fixtures and canonical observation helpers;
- keep one compact ledger;
- avoid copying large source fixtures into notes;
- use compiler diagnostics as the mechanical migration inventory;
- run one focused test at a time during implementation;
- run full workspace verification only after focused tests pass;
- run Docker verification once;
- capture long logs once;
- inspect the causal error region;
- do not rerun an unchanged failure;
- do not ask subagents to design competing APIs;
- do not create a broad repository summary;
- do not reread all documentation after every edit;
- do not add instrumentation for API cost;
- do not create a permanent prompt archive;
- stop after the selected vertical.

Use a subagent only for an independent bounded question.

Examples of acceptable bounded questions:

- compare importer enum binder naming rules with function binder naming rules;
- inventory all `CreateEnum` callers;
- verify deletion already tombstones enum type parameters.

Give the subagent exact symbols and a stop condition.

Do not delegate overlapping implementation.

The lead agent owns the representation decision and final verification.

## Designs to Reject

Reject a design that adds `CreateGenericEnum`.

Reject a design that retains old and new enum creation APIs.

Reject a design that accepts enum bounds and ignores them.

Reject a design that accepts enum bounds only to reject all non-empty vectors.

Reject a design that models a binder by a string inside field types.

Reject a design that uses vector position as the caller's only binder reference.

Reject a design that exposes `EntityAddress::EnumTypeParameter`.

Reject a design that publishes stable IDs before transaction success.

Reject a design that reparses generated source.

Reject a design that creates synthetic source for compilation.

Reject a design that adds a generic nominal declaration trait.

Reject a design that makes products generic in the same change.

Reject a design that adds recursive self-reference by name.

Reject a design that adds a placeholder enum entity to the current snapshot during
staging.

Reject a design that adds a second type resolver.

Reject a design that weakens enum type-argument validation.

Reject a design that rejects phantom binders.

Reject a design that adds a dedicated enum query without a consumer.

Reject a design that adds persistence because draft handles are local.

Reject a design that serializes transactions.

Reject a design that adds a workspace daemon.

Reject a design that adds a cache to compensate for full recompute.

Reject a design that reorganizes all workspace modules.

Reject a design that splits files only because they are long.

Reject a design that adds line-count limits.

Reject a design that updates performance claims without measurement.

## Multi-Turn Behavior

This prompt is intentionally one vertical.

It does not require deciding the entire future language architecture.

If implementation exposes a genuinely broader contradiction, do not solve every adjacent
problem in this turn.

First determine whether the contradiction blocks the exact source-free generic enum
declaration story.

If it does not block the story, record it as a remaining gap and stop after the selected
vertical.

If it does block the story and requires a cross-cutting representation change, produce a
focused audit before expanding.

The audit must state:

- the exact failing operation;
- the exact current consumer;
- the authoritative invariant;
- why the strong default representation cannot satisfy it;
- the minimum alternative;
- identities affected;
- atomicity affected;
- tests required;
- explicit new non-goals.

Do not ask the user to choose among internal designs when evidence is sufficient.

Do not create scaffolding for the next turn.

At the end, provide an exact handoff.

A good handoff says:

```text
The source-free generic enum declaration vertical is complete.
The next unresolved gap observed was X.
It was not implemented because no current consumer or focused failure proved it.
The smallest future characterization would be Y.
```

A bad handoff says:

```text
Continue improving generics, persistence, performance, and the daemon.
```

## Completion Checklist

Do not mark complete until every applicable item is true.

### Product

- [ ] An empty workspace can create a generic enum declaration.
- [ ] The enum may have one or more ordered binders.
- [ ] The enum may have a phantom binder.
- [ ] Variant fields may refer to same-edit binders.
- [ ] Publication yields stable binder entities.
- [ ] Existing exact generic enum values consume the new declaration.
- [ ] Existing exact generic enum patterns consume the new declaration.
- [ ] A complete source-free program compiles.
- [ ] The generic VM executes it.
- [ ] The primary example returns `42`.
- [ ] Source loading remains zero.
- [ ] Parser invocation remains zero.

### Semantic Correctness

- [ ] Binder vector order is declaration order.
- [ ] Draft binder ID is not declaration order.
- [ ] Duplicate local IDs reject.
- [ ] Duplicate names reject.
- [ ] Unknown local references reject.
- [ ] Invalid names reject.
- [ ] Phantom binders do not reject.
- [ ] Enum binders have no bounds.
- [ ] `Never` fields reject.
- [ ] Unsupported ownership fields reject.
- [ ] Wrong generic arity rejects.
- [ ] Foreign identities reject.
- [ ] Stale identities reject.
- [ ] Wrong kinds reject.
- [ ] Wrong owners reject.
- [ ] Field queries expose stable binder identities.
- [ ] Nullary variants retain concrete instantiation.

### Identity

- [ ] Enum identity is stable after unrelated edits.
- [ ] Binder identities are stable after unrelated edits.
- [ ] Variant identities are stable after unrelated edits.
- [ ] Field identities are stable after unrelated edits.
- [ ] Private compaction preserves survivors.
- [ ] Deletion tombstones contained identities.
- [ ] Old snapshots remain valid.
- [ ] Same-name recreation uses fresh generations.
- [ ] Diffs omit private movement.

### Atomicity

- [ ] Failure publishes nothing.
- [ ] Failure preserves the snapshot pointer.
- [ ] Failure preserves revision.
- [ ] Failure preserves allocator state.
- [ ] Failure preserves future deterministic allocation.
- [ ] Valid retry matches a clean control run.

### Architecture

- [ ] No second semantic authority exists.
- [ ] No second binder identity domain exists.
- [ ] No second type resolver exists.
- [ ] No generic nominal framework exists.
- [ ] No compatibility layer exists.
- [ ] No parser round-trip exists.
- [ ] No persistence exists.
- [ ] No daemon or RPC exists.
- [ ] No cache or generalized incrementality exists.
- [ ] No native-route expansion exists.
- [ ] No unrelated reorganization exists.

### Tests

- [ ] Primary source-free success test passes.
- [ ] Imported/source-free convergence passes.
- [ ] Multiple-binder ordering passes.
- [ ] Phantom/nullary case passes.
- [ ] Duplicate-ID rejection passes.
- [ ] Duplicate-name rejection passes.
- [ ] Unknown-handle rejection passes.
- [ ] Wrong-owner or foreign identity rejection passes.
- [ ] Atomic retry stability passes.
- [ ] Identity deletion or compaction evidence passes.
- [ ] Existing generic enum tests pass.
- [ ] Compiler crate tests pass.

### Documentation

- [ ] Workspace spec reflects the new API and semantics.
- [ ] Status reflects implemented facts.
- [ ] Architecture reflects the binder flow only if needed.
- [ ] Roadmap does not retain the completed gap.
- [ ] Roadmap does not invent speculative `Now` work.
- [ ] README is accurate.
- [ ] Performance documentation contains no unsupported claim.

### Verification

- [ ] Formatting check passed.
- [ ] Clippy passed with warnings denied.
- [ ] Workspace tests passed.
- [ ] Locked release build passed.
- [ ] Docker verification passed or exact environment failure is reported.
- [ ] `git diff --check` passed.
- [ ] Final status was inspected.
- [ ] No prompt or log artifact is staged.
- [ ] One cohesive local commit exists when permitted.
- [ ] No remote state changed.

## Final Report Format

Use this exact structure:

```text
Starting commit:
Ending commit:
Selected vertical:
Current consumer:
Observed initial gap:
Semantic result:
Public API decision:
HIR decision:
Draft binder decision:
Phantom binder behavior:
Compatibility breaks:
Identity behavior:
Atomicity behavior:
Deletion and compaction behavior:
Query behavior:
Compiler/runtime evidence:
Parser/source-load evidence:
Focused tests:
Focused commands:
Full verification:
Docker verification:
Measurements:
Documentation:
Deliberate non-goals:
Remaining evidenced gaps:
Worktree state:
Remote state:
```

Under `Public API decision`, state the final `CreateEnum` and field draft shapes.

Under `Compatibility breaks`, state that old Rust callers were migrated directly and no
compatibility layer was retained.

Under `Remaining evidenced gaps`, distinguish:

- an actual observed blocker;
- an unselected future possibility;
- no known blocker.

Do not claim unrun verification.

Do not claim a performance improvement without measurement.

Do not paste hidden reasoning.

Do not paste full logs.

Do not include this prompt in the commit.

## Final Instruction

Make the smallest clean change that turns the existing imported generic enum declaration
consumer into a fully source-free generic enum declaration consumer.

Use the existing semantic workspace.

Use the existing local binder handle domain.

Use the existing stable type-parameter entity domain.

Use the existing declaration type.

Use the existing staged type resolver.

Use the existing enum HIR.

Use the existing generic enum value and pattern authoring.

Use the existing compiler and VM.

Preserve failure atomicity.

Preserve stable identity.

Allow phantom binders.

Do not add enum bounds.

Do not build a generic framework.

Do not build future infrastructure.

Complete the vertical.

Verify it.

Commit it locally when permitted.

Stop.
