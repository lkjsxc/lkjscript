# Authentic v0.1.36 transaction predecessor

The copied product executable from `3fc9ff6c47022d2f6fee7edfcda1f63003c9338c`
(SHA-256 `f5e03f57d23a0ceb0b09eb4e6386f1be967dfcb0ee144e1076095f2fd1023a60`)
authored, reviewed, checked, exported, built and ran these original literal typed-cell requests.
That source contains the frozen v0.1.36 capability from
`eea968b910ac62c3feab95a8896fff1a91904a17`; no transaction-outcome implementation was present.
The retained generations are Graph 15, compiler 11, bytecode 7 and artifact 19.
`provenance.json` records exact identities and hashes of the original material.

The producer's ordinary `UpdateAttempt<T>` and legacy `transaction` retain their meaning:
I64 direct/bound updates return candidate 13, Text returns `a!`, and an auxiliary failed
expectation returns candidate 16 with the primary condition true while publishing nothing.
The source fixture includes accepted history, dependency selections and a derived compiler cache.
The original source packs, package transports and artifact must remain byte-for-byte unchanged.

The existing requirement-library proof owner loads and checks the old source, rebuilds derived
output, imports the exact old transport into a new target package, and runs original, rebuilt and
imported artifacts against fresh disposable data. It checks independent stored scalar bytes and
unchanged HEAD after suppression, and makes a reviewed identity-preserving source edit.
This is compatibility material, not a new application or a receipt for current execution.
