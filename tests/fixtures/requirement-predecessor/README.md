# Authentic requirement-campaign predecessor

These project, transport and artifact bytes were authored through the copied frozen executable
from `4306ef64783462a8dda48f2a855a88482d1faec8` (v0.1.35 candidate). Its SHA-256 is
`9146533e377797ba95e736f980005cda55783549acfcdf448c7e9af74230c4e1`.
`accepted.request` is the literal accepted public request, applied to its own copied command
starter. No graph storage was patched. `provenance.json` authenticates every retained input.
The fixture includes accepted history, exact dependency selection and a predecessor derived
compiler cache. The unsupported Graph 13 baseline is not substituted for this Graph 14 input.

The old executable checked, built, exported and executed both scalar encoders. Their exact typed
data results are retained in `expected.json`; the current candidate independently loaded and ran
the original artifact with those same bytes. An initial descriptor used an absolute artifact
path and was rejected before execution; a separate corrected relative descriptor produced the
retained values. The original failure remains in campaign artifacts.

The ordinary offline-package owner embeds these exact inputs, checks accepted HEAD and all old
pack bytes, rebuilds derived output, imports the old transport, runs original/rebuilt/imported
artifacts and makes a reviewed public rename. New derived packs are allowed; inspection or
revalidation cannot rewrite accepted source or historical bytes. Later wrappers and edited roots
may have successor identities. The finite-callable predecessor owner separately retains invalid
historical inspection, complete repair and original accepted retry identities.
